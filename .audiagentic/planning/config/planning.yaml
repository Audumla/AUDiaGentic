planning:
  convention_version: 1
  default_profile: feature
  default_guidance: standard
  
  # Config-driven kind definitions - add new kinds here without code changes
  kinds:
    request:
      dir: requests
      id_prefix: request
      counter_file: requests.json
      has_domain: false
      required_refs: []
      standard_refs_inheritance: []
      required_fields: [id, label, state, summary]
      optional_fields: [profile, source, context, current_understanding, open_questions, guidance, request_refs, meta, deleted, deleted_at, deletion_reason, archived_at, archived_by, archive_reason, restored_at, restored_by]
      
    spec:
      dir: specifications
      id_prefix: spec
      counter_file: specs.json
      has_domain: false
      required_refs: []
      standard_refs_inheritance: []
      required_fields: [id, label, state, summary, request_refs]
      optional_fields: [workflow, standard_refs, meta, deleted, deleted_at, deletion_reason, archived_at, archived_by, archive_reason, restored_at, restored_by]
      
    plan:
      dir: plans
      id_prefix: plan
      counter_file: plans.json
      has_domain: false
      required_refs: []
      standard_refs_inheritance: []
      required_fields: [id, label, state, summary]
      optional_fields: [workflow, standard_refs, meta, deleted, deleted_at, deletion_reason, archived_at, archived_by, archive_reason, restored_at, restored_by]
      
    task:
      dir: tasks
      id_prefix: task
      counter_file: tasks.json
      has_domain: true
      required_refs: [spec]
      standard_refs_inheritance:
        - field: spec_ref
          type: direct
        - field: parent_task_ref
          type: direct
      required_fields: [id, label, state, summary]
      optional_fields: [workflow, standard_refs, meta, deleted, deleted_at, deletion_reason, archived_at, archived_by, archive_reason, restored_at, restored_by, spec_ref, parent_task_ref, target, packet_ref, request_refs]
      
    wp:
      dir: work-packages
      id_prefix: wp
      counter_file: wps.json
      has_domain: true
      required_refs: [plan]
      standard_refs_inheritance:
        - field: plan_ref
          type: direct
        - field: task_refs
          type: recursive
          sub_inheritance:
            - field: standard_refs
            - field: spec_ref
              type: direct
              sub_inheritance:
                - field: standard_refs
      required_fields: [id, label, state, summary, plan_ref]
      optional_fields: [workflow, standard_refs, meta, deleted, deleted_at, deletion_reason, archived_at, archived_by, archive_reason, restored_at, restored_by, task_refs]
      
    standard:
      dir: standards
      id_prefix: standard
      counter_file: standards.json
      has_domain: false
      required_refs: []
      standard_refs_inheritance: []
      required_fields: [id, label, state, summary]
      optional_fields: [meta, deleted, deleted_at, deletion_reason, archived_at, archived_by, archive_reason, restored_at, restored_by]
      
  dirs:
    base: docs/planning
    attachments: docs/planning/attachments
    indexes: .audiagentic/planning/indexes
    meta: .audiagentic/planning/meta
    extracts: .audiagentic/planning/extracts
    events: .audiagentic/planning/events
    claims: .audiagentic/planning/claims
    cache: .audiagentic/planning/cache
  task_domains: [core, provider, architecture, infra, docs, ui]
  work_package_domains: [core, provider, architecture, infra, docs, ui]
