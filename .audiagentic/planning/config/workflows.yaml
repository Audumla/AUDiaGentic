planning:
  workflows:
    request:
      default: standard
      named:
        standard:
          initial: captured
          values: [captured, distilled, closed, superseded]
          transitions:
            captured: [distilled, closed, superseded]
            distilled: [closed, superseded]
            closed: []
            superseded: []
    spec:
      default: standard
      named:
        standard:
          initial: draft
          values: [draft, ready, in_progress, blocked, done, cancelled]
          transitions:
            draft: [ready, blocked, cancelled]
            ready: [in_progress, blocked, cancelled]
            in_progress: [blocked, done, cancelled]
            blocked: [draft, ready, in_progress, cancelled]
            done: []
            cancelled: []
    plan:
      default: standard
      named:
        standard:
          initial: draft
          values: [draft, ready, in_progress, blocked, done, cancelled]
          transitions:
            draft: [ready, blocked, cancelled]
            ready: [in_progress, blocked, cancelled]
            in_progress: [blocked, done, cancelled]
            blocked: [draft, ready, in_progress, cancelled]
            done: []
            cancelled: []
    task:
      default: standard
      named:
        standard:
          initial: draft
          values: [draft, ready, in_progress, blocked, done, cancelled]
          transitions:
            draft: [ready, blocked, cancelled]
            ready: [in_progress, blocked, cancelled]
            in_progress: [draft, blocked, done, cancelled]
            blocked: [draft, ready, in_progress, cancelled]
            done: []
            cancelled: []
        review_heavy:
          initial: draft
          values: [draft, review, ready, in_progress, blocked, done, cancelled]
          transitions:
            draft: [review, blocked, cancelled]
            review: [draft, ready, blocked, cancelled]
            ready: [in_progress, blocked, cancelled]
            in_progress: [blocked, done, cancelled]
            blocked: [draft, review, ready, in_progress, cancelled]
            done: []
            cancelled: []
    wp:
      default: standard
      named:
        standard:
          initial: draft
          values: [draft, ready, in_progress, blocked, review, done, cancelled]
          transitions:
            draft: [ready, blocked, cancelled]
            ready: [in_progress, blocked, cancelled]
            in_progress: [review, blocked, done, cancelled]
            review: [in_progress, done, blocked, cancelled]
            blocked: [draft, ready, in_progress, cancelled]
            done: []
            cancelled: []
