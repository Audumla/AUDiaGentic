contract-version: v1
components:
  core-lifecycle: { enabled: true }
  release-audit-ledger: { enabled: true }
  agent-jobs: { enabled: true }
  providers: { enabled: true }
  discord-overlay: { enabled: false }
  optional-server: { enabled: false }
