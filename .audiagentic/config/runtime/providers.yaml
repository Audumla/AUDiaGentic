providers:
  aider:
    enabled: true
    install-mode: external-configured
    access-mode: cli
  copilot:
    enabled: true
    install-mode: external-configured
    access-mode: cli
  openhands:
    enabled: true
    install-mode: external-configured
    access-mode: cli
  pi:
    enabled: true
    install-mode: external-configured
    access-mode: cli
  claude:
    enabled: true
    install-mode: external-configured
    access-mode: cli
  cline:
    enabled: true
    install-mode: external-configured
    access-mode: cli
  codex:
    enabled: true
    install-mode: external-configured
    access-mode: cli
  gemini:
    enabled: true
    install-mode: external-configured
    access-mode: cli
  opencode:
    enabled: true
    install-mode: external-configured
    access-mode: cli
  qwen:
    enabled: true
    install-mode: external-configured
    access-mode: cli
