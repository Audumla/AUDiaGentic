# 2026-07-19 SH07 RV739 Docker E2E Verification — lite-coder-opencode

- Request: `req_c20f2f0056c3402f` (first attempt `req_0e8467f2ac694a33` failed on
  harness infrastructure, see the 2026-07-19 batch incident doc)
- Agent profile: `lite-coder-opencode`
- Model: `brutus/coder-quality-lite`
- Plan item: SH07, review RV739 (Docker opencode e2e / enabled_providers fix)
- Task class: validation-only — verify fix presence, unit coverage, run gated Docker e2e, redaction audit
- Turn time: ~2.5 minutes
- Outcome: accepted as-is; RV739 closed on this evidence

## Task shape

Read-only verification with an explicit safety rule (never write to or point
OPENCODE_CONFIG at the real user-global config — added after the incident this
morning) and a targeted edge-case question: what happens when the project
config has no `provider` block.

## Competency scores

| Competency | Score | Notes |
| --- | ---: | --- |
| scope-control | 5 | Zero file edits; no config experiments against real paths; respected the safety rule. |
| code-correctness | 5 | Correctly traced both derivation sites (acp.py, execution_environment.py) plus the e2e test's subprocess variant, with accurate line references. |
| test-quality | 4 | Ran the exact suites: 41/41 unit, 1/1 Docker e2e with the opt-in gate; verified rig_requests non-empty path. |
| validation-honesty | 5 | Distinguished "fix present" from "coverage gap on the no-provider-block path" and correctly labeled the edge case pre-existing, not a regression. |
| architecture-boundary | 5 | Did not propose or perform source changes on a validation task. |
| redaction-hygiene | 5 | Confirmed failure diagnostics stay structural; never echoed the API key or config contents; noted the apiKey substitution happens in-memory only. |
| workflow-discipline | 5 | No ledger event (no file changed — correct per instructions); no plan edits. |
| reviewability | 5 | Verdict-first summary with counts, edge-case answer, and coverage gap cleanly separated. |
| runtime-lifecycle | 5 | Fastest lane of the batch; clean end_turn. |
| independence | 5 | No controller repair; controller only cross-checked the edge-case answer against source. |

## Controller review

- The no-provider-block edge case answer is correct and materially important:
  this project's own `.opencode/opencode.json` has no `provider` block, so
  gateway ACP launches omit `enabled_providers` and fall back to the user-global
  whitelist — which is exactly why the global config clobber this morning
  killed all three worker profiles. The identified coverage gap (no test for
  the absent-provider-block path) is real and is now tracked in SH07 notes.
- RV739 closed: fix present at both sites, positive-path unit coverage exists,
  Docker e2e passes end-to-end with rig traffic observed.

## Assessment

Best lite-coder result to date and a model for the profile's sweet spot:
bounded read-verify-report work with named files, exact commands, and one
sharp analytical question. The edge-case answer fed directly into the incident
root-cause. Continue reserving lite-coder for verification lanes and giving it
one targeted "what happens when X" question per run — it pays for the whole
turn.
