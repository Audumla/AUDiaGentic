---
id: R2B2
order: 5
plan: plan-refactor-round2
state: not_done
validate-first: true
priority: MEDIUM
complexity: simple
---

# coding_lsp/lsp_api.py — split config/dependency management

## Finding

LSP *operations* (workspace_symbols/definition/hover/references/diagnostics/rename + `_open_file_session`/`_resolve_language_server`, lines 84–142 & 284–313) mixed with *language/dependency config management* (`config_status` 166, `add_language` 195, `remove_language` 208, `list_languages` 221, `install_lsp_dependencies` 234, `list_missing` 273 + `_configured_*`/`_*dependency*` helpers).

## Options

(a, recommended) extract the config/dependency half → `lsp_config_api.py`; keep the LSP operations in `lsp_api.py`; re-export moved public names. The MCP wrapper `lsp_manage_mcp.py` already separates these calls, so the seam matches the existing tool grouping.

## Steps

Extract config/dependency functions to new module; re-export from `lsp_api.py`; update imports.

## Files

`components/optional/coding_lsp/lsp_api.py` (315 lines), new `lsp_config_api.py`

## Validation

`pytest tests/ -q -k 'lsp'`.

## Risk

Low — clean functional boundary.

## Notes

Independent of B1.