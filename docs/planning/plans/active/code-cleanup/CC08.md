---
id: CC08
order: 8
plan: plan-code-cleanup
state: draft
priority: P1
---

# A1 Follow-Up — Generic host capabilities

## Description

12+ VS Code coupling sites (hardcoded `code` CLI, `~/.vscode/extensions` paths, `vscode-mode` config, `package_manager == 'vscode'` gating, `VsCodeExtension()` constructor, schema `'vscode'` enum values).

## Steps

`HostAdapter` Protocol in `providers/adapters/host_adapter.py` with `VsCodeAdapter` concrete implementation. `resolve_host_adapter()` factory detects available host. All VS Code-specific logic moves into the adapter.

**Migration order:** Schemas (Tier 1) → Adapter infra (Tier 2) → Config/gating (Tier 3) → Cleanup (Tier 4).

**New files (3):**
- `providers/adapters/host_adapter.py` — `HostAdapter` Protocol + `VsCodeAdapter`
- `providers/adapters/host_factory.py` — `resolve_host_adapter(project_root)`

**Modified files (14+):**
- `toolchains.yaml`, `providers/services/host_capabilities.py`, `providers/adapters/roo/descriptor.py`, `providers/descriptors/base.py`, `providers/services/status.py`, `providers/services/provider_config.py`, `providers/descriptors/registry.py`, `providers/services/reconcile.py`, 5 schema files, 9 adapter descriptors

## Files

17+ files — see plan for full list

## Validation

Zero `'code'` CLI literals in non-adapter code, zero `~/.vscode/extensions` paths in non-adapter code, `HostAdapter` Protocol defined, VS Code works identically.

## Effort & Risk

Medium — 17+ files touched, but each tier is independently testable.

## Notes


