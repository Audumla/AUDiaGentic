---
id: CC06
order: 6
plan: plan-code-cleanup
state: not_done
validate-first: true
priority: P1
complexity: simple
---

# Stage 5 — Foundation decommission and unification

## Description

Dead code in foundation event layer: two classes with zero importers (source and CLI).

> **⚠ Corrected 2026-06-21 (validation pass 3).** Original plan listed `validate_schemas.py` and `validate_packet_dependencies.py` as 'CLI-only, zero importers' → delete. **They are live CI entry points**, invoked as `python -m` scripts by `.github/workflows/ci-contracts.yml`. Deleting them **breaks the contracts CI job** plus tests. **They are NOT dead code — removed from the deletion list.**

**Modules to delete (verified dead — no source importers, no CLI invocation):**
- `foundation/event/event_replay.py` — `ReplayService`
- `foundation/event/event_service.py` — `EventService`

## Steps

1. Delete `event_replay.py` + `event_service.py` + their two test files (`tests/unit/foundation/event/test_replay.py`, `tests/unit/foundation/event/test_service.py`).
2. Update `event/__init__.py` exports and `event/README.md`.
3. Verify: `grep -rn 'EventService|ReplayService' src/ tests/` returns zero; `python -m audiagentic.foundation.event` imports clean.
4. Run full suite + `ruff check src/`; confirm contracts CI job still passes (validators untouched).

**Explicitly NOT deleted:**
- `foundation/contracts/validate_schemas.py` — CI entry point (`ci-contracts.yml:21`). Keep.
- `foundation/contracts/validate_packet_dependencies.py` — CI entry point (`ci-contracts.yml:25`). Keep.

## Files

Delete: `foundation/event/event_replay.py`, `foundation/event/event_service.py`, `tests/unit/foundation/event/test_replay.py`, `tests/unit/foundation/event/test_service.py`
Modify: `foundation/event/__init__.py`, `foundation/event/README.md`

## Validation

`EventService`/`ReplayService` have zero remaining references in `src/` and `tests/`, full suite green, ruff clean, foundation imports successfully, contracts CI job passes.

## Effort & Risk

**Medium** — `__init__.py` exports both classes as public API. Any caller doing `from audiagentic.foundation.event import *` or `import audiagentic.foundation.event` expects these names. Must audit all public API consumers before deletion. **Attempted deletion 2026-06-22 broke runtime imports — reverted.**

## Notes

Subsumes CC02's `event_service.py:60` fix (delete > fix).