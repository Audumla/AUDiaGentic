---
id: CC07
order: 7
plan: plan-code-cleanup
state: not_done
priority: P1
complexity: complex
---

# A21 — State machine unification

## Description

Two independent state machine implementations: foundation `StateMachine` (config-driven, protocol-based, event/cascade support) and agent-jobs `state_machine.py` (hardcoded transitions, direct file I/O, no events).

## Steps

Extract lightweight `TransitionEngine` into `foundation/workflow/transition_engine.py`. Both existing engines use it. Foundation `StateMachine` adds lifecycle/cascade/event on top. Agent-jobs becomes a thin adapter.

**Migration (4 phases):**
1. Create `transition_engine.py` + tests (no behavior change)
2. Refactor agent-jobs to use `TransitionEngine` (backward-compatible)
3. Foundation `StateMachine` adopts `TransitionEngine`
4. Cleanup old validation logic, add deprecation warnings

## Files

New: `foundation/workflow/transition_engine.py` — `TransitionConfig`, `TransitionEngine`, `PersistenceProtocol`
Modified: `foundation/workflow/state_machine.py`, `foundation/workflow/interfaces.py`, `components/optional/agent_jobs/state_machine.py`

## Validation

Single validation path, no direct persistence coupling in state machines, all existing consumers work without code changes, unified `WFSM-*` error codes.

## Effort & Risk

Low — additive refactor with backward-compatible adapters.

## Notes


