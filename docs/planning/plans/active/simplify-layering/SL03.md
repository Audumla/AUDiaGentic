---
id: SL03
order: 3
plan: plan-simplify-layering
state: draft
priority: P0
---

# Add provenance tracking to options resolution

## Description

`resolve_options` merges layers (descriptor defaults -> component options -> feature/implementation state) but discards which layer provided each value. No audit trail for debugging.

## Proposed Solution

Return a `ResolvedOption` dataclass `(value, source: str)` alongside the resolved dict, where `source` is one of `descriptor-default`, `component-state`, `feature-state`, `implementation-state`. Add an MCP tool `get_option_provenance` for debugging.
