---
id: RV785
review-of: MI08
plan: mcp-launch-isolation
state: closed
reviewed-by: ''
reviewed-at: ''
---

# Test-matrix approach: drive AG's own install/CLI capabilities, not stubs

## Notes

User direction: the MI08 isolation test matrix should install/enable pi and opencode through AUDiaGentic's own capabilities the way a real user would -- component install/enable via the project API (install_component/enable_component, already the pattern in tests/integration/providers/test_provider_full_lifecycle.py) or, for a closer user-interactive simulation, subprocess invocation of the real `audiagentic` console-script entry point (pyproject.toml: audiagentic = audiagentic.launcher:main) -- rather than monkeypatching internals or hand-writing stub JSON. This directly fixes a known gap: test_provider_full_lifecycle.py currently explicitly skips pi ('requires special setup') and only exercises the API layer, never a real CLI-driven session. Documented as the required test-construction approach for MI08's matrix.

## Findings



## Conclusion

documented-test-approach
