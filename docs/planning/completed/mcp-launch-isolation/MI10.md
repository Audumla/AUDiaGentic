---
id: MI10
order: 100
plan: plan-mcp-launch-isolation
state: completed
breadth: medium
skill: 2
---

# Remove stale foundation contracts and enforce projection errors

## Description

Verify and simplify foundation component IDs, remove the obsolete packet-document dependency validator, and enforce canonical registered errors on the live MCP projection path.

## Steps

['Replace duplicate component-ID registry derivation with descriptor-owned registry facts while retaining justified semantic IDs.', 'Delete the obsolete Markdown packet validator and all CI/test/docs references.', 'Convert MCP projection collision failure to the canonical error factory.', 'Add descriptor-alignment, live collision, and source error-catalog guard tests.']

## Detailed Solution & Technical Design

Kept the small core semantic-ID module but corrected its ownership doctrine and removed its duplicate registry enumeration helper; ID validation now reads all descriptor IDs directly and a guard proves the two semantic constants equal the core YAML descriptors. Deleted the obsolete packet dependency validator, its missing Markdown default, CI invocation, integration test, README entry, and stale package source entry. Converted both foundation MCP error construction sites to make_error. Added a live collision regression, an AST factory guard for foundation MCP, and a whole-foundation literal error-catalog guard; catalogued ten previously missing foundation transport/event errors.

## Code Samples & Guidance



## Files



## Validation

['Ruff passed on all affected Python files.', '25 focused component-ID, MCP error, error catalogue, ID validator, and CI contract tests passed.', 'Production projection callers confirmed in provider public execution, Pi native materialization, and OpenCode native materialization.', 'Reference scan found no remaining packet-validator, deleted registry-document, or optional-ID-helper references.']

## Effort & Risk



## Standards

Architecture Standards §§3–7; Creating a Component error doctrine; foundation domain neutrality.

## Notes

projection.py is live and retained. ids.py is retained only for semantic project/session branches, not registry enumeration. The packet validator was implementation-history tooling incorrectly shipped and invoked as a runtime contract validator.
