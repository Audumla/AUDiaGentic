---
id: RS07
order: 7
plan: plan-recipe-system-review
state: completed
validate-first: true
priority: P2
work: M
created-by: gpt-5.5-thinking
---

# Add recipe standards and architecture regression tests

## Description

Add a focused standards gate for the recipe system and dependants. The goal is to prevent future duplication of lifecycle orchestration, provider-specific branches in generic code, foundation layer leaks, unsafe shell output, and untracked direct mutations.

## Steps

This is the parent/analysis item; execution happens in RS19 (add the missing guard-test delta to the existing suite). Do not implement fixes directly under this ID.

1. Confirmed (RV228/RV229 audit) `tests/unit/foundation/toolchains/test_architecture_boundaries.py` already exists (431 lines) and already covers:
   - Std §1: `TestFoundationToolchainsBoundaries.test_no_provider_imports` (foundation/toolchains has no `components.providers` imports), `test_no_mcp_specific_helpers_in_config_patcher`, `test_no_mcp_hooks_plugins_in_docstring`.
   - Std §1 (memory side): `TestMemoryComponentBoundaries` — no provider surface manager import, no `enabled-providers` in hindsight.yaml, no `hindsight_recipe.py` outside the package, memory-core has no provider imports, hindsight's provider imports contained to an explicit allowlist (`descriptors.registry`, `services.recipes`, `services.mcp`).
   - Behavioral guards: `TestProviderRecipeTests` — `ProviderRecipeKind` enum shape, `ProviderRecipeResult.ok/fail`, registry register/get/list, matrix source gate, unverified installer refuses execution, shell-pipe install runs via shell, plugin config skips manual-instruction rows, no Hindsight modules under `providers/services/`.
2. Confirmed NOT yet covered (the real remaining scope for RS19):
   a. Std §2 static check: no generic AST-based test asserts "no `if`/`elif` branching on provider or component name strings" across the recipe dispatch modules (`strategies.py::_RECIPE_FACTORIES`, `providers/services/recipes.py`) — current tests check specific strings in specific files, not a general anti-pattern scan.
   b. Std §8 redaction gate: no test asserts shell/recipe results are free of secret-shaped values — this depends on RS16 landing `foundation/logging/redaction.py` first.
   c. Override-allowlist: no test fails when a dependant reimplements `provision()`/`teardown()` without an allowlisted rationale comment.
   d. Factory completeness: no test asserts every `ProviderRecipeKind` value used by a live matrix row has an entry in `strategies.py::_RECIPE_FACTORIES`, or explicitly falls back to `_GUIDANCE_SPEC` with non-empty `action_needed` rather than silently degrading.
   e. Std §10 migration safety: no test greps for old path strings post-refactor.
3. Do not duplicate (1) in RS19 — extend the same file with only (2a-2e).

## Files



## Validation

1. `python -m pytest tests/unit/foundation/toolchains` passes with all recipe-related tests.
2. Architecture-boundary tests prove foundation remains domain-neutral: `recipe_contract`, `provision_steps/*`, and `recipe_patterns` import no component/provider modules (verified by test, not just grep).
3. Static/golden tests catch accidental provider-specific recipe dispatch in foundation or generic providers code.
4. Std §8: redaction gate tests fail if stdout/stderr/secrets are emitted in structured results.
5. Std §1: override tests fail if a dependant reimplements `provision()` / `teardown()` without allowlist and rationale comment.
6. Factory completeness tests fail if a matrix recipe kind would silently fall back to guidance-only instead of surfacing action_needed.
7. Assembled recipe tests fail if a lifecycle primitive is not delegated or status overrides orphan delegate calls.
8. Std §10 migration safety: after any RS implementation, full test suite proves no string-literal references to old paths remain.

## Effort & Risk

Mid. Tests may expose existing questionable patterns. Prefer targeted allowlists with comments over broad brittle regexes.

## Standards

Std §1 layer boundaries, Std §2 config-over-code, Std §3 logic containment, Std §8 error handling, Std §9 logging, Std §10 migration doctrine, Std §11 lazy initialization, Observability Standards redaction.

## Notes

This is the safety net for RS01-RS06. It should land before large recipe refactors so cleanup work has guardrails.

RV226 incorporated. Validation confirmed existing architecture tests are under `tests/unit/foundation/toolchains`, and missing guards belong in RS19.

RV229 incorporated: confirmed `test_architecture_boundaries.py` (431 lines) already covers the Std §1 import-boundary and several behavioral guards listed above in full detail. The real remaining gap is exactly 5 items (2a-2e above) — RS19 must add only these, extending the existing file, not building a parallel suite.

## Ledger Events

- chg_20260710_040722_recipe-system-review-secret-r_1791
