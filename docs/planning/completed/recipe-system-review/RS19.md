---
id: RS19
order: 19
plan: plan-recipe-system-review
state: completed
validate-first: true
priority: P2
work: S
created-by: gpt-5.5-thinking
---

# Add recipe architecture and standards guard tests

## Description

Small execution slice for RS07. Add targeted tests that prevent recipe-system standards regressions. This is the safety net for RS01–RS06 implementations and must land before or alongside large recipe refactors.

Dependency: RS19 should land BEFORE modify items RS10/RS12/RS14/RS16/RS18 to provide guardrails during implementation. If already past that point, RS19 still serves as regression protection for future work.

## Steps

Extend `tests/unit/foundation/toolchains/test_architecture_boundaries.py` (431 lines, existing) with exactly these 5 new tests — do not create a second file, do not restate the coverage RS07's audit already confirmed present:

1. `test_no_provider_name_branching_in_dispatch` (Std §2): read `strategies.py` and `providers/services/recipes.py` source; use `ast.walk` to find `If`/comparisons where a string literal is compared against a variable named like `provider_id`/`recipe_kind` in a chain of 3+ `elif`s (a single `if row.source_status != "verified"` gate check is fine — that is a value check, not entity-name branching; the anti-pattern is `if provider_id == "a": ... elif provider_id == "b": ...`). Assert zero matches. Confirm `strategies.py::_RECIPE_FACTORIES` (a dict keyed by `ProviderRecipeKind`, not an if/elif chain) is why this test passes today — it is a regression guard, not a fix.
2. `test_redaction_gate_on_shell_step_results` (Std §8, depends on RS16): import `redact_text` from `foundation.logging.redaction` (skip with `pytest.importorskip` or an explicit `pytest.skip("RS16 not yet landed")` if the module doesn't exist yet, so this test can be added before RS16 completes without breaking CI) and assert running a `ShellProvisionStep` whose command echoes a token-shaped string never leaves that string unredacted in the returned `StepResult.outputs`.
3. `test_provision_teardown_override_requires_allowlist_comment` (Std §1 override discipline): scan all concrete `ProvisioningRecipe`/`ProviderCapabilityRecipe` subclasses under `src/audiagentic/components/` for a `def provision(` or `def teardown(` override (via `ast`, looking for method defs named `provision`/`teardown` in class bodies that inherit from a recipe base) — for each match, require a comment containing `RS19-allowlist` within 3 lines above the `def` line, else fail listing the file:line. As of this audit, confirm whether any dependant currently overrides `provision`/`teardown` (recipes.py/plugin_recipes.py do NOT override them, per this audit — they only override the 6 primitives) — if the scan finds zero overrides, the test should assert that (a green "no violations" test that will catch future additions), not be skipped.
4. `test_matrix_recipe_kind_has_factory_or_explicit_guidance` (factory completeness): for every distinct `recipe_kind` value present across `HINDSIGHT_RECIPE_MATRIX` rows (import from `hindsight.matrix`), assert it is either a key in `strategies.py::_RECIPE_FACTORIES` OR (if absent) that `build_hindsight_recipe()` for a synthetic row of that kind returns a recipe whose `probe({})` yields non-empty `action_needed` (proving the guidance-only fallback surfaces guidance rather than silently no-op'ing).
5. `test_no_stale_path_strings_from_rs_refactors` (Std §10): after RS02/RS03/RS04/RS05/RS06 land, `git grep` (via `subprocess` inside the test, scoped to `src/` and `tests/`) for any old dotted path this refactor removed — seed the list from each RS item's Notes once they close (e.g. if RS12 deletes a duplicated method name, add that name here). Leave a `# TODO(RS12/RS14/RS18): add removed identifiers here once those items close` placeholder list if none have closed yet at the time this test is written — do not leave the test doing nothing silently; assert the placeholder list is non-empty once at least one of RS10/RS12/RS14/RS16/RS18 has landed.
6. Run `python -m pytest tests/unit/foundation/toolchains/test_architecture_boundaries.py -v`.

## Files

[TEST] `tests/unit/foundation/toolchains/test_architecture_boundaries.py`
[TEST] `tests/unit/foundation/toolchains/test_recipe_architecture.py`
[TEST] `tests/unit/foundation/toolchains/test_shell_provision_step.py` — redaction gate tests only
[TEST] `tests/unit/providers/test_provider_recipes.py` — stamping guard tests only
[TEST] `tests/unit/memory/hindsight/`
[TEST] `tests/unit/memory/hindsight/test_assembled_recipes.py`

## Validation

1. `python -m pytest tests/unit/foundation/toolchains/test_architecture_boundaries.py -v` passes with 5 new tests (431 existing lines + ~5 new test functions, not a new file).
2. `test_redaction_gate_on_shell_step_results` either passes (RS16 landed) or is explicitly skipped with a named reason (RS16 pending) — never silently absent.
3. `test_provision_teardown_override_requires_allowlist_comment` and `test_matrix_recipe_kind_has_factory_or_explicit_guidance` both pass against current code with zero violations found (this audit confirmed no current overrides and full factory coverage for the 6 known kinds).
4. No duplicate assertions of what RS07's audit already confirmed present (Std §1 import-boundary tests) — diff review confirms only additive test functions, no restated coverage.

## Effort & Risk

Low risk, low effort. Tests-only item — adds guardrails without changing production code. Risk: tests may catch existing violations, which is by design. These should be treated as findings for follow-up items, not blockers for this item. Prefer targeted allowlists with comments over broad brittle regexes.

## Standards

Std §1: architecture boundary tests prove foundation modules do not import component/provider code. Std §2: no hardcoded entity lists or if-elif chains on provider/component names in generic recipe code. Std §3: logic containment — no god objects, no duplicated dataclasses. Std §8: error handling compliance gate — raw stdout/stderr/secrets fail the test. Std §9: logging compliance gate — no inline logger creation, no MCP arg logging. Std §10: migration safety — old path string references fail the test. Observability Standards: timeline attribute redaction tested.

## Notes

RV228 incorporated: same overlap as RS07 — `tests/unit/foundation/toolchains/test_architecture_boundaries.py` already exists and already covers the Std §1 import-boundary checks this item's validation step 2 asks for. Re-scope to add only the not-yet-present delta: Std §2 static-analysis test for provider/component if-elif chains, Std §8 redaction gate, Std §10 migration-safety string-reference gate, override-allowlist test, and factory-completeness test. Extend the existing file/suite rather than creating a parallel one.

## Ledger Events

- chg_20260710_040407_added-architecture-guard-tests_8976
- chg_20260710_040722_recipe-system-review-secret-r_1791
