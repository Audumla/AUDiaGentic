---
id: RS14
order: 14
plan: plan-recipe-system-review
state: completed
validate-first: true
priority: P1
complexity: simple
created-by: gpt-5.5-thinking
---

# Normalize provisioning step path and render helpers

## Description

Small execution slice for RS04. After RS13 characterization tests land, normalize the smallest proven inconsistencies in step path resolution and runtime rendering. Change only what RS13 identified as genuinely inconsistent or unsafe.

Dependency: RS13 must be completed first. Only modify files listed here; RS13 tests serve as safety baseline.

## Steps

1. Confirm RS13's characterization tests are green, and read RS13's Notes for the outcome of `test_shell_render_has_no_second_consumer`.
2. If RS13 confirms `str.format(**context)` runtime substitution has no second consumer (the expected outcome per this audit): do NOT extract a shared helper. Instead, add a module docstring note to `shell.py::ShellProvisionStep._render()` explaining the two-stage substitution (YAML-time `_substitute()` in `_shell_from_dict`, then run-time `.format(**context)` in `_render()`) so future readers don't mistake it for redundant/buggy double-substitution. This satisfies the item's own risk guidance ("prefer documentation over breaking changes") and Std §3 (no single-consumer extraction).
3. If RS13's test instead finds a second consumer has appeared, extract a shared `_render_runtime_template(value, context)` helper into `provision_steps/base.py` next to `_substitute`, and have both consumers call it.
4. Regardless of outcome, add one clarifying docstring line to `hindsight/recipes.py::_absolute_project_path()` (if not already present) cross-referencing this item and RS04's Notes, stating explicitly that foundation steps intentionally do not anchor to a project root and this function is the capability-layer's deliberate addition of that concept — do not move `_absolute_project_path` into foundation.
5. Run `python -m pytest tests/unit/foundation/toolchains/test_provision_step.py -v` and confirm RS13's tests remain green with zero test edits.

## Files

[FOUNDATION] `src/audiagentic/foundation/toolchains/provision_steps/base.py`
[FOUNDATION] `src/audiagentic/foundation/toolchains/provision_steps/shell.py`
[FOUNDATION] `src/audiagentic/foundation/toolchains/provision_steps/config_set.py`
[FOUNDATION] `src/audiagentic/foundation/toolchains/provision_steps/managed_block_step.py`
[FOUNDATION] `src/audiagentic/foundation/toolchains/provision_steps/write_file.py`
[TEST] `tests/unit/foundation/toolchains/test_provision_steps.py`

## Validation

1. `python -m pytest tests/unit/foundation/toolchains/test_provision_step.py -v` passes unchanged from RS13's baseline.
2. If no extraction occurred: `git diff --stat src/` shows only docstring/comment changes, no logic changes, and this item's Notes explains why (citing RS13's single-consumer finding).
3. If extraction occurred: the new helper has exactly 2 call sites, verified by `git grep -n "_render_runtime_template"`.
4. Std §9 assertion: dry-run output for shell/write-file/config-set/managed-block steps is unchanged byte-for-byte from before this item (no behavior change) — spot check with `tests/unit/foundation/toolchains/test_provision_step.py`'s existing dry-run tests.

## Effort & Risk

Low risk if RS13 characterization is green. Risk: normalizing substitution order may affect recipe YAML that relied on undocumented lenient behavior. Mitigate: RS13 tests lock current behavior; normalize only proven inconsistencies, not working-but-different paths. Prefer documentation over breaking changes.

## Standards

Std §1: foundation remains domain-neutral — normalized helpers must not import component/provider modules. Std §3: consolidate shared path/substitution mechanics into reusable base helpers. Std §8/§9: normalized output must not expose secrets from context variables.

## Notes



## Ledger Events

- chg_20260710_040722_recipe-system-review-secret-r_1791
