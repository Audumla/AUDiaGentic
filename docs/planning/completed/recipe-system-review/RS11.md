---
id: RS11
order: 11
plan: plan-recipe-system-review
state: completed
validate-first: true
priority: P1
complexity: simple
created-by: gpt-5.5-thinking
---

# Characterize Hindsight recipe strategies before consolidation

## Description

Small execution slice for RS03. Add characterization tests for Hindsight strategy selection and recipe behavior before any plugin/helper consolidation.

Dependency: none (characterize phase — must complete before RS12).

## Steps

1. In `tests/unit/memory/test_hindsight_strategy.py` (existing file — confirmed present), add characterization tests for `resolve_hindsight_strategy()` and `build_hindsight_recipe()` in `strategies.py`:
   a. `test_resolve_strategy_platform_gate_falls_back`: a row with `recipe_kind` in `_INSTALLER_KINDS` (HOOKS/WRAPPER_CLI/PLUGIN_CONFIG) and `platform_constraints` that exclude the current platform — assert `resolve_hindsight_strategy()` returns a row from `_external_fallback_row()` (check `integration_type` is `"fallback-mcp"` or `"rules-only"` depending on whether the provider descriptor has `mcp_config.remote`).
   b. `test_build_recipe_dispatches_by_kind`: for each of the 6 keys in `_RECIPE_FACTORIES` (HOOKS, WRAPPER_CLI, PLUGIN_CONFIG, MCP_CONFIG, HYBRID, GUIDANCE_ONLY), assert `build_hindsight_recipe()` returns an instance of the expected recipe class (e.g. PLUGIN_CONFIG → one of `PluginConfigRecipe`/`_PluginArrayRecipe`/`_PluginUrlConfigRecipe` depending on row fields, per `_build_plugin_config_recipe`'s precedence: plugin_array > url_config_path > generic MCP target > guidance-only).
   c. `test_build_recipe_unknown_kind_falls_back_to_guidance`: a row with a `recipe_kind` not in `_RECIPE_FACTORIES` — assert the returned recipe is built via `_GUIDANCE_SPEC` (its `probe()` returns `action_needed` non-empty, matching `RecipeState.ABSENT`).
2. In `tests/unit/memory/test_hindsight_recipe.py` (existing file), add characterization tests for the 4 duplicated gated-step blocks identified in RS03's Notes, run against the CURRENT `plugin_recipes.py` code (pre-RS12):
   a. `test_plugin_config_install_refuses_unverified_source`: construct a `PluginConfigRecipe` with a row where `source_status != "verified"` and `install_steps` set and `audia_action == "call_official_installer"` — assert `install()` returns `success=False` and `action_needed` equals the row's `notes` (or `audia_action` if notes is empty).
   b. `test_plugin_url_config_install_refuses_unverified_source`: same assertion for `_PluginUrlConfigRecipe.install()`.
   c. `test_plugin_config_uninstall_refuses_unverified_source` and `test_plugin_url_config_uninstall_refuses_unverified_source`: same gate check on `uninstall()` for both classes.
   d. `test_should_run_plugin_command_matches_audia_action`: for both `PluginConfigRecipe` and `_PluginUrlConfigRecipe`, assert `_should_run_plugin_command()` returns `True` iff `row.audia_action == "call_official_installer"` (parametrize over both classes to prove the duplicated method behaves identically — this is the pre-consolidation baseline RS12 must not change).
3. Add one cross-strategy comparison note (not a test) to this item's Notes: list the 4 call sites (file:line) that RS12 is allowed to touch, so RS12 does not go hunting for "one more" duplicate beyond what this item confirmed.
4. Run `python -m pytest tests/unit/memory -v` and confirm all new + existing tests pass.

## Files

[COMPONENT:memory] `src/audiagentic/components/memory/hindsight/strategies.py`
[COMPONENT:memory] `src/audiagentic/components/memory/hindsight/recipes.py`
[COMPONENT:memory] `src/audiagentic/components/memory/hindsight/plugin_recipes.py`
[COMPONENT:memory] `src/audiagentic/components/memory/hindsight/recipe_spec.py`
[CONFIG] `src/audiagentic/config/components/memory/hindsight_matrix.yaml`
[TEST] `tests/unit/memory/test_hindsight_strategy.py` (CORRECTED path — file already exists; `tests/unit/memory/hindsight/` does not exist as a directory)
[TEST] `tests/unit/memory/test_hindsight_recipe.py` (CORRECTED path)

## Validation

1. `python -m pytest tests/unit/memory/test_hindsight_strategy.py tests/unit/memory/test_hindsight_recipe.py -v` passes with ≥ 8 new characterization tests (3 in test_hindsight_strategy.py, 5 in test_hindsight_recipe.py, counting the parametrized `_should_run_plugin_command` test as one).
2. This item's Notes lists the exact 4 file:line call sites RS12 must consolidate (from RS03's finding), plus confirmation that `_should_run_plugin_command` is duplicated (not just similar) across the 2 classes.
3. No production code changed — `git diff --stat src/` is empty.

## Effort & Risk

Low risk, low effort. Read-only characterization of Hindsight strategy selection and recipe behavior. Risk: tests may reveal strategy dispatch inconsistency or undocumented fallback behavior. If found, tag for RS12 — do NOT fix here.

## Standards

Std §2: config-driven integration data — tests must verify strategy selection comes from matrix YAML, not hardcoded Python branches. Std §3: shared repeated mechanics should be extracted — characterization should identify which patterns are truly duplicated vs genuinely unique per strategy.

## Notes

RV228 incorporated. Original file list cited a nonexistent `tests/unit/memory/hindsight/` subdirectory; corrected to the real location `tests/unit/memory/*.py`, where `test_hindsight_strategy.py` and `test_hindsight_recipe.py` already exist. Validation commands referencing the old path must be updated to `python -m pytest tests/unit/memory`.

## Ledger Events

- chg_20260710_040722_recipe-system-review-secret-r_1791
