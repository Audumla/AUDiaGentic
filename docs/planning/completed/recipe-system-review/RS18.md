---
id: RS18
order: 18
plan: recipe-system-review
state: completed
validate-first: true
priority: P2
work: S
created-by: gpt-5.5-thinking
---

# Replace or document direct recipe mutations

## Description

Small execution slice for RS06. After RS17 classification, replace direct recipe mutations classified as "shared-primitive-available" with existing primitives. Document items classified as "intentional-one-off" with rationale comments. Do NOT touch items classified as "requires-investigation" — defer to follow-up item.

Dependency: RS17 must be completed first. Replace only "shared-primitive-available" items; document "intentional-one-off" items; defer "requires-investigation".

## Steps

1. Open `docs/planning/active/recipe-system-review/RS17-CLASSIFICATION.md` and filter for rows marked `shared-primitive-available`. Only touch those rows in this item.
2. For `_PluginUrlConfigRecipe.configure()`'s direct `write_text` (if classified shared-primitive-available): replace with an `ArtifactRegistry`-aware write. Simplest correct approach given this class has no `ArtifactRegistry` instance passed in today: change the constructor to accept an optional `registry: ArtifactRegistry | None = None` (mirroring how `ConfigSetStep`/`WriteFileStep` take one), and in `configure()` call `self._registry.register(self.provider_id, files=[str(self._url_config_path)])` after the write when a registry is present. Do not force every caller to pass one — keep it optional so this is additive, not a breaking constructor change beyond adding one optional kwarg with a default.
3. For `_PluginUrlConfigRecipe.prune()`'s direct `unlink(missing_ok=True)` (if classified shared-primitive-available): if a registry is present, prefer `self._registry.prune(self.provider_id)` for the tracked file; otherwise fall back to the existing direct `unlink(missing_ok=True)` (keep the fallback — do not make registry mandatory, since not every call site constructs one today per RS17's inventory).
4. For `_repair_windows_plugin_mcp()` (expected classification: intentional-one-off per RS06/RS17): do NOT replace. Add a one-line comment directly above the function citing this item: `# RS18: intentional one-off — patches a plugin-owned file this recipe does not create/own; not expressible via ArtifactRegistry.` Confirm this matches RS17's actual (not preliminary) classification before leaving it untouched — if RS17 classified it differently, follow RS17's classification instead.
5. For any `requires-investigation` row: do not touch. Leave a one-line note in this item's Notes listing each deferred file:line so it isn't silently lost.
6. Add/update tests in `tests/unit/memory/test_hindsight_recipe.py` proving: (a) provider-managed MCP entries still go through `sync_managed_provider_mcp_subset`/`get_managed_entry_status` (no regression — these were already correct, just confirm), (b) the now-registry-aware `configure()`/`prune()` round-trip correctly (write then prune removes exactly the tracked file, leaves other files in the same directory untouched).
7. Run `python -m pytest tests/unit/memory tests/unit/foundation/toolchains -v`.
8. Whole-repo grep for any now-removed direct-mutation pattern to confirm zero remaining occurrences outside intentional one-offs.

## Files

[COMPONENT:memory] `src/audiagentic/components/memory/hindsight/plugin_recipes.py`
[COMPONENT:memory] `src/audiagentic/components/memory/hindsight/recipes.py`
[FOUNDATION] `src/audiagentic/foundation/toolchains/artifact_registry.py` — tests only unless bug found
[FOUNDATION] `src/audiagentic/foundation/toolchains/provision_steps/*` — only if RS17 requires
[TEST] `tests/unit/memory/hindsight/`
[TEST] `tests/unit/foundation/toolchains/`

## Validation

1. `python -m pytest tests/unit/memory tests/unit/foundation/toolchains -v` passes in full.
2. Every `shared-primitive-available` row in `RS17-CLASSIFICATION.md` has a corresponding code change; every `intentional-one-off` row has a corresponding rationale comment in the source; every `requires-investigation` row is listed untouched in this item's Notes.
3. Tests prove plugin config pruning removes only the targeted file/key — unrelated files in the same directory are asserted still present after prune.
4. `git diff --stat` touches only `plugin_recipes.py` (+ its test file) unless RS17 classified additional files — no drive-by changes to files RS17 didn't flag.

## Effort & Risk

Low risk if RS17 classification is complete and accurate. Risk: replacing a direct mutation that was intentional one-off because the component needs optional JSON keys or Windows-specific repair logic. Mitigate: replace only items classified as "shared-primitive-available"; document one-offs with rationale comments citing RS18.

## Standards

Std §3: replace direct mutations with shared primitives where RS17 classified "shared-primitive-available". Std §10: remove duplicate paths atomically with tests; no legacy string references remain after removal. Creating Components §6: provider-specific rendering stays in providers; do not migrate provider-owned config writes into generic helpers.

## Notes



## Ledger Events

- chg_20260710_040722_recipe-system-review-secret-r_1791
