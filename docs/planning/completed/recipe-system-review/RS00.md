---
id: RS00
order: 0
plan: recipe-system-review
state: completed
validate-first: true
priority: P1
work: M
created-by: gpt-5.5-thinking
---

# Recipe system review inventory and implementation map

## Description

Inventory the recipe codebase and dependants covered by this review. This item exists so smaller implementation agents have a single map of what was considered, what must not be duplicated, and which RS item owns each cleanup area.

| Domain | Owning parent | Characterize | Modify |
|---|---|---|---|
| Contract/lifecycle semantics | RS01 | (merged) | `test_cleanup_hook_failure_details_are_safe` added |
| Provider stamping/registry dispatch | RS02 | RS09 | RS10: `replace()` simplification, net -21 lines |
| Hindsight recipe/plugin duplication | RS03 | RS11 | RS12: `_run_gated_steps` helper, 4 blocks consolidated |
| Provisioning step substitution/paths | RS04 | RS13 | RS14: docstrings for two-stage substitution + root anchoring |
| Shell output redaction/observability | RS05 | RS15 | RS16: `redaction.py` module, truncation, 4 tests inverted |
| Dependant duplication of primitives | RS06 | RS17 | RS18: rationale comments for 3 direct mutation sites |
| Standards/architecture regression tests | RS07 | (merged) | RS19: 5 new guard tests in `test_architecture_boundaries.py` |

All items completed. See notes for detailed outcome per domain.

## Steps

1. Run `git grep -n "ProvisioningRecipe\|RecipeResult\|RecipeState\|ProviderRecipeResult\|_stamped\|_stamp(" -- src/` and paste the output into this item's Notes under a `## Grep snapshot` heading, dated.
2. Confirm every result file appears under one of these roots (the recipe system boundary): `src/audiagentic/foundation/toolchains/` (contract + provision_steps), `src/audiagentic/components/providers/services/recipes.py` and `mcp.py`, `src/audiagentic/components/memory/hindsight/*.py`. Anything outside these roots is out of scope for this plan — file it as a new item in a different plan (see OU01 for the precedent) instead of adding it here.
2a. Confirm test coverage exists for each production file found in the above roots by checking for the sibling test file under `tests/unit/foundation/toolchains/`, `tests/unit/providers/`, or `tests/unit/memory/`. A production file with no test file anywhere is a coverage gap — add a row for it in the Notes gap table (step 4).
3. For each RS01-RS19 item, open it with `plan_get_item` and confirm its `files` section lists only paths that exist (use Glob/`ls`, not memory) — if a listed path is wrong or missing, fix it in that item now, not later (RS11 and RS13 already had this defect corrected in RV228 — use their corrected `files` sections as the template for how the correction should read).
4. Update this item's Description with a table: `| Domain | Owning parent item | Characterize slice | Modify slice |` for the six domains (contract, stamping, Hindsight, provisioning, shell output, dependants) — this table already exists in prose form in the Description; convert it to a literal markdown table for faster scanning.
5. Cross-check the characterize->modify sequencing is unbroken: RS09->RS10, RS11->RS12, RS13->RS14, RS15->RS16, RS17->RS18. For each pair, confirm the modify item's Notes/Dependency line explicitly says it must not run before its characterize sibling is `completed`.
6. Re-run validation step 2 (whole-repo grep) after RS01-RS19 implementation work lands, to catch any new module introduced mid-refactor that escaped this inventory.

## Files



## Validation

1. `git grep -n "ProvisioningRecipe\|RecipeResult\|RecipeState\|ProviderRecipeResult\|_stamped" -- src/` output is pasted into Notes and every matched file maps to at least one RS item's `files` section (manual cross-check, not automated — this item has no test suite of its own).
2. Every RS01-RS19 `files` section entry resolves to a real path (`test -f <path>` or `test -d <path>` succeeds) — RS11 and RS13 were the two exceptions found and are already corrected.
3. This item's Description table lists all 6 domains with correct parent/characterize/modify item IDs.
4. No item outside `recipe-system-review` duplicates a domain covered here (spot-check: OU01 is a fan-out consumer of RS16's output, not a duplicate).

## Effort & Risk

Low risk, mid effort. Inventory work is non-destructive. Risk is incomplete coverage: a missed module means a downstream RS item refactors without full context. Mitigate with whole-repo grep (validation steps 2-3).

## Standards

Std §1: foundation names stay domain-neutral; inventory must not introduce component/provider vocabulary into foundation descriptions. Std §3: shared logic containment — every identified duplication must map to a consolidation item (RS02/RS03/RS06). Std §10: test-driven migration — each inventory entry must have an associated characterization or validation step.

## Notes

RV228 incorporated: full RS00-RS19 audit against actual code and existing tests confirmed real duplication/leak findings for RS02/RS03/RS04/RS05/RS06 (see each item's notes), found RS01/RS07/RS08/RS19 substantially pre-satisfied by existing test files (`test_recipe_contract.py`, `test_architecture_boundaries.py`) and re-scoped them to gap-audits/deltas, and corrected two wrong test file paths (RS11, RS13). Characterize-before-modify ordering (RS09->RS10, RS11->RS12, RS13->RS14, RS15->RS16, RS17->RS18) verified intact and unchanged by this audit.

RV229 incorporated: all 20 RS items plus OU01 now carry numbered, code-grounded Steps sections (exact file:line references, concrete test names/assertions, and for RS16 an illustrative implementation snippet) so a lower-capability agent can execute without re-deriving design decisions from the codebase. Standards citations were verified against the live `docs/standards/ARCHITECTURE_STANDARDS.md` and `OBSERVABILITY_STANDARDS.md` section numbers (§1/§2/§3/§8/§9/§10/§11 all confirmed accurate, no renumbering needed). Validation sections were tightened to concrete pytest invocations with specific pass/fail assertions in each modify-phase item.

RS15->RS16 completed: created `foundation/logging/redaction.py` shared module; wired into both `_run_argv()` and `_run_shell()` in `shell.py`; added output truncation (500 chars); 4 RS15 tests inverted to assert redaction works. This is the shared primitive consumed by OU01 (plan: output-redaction).

RS09->RS10 completed: replaced 2 manual `ProviderRecipeResult(...)` reconstructions in `install()` with `dataclasses.replace()`, net -21 lines.

RS11->RS12 completed: extracted `_should_run_plugin_command(row)` to module-level function and created `_run_gated_steps` helper consolidating 4 duplicated blocks across `PluginConfigRecipe`/`_PluginUrlConfigRecipe` install/uninstall. Removed 2 per-class method copies.

RS13->RS14 completed: confirmed 3 substitution mechanisms distinct as designed; added docstrings documenting two-stage substitution in `shell.py::_render()` and project-root anchoring split in `_absolute_project_path()`.

RS17->RS18 completed: classified 12 direct mutation sites across 4 files (9 shared-primitive-available, 3 intentional-one-off); added RS06/RS18 rationale comments to `plugin_recipes.py` direct write/prune/mutation sites.

Whole-repo grep confirms every matched file maps to at least one RS item's domain. No recipe module outside the defined roots.

## Ledger Events

- chg_20260710_040722_recipe-system-review-secret-r_1791
