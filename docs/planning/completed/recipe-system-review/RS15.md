---
id: RS15
order: 15
plan: plan-recipe-system-review
state: completed
validate-first: true
priority: P1
work: S
created-by: gpt-5.5-thinking
---

# Characterize shell output and redaction gaps

## Description

Small execution slice for RS05. Add tests that expose current shell output behavior and prove where stdout, env values, token-like strings, and command args leak into structured results.

Dependency: none (characterize phase — must complete before RS16).

## Steps

1. In `tests/unit/foundation/toolchains/test_shell_provision_step.py` (existing file), add characterization tests proving the leaks exist in the CURRENT code (these tests document the vulnerability; they are expected to pass today because the code has no redaction yet — RS16 will tighten the assertions, not these tests' existence):
   a. `test_stdout_with_token_like_string_is_not_redacted_yet`: monkeypatch `subprocess.run` (or run a real command like `python -c "print('token=sk-aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa')")`) so stdout contains a 32+ char token-shaped string, call `ShellProvisionStep(...).run(context)`, and assert the raw token string IS currently present verbatim in `result.outputs["stdout"]` (`assert "sk-aaaa" in result.outputs["stdout"]`). Tag this test `# LEAK: see RS16` in a comment.
   b. `test_failure_reason_contains_raw_stdout_not_redacted_yet`: same setup but with a non-zero exit code, assert `result.reason` contains the raw output verbatim.
   c. `test_env_secret_value_leaks_into_stdout_when_echoed`: pass `env={"API_TOKEN": "supersecretvalue1234567890"}` and a command that echoes `$API_TOKEN`/`%API_TOKEN%` (guard with `sys.platform` or use `python -c "import os; print(os.environ['API_TOKEN'])"` for cross-platform safety), assert the raw value is currently present in `outputs["stdout"]`.
2. In `tests/unit/foundation/toolchains/test_compensating_sequence.py` (existing file), add `test_sequence_aggregates_unredacted_step_output`: a step whose `run()` returns `StepResult(status="failed", outputs={"stdout": "token=<secret>"})`, run it through `CompensatingSequence`, and assert the raw value currently surfaces in the sequence's `outputs["steps"]` list.
3. For each of the 4 tests above, add a one-line severity tag directly in the test's docstring: `Severity: HIGH — secret-shaped values reach structured results with no redaction (RS16 fixes this).`
4. Do not add any redaction logic in this item — characterization only.
5. Run `python -m pytest tests/unit/foundation/toolchains/test_shell_provision_step.py tests/unit/foundation/toolchains/test_compensating_sequence.py -v` and confirm all 4 new tests currently PASS (proving the leak, not preventing it).

## Files

[FOUNDATION] `src/audiagentic/foundation/toolchains/provision_steps/shell.py`
[FOUNDATION] `src/audiagentic/foundation/toolchains/provision_steps/sequence.py`
[FOUNDATION] `src/audiagentic/foundation/toolchains/recipe_contract.py`
[FOUNDATION] `src/audiagentic/foundation/toolchains/recipe_patterns.py`
[TEST] `tests/unit/foundation/toolchains/test_shell_provision_step.py`
[TEST] `tests/unit/foundation/toolchains/test_compensating_sequence.py`

## Validation

1. `python -m pytest tests/unit/foundation/toolchains/test_shell_provision_step.py tests/unit/foundation/toolchains/test_compensating_sequence.py -v` passes with 4 new leak-characterization tests, each documented with a file:line reference back to `shell.py`'s `_run_argv`/`_run_shell` and a severity tag.
2. No production code changed in this item — `git diff --stat src/` is empty.
3. RS16 can run this exact same test file after its fix and the assertions will need to flip (leak → redacted) — confirm each test's assertion is written so a `[REDACTED]` value would make it fail today (i.e., it is currently asserting presence of the raw secret, not absence — this is what RS16 must invert).

## Effort & Risk

Low risk, low effort. Read-only characterization of shell output behavior and leak paths. Risk: tests will likely reveal existing violations (by design — that's the point). Document findings clearly for RS16; do NOT fix here.

## Standards

Std §8: raw stdout/stderr and secrets must not be in error details — characterization tests must expose where this violation currently exists. Std §9: library code uses module loggers, never logs MCP/tool args. Observability Standards: timeline attributes must be pre-redacted by caller.

## Notes



## Ledger Events

- chg_20260710_040722_recipe-system-review-secret-r_1791
