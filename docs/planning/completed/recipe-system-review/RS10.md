---
id: RS10
order: 10
plan: plan-recipe-system-review
state: completed
validate-first: true
priority: P1
work: S
created-by: gpt-5.5-thinking
---

# Simplify provider recipe stamping after characterization

## Description

Small execution slice for RS02. After RS09 characterization tests land, simplify provider result conversion and remove duplicate `ProviderRecipeResult` reconstruction where tests prove it is safe.

Dependency: RS09 must be completed first. Only modify files listed here; RS09 characterization tests are already committed and serve as safety baseline.

## Steps

1. Confirm RS09's characterization tests are committed and green before starting: `python -m pytest tests/unit/providers/test_provider_recipes.py -v`.
2. Open `src/audiagentic/components/providers/services/recipes.py::ProviderRecipeRegistry.install()`. Replace the manual `ProviderRecipeResult(...)` reconstruction in the failure branch (currently inside the `for op in (recipe.install, recipe.configure):` loop, the `if not result.success:` block) with `from dataclasses import replace` at module top, then `return replace(result, artifacts_owned=owned)`.
3. Replace the manual reconstruction after `verified = recipe.verify(ctx)` with `return replace(verified, artifacts_owned=[*owned, *verified.artifacts_owned])` — this mirrors `recipe_contract.py::ProvisioningRecipe.provision()`'s own pattern (`replace(verified, artifacts_owned=[*owned, *verified.artifacts_owned])`) verbatim; the two code paths should now read identically in intent.
4. `ProviderRecipeResult` is declared `@dataclass(frozen=True)` (confirmed) so `dataclasses.replace()` works without further changes to the class.
5. Repeat the same audit for `ProviderRecipeRegistry.uninstall()` — confirm whether its `for op in (recipe.prune, recipe.uninstall):` loop has the same manual-reconstruction pattern; if it returns `result` directly (as observed in this audit) no change is needed there, but verify this against the current file since RS02/RS09 line numbers may have shifted.
6. Run `python -m pytest tests/unit/providers/test_provider_recipes.py tests/unit/memory/test_hindsight_recipe.py -v` — all RS09 tests must remain green with zero test edits (if a test needs editing to pass, the refactor changed behavior and must be fixed, not the test).
7. Whole-repo grep: `git grep -n "ProviderRecipeResult(" -- src/` and confirm every remaining raw constructor call is in `recipe_contract.py`, `providers/services/recipes.py`'s dataclass definition/`ok`/`fail` classmethods, or Hindsight's `_RowRecipe._stamp()`/`_McpConfigAdapter` (which legitimately construct fresh instances, not reconstructions of an existing result) — no other production file should raw-construct one.
8. Run the full test suite touching this file: `python -m pytest tests/unit/providers tests/unit/memory -v`.

## Files

[COMPONENT:providers] `src/audiagentic/components/providers/services/recipes.py`
[COMPONENT:memory] `src/audiagentic/components/memory/hindsight/recipes.py`
[COMPONENT:memory] `src/audiagentic/components/memory/hindsight/recipe_spec.py`
[TEST] `tests/unit/providers/test_provider_recipes.py`
[TEST] `tests/unit/memory/hindsight/test_recipe_spec.py`
[TEST] `tests/unit/memory/hindsight/test_hindsight_recipes.py`

## Validation

1. `python -m pytest tests/unit/providers/test_provider_recipes.py tests/unit/memory/test_hindsight_recipe.py -v` passes unchanged from RS09's baseline (no test file edits in this item's diff).
2. `git diff src/audiagentic/components/providers/services/recipes.py` shows the two manual `ProviderRecipeResult(...)` reconstructions replaced by `dataclasses.replace(...)` calls, net negative line count.
3. `git grep -n "ProviderRecipeResult(" -- src/` output reviewed manually — no new raw-construction call sites introduced by this change.
4. `python -m pytest tests/unit/providers tests/unit/memory -v` passes in full (no regression outside the two targeted test files).

## Effort & Risk

Low risk if RS09 characterization is green. Risk: simplifying stamping may break a custom subclass that relied on undocumented field behavior. Mitigate: RS09 tests catch shape incompatibilities before refactor. If RS09 reveals data-loss bug, fix scope expands to include that correction.

## Standards

Std §2: registry-driven dispatch — remove any remaining if-chains on capability/provider names in result construction. Std §3: consolidate duplicate `ProviderRecipeResult` reconstruction into shared path via `to_result()` or `_stamped()`. Std §8: stamped result fields must remain stable, typed, and redaction-safe after consolidation.

## Notes



## Ledger Events

- chg_20260710_040722_recipe-system-review-secret-r_1791
