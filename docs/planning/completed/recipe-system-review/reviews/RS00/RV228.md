---
id: RV228
review-of: RS00
plan: plan-recipe-system-review
state: closed
reviewed-by: claude-sonnet-5
reviewed-at: ''
---

# RS00-RS19 validation audit: code-checked findings, path corrections, and RS05 scope decision

## Notes

Audited RS00-RS19 against actual code (recipe_contract.py, providers/services/recipes.py, hindsight recipes.py/plugin_recipes.py, provision_steps/shell.py+base.py) and existing tests.

Findings requiring updates:
1. RS02 duplication CONFIRMED: ProviderRecipeRegistry.install() fallback path (recipes.py ~L318-340) manually reconstructs ProviderRecipeResult instead of using to_result(). Real drift risk.
2. RS03/RS11/RS12 duplication CONFIRMED: plugin_recipes.py repeats source_status-gated steps_from_defs/run_steps block near-identically across 3 classes x install/uninstall.
3. RS04 substitution mechanisms CONFIRMED distinct: _substitute (base.py, strict/fail-closed) vs substitute_params (recipes.py/recipe_patterns.py/factory.py, lenient) vs runtime str.format (shell.py). Real inconsistency.
4. RS05/RS15/RS16 leak CONFIRMED: shell.py captures full proc.stdout into StepResult.outputs['stdout'] and 'reason' with zero redaction. Highest-priority finding (security-relevant).
5. RS06/RS17/RS18 CONFIRMED some direct mutations (write_text/unlink in _PluginUrlConfigRecipe, direct json.load/dump in _repair_windows_plugin_mcp) but several look like legitimate documented one-offs per the items' own notes -- expect outcome to be mostly documentation, not replacement.
6. RS07/RS19 largely already satisfied: tests/unit/foundation/toolchains/test_architecture_boundaries.py ALREADY EXISTS and already covers foundation-import boundary checks, MCP-in-config_patcher checks. Re-scope both to 'extend existing suite with the redaction-gate/stamping-guard delta' rather than 'build from scratch'.
7. RS01/RS08 largely already satisfied: tests/unit/foundation/toolchains/test_recipe_contract.py ALREADY EXISTS with lifecycle tests. Re-scope RS08 as a gap-audit merged into RS01 rather than a from-scratch characterization item.
8. RS11 cites a nonexistent test directory tests/unit/memory/hindsight/ -- real tests live under tests/unit/memory/*.py (e.g. test_hindsight_strategy.py already exists). Path must be corrected.
9. RS13 cites tests/unit/foundation/toolchains/test_provision_steps.py (plural) -- actual file is test_provision_step.py (singular). Path must be corrected.
10. SL13 removal of ManagedEntryRecipe/ConfigEntryTarget verified still holds (only a docstring reference in recipe_patterns.py, no reintroduction) -- RS03 validation #5 already passes.

Scope decision on RS05: the unredacted-stdout-capture pattern is NOT unique to provision_steps/shell.py -- the same shape exists in foundation/workflow/invocation/steps.py::ShellStep (RS04's own won't-merge sibling), components/source_control/git_commits.py, provider adapters/hooks that shell out, and harness runners (pi/opencode). An existing but non-reusable redaction primitive already exists at foundation/logging/audit.py::_RedactionFilter (config-driven via cfg.ai_audit.redact_patterns). Rather than widen RS05's own boundary (it explicitly must not change execution semantics and stays recipe-scoped), RS16's redaction utility is upgraded from optional-location to mandatory-shared-location so it can be reused, and a new item OR01 (plan: output-redaction) is created to fan the same primitive out to the other call sites outside recipe-system layering.

## Findings



## Conclusion

Findings incorporated: RS01, RS07, RS08, RS11, RS13, RS16, RS19 updated in place; RS00 order/dependency map updated; new plan output-redaction/OR01 created for the cross-cutting redaction fan-out. Review closed same session.
