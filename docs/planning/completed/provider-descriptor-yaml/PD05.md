---
id: PD05
order: 0
plan: plan-provider-descriptor-yaml
state: completed
validate-first: true
priority: MEDIUM
complexity: simple
---

# Remove legacy Python descriptor modules and update tests

## Description

Remove the legacy Python provider descriptors now that YAML + the shared mechanism are in place. Atomic with the replacement (Rule 11 — no shims, no legacy left, no broken intermediate state).

## Delete
- src/audiagentic/components/providers/adapters/*/descriptor.py (ALL adapters — confirm via iter_modules, not the original list of 11)

## Keep
- adapters/*/catalog.py, adapters/*/hooks.py, adapters/*/surface.py, adapters/*/adapter.py (hook + surface modules referenced by YAML dotpaths or surface registration)
- components/providers/descriptors/base.py (ProviderDescriptor + spec dataclasses — still the typed target the loader builds)
- foundation/components/registry.py, foundation/features/registry.py (OUT OF SCOPE — PD08 D2; do NOT delete)

## Tests
- Unit: resolver (hit/miss/nested), step builder (all 5 types), loader (required-field validation, nested mcp-config), DescriptorRegistry.
- Integration: load every config/providers/*.yaml and assert ProviderDescriptor fidelity (golden compare can be retired once Python descriptors are gone — snapshot the expected field dict instead).
- Cross-system regression (PD08 #5): registering providers populates the feature registry with each provider's ImplementationDescriptor + impl-features.
- _resolve_probe_custom colon patch: existing component-dependency probe tests updated + green.
- Full suite: no regressions.

## Docs
- Update components/providers/descriptors/README.md: YAML descriptor authoring + the foundation/descriptors mechanism + colon dotpath convention.

## Steps

['Delete all adapters/*/descriptor.py (verify none missed via iter_modules)', 'Replace golden-vs-Python comparison tests with snapshot-based YAML fidelity tests', 'Add cross-system coupling regression test (provider register -> feature registry entries)', 'Confirm _resolve_probe_custom colon patch tests are present and green', 'Run full test suite; fix regressions', 'Update descriptors/README.md + any provider authoring docs', 'Confirm foundation component/feature registries untouched', 'Get review on cleanup + tests']

## Files

Delete:
- src/audiagentic/components/providers/adapters/*/descriptor.py
Create/Modify:
- tests/unit/providers/test_provider_yaml_loader.py (snapshot fidelity)
- tests/unit/providers/test_feature_coupling.py (cross-system regression)
- src/audiagentic/components/providers/descriptors/README.md
Must remain unchanged:
- src/audiagentic/foundation/components/registry.py
- src/audiagentic/foundation/features/registry.py

## Validation

- No descriptor.py remains under adapters/ (glob check).
- Full test suite green.
- Feature-coupling regression passes.
- No `from ...descriptors.registry import register` left in adapter packages.
- Docker-gated env tests (per project policy) validated in the image, not on host.

## Effort & Risk

Effort: simple-mid. Risk: low once PD03/PD04 land — deletion is mechanical. Only risk is an adapter whose descriptor.py also performed surface registration; that path must be preserved (covered in PD04). Note: removal must be in the SAME change that proves YAML replacement (atomic migration).

## Standards



## Notes

## Execution guidance for lighter agents

- Do not start PD05 until PD03 and PD04 are complete and all YAML fidelity tests
  pass.
- Delete descriptors atomically; do not leave compatibility shims or forwarding
  `descriptor.py` files.
- After deletion, run glob/grep gates:
  no `adapters/*/descriptor.py`, no `from ...descriptors.registry import register`
  in adapter packages, and no backend-specific provider YAML sections.
- If any adapter descriptor also registered surfaces or hooks by side effect,
  stop and fix discovery wiring rather than keeping the descriptor shim.

## Decisions inherited from PD08 (2026-06-26)
- Do NOT delete foundation/components/registry.py or foundation/features/registry.py — out of scope (D2).
- Add the cross-system coupling regression test (D5/#5).
- Probe tests cover existing prefixes + the colon custom: patch; no `cli` probe type.

## Reviewed 2026-06-26
- Still aligned with current no-legacy rule: delete Python descriptors atomically after YAML replacement proves fidelity; do not keep descriptor shims.
- Hindsight containment is not part of provider descriptor deletion; keep `config/components/memory/hindsight_matrix.yaml` and `components/memory/hindsight/` under the memory implementation.
