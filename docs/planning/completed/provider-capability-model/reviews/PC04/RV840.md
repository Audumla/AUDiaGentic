---
id: RV840
review-of: PC04
plan: plan-provider-capability-model
state: closed
reviewed-by: ''
reviewed-at: ''
---

# Clean — retired-key list needs 21 orthogonal fields explicitly enumerated before VAL-PCAP-013 can be written

## Notes

Code-verified review of PC04. I confirmed the field classification from base.py and verified that the retired-key list in the plan is incomplete.

## Findings

1. RETIRED KEY LIST INCOMPLETE: The plan lists many retired keys but the full enumeration from base.py (ProviderDescriptor, lines 111-289) shows 21 orthogonal fields that must be KEPT:
   provider_id, display_name, description, url, prompt_aliases, cli_probe,
   execution_isolation_tier, mcp_launch_isolation_tier, access_mode,
   skill_surface_path, instruction_file, model_entry_renderer,
   supported_connectors, vendor_key_injection, receive_lsp_mcp,
   launches, execution, interactive, acp,
   annotations, session_surfaces

   The plan mentions most of these but misses: cli_probe (used by list_provider_descriptors), model_entry_renderer (used by model projection), lsp_support_probe (used by self-provided-lsp family). These are NOT capability fields — they stay flat.

2. TAXONOMY DOC GENERATOR: The plan proposes rendering taxonomy.md from the catalogue + descriptors. This is correct and prevents drift. The test asserting committed doc == generated output is the right CI gate.

3. NO BLOCKING ISSUES: Once PC03 completes, PC04 is straightforward grep-driven removal + VAL-PCAP-013 guard.

## Conclusion

Clean. Retired-key list must be completed before implementation.
