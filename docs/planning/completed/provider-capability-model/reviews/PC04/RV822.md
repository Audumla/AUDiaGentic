---
id: RV822
review-of: PC04
plan: plan-provider-capability-model
state: closed
reviewed-by: ''
reviewed-at: ''
---

# Clean — verify retired-key list completeness during PC02/PC03

## Notes

Well-scoped final cleanup. No blocking issues. The only建议 is to have the executor build the complete retired-key list from base.py fields during PC02/PC03 rather than relying on the draft list in this plan.

## Findings

1. CLEAN FINAL ITEM: PC04 is correctly scoped as the cleanup after PC03. Removing the dual-read branch and adding VAL-PCAP-013 to reject legacy keys is the right conclusion.

2. RETIRED KEY LIST NEEDS TO BE COMPLETE: The plan lists many keys but the executor must verify the FULL list against base.py. The plan mentions keeping 'provider_id, display_name, url, description, execution_isolation_tier, mcp_launch_isolation_tier, launches, execution/interactive/acp, access_mode, annotations, session_surfaces'. Missing from the keep-list: `prompt_aliases`, `cli_probe`, `model_entry_renderer`, `receive_lsp_mcp`. The plan says 'confirm the final folded set during PC02/PC03 and pin it here' which is correct — but the draft list should be complete to avoid surprises.

3. TAXONOMY DOC GENERATION IS THE RIGHT END STATE: Making capability-id-taxonomy.md a generated artifact prevents drift. The test that asserts `committed doc == generated output` is the right CI gate.

4. NO BLOCKING ISSUES: This item is contingent on PC03 being complete and green. Once PC03 lands, PC04 is straightforward grep-driven removal + the VAL-PCAP-013 guard.

## Conclusion


