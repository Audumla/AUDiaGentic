---
id: PC05
order: 4
plan: provider-capability-model
state: superseded
created-at: '2026-07-27T04:23:53.996864+00:00'
breadth: ''
skill: 2
created-by: agent
work: M
---

# Derive foundation features from capability instances; retire feature_mapping._CAPABILITIES table

## Description

feature_mapping.py is a THIRD parallel enumeration of provider capabilities (after capability_facts and automation_capabilities): its _CAPABILITIES table re-derives mcp/lsp-support/surface/skills from RAW descriptor fields (d.mcp_config, d.language_servers_config, d.skill_surface_path, d.agent_files/instruction_file) and projects them into foundation FeatureDescriptors (the enable/disable layer). After the capabilities: migration these are the same capabilities the catalogue already declares. Retire the hand-maintained field->kind table and derive impl-scoped features DIRECTLY from the provider's capability instances — each feature-bearing capability kind emits one FeatureDescriptor. This makes capabilities the single source (declaration) and features a thin enable-gated projection.

HARD DEPENDENCY on PC04: feature_mapping reads the raw fields (mcp_config/language_servers_config/skill_surface_path/agent_files/instruction_file) that PC04 deletes. If feature_mapping is not converted in the same coordinated cut, it breaks when the fields are removed. Sequence with / inside PC04's field-removal.

## Steps



## Detailed Solution & Technical Design

1. Mark feature-bearing kinds in the catalogue. The current feature kinds (KIND_MCP='mcp', KIND_LSP_SUPPORT='lsp-support', KIND_SURFACE='surface', KIND_SKILLS='skills') map to catalogue kinds mcp, language-servers, surfaces/instructions, surface(-skill). Add a catalogue attribute (e.g. feature_kind, or derive: any provisioned kind + surfaces/skills) so the catalogue knows which capabilities project to features. Keep feature kind ids stable for the foundation features system (FEATURE_SCOPE_IMPLEMENTATION) unless a rename is coordinated with feature_resolution/manager/registry consumers.
2. Rewrite impl_features_for(descriptor) to iterate descriptor.capabilities (the normalized instances from PC02) and emit one FeatureDescriptor per feature-bearing capability, using the catalogue's feature_kind + the capability's target ids (surface files come from the capability mechanism/derived ownership, NOT the removed agent_files field). Delete the _CAPABILITIES table and the raw-field lambdas.
3. Verify the enable/disable gating in surfaces/manager.py (_active_feature_providers KIND_SURFACE/KIND_SKILLS), feature_resolution.py, and registry.py produces identical feature sets before/after (equivalence test over all_descriptors()).

## Code Samples & Guidance

# BEFORE (feature_mapping.py) — parallel table off raw fields
_CAPABILITIES = (
  CapabilitySpec(KIND_MCP, lambda d: (KIND_MCP,) if d.mcp_config is not None else ()),
  CapabilitySpec(KIND_LSP_SUPPORT, lambda d: (...) if d.language_servers_config else ()),
  CapabilitySpec(KIND_SURFACE, _surface_ids),
  CapabilitySpec(KIND_SKILLS, lambda d: (...) if d.skill_surface_path else ()),
)

# AFTER — derive from capability instances (single source)
def impl_features_for(descriptor):
    features = []
    for cap in descriptor.capabilities:            # normalized instances (PC02)
        kind = catalogue[cap.kind]
        if kind.feature_kind is None:              # not feature-projected
            continue
        for fid in feature_ids_for(cap, kind):     # target ids from the mechanism, not raw fields
            features.append(FeatureDescriptor(parent='providers', kind=kind.feature_kind,
                                              feature_id=fid, scope=FEATURE_SCOPE_IMPLEMENTATION,
                                              implementation=descriptor.provider_id))
    return features

## Files

['src/audiagentic/components/providers/descriptors/feature_mapping.py (delete _CAPABILITIES; derive from descriptor.capabilities)', 'src/audiagentic/components/providers/descriptors/capability_catalogue.py (feature_kind attribute on CapabilityKind, from _capabilities.yaml)', 'src/audiagentic/config/providers/_capabilities.yaml (feature_kind on the feature-bearing kinds)', 'tests/unit/providers/ (feature-set equivalence over all_descriptors before/after)']

## Validation

impl_features_for produces the SAME FeatureDescriptor set (parent/kind/feature_id/scope/implementation) for every provider before and after, asserted over all_descriptors(). Enable/disable gating in surfaces/manager.py, feature_resolution.py, registry.py behaves identically. No reference to the removed raw fields remains (grep). Full provider + coding-lsp suites green.

## Effort & Risk

M / skill 2. Risk: the foundation features system gates real enable/disable behaviour (surface rendering, skill files) — a wrong feature set silently changes what renders. Mitigation: before/after equivalence test over all descriptors is the gate. Must land in the SAME coordinated cut as PC04 (shared raw-field removal).

## Standards

ARCHITECTURE_STANDARDS.md (single source of truth, config-over-code, no field->kind literal tables); features vs capabilities layering (declaration vs enablement).

## Notes

SUPERSEDED 2026-07-28 (PC07 review pass): this item's premise was that feature_mapping._CAPABILITIES reads RAW descriptor fields (d.mcp_config, d.language_servers_config, d.skill_surface_path, d.agent_files, d.instruction_file) that PC04 was going to delete, creating a 'hard dependency' and a '3rd parallel enumeration' problem. That premise is now stale: PC04/PC07 did NOT delete those fields -- they became @property accessors over the unified `capabilities` tuple (the same accessor-preserving pattern used throughout PC07 for cli_probe/lsp_support_probe/launch/models, chosen specifically because Python attribute access can't distinguish a dataclass field from a property, so zero consumer code needed to change). feature_mapping.py's 4-row table therefore already reads through capability-backed projections, not raw storage -- there is no divergence risk left to close. Verified directly: grepped base.py, confirmed mcp_config/language_servers_config/skill_surface_path/agent_files/instruction_file are all live @property definitions backed by _mechanism(kind)/capabilities, not fields. Rewriting impl_features_for to iterate descriptor.capabilities and match by raw kind string (PC05's original code sample) would trade typed accessor names for string matching -- a lateral/negative move, not a precision gain. No code change made. See [[project_provider_capability_model]] for the full PC07 session log this finding came out of.

## Change Log

- 2026-07-27T04:23:53.996864+00:00 (created-by): Created by agent
- 2026-07-28T11:38:18.980134+00:00 (updated-by): Updated: section:notes
- 2026-07-28T11:38:23.996263+00:00 (state-transition): State: pending → superseded
