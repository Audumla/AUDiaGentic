---
id: RV971
review-of: GP14
plan: gpt-auto-conversation
state: closed
reviewed-by: gpt-auto-reviewer-agent
reviewed-at: '2026-08-17'
---

# gpt-auto reviewer assessment: GP14 as coordinating umbrella, AS92/AS102/AS94 own schemas/redaction/live-read

## Notes

External gpt-auto reviewer assessment (req_4e3e5dea76334d6b, session ses_3a638e4afac04af7, outcome success, read via retained ChatGPT conversation). Assessed GP14 against current tree and AS/A2A/ACP targets. Core recommendation: GP14 becomes the coordinating gateway-surface cleanup umbrella — it must NOT invent a second canonical request-status contract beside AS92 nor a second redaction layer beside AS102. V4 remains justified as a real successor to live response-version 3, not a rename of AS92's V3.

## Findings

1) RELATIONSHIP: GP14 = coordinating umbrella; AS92 = sole owner of request-status response schema/version; AS102 = sole public sanitization boundary; AS94 = sole live-turn read envelope; AS99/AS100 = consumers/conformance targets, not schema owners. Do NOT fold GP14 into AS92+AS102 (session_list/list_requests/overview/close/control would lose a cleanup owner). Do NOT keep GP14 independent-implementing (duplicate-contract problem). 2) V3 vs V4: response-version 3 is already live (_PUBLIC_RESPONSE_VERSION: int = 3, gateway/api.py:560) and AS56 made progress-disposition/interruptibility part of it — V3 cannot be repurposed for breaking regroup/rename/remove. AS92 = 'formally specify and validate the response-version 3 contract the runtime already emits' (schema it, do not reinterpret). GP14's cleaned contract = response-version 4. 3) AS92 baseline correction: agent-execution-record.schema.json now pins contract-version const v5 (not v2), no agent-status — it is the durable-record schema, not a response schema; AS92's justification should be 'no dedicated schema for the already-emitted response-version 3 request-status response'. 4) FINDINGS CONFIRMED: huge flat projection (project_public_status ~50 fields, _records.py:522-571); three version namespaces (storage v5, session v3, response v3); provider-turn-pending under 3 paths (_records.py:548-600, progress.py:301-313, session provider.metadata additionalProperties:true at agent-execution-session.schema.json:69-81); latest-transition semantic collision (durable {event,state,timestamp} vs synthesized {state,timestamp}, progress.py:269-281); retired fields gateway-execution-lane-key/resolved-queue-limits still allow-listed (_records.py:535-547) but build_record no longer writes them (drops them at _records.py:418). 5) A2A/ACP (IMPORTANT RESULT — better than feared): neither adapter depends on contract-version, provider.metadata, dispatch, or watchdog fields. A2A uses canonical Agent Context/Work port (a2a/server.py:17-31), state mapping tiny (a2a/mapping.py:7-10); ACP defines AgentsPort over Context/Work/Output, never parses raw gateway status (acp/agent.py:25-37). Acceptance criterion must be 'protocols consume canonical Agents semantic ports, never durable gateway records or gateway-MCP response dicts' — NOT 'A2A/ACP parse V4 dict' (that would be architectural regression). GP14 should add regression/code-ownership test preventing protocols/a2a and protocols/acp from importing gateway store/session persistence/gateway MCP projection. 6) SCOPE: (a) declare ownership boundaries; (b) define V4 public info architecture (one-level functional grouping: identity, lifecycle, execution/provider, progress/activity, session, result/error, timing; remove contract-version/dispatch fencing/worker ownership/admission internals/obsolete lane+queue fields); (c) resolve duplicate semantics (one provider-turn-pending; one authoritative transition concept; runtime-attachment vs durable-lifecycle distinct; no storage/version terms client-facing); (d) bring ALL gateway response families under explicit projection contracts (status, list-request item, session-list/close/resume, overview, control ack — control ack should stay purpose-specific, not return session persistence record); (e) every projection through AS102 — unknown persistence keys must not become public; (f) protocol conformance tests; (g) own V3→V4 negotiation + deprecation (AS92 provides schemas; GP14 defines caller selection + endpoint-family transition). 7) OVERVIEW: active-count is live-attachment count not durable-active (sum of s['live'], api.py:1020-1058) — real misnomer; snake_case (total_requests/by_state/recent_failures/providers_loaded/skipped_count) mixed with hyphen-case. Clean in V4 with explicit runtime-attached-count vs durable counts. Do NOT rename V3's active-count while keeping version. 8) MIGRATION: Phase A formalize existing V3 (AS92 declares agent-status); Phase B explicit V4 opt-in with V3 default, requested version explicit in result, unsupported versions fail (no silent fallback); Phase C extend versioning discipline across list/session/overview; Phase D deprecation window with equivalence tests + recorded removals/renames; Phase E remove V3 after consumers migrate. NUANCE: security redaction need not wait for V4 — an actual secret-bearing field should be removed as documented hardening, not version evolution. 9) REDACTION: _sparse() (gateway_mcp.py:28-47) removes only None/''/{}/[] — NOT a sanitizer; AS102 must operate before it or replace the projection layer. recent_failures[].error (api.py:1000-1014) mitigated by persistence-time _redact_error() to {code,message,kind} + bounded allow-listed detail envelope, schema limits error to code/message/kind/details (agent-execution-record.schema.json:215-226) — classify as AS102 boundary violation (trusts upstream redaction), NOT demonstrated raw-secret leak. SESSION provider.metadata = definite AS102 problem, substantially worse: v3 session store copies arbitrary provider_metadata (sessions_store.py:250-275), schema additionalProperties:true, _public_session_projection (api.py:745-757) redacts binding via public_binding_projection but copies record wholesale → provider metadata survives. Strip by default; promote approved semantics (e.g. unresolved-turn-pending) into named public fields. 10) SH22: no architectural blocker, but sequencing hazard. Correction: SH22 watchdog facts ARE already publicly exposed today accidentally — project_public_status allow-lists last-activity-at/activity-sequence/activity-source/activity-lease-expires-at/watchdog-state/watchdog-reason/watchdog-policy/terminal-classification (_records.py:539-547), get_execution_request copies projection wholesale, _sparse keeps non-empty (e.g. initial watchdog-state 'not-started', _records.py:269-281). No current last-verified-activity-at symbol found — schema defines last-activity-at as gateway-received, never worker-supplied; AS92/GP14 must define whether SH22's last-verified-activity-at is the same semantic fact (no silent rename). V4 should expose only redacted useful subset under activity/health grouping, not full watchdog-policy object. Coordinate SH22 before freezing V4.

## Conclusion

APPROVE GP14 but revise before implementation. Role changes from independent schema-cleaning item to coordinating gateway-surface cleanup umbrella. AS92 = sole request-status schema/version owner; AS102 = sole public sanitization boundary; AS94 = sole live-turn read envelope; GP14 = cross-endpoint coherence, V3→V4 migration, session/list/overview/control cleanup, naming/grouping, protocol-adapter conformance enforcement. response-version 3 stays frozen as today's emitted contract; GP14's cleaned status is response-version 4. ACP/A2A must NOT parse the V4 dict — their canonical-port architecture is correct and should be enforced. Highest-priority AS102 defect = session provider.metadata (arbitrary passthrough), not recent_failures.error. Coordinate SH22 before freezing V4 (accidental watchdog-field leaks become intentional bounded activity/health projection).
