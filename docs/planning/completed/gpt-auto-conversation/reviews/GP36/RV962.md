---
id: RV962
review-of: GP36
plan: gpt-auto-conversation
state: closed
reviewed-by: gpt-auto
reviewed-at: ''
---

# gpt-auto backlog review: split the status-freeze defect from the DOM-recovery feature

## Notes



## Findings

The embedded status-freeze defect is more urgent than the recovery feature itself: stale running state encourages unsafe human/retry decisions. GP44 has since proven at least one 'frozen status' incident was actually a submission-proof false negative caused by DOM normalization, not a generic gateway status bug. Must reproduce the trivial GP36 case on current GP43/GP44 code before concluding the gateway activity path is defective.

## Conclusion

Split into a narrower child: first determine where state stops advancing (turn event emission, transport callback, gateway attempt/activity projection, persistence, or status projection) and reproduce on current code. Then implement DOM recovery (recover_last_exchange) as a separate read-only capability; make it request-correlated using the GP30 request-scoped span resolver, not conversation-latest.
