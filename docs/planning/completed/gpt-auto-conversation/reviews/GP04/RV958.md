---
id: RV958
review-of: GP04
plan: gpt-auto-conversation
state: closed
reviewed-by: gpt-auto
reviewed-at: ''
---

# gpt-auto backlog review: runbook drift between GP04 and GP06/GP31 must be corrected before next restart

## Notes



## Findings

HIGH: GP04 still instructs operators to 'close an orphaned durable source and resume a successor' after gateway death. GP06 explicitly corrects this model: an active durable session whose process-local owner disappeared must remain active and be rehydrated in place (lazily via SessionRuntime.rehydrate_session()), with successor-session resume only a fallback when the durable lifecycle genuinely requires terminalization. Following the stale GP04 wording is a real operational hazard. MEDIUM: GP04's restart procedure must add an explicit distinction from GP31 — GP04 uses clean stop + confirmed exit then start_or_attach; GP31's incident spliced manual polling into an already-failed/draining managed restart. Runbook must say never to splice the two mechanisms.

## Conclusion

Patch GP04 wording before any further restart work: make in-place rehydration the default and restrict successor-resume to genuine terminalization; document that managed restart (CON-MSVC-028) must be retried as a whole, never replaced by a hand-rolled stop/start.
