---
id: RV967
review-of: GP26
plan: gpt-auto-conversation
state: closed
reviewed-by: gpt-auto
reviewed-at: ''
---

# gpt-auto backlog review: dependency-ready (GP09 complete); needs failure-policy detail

## Notes



## Findings

GP09 is completed with schema-migration/default slices live-validated, so GP26 is no longer dependency-blocked. Risk: a bad startup scan can turn observability into gateway unavailability. A stale known-projects.json entry, deleted checkout, offline drive, inaccessible project, or malformed registry must not stop an otherwise healthy machine gateway. Only genuinely incompatible machine-global config should fail startup.

## Conclusion

Ready but preventative rather than urgent. Add bounded-time project scan failures, atomic registry writes, canonicalized project identity, and a visible 'project blocked/incompatible' result rather than making serve_forever() fragile. Implement after GP10.
