---
id: RV961
review-of: GP33
plan: gpt-auto-conversation
state: closed
reviewed-by: gpt-auto
reviewed-at: ''
---

# gpt-auto backlog review: second-highest priority; confirmed not GP43's whitespace bug

## Notes



## Findings

Critical correctness/double-submit risk — lives at the side-effect boundary. GP43 explicitly closed the whitespace-normalization fingerprint bug; GP33 is a different failure class (observed text LONGER than expected: 242 vs 166). Provider send may have happened with wrong correlation/content. Determine what the extra ~76 chars represent: prior composer residue, another user-message node, UI text captured, duplicated prompt content, or genuine malformed submission.

## Conclusion

Investigate second — after GP27 — by inspecting the composer/typing layer (gpt_auto_cdp.py submit()/_submit_once(), turn.py). Do not funnel through more fingerprint tolerance. Governed invariant: once provider-side send may have happened, never auto-retype or resubmit.
