---
id: RV960
review-of: GP27
plan: gpt-auto-conversation
state: closed
reviewed-by: gpt-auto
reviewed-at: ''
---

# gpt-auto backlog review: pivot from crash-timing to provenance detector

## Notes



## Findings

Confirmed P0 and first in the critical path. Crash point #1 already ruled out (record survives normal atomic persistence + process death; recover_gateway_requests() correctly yields interrupted). Strongly shifts suspicion to a later path/lifecycle operation: archive, purge, rename, replace, unlink, rmtree, project-root resolution, retention.

## Conclusion

Implement the provenance detector next: instrument every operation capable of changing the existence/location of request records, logging request_id, resolved project_root, absolute record_path, operation, before_exists, after_exists, pid, owner_epoch. Do not touch GP28 until the mechanism is explained. If a deletion/path-resolution bug is found, fix it with a regression test in the same pass.
