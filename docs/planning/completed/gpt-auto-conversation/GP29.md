---
id: GP29
order: 1
plan: gpt-auto-conversation
state: completed
created-at: '2026-08-16T22:12:47.006957+00:00'
breadth: ''
skill: advanced
created-by: agent
work: S
---

# Add unified ordered DOM message sequence to gpt-auto ChatSnapshot

## Description

Slice 1 of GP08 (see GP08's notes for the full live design consultation) -- the smallest, most concrete, lowest-risk GP08 slice, and the foundation the other GP08 slices (GP24, GP29) depend on. Fixes a real already-merged-code gap: ChatSnapshot's user_message_ids/texts and assistant_message_ids/texts are two separate per-role arrays, not one true DOM total order, so it's currently impossible to distinguish 'A-A then U-human' from 'U-human then A-A' when both appear in the same poll -- exactly the ordering GP08's boundary rule depends on. Also fixes a related latent bug: IDs and text are currently collected via separately-filtered passes, so an empty/transient text node can desync the id/text array pairing.

## Steps

- Walk user+assistant DOM nodes together in one pass in _SNAPSHOT_FN (gpt_auto_cdp.py), producing a single ChatMessageRef{role, message_id, text, sequence} ordered sequence.
- Add ChatSnapshot.message_refs: tuple[ChatMessageRef,...]; derive the existing four arrays from it for compatibility so no other call site needs to change yet.
- Additive only -- does not change turn.py's resolution logic (that's the later _await_response() rewrite slice).

## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files

src/audiagentic/components/providers/adapters/gpt_auto/gpt_auto_cdp.py, src/audiagentic/components/providers/adapters/gpt_auto/snapshot.py

## Validation

A snapshot taken mid-interleaving (assistant message followed by a foreign user message in the same poll) produces message_refs in the correct true DOM order; the four legacy arrays derived from it are unchanged in shape/content from today's behavior for non-interleaved cases.

## Effort & Risk



## Standards



## Acceptance Criteria



## Notes

2026-08-17 IMPLEMENTED AND VALIDATED. Added ChatMessageRef dataclass + ChatSnapshot.message_refs (snapshot.py), rewrote _SNAPSHOT_FN's user/assistant collection into one unified single-pass query over both roles in true DOM order (gpt_auto_cdp.py), deriving the four legacy arrays (userMessageIds/Texts, assistantMessageIds/Texts) from that same unified pass so id/text can no longer desync between them. Two new deterministic tests (test_bridge_snapshot_preserves_true_dom_order_across_roles verifying interleaving order survives the bridge round-trip; caught and fixed a self-inflicted test-authoring bug where an Edit call orphaned an assertion from an adjacent test into the wrong function -- corrected before commit). Full gpt_auto suite (137 tests) passes. LIVE-VALIDATED on gpt-auto-t1 (per user correction: use t1/t2 test profiles, not the real project, for throwaway live tests) -- two sequential turns on session ses_de870fa88ab34665 (req_47e5b80ff09e4e0a, req_bb2028ab4840479a) both completed cleanly post-restart (owner-epoch 08b1643def034f2ab4d74f99acb3e04a) with the rewritten single-pass DOM extraction, confirming no regression to basic turn completion or message counting across multiple turns.

## Change Log

- 2026-08-16T22:12:47.006957+00:00 (created-by): Created by agent
- 2026-08-16T22:25:12.729024+00:00 (updated-by): Updated: section:notes
- 2026-08-16T22:25:12.855954+00:00 (state-transition): State: pending → completed

## Ledger-events

- chg_20260816_222519_added-a-unified-correctly-ord_1164
- 2026-08-16T22:25:19.186996+00:00 (updated-by): Updated: section:ledger-events
