---
id: GP23
order: 4
plan: gpt-auto-conversation
state: completed
created-at: '2026-08-16T22:11:38.730651+00:00'
breadth: ''
skill: advanced
created-by: agent
work: S
---

# Split gateway-client cache invalidation from RPC replay-safety on NET-AGSV-002

## Description

Slice 4 of GP06 (see GP06's notes for full design), independent of the other GP06 slices -- can land in any order relative to them. NET-AGSV-002 (URLError/TimeoutError/OSError) currently only needs cache invalidation confirmed correct via code inspection, but conflates 'reacquire a live gateway client' (always safe) with 'is it safe to replay the RPC that failed' (not always safe -- a timeout doesn't prove the RPC never reached the server).

## Steps

- Keep automatic dead-client-cache invalidation + reacquire/bootstrap unconditional (already correct, no change needed there).
- Add a separate retry policy deciding whether the original failed RPC is replay-safe: reads/status/list calls may retry freely; mutating operations (submit, resume, control, close) need proven idempotency for that specific request identity, or transport-level proof the failure occurred before server acceptance, before being replayed.
- Consider reusing StandaloneGatewayClient._call()'s existing bounded reattach precedent for CON-AGSV-018 as a pattern, even though it's a less exact semantic match.

## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files

src/audiagentic/components/agents/gateway/ (client-side RPC layer)

## Validation

A simulated NET-AGSV-002 timeout on a mutating RPC (e.g. submit) does not get blindly replayed; a simulated timeout on a read-only RPC (e.g. status) does retry automatically after cache invalidation.

## Effort & Risk



## Standards



## Acceptance Criteria



## Notes

2026-08-17 ALREADY IMPLEMENTED, tracking-only closure: found while picking this up for implementation that call_gateway_method() (client.py:342-371) already does exactly this -- unconditional reconnect on NET-AGSV-002 via reset_gateway_client()+get_gateway_client(), but only replays _READ_ONLY_GATEWAY_METHODS (get_execution_request, list_execution_requests, gateway_overview, list_execution_sessions, request_runtime_status, list_agent_contexts, get_agent_context, get_agent_work, list_agent_work); mutating calls re-raise instead of being blindly replayed. Already covered by test_call_gateway_method_reconnects_and_retries_read_only_after_net_agsv_002 and test_call_gateway_method_does_not_replay_mutating_call_after_net_agsv_002 in tests/unit/agents/test_agents_gateway_client.py. This was implemented during the earlier GP06 live investigation, before this item was split out -- no further action needed.

## Change Log

- 2026-08-16T22:11:38.730651+00:00 (created-by): Created by agent
- 2026-08-16T22:26:06.274150+00:00 (updated-by): Updated: section:notes
- 2026-08-16T22:26:06.288385+00:00 (state-transition): State: pending → completed
