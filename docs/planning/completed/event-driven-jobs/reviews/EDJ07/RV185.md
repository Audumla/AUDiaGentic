---
id: RV185
review-of: EDJ07
plan: plan-event-driven-jobs
state: closed
reviewed-by: ''
reviewed-at: ''
---

# job_timeline_path helper should land before consumers, not after — split from P2 observability work

## Notes

Split recommendation: move step 1 (job_timeline_path helper) to EDJ03 as an inline step; leave EDJ07 as P2 standardization that refactors and extends the existing helper.

## Findings

EDJ07 order: 90 (after EDJ05)
Consumers of job_timeline_path: EDJ03 (order 60, step 4), EDJ05 (order 80, step 6)

The item itself is P2 complexity 'mid', but the job_timeline_path helper is a single function in paths.py that returns `.audiagentic/runtime/jobs/<job_id>/timeline.ndjson`. It doesn't depend on any of the broader observability standardization work.

The rest of EDJ07 (aligning control-events, publishing lifecycle events with subject metadata) can remain P2. Only the helper function is a hard dependency for earlier items.

## Conclusion

SHOULD-FIX: EDJ07's order (90) places it after all consumers (EDJ03 at 60, EDJ05 at 80). The job_timeline_path helper is trivial and should land first. Split the helper into a separate P1 item or fold it into EDJ03.
