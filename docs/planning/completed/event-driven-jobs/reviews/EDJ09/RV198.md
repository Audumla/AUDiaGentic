---
id: RV198
review-of: EDJ09
plan: plan-event-driven-jobs
state: closed
reviewed-by: ''
reviewed-at: ''
---

# Documentation completeness and operational runbooks

## Notes



## Findings

1. **Missing touched file: components/agent_jobs/README.md** — Step 1 says 'Update agent-jobs README' but this file may not exist yet (the files list omits it). Add explicit step: create `src/audiagentic/components/agent_jobs/README.md` if it doesn't exist, with event-driven job flow as a section.

2. **Missing impacted component documentation: Agent profiles** — EDJ09 documents the agent-jobs/agents split but does not mention the agent profile resolution chain (`components/agents/agents_api.py`). The `agent-profile-id` field in trigger config flows through: trigger -> context builder (EDJ10) -> gateway event -> agents_gateway_events normalization -> profile resolution. This full chain should be documented in EDJ09.

3. **Missing touched file: AGENTS.md** — The project's `AGENTS.md` managed section describes agent profiles, component profiles, and workflow profiles. Adding event-driven jobs as a new operational concept should update AGENTS.md with a brief reference to the event-driven job flow (or link to the new documentation).

4. **Missing documentation: Operational runbooks** — Step 5 says 'Document failure/retry behavior and where to inspect state' but does not specify which runtime artifacts operators inspect:
   - Job records: `.audiagentic/runtime/agent-jobs/jobs/`
   - Timeline files: per-job `timeline.ndjson`
   - Event store: `.audiagentic/runtime/events/` (if enabled)
   - Gateway request records: separate location
   Add a diagnostics section with paths and commands.

5. **Missing impacted code group: Provider dispatch** — The plan should document how the gateway's provider dispatch (`agents_gateway_dispatch.py`) interacts with event-driven jobs: retry policy, fallback profiles, terminal vs transient error classification. This is observable from the job side via timeline entries.

## Conclusion

Handled: README creation, agent profile resolution docs, AGENTS managed-source note, operational runbook paths, and provider dispatch behavior folded into EDJ09.
