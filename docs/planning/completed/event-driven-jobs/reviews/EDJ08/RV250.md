---
id: RV250
review-of: EDJ08
plan: event-driven-jobs
state: closed
reviewed-by: claude
reviewed-at: ''
---

# Convert propagation evaluation into concrete cancel-propagation implementation

## Notes



## Findings

EDJ05 landed with explicit gateway->job outcome handling, settling the engine-vs-explicit question for that direction. The remaining real gap is the reverse direction: control.py cancels the job but the owning gateway request keeps running/queued (no cancel topic exists on the gateway; it only subscribes to agents.llm.gateway.requested). The evaluation should collapse into (a) a recorded reject-engine decision and (b) implementing job-cancelled -> gateway cancel via a new gateway-subscribed event.

## Conclusion

Rewrite EDJ08 as a concrete implementation item with the decision embedded.
