---
id: RV174
review-of: EDJ05
plan: event-driven-jobs
state: closed
reviewed-by: ''
reviewed-at: ''
---

# Gateway-to-job matching strategy must align with EDJ03 join key

## Notes



## Findings

Step 2 says "Match events to jobs via metadata job id or stored gateway request id" — two options without committing. Matching strategy must align with whatever join key EDJ03 implements.

Step 5 says "Handle missing/terminal jobs idempotently" but doesn't specify how terminal-state check should work (state machine `pending` vs record-level flag).

## Conclusion

Specify the exact matching key by referencing the field name committed in EDJ03 (e.g. `launch_metadata.gateway_request_id`). Implement idempotency via state machine terminal-state check.
