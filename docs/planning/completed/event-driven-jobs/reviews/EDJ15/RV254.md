---
id: RV254
review-of: EDJ15
plan: plan-event-driven-jobs
state: closed
reviewed-by: claude
reviewed-at: ''
---

# Convert filter design gate into concrete payload-filter implementation

## Notes



## Findings

A real consumer now exists: without payload filters, every planning.item.created fires an LLM job regardless of priority — a cost/noise problem the EDJ09 example itself hits. The filter half of EDJ15 has a concrete consumer and should become an implementation item (equality/inclusion match on dotted paths, reusing foundation/templates.py path resolution). Non-event trigger kinds still have no consumer and stay a deferred note, not an item.

## Conclusion

Rewrite EDJ15 as concrete filter implementation; drop the non-event-kinds half to a note.
