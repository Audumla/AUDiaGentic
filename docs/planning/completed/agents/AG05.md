---
id: AG05
order: 5
plan: agents
state: completed
validate-first: true
priority: P1
work: M
---

# Management MCP server for agent profiles

## Description

Two MCP servers following the established two-server pattern (management + operational). See `planning.yaml`: `ag-planning-mgmt` (management) and `ag-planning` (operational).

---

## Steps

['Populate `src/audiagentic/components/agents/agents_manage_mcp.py` (stubbed in AG01) with five management tools (list, get, create, update, delete)', 'Populate `src/audiagentic/components/agents/agents_mcp.py` (stubbed in AG01) with three operational tools (resolve_profile, resolve_default_profile, list_profiles)', 'Add both `mcp-servers:` entries to `agents.yaml`: `ag-agents-mgmt` (propagate: audiagentic) and `ag-agents` (propagate: audiagentic,providers)', 'Add `harness-instructions:` block ONLY for non-derivable doctrine — the tool catalog is auto-generated from mcp-servers[].direct-tools + tool-descriptions at load time (per CREATING_A_COMPONENT.md §7); do not hand-write a What-you-can-do tool list', 'Add `contributions:` block with agent profile doctrine (preferred-targets: skill/instruction)', 'Verify: both MCP servers start without errors, all tools discoverable', 'Verify: create a profile via `ag-agents-mgmt` agent_create_profile, then resolve it via `ag-agents` agent_resolve_profile']

## Files

['src/audiagentic/components/agents/agents_manage_mcp.py', 'src/audiagentic/components/agents/agents_mcp.py', 'src/audiagentic/config/components/agents.yaml']

## Validation

MCP server starts without errors. Tools are discoverable. Create a profile via MCP, then list it to confirm round-trip.

## Effort & Risk



## Notes

Both server modules are stubbed in AG01 and populated here. Use mcp_server(__name__) + run_mcp_server(...) per Architecture Standard §6. Management vs activity split per CREATING_A_COMPONENT.md §6.1: ag-agents-mgmt is CLI-side only (propagate: audiagentic), ag-agents is the capability handed to providers (propagate: audiagentic,providers).
