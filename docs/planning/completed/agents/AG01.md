---
id: AG01
order: 1
plan: plan-agents
state: completed
validate-first: false
priority: P1
complexity: simple
---

# Agent component descriptor and package scaffolding

## Description

Bootstrap the `agents` component following the CREATING_A_COMPONENT.md recipe. Establish the YAML descriptor, package directory, and `__init__.py` so the runtime can discover the component via `register_all_components()`.

## Steps

['Create `src/audiagentic/config/components/agents.yaml` with the descriptor above', 'Create `src/audiagentic/components/agents/__init__.py` (empty package marker)', 'Create `src/audiagentic/components/agents/README.md` with component intent', 'Create `src/audiagentic/components/agents/agents_paths.py` with `agent_profiles_path(project_root) -> Path` returning `.audiagentic/config/agent-profiles.yaml`', 'Create `src/audiagentic/components/agents/agents_manage_mcp.py` (management server — stub, populated in AG05)', 'Create `src/audiagentic/components/agents/agents_mcp.py` (operational server — stub, populated in AG05)', 'Verify: run any component CLI or MCP server start to trigger `register_all_components()` — agents descriptor must load without validation errors']

## Files

['src/audiagentic/config/components/agents.yaml', 'src/audiagentic/components/agents/__init__.py', 'src/audiagentic/components/agents/README.md', 'src/audiagentic/components/agents/agents_paths.py', 'src/audiagentic/components/agents/agents_manage_mcp.py', 'src/audiagentic/components/agents/agents_mcp.py']

## Validation

Run any component CLI command or MCP server start to trigger register_all_components() and confirm the agents descriptor loads without errors.

## Effort & Risk



## Notes

Per §2: package naming follows <id>_api.py, <id>_mcp.py, <id>_paths.py convention. The management server uses <id>_manage_mcp.py (matches planning/planning_manage_mcp.py pattern). The `models.py` module is additional (not in the base template) but justified for data model separation. Per §4: detection-marker auto-derived from id=agents, scope defaults to project. Per §6.1: management server propagate=audiagentic only, activity server propagate=audiagentic,providers.
