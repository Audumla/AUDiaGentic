---
id: RV13
review-of: AG10
plan: plan-agents
state: closed
reviewed-by: Claude
reviewed-at: ''
---

# Provider dispatch — execute_provider is keyword-only; verify result/prompt contract

## Notes

Read providers/services/execution.py and representative adapters (claude, local_openai).

## Findings

LOW (correctness) — execute_provider is keyword-only: execute_provider(*, provider_id, packet_ctx, provider_cfg) at execution.py:45. AG10's step shows a positional call 'execute_provider(provider_id, packet_ctx, provider_cfg)'. Use keyword args; update the step so the dispatch call matches source.

MEDIUM — result normalization: adapters return a dict with keys 'output', 'completion' (completion is a normalized sub-dict), 'status', 'model', 'stdout'/'stderr'/'returncode' (CLI) or 'usage' (api). AG10's 'Normalize provider result into gateway output/completion/usage/status' should key off these actual fields. Note usage is only present for API providers; CLI providers omit it.

MEDIUM — prompt plumbing: adapters read packet_ctx['prompt-body']; ensure the gateway sets prompt-body (not 'messages' — see RV12). model selection comes from provider_cfg['default-model'] or packet_ctx['model-id']/['model-alias'] (local_openai/adapter.py:194), so populate model-id/model-alias in packet_ctx from the resolved profile.

LOW — failure classification: adapters raise AudiaGenticError with provider-specific codes (EXT-CLAUDE-001, EXT-OPENAI-002 HTTP, NET-OPENAI-001, TO-OPENAI-001). AG10's transient-vs-terminal classification should map on these code families / kinds rather than string matching; confirm the taxonomy against real adapter error codes.

## Conclusion

Considered. AG10 updated to use keyword-only execute_provider call, actual adapter packet/result fields, prompt-body/model-id/model-alias plumbing, and AudiaGenticError code-family failure classification.
