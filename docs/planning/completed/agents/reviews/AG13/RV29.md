---
id: RV29
review-of: AG13
plan: agents
state: closed
reviewed-by: ''
reviewed-at: ''
---

# agents.yaml gateway files declaration present but no status_hook or lifecycle_hook registered; README not updated

## Notes

Implementation review of agents.yaml (partial — gateway runtime path is declared). AG13 deliverables: README, status hook, integration tests — none implemented yet.

## Findings

LOW — .audiagentic/runtime/agent-llm-gateway runtime directory IS declared in agents.yaml files section (line 14-16: lifecycle: runtime-only, recursive: true). This is correct per AG08 and AG13.

MEDIUM — agents.yaml has no status_hook or lifecycle_hook registered. If AG13 adds a status hook returning ComponentStatusPayload, it must add to agents.yaml: status_hook: audiagentic.components.agents.<module>.status_handler. Similarly, AG12's lifecycle observer needs lifecycle_hook: audiagentic.components.agents.agents_gateway_events.lifecycle_observer.

MEDIUM — No integration tests exist for gateway flows (submit -> wait -> completed / event -> completed). The plan says 'Add integration test for direct async submit -> wait -> completed using fake provider adapter.' This is a critical gap since queue dispatch + store persistence works correctly in isolation but the end-to-end flow through agents_gateway_api -> enqueue -> dispatch_request has not been exercised.

LOW — README update (gateway purpose, request modes, state model) is untracked. The agents component currently has no README — adding one with gateway documentation is a good practice per Standard 5 (no undocumented behavior).

NOTE — No ledger change event recorded yet despite substantive implementation work on AG08-AG11. Per project doctrine: 'Record a change event fragment after substantive implementation work.' This should be done before considering the gateway slice complete.

## Conclusion

Implemented. status-hook (agents_api.agent_status) and lifecycle-observer (agents_gateway_events) both registered in agents.yaml. README.md extended with a full 'Agent LLM Gateway' section (request modes, state model, params, lifecycle events, known limitations) rather than written from scratch — the file already existed (covering profile management) before this item; RV29's note that 'the agents component currently has no README' was stale by the time this item was implemented. 5 integration tests added in tests/integration/agents/test_agents_gateway_flow.py covering exactly the flows named: async submit->wait->completed, blocking run, event->completed event, disabled-provider and missing-default-profile user-facing errors. Ledger change event chg_20260702_110333_added-an-agent-llm-gateway-su_5267 recorded.
