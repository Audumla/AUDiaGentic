---
id: RV983
review-of: AG16
plan: agents
state: closed
reviewed-by: gpt-auto
reviewed-at: ''
---

# GPT post-review: admission authority, parity, and template snapshot gaps

## Notes

Source request req_f1ddaee11bde4bb8 and pasted full review attachment.

## Findings

Four P1 gaps: prompt profile is resolved in MCP instead of gateway admission; real gateway-shaped packets break default byte parity because None is rendered as empty; prompt profile/template identity is absent from runtime/context fingerprints; only profile ID is snapshotted while template semantics remain mutable. P2 gaps: providers depend on agent_jobs prompt loader, profile registry has two authorities, and AG16 lacks both SH21 delivery-mode tests.

## Conclusion

All four P1 findings are addressed and regression coverage is now green: admission derives immutable template/profile identity, packet model precedence is preserved, agent/config/prompt fingerprints are recorded from the admitted composition, and the exact rendered prompt snapshot is durable for retry/session dispatch. Full tests/unit/agents and tests/unit/providers pass; custom adapter goldens and SH21 delivery tests remain green.
