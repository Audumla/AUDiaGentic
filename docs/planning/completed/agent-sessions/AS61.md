---
id: AS61
order: 61
plan: agent-sessions
state: completed
blocked-by: AS60 reassessment
validate-first: true
priority: P1
work: M
created-by: chatgpt
---

# Add minimal reusable roles

## Description



## Steps

1. Do not deep-characterize or reuse `agent_jobs/prompt_templates.py` as Role's instruction owner (RV889, 2026-08-04) -- that `@tag` prompt-launch mechanism is a dangling feature slated for rework, superseded by harness-native Skill discovery (a harness matches on `trigger`/`description`, not literal `@tag` text). Keep Role's `instructions` field a plain string or opaque reference for now; revisit reuse once agent_jobs' actual rework is scoped. See project memory `project_agent_jobs_rework_pending.md`.
2. Add one simple Role definition and the minimum storage/resolution needed.
3. Support one Role per Agent Definition.
4. Support only capability checks required by demonstrated Roles.
5. Leave runtime tool/MCP/permission policy as an inert optional placeholder/reference; do not resolve or enforce it.
6. Keep management MCP concerns limited to thin CRUD/config adapters over the Role public API.
7. Before registering Role lookup through composition, check whether it is genuinely stateful with a lifetime worth owning (ARCHITECTURE_STANDARDS.md §1 "Composition candidates") -- do not assume it needs a binding the way the AS60 gateway-registry work initially assumed and had to correct (RV888). If it is a stateless plain function like project-local execution-profile resolution turned out to be, it does not need composition at all.
8. Update all callers directly; delete superseded names, paths, handlers, fixtures and tests in the same change.
9. Add architecture checks preventing provider/runtime fields, concrete-store imports and composition lookup inside domain/application code.

## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files



## Validation

- `tests/unit/agents/test_roles.py` (new) — Role model validation, one Role
  resolving against two different Execution Profiles, and one Execution Profile
  backing two different Roles (the reassessment gate below, as an executable
  test rather than a manual check).
- A fake Role lookup is substituted at graph build time without importing
  concrete storage — proves the composition seam.
- `grep -rn "role" src/audiagentic/components/providers` returns no runtime
  tool, MCP or permission field added by this item.
- An architecture test asserts Role domain code imports no concrete store and
  performs no composition-graph lookup.
- `python -m pytest tests/unit` green, with every superseded name, fixture and
  test deleted in the same change (not left alongside).

## Effort & Risk



## Standards

- `arch-standards` §3 Evidence-led scope — a Role carries only fields a
  demonstrated Role actually needs. The runtime tool-policy reference stays an
  inert placeholder with no resolution or enforcement until an owning item
  exists.
- `arch-standards` §3 Ownership and Abstraction — do NOT reuse `agent_jobs/prompt_templates.py`
  as Role's instruction owner (RV889, 2026-08-04): it is slated for rework and superseded by
  harness-native Skill discovery. Keep `instructions` a plain string/opaque reference until a
  stable owner exists to reuse; this is a deliberate narrowing, not a second instruction store.
- `arch-standards` §1 Dependency Boundaries — Composition candidates: check whether Role lookup
  is genuinely stateful with a lifetime worth owning before registering it through composition
  (RV888's corrected reasoning, not the assumption AS60 started with). If stateful, it is built
  by runtime through composition; Role domain code performs no graph lookup and imports no
  concrete store either way.
- `arch-standards` §8 Protocol Adapter Boundaries — management MCP is a thin
  CRUD adapter over the Role public API; it duplicates no validation or
  persistence.
- `arch-standards` §4 Public Contracts — superseded names, paths, handlers,
  fixtures and tests are deleted in the same change; no parallel Role path.

## Acceptance Criteria

- Role is a small reusable behavioral definition.
- Existing harness runtime tool, MCP and permission configuration remains authoritative and unchanged.
- Management MCP remains only an admin/config adapter over the Role public API.
- Runtime tool policy is at most an inert placeholder for a future owning plan.
- No provider/runtime configuration leaks into Role.
- Stateful Role dependencies are built by runtime through the foundation composition bootstrap.
- Role/domain code contains no composition-framework lookup.
- No legacy or parallel Role path remains.

## Notes



## AS59 disposition

Disposition: keep  
Authority: AS59  
Position: Composition track, position 4.

- Sole Role owner. Runtime tool/MCP/permission policy stays out -- inert placeholder only.
- `blocked-by: AS60 reassessment` is correct again now that AS60 is the Execution Profile item.

## Goal

Add a small provider-independent `Role` that can be used with different Execution Profiles.

For this item, a Role contains only:

- instructions as a plain string or opaque reference (RV889, 2026-08-04: not a
  reuse-dependency on `agent_jobs/prompt_templates.py`'s `@tag` mechanism, which is slated for
  rework and superseded by harness-native Skill discovery -- see project memory
  `project_agent_jobs_rework_pending.md`);
- simple required capabilities where a demonstrated role needs them;
- optional output guidance;
- an optional reserved runtime-tool-policy reference with no runtime effect.

A Role does not own or alter:

- management MCPs used by AUDiaGentic CLI/admin;
- runtime MCP servers or tools materialized into a harness;
- harness permissions;
- provider/model selection, session surfaces, queues, runtime behavior or protocol settings.

Each harness continues to use its existing configured/default runtime tools, MCP servers and permissions.

## Boundary

Expose Role lookup through one narrow public API only where current storage/I/O requires it. Keep concrete loading behind that boundary and inject it into callers. Do not add a service wrapper around pure validation or create a tool, MCP, permission or instruction registry.

Management MCP adapters may expose Role CRUD through the owning public API when required by the CLI. That does not make management MCP part of Role execution and must not duplicate Role validation or persistence.

## Foundation composition adoption

If Role storage or lookup is stateful, register its concrete implementation in the foundation composition builder and inject the public Role lookup into Agent Definition resolution and management adapters.

- runtime/bootstrap selects the concrete Role repository/lookup from approved configuration;
- the Role model and pure validators remain ordinary Python and do not depend on the composition framework;
- MCP tools receive the Role public API from their adapter composition root rather than constructing or globally locating it;
- tests substitute the Role lookup at graph build time;
- do not create a Role-specific container, global singleton, decorator or runtime registration path.

## Work

1. Characterize existing instruction handling and reuse its owner.
2. Add one simple Role definition and the minimum storage/resolution needed.
3. Support one Role per Agent Definition.
4. Support only capability checks required by demonstrated Roles.
5. Leave runtime tool/MCP/permission policy as an inert optional placeholder/reference; do not resolve or enforce it.
6. Keep management MCP concerns limited to thin CRUD/config adapters over the Role public API.
7. Register any stateful Role lookup/repository in the foundation composition bootstrap and inject it into current consumers.
8. Update all callers directly; delete superseded names, paths, handlers, fixtures and tests in the same change.
9. Add architecture checks preventing provider/runtime fields, concrete-store imports and composition lookup inside domain/application code.

## Do not add

- copied runtime tool, MCP or permission declarations;
- management MCP definitions inside Role;
- runtime tool-set normalization, merging or overrides;
- runtime tool-policy resolution or enforcement;
- role inheritance or stacking;
- arbitrary merge behavior;
- policy revisions or overlays;
- memory/workflow/delegation frameworks;
- service locators, global registries or compatibility paths.

## Live behavior

Do not add live Role mutation. A different Role applies to a future task or new session unless an existing harness path already supports a safe change without new infrastructure.

## Reassess before AS62

Prove one Role works with two Execution Profiles and one Execution Profile works with two Roles. Remove every field without a current consumer. Confirm current harness runtime tool sets are unchanged and a fake Role lookup can be substituted by a consumer (plain-Python parameter injection at the call site) without loading concrete storage -- Role storage is a stateless plain function, not a composition binding (RV889 / this item's step 7 correction), so substitution happens at the call site, not through the foundation graph. AS62's Agent Definition resolver should receive Role lookup as an injected callable/Protocol rather than importing roles_api directly, so this is checkable there.

## Ro01 Absorbed 2026 08 03 Profile Precedence

The `role-capabilities` plan was consumed into `agent-sessions` on 2026-08-03 (RV886). RO01
("Define agent-role and capability contracts distinct from agent profiles") is superseded by this
item plus AS77.

Most of RO01 was already covered here or in AS64. Two pieces were not, and this item now owns
one of them.

## Owned here: precedence against the other three profile concepts

RO01 step 4 required specifying "how roles combine with agent-profile-id, workflow-profile, and
component-profile, including precedence and conflict errors". Nothing owned that. This item did
not — it defined the Role type and "one Role per Agent Definition", but never said what happens
when a Role and an agent profile disagree.

Add to this item's work:

- State the precedence order between `agent-role-id`, `agent-profile-id`, `workflow-profile` and
  `component-profile`, reusing the resolution-precedence form already documented in CLAUDE.md for
  agent profiles at job launch.
- **Conflicts fail closed, before dispatch.** A Role requirement that contradicts an explicitly
  selected Execution Profile is an error, never a silent override in either direction. RO01's
  Validation required exactly this and it is carried over verbatim in intent.
- Terminology must remain distinguishable from the three existing profile concepts. AUDiaGentic
  already carries four distinct meanings of "profile" (component, agent, workflow, rig model) —
  documented in CLAUDE.md — and Role must not become a fifth by accident.

This is a documentation-and-validation addition, not new machinery. It adds no field to Role.

## Not owned here: the capability vocabulary

RO01 step 2 asked for a typed capability vocabulary spanning components, features, MCP
tools/servers, provider execution capabilities, permissions and resource needs. That belongs to
**AS77**, not here.

This item's "simple required capabilities where a demonstrated role needs them" stays exactly as
narrow as it reads, and its `Do not add` list is unchanged. The inert runtime-tool-policy
placeholder this item reserves is the seam AS77 later fills — AS77 is the "future owning plan"
referred to in this item's Acceptance criteria.

## Why the sole-ownership claim needed correcting

This item's AS59 disposition reads "Sole Role owner". That was asserted against the
agent-sessions set only — AS59's Disposition Table states its own scope in its first line — and
the `role-capabilities` plan was never in view. The claim is now true rather than merely
unchallenged: RO01-RO04 are superseded, and Role semantics live here with capability resolution
in AS77.

## Change Log

- 2026-08-03T02:51:46.189704+00:00 (updated-by): Updated: section:ro01_absorbed_2026_08_03_profile_precedence

## Ledger-events

- chg_20260803_151650_implemented-a-minimal-reusabl_6178
- 2026-08-03T15:16:50.877835+00:00 (updated-by): Updated: section:ledger-events

## Implementation Landed 2026 08 04

**Role implemented, minimal scope, no composition.**

Storage-shape check (revised step 7) resolved cleanly: Role has no shared/gateway split at all -- it is purely project-scoped, stateless config, so it needs no composition binding. Mirrors `ExecutionProfile`'s existing shape:

- `components/agents/roles.py` -- `Role` dataclass (`role_id`, `instructions`, `required_capabilities`, `output_guidance`, `runtime_tool_policy_ref`, `description`), `validate_role`, `role_from_dict`/`role_to_dict`, `RoleStore`. No provider/model/runtime fields (enforced by an architecture test).
- `components/agents/roles_api.py` -- CRUD (`list_roles`, `get_role`, `create_role`, `update_role`, `delete_role`), reading/writing `.audiagentic/config/roles.yaml`.
- Error codes `VAL-ROL-001/002`, `RES-ROL-001/002`, `IO-ROL-001/002` registered in `agents/error-resolutions.yaml`.
- Management MCP: `agent_list_roles`/`agent_get_role`/`agent_create_role`/`agent_update_role`/`agent_delete_role` added to the existing `ag-agents-mgmt` server (reused, not a new server) with tool-descriptions in `agents.yaml`.
- `runtime_tool_policy_ref` is carried as an inert field only -- no resolution, no enforcement, matching the `do_not_add` list.

Tests: `tests/unit/agents/test_roles.py` (model/store/CRUD, 25 tests) including the reassessment gate as executable tests -- one Role's definition carries no provider/model reference so it is trivially usable against any Execution Profile, and one Execution Profile coexists with two independently-defined Roles with no cross-contamination. `tests/unit/agents/test_roles_architecture.py` -- no provider/runtime/MCP/permission field leaked into `components/providers` (frozen baseline of 4 pre-existing unrelated `role` hits -- OpenAI chat-message roles, a DOM attribute, a capability flag -- audited and excluded, not a permissive allowlist to grow), Role domain code imports no composition/concrete-store, Role's dataclass fields exclude provider/model/runtime keys. `tests/unit/agents/test_agents_manage_mcp.py` extended with delegation tests for the 5 new MCP tools. Full `tests/unit/agents` green (97 tests).

**Not built, correctly deferred:** Agent Definition (doesn't exist yet, not this item's scope -- Role is usable standalone), capability-vocabulary resolution (AS77's scope), precedence/conflict validation against Execution Profile selection (documentation-only requirement per the RO01-absorbed section above; no active conflict-checking code exists since nothing yet produces a capability mismatch to check against).
- 2026-08-03T15:11:07.773579+00:00 (updated-by): Updated: section:implementation_landed_2026_08_04
- 2026-08-03T15:11:11.612116+00:00 (state-transition): State: pending → in_progress

## Precedence Doc Landed 2026 08 04

**Remaining RO01-absorbed gap closed: role selection precedence documented.**

Added a second contribution, `agents/role-precedence`, alongside the existing `agents/profile-doctrine` contribution in `config/components/agents.yaml` (surfaces into CLAUDE.md on next `surface apply`, same mechanism the execution-profile precedence doc already uses).

Honest about current state rather than speculative: there is no standalone `agent-role-id` job-request field today -- Role is reached only through `agent_id` -> Agent Definition -> `role_id` (`agent_task_submit`). That field belongs to AS78, not this component. The contribution states the precedence order and the fail-closed conflict rule that MUST hold once AS78 introduces one (agent-role-id first, then role-via-agent-id; execution-profile selection composes independently rather than overriding; workflow-profile/component-profile stay orthogonal to both), so the rule is fixed now rather than improvised later -- exactly the 'documentation-and-validation addition, not new machinery' this item's absorbed RO01 section called for. No conflict-checking code was added, because (as the item's own notes already recorded) nothing today produces a role/profile mismatch to check against.

Initial draft used 'agent-profile-id' and was caught by `tests/unit/agents/test_no_stale_agent_profile_vocabulary.py` (AS60's stale-vocabulary guard) -- corrected to `execution-profile-id`, the current term.

`python -m pytest tests/unit/agents tests/unit/providers/test_surface_contributions.py tests/unit/providers/test_surface_consistency.py` green.

This closes the last open item from AS61's RO01 absorption. Role itself (`implementation_landed_2026_08_04`), the capability-vocabulary boundary against AS77, and this precedence documentation are all now landed.
- 2026-08-04T12:13:46.475449+00:00 (updated-by): Updated: section:precedence_doc_landed_2026_08_04

## Ledger-events

- chg_20260804_121351_documented-how-role-selection_1500
- 2026-08-04T12:13:51.559136+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-04T12:13:53.843919+00:00 (state-transition): State: in_progress → completed
