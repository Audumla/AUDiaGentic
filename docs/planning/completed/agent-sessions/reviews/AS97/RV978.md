---
id: RV978
review-of: AS97
plan: agent-sessions
state: closed
reviewed-by: gpt-auto-reviewer-agent
reviewed-at: '2026-08-17T15:13:09Z'
---

# Adversarial gpt review of AS97 OpenCode ACP proof

## Notes

Adversarial review requested per layering-tranche review pass. Focus: whether behavioural proof (permission/tool/cancel/rollback) for OpenCode ACP is necessary given the thin doctrine or whether provider-native ACP handles these; risk of overclaiming; Docker fixture cost vs value.

Retry 2 (first attempt returned empty output). Substantive.

## Findings

1. **Five-probe scope — partly verified.** The item says "proof, not capability expansion", but the five probes mix provider facts with invariants already owned by foundation/session orchestration. Only two claims clearly require OpenCode-native evidence: whether OpenCode actually emits a permission request that reaches the canonical surface, and whether it actually produces tool-call lifecycle evidence. Cancellation semantics, failed-open atomicity and content gating are not OpenCode capabilities. **RESIZE** AS97 to declaration reconciliation plus those two conditional provider probes.

2. **Permission request/response — partly verified.** A real OpenCode permission-request stimulus is useful because opencode.yaml declares `waiting-permission` under `harness_observability.supported_statuses`. But `test_default_permission_denial` already freezes default-deny and proves a canonical permission-request event; `test_explicit_policy_grant` covers the policy callback. OpenCode's session declaration says `respond-permission: "unsupported"` — a provider probe cannot legitimately expand that control. **RESIZE**: retain only "does real OpenCode emit a request that maps to the declared status?" If a deterministic native stimulus cannot be obtained, remove/mark `waiting-permission` UNPROVEN rather than adding machinery.

3. **Tool-call stimulus — verified.** The strongest probe. `test_prompt_with_tool_events` proves generic normalization (TOOL_REQUESTED/TOOL_FINISHED) but not that OpenCode itself emits them. opencode.yaml advertises `tool-calling`, so the declaration needs provider evidence. **KEEP**, narrowly: native OpenCode stimulus → existing ACP transport → canonical observation. If a deterministic stimulus cannot be demonstrated, undeclare/mark `tool-calling` UNPROVEN.

4. **Cancellation acknowledgement vs terminal — refuted as an AS97 provider proof.** Already frozen in `test_cancel_accepted_does_not_imply_terminal`: ACCEPTED is merely the control disposition; terminality requires correlated terminal/process-death evidence. OpenCode declares `cancel-turn: "unsupported"`. There is no OpenCode capability claim to justify with this probe. **DROP** from AS97.

5. **Failed-open bind rollback — partly verified and mis-owned.** `test_failed_open_unwinds_child` already covers transport cleanup generically. The stronger requirement — "no active record/index entry" — is an agents-owned session binding transaction invariant, not an OpenCode behaviour; proving it against a real provider couples orchestration correctness to provider behaviour. **FOLD** into the generic session-open/bind owner using an injected failing transport; remove from AS97.

6. **Content-channel-disabled — refuted as an OpenCode capability probe.** OpenCode's declared ACP surface has an assistant-text content channel; whether content is admitted into a projection when the channel is disabled is a foundation/session/public-projection gating invariant. **FOLD** into shared content/projection contract tests if lacking coverage; drop from AS97. Do not describe OpenCode assistant text itself as unsupported.

7. **"Surface declaration must exactly match probes" — verified, should be AS97's centre of gravity.** The declaration currently claims `model-thinking`, `tool-calling`, `waiting-permission` as supported while session controls keep `respond-permission` and `cancel-turn` unsupported. AS97 should be primarily a declaration audit with evidence gates, not a five-behaviour feature-validation campaign.

## Conclusion

Shrink AS97 from five probes to two conditional provider probes (tool-call KEEP, permission-request narrow KEEP/RESIZE) plus declaration cleanup. Cancellation DROP, failed-open rollback FOLD into generic session tests, content-disabled FOLD/DROP from provider proof. Materially thinner and follows the item's own "unproven rather than implemented" rule.


