---
id: RV617
review-of: AS06
plan: agent-sessions
state: closed
reviewed-by: claude
reviewed-at: ''
---

# AS06 confirmation: RV590 byte-budget gap is partially stale — caps are tested at foundation ACP level; the real fake-agent EventBus path gap stands

## Notes

T3 verification pass on open RV590.

## Findings

Partial correction to RV590: MAX_PAYLOAD_BYTES/MAX_TOTAL_BYTES ARE exercised in tests/unit/foundation/test_acp_execution.py, so the byte-cap logic is not untested — but RV590 is right that the caps are proven at the foundation ACP unit level, not through the AS06 fake-agent integration path, and no test drives a real fake-agent turn through EventBus publication end-to-end. The distinction matters because the integration path is where cap accounting and terminal-result retention interact. Recommend RV590's narrow extension (real fake-agent run exceeding budgets, terminal result retained, dropped-count asserted, lifecycle + intra-turn events observed on the bus) without duplicating the existing unit coverage.

## Conclusion

Keep RV590 open with its scope narrowed to the integration-path proof; the unit-level cap coverage claim in it should be corrected when triaged.
