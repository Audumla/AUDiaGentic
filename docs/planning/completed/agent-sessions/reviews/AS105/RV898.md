---
id: RV898
review-of: AS105
plan: agent-sessions
state: closed
reviewed-by: ''
reviewed-at: ''
---

# Ownership correction: Compute Resource/Model Instance capacity folded into providers' existing model-sources.yaml, not new agents-owned files

## Notes

AS101's Slice 1 (see RV897) built this item's config_placement_decided_2026_08_05/code_samples exactly as sketched: separate compute-resources.yaml/model-instances.yaml at the user-global tier, owned by agents. The owner then pointed out providers already owns a master model catalog (model-sources.yaml) that projects into every provider's native config. Building a second, agents-owned catalog for the same underlying concept (a named, addressable place work runs, with a concurrency figure) duplicated ownership rather than adding new information.

## Findings



## Conclusion

This item's decided taxonomy (Model Instance/Compute Resource/free-instance dispatch, drain-before-swap, per-project fairness) is unchanged and still governs AS101's dispatch-layer work. Only the storage/ownership half of the design is corrected: capacity (resource-id, concurrency) is now optional fields on a providers-owned model-sources.yaml source, layered user-global+project-local via load_layered_config, not separate compute-resources.yaml/model-instances.yaml files owned by agents. Added a linked correction note; code_samples' CRES/MINST YAML sketches are superseded by it.
