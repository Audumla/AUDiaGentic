---
id: RV871
review-of: AS21
plan: agent-sessions
state: closed
reviewed-by: ''
reviewed-at: ''
---

# Codebase validation: AS21 essentially complete, narrow closure checklist only

## Notes

The 'durable_authority_precedence' adapter the spec calls for (combining AS21 decisions with SH07 durable facts, durable terminal states always winning) already exists as components/agents/agents_status_projection.py -- its own module docstring states 'Durable SH07 state ... always wins -- SH07 is the source of truth for whether/how a request ended.' This was missed by an initial grep for the literal string 'durable_authority_precedence' since the module uses different naming. 20 tests in tests/unit/agents/test_agents_status_projection.py across TestCompletedRecord/TestFailedRecord/TestCancelledRecord/TestInterruptedRecord/TestRunningWithDecision/TestRunningNoDecision/TestQueuedRecord/TestObservedAtFallback/TestScopeInvariant/TestProjectedAt -- this directly covers the durable-precedence scenarios the plan's validation section asks for.

Also confirmed: components/agents/agents_session_lifecycle_projection.py (765 lines) implements project_session_lifecycle() with every rule the plan's projection_rules section describes (empty/rejected/candidate-only -> unknown, mixed-turn/duplicate-sequence -> unknown, turn-started/activity/tool-active -> active, waiting/permission-wait -> waiting, terminal-without-finalization -> completing, terminal-success+finalization-committed+work-cleared -> available as the ONLY path to available). 52 tests in tests/unit/agents/test_agents_session_lifecycle_projection.py plus 36 in test_as21_consumer_slice.py.

## Findings



## Conclusion

AS21's remaining real work is a narrow verification/closure pass, not implementation. Wrote a concrete closure checklist into the item rather than a design plan.
