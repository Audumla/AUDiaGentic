---
id: RV686
review-of: AS28
plan: plan-agent-sessions
state: closed
reviewed-by: codex
reviewed-at: ''
---

# Inventory and remove ACP-specific gateway implementation during neutral-seam migration

## Notes



## Findings

Current implementation has ACP coupling beyond agents_gateway_sessions.py: ACP-specific imports/types and public factory flow; _TurnEventProjector reads ACP ext/raw payloads; _make_on_event_callback uses ACP callback shape; SessionRuntime constructor/open/prompt/request_cancel contract depends on AcpLaunch/AcpResult/AcpSessionTransport/asyncio Event; agents_gateway_dispatch extracts output directly from AcpResult.events; foundation/transports/__init__.py exposes ACP as the apparent default. AS28 needs an explicit removal map, compatibility boundary, and deletion gates so migration does not leave two gateway APIs or raw ACP parsing in agents.

## Conclusion

Add implementation-level refactor inventory and cleanup acceptance gates to AS28, then rebase AS19/AS27 references as necessary.
