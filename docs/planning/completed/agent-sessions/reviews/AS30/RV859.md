---
id: RV859
review-of: AS30
plan: agent-sessions
state: closed
reviewed-by: ''
reviewed-at: ''
---

# Reassessment: named concrete gap in resume_binding/replace_binding mutators

## Notes

Codebase-wide triage (2026-07-28). Verified: foundation/transports/session_binding.py (BindingRelation, ProviderSessionRef, SessionBindingIntent) and agents_gateway_session_bindings.py exist with create_open_binding and attach_binding implemented. Grepped all `def` in agents_gateway_session_bindings.py: resume_binding and replace_binding (step 8's full function set) are NOT found.

## Findings

This item's own existing_baseline_to_preserve/remaining_completion_focus sections already anticipate a partial implementation state, so the item's premise is accurate as written -- but the specific gap (resume_binding/replace_binding missing) should be named explicitly rather than left implicit, since AS49 (resume) and the replacement flow depend on exactly these two functions.

## Conclusion

Update item notes to name the concrete gap explicitly; no change to acceptance criteria or scope.
