---
id: RV873
review-of: AS30
plan: agent-sessions
state: closed
reviewed-by: ''
reviewed-at: ''
---

# Code-level plan for the confirmed resume_binding/replace_binding gap

## Notes

Confirmed RV859's finding precisely and read the exact existing pattern to extend: create_open_binding (L381) and attach_binding (L419) in agents_gateway_session_bindings.py are both pure builders -- they call build_binding() (L60, itself a pure dict constructor keyed by provider_ref_key()) and return the binding dict; they do NOT touch the index themselves. Index registration is a separate step via register_open_binding(project_root, session_record) (L309), which is relation-agnostic (reads binding['relation'] generically, works for any BindingRelation value already). retire_binding(project_root, session_record, state=...) (L356) is the existing retirement primitive. BindingRelation already has RESUMED_FROM and REPLACED members (foundation/transports/session_binding.py) and the v2 schema (agent-llm-session.schema.json) already accepts both relation values plus predecessor-binding-id -- confirmed no schema change needed, only the two builder functions plus their call sites.

Also confirmed no call site exists yet anywhere in agents_gateway_api.py for resume or replace (grepped -- only create_open_binding/attach_binding are referenced elsewhere, resume/replace are wired nowhere). This is consistent with AS49 (resume) and the replacement flow (AS30 step 17) not being implemented at the call-site level either, not just the binding-module level.

## Findings



## Conclusion

Wrote resume_binding()/replace_binding() function bodies directly mirroring attach_binding()'s exact style (same validation-error pattern VAL-AGW-1xx, same pure-builder contract, same reliance on the existing register_open_binding/retire_binding primitives) into AS30's code_samples field, plus the exact call-site sequence needed (retire old binding, build new binding with predecessor_binding_id set, register new binding) and the test scenarios already scaffolded in test_agents_gateway_session_bindings_race.py/_recovery.py that resume/replace tests should follow the same class-per-scenario pattern as.
