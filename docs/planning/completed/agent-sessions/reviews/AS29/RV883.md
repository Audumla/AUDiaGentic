---
id: RV883
review-of: AS29
plan: agent-sessions
state: closed
reviewed-by: codex
reviewed-at: ''
---

# Reduce stale migration guidance to the actual closure delta

## Notes



## Findings

AS29's Steps still instruct implementers to add pi.yaml declarations and obsolete O0/O1 validation fields, but its Notes correctly say the ValidationEvidence migration and declaration already landed. Real-descriptor AS41 tests prepare pi-community-acp through the public resolution API. Production callers use providers_api.prepare_provider_session_transport; remaining direct build_acp_launch calls are adapter/probe/e2e tests.

## Conclusion

Replace the stale implementation steps with a focused closure slice: add/rename a provider-surface integration test owned by AS29 that proves enabled pi resolution through the public API, document why direct adapter calls remain valid only in adapter/probe tests, run focused provider tests and the full unit suite, then complete AS29.
