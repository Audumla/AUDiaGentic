---
id: RV717
review-of: AS29
plan: agent-sessions
state: closed
reviewed-by: claude
reviewed-at: ''
---

# Absorb AS30's identity/mapping facts, resolve the schema self-contradiction, become the sole surface declaration (verified)

## Notes

Source: external plan/code review 2026-07-18; verified 2026-07-19.

## Findings

1) IDENTITY MODEL INCOMPLETE vs AS30 (P2): AS29 defines only open/attach/resume-by-ref/discover; AS30 then extends the resolved surface with ref scope/namespace, identity context, project/context requirements, concurrency, share, replacement — a second contract migration. Required: move all operation-neutral identity and mapping facts AS30 needs into AS29 now. Avoid speculative runtime operations: REPLACED is normally an AG relation implemented by open_new (no native replace_existing capability unless a real surface proves one); share_existing stays documentation/evidence-only until a concrete runtime consumer exists.
2) SCHEMA CONTRADICTION (P3): step 6 creates session-surface-capability.schema.json; step 10 removes it unless an external API needs serialization. Decide now: internal frozen dataclasses only, with an explicit allow-listed public diagnostic projection if needed; no JSON schema until an actual external serialized contract exists.
3) SOLE DECLARATION (P4): AS19's harness_status_observers descriptor list must NOT exist — observer/lifecycle declaration belongs inside AS29 session_surfaces.lifecycle. AS29 is the only runtime session-surface declaration and resolver; AS19 and AS28 both consume its manifest.
4) LEGACY OPENCODE FACTS (P22): the current OpenCode descriptor mixes generic repo-version evidence, live session support, planned ACP resume, a CLI 'run --session' fallback, and shared-live-session claims. AS29/AS32 must split these into exact surface/version/platform evidence; an ACP resume capability and a CLI resume capability are different surfaces and must not appear as one fallback route.
5) STANDARDS SECTION BLANK (P23): cite public provider API boundary, evidence-led scope, one declaration owner, explicit migration/deletion before implementation.

## Conclusion

AS29 is the keystone declaration item; complete its identity model and internal consistency before AS28 factories or AS19 observers are built against it.
