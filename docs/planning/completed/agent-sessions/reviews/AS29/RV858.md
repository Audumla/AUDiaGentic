---
id: RV858
review-of: AS29
plan: agent-sessions
state: closed
reviewed-by: ''
reviewed-at: ''
---

# Reassessment: session-surface declaration/resolver already largely implemented

## Notes

Codebase-wide triage (2026-07-28), cross-checked against an earlier same-session investigation for PC06 (provider-capability-model unification question) that already read this exact code. Verified: descriptors/session_surface_declarations.py (SessionSurfaceDeclaration), foundation/transports/session_surface.py (SessionSurfaceRef, ResolvedSessionSurface), providers/services/session/session_surface_resolution.py (resolve_session_surface), providers_api.py (resolve_session_surface, prepare_provider_session_transport) all exist and match sole_declaration_owner/foundation_value_model/resolver sections closely. Also separately confirmed (PC06 investigation): this declaration shape is intentionally NOT unified with the provider capability catalogue (_capabilities.yaml/capability_catalogue.py) -- the two are structurally different for good domain reasons (typed multi-facet composite with version/platform freezing vs. polymorphic kind/mechanism catalogue) and PC06 should not attempt to force them together.

## Findings

Item text frames the resolver/schema as still to be designed; it's largely built. Remaining real work per validation_and_evidence_rules and the required matrices is probe/validation completion (exact version/platform matrices, Pi RPC/ACP frozen-continuation proof), not schema/resolver design.

## Conclusion

Update item notes with actual completion state and the PC06-boundary finding (so a future PC06 attempt doesn't re-propose merging AS29 into the provider capability catalogue).
