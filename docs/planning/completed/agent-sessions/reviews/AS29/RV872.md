---
id: RV872
review-of: AS29
plan: agent-sessions
state: closed
reviewed-by: ''
reviewed-at: ''
---

# Codebase validation: resolver built and tested, but pi.yaml has zero session_surfaces entries

## Notes

The resolver machinery (resolve_session_surface, SessionSurfaceDeclaration, ResolvedSessionSurface, providers_api.resolve_session_surface/prepare_provider_session_transport) is real, complete, and tested -- confirmed via direct code read: descriptors/session_surface_declarations.py (duplicate-key rejection, 5-rule validate_session_surface_declarations()), foundation/transports/session_surface.py, providers/services/session/session_surface_resolution.py's resolve_session_surface() (project/provider enablement, declaration selection, blocked-declaration check, platform match, validation ceiling, AS27 inventory cross-check, adapter-factory check all implemented, not stubbed).

BUT: grepped every provider config YAML for 'session_surfaces:' and found it populated in ONLY src/audiagentic/config/providers/opencode.yaml (two full entries: opencode-acp, opencode-cli-session, with real platform evidence). src/audiagentic/config/providers/pi.yaml has ZERO session_surfaces entries. 'pi-community-acp' only exists as inventory/evidence bookkeeping in components/providers/services/session/harness_observability_inventory.py (a HarnessSurfaceCapabilityFact with validation_state=PROBE_REQUIRED) -- not as an actual SessionSurfaceDeclaration. This means resolve_session_surface(project_root, 'pi', ...) currently cannot resolve anything for pi -- AS40/AS41's build_acp_launch calls bypass the AS29 resolver entirely (called directly from acp.py, not through providers_api.prepare_provider_session_transport).

## Findings



## Conclusion

AS29's remaining work is concrete and larger than 'probe completion' alone: author the actual pi.yaml session_surfaces entry for pi-community-acp using opencode.yaml's entries as the exact template, using the real Windows + Linux Docker evidence AS40 gathered this session (real pi-acp probe, real concurrency proof, real Docker E2E transcript proof) as the platform evidence rows. Wrote a concrete, code-level plan including the actual YAML to write.
