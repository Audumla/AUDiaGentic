---
id: RV869
review-of: AS40
plan: agent-sessions
state: closed
reviewed-by: ''
reviewed-at: ''
---

# Docker E2E transcript proof passes; tarfile compat fix (increment 5)

## Notes

Dockerfile.pi-rpc-tap-e2e / test_pi_rpc_tap_transcript_e2e.py now pass end-to-end after three fixes surfaced by actually running the image (per this repo's Docker-test doctrine, never by overriding the gate on the host): (1) tarfile.extractall(filter='fully_trusted') requires Python 3.11.4+, but the shared audia-test-base image's system python3 is 3.11.2 -- fixed with a TypeError fallback in runtime/rig/embedded/binaries.py::_extract_tar_gz (this also silently affected the pre-existing Dockerfile.gateway-pi-smoke, not just AS40's new suite); (2) a transient huggingface.co model download hiccup (0 bytes on two consecutive runs, confirmed not reproducible in isolated curl/urllib tests -- genuinely transient, no code change); (3) build_acp_launch's per-request PI_CODING_AGENT_DIR isolation (correct production behavior) meant the real models.json the install had materialized was never visible to the isolated pi process, so pi-acp's session/new rejected with 'Authentication required' before reaching the prompt exchange -- fixed in the test itself (not product code) by copying models.json into the isolated agent dir first, mirroring exactly what agents_gateway_worker._replacement_environment already does for the one-shot dispatch path.

## Findings



## Conclusion

Final run: real pi-acp + real pi + real local embedded model, full session/new -> session/prompt turn, tapped frames span agent_start -> message_update/turn_end -> agent_end as asserted. This closes AS40's validate_first exact-version transcript-fixture requirement. Also closes the required_concurrency_proof acceptance criterion (increment 4, RV868). Remaining AS40 scope: POSIX process-tree cleanup for the real pi child (Job Object adoption is Windows-only), the full crash/timeout/restart cleanup matrix beyond fail-open-on-unreachable-tap, and the AS29 surface capability update -- all still open.
