---
id: RV722
review-of: AS16
plan: plan-agent-sessions
state: closed
reviewed-by: claude
reviewed-at: ''
---

# Narrow to four remaining gaps and close; durable-only list no longer starts a runtime (fixed 2026-07-19)

## Notes

Source: external plan/code review 2026-07-18; verified 2026-07-19. Already FIXED in tree: list_llm_sessions now uses peek_session_runtime and an empty live-ID set when no runtime exists (C12).

## Findings

1) FIRST-TURN LOOKUP GAP (Medium, C11): for a keep-alive request that opens a session, the request record only receives the new session-id at the terminal transition, so request_runtime_status cannot locate the live session during the active first turn. Required: update the owned running request with the session id immediately after durable session open (owner-epoch protected), or add a request-id lookup in SessionRuntime; add a first-turn diagnostic test.
2) NARROW AND CLOSE (P19): remaining scope should be explicit and small — add the closed application/client/service/MCP operation; first-turn request→session lookup (above); immutable snapshot and impossible-count race tests; then close AS16. Do not leave it a broad active item.
3) Note: close_llm_session and reconcile_gateway_state still call get_session_runtime(); the reconcile path is covered by RV721 (AS26).

## Conclusion

Mostly delivered. Trim the item to the enumerated gaps, implement them, and complete the item.
