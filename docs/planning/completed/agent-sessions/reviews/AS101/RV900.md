---
id: RV900
review-of: AS101
plan: agent-sessions
state: closed
reviewed-by: ''
reviewed-at: ''
---

# Slices 2-3 implemented: ExecutionProfile v2 + free-instance dispatch rewrite

## Notes

Full implementation pass covering ExecutionProfile.model_id -> instances (v2 contract), ResolvedExecutionProfile v2 (GatewayExecutionLaneKey retired), and queue.py's dispatch rewrite (InstanceCapacityTracker/_ResourceCapacityTracker replacing per-lane semaphores for gated profiles, drain-before-swap + bounded anti-starvation guard, per-project round-robin fairness). Investigation before coding surfaced true blast radius far exceeding the item's original steps text: gateway/queue/dispatch.py and gateway/session/dispatch.py (both re-resolve the profile fresh and read model_id directly -- 6 read-sites), contracts/execution_context.py's ManifestIdentity (hard-required non-empty model_id at admission, blocking deferred binding), and agent_jobs/prompt_launch.py (a separate legacy resolver with the same contract). All fixed.

## Findings



## Conclusion

Landed with two scope decisions not in the original design: (1) hybrid gating -- a profile's instances are either fully gated (every instance's model-sources.yaml source declares resource-id+concurrency, participates in free-instance dispatch) or fully ungated (none do, exact legacy params.max-concurrency semaphore behavior preserved) -- a mixed set raises VAL-AGW-024; (2) AS15 session two-phase concurrency still gates via the legacy per-lane semaphore even for gated profiles (the resource-tracker slot is held for the whole dispatch lifetime rather than released between turns) -- named limitation, not a correctness bug. Slice 4 (reporting scope: all_queue_depths/gateway_overview project-scoping) remains unimplemented.
