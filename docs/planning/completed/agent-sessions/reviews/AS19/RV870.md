---
id: RV870
review-of: AS19
plan: agent-sessions
state: closed
reviewed-by: ''
reviewed-at: ''
---

# Codebase validation: AS19 essentially complete, narrow closure checklist only

## Notes

Deep validation against the actual tree (not just grepping for names literally matching plan-section headings) found the 'agents evidence sink' AS19 calls for already exists under a different name: components/agents/agents_harness_status_evidence.py::StatusEvidenceSink, which does exactly what the spec requires (scalar allowlist, redacted timeline persistence, exactly-one agents.turn.status.observed publish via get_bus().publish, best-effort). This was missed by an initial grep for 'evidence_sink'/'status-observed' as it uses TURN_STATUS_OBSERVED_TOPIC naming. 22 tests exist in tests/unit/agents/test_agents_harness_status_evidence.py.

Also confirmed: foundation/transports/agent_session.py (511 lines) defines every canonical type the spec lists (TransportObservationKind, SessionControlAction, ControlDisposition, TransportObservation, SessionPrompt, SessionOpenResult, SessionTurnResult, SessionControlRequest, SessionControlResult, AgentSessionTransport) with matching fields -- 74 tests in test_agent_session.py plus 35 in test_acp_agent_session_transport.py. No raw Acp* type imports found in components/agents/ (only 2 stale-looking comments, no actual imports) -- tests/unit/agents/test_as28_architecture_boundary_inventory.py already enforces this via TestAcpTypeImports/TestProviderApiImports/TestAcpExtReads classes.

## Findings



## Conclusion

AS19's remaining real work is a narrow verification/closure pass, not implementation. Wrote a concrete closure checklist into the item (steps field) rather than a design plan, since the design is already built and tested.
