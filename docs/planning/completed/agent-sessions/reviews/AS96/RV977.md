---
id: RV977
review-of: AS96
plan: agent-sessions
state: closed
reviewed-by: gpt-auto-reviewer-agent
reviewed-at: '2026-08-17T15:10:10Z'
---

# Adversarial gpt review of AS96 Pi RPC tap evidence mapping

## Notes

Adversarial review requested per layering-tranche review pass. Focus: whether the Pi RPC tap is worth keeping at all under thin doctrine (evidence enrichment only, never authority); source-tag precedence design; whether unproven frame types should remain TRANSPORT_UNKNOWN or be dropped entirely.

Retry 2 (first attempt returned empty output). Substantive.

## Findings

1. **Pi tap's current incremental value — REFUTED as presently justified.** `rpc_tap_evidence.py` says the only tap observation surviving normalization is ACTIVITY, mapping `agent_start -> ACTIVITY`; AS96 describes that as model-thinking. The real transcript test proves only the presence of agent_start/agent_end/message_update/turn_end — it does not prove agent_start means model thinking, nor inspect a thinking-specific message_update subtype. The one claimed unique semantic gain is stronger than the evidence. **RESIZE**: keep the Pi tap only if a real transcript proves a genuinely unique neutral fact such as an explicit thinking event; otherwise drop AS96's evidence enrichment — ACP already owns the actual session lifecycle.

2. **Making Pi tap observability a first-class generic feature — PARTLY.** The PreSpawnHook/AuxiliaryObservationSource seams are appropriately provider-blind and AS96 correctly confines Pi frame knowledge to the Pi adapter. But one provider + one advisory status enrichment does not justify making Pi-origin precedence, Pi frame taxonomy or Pi observability expectations part of the general session contract. **FOLD**: leave the already-landed generic seams alone; treat Pi tap usage as optional adapter-private enrichment; no generic capability depends on it.

3. **Unproven frame families as future obligations — REFUTED.** The implementation is thinner than the plan: its closed vocabulary contains only the four transcript-proven frame types, with tool_execution_*, compaction_*, queue_update, auto_retry_* as TRANSPORT_UNKNOWN. Describing those families as unfinished AS96 work creates unnecessary scope pressure. **RESIZE**: declare them unsupported/out-of-scope for AS96; a later transcript can justify a later change.

4. **"Source-tag is the necessary remaining blocker" — REFUTED.** The collision is real (Pi counter starts at 1, ACP has an independent sequence domain, StatusEvidenceSink applies one monotonic counter), but adding an origin field + "ACP wins" precedence is not the thinnest fix. Both TransportObservation.sequence and StatusEvidence.sequence allow None and the sink accepts unsequenced evidence carrying no ordering claim. A Pi-local counter is not a canonical turn sequence. **DROP** the source-tag/precedence work: emit auxiliary Pi observations with sequence=None unless Pi supplies the same ordering domain as ACP.

5. **"Source-tag is the only open item" — REFUTED.** Steps 4 (process-tree cleanup/crash-restart coverage) and 5 (surface-evidence declaration) remain. **RESIZE**: after removing the unnecessary precedence blocker, close only the bounded fail-open cleanup/crash proof and make the descriptor declare exactly the evidence actually proven.

6. **Deletable AS96 machinery without losing provider/session capability — VERIFIED.** The source-origin field, ACP-vs-tap precedence policy, speculative mappings for unproven Pi events, and duplicate tap lifecycle interpretation can all disappear without affecting turn execution, resume, termination, output or ACP lifecycle authority.

## Conclusion

Reduce AS96 to "optional Pi-private, fail-open auxiliary evidence for transcript-proven facts". Drop the source-tag/precedence work (use sequence=None); drop speculative mappings; declare unproven frame families out-of-scope. If an explicit thinking signal cannot be transcript-proven, the strongest thin outcome is to drop the Pi evidence tap entirely — the only loss is advisory status richness, not agent capability.


