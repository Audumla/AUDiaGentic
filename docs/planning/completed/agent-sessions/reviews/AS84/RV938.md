---
id: RV938
review-of: AS84
plan: agent-sessions
state: closed
reviewed-by: gpt-auto
reviewed-at: ''
---

# AS84 code validation — claim 5 false, mutability broader than stated, authority not enforced

## Notes

Review performed by gpt-auto agent (req_5d09fca5bcb549d7) against branch ASA-ACP-DI-refactor. All claims verified against code except claim 5 which is false — and worse than stated.

## Findings

HIGH: Claim 5 false — current Role is fully mutable. RoleStore.get() returns stored object itself (not copy). role_api.py mutates _roles directly. Callers retaining Role refs can mutate after passing to add(). Steps 1+2 must be one invariant: immutable Role values + store-only replacement, before deterministic resolver/fingerprint is built.

HIGH: Authority discriminator exists but not enforced — evidence-only capabilities can appear in required_capabilities because requirements are just strings, not typed IDs with authority validation.

MEDIUM: Role contains governance_policy, recommended_tools, mutable metadata — resolver must not silently let these become manifest inputs or fingerprint material. These fields need explicit exclusion or separation from the role requirements consumed by the resolver.

MEDIUM: Step 2 accurately motivated — Role parsing/construction still in role_api, which bypasses store abstraction by mutating _roles directly.

LOW: Claims 1-4 verified correct (13 domains/20 kinds, authority discriminator present, both isolation-tier APIs exist, Role is not frozen/slots).

LOW: Step 8 / AST architecture test fix verified as landed.

## Conclusion

AS84 technical premise sound but claim 5 is false — mutability problem is broader than stated. Steps 1+2 must be one invariant (frozen Role + store-only replacement). Authority discriminator must be enforced at Role boundary, not just documented. Fingerprint must explicitly exclude governance_policy/recommended_tools/metadata fields. Updated AS84 to reflect these corrections.
