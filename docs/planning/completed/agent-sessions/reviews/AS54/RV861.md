---
id: RV861
review-of: AS54
plan: agent-sessions
state: closed
reviewed-by: ''
reviewed-at: ''
---

# Reassessment: proposed probe/evidence matrix already exists untracked as an 'AS27' inventory

## Notes

Codebase-wide triage (2026-07-28). Found src/audiagentic/components/providers/services/session/harness_observability_inventory.py implementing CapabilityFactValidationState, HarnessSurfaceCapabilityFact (with a `recipe` field), and VAL-HINV-001/002 conformance rules -- structurally the same three-axis (declared/validation/effective) model and recipe A/B/C/D matrix this item (AS54) proposes to create. opencode-acp is already marked VALIDATED/O1/Recipe A there with a real probe_anchor at tests/e2e/agents/test_opencode_acp_e2e.py. Checked mcp__ag-planning__plan_get_item('AS27') -- not found (VAL-PLN-001), meaning this inventory was either built and its tracking record deleted, or built outside the tracked plan entirely.

## Findings

AS54's premise ('This plan creates the reusable probe/evidence matrix') is stale -- the matrix already exists in code, just untracked. AS54 should be rewritten to extend/reconcile with harness_observability_inventory.py (fill remaining candidate rows from surface_matrix, formalize/confirm the probe schema matches) rather than build from scratch. This also means AS55 (OpenCode ACP vertical slice) is further along than its own text implies -- see the AS55 review.

## Conclusion

Update item description/notes to reference the existing harness_observability_inventory.py as the baseline to extend, not the thing to build. Recommend also creating a backfilled AS27 record (or a note in AS54) documenting that this inventory exists and is untracked, for future plan hygiene.
