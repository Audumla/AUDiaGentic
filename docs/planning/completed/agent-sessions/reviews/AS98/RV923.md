---
id: RV923
review-of: AS98
plan: agent-sessions
state: closed
reviewed-by: ''
reviewed-at: ''
---

# Deferral justification too vague — replace 'volatility' with concrete platform-scoped gates

## Notes

Two-turn adversarial review, 2026-08-09. Opening position deferred AS98 until 'the core lifecycle and ACP proof path are less volatile'. Reviewer challenged that as unevidenced and cited the adjacent plans as showing the opposite: AS89 records ACP resume-by-ref already proven against real pi-acp, AS90 states most child supervision is already built and narrows the remaining generic problem to one Windows close() race, AS97 records OpenCode ACP already has a validated real e2e anchor. Verified AS98's premise independently against the tree: adapters/codex/acp.py does still fall back to npx for the bridge executable, and its module docstring states model_id passthrough is 'pending empirical verification' — so AS98 has concrete isolated work available now. Also challenged and refined the proposed AS90 gate: AS90 is a narrow Windows-only close() race, so it gates only a Windows deterministic-close/cleanup claim, not the whole item.

## Findings



## Conclusion

Do not park AS98 at the back of the tranche and do not add AS90 as a whole-item Blocked-by. Its substantive work — characterizing CLI vs bridge, managing them as separately probed dependencies, eliminating the npx -y fallback, pinning/validating versions, provider-private launch registration, real model fidelity — is orthogonal to the AS93/94/95 public read/control projections and can run in parallel. Platform-scope the gate instead: if initial promoted evidence is Linux, AS98's own Linux Docker fixture proves close/cleanup independently; only a Windows deterministic-close promotion waits on AS90. AS97 is coordination/shared-evidence, not a hard dependency.
