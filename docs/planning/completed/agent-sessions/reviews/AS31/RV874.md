---
id: RV874
review-of: AS31
plan: agent-sessions
state: closed
reviewed-by: ''
reviewed-at: ''
---

# Codebase validation: real sole-owner violation found (legacy dual-write path), relay is a Stage-2 partial

## Notes

foundation/transports/agent_output.py exists (AgentOutputKind: assistant-text-delta/assistant-final, AgentOutputEvent, OutputSink Protocol, MAX_OUTPUT_TEXT_BYTES=65_536) -- matches the plan's minimal event shape closely, though it has a single scalar `sequence` field rather than a source-sequence-range. agents_gateway_output.py exists labeled 'AS31 Stage-2' (OutputPolicy, OutputRelay(OutputSink), per-turn registration, sequence monotonicity, fragment/turn byte caps, atomic per-event persistence via atomic_write_json, graceful degrade-on-failure, read_output_events/read_output_index). Storage layout confirmed real: agents_paths.py:130-156 (gateway_output_dir/events_dir/index_path/lock_path).

Concrete, confirmed sole-owner violation: agents_gateway_dispatch.py:32-40 defines _ENV_STREAM_OUTPUT/_write_output_chunk (appends to <request-dir>/output.ndjson, env-gated AUDIAGENTIC_GATEWAY_STREAM_OUTPUT, off by default), called at L323 in the terminal-success branch alongside the durable final-output write. This is exactly the pattern AS31's dispatch_migration section says to remove ('any output-file write outside agents_gateway_output.py'). Low blast radius to remove (opt-in, off by default, no test found gating on it).

OutputRelay gaps confirmed by direct read: no AS29 channel/kind authorization, no coalescing (byte/time/final-boundary), no max_events cap, no delivery-state (active/closed/degraded) in the index, no 'adopt contiguous events exactly once' crash-recovery logic on reopen, event filenames use raw sequence not the plan's zero-padded scheme (low-priority cosmetic gap). No test file exists at all for agents_gateway_output.py/OutputRelay -- only test_agent_output_contract.py (40 tests) covering the foundation event type, not the relay. read_llm_request_output (with cursor/limit/SH04 lease auth) does not exist; read_request_output exists but lacks cursor/auth. No final-output selector unifying the durable request-record output field with the sidecar relay exists yet.

## Findings



## Conclusion

Wrote a concrete, prioritized remaining-work list into AS31's steps field: (1) remove the legacy dual-write path -- smallest, highest-value fix; (2) add AS29 channel authorization to OutputRelay's registration; (3) add coalescing; (4) add crash-recovery adoption logic; (5) build the final-output selector; (6) build read_llm_request_output with cursor auth; (7) write the missing OutputRelay test suite. Ordered by dependency and risk, not by the plan's original section order.
