---
id: RV875
review-of: AS41
plan: agent-sessions
state: closed
reviewed-by: ''
reviewed-at: ''
---

# Code-level plan for tap-evidence mapping, grounded in existing ACP mapper pattern and pi-acp-status-mapping.md

## Notes

docs/reference/PROVIDER_CAPABILITY_REFERENCE/harnesses/profiles/pi-acp-status-mapping.md (pre-existing, already written) documents that the AS19/AS21 evidence pipeline for Pi ACP is currently fully SKIPPED end-to-end -- not partially wired -- because Pi is not validated in AS27's harness_observability_inventory.py (matches this session's AS29 finding: pi.yaml has zero session_surfaces entries). The doc's own 'Path A' (§3.3) is the smallest fix: validate pi-rpc/pi-community-acp in the inventory, declare a HarnessStatusObserverCapability for Pi, and the existing agents_gateway_sessions.py._open_session()/_observation_sink() wiring picks it up with NO further code change -- it already calls providers_api.open_harness_status_observer() generically.

The existing ACP-to-observation mapper (foundation/transports/acp.py:856, _map_acp_event_to_observation) is the exact pattern AS41's tap-frame mapper should follow: closed-vocabulary kind mapping, bounded attribute allowlist, unknown kinds degrade to TRANSPORT_UNKNOWN with no attributes rather than leaking raw payload. The doc's own gap table (§4.2: file-change/terminal-output/plan-update/fine-grained tool-call status all currently drop to TRANSPORT_UNKNOWN via the ACP path) is precisely the gap AS41's richer RPC tap evidence (proven this session via AS40 increments 1-5, capturing real agent_start/message_update/tool_execution_start-update-end/turn_end/agent_end frames) can fill.

## Findings



## Conclusion

Wrote AS41's guidance to sequence explicitly after AS29's pi-community-acp descriptor lands (hard blocking dependency, not yet satisfied) and to reuse the existing _map_acp_event_to_observation pattern for the new tap-frame mapper, with the critical correlation constraint (tap evidence must reuse the SAME turn_id the ACP transport already tracks, never open a second correlation identity) called out as the top implementation risk.
