---
id: RV936
review-of: AS92
plan: agent-sessions
state: closed
reviewed-by: agent
reviewed-at: ''
---

# AS92 — gpt-auto review assessment (RV928 findings validated against tree)

## Notes

External review from gpt-auto agent (req_da8651408ec047cf) assessed claim-by-claim against the codebase.

## Findings

All 6 substantive claims confirmed correct:

1. REJECTED mapping gap — CONFIRMED. _DURABLE_OUTCOME_MAP in status_projection.py has completed/failed/cancelled/interrupted only. Workflows.yaml declares rejected as terminal (gateway-request state-sets). No test coverage for rejected exists.

2. Local _now_iso() — CONFIRMED. Lines 38-40 of status_projection.py define a private _now_iso() instead of importing foundation.time.now_iso_z. The function is identical to foundation.time.now_iso_z and used 3 times in the same file.

3. Status attachment duplication — CONFIRMED. _attach_agent_status() (api.py:216-234) gets session runtime + decision lookup + snapshot_for_request. get_execution_request() (api.py:521-540) does the exact same inline logic, duplicating peek_session_runtime() and latest_lifecycle_decision.

4. Async submit/list don't return agent-status — CONFIRMED. Async submit returns record directly (api.py:~line 615). list_execution_requests (api.py:683-700) returns store.project_public_status() without any snapshot_for_request call. Only blocking submit (_enrich_terminal_result → _attach_agent_status), get_execution_request, and wait paths include agent-status.

5. Metadata exposed unrestricted — CONFIRMED. project_public_status(_records.py:370-391) includes "metadata" in its visible fields tuple with no allowlist or sanitization applied.

6. Response version ambiguity (v2 vs v3) — CONFIRMED. _PUBLIC_RESPONSE_VERSION = 2 (api.py:518). Durable contract-version = "v2" (_shared.py:28). Plan says "response schema at response version 2" then "increment the response schema explicitly." Inconsistent. Response version 3 is cleaner for a new formalized public status schema.

Conclusion: BLOCK is justified — the item's implementation assumption that only schema/version/tests remain is factually wrong. Steps 1-3 and cross-path agent-status are still real work.

## Conclusion

APPROVE-FINDINGS-ACCURATE. All claims validated against tree. Item should be updated to retain these remaining tasks.
