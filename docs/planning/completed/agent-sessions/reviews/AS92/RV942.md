---
id: RV942
review-of: AS92
plan: agent-sessions
state: closed
reviewed-by: gpt-auto-reviewer-agent
reviewed-at: ''
---

# gpt-auto review of AS92 - ~80% built, 3 HIGH response-path gaps

## Notes



## Findings

**Verdict:** ~80% built already. Almost implementation-ready but not quite. Stays S/intermediate.

**Confirmed as correct:** Core projector built (snapshot_for_request with durable-terminal precedence, queued→pending, decision fallback, conservative unknown, interrupted mapping). 21 tests exist covering every mapping except rejected. AgentOutcome.REJECTED exists - Step 1 just completing durable mapping. _now_iso cleanup real and trivial. Schema gap absolutely real - agent-status declared in no schema on v2 response.

**HIGH gaps found:**
1. Step 3/4 misses cancel responses. cancel_execution_request() returns queue manager's durable record directly with no response-version or agent-status attachment. Gateway application explicitly exposes cancel alongside submit/get/wait/list as public operation. Fix: Step 3 should say "one project_request_response(...) function used by submit, run/blocking submit, get, wait, cancel and list."

2. Blocking submit timeout doesn't get agent-status. _enrich_terminal_result() calls _attach_agent_status() only if record is terminal. GatewayQueueManager.wait() returns current non-terminal record when timeout expires. That's path divergence AS92 exists to eliminate. Fix: don't special-case terminal enrichment as status attachment boundary; canonical public response projection must run regardless of lifecycle state.

3. Async submit bypasses public projection entirely. After enqueue, async submit returns durable record rather than project_public_status(). enqueue() returns store.read_record(...) - private durable shape. Step 4 should not be implemented by merely doing _attach_agent_status(record) because that would decorate a private record with a public field. Correct change: all request-returning APIs go through same public request projection first, then attach canonical status snapshot.

**MEDIUM findings:**
- Step 4 not "largely satisfied" - wiring is closer to half done (get/wait/terminal blocking submit yes; nonterminal timeout/async submit/list/cancel no)
- Schema ownership needs explicit file placement: authoritative schema under components/agents/contracts/, byte-identical mirror under foundation/contracts/schemas/ if registry validation required
- Step 6 mostly specification work - AgentStatusSnapshot already explicitly defines scope/lifecycle/projected_at/outcome/wait_reason/decisions/request/session/turn IDs/generation/attempt/observed_at; enforces lifecycle/outcome invariants. Should be schema encoding of existing type, not new shape

**Metadata handling:** AS92 can omit metadata from v3 responses unless AS102's canonical public metadata projector already landed. No temporary AS92 allowlist needed.

**No dependency on AS102 if metadata simply omitted in v3.**

## Conclusion


