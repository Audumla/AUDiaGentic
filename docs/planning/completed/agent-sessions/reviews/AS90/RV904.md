---
id: RV904
review-of: AS90
plan: agent-sessions
state: closed
reviewed-by: codex
reviewed-at: ''
---

# AS90 Windows lifecycle review

## Notes



## Findings

The named close path is incorrect: the defect is in AcpSessionTransport.close(). The real subprocess test passed 10/10 on the current Windows host, but code still kills after timeout without a final await proc.wait(). The plan's reproducible-failure premise and baseline-exception removal are stale.

## Conclusion

Reframe as deterministic lifecycle hardening with a fake timeout/kill/reap test, then repeated real-process confirmation. Reuse one bounded terminate-wait-kill-reap helper where semantics match.
