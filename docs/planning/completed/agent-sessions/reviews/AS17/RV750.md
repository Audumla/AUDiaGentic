---
id: RV750
review-of: AS17
plan: agent-sessions
state: closed
reviewed-by: codex
reviewed-at: ''
---

# AS17 delegated foundation-slice review

## Notes



## Findings

The smaller AS17 Windows-safety repair correctly added external-child Job Object guards and closed the SetInformationJobObject failure handle; tests prove those new invariants. Independent suite still failed 2/20 because the new adoption implementation/tests simulate POSIX by patching `os.name` on a Windows host, then call unavailable `os.getsid`. Portability repair `req_83d024c5f5cd4d38` is queued with a small capability-guard/test-only scope. AS17 remains open; the separate event-schema and deeper PID-reuse characterization work is still outstanding after this foundation slice.

## Conclusion

Foundation-slice corrections are now independently validated on the real Windows host: `pytest -q tests/unit/foundation/system/test_adopted_process.py tests/unit/foundation/system/test_process_signal_boundaries.py` passes 20/20. External children are not assigned to or closed through a kill-on-close Job Object; Job Object setup closes its handle on configuration failure; test portability is fixed. Deeper PID-reuse characterization/event schema work remains future AS17 scope, and reaper integration/POSIX hard-death Docker proof remain AS26/deferred.
