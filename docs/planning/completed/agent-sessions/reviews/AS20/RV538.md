---
id: RV538
review-of: AS20
plan: agent-sessions
state: closed
reviewed-by: opencode
reviewed-at: ''
---

# AS20 dependency and atomic re-acquisition review

## Notes



## Findings

1. **Implicit dependency on AS15 being correct** — AS20 adds pq.llm_running as a second dimension to profile concurrency, but the O2 accounting (pq.running) must be proven stable first. Add explicit dependency on AS15 completion and validation.

2. **Atomic re-acquisition risk** — The requirement that model.started after a tool "re-acquires atomically before model work continues" is correct in principle but needs careful implementation. If the LLM slot isn't available (another O3 session took it), should we block? Fail the turn? This edge case needs explicit handling.

3. **Feature flag is necessary** — Default-off until two-session tests pass is the right approach for a complex, event-driven optimization.

## Conclusion

No blockers; one dependency clarification needed
