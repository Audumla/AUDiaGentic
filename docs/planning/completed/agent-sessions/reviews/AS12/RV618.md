---
id: RV618
review-of: AS12
plan: agent-sessions
state: closed
reviewed-by: claude
reviewed-at: ''
---

# AS12 confirmation: RV591 correlation finding endorsed; fold topic constants into the same fix

## Notes

T3 confirmation of open RV591 — no re-litigation, one prioritization note.

## Findings

CORRECTION 2026-07-18 (second verification pass): RV591's primary claim is now STALE against the tree — _publish_session_event accepts correlation_id and all five session lifecycle publications (opened, turn-finished, failed, closed, expired) thread it through handle/record correlation context (agents_gateway_sessions.py:468, :594, :616-624, :658-666); the _close timeline event also records correlation-id. What REMAINS valid from RV591 is the topic-constants point: the session (_SESSION_TOPIC_*) and turn (_TURN_TOPIC_*) constants are module-private in agents_gateway_sessions.py while the request-event topics are public in agents_gateway_events.py (REQUESTED_TOPIC etc.) — subscribers (AS20's slot re-acquisition, AS25's future correlation queries) must either import private names or restate string literals. Export the session/turn topics from an agents-owned public constants location alongside the request topics.

## Conclusion

Resolved. All agents-owned request, lifecycle, session, and intra-turn topic strings now live in the side-effect-free `agents_event_topics.py` owner module. Publishers/subscribers import those constants, and the deprecated private observer aliases were deleted. Focused agents/event validation passes (67 tests).
