---
id: AS07
order: 7
plan: plan-agent-sessions
state: completed
validate-first: false
priority: P1
work: S
created-by: claude
---

# Live e2e validation on brutus/opencode: two-turn context retention + cleanup verification

## Description

Prove the slim slice against the real opencode ACP agent (brutus, coder-quality-mid): open a keep-alive session, ask it to remember a token, second request in the same session must recall it; then close and verify no opencode process remains; separately let a session idle-expire and verify the reaper closed it and the record shows close-reason idle-timeout.

## Steps

1. Keep-alive run: 'Remember the token ZEBRA-42…' → capture session-id.
2. Continue: 'What token did I ask you to remember?' with session-id → response must contain ZEBRA-42.
3. agent_llm_session_close → verify process gone (tasklist/pgrep on host), session record closed/client-request.
4. Idle test with a short idle-timeout (e.g. 60s) → verify expired record + no process.
5. Record findings as a review on AS02/AS04 if defects found.

## Files

no code — operational validation; findings recorded as plan reviews

## Validation

Both context retention and both cleanup paths observed on the live agent.

## Effort & Risk

Simple/mid execution effort. Risk: opencode ACP mode behavioral surprises (permission requests mid-turn under default-deny) — capture and file as reviews.

## Standards



## Notes

PASSED LIVE 2026-07-17 (Claude, via restarted ag-agents-gateway MCP server, profile deep-coder-opencode on brutus).
1. Context retention: keep-alive session ses_8596bfdd8ef74897 — turn 1 stored ZEBRA-42-KUMQUAT (97s incl. spawn), turn 2 in same session recalled it exactly (10s — no respawn, same provider-session-ref ses_092e184d8ffe…). Record shows turn-count 2, both request-ids linked.
2. Explicit close: agent_llm_session_close → state closed/client-request; the opencode child spawned at session open (pid 81976) confirmed GONE; unrelated pre-existing opencode processes untouched.
3. Idle expiry: session ses_c10731fe26cd4f05 with idle-timeout 60s → reaper expired it 61s after last activity (state expired, close-reason idle-timeout); child process confirmed gone.
4. DEFECT FOUND AND FIXED: _session_output_from_result joined agent_message_chunk fragments with '\n', corrupting output ('TOKEN\n STORE\nD\n.'); fixed to ''.join with a pinning test (test_session_output_concatenates_stream_chunks). NOTE: the running MCP server predates this fix — session outputs remain chunk-mangled until the next server restart; context retention and cleanup are unaffected.
Earlier same-day corroboration: the codex overseer MA28 delegation ran a full real work cycle through this path (terminal end_turn, cleanup verified).

## Ledger Events

- chg_20260716_225419_live-agent-sessions-passed-the_2406
