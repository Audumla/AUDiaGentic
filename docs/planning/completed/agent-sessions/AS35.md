---
id: AS35
order: 35
plan: plan-agent-sessions
state: completed
validate-first: true
priority: P2
complexity: simple
created-by: codex
---

# Redact protected binding internals from session close results

## Description

`agent_llm_session_close` currently returns the full session record from close paths, including protected binding internals, while session list/overview use redacted binding projection. Align close-result output with public session projection so controller/tool surfaces do not expose provider session refs or full provider-ref-key values.

## Steps

1. Reproduce with a closed live session and inspect the public close result; do not copy protected refs into docs. 2. Add or reuse a public session projection helper so `close_llm_session` returns the same redacted binding shape as `list_llm_sessions`. 3. Preserve durable record storage unchanged; only public API/MCP/client output is redacted. 4. Add tests proving close results omit `provider-session-ref` and full `provider-ref-key` while retaining `provider-ref-key-prefix`, session id, state, close reason, and turn count. 5. Check idempotent close of already terminal and stale non-live records. 6. Record ledger and update agent capability reviews with any delegation findings.

## Files

src/audiagentic/components/agents/agents_gateway_api.py, agents_gateway_session_bindings.py or sessions_store projection helper, tests/unit/agents/test_agents_gateway_sessions.py, MCP/client tests if needed.

## Validation

Controller validation: `.venv/Scripts/python.exe -m pytest tests/unit/agents/test_agents_gateway_sessions.py tests/unit/agents/test_agents_gateway_recovery.py tests/unit/agents/test_gateway_service_application.py tests/integration/agents/test_gateway_e2e_states.py -q` -> 58 passed. Gateway cleanup after worker cancellation: request `req_4a8a1dd00e684e9e` cancelled, queues empty, session active-count 0.

## Effort & Risk



## Standards



## Notes

Implemented 2026-07-19. Added shared public session projection so list and close public paths redact protected binding internals while durable session records retain provider refs and full provider-ref-key for ownership/recovery. Controller repaired worker tests by adding full provider-ref-key leak assertions for idempotent and stale close paths. Delegation review recorded in docs/research/agent-capability-reviews/2026-07-19-as-gateway-worker-reviews.md. Harness finding: the already-running MCP/gateway process returned the pre-change raw close shape during cleanup, indicating loaded gateway code can remain stale until restart; source-level API tests validate the corrected behavior.

## Ledger Events

- chg_20260719_025012_planned-a-follow-up-to-redact_3244
- chg_20260719_025704_session-close-results-now-reda_6044
- chg_20260719_025825_session-close-results-now-use_4396
