---
id: AS103
order: 23
plan: agent-sessions
state: superseded
created-at: '2026-08-06T23:30:59.930501+00:00'
breadth: ''
skill: intermediate
created-by: agent
work: S
---

# Cursor and control-token integrity for paged reads

## Description

Make paged reads fail clearly on a malformed, stale or mismatched cursor, so a caller can distinguish a service restart from a bad request.

**Rescoped 2026-08-05 after the deployment model was stated: local, internal, non-hostile users, not externally facing.** This item previously specified tamper-evident cursor tokens and replay-safe control keys. Against a non-hostile local caller there is nothing to make tamper-evident — signing a cursor defends against an attacker this deployment does not have, and `arch-standards` §3 forbids building for requirements that do not exist.

What survives is the robustness half, which was always the genuinely useful part: a cursor that no longer means what the caller thinks should produce a clear typed error or a deterministic gap, never silently wrong data.

## Steps

1. Reject malformed and oversized cursors before parsing, with a registered code.
2. Distinguish the failure modes with distinct codes so a caller can act on them: malformed input, a generation that no longer matches (service restarted), and a cursor belonging to a different request/session/turn.
3. Keep the deterministic-gap behaviour for the restart case. A valid cursor from a prior generation is a normal, expected outcome on a shared long-lived gateway — it returns a gap, not an error.
4. Bound page size and cursor length.

## Detailed Solution & Technical Design

Deliberately **not** in scope, with the reason recorded so it is not re-added by habit:

- Cursor signing, HMAC or any tamper-evidence mechanism.
- Replay protection on control idempotency keys.
- Any foundation crypto/integrity seam introduced for this purpose.

All three defend against a caller forging or replaying a token. The stated deployment has no such caller. If the gateway ever becomes externally facing, raise that as its own item against that requirement.

The idempotency keys themselves stay — they prevent *accidental* double-dispatch on retry, which is a real failure mode with cooperative callers. It is only the anti-replay hardening that is dropped.

## Code Samples & Guidance



## Files

Retired — no files are owned by this item. See `dissolved_2026_08_09` for where each requirement now lives (AS93 / AS94 / AS95).

Explicitly **not** created: the "one foundation integrity helper" this section previously listed. That entry contradicted this item's own Detailed Solution and is withdrawn.

## Validation

- A malformed or oversized cursor is rejected before parsing, with a distinct registered code.
- A cursor from a prior generation returns a deterministic gap, not an error — this is the common case after a gateway restart and must not surface as a failure.
- A cursor naming a different request, session or turn is rejected with its own code, distinguishable from the restart case.
- Page size is clamped.
- No signing or integrity mechanism was added.

## Effort & Risk

Small. The risk is scope creep back toward integrity machinery — the Detailed Solution's exclusion list is the control.

## Standards

- `arch-standards` §3 Ownership and Abstraction — one integrity mechanism, not one per surface.
- `arch-standards` §4 Public Contracts — distinct registered codes for distinct failures.

## Acceptance Criteria

- Malformed, stale and mismatched cursors each produce a distinct, registered, actionable outcome.
- The restart case remains a deterministic gap rather than an error, preserving the behaviour the read surfaces depend on.
- Page size and cursor length are bounded.
- No cursor signing, HMAC or replay-protection mechanism exists — asserted by absence, since the temptation is to add it "while we're here".
- Idempotency keys still prevent accidental double-dispatch.

## Notes

Rescoped from the original tamper-evidence framing. Now small enough that it could reasonably fold into whichever of the metadata-polling or live-read items lands first, rather than being a separate pass — the implementer may merge it if that is simpler.

The original framing came from the old AS48, which blocked its own delivery waiting for an "approved signing helper". That blocker is now formally removed: no such helper is needed for this deployment.

## Change Log

- 2026-08-06T23:30:59.930501+00:00 (created-by): Created by agent
- 2026-08-06T23:36:42.523895+00:00 (updated-by): Updated: section:acceptance_criteria

## Ledger-events

- chg_20260807_000724_refocused-the-plans-isolation_4380
- 2026-08-07T00:07:24.168507+00:00 (updated-by): Updated: section:ledger-events

## Dissolved 2026 08 09

**Retired as independently executable work after adversarial review (RV921). Its requirements are not cancelled — they are re-homed to the items that already own the surfaces in question.**

The original proposal was to fold this wholesale into whichever of AS93/AS94 landed first. That was rejected on inspection: every surviving requirement already has a distinct, obvious owner, and hosting them all in one item would misplace the rest.

**Distribution:**

- **AS93** absorbs, for the metadata-polling cursor: length/parser bounds, session-identity mismatch, and generation/restart deterministic-gap semantics. AS93 already owns that cursor's session identity, sequence, per-process generation and page-size bounds.
- **AS94** absorbs, for the unified live-read cursor: request/task/session/turn mismatch handling, and malformed/oversized outer-cursor rejection. AS94 already requires exactly this for its combined output/metadata continuation.
- **AS95** absorbs **nothing new**. It already owns client control IDs and already requires a duplicate ID to return the original result without double-dispatch — which is the whole of what survived here.

**Do not create a shared "integrity helper".** The three mechanisms' only remaining commonality is "validate input correctly"; a common abstraction over that is precisely the over-building the 2026-08-05 rescope removed.

**Stale text this supersedes:** the `files` section still listed "one foundation integrity helper" and "the control idempotency key path", contradicting this item's own Detailed Solution, which explicitly forbids any integrity/tamper-evidence mechanism and records that anti-replay was dropped. That was leftover from the pre-rescope framing. Where the exclusion is carried into AS93/AS94, phrase it as **cryptographic/tamper integrity is excluded** — not "cursor validation is excluded", which would wrongly drop the requirements above.
- 2026-08-09T01:19:28.422410+00:00 (updated-by): Updated: section:dissolved_2026_08_09, section:files
- 2026-08-09T01:19:37.252123+00:00 (state-transition): State: pending → superseded

## Ledger-events

- chg_20260809_012118_planning-for-the-agent-session_8017
- 2026-08-09T01:21:18.610361+00:00 (updated-by): Updated: section:ledger-events
