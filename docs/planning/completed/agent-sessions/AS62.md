---
id: AS62
order: 62
plan: agent-sessions
state: superseded
blocked-by: AS61 reassessment
validate-first: true
priority: P1
work: M
created-by: chatgpt
---

# Compose execution profiles and roles into agents

## Description



## Steps



## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files



## Validation

- `tests/unit/agents/test_agent_definitions.py` (new) — one Execution Profile
  backing two agents with different Roles; an Agent Definition re-pointed to a
  different compatible Execution Profile while keeping its stable public ID, with
  no versioning infrastructure introduced.
- A test asserts publication flags (`internal`/`ACP`/`A2A`) change neither the
  runtime MCP projection nor provider permissions.
- Resolution launches no process and creates no durable task or session —
  assert by fake provider that `open`/`prompt` are never called.
- Fake profile, role and provider dependencies are supplied via plain-Python
  parameter injection at the call site (RV890 correction), not through the foundation graph.
- An architecture test asserts no provider-native data, protocol type,
  management-MCP configuration, concrete-store import or runtime graph lookup.
- `python -m pytest tests/unit` green with the superseded direct Execution
  Profile work-selection path deleted.

## Effort & Risk



## Standards

- `arch-standards` §3 Ownership and Abstraction — one narrow Agent Definition
  lookup/resolution API. Do not split lookup, resolution and publication into
  separate services without a second current consumer.
- `arch-standards` §3 Evidence-led scope — no snapshot objects, definition
  versioning, inheritance or override machinery; record only IDs and execution
  facts the session/execution manifest already requires.
- `arch-standards` §1 Dependency Boundaries — Composition candidates (RV890 correction):
  the resolver is plain Python with Execution Profile, Role and `providers_api` injected as
  explicit parameters, not composed through the graph; no caller imports concrete stores
  from outside the module that owns them.
- `arch-standards` §6 Platform and Provider Boundaries — publication flags are
  authorization metadata only and must not alter runtime MCP projection or
  provider permissions.
- `arch-standards` §4 Public Contracts — the direct Execution Profile
  work-selection path is deleted, not aliased.

## Acceptance Criteria

- Agent Definition is the logical agent selected by agent-work callers.
- Resolution launches no process and creates no durable task/session.
- Publication eligibility is separate from protocol implementation and runtime tool materialization.
- Existing session runtime, provider MCP projection and persistence remain unchanged.
- Lookup/resolution is plain Python, injected explicitly at the call site (RV890 correction).
- Domain/application code contains no container or graph lookup.
- No direct Execution Profile agent-work path or compatibility handler remains.

## Notes



## AS59 disposition

Disposition: keep  
Authority: AS59  
Position: Composition track, position 5.

- Sole Agent Definition composition/lookup owner. Publication flags are visibility metadata only.
- Blocked by AS61's reassessment.

## Goal

Add `AgentDefinition` as a small logical composition:

- stable ID, name and description;
- one Execution Profile reference;
- one Role reference;
- optional advertised skills;
- optional internal, ACP and A2A publication flags.

Provider, model, surface and launch details remain in the Execution Profile. Runtime tool/MCP and permission ownership remains with existing component/provider materialization.

## Boundary

Expose one narrow Agent Definition lookup/resolution API. It receives the Execution Profile, Role and AS29 provider APIs explicitly and returns a short-lived resolved value for admission. It does not own provider execution, session creation, task dispatch, protocol publication or runtime tool materialization.

Do not split lookup, resolution and publication into separate services unless existing ownership or a second current consumer requires it.

## Foundation composition adoption (RV890 correction, 2026-08-04)

Agent Definition storage and resolution are plain Python, not a composition binding --
structurally identical to Role and project-local Execution Profile resolution, both of
which were checked against ARCHITECTURE_STANDARDS.md §1 "Composition candidates" and
found to need no composition (RV889, RV888). The resolver "returns a short-lived resolved
value for admission" -- the same stateless per-call shape, not a registry with
install/uninstall or hot-reload semantics.

- the resolver receives Execution Profile lookup, Role lookup and the approved
  provider/session-surface API as explicit plain-Python parameters (constructor or call-site
  injection), not through the foundation graph;
- keep the Agent Definition model and pure composition validation container-free;
- management MCP/CLI adapters call the public CRUD API directly, as Role's do;
- no caller imports concrete stores from outside the module that owns them, and no code
  performs a foundation-composition graph lookup;
- if a future consumer (AgentTask, ACP/A2A publication) introduces genuine runtime state --
  a live connection, install/uninstall lifecycle, construction/shutdown ordering worth owning
  -- re-run this same check against that consumer specifically before assuming it needs
  composition either.

## Publication flags

Publication flags are authorization/visibility metadata only:

- `internal`: may be selected by AUDiaGentic callers;
- `ACP`: eligible for AS65 external ACP exposure after capability validation;
- `A2A`: eligible for AS66 Agent Card/task exposure after capability validation.

They do not:

- install or select MCP servers;
- grant management MCP access;
- alter the harness default/configured runtime tools;
- imply the selected session surface supports ACP/A2A;
- bypass AS64/AS65/AS66 validation and authorization.

AS66 may separately authorize the runtime gateway/delegation capability for a source agent. That is not a generic Agent Definition tool-set field.

## Work

1. Add the minimal Agent Definition model and storage.
2. Resolve its Execution Profile and Role through their public APIs.
3. Resolve the selected AS29 surface through `providers_api` without launching.
4. Validate current Role requirements.
5. Record only active IDs and execution facts already required by the session/execution manifest.
6. Expose the lookup/resolver as a plain-Python public API (RV890 correction — not composed) and inject it explicitly into AgentTask and adapter call sites when they are built.
7. Update agent-work callers to select an Agent Definition.
8. Delete superseded direct Execution Profile work-selection APIs, fields, handlers, fixtures and tests in the same change.
9. Add architecture checks preventing provider-native data, protocol types, management MCP configuration, concrete-store imports and runtime graph lookup.
10. Add tests proving publication flags do not change runtime MCP projection or provider permissions.

## Do not add

- copied Agent/Role/Profile definitions in session records;
- snapshot objects or definition versioning infrastructure;
- arbitrary overrides;
- multiple roles or inheritance;
- a new session/runtime store;
- publication registries;
- runtime tool/MCP lists;
- management MCP access flags;
- service locators, global registries, migration shims or dual selection paths.

## Reassess before AS63

Prove one Execution Profile can back two agents with different Roles. Prove an Agent Definition can be edited to reference another compatible Execution Profile while retaining its stable public ID, without adding versioning infrastructure. Confirm publication flags do not alter the selected harness tools and fake profile/role/provider dependencies can be substituted by plain-Python parameter injection at the call site (RV890 correction) -- not through the foundation graph, since Agent Definition resolution is not composed.

## Implementation Landed 2026 08 04

**AgentDefinition core implemented (steps 1, 2, 4, 5, 9, 10). Steps 3 and 6-8 explicitly deferred, not silently dropped.**

Mirrors Role's shape, corrected per RV890 (no composition):

- `components/agents/agent_definitions.py` -- `AgentDefinition` dataclass (`agent_id`, `name`, `execution_profile_id`, `role_id`, `description`, `advertised_skills`, `internal`/`acp`/`a2a` publication flags), `validate_agent_definition`, `agent_definition_from_dict`/`to_dict`, `AgentDefinitionStore`.
- `components/agents/agent_definitions_api.py` -- CRUD against `.audiagentic/config/agent-definitions.yaml`, plus `resolve_agent_definition(project_root, agent_id, *, execution_profile_lookup=None, role_lookup=None)` -- a short-lived dict, launches no process, creates no session/task. Fakes substitute via plain-Python keyword parameters (RV890), defaulting to the real `agents_api.get_execution_profile`/`roles_api.get_role` when omitted.
- Error codes `VAL/RES/IO-AGD-*` registered. 5 new MCP tools on the existing `ag-agents-mgmt` server (still one management path, not a new server).
- Tests (`test_agent_definitions.py`, 21 tests) prove the reassessment gate directly: one Execution Profile backs two agents with different Roles; an agent definition is re-pointed to a different compatible profile while keeping its stable `agent_id`, with no `version`/`generation` field anywhere and exactly one record (not a second); resolution calls only the two lookup functions by ID, nothing launch/dispatch-shaped; publication flags round-trip unchanged through resolution. `test_agent_definitions_architecture.py` checks by import inspection (not string-matching a common word like "acp") that neither module imports MCP or provider-adapter internals, and that the dataclass carries no provider-native/protocol/versioning fields.

**Step 3 (AS29 surface resolution) is genuinely blocked, not skipped for convenience:** `providers_api.resolve_session_surface` requires a `SurfaceHint.surface_id`, and `ExecutionProfile` carries no `surface_id` field -- AS60's own step 5 ("Add AS29 surface selection only after AS29 defines its contract") was correctly deferred and never landed. There is nothing to resolve a surface *from* yet. Add step 3 once AS60 step 5 lands a surface_id field.

**Steps 6-8 (composition registration -- now moot per RV890; updating "agent-work callers" to select an Agent Definition; deleting the direct Execution Profile work-selection path) are deliberately not done.** They touch the same `agents_gateway_api` free-function submission surface already assigned to AS63's `AgentTask` redesign (AS59's own disposition table). Migrating callers now, before AgentTask exists, risks exactly the guess-then-redo cycle this session has been correcting elsewhere (RV888/889/890) -- AS62's own consumer references ("expose as a root dependency for AgentTask, ACP and A2A wiring") don't have a real consumer yet either. AgentDefinition is fully usable standalone via its CRUD/resolve API in the meantime, same shape as Role's standalone usability.

Full `tests/unit/agents` green (118 tests). Full `tests/unit` run pending confirmation.

## Change Log

- 2026-08-04T00:14:27.032685+00:00 (updated-by): Updated: section:implementation_landed_2026_08_04
- 2026-08-04T00:14:31.617502+00:00 (state-transition): State: pending → in_progress

## Ledger-events

- chg_20260804_001940_implemented-agentdefinition-t_8188
- 2026-08-04T00:19:40.821445+00:00 (updated-by): Updated: section:ledger-events

## Superseded 2026 08 05

**Superseded by AS81 (close-out validation) and AS82 (surface selection).**

Substantively complete. `AgentDefinition` (dataclass, validation, from/to-dict, Store), its CRUD API against `agent-definitions.yaml`, `resolve_agent_definition` with plain-Python lookup injection per the RV890 correction, five management MCP tools on the existing server, and 21 tests proving the reassessment gate directly — one profile backing two agents with different roles, re-pointing a definition while keeping its stable id with no versioning field anywhere.

Its three deferred steps are re-owned rather than left open:

- **Step 3 (AS29 surface resolution)** was genuinely blocked — `ExecutionProfile` had no `surface_id` to resolve from -> **AS82**, which adds exactly that field.
- **Steps 6-8** (composition registration, migrating agent-work callers, deleting the direct profile work-selection path) were correctly held for AS63's AgentTask redesign. AS63 is complete: `agent_task_submit` is the sole MCP submission surface and resolves through Agent Definition. AS81 validates that end state.
- 2026-08-06T23:33:32.061687+00:00 (updated-by): Updated: section:superseded_2026_08_05
- 2026-08-06T23:33:45.996266+00:00 (state-transition): State: in_progress → superseded
