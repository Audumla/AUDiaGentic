---
id: R2B3
order: 6
plan: refactor-round2
state: done
validate-first: true
priority: INVESTIGATE
work: S
---

# runtime/harness MCP config I/O trio — investigate

## Finding

`read_mcp_config` / `write_mcp_config` / `remove_mcp_config` / `mcp_config_path` defined 3× (`harness/__init__.py`, `harness/pi/install/__init__.py`, `harness/opencode/install/__init__.py`).

## Investigation Result (2026-06-22)

**Genuinely divergent.** Pi uses `pi.mcp_format` (harness-specific JSON format), opencode uses `foundation.mcp.json_format` (generic JSON format). The facade delegates to the active harness via `_mod()`. Duplication is structural, not accidental — do NOT consolidate.

## Options

(a) shared `mcp_config.py` with a format param; (b) leave if genuinely divergent.

## Decision

**Leave as-is.** The per-harness format variants make this structural duplication, not accidental. A shared helper would add a format param that just routes back to the same per-harness format modules.

## Files

`runtime/harness/__init__.py`, `runtime/harness/pi/install/__init__.py`, `runtime/harness/opencode/install/__init__.py`

## Validation

`pytest tests/ -q -k 'harness and mcp'`.

## Risk

Low — investigate first, only consolidate if impls match.

## Notes

Do NOT assume — diff before acting.
