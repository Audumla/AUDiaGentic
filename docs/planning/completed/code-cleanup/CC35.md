---
id: CC35
order: 100
plan: plan-code-cleanup
state: completed
validate-first: true
priority: P3
work: M
created-by: claude
---

# Placeholder: reorganize foundation/toolchains package for cohesion (post-Block-C)

## Description

REORGANIZE foundation/toolchains package by cohesion. Consolidation survey (2026-07-14) found this is a grab-bag of 17 files across 4 distinct concerns with no intra-package dependency between clusters — pure discoverability/cohesion debt.

## Steps

1. ✓ Create config/ subdirectory; move config_reader, fragments, config_patcher, managed_block, managed_config, artifact_registry into config/
2. ✓ Update intra-cluster import: managed_config → config.fragments
3. ✓ Create __init__.py for config/ with re-exports
4. Update ALL external callers for config/ imports (providers/descriptors/*, providers/services/*, coding_lsp/git_hooks_sync, source_control/source_control_bootstrap, foundation/steps/structured)
5. Run full unit suite to verify zero behavior change
6. Verify no stale import paths with whole-repo grep

## Detailed Solution & Technical Design

ARCHITECTURE VALIDATION (against §3 Ownership and Abstraction):

Domain-neutrality alone qualifies for foundation (user-confirmed constraint). All clusters are domain-neutral — no provider-specific logic in any file.

Consumer analysis confirms all multi-consumer:
- detect.py, loader.py: coding_lsp + source_control + foundation/components/dependencies → ✓ stays flat
- probes.py: recipe_materializer (internal) + foundation/components/dependencies (external) → ✓ stays flat
- config/*.py: providers/descriptors/* + providers/services/* (5+ files) + coding_lsp + source_control + foundation/steps/structured + foundation/components/dependencies → ✓ moved to config/
- recipe_*.py: providers/services/declarative_recipe_handler (current consumer; generic enough for future consumers) → ✓ stays flat

Intra-package dependency edges (only 2):
- managed_config.py → fragments.py (both in config/, import updated)
- recipe_materializer.py → probes.py (probes stays flat, no change needed)

ACTION:
1. ✓ REORGANIZE: config/ subdirectory for config ownership/mutation modules (moved + imports updated)
2. Recipe engine stays at flat paths — natural boundary already exists (no cross-dependency with other clusters)

## Code Samples & Guidance

ILLUSTRATIVE ONLY -- the actual split shape must come from the real import graph (see METHOD in notes), this shows the MECHANICAL PATTERN to follow once a grouping is chosen, not a prescribed grouping:

IF the import graph shows the recipe_* cluster (recipe_contract.py, recipe_loader.py, recipe_materializer.py, recipe_patterns.py, recipe_execution.py, recipe_catalogue.py) only depends on itself and on config_reader.py/fragments.py (not the reverse), the move pattern is:

  mkdir src/audiagentic/foundation/toolchains/recipes/
  git mv src/audiagentic/foundation/toolchains/recipe_*.py src/audiagentic/foundation/toolchains/recipes/
  # rewrite intra-cluster imports: recipes/recipe_loader.py's
  #   from audiagentic.foundation.toolchains.recipe_contract import X
  # becomes
  #   from audiagentic.foundation.toolchains.recipes.recipe_contract import X

Then in foundation/toolchains/__init__.py, re-export every symbol external callers currently import from the old flat paths, so nothing outside the package breaks:

  # foundation/toolchains/__init__.py
  from audiagentic.foundation.toolchains.recipes.recipe_contract import ProvisioningRecipe, RecipeResult  # noqa: F401
  from audiagentic.foundation.toolchains.recipes.recipe_loader import ...  # noqa: F401
  # ... one line per symbol actually imported by an external caller (found via the whole-repo grep below)

Whole-repo caller-surface grep to run BEFORE moving anything (this determines which re-exports are actually needed, and whether any caller imports a submodule path directly, e.g. `from audiagentic.foundation.toolchains.recipe_loader import X`, rather than a __init__-level re-export -- direct submodule imports are the ones a plain __init__.py re-export does NOT fix, and need either a compat shim module at the old path or an explicit whole-repo call-site update):

  grep -rn 'from audiagentic\.foundation\.toolchains' src/ tests/ | grep -v '^src/audiagentic/foundation/toolchains/'

Run the full unit suite after any move (`python -m pytest tests/unit/foundation -q` at minimum, per this codebase's standard practice) before considering the split complete -- zero behavior change is the item's own explicit bar (validation criterion 2).

## Files

foundation/toolchains/config/ (new: 6 files moved + __init__.py)
All external callers: providers/descriptors/base.py, providers/descriptors/loader.py, providers/services/mcp.py, providers/services/language_server_family.py, providers/services/managed_hooks_family.py, providers/services/managed_mcp_family.py, providers/services/managed_mcp_registry.py, providers/services/models.py, providers/services/plugin_entries.py, providers/services/status.py, coding_lsp/git_hooks_sync.py, source_control/source_control_bootstrap.py, foundation/steps/structured.py

## Validation

1. Build import graph: grep -rn 'from audiagentic\.foundation\.toolchains' src/ tests/ — no stale paths
2. Full unit suite green: python -m pytest tests/unit/foundation/toolchains/ -q
3. Zero behavior change — pure move with import path updates
4. Verify probes.py stays accessible at flat path for foundation/components/dependencies
5. Verify recipe_engine imports from foundation.toolchains.probes (valid foundation dependency)

## Effort & Risk

Low-mid, mechanical. 17 files → 4 clusters with clean boundaries (only 2 intra-package dependency edges). No behavioral change — only path updates + re-exports. Risk: missing a re-export for an external caller importing directly from submodule path.

## Standards

ARCHITECTURE_STANDARDS §1 (Dependency Boundaries: foundation may not depend on components), §3 (Evidence-led scope; split by cohesive responsibility; foundation only for domain-neutral multi-consumer capabilities), §4 (Public Contracts: backward compat via re-exports during migration)

## Notes

RV401 REBASE: prior expectation that MA10 adds a generic recipe_spec is withdrawn. Re-survey only after actual Block C code lands. Do not create package areas for planned abstractions that were never needed.

GATE SATISFIED (2026-07-25): MA10, MA12, and MA13 are all confirmed completed (MA10: closed as already-resolved per RV409, no code changes needed -- recipe consolidation already existed across recipe_contract.py/recipe_patterns.py/providers/services/recipes.py; MA12: CLI automation migration completed with 91 focused tests + real Docker install/uninstall proof; MA13: probe unification completed, single `probe_from_spec` parser in probes.py). This item's wait condition is met -- it is now executable, not still a placeholder.

Absorbed research from DU05 (deleted 2026-07-25 as a duplicate of this item -- same target package, thinner spec, different/incorrect gate condition naming CC41 instead of the real MA10/12/13 gate): current foundation/toolchains/ contents (16 files, re-glob before starting in case it's changed): config_patcher.py, config_reader.py, detect.py, fragments.py, loader.py, __init__.py, recipe_execution.py, recipe_patterns.py, recipe_materializer.py, managed_block.py, artifact_registry.py, managed_config.py, recipe_loader.py, recipe_catalogue.py, recipe_contract.py, probes.py. Note CC41 (the declarative recipe loader, separately landed 2026-07-15 to -17, 6/7 activities done) added recipe_loader.py/recipe_materializer.py/recipe_catalogue.py to this list after this item's original 2026-07-14 survey -- the module census in this item's description predates CC41 and is now stale; re-survey from the current file list above, not the description's enumeration.

METHOD (before proposing any split): build the actual import graph first -- `for f in src/audiagentic/foundation/toolchains/*.py; do echo "=== $f ==="; grep -o 'from audiagentic\.foundation\.toolchains\.[a-z_]*' "$f"; done` -- and only split along boundaries the graph actually shows (one-directional dependency clusters), not filename-pattern guesses. Grep the whole src/ tree for `foundation.toolchains` import paths first so any file move preserves every public import path via __init__.py re-exports. A conclusion of 'no natural boundaries exist, leave as-is' is a valid, complete outcome for this item per its own step 3 ('reorganize only if natural boundaries exist') -- do not force a split the import graph doesn't support.

## Ledger Events

- chg_20260714_030354_locked-in-the-cross-cutting-co_3200
- chg_20260714_122529_kept-deep-provider-capability_7070

## Change Log

- 2026-07-25T04:42:20.371714+00:00 (updated-by): Updated (no visible changes)
- 2026-07-25T04:43:43.792919+00:00 (state-transition): State: pending → completed
