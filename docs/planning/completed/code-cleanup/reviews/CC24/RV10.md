---
id: RV10
review-of: CC24
plan: code-cleanup
state: closed
reviewed-by: Codex
reviewed-at: ''
---

# Review CC24 generic config surface

## Notes

Tests not run; review only.

## Findings

src/audiagentic/components/planning/planning_api.py:L54: bug: CC24 requires planning_get_config/planning_set_config, but API only adds status changes. Add memory-style config API with schema/defaults/validation/persist.

src/audiagentic/components/planning/planning_manage_mcp.py:L53: bug: management MCP still exposes only select/list/status. Add planning_get_config(implementation_id=None) and planning_set_config(implementation_id, updates) tools.

src/audiagentic/config/components/coding-lsp/ag-lsp.yaml:L9: risk: ag-lsp has implementation-level options-schema (mutation-enabled), but lsp_manage_mcp.py has no generic lsp_get_config/lsp_set_config. Existing language-option tools do not cover this.

tests/unit/planning/test_planning_api.py: risk: no CC24 tests for config schema/validation/persistence. Added tests cover item ID generation, unrelated to CC24 acceptance.

## Conclusion

CC24 not complete. Required generic config surface is missing for planning, and coding-lsp audit points to missing implementation-level config tools too.
