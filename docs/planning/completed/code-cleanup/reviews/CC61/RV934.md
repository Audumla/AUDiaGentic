---
id: RV934
review-of: CC61
plan: code-cleanup
state: closed
reviewed-by: claude
reviewed-at: ''
---

# Split error-path fix into CC62; CC61 becomes typed-return-cores only

## Notes

User confirmed Slice 1 (error convention) is worth doing standalone and asked for the split. Also cleaning up: earlier update calls in this session used ad-hoc keys (e.g. 'slice_1_the_adapter_boundary_decorator') instead of the nine canonical sections, so plan_update_item persisted them as orphaned extra body sections. Consolidating back into description/steps/detailed_solution/code_samples/files/validation/effort_risk/standards/notes only.

## Findings

The dependency between the two halves only runs one way: a tool with a declared return type that still returns report_error()'s error dict fails pydantic validation instead of surfacing the real error, so CC61 (typed cores) needs CC62 (error convention) to land first. CC62 needs nothing from CC61 and is a net deletion on its own (one decorator, 25 unwound call sites verified by AST walk — 19 in project_mcp.py, 6 in session_mcp.py, all inside except handlers — one mechanical rename, report_error() deleted). Also corrected an inflated count: earlier text said '27' call sites; the AST-verified figure is 25 (the earlier count included the two import statements).

## Conclusion

Created CC62 with the full error-path content. Rewriting CC61 now to drop everything Slice-1-specific, remove the 'Slice 2 cannot start before Slice 1' framing (replaced with a plain dependency-on-CC62 note), resize from L to M given the opportunistic (not campaign) scope already agreed in RV933, and consolidate the orphaned ad-hoc sections back into the nine canonical fields.
