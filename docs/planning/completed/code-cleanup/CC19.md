---
id: CC19
order: 19
plan: code-cleanup
state: completed
validate-first: true
priority: P2
work: M
---

# FND-003 — Consolidate remaining foundation duplication and god objects

## Description

Remaining foundation-level findings:
- `_pid_alive()` duplicated in `system/process.py:83-89` and `workflow/id_gen.py:111-116`
- Probe type if/elif chain in `components/dependencies.py:53-89` (5 branches)
- Descriptor type if/elif chain in `features/loader.py:189-194` (3 branches)
- Platform-to-package-manager detection in `toolchains/detect.py:41-54`
- Formatter selection if/elif in `logging/config.py:294-301`
- `StatePropagationEngine` god object in `workflow/propagation/engine.py` (303 lines, 6+ responsibilities)

## Steps

1. ✅ Move `_pid_alive()` to `system/process.py`, import in `workflow/id_gen.py`
2. ✅ Convert probe type dispatch in `components/dependencies.py` to `PROBE_RESOLVERS` registry dict
3. ✅ Convert descriptor type dispatch in `features/loader.py` to `_DESCRIPTOR_LOADERS` dispatch table
4. ✅ Replace platform-to-pm-list if/elif in `toolchains/detect.py` with `PLATFORM_PM_MAP` dict
5. ✅ Convert formatter selection in `logging/config.py` to `FORMATTER_REGISTRY` dict
6. ✅ Decompose `StatePropagationEngine` (303 lines) into focused classes: `PropagationRuleEvaluator`, `ActionExecutor`, `HealingValidator`, `LogRecorder`

## Files

Modified: `system/process.py`, `workflow/id_gen.py`, `components/dependencies.py`, `features/loader.py`, `toolchains/detect.py`, `logging/config.py`, `workflow/propagation/engine.py`
New: `workflow/propagation/rule_evaluator.py`, `workflow/propagation/action_executor.py`, `workflow/propagation/healing_validator.py`, `workflow/propagation/log_recorder.py` (optional)

## Validation

No duplicate `_pid_alive()`, all dispatch tables use registry pattern, `StatePropagationEngine` under 350 lines with single responsibility

## Architecture Standards Assessment

**Standard #3 (Logic Containment):** Duplicate `_pid_alive()`, god object `StatePropagationEngine`. **Standard #2 (Config Over Code):** 3 if/elif chains on entity names (probe types, descriptor types, formatters).

## Resolution Assessment

**Complexity: Mid.** Registry conversions are straightforward. `StatePropagationEngine` decomposition requires careful interface design.

**Why mid:** Engine decomposition has 4+ new classes with interdependencies. Registry conversions are simple.

## Effort & Risk

Medium — registry conversions are low risk, engine decomposition requires careful testing.

## Notes
