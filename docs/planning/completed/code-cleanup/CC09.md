---
id: CC09
order: 9
plan: code-cleanup
state: done
validate-first: true
priority: P0
work: S
---

# FND-002 — Extract shared `_pattern_matches()` and `deep_merge()` utilities

## Description

Two pairs of identical implementations scattered across foundation:
- `_pattern_matches()` in `event/event_bus.py:236` and `event/event_store.py:137` (wildcard pattern matching with `*` and `**` segments)
- `deep_merge()` in `paths/resolution.py:26` and `logging/config.py:89` (recursive dict merge with deepcopy)

## Steps

1. Create `foundation/util.py`
2. Add `pattern_matches(pattern: str, event_type: str) -> bool` extracted from event bus version
3. Add `deep_merge(base: dict, override: dict) -> dict` extracted from paths version
4. Replace local implementations in all 4 files with imports from `foundation.util`
5. Fix `contracts/errors.py:63-81` and `mcp/component_server.py:55-85` overlapping redaction — extract unified `_redact_sensitive()` with configurable rules

## Files

New: `foundation/util.py` — `pattern_matches()`, `deep_merge()`, `_redact_sensitive()`
Modified: `event/event_bus.py`, `event/event_store.py`, `paths/resolution.py`, `logging/config.py`, `contracts/errors.py`, `mcp/component_server.py`

## Validation

All 4 duplication sites import from `foundation.util`, no local implementations remain, redaction handles both bearer tokens and API-key prefixes uniformly

## Architecture Standards Assessment

**Standard #3 (Logic Containment):** Shared logic in 2+ files must be extracted to foundation immediately. This directly resolves the violation.

## Resolution Assessment

**Complexity: Simple.** Extract functions, replace imports. No behavioral change.

**Why simple:** Both functions are self-contained utilities with no dependencies on surrounding class state.

## Effort & Risk

Low — pure extraction, no behavioral change.

## Notes
