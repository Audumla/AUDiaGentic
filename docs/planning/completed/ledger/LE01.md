---
id: LE01
order: 0
plan: plan-ledger
state: completed
validate-first: true
priority: P2
work: S
created-by: assistant
---

# Purge synced fragments on ledger sync

## Description

After `sync_ledger` merges fragments into CURRENT_RELEASE_LEDGER.ndjson, the individual .json fragment files are no longer needed. This change purges synced fragments and cleans stale entries (empty files, stray dirs) immediately after sync.

## Steps

['Add SyncResult.purged_fragment_count field', 'Implement _purge_synced_fragments in sync.py — delete matched .json files, empty non-JSON files, empty dirs', 'Call purge after manifest write in sync_current_release_ledger', 'Thread purged count through ledger_api record_change/record_changes/sync', 'Add unit tests for purge behavior, reported count, stray directory cleanup', 'Verify existing 29 release tests pass']

## Files

['src/audiagentic/components/ledger/sync.py', 'src/audiagentic/components/ledger/ledger_api.py', 'tests/unit/release/test_ledger_api.py']

## Validation

['python -m pytest tests/unit/release/ passes (29+ tests)', 'sync after record_change leaves 0 .json fragments on disk', 'reported purged-fragment-count matches actual deletion count']

## Effort & Risk



## Standards



## Notes

Event chg_20260714_074801_ledger-sync-now-cleans-up-merg_7019 linked to this plan item.

## Ledger Events


