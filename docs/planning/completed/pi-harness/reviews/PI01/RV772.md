---
id: RV772
review-of: PI01
plan: pi-harness
state: closed
reviewed-by: ''
reviewed-at: ''
---

# Version enforcement was advisory-only; now fail-closed with test matrix

## Notes

Code audit + fix: apply_mcp_exclusive_patch only logged a warning for unsupported/unknown pi-mcp-adapter versions instead of rejecting, contradicting the item's own detailed-solution text (Reject if version is outside range). Fixed: added _version_supported() caret-range check; unknown or out-of-range versions now abort the patch (fail-closed), consistent with existing anchor fail-closed behavior -- callers (mcp_surface.py) already treat a False return as a graceful fallback to additive isolation, so no behavior change needed there. Added tests/unit/providers/test_mcp_exclusive_patch.py covering supported/unsupported/missing version, idempotency, and missing-anchor fail-closed-on-both-files. Item can close.

## Findings



## Conclusion

closeable
