---
id: RV773
review-of: PI04
plan: plan-pi-harness
state: closed
reviewed-by: ''
reviewed-at: ''
---

# Native/isolated Pi config separation was already implemented; added closing test

## Notes

Code audit confirmed the separation this item asks for is already implemented: agents_gateway_worker._replacement_environment replaces HOME/USERPROFILE/PI_CODING_AGENT_DIR with paths under a private, disposable worker home for every isolated job, and the only interaction with the caller's native ~/.pi is a one-way read of models.json (never written back). tests/unit/providers/test_mcp_launch_isolation.py already covered request-owned MCP config uniqueness across concurrent jobs. Added tests/unit/agents/test_agents_gateway_worker_pi_isolation.py asserting native ~/.pi/mcp.json and models.json are byte-for-byte unchanged after preparing an isolated job's environment, and that the job's PI_CODING_AGENT_DIR never points at the native tree. No production code changes needed. Item can close.

## Findings



## Conclusion

closeable
