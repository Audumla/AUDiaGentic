---
id: SH21
order: 0
plan: shared-gateway
state: completed
breadth: ''
skill: ''
---

# Isolated worker swallows real exception detail on unexpected failure (INT-AGW-076 opaque)

## Description

agents_gateway_worker_host.py:118-129's bare `except Exception` catch-all reports every unexpected worker-side failure as the same generic message ("isolated provider worker failed unexpectedly", code INT-AGW-076) with zero diagnostic detail — no traceback, no exception type/message. Compounding this, `with redirect_stdout(sys.stderr)` (line 96) discards the provider's stdout entirely, and agents_gateway_worker.py's spawn_supervised call captures the child's stderr but discards `_stderr` unconditionally (line 186) instead of persisting it on error. When a real bug happens inside providers_api.execute_provider_turn, there is currently no way to diagnose it after the fact — not from the gateway record, not from any log file.

Discovered 2026-07-20 while live-testing the gateway MCP surface (agent_llm_submit) against the pi/mid-coder profile: a short, simple prompt succeeded normally, but a longer (~4KB), code-fence-heavy prompt asking the agent to implement a real feature failed in under 1 second with exactly this opaque error. The failure is almost certainly a real bug somewhere in prompt encoding/handling for that size/content class (candidates: worker_protocol message encode/decode with embedded backticks or multi-line content, or a pi-adapter command-line/argument-length limit), but it cannot be root-caused today because the exception detail never survives past the worker process boundary.

## Steps

1. Reproduce: submit a large, backtick/code-fence-heavy prompt via agent_llm_submit against a full-isolation provider (pi/mid-coder reproduced it 2026-07-20) and confirm the near-instant INT-AGW-076 failure.
2. Capture real diagnostics safely: in agents_gateway_worker_host.py's `except Exception` handler, capture traceback.format_exc() and the exception type/message, and persist them somewhere redacted-safe for operator diagnosis (NOT across the public protocol pipe, which must stay a clean data channel) — e.g. persist the child's captured stderr in agents_gateway_worker.py on any non-zero/error outcome instead of discarding it.
3. Once real diagnostics are visible, root-cause the actual reproduction case from step 1 and fix it (likely a prompt-size/content encoding issue).
4. Add a regression test using a similarly-shaped large/code-fence-heavy prompt through the real gateway to prevent recurrence.

## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files

src/audiagentic/components/agents/agents_gateway_worker_host.py; src/audiagentic/components/agents/agents_gateway_worker.py

## Validation

A deliberately large/complex prompt that previously triggered opaque INT-AGW-076 either succeeds, or fails with real diagnostic detail (exception type/message/traceback) captured somewhere an operator can actually read it.

## Effort & Risk



## Standards

- `arch-standards` §4 Public Contracts — errors, logs and results must not expose secrets or unredacted raw output; the diagnostic is bounded and redacted by construction.
- `arch-standards` §8 Protocol Adapter Boundaries — the worker protocol pipe stays a clean data channel; diagnostics travel on a separate, operator-only channel (stderr), never mixed into the public envelope.

## Acceptance Criteria

- An unexpected (non-`AudiaGenticError`) worker-side exception produces a bounded, redacted diagnostic an operator can actually read — not just the generic `INT-AGW-076` message.
- The diagnostic (exception type, message, truncated traceback) is written to the worker's stderr channel only — never the protocol stdout pipe, which stays a clean data channel.
- The parent process persists that stderr on a non-zero/error outcome instead of discarding it.
- The diagnostic payload is bounded (does not grow unbounded on a huge traceback) and contains no secret values.
- The public error envelope returned over the protocol pipe contains no raw traceback.
- A large, code-fence-heavy prompt that previously reproduced the opaque failure now either succeeds or fails with real diagnostic detail.

## Notes

Pi stdin transport slice accepted after Windows correction. Parent validation: `python -m pytest -q tests/unit/agents/test_sh21_gateway_pi_smoke.py tests/unit/agents/test_sh21_prompt_truncation_and_diagnostics.py tests/unit/agents/test_gateway_worker_protocol.py tests/unit/providers/test_pi_acp_launch.py tests/unit/providers/test_pi_execution_dispatch.py` => 58 passed. The deterministic smoke proves gateway worker -> provider loader -> Pi custom adapter -> Windows-compatible fake Pi, exact multiline stdin, no prompt argv, and redacted public error/output. SH21 remains pending solely for RV769: unexpected-worker `INT-AGW-076` must persist bounded redacted operator diagnostics.

## Closed 2026 08 05

**Verified against the tree and closed — already fully implemented and tested.**

Every item in this item's own Steps and Validation is present:

- `gateway/queue/worker_host.py::_emit_worker_diagnostic` captures `traceback.format_exception`, bounds it at `_MAX_DIAGNOSTIC_BYTES` (64 KiB), and writes it to **stderr only** — the docstring states explicitly "The worker protocol pipe (stdout) must never carry raw traceback data".
- `gateway/queue/worker.py` (~line 243) captures the child's `stderr_text` and includes a bounded redacted diagnostic on the record when `error_code == "INT-AGW-076"`, closing the exact discard bug this item's Description named ("discards `_stderr` unconditionally").
- `_safe_error` keeps the public `WorkerErrorEnvelope` clean — code/kind/message only, no traceback.
- Tests exist and are green: `tests/unit/agents/test_sh21_prompt_truncation_and_diagnostics.py` (`test_unexpected_exception_emits_stderr_diagnostic_via_subprocess`, `test_error_envelope_does_not_contain_traceback`, `test_protocol_stdout_not_contaminated_by_traceback`, `test_diagnostic_contains_exception_class_and_message`, `test_diagnostic_is_bounded`, `test_diagnostic_does_not_contain_secret_values`, `test_worker_host_emits_clean_error_envelope_and_stderr_diagnostic`, `test_worker_diagnostic_is_bounded`) plus `test_sh21_gateway_pi_smoke.py`. `python -m pytest tests/unit/agents/test_sh21_gateway_pi_smoke.py tests/unit/agents/test_sh21_prompt_truncation_and_diagnostics.py -q` — 25 passed.

This item's own Notes said it "remains pending solely for RV769: unexpected-worker INT-AGW-076 must persist bounded redacted operator diagnostics" — that is exactly what is verified above. Closing.

**This directly unblocks SH16**, whose Docker smoke failure investigation was explicitly waiting on this diagnostic capture to exist.

## Change Log

- 2026-08-07T04:38:00.390483+00:00 (updated-by): Updated: section:acceptance_criteria, section:standards, section:closed_2026_08_05
- 2026-08-07T04:38:03.434431+00:00 (state-transition): State: pending → completed

## Ledger-events

- chg_20260807_044121_finished-rationalizing-both-pl_2461
- 2026-08-07T04:41:21.910587+00:00 (updated-by): Updated: section:ledger-events
