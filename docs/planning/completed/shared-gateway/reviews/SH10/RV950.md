---
id: RV950
review-of: SH10
plan: shared-gateway
state: closed
reviewed-by: codex
reviewed-at: ''
---

# Gateway recover CLI targets project root instead of machine service store

## Notes



## Findings

Confirmed live: the old automatic service process stopped, the machine record remained draining, and CLI recovery searched under the project path and failed VAL-MSVC-010. Direct machine-store recovery succeeded safely after proving the recorded PID dead. CLI now calls the same default machine-scoped store as automatic bootstrap.

## Conclusion

Incorporated and covered by lifecycle CLI tests. Recovery remains explicit-confirmation, record-only, and never signals an unproven PID.
