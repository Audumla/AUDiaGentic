---
id: RV992
review-of: SH32
plan: shared-gateway
state: closed
reviewed-by: dev-gpt-agent
reviewed-at: ''
---

# Adversarial scope refinement and fencing dependency

## Notes

Follow-up adversarial review supplied by the user after the initial SH32 agreement.

## Findings

The single SH32 item is the smallest safe scope. Do not introduce a durable retired state, provider deletion, or configurable archive/delete modes. Use an authoritative AS107 session mutation fence; add a narrowly scoped AS prerequisite only if capability inspection proves the existing fence insufficient. Use explicit machine-global session_purge_enabled and session_purge_inactivity_seconds, disabled by default, with last_accepted_turn_at as the sole purge clock. SESSION_PURGE must delete only gateway-local, exclusively-owned lineage and linked request descendants, with parent last and durable idempotent evidence.

## Conclusion

Agree with changes. Refine SH32 terminology and acceptance criteria; defer any AS item until AS107 fencing is inspected.
