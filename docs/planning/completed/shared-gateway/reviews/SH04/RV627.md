---
id: RV627
review-of: SH04
plan: plan-shared-gateway
state: closed
reviewed-by: claude
reviewed-at: ''
---

# SH04 in-progress code review: idle clients lose their lease with no re-acquire path; /v1/call bypasses the SH02 envelope with raw **kwargs; auth failures are 400

## Notes

T3 review of the in-flight SH04 implementation (agents_gateway_service_host.py, agents_gateway_service_application.py, agents_gateway_http_transport.py, agents_gateway_remote_client.py) so findings land before closure. Positives: loopback-only bind enforced at both server and client, constant-time token compare, request-size caps, error envelopes preserved end-to-end, ManagedServiceOwner/store reuse instead of a hand-rolled registry, and AR25 layering verified by test_protocol_adapter_boundaries.py.

## Findings

Revalidated against completed SH04 code. StandaloneGatewayClient clears and reacquires expired/rotated leases before domain invocation; GatewayServiceApplication rejects unknown parameters and validates submit/run inputs through SubmissionEnvelope.from_mapping; authentication failures map to HTTP 401; an unrestricted wait passes transport timeout None. SH04/RV625 validation covers the hardened contract.

## Conclusion

All four findings are incorporated by the completed SH04 implementation. No additional action remains.
