---
id: RV667
review-of: SH02
plan: plan-shared-gateway
state: closed
reviewed-by: codex
reviewed-at: ''
---

# Execution-manifest kwargs break the live gateway runner contract

## Notes

Resolved through the completed SH06 typed worker boundary. Dispatch now receives the frozen manifest/context/profile/tier contract; a fresh real gateway request completed with one owned attempt and exact output. Child-reported PID, creation identity, and canonical working directory are verified before accepting results.

## Findings

agents_gateway_api partially binds manifest_id, context_fingerprint, component_profile, and provider_isolation_tier into dispatch_request, whose contract accepts only dispatch_prompt. Every real request fails with TypeError before provider resolution; shallow admission tests did not protect the live runner signature. Restore the current runner contract until SH06 introduces a complete typed worker boundary, and add a regression that executes the actual queue/dispatch path and asserts a provider attempt/output.

## Conclusion

Implemented and validated raw CLI -> providers_api -> gateway API. Three real OpenCode profiles started within 4 ms, ran in overlapping windows, and returned exact outputs. Host and Docker process lifecycle gates cover concurrency, private HOME vs project CWD, timeout, provider crash, and descendant teardown.
