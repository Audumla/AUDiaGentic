---
id: RV611
review-of: SH02
plan: shared-gateway
state: closed
reviewed-by: claude
reviewed-at: ''
---

# SH02 gap from RV577 is closed in code, but the seam adds providers-internals imports, the manifest omits the active component profile, and the item notes still say pending

## Notes

T3 post-completion review. Verified in agents_gateway_api.submit_llm_request: SubmissionEnvelope construction/validation, ExecutionManifest resolution and persistence, prompt_digest redaction with the raw prompt threaded in-memory only — the RV577 correction was genuinely implemented before closure.

## Findings

1. The agents gateway still resolves provider isolation through a provider-internal descriptor import. This is the existing cross-component execution-boundary defect already tracked canonically by RV589/MA17 and is not duplicated here.
2. Confirmed defect: SH02 omitted the active component profile from SubmissionEnvelope and passed an empty component overlay into the runtime digest. Requests under different component profiles could therefore share the same execution fingerprint.
3. Completed-item notes are stale, but no separate implementation defect follows from that documentation drift.

## Conclusion

Finding 2 is fixed: admission now reuses foundation.paths.names.get_active_profile(), freezes the profile into manifest identity, and includes it in the runtime digest input. Focused SH02/context validation passes (47 tests). Finding 1 is consolidated into RV589/MA17; no duplicate implementation work remains in this review.
