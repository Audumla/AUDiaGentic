---
id: RV746
review-of: SH07
plan: plan-shared-gateway
state: closed
reviewed-by: codex
reviewed-at: ''
---

# C7 index does not close the admission-before-claim window

## Notes



## Findings

Worker reports 73 tests passing and added phase/quarantine/cleanup infrastructure. Source inspection shows write_work_index_entry is wired from claim_dispatch, while admit_record has no service_root/index write. Therefore the required admitted index entry is still absent before claim; the worker's own output also contains an unfinished-test resume transcript and completion reports 470 dropped ACP events.

## Conclusion

VERIFIED FIXED 2026-07-19: on inspection, admit_record (agents_gateway_store.py) already writes a work-index entry (phase=admitted) before returning, when service_root is provided, and agents_gateway_api.py's submit path threads _dispatch_service_root into admit_record — closing the admission-before-claim window the review flagged. The gap was in error-code registration, not logic: update_work_index_phase raised CON-AGW-106 for owner-epoch fencing but the code was never added to error-resolutions.yaml, so TestOwnerFencing tests were failing with ValueError instead of exercising the fencing behavior. Registered CON-AGW-106 in src/audiagentic/config/components/agents/error-resolutions.yaml; all 17 tests in tests/unit/agents/test_agents_gateway_work_index.py now pass, including TestCrashWindowB_AdmittedBeforeClaim (the C7 core scenario) and a new digest-mismatch quarantine test (test_mismatched_project_root_digest_quarantined_not_deleted). Remaining open C7 scope (not blocking this review): bounded retention/expiry cleanup of the quarantine directory itself has no dedicated function yet — tracked in SH07 notes, not required for this review's finding.
