---
id: RV675
review-of: SH07
plan: shared-gateway
state: closed
reviewed-by: codex
reviewed-at: ''
---

# Verify advisory idempotency index against authoritative request record

## Notes

Post-implementation review of SH07's bounded admission slice.

## Findings

Implemented the correction. `admit_record()` now treats the project-local persisted request as authoritative: it verifies the referenced record carries the reserved key digest, recomputes its immutable intent digest, repairs a well-formed stale index, and only replays when the candidate equals the recomputed record intent. A changed candidate receives CON-AGW-081. Missing/orphaned and mismatched record index entries continue through the locked recovery scan; no raw key or prompt is included in diagnostics.

## Conclusion

Resolved. Focused Docker validation passed: 42 store/API tests, including stale valid-index repair/rejection, four real interpreter processes contending with divergent intents (one admission and three CON-AGW-081 results), and gateway API replay after a stale index with exactly one enqueue. Ruff passed on all touched files.
