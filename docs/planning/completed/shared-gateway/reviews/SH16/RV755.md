---
id: RV755
review-of: SH16
plan: shared-gateway
state: closed
reviewed-by: claude
reviewed-at: ''
---

# Independent re-verification against live tree: SH16's premise is stale, most of the item is already implemented

## Notes

Verification method: read pi.yaml directly, grepped adapters/pi for acp.py, ran the 3 existing pi test files locally (11/11 pass), grepped UNS-AGW-076 call sites, read agents_gateway_sessions.py and providers_api.py to confirm AS28 slice 4a is real, read AS13/AS17/AS28/SH11/SH19 plan items and cross-checked their own re-verification notes against code rather than trusting state labels.

## Findings

Re-verified SH16 against the live tree (not the item's prose) on 2026-07-20.

1. FALSE PREMISE: the description claims 'Pi ... fails UNS-AGW-076 because its descriptor is no-isolation and it has no ACP launch adapter.' Both halves are now false. src/audiagentic/config/providers/pi.yaml:4 declares `execution_isolation_tier: full-isolation` (matches codex/opencode/qwen, satisfies agents_gateway_worker_host.py's UNS-AGW-076 gate at line ~83-89). An ACP launch adapter exists at src/audiagentic/components/providers/adapters/pi/acp.py (`build_acp_launch`), registered via `load_acp_launch_builder('pi')`.

2. Steps 1-4 are substantially DONE, landed in commit e326aefe ('feat(providers,foundation): new PI ACP adapter, process adoption, transport improvements'): managed-bridge pattern reused (src/audiagentic/runtime/harness/pi/install.py + config/provisioning/harness/pi.yaml, pinned cli/mcp_adapter/acp package versions, no second installer abstraction); request-local Pi state materialization exactly as step 4 specifies (PI_CODING_AGENT_DIR, --session-dir, Windows HOME/TEMP/XDG redirects in adapters/pi/acp.py `_request_environment`); AS17's formal adopt/observe/stop contract (foundation/system/adopted_process.py) is generic and covers Pi's child lifecycle without any Pi-specific supervisor.

3. 3 of 4 files listed under 'files' already exist and pass: tests/unit/providers/test_pi_acp_launch.py, tests/unit/provisioning/test_pi_install.py, tests/unit/provisioning/test_pi_request_runtime.py (ran all 11 tests locally: 11 passed). docs/reference/PROVIDER_CAPABILITY_REFERENCE/harnesses/profiles/pi.md also exists. Only tests/integration/agents/test_gateway_pi_smoke.py is genuinely missing anywhere in the tree.

4. Dependency status, independently checked (not trusted from state labels):
   - AS13 (state: pending) — managed bridge install/probe pattern is real and reusable; Pi's provisioning harness follows the same recipe shape. Live-session activation piece of AS13 is itself gated on AS28, same as Pi.
   - AS17 (state: pending) — the SPECIFIC capability SH16 needs (adopt/observe/stop contract, Job Object hard-death on Windows, PID-reuse refusal) is IMPLEMENTED per AS17's own 2026-07-19 note and verified present at foundation/system/adopted_process.py; remaining AS17 scope (POSIX hard-death honesty docs, architecture tests) does not block Pi.
   - AS28 (state: pending) — partially landed: agents_gateway_sessions.py's OPEN path already routes through the neutral `providers_api.prepare_provider_session_transport` seam (module docstring: 'AS28 slice 4a'). However only opencode.yaml declares `session_surfaces`; Pi has no session-surface declaration, so Pi's ACP adapter remains reachable only via the temporary `prepare_provider_acp_launch` path for live sessions, exactly as SH16's notes anticipated ('keep the Pi ACP adapter behind providers_api and out of agents' until AS28 fully lands). This is correctly deferred, not a defect.
   - SH11 (state: pending, confirmed not started per its own 2026-07-20 note) — this is generic gateway-management/client-implementation-selection plumbing (embedded/standalone/automatic). It is orthogonal to whether Pi can complete a one-shot gateway dispatch; not a real blocker for SH16's core scope.

5. SH19 relevance: SH19 found local-openai's no-isolation tier was a false-negative (nothing to isolate). Pi is the opposite case: a real CLI-backed provider with real local state (PI_CODING_AGENT_DIR, ~/.pi/agent/models.json, session dirs) — and unlike local-openai, Pi's isolation was NOT solved by a 1-line tier reclassification; it required (and got) real redirection infrastructure (request-local PI_CODING_AGENT_DIR, session-dir, Windows profile/XDG redirects) plus a genuine full-isolation declaration, matching SH19's 'needs actual redirection support' category (like claude's CLAUDE_CONFIG_DIR). This confirms Pi was correctly NOT given the shortcut treatment.

Recommendation: SH16 is not stale-wrong, it is stale-COMPLETE — the steps/notes describe work that has already landed. Remaining scope is narrow: add tests/integration/agents/test_gateway_pi_smoke.py (real Docker gateway one-shot dispatch proof for Pi, the one item file still missing), and re-verify the description/steps text so it stops asserting a bug that no longer exists. Not recommending closure myself (per process, that decision belongs to the orchestrating session), but the item should be re-scoped down to just the Docker smoke test before anyone attempts the full original step list.

## Conclusion

correction
