---
id: RV767
review-of: SH21
plan: shared-gateway
state: closed
reviewed-by: codex
reviewed-at: ''
---

# SH21 Pi stdin adapter independent review

## Notes



## Findings

Initial mid implementation adds `adapters/pi/adapter.py`, which the execution loader will prefer over the descriptor runner. Independent focused suite failed on Windows: `test_real_subprocess_stdin_preserves_newline_boundary` embeds an unescaped temp path in `python -c`, raising `SyntaxError: unicodeescape`. The initial tests also asserted the custom helper in isolation but did not prove `_load_runner('pi')` selects it or that `run()` passes multiline `input_text` to `run_streaming_command`. Correction submitted as req_7e5e044b234548b6; review remains open.

## Conclusion

Do not accept SH21 yet. The design direction is plausible, but Windows-safe coverage and production-path proof are required; live Pi smoke remains required after unit acceptance.
