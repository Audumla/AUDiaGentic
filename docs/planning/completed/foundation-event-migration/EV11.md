---
id: EV11
order: 17
plan: plan-foundation-event-migration
state: completed
validate-first: false
priority: P2
complexity: simple
---

# Add subscription_count() and subscribers(pattern) to EventBusProtocol for observability

## Description

Tests and diagnostics must access private `_subscriptions` to inspect subscriber count (`test_agents_gateway_events.py:95`, `test_agents_gateway_events.py:98`). The protocol exposes no observability surface, so external code reaches into internals.

**Affected files:**
- `src/audiagentic/foundation/event/event_bus.py` — private `_subscriptions` dict
- `tests/unit/agents/test_agents_gateway_events.py:95-98` — accesses private field

## Steps

['Add `subscription_count(pattern=None)` to EventBusProtocol — returns total count or count for specific pattern\n- Pattern is optional: None returns total subscriber count across all patterns', 'Migrate test access from `_subscriptions` to public API\n- Affected: `test_agents_gateway_events.py`, any other test that accesses private fields']

## Files

['src/audiagentic/foundation/event/event_bus.py']

## Validation



## Effort & Risk



## Standards



## Notes

Implemented 2026-07-06 per RV152: subscription_count(pattern=None) added to EventBusProtocol + EventBus — exact-pattern-key semantics (no wildcard expansion), documented on the protocol; subscribers(pattern) from the title deliberately NOT added (would expose handle internals; count satisfies the known consumers). test_agents_gateway_events.py migrated off _subscriptions to subscription_count. Semantics + total-count + unsubscribe_all covered in test_bus.py.
