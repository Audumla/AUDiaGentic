---
id: RV149
review-of: EV08
plan: foundation-event-migration
state: closed
reviewed-by: claude
reviewed-at: ''
---

# Still valid; steps and validation disagree (unsubscribe_all, EVT-BUS-002 post-close raise are unstated steps)

## Notes



## Findings

1. CORE CLAIM CONFIRMED: EventBusProtocol (event_bus.py:59-85) declares only publish/subscribe/unsubscribe; concrete EventBus adds close() (264), wait_idle() (268), publish_envelope() (144) off-protocol.
2. STEPS/VALIDATION MISMATCH: validation #1 requires unsubscribe_all() and validation #2-3 require post-close publish raising EVT-BUS-002 and idempotent close — none of these appear in the steps. Post-close behavior today: sync publish silently succeeds after close (only the async executor is shut down), so the EVT-BUS-002 requirement is a real behavior change needing a _closed flag + checks in publish/publish_envelope/subscribe — should be an explicit step.
3. Note: adding abstract methods to EventBusProtocol is safe today — EventBus is the only implementation (grep confirms no other subclass).

## Conclusion

Keep. Amend steps to match validation: add _closed-flag step (raise EVT-BUS-002 on post-close publish/subscribe, idempotent close), add unsubscribe_all() step, and a test for wait_idle timeout semantics.
