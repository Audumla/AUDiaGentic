---
id: RV152
review-of: EV11
plan: plan-foundation-event-migration
state: closed
reviewed-by: claude
reviewed-at: ''
---

# Valid; title promises subscribers(pattern) the steps never add; files list omits the test to migrate; validation empty

## Notes



## Findings

1. CORE CLAIM CONFIRMED: tests reach into privates — tests/unit/agents/test_agents_gateway_events.py:95,98 read get_bus()._subscriptions directly; no public observability API exists on the bus or protocol.
2. TITLE/STEPS MISMATCH: title includes 'subscribers(pattern)' but the steps only add subscription_count(pattern=None). Decide: subscription_count alone satisfies the known consumers (the gateway test wants per-pattern handle lists though — it compares before/after lists, so count is sufficient for its assertions). Recommend: ship subscription_count only; drop subscribers() from the title to avoid exposing handle internals.
3. FILES GAP: steps migrate the test but Files lists only event_bus.py; add tests/unit/agents/test_agents_gateway_events.py.
4. Validation section empty. Minimum: count with exact-pattern arg counts only that pattern's handles (no wildcard expansion — document that choice); total count sums all patterns; test no longer touches _subscriptions (grep assertion).

## Conclusion

Keep as simple item; retitle to subscription_count only, add the test file to Files, populate validation with the pattern-arg semantics decision.
