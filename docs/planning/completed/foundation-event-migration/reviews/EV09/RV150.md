---
id: RV150
review-of: EV09
plan: plan-foundation-event-migration
state: closed
reviewed-by: claude
reviewed-at: ''
---

# Valid; step text contradicts validation ('re-raise' would break subscriber isolation) and log level changes error→warning

## Notes



## Findings

1. CORE CLAIM CONFIRMED: SubscriberError defined (event_exceptions.py:54) and exported but never raised anywhere; _dispatch_sync catches raw Exception and logs at ERROR (event_bus.py:201-217).
2. CONTRADICTION: step 1 says 'Catch generic Exception, re-raise as SubscriberError' while validation #2 demands log-and-swallow with remaining subscribers still receiving the event. Re-raising from _dispatch_sync would abort dispatch for later subscribers — the isolation guarantee (bus docstring feature #3). Correct behavior: WRAP into SubscriberError for structured logging, never re-raise from the dispatch loop.
3. LOG LEVEL: current code logs at ERROR; validation #1 says WARNING per §9. That is a deliberate downgrade — fine, but tests asserting on caplog levels must be updated; state it as an explicit step.

## Conclusion

Keep (simple, worthwhile for structured diagnostics). Amend step 1 to 'wrap and log, do not re-raise'; add explicit note that the level change error→warning is intentional and test-visible.
