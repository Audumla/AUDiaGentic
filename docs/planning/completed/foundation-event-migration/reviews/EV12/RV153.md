---
id: RV153
review-of: EV12
plan: plan-foundation-event-migration
state: closed
reviewed-by: claude
reviewed-at: ''
---

# Valid P3; internal contradiction between 'keep backward compat' step and 'no shims' validation must be resolved before implementation

## Notes



## Findings

1. CORE CLAIM CONFIRMED: handler type is untyped Callable[[str, dict, dict], None] (event_bus.py:47,79); lifecycle_observer does manual payload validation.
2. CONTRADICTION: step 1 says 'Keep backward compat: untyped handlers still accepted' and step 2 gives EventHandler[T] a dict default, while validation #3 and the notes (§10 No backward compatibility shims) demand all callers migrate simultaneously with no shim paths. These cannot both hold. Resolution consistent with §10: TypedDict payloads are STRUCTURAL typing — a TypedDict IS a dict at runtime, so typed handlers and dict-typed handlers coexist without any runtime shim; the 'no shims' clause is about runtime compat layers, not static generics. Rewrite steps to say: typing-only change, zero runtime branches, all lifecycle-domain callers annotated in the same change.
3. SCOPE: correctly narrowed to lifecycle events. Note Path in LifecycleEventPayload: envelope payloads should stay JSON-serializable for the event store — verify whether project_root is stored as Path or str in current published events before enshrining Path in the TypedDict (event_store persistence may already coerce).

## Conclusion

Keep at P3. Amend steps to the typing-only formulation; validate the Path-vs-str payload question against event_store serialization before choosing field types.
