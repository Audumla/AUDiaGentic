---
id: EV09
order: 15
plan: foundation-event-migration
state: completed
validate-first: false
priority: P2
work: S
---

# Wire SubscriberError into dispatch handler isolation

## Description

`SubscriberError` is defined in `event_exceptions.py:54-76` with structured details (pattern, handler_name, event_type), but `_dispatch_sync` in `event_bus.py:204` catches raw `Exception` and logs it directly. The exception type exists but serves no purpose.

**Affected files:**
- `src/audiagentic/foundation/event/event_exceptions.py:54-76` — unused class
- `src/audiagentic/foundation/event/event_bus.py:201-217` — raw exception handling

## Steps

['Wrap handler exceptions in `SubscriberError` with pattern, handler_name, event_type populated\n- Catch generic Exception in `_dispatch_sync`, re-raise as SubscriberError\n- Log the SubscriberError for structured diagnostics', 'Add optional retry config to SubscriberError if future retry logic is desired (not implemented now)', 'Verify tests still pass — no behavioral change expected for existing handlers']

## Files

['src/audiagentic/foundation/event/event_bus.py', 'src/audiagentic/foundation/event/event_exceptions.py']

## Validation

1. Handler exceptions are caught and logged as SubscriberError with exc_info=True at warning level
2. No behavioral change: remaining subscribers still receive the event (log-and-swallow pattern)
3. SubscriberError is populated with pattern, handler_name, and event_type

## Effort & Risk



## Standards



## Notes

Implemented 2026-07-06 per RV150: handler failures are WRAPPED in SubscriberError (pattern/handler_name/event_type populated) and logged — never re-raised (the 're-raise' step wording was wrong; re-raising would break subscriber isolation). Log level deliberately changed ERROR→WARNING per §9; existing structured-log test updated to assert WARNING + error_code INT-EVT-002. New isolation test proves remaining subscribers still receive the event after a handler failure.
