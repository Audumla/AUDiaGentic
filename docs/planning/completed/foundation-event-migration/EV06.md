---
id: EV06
order: 35
plan: plan-foundation-event-migration
state: completed
validate-first: true
priority: P1
work: S
---

# Delete foundation/capabilities.py (register_capability/get_capability) once all callers are removed

## Description

Once EV01 (deletes harness.config-refresh and harness.runtime-sync), EV02 (deletes providers.mcp-projector), EV03 (deletes provider_recipes.py and its two capability keys), EV05 (deletes providers.surface-validator), and the CP00 rewrite (drops its six planned capability registrations in favor of a component-registry presence check + direct import) have all landed, there are zero remaining callers of register_capability/get_capability anywhere in the codebase. Per EV04's standards correction, 'registered callbacks' is no longer a sanctioned decoupling mechanism -- the module should be deleted outright rather than left in place as unused, speculative infrastructure (no code that isn't earning its keep; a future genuine need, if one ever appears, can re-introduce it deliberately rather than leaving a temptation in place).

## Steps

["PREREQUISITE (make explicit, do not just note): confirm EV04's standards edit (strikes 'registered callbacks') has landed, AND confirm EV01, EV02, EV03, EV05 have all landed, AND confirm component-profiles/CP00's rewrite (registry-presence-check + direct import, no capability registrations) has landed -- do not proceed until all five are verified, not just assumed from plan state", 'Repo-wide grep for register_capability, get_capability, and any import of audiagentic.foundation.capabilities across src/, tests/, docs/, config/components/*.yaml (managed doctrine blocks), and any generated/managed output (e.g. CLAUDE.md, release artifacts) -- confirm zero remaining references before deleting anything; this is broader than a single directory sweep since capability mentions could appear in doctrine text (see EV04) or historical planning docs', 'Delete src/audiagentic/foundation/capabilities.py', "Delete any tests that exist solely to cover foundation/capabilities.py's own behavior (registration idempotency, VAL-CAP-001 conflict error) -- these have no remaining subject once the module is gone", 'Remove foundation/capabilities.py from any documentation, README, or __init__ export list that references it as an available seam', 'Confirm no packaging config (pyproject.toml, MANIFEST, etc.) references the deleted module path explicitly']

## Files

['src/audiagentic/foundation/capabilities.py', 'src/audiagentic/foundation/__init__.py']

## Validation

['grep confirms zero references to register_capability, get_capability, or foundation.capabilities anywhere in src/, tests/, or docs/ (excluding historical planning-doc records under docs/planning/completed/ or reviews/, which are allowed to retain historical mentions as an audit trail)', "grep confirms no config/components/*.yaml doctrine block still recommends the capability-registry pattern (cross-check against EV04's doctrine scan)", 'Full test suite passes with the module removed']

## Effort & Risk

Simple: pure deletion, but gated entirely on every other item in this plan (EV01-05) AND component-profiles/CP00 landing first -- verify all five explicitly rather than assuming from plan state, since this is the irreversible step. This must be the last item to land.

## Standards



## Notes

The logical end state of the whole capability-registry audit that started with the component-profiles CP00/CP10 reviews: every real and planned use of get_capability in this codebase turned out to be a misapplication (RV93-96, EV01-03, EV05, and the CP00 rewrite). Depends on EV01, EV02, EV03, EV04, EV05, and component-profiles/CP00 all landing first. Per review RV100 (2026-07-05): broadened the Files/validation scope beyond capabilities.py alone (tests, docs, config/components/*.yaml doctrine, packaging config, __init__ exports) and made the five-item prerequisite an explicit first step rather than only a note, since this deletion is the irreversible step in the whole plan and should not proceed on an assumption.
