---
id: DU02
order: 0
plan: plan-duplication-sweep
state: completed
breadth: ''
skill: low
---

# Consolidate duplicate payload key normalization into one neutral accessor

## Description

Several modules independently accept dash and underscore variants using local helpers.
If semantics match, introduce one neutral mapping accessor.
Do NOT normalize unrelated public contracts merely because their field names overlap.

## Steps

['Audit modules for duplicate dash/underscore normalization helpers', 'Compare semantics of each helper — do they truly match?', 'Introduce one neutral mapping accessor where semantics match', 'Leave unrelated public contracts alone even if field names overlap']

## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files

['src/audiagentic/']

## Validation

One shared mapping accessor where semantics match. No unrelated contracts forced to normalize.

## Effort & Risk



## Standards



## Notes

Audit complete. Dash↔underscore normalization is a one-line key.replace("_", "-") operation used in agents_gateway_sessions_store.py and agents_gateway_store/_transitions.py — both within the same component (agents). Per architecture standards §3, foundation extraction requires multiple independent consumers — not applicable here. Local utility within agents adds indirection without benefit. Patterns are clear, correct, and locally owned. No consolidation needed.

## Priority

P2
