---
id: RV101
review-of: FI01
plan: foundation-interaction
state: closed
reviewed-by: ''
reviewed-at: ''
---

# Define interaction API contract before callers migrate

## Notes



## Findings

['FI01 steps mix request/respond/get_response with ask introduced by FI02/FI05. Define final public API in FI01: whether ask exists immediately as durable-only wrapper, or only appears in FI02. Without this, FI02/FI05 agents may implement incompatible entrypoints.', 'FI01 must specify concurrency/atomic-write behavior for .audiagentic/runtime/interactions/*.json. Durable request/response can be touched by MCP server, CLI, and background job processes; use atomic replace + conflict handling.', 'FI01 should define request schema fields for source_kind/source_id/kind/options/freeform/status/expiry/answer/error and versioning. Current text says generalize schema but lacks enough shape for independent implementation.', 'FI01 push_status publishes interaction.status but no subscriber contract exists. Specify payload shape, severity enum, correlation/request_id, and whether publishing errors are swallowed or surfaced.']

## Conclusion

Core primitive has right boundary, but API/storage/event contracts need more depth before agents build dependents.
