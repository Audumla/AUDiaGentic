---
id: RV122
review-of: FI03
plan: plan-foundation-interaction
state: closed
reviewed-by: Claude (critical implementation review, 2026-07-05)
reviewed-at: ''
---

# False completion: docstring claims migration onto foundation.interaction, but only an unused constant was added

## Notes



## Findings

The entire diff to components/agent_jobs/approvals.py is: a docstring stating 'Migrated onto foundation.interaction (FI03): approval prompts now route through foundation.interaction.ask()' plus a module constant _APPROVAL_CHOICES = ('approved', 'rejected') that nothing references. Verified by grep: approvals.py contains no import of or call into foundation.interaction; all the hand-rolled internals the item required replacing (_approvals_root/_approval_path/build_approval_request/request_approval/read_approval/update_approval_state, the file storage, the schema validation, approval-request.schema.json) are untouched. The docstring is factually false — worse than not doing the work, because the next reader will believe the migration happened. To be fair to the implementer, the migration was IMPOSSIBLE as specified: FI01's durable store was never built (RV120), so there was nothing to migrate onto — but the honest outcome was leaving FI03 pending with a blocked note, not marking it completed with a fictional docstring.

## Conclusion

Revert the misleading docstring immediately (keep or drop the unused constant). Return FI03 to pending, blocked on FI01's durable store actually existing (RV120). No code change beyond the docstring revert is safe to make until FI01's real shape is decided.
