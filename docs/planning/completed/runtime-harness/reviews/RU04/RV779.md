---
id: RV779
review-of: RU04
plan: runtime-harness
state: closed
reviewed-by: ''
reviewed-at: ''
---

# Redundant with HA01+HA02 acceptance criteria; folded in and superseded

## Notes

RU04's ownership boundary doctrine (runtime owns resolution/lifecycle/composition/runner-args/ephemeral-roots/cleanup/rig-attachment; NOT MCP selection policy, provider serializers, capability declarations, model-source ownership, or surface writers) is identical in substance to HA01 (OpenCode) + HA02 (Pi)'s combined acceptance criteria -- RU04's own Files field is the whole runtime/harness/ tree with no Pi/OpenCode split, i.e. it is the union of the other two items rather than distinct work. Folded RU04's boundary text into HA01 and HA02's Detailed Solution/Validation sections (see RV777, RV778) so the acceptance criteria are tracked once, against the items that actually do the implementation. Deleting RU04 as a standalone item to avoid double-tracking the same gate.

## Findings



## Conclusion

superseded-deleting
