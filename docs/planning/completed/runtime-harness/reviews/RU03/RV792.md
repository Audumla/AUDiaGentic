---
id: RV792
review-of: RU03
plan: runtime-harness
state: closed
reviewed-by: ''
reviewed-at: ''
---

# Executed: Pi's mcp_format.py now delegates to shared foundation projection

## Notes

Consolidated Pi's independent read_pi_mcp_json/write_pi_mcp_json/remove_pi_mcp_json into re-exports of the shared foundation/mcp/json_format.py functions (read_mcp_json/write_mcp_json/remove_mcp_json), matching OpenCode's existing pattern exactly. Kept build_pi_mcp_dict/_entry_to_pi_cfg/pi_mcp_path Pi-specific (full-rebuild-only lifecycle/settings fields), matching OpenCode's build_opencode_mcp_dict precedent -- individual add/remove entry management never needs those fields since the only production caller (session_api.diagnose_mcp_servers, added this session) only reads. Added a mcp_config_path alias in both pi and opencode mcp_format.py modules for facade-name consistency. Removed the fully redundant 1:1 forwarding wrappers (mcp_config_path/read_mcp_config/write_mcp_config/remove_mcp_config) from both runtime/harness/{pi,opencode}/install/__init__.py -- confirmed zero other callers via repo-wide grep first. Updated runtime/harness/__init__.py's facade to dispatch to each provider's mcp_format module directly instead of through the now-removed install-layer wrappers; the facade's own public function names are unchanged so no external caller (session_api.py) needed updating. Verified end-to-end (write/read/remove round-trip through the facade) plus full tests/unit/provisioning/, tests/unit/harness/, tests/unit/providers/ suites -- all green. Item complete.

## Findings



## Conclusion

closeable
