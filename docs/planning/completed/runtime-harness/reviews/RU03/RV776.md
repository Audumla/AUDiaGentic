---
id: RV776
review-of: RU03
plan: runtime-harness
state: closed
reviewed-by: ''
reviewed-at: ''
---

# OpenCode side already consolidated; narrow scope to Pi + install wrappers

## Notes

Code audit: runtime/harness/opencode/mcp_format.py (52 lines) already delegates to foundation/mcp/json_format.py (read_mcp_json/write_mcp_json/remove_mcp_json) -- OpenCode-side duplication is resolved. runtime/harness/pi/mcp_format.py (121 lines) still has a fully independent serializer (_entry_to_pi_cfg, read_pi_mcp_json, write_pi_mcp_json) with Pi-specific lifecycle/settings fields, not delegating to the shared foundation module. Additionally both runtime/harness/{pi,opencode}/install/__init__.py carry a second layer of local mcp_config_path/read_mcp_config/write_mcp_config/remove_mcp_config wrapper functions (opencode:194-212, pi:157-176) around their respective mcp_format modules -- a smaller, separate duplication from the one described in this item. Narrowing scope to reflect what's actually still duplicated.

## Findings



## Conclusion

scope-narrowed-not-closed
