---
id: RV791
review-of: RU02
plan: plan-runtime-harness
state: closed
reviewed-by: ''
reviewed-at: ''
---

# CLI visibility feature removed entirely (superseded, not just narrowed)

## Notes

Per explicit direction, removed the CLI visibility feature entirely rather than trying to preserve/replicate it across harnesses: it was already broken/dead in part (hide_tool_use -> audiagenticHideToolUse had zero consumer anywhere in current templates -- a leftover from a removed embedded/forked-Pi patch mechanism) and had zero OpenCode support at all (no ui.* handling in opencode's install path, and OpenCode has no plugin/extension templates at all, so the underlying live-reload-notification mechanism this feature depended on is Pi-only too). Deleted session_visibility.py entirely; removed cli_visibility/set_cli_visibility from session_api.py, session_mcp.py, session.yaml (tools, descriptions, instructions text), README.md; removed the VAL-SESSVIS-*/RES-SESSVIS-* error-resolution entries; removed hide_thinking_block/hide_tool_use handling from pi/install/config.py's _build_settings_config and their defaults from pi.yaml; removed the now-nonexistent session-ui-visibility-updated reason from reload.py's _ALWAYS_RELOAD set. Updated/removed all affected tests (test_dynamic_agent_config.py, test_mcp_component_servers.py, test_reload.py, test_error_resolutions.py, docker _server_smoke.py) and the stale repo-root SYSTEM.md artifact. This removes one of RU02's five original call sites (session_visibility.py) entirely rather than narrowing it -- remaining RU02 scope is now project_api.py, session_api.py (refresh_harness_config only), session_runtime_status.py, project_components.py's harness_cli_available check.

## Findings



## Conclusion

feature-removed-ru02-scope-narrowed
