---
id: TE01
order: 1
plan: test-fixes
state: completed
validate-first: false
priority: P1
work: L
---

# Fix test suite failures: circular import, CFG-PATHS-001, Windows permissions, schema validation

## Description

### Test failure summary

**Host suite**: 157 failed, 1916 passed, 316 skipped, 1 error in ~67s
**Docker suite**: Additional failures in container (release-bootstrap CLI CFG-PATHS-001, comprehensive provider test)

### Root cause categories

#### 1. Circular import: Registry.register(replace=True) triggers recursive load_providers()

Call chain:
```
_registry.pop(key) with replace=True
 -> _ensure_loaded()
 -> _ensure_provider_modules_registered()
 -> importlib.import_module("audiagentic.components.providers")
 -> adapters/__init__.py : load_providers()
 -> _discover_adapter_packages() imports aider/surface.py
 -> register_contribution_renderer("aider", ..., replace=True)
 -> _contribution_renderer_registry.pop("aider") — triggers ANOTHER _ensure_loaded()
 -> claude/hooks.py -> restrictions_loader.py -> package.py
 -> find_repo_root() fails with CFG-PATHS-001 in pip-installed context
```

Affected files:
- `src/audiagentic/foundation/registry_utils.py:77` — `register()` calls `self.pop(key)` on replace
- `src/audiagentic/foundation/registry_utils.py:163` — `pop()` always calls `_ensure_loaded()`
- `src/audiagentic/components/providers/surfaces/registry.py:57` — `register_contribution_renderer` uses `replace=True`
- All surface modules that call `register_renderer` or `register_contribution_renderer` at import time

Affected tests (~120 failures):
- `tests/integration/lifecycle/test_component_lifecycle.py` — all parametrized variants
- `tests/e2e/cli/test_component_cli.py` — component install/status/uninstall lifecycle
- `tests/e2e/cli/test_release_bootstrap_cli.py` — all 6 release-bootstrap tests
- `tests/e2e/cli/test_install_file_content.py` — all 8 file content tests
- `tests/integration/planning/test_planning_provisioning.py` — all 10 planning provisioning tests
- `tests/integration/release/test_end_to_end_release_flow.py`
- `tests/unit/providers/test_provisioning.py` — all provider CLI and reconcile tests
- `tests/unit/memory/` — memory status, backend config, hindsight provision tests
- `tests/unit/lifecycle/test_harness_seam.py` — lifecycle handler tests
- `tests/integration/jobs/test_job_approvals.py`

#### 2. find_repo_root() at module level in package.py — known limitation, not in TE01 scope

`src/audiagentic/foundation/paths/package.py:54` executes `REPO_ROOT = find_repo_root()` at import time.
This is a structural concern (lazy resolution would be better) but resolves from `__file__`, so dev-mode imports find pyproject.toml via the installed package location. Not the root cause of current test failures — belongs in an AR item if architectural cleanup is needed.

#### 3. Windows permission errors during fresh install

```
PermissionError: [WinError 5] Access is denied:
  'C:\Users\mgs\.gemini\antigravity\mcp_config.json'
  'C:\Users\mgs\.claude\mcp.json'
```

**RV225 correction**: `src/audiagentic/components/providers/services/mcp_sync.py:34-35` already catches `Exception` broadly. The PermissionError likely originates from a different code path in the fresh install flow (lifecycle observer or direct file write). Need to identify the exact caller and add targeted error handling.

Affected test:
- `tests/e2e/lifecycle/test_fresh_install.py::test_fresh_install_creates_scaffold_and_manifest`

#### 4. Schema validation error in fresh install — root cause confirmed by RV225

`assert not list(validator.iter_errors(project_cfg))` fails with "None is not of type 'object'".
Root cause: project.yaml declares `.audiagentic/config/project.yaml` as `create-if-missing`, no template file exists under `src/audiagentic/config/components/project/config/`, and `baseline_sync.py:182` writes `# Installation marker`; YAML loads as `None`, while schema requires `contract-version`, `project-id`, `project-name`, etc.

Affected test:
- `tests/e2e/lifecycle/test_fresh_install.py::test_fresh_install_creates_scaffold_and_manifest`

#### 5. Missing uninstall recipe for antigravity provider (Docker only)

`src/audiagentic/config/providers/antigravity.yaml` has no `cli_install.uninstall` step.
Test `test_cli_providers_have_install_recipe[antigravity]` asserts all CLI providers have both install and uninstall.

---

### Preferred option (revised per RV225)

**A + Schema template + PermissionError fix + G** — Concrete fixes for each root cause:

1. **A**: `Registry.pop(bypass_lazy_loader=True)` when key absent + remove `replace=True` from surface registrations. The bypass parameter is explicit: when caller knows the key doesn't exist, skip lazy load entirely. Default remains False for backward compatibility.

2. **Schema template**: Create `src/audiagentic/config/components/project/config/project.yaml` with minimal valid content that satisfies project-config.schema.json. This replaces the `# Installation marker` fallback for this specific file.

3. **PermissionError fix**: Identify the exact code path raising PermissionError in fresh install and add targeted try/except handling. The mcp_sync module already has broad exception coverage; the likely source is outside its scope.

4. **G**: Add `uninstall` ShellStep to antigravity provider descriptor YAML.

### Implementation steps

(See steps field for detailed sequence.)

### Validation

- `pytest tests/integration/lifecycle/test_component_lifecycle.py -v` — all ~100 parametrized tests pass
- `pytest tests/e2e/cli/test_component_cli.py -v` — all 19 component CLI tests pass
- `pytest tests/e2e/cli/test_release_bootstrap_cli.py -v` — all 6 release-bootstrap tests pass
- `pytest tests/unit/providers/test_provisioning.py -v` — all provider provisioning tests pass
- `pytest tests/integration/providers/test_provider_cli_comprehensive.py -v` — antigravity test passes
- `python tests/run_all.py --fast` — host suite passes (or only expected skips)

## Steps

['Add bypass_lazy_loader: bool = False parameter to Registry.pop(); when key not in _items and bypass_lazy_loader is True, skip _ensure_loaded() call entirely; document default semantics and alias cleanup behavior', 'Remove replace=True from register_renderer() at line 41 and register_contribution_renderer() at line 57 in surfaces/registry.py — these are initial registrations that never need replacement', 'Verify no surface module performs a second registration of the same provider_id (grep for repeated register_renderer calls)', 'Create src/audiagentic/config/components/project/config/project.yaml template with valid schema content: contract-version v1, default project-id, workflow-profile standard, and all required fields per project-config.schema.json', 'Identify PermissionError source in fresh install by adding logging to lifecycle/fresh_install.py provider invocation path; wrap MCP sync call in explicit try/except(PermissionError) that logs warning without aborting', 'Add uninstall ShellStep to src/audiagentic/config/providers/antigravity.yaml cli_install section', 'Run full host suite (python tests/run_all.py --fast) to verify all fixes', 'Run pytest on specific test modules: test_component_lifecycle, test_component_cli, test_release_bootstrap_cli, test_fresh_install, test_provisioning']

## Files

['src/audiagentic/foundation/registry_utils.py', 'src/audiagentic/components/providers/surfaces/registry.py', 'src/audiagentic/components/providers/services/mcp_sync.py', 'src/audiagentic/config/providers/antigravity.yaml', 'src/audiagentic/config/components/project/config/project.yaml', 'tests/integration/lifecycle/test_component_lifecycle.py', 'tests/e2e/cli/test_component_cli.py', 'tests/e2e/cli/test_release_bootstrap_cli.py', 'tests/e2e/lifecycle/test_fresh_install.py', 'tests/unit/providers/test_provisioning.py']

## Validation

['pytest tests/integration/lifecycle/test_component_lifecycle.py -v — expect ~100 tests pass', 'pytest tests/e2e/cli/test_component_cli.py -v — expect 19 tests pass', 'pytest tests/e2e/cli/test_release_bootstrap_cli.py -v — expect 6 tests pass', 'pytest tests/unit/providers/test_provisioning.py -v — expect all pass', 'python tests/run_all.py --fast — expect 0 failures (only expected skips)']

## Effort & Risk

**Effort**: 6 concrete changes across 5 source files. Registry.pop change is low-risk (new parameter with safe default). Surface `replace=True` removal is safe (initial registrations only). Schema template is additive (new file). Antigravity YAML is one-line addition. PermissionError fix requires investigation of exact stack.

**Risk**: Medium. Registry.pop's `bypass_lazy_loader` changes foundation utility behavior — any existing callers relying on lazy-load-on-pop semantics would be unaffected (default False), but new test coverage is needed. Schema template content must match project-config.schema.json exactly or fresh install validation will continue to fail.
## Standards

arch-standards (§7 Error handling: AudiaGenticError at boundaries; §9 Logging: structured extra on warnings); component-creation (component template files must provide valid schema content for create-if-missing assets)

## Notes

RV225 review incorporated 2026-07-09. Post-validation re-assessment:

**Incorporated (4 findings):**
1. mcp_sync.py:35 already catches Exception broadly — need stack trace for actual failure path
2. Schema root cause: project template missing; baseline_sync writes comment-only marker → None on YAML load
3. Registry.pop design now specifies call site, default semantics, and alias behavior
4. Standards field filled with arch-standards §5/§7/§9 references; antigravity path corrected

**Deferred (1 finding):** package.py REPO_ROOT lazy resolution is architectural cleanup, not a test-fix trigger. `find_repo_root()` resolves from `__file__` which points to installed site-packages in dev mode — pyproject.toml found correctly. Out of scope for TE01.
