---
id: RV225
review-of: TE01
plan: test-fixes
state: closed
reviewed-by: codex
reviewed-at: ''
---

# Critical review: TE01 plan is not implementable as written

## Notes



## Findings

- `docs/planning/active/test-fixes/TE01.md:160`: Preferred option excludes `package.py` lazy `REPO_ROOT`, but `REPO_ROOT = find_repo_root()` still runs at import time in `src/audiagentic/foundation/paths/package.py:54`, with many `src/` importers. The claim that CFG-PATHS-001 is secondary only is not proven. Fix `package.py` or add validation proving pip-installed imports no longer hit it.
- `docs/planning/active/test-fixes/TE01.md:180`: MCP PermissionError fix targets wrong code. `src/audiagentic/components/providers/services/mcp_sync.py:34` already catches `Exception`, so `PermissionError` is already covered there. If fresh install aborts, stack likely originates from lifecycle observer/direct projection path. Reproduce stack and target exact caller/write path.
- `docs/planning/active/test-fixes/TE01.md:183`: Schema failure is left as investigation even though root cause is visible: `src/audiagentic/config/components/project.yaml:8` declares `.audiagentic/config/project.yaml`, no source file exists under `src/audiagentic/config/components/project/`, and `baseline_sync.py:177-182` writes `# Installation marker`; YAML loads as `None`, while schema expects object. Plan must prescribe template or generation fix.
- `docs/planning/active/test-fixes/TE01.md:176`: `Registry.pop(bypass_loader=True)` design is underspecified. It does not state call site (`register()` vs surface registry only), default semantics, alias behavior, or tests for lazy registry replacement. Since `Registry` is shared foundation utility, silent lazy-load collision behavior changes are risky.
- `docs/planning/active/test-fixes/TE01.md:208`: `Standards` is empty while the item touches `src/`, violating `docs/planning/CREATING_PLANS.md` section 3/7. Add relevant architecture/error/logging/boundary standards before implementation.
- `docs/planning/active/test-fixes/TE01.md:77`: Antigravity descriptor path is stale. Real file is `src/audiagentic/config/providers/antigravity.yaml`, not `src/audiagentic/components/providers/adapters/antigravity/descriptor.yaml`.


## Conclusion

Do not implement TE01 as-is. Revise first: split verified fixes, add exact failing stacks, fill Standards and Effort & Risk, and make schema plus PermissionError fixes concrete.
