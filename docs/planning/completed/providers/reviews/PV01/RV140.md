---
id: RV140
review-of: PV01
plan: providers
state: closed
reviewed-by: agent
reviewed-at: ''
---

# PV01 Step 1(b): MCP config path verification — no support in CLI, removed entire block

## Notes

This is the critical correction: unverified mcp_config would have written to ~/.gemini/antigravity/mcp_config.json, a file Antigravity CLI never reads. Removal eliminates silent misconfiguration risk identified in PV01 effort_risk section.

## Findings

Step 1(b) verification complete: Searched google-antigravity/antigravity-cli repository for mcp_config (0 matches), AGENTS.md (no evidence in codebase). Original ~/.gemini/antigravity/mcp_config.json was a mistaken assumption from initial addition. Removed entire mcp_config block per PV01 validation criteria: 'correct every claim that fails verification'. Surfaces.renderer and contribution-file retained but marked UNVERIFIED for instruction_file convention. Skill surface path .agents/skills/{tag}/SKILL.md remains (codelab confirmed pattern). Tests reduced from 393→391 as antigravity no longer appears in MCP config parametrization — correct behavior.

## Conclusion

mcp_config section removed entirely — Antigravity CLI does not support MCP server configuration
