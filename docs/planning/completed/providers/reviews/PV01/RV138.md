---
id: RV138
review-of: PV01
plan: providers
state: closed
reviewed-by: agent
reviewed-at: ''
---

# PV01 Step 1(a): CLI install verification — npm package does not exist, corrected to curl scripts

## Notes

YAML edits complete and verified. Remaining steps from PV01: (b) MCP config path confirmation, (c) host-adapter coverage check, test parametrization verification, adapter README review.

## Findings

Step 1(a) verification: `@google/antigravity-cli` npm package returns 404 — DOES NOT EXIST. Correct install method is curl scripts per README: macOS/Linux (curl -fsSL https://antigravity.google/cli/install.sh | bash), Windows PS (irm ... | iex), Windows CMD (curl ... && install.cmd). Step 1(b) MCP config path: matches docs pattern at ~/.gemini/antigravity/mcp_config.json but marked UNVERIFIED pending confirmation. Step 1(c) instruction_file AGENTS.md and skill_surface_path: codelabs confirm .agents/skills/ for project, ~/.gemini/antigravity/skills/ for global; AGENTS.md convention marked UNVERIFIED. Applied fix: replaced toolchain-based cli_install with explicit script form per goose.yaml pattern (package_manager: script, shell command). All 393 provider tests pass.

## Conclusion

Findings incorporated: antigravity.yaml corrected from npm-based to curl script install; unverified claims marked
