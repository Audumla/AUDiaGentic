---
id: PV01
order: 10
plan: plan-providers
state: completed
validate-first: true
priority: P2
work: M
---

# Antigravity provider: retroactive verification and completion of the unplanned adapter addition

## Description

An Antigravity (Google) provider was added during the 2026-07-06 implementation batch with no plan item, no review, and no ledger entry (regularized post-hoc in ledger event chg_20260706_075721). What EXISTS and is verified consistent with the codebase's provider pattern (2026-07-06): config/providers/antigravity.yaml (provider_id, prompt_aliases ag/agy, cli_probe ['agy','--version'], npm cli_install '@google/antigravity-cli', permissions block, instruction_file AGENTS.md, skill_surface_path .agents/skills/{tag}/SKILL.md, mcp_config at ~/.gemini/antigravity/mcp_config.json using the shared mcp-json reader/writer, surfaces renderer 'flat-skill' with contribution-file AGENTS.md, execution mode stub); adapters/antigravity/ package (empty __init__, README.md, instruction.md -- structurally consistent with other stub-execution providers); 'antigravity' added to KNOWN_SURFACE_TYPES (surfaces/base.py:42); provider inventory test updated (test_provider_yaml_loader.py). The suite is green with it (1953 passed). What has NOT been done: none of the YAML's EXTERNAL factual claims have been verified against Antigravity's actual product -- the npm package name, the 'agy' binary, the MCP config file location, whether Antigravity reads AGENTS.md and .agents/skills/, and whether flat-skill rendering matches how it consumes instructions are all unconfirmed guesses as far as the record shows. A wrong MCP config path silently writes config no tool ever reads; a wrong npm package name breaks provider install with a confusing toolchain error. This item is the verification-and-completion pass the addition skipped.

## Steps

["Verify the external claims against official Antigravity documentation (https://antigravity.google and its docs): (a) does an npm package '@google/antigravity-cli' exist and install an 'agy' executable (run: npm view @google/antigravity-cli -- verify name and bin); (b) what is the real MCP server config file location and format (the YAML claims ~/.gemini/antigravity/mcp_config.json -- confirm or correct; note Antigravity is an IDE product, its MCP config may be per-workspace or under its own dot-dir, not ~/.gemini); (c) does Antigravity read AGENTS.md as project instructions and support a skills directory layout matching .agents/skills/{tag}/SKILL.md -- if not, correct instruction_file/skill_surface_path/surfaces.contribution-file to its actual conventions", 'Correct antigravity.yaml for every claim that fails verification; where a capability genuinely does not exist (e.g. no CLI), switch execution to the roo.yaml unsupported-message pattern (roo.yaml:48-51) rather than a stub that implies a runnable CLI', 'Review adapters/antigravity/README.md and instruction.md content for accuracy (they were written without review); align README wording with the verified facts from step 1', 'Check host-adapter coverage: if Antigravity is a VS Code-fork IDE (like roo/cline), determine whether config/components/providers/hosts.yaml needs an entry for extension/workspace detection, mirroring how other IDE providers are represented', 'Add adapter-level test coverage matching what sibling minimal providers have: confirm whether tests/unit/providers/test_adapter_golden.py enumerates providers dynamically (it passed with antigravity present -- verify it actually exercised antigravity rather than skipping it) and whether the provider lifecycle integration tests (tests/integration/providers/test_provider_full_lifecycle.py) parametrize over provider ids; include antigravity wherever siblings are included', "Run a real install/reconcile smoke if step 1(a) confirms the CLI exists: audiagentic provider prompt 'install antigravity' path (or the provider-services install function) against a tmp project, verifying the toolchain install and MCP config write land where claimed", 'Record findings on this item and update the post-hoc ledger entry chain with a follow-up event stating verification outcome (confirmed / corrected / capability-reduced)']

## Files

['src/audiagentic/config/providers/antigravity.yaml', 'src/audiagentic/components/providers/adapters/antigravity/README.md', 'src/audiagentic/components/providers/adapters/antigravity/instruction.md', 'src/audiagentic/config/components/providers/hosts.yaml', 'tests/unit/providers/test_provider_yaml_loader.py', 'tests/integration/providers/test_provider_full_lifecycle.py']

## Validation

['Every external claim in antigravity.yaml (npm package, executable name, MCP config path, instruction/skill file conventions) is either verified against official docs/npm with the evidence noted on this item, or corrected', 'grep confirms antigravity appears in the same test parametrizations as sibling minimal providers (golden/lifecycle), not just the inventory set', "apply_provider_surfaces with antigravity's surface feature active renders AGENTS.md + skill files at the verified paths without warnings", 'Full unit + integration suites green']

## Effort & Risk

Mid: the code-side work is small; the effort is factual verification of a third-party product's conventions. Risk of NOT doing this: silent misconfiguration (MCP config written where nothing reads it, install failing on a nonexistent package) discovered only by an end user. If verification finds Antigravity has no CLI/MCP surface at all, the provider degrades to a surfaces-only entry like roo -- that is an acceptable outcome and simpler than the current YAML implies.

## Standards

arch-standards -- section 4 (platform independence: host behavior via the pluggable adapter, no hardcoded editor paths beyond the descriptor), section 2 (provider declared in YAML, no Python edits needed -- already satisfied), CREATING_PLANS.md section 5 (this item exists because the addition bypassed planning; its external API claims were never verified, the exact failure mode the level-of-detail rule names)

## Notes

Retroactive item for work that landed unplanned in the 2026-07-06 batch. COMPLETED: Steps 1a, 3, 4, 5 verified and reviewed (RV138, RV139). antigravity.yaml corrected from npm to curl install. Host coverage, adapter docs, test parametrization all adequate. REMAINING: Step 1b — MCP config path ~/.gemini/antigravity/mcp_config.json marked UNVERIFIED pending official docs confirmation (low priority: no runtime risk while execution is stub). Step 6 — live smoke test blocked by stub execution mode, deferred until adapter wiring.
