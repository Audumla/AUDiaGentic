---
id: TO05
order: 5
plan: toolchain-provisioning
state: completed
validate-first: true
priority: P1
work: M
---

# TP05 — Install artifact ownership and prune metadata

## Description

Metadata tracking for what files, keys, and hooks a recipe owns. Enables clean uninstall that removes only managed bits without touching user customizations.

## Steps

['Define ArtifactRegistry: tracks owned files, config keys, hooks, env entries per recipe name', 'Implement ownership registration: recipe declares what it creates/modified at install time', 'Implement prune logic: iterate registry, remove only owned artifacts, skip unknown', 'Add collision detection: warn if owned path already exists from another recipe', 'Persist registry as JSON sidecar in .audiagentic state directory']

## Files

src/audiagentic/foundation/toolchains/artifact_registry.py
tests/unit/foundation/toolchains/test_artifact_registry.py

## Validation



## Effort & Risk



## Notes

RV01 Action: `ArtifactRegistry` requires a `dry_run` mode and must handle "missing" managed items gracefully to prevent `prune` from failing due to manual user changes.
