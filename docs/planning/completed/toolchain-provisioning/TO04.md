---
id: TO04
order: 4
plan: toolchain-provisioning
state: completed
validate-first: true
priority: P1
work: M
---

# TP04 — Managed config patch helpers

## Description

Generic helpers for managed config file mutations: TOML/JSON/YAML key insert/remove, hook block registration/removal, MCP entry add/remove. Each operation tracks what it owns for later cleanup.

## Steps

['Implement ConfigPatcher base class with format detection (TOML/JSON/YAML)', 'Add key insert/remove: set value at path, remove key at path, track ownership', 'Add hook block registration: append/insert block into known section, track block boundaries', 'Add MCP entry add/remove: structured entry in config file, dedup on add', 'Each mutation returns OwnedChange with rollback info for prune/uninstall']

## Files

src/audiagentic/foundation/toolchains/config_patcher.py
tests/unit/foundation/toolchains/test_config_patcher.py

## Validation



## Effort & Risk



## Notes

RV01 Action: `ConfigPatcher` must implement deep-path creation (auto-creating intermediate keys) to prevent failures when patching nested structures.
