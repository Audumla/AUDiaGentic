---
id: RV778
review-of: HA02
plan: plan-harness-runtime-ownership
state: closed
reviewed-by: ''
reviewed-at: ''
---

# Stale MO03 dependency reference; fill target solution; fold in RU04 boundary + RU03 overlap

## Notes

Code audit confirmed: runtime/harness/pi/mcp_format.py (121 lines) contains a full independent Pi MCP serializer (_entry_to_pi_cfg, read_pi_mcp_json, write_pi_mcp_json), separate from components/providers/adapters/pi/ serializers; runtime/harness/pi/install/__init__.py imports these runtime-local functions (~158-173) instead of a providers-owned serializer. This item genuinely still needs the implementation described.

Stale reference: same MO03 mismatch as HA01 (see RV777 on HA01) -- dropping it. MO06 (completed) remains the real, satisfied foundation dependency.

RU04 describes the same runtime/harness ownership boundary as HA01+HA02 combined -- folding its boundary doctrine into this item's Detailed Solution too, then closing RU04 as a separate item once HA01+HA02 land. RU03 (Pi mcp_format consolidation onto the shared foundation projection) is a direct implementation dependency of this item -- sequence RU03's Pi work alongside this item since both touch the same files.

## Findings



## Conclusion

documented-target-solution-not-executed
