---
id: RV793
review-of: HA02
plan: plan-harness-runtime-ownership
state: closed
reviewed-by: ''
reviewed-at: ''
---

# Audited violation resolved via RU03+PI01+PI05; closing with one new finding logged for follow-up

## Notes

HA02's audited violation (RV778) was specifically Pi's independent mcp_format.py serializer -- resolved by RU03 (now delegates to the shared foundation projection, matching OpenCode). Checked the rest of HA02's steps/validation against current code:
- Package patches gated by version/idempotence: done (PI01, mcp_exclusive_patch.py).
- Stale wrappers/serializers/aliases deleted: done (RU03 removed the install/__init__.py 1:1 forwarding wrappers).
- No universal reconcile/second gateway in Pi install path: confirmed, none present.
- Embedded runtime-home assets remain runtime-owned: confirmed (rig binaries/models/agent dir under runtime/harness/pi/).
- Uninstall preserves models/logs/user assets: confirmed -- cleanup_runtime() only removes target/'agent', explicitly documented as preserving rig binaries/models/logs.
- _build_models_config (embedded-rig connection settings) reviewed and judged correctly runtime-owned -- this is rig attachment (explicitly runtime's job per the doctrine), not general provider/model-source management; it's a different concern from providers/adapters/pi/model_config.py's desired-state model registry, which already lives under providers correctly.

New finding (not part of HA02's original audited scope, logged for a future decision rather than acted on unilaterally): Pi is the only managed provider with no components/providers/adapters/pi/surface.py -- every other provider (aider, claude, cline, codex, local_openai, opencode, roo) has one. Pi's SYSTEM.md materialization (_build_system_md in runtime/harness/pi/install/config.py) instead goes through the shared runtime.harness.system_prompt injection builder. This may be an intentional architectural difference (SYSTEM.md is Pi's actual system prompt, not a supplementary surface doc like AGENTS.md/CLAUDE.md) or may be the same class of violation HA01 is fixing for OpenCode's AGENTS.md -- genuinely unclear without a design decision, so not folded into this closure.

## Findings



## Conclusion

closeable-with-followup-noted
