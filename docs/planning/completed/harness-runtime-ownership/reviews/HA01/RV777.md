---
id: RV777
review-of: HA01
plan: harness-runtime-ownership
state: closed
reviewed-by: ''
reviewed-at: ''
---

# Stale MO03 dependency reference; fill target solution; fold in RU04 boundary + RU03 overlap

## Notes

Code audit confirmed the core claim: runtime/harness/opencode/install/__init__.py:72 _build_agents_md() writes AGENTS.md directly and :86 _build_opencode_provider_config() builds the provider baseURL/apiKey/models block written at :150-154 -- both are runtime-owned provider-config/surface writers that duplicate a provider-owned equivalent already sitting unused: components/providers/adapters/opencode/surface.py:14 already exposes render_contributions = make_single_file_contribution_renderer('AGENTS.md'). This item genuinely still needs the implementation described.

Stale reference: HA01's dependency on 'MO03 canonical OpenCode path decision' does not match any current MO03 (that ID's actual completed title is unrelated -- first-wave local model provider adapters). The real OpenCode-path decision already appears resolved in code: the install/__init__.py comment block documents that .mcp.json is not used and OpenCode receives its MCP surface via OPENCODE_CONFIG_CONTENT at launch (mcp_surface.py). Dropping the stale MO03 reference; MO06 (completed -- managed-config descriptor/registry/sync core) remains the real foundation dependency and is already satisfied.

RU04 describes the same runtime/harness ownership boundary as HA01+HA02 combined, with no Pi/OpenCode split of its own -- folding its boundary doctrine into HA01's (and HA02's) Detailed Solution and Validation, then closing RU04 as a separate item rather than tracking the same acceptance criteria twice. RU03 (Pi mcp_format consolidation) is a preceding/companion piece to HA02 specifically, not HA01 -- OpenCode's mcp_format duplication is already resolved per RU03 audit.

## Findings



## Conclusion

documented-target-solution-not-executed
