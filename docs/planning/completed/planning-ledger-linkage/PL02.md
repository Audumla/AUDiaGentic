---
id: PL02
order: 1
plan: plan-planning-ledger-linkage
state: completed
validate-first: true
priority: P2
complexity: simple
created-by: ''
---

# Add ledger-events body section to plan item schema

## Description

One-line change: add `"ledger-events"` to `ITEM_SECTION_HEADING` in `src/audiagentic/components/planning/item_store.py` line 66.

```python
ITEM_SECTION_HEADING["ledger-events"] = "Ledger Events"
```

This creates the body-section slot that PL04's subscription handler writes into. Zero behavioural change — `update_item` already routes any key in `ITEM_SECTION_HEADING`. The section will be auto-populated by the event-driven linkage (PL04), not agent-driven.

**Implement before PL04** so the planning subscription handler has a valid target section.

## Steps



## Files



## Validation



## Effort & Risk



## Standards



## Notes

Partially superseded by PL04. The `ledger-events` section is still needed as the storage target on plan items, but population happens automatically via event subscription instead of agent-driven `plan_update_item`. If PL04 is implemented first, only the item_store.py change (adding to ITEM_SECTION_HEADING) remains from this item.
