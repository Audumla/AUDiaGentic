---
id: RV392
review-of: BU01
plan: bus-topic-standards
state: closed
reviewed-by: claude
reviewed-at: ''
---

# T3 verification: accept core delivery; fix topic ownership + missing step 5 helper

## Notes

Verified delivered: ARCHITECTURE_STANDARDS §17 standard, event-topics.schema.json, topic_registry.py (CON-EVT-011 duplicate-owner, last-wins overlay, grammar validation), AST conformance detector (9 tests, tests/unit/foundation/event/), OBSERVABILITY boundary note, seeded events.yaml for agents/planning/foundation/release, allowlist reduced to ONE dynamic f-string site (agents_gateway_queue agents.llm.{event_suffix} — BU02's first target). Full tests/unit green. DEFECTS: (1) ledger.event.recorded seeded under planning/events.yaml — violates the standard's own single-owner rule; must move to a ledger-owned events.yaml (the duplicate-owner guard cannot catch wrong-single-owner). (2) Step 5 assert_event_payload test helper not implemented. (3) Nit: §17 should disambiguate `config/components/<component>/events.yaml` [package-owned topic registry] from `.audiagentic/config/interoperability/event-subscriptions.yaml` [project-local subscriptions — separate concern]. No ledger event was recorded for the implementation — add one.

FIXED 2026-07-14: (3) renamed interoperability/events.yaml to event-subscriptions.yaml, updated project.yaml descriptor, added disambiguation line to OBSERVABILITY_STANDARDS.md §"Bus topics vs timeline event names".

## Findings

VERIFIED DELIVERED (accept, do not redo): ARCHITECTURE_STANDARDS §17 standard; event-topics.schema.json; foundation/event/topic_registry.py (CON-EVT-010 grammar, CON-EVT-011 duplicate-owner, VAL-EVT-012 schema, singleton + load_all_event_topics via get_component_config_dirs); AST conformance detector tests/unit/foundation/event/test_event_topic_conformance.py (9 tests); OBSERVABILITY boundary note; seeded events.yaml for agents/planning/foundation/release; allowlist reduced to one dynamic f-string site (agents_gateway_queue.py:58 `agents.llm.{event_suffix}` — BU02's first migration target). Full tests/unit 0 failures.

FIXED DURING VERIFICATION (already applied, verified green — 22 event tests, full unit 0 failed): `ledger.event.recorded` was seeded under planning/events.yaml, violating §17's single-owner rule; moved verbatim to NEW src/audiagentic/config/components/ledger/events.yaml (owner: ledger). Note: the duplicate-owner guard cannot catch wrong-single-owner — seeding must be reviewed against actual publishers.

REMAINING WORK (implementing agent):
1. STEP 5 HELPER — add `assert_event_payload(topic: str, payload: dict, metadata: dict | None = None) -> None` to foundation/event/topic_registry.py. Behavior: resolve spec from the singleton registry; AssertionError naming the topic when unregistered; AssertionError naming the exact missing keys against spec.payload_required; when metadata is passed and spec.metadata_keys non-empty, assert those keys present. Test-time only — never call in the publish path. IMPLEMENTATION TRAP (hit and reverted during verification): a naive `if not registry.topics: load_all_event_topics()` guard FAILS inside test_event_topic_conformance.py because earlier tests in that file leave the singleton in a partially-populated state (scratch registrations without a full load), so emptiness is a false signal. Required approach: module-level `_fully_loaded` flag set by load_all_event_topics (reset alongside _registry_instance), helper loads when the flag is unset; AND the new tests must isolate the singleton via a fixture that saves/restores `_registry_instance` (+ flag). Add tests (new class in the conformance test file or a test_topic_registry_helper.py): valid payload for `ledger.event.recorded` passes; missing `plan-item-ids` raises AssertionError matching 'plan-item-ids'; unregistered `nosuch.topic.happened` raises matching 'not registered'; metadata-key check exercised on a scratch spec.
2. §17 NAMING-DISTINCTION NOTE — one sentence in the §17 registration subsection: the project-scoped scaffold file `.audiagentic/config/interoperability/events.yaml` (project component, create-if-missing placeholder currently containing only an installation marker) is NOT a topic registry and is never loaded by topic_registry.py; component registries live only at config/components/<owner>/events.yaml.
3. LEDGER EVENT — the BU01 implementation recorded no change event (doctrine violation). Record one covering: §17 standard, schema, topic_registry.py, conformance detector, 4+1 seeded events.yaml files (incl. the ledger ownership fix), OBSERVABILITY note, and the step 5 helper; change-class docs+feature is acceptable as `feature`; include plan-item-ids ["BU01"].
4. COMPLETION — after 1-3: update BU01 notes (steps 5/6/7 completion, ownership-fix record), run tests/unit/foundation/event/ + full tests/unit (expect 0 failures), set BU01 state completed, close this review (RV392), and update MA00 (managed-config-consistency) progress note + dispatch board: BU01 done unblocks MA17 step 11; BU02 becomes next spine-independent slice.

## Conclusion

Fix ownership seeding, add step 5 helper + test, add interop naming note, record ledger event; then BU01 completes.
