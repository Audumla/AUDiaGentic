---
id: RV393
review-of: BU01
plan: bus-topic-standards
state: closed
reviewed-by: claude
reviewed-at: ''
---

# Complete BU01: step 5 assert_event_payload helper, implementation ledger event, completion protocol

## Notes

Follow-up to RV392, which was closed after fixing only its item (3) (interoperability scaffold renamed to event-subscriptions.yaml + OBSERVABILITY disambiguation — verified present, accepted). Items (1), (3-ledger) and (4) of RV392's findings remain outstanding; this review carries them. Verification state at creation: assert_event_payload absent from topic_registry.py (grep 0); only ledger fragment for BU01 work is the rename event chg_20260714_093557 — the main §17/schema/registry/detector/seeding implementation still has NO change event; BU01 state still pending. Also already applied and verified by reviewer (do not redo): ledger.event.recorded moved from planning/events.yaml to new src/audiagentic/config/components/ledger/events.yaml per the single-owner rule; event suite 22/22, full unit 0 failures.

## Findings

WORK ITEMS (implement in order):

1. STEP 5 HELPER — add `assert_event_payload(topic: str, payload: dict[str, Any], metadata: dict[str, Any] | None = None) -> None` to foundation/event/topic_registry.py. Behavior: resolve the spec from the singleton registry; raise AssertionError naming the topic when unregistered; raise AssertionError naming the exact missing keys against spec.payload_required; when metadata is provided and spec.metadata_keys is non-empty, assert those keys are present. Test-time hook only — never called in the publish path (state this in the docstring).
   KNOWN TRAP (reviewer hit this and reverted): a naive `if not registry.topics: load_all_event_topics()` lazy-load guard FAILS when run inside tests/unit/foundation/event/test_event_topic_conformance.py — earlier tests there leave the singleton partially populated with scratch registrations, so registry emptiness is a false 'loaded' signal and real topics like ledger.event.recorded are missing. Required design: add a module-level `_fully_loaded: bool` set by load_all_event_topics() and reset wherever `_registry_instance` is reset; the helper triggers load_all_event_topics() when the flag is unset. The new tests MUST isolate the singleton via a fixture that saves/restores `_registry_instance` and the flag.
   TESTS (new class in the conformance file or tests/unit/foundation/event/test_topic_registry_helper.py): (a) valid payload for `ledger.event.recorded` ({'event-id': ..., 'plan-item-ids': [...]}) passes; (b) payload missing plan-item-ids raises AssertionError matching 'plan-item-ids'; (c) unregistered topic `nosuch.topic.happened` raises AssertionError matching 'not registered'; (d) metadata-keys check exercised via a scratch EventTopicSpec registration.

2. IMPLEMENTATION LEDGER EVENT — record the missing change event for the whole BU01 implementation (doctrine: substantive work must be ledgered; only the rename has an event). change-class: feature; plan-item-ids: ['BU01']; files: ARCHITECTURE_STANDARDS.md §17, OBSERVABILITY_STANDARDS.md, event-topics.schema.json, foundation/event/topic_registry.py, tests/unit/foundation/event/test_event_topic_conformance.py, the five seeded events.yaml files (agents, planning, foundation, release, ledger), plus the step-1 helper files from this review. technical-summary must cover: §17 standard (naming/ownership/registration/constant-not-literal/payload-contract rules), registry loader with CON-EVT-010/011 + VAL-EVT-012, AST conformance detector with single-entry pre-migration allowlist (agents_gateway_queue.py:58 dynamic f-string — BU02's first target), seeded topic counts, ledger ownership fix, assert_event_payload helper.

3. COMPLETION PROTOCOL — (a) update BU01 notes: steps 5/6/7/9 completion state, the ownership fix, the event-subscriptions rename; (b) run tests/unit/foundation/event/ and full tests/unit — both must be 0 failures; (c) plan_set_state BU01 completed; (d) close THIS review; (e) update MA00 (managed-config-consistency): progress note + dispatch board — BU01 completion unblocks MA17 step 11, BU02 becomes the next spine-independent dispatchable slice, and add the exact test commands/results to MA00's verification register.

## Conclusion

BU01 must not be marked completed until all three work items land; any deviation from the helper design here needs a new review with rationale.
