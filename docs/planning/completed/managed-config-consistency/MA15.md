---
id: MA15
order: 20
plan: plan-managed-config-consistency
state: completed
validate-first: true
priority: P1
work: M
created-by: codex
---

# Add packaged startup and public CLI smoke suite

## Description

Block A2, executed only after MA11 implementation review accepts the final step/CLI substrate. Complete existing Docker packaging/startup work; do not create another image or CLI test framework. Prove installed wheel import, public console entrypoint, package data/component loading, stable negative exits, and containment of all mutations inside disposable container paths.

## Steps

0. ENTRY GATE: confirm MA11 is completed and reviewed. Read current `tests/docker/Dockerfile.packaging`, `tests/docker/run_packaging.sh`, `pyproject.toml` console script, launcher, `tests/run_all.py`, CI config, and testing docs. Run `bash -n tests/docker/run_packaging.sh`. Do not edit before recording current behavior.
1. Inventory current partial implementation: Dockerfile already sets disposable HOME/AUDIAGENTIC_HOME/XDG roots; runner already checks installed import, `audiagentic --help`, component list from `/tmp`, parser failure, harness containment. Mark each MA15 requirement present/missing; avoid duplicating passing checks.
2. Validate Docker availability with `docker version`. If unavailable, record exact blocker and stop MA15 pending; do not replace with host-mutating tests.
3. Build using existing packaging Docker recipe with an explicit timeout sufficient for dependency/wheel build. Capture command, image tag, rc, and final output. Do not mount host home/config and do not inject checkout `PYTHONPATH` into installed assertions.
4. Inside image, build wheel from checkout, install wheel into isolated environment, then change cwd to a directory outside checkout. Unset `AUDIAGENTIC_REPO_ROOT` and `PYTHONPATH` for installed-package checks.
5. Run import smoke: import package and launcher; assert rc 0, no traceback, and no files outside disposable HOME/project/cache roots using before/after snapshot.
6. Run installed `audiagentic --help`; assert console script resolves from installed distribution. Run `audiagentic --project <empty-temp-project> component list`; parse output according to actual public format and assert foundational/project component descriptors load from package data.
7. Run negative matrix once: unknown global option, unknown command, command missing required argument/subcommand, and one handled AudiaGenticError route. Pin exit codes/stderr/no-traceback from launcher contract; do not duplicate every command parser test.
8. Run minimum lifecycle dispatch smoke required to prove packaged command wiring. Mark Docker test `mutates_host`; assert resulting files remain under container temp roots. Do not launch interactive provider UI or install external providers.
9. Add/finish shared subprocess result helper only if two or more Docker checks need identical capture/timeout/snapshot logic. Keep helper test-owned; do not add production CLI abstraction.
10. Wire existing packaging runner into `tests/run_all.py` and CI packaging job using current Docker conventions. If CI already invokes runner, document rather than duplicate job.
11. Update `tests/TESTING.md` with one canonical local command, prerequisites, containment guarantee, expected duration, and image cleanup. Link MA09 acceptance.
12. FINAL VALIDATION: `bash -n`, Docker build/run twice from clean container state, host unit launcher tests, containment snapshot. Record exact outputs and ledger event. Mark complete only after real Docker pass. Stop for Block A review.

## Files

[TEST] host-only mocked launcher parser/dispatch units
[TEST] Docker-gated installed CLI startup/load tests and shared subprocess fixture
[DOCKER] tests/docker/Dockerfile.packaging and existing packaging runner
[CI] existing packaging image registration/gate
[DOCS] tests/TESTING.md startup/CLI Docker procedure

## Validation

1. MA11 reviewed complete before start. 2. Existing Docker image/runner reused; no parallel image/framework. 3. Wheel installs and imports outside checkout without PYTHONPATH/repo-root dependency. 4. Installed console help and component list pass. 5. Negative matrix has stable nonzero exits/actionable stderr/no traceback. 6. No host home/config mount; every mutation inside disposable container roots. 7. Docker run succeeds twice cleanly. 8. tests/run_all/CI/docs point to same runner. 9. Exact command/results and ledger event recorded. STOP after MA15 for Block A review.

## Effort & Risk

Mid. Docker startup adds build time but matches existing clean-room packaging convention. Reuse `Dockerfile.packaging`; avoid duplicate images. Container must build/install local artifact without network and never bind-mount host home/config.

## Standards



## Notes

RV312/RV357 incorporated. Final gate uses one existing Dockerfile/runner. Exact validation: `pytest tests/unit/test_launcher_arguments.py tests/unit/foundation/paths tests/unit/release/test_release_packaged_assets.py -q` => 9 passed; `docker run --rm -v "${PWD}:/workspace:ro" audia-packaging bash -n /workspace/tests/docker/run_packaging.sh` => rc 0; `docker build -f tests/docker/Dockerfile.packaging -t audia-packaging .` => rc 0; two consecutive `docker run --rm audia-packaging` => RUN=1 rc 0, RUN=2 rc 0. Each run passed wheel import, public help, isolated descriptor load, four-case negative CLI matrix, harness install, all declared MCP server smoke checks, 35 release CLI E2E tests, required workflow package-data checks, and immutable /app+/root containment snapshot. Registered command unknown tokens now fail closed; provider no-command passthrough remains explicit behavior. Docker server smoke uses disposable /tmp project root. Full unit sweep also found unrelated coding-LSP planned-command nesting and stale planning tests; generic LSP defect fixed separately, planning-test cleanup remains outside MA15.

## Ledger Events

- chg_20260712_114538_added-docker-isolated-startup_8101
- chg_20260713_115022_packaged-audiagentic-now-start_9752
