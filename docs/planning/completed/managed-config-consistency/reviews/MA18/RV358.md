---
id: RV358
review-of: MA18
plan: plan-managed-config-consistency
state: closed
reviewed-by: codex
reviewed-at: ''
---

# Prove existing gateway transport before ACP work

## Notes



## Findings

Existing blocking gateway baseline proven and repaired: authoritative project working-root plus profile stream-controls now reach neutral provider packet; metadata cannot redirect cwd. Focused gateway/provider tests pass and fresh OpenCode deep-coder round-trip returns exact output. Official ACP research confirms Agent Client Protocol protocol version 1 and OpenCode 1.17.18 native `opencode acp`. Implemented optional official Python SDK 0.11.0 transport in foundation, thin OpenCode launch/model override binding, removed incorrect Agent Communication Protocol scaffold, and proved real incremental ACP session with exact `ACP_DEEP_CODER_OK`. No duplicate queue/session/event store, provider branch, raw persistence, or string registry added. Remaining MA18 work: gateway event forwarding/persistence, cancellation, bounded retention/backpressure, redaction and malformed/process-exit fixtures, Docker clean-package smoke.

## Conclusion

Baseline and first ACP vertical slice validated. Review findings incorporated. MA18 remains pending until gateway/event hardening and full validation complete.
