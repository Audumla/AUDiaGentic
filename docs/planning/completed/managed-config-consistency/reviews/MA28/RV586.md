---
id: RV586
review-of: MA28
plan: plan-managed-config-consistency
state: closed
reviewed-by: codex
reviewed-at: ''
---

# MA28 left a forbidden LanguageServerEntry compatibility re-export

## Notes

Independent post-completion audit for the 24-hour cohort. Review against Architecture Standards, Architecture Guidelines, Observability Standards, parent-plan intent, and reuse-before-build doctrine. Check foundation paths/workflow/events/process/observability primitives before accepting new machinery; prefer contained corrections and deletion/consolidation over parallel abstractions.

## Findings

HIGH — MA28 validation requires the old Coding-LSP projection entry point and compatibility routes to be absent, but `components/coding_lsp/language_servers.py` re-exported `providers_api.LanguageServerEntry` explicitly `for backwards compatibility`, and three provider test modules still imported the obsolete requester-owned path. This violates Architecture Standards §4 and Guidelines §9 and masks the intended ownership direction.

## Conclusion

Resolved in this audit by deleting the re-export module and migrating all remaining consumers to `providers.providers_api.LanguageServerEntry`. Focused provider/architecture validation passes: 98 tests.
