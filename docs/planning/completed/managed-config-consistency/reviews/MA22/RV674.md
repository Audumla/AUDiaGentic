---
id: RV674
review-of: MA22
plan: plan-managed-config-consistency
state: closed
reviewed-by: codex
reviewed-at: ''
---

# Triage lifecycle provider projection registration and disabled planning semantics

## Notes



## Findings

Verified the stale planning test contradicted the authoritative provider lifecycle: disabling an installed component removes its managed provider MCP entries. Seven descriptor-backed surface renderers (antigravity, copilot, gemini, goose, openhands, plandex, qwen) lacked the existing generated-surfaces family declaration, so universal generated-surface dispatch rejected them with RES-PREC-001.

## Conclusion

Implemented the narrow MA22 correction. The planning lifecycle test now asserts remove-on-disable; all seven renderer descriptors declare the pinned generated-surfaces capability; a contract test proves every registered surface renderer declares the exact family pin. Docker focused validation passed: 39 tests across planning provisioning, generated-surface contracts, YAML loading, and descriptor-driven renderers. No new handler, API, registry heuristic, or reconciliation change was added.
