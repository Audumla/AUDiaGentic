---
id: RV781
review-of: MA22
plan: managed-config-consistency
state: closed
reviewed-by: ''
reviewed-at: ''
---

# Executed: removed universal reconcile from public API and MCP surface

## Notes

Blockers (SH07, MA17) were already completed per plan state. Executed the remaining narrow removal: deleted reconcile_provider/reconcile_all_providers MCP tool wrappers from providers_mcp.py, their declarations/descriptions from config/components/providers.yaml, and their public providers_api.py wrappers + __all__ exports. Verified via grep that commands/launch.py's on-launch reconciliation already calls services.lifecycle.reconcile_all_providers directly (the private provider-owned composition path this item wants preserved) rather than through providers_api or MCP -- updated its stale comment referencing 'available via the provider MCP server'. Updated tests/e2e/cli/test_mcp_component_servers.py::test_providers_server_exposes_expected_tools to assert absence of both tool names instead of presence. tests/unit/providers/test_provisioning.py and tests/unit/harness/test_launch_provider_reconcile.py were unaffected (they exercise services.lifecycle/launch.py internals directly, not the deleted public surface). Full tests/unit/providers/ suite (367 tests) and tests/unit/foundation/toolchains/test_architecture_boundaries.py pass. Two pre-existing, unrelated e2e failures in test_mcp_component_servers.py were confirmed present before this change too (via git stash) and are out of scope. Item complete.

## Findings



## Conclusion

closeable
