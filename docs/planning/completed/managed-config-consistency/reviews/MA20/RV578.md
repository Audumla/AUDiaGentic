---
id: RV578
review-of: MA20
plan: plan-managed-config-consistency
state: closed
reviewed-by: codex
reviewed-at: ''
---

# MA20 start audit: descriptor isolation-tier validation blocks provider loading

## Notes



## Findings

Valid at discovery, now incorporated. execution_isolation_tier is a required provider-level descriptor field with the three allowed values, all provider YAML descriptors declare it, and automation capabilities no longer carry or duplicate isolation policy. The provider loader and recipe registry load successfully.

## Conclusion

Resolved with the contained provider-level seam requested by the review. No provider-name inference or gateway-owned fallback was introduced. Focused descriptor, recipe, declarative adapter, and registry validation passes (59 tests).
