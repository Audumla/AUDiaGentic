---
id: RV441
review-of: MA09
plan: plan-managed-config-consistency
state: closed
reviewed-by: opencode-deep-review
reviewed-at: ''
---

# Closure gate cannot validate: depends on MA17 graph + §18 carve-out while MA17 is blocked by unresolved MA16/RV412

## Notes

MA09 is Block E (final closure gate). It is sound in its static checks (MA10/MA11 verified completed) but its dynamic gates (MA17 graph, §18 carve-out) depend on resolving the RV412/RV414 divergence first.

## Findings

1. MA09.md:21-22 (step 2-3) requires re-running the MA01 scanner and 're-run MA17 graph: only sanctioned providers public API edges.' But MA17 (the graph owner) is itself blocked by the open RV412/RV414 and by MA16's unresolved API-surface contradiction (RV436/439). A graph that does not yet have a settled boundary cannot be re-run to a green state.

2. MA09.md:54 (Notes, 'RV405/RV406 GATE') updates criterion 4 to verify the arch §18 rule with its carve-out, but the implemented `providers_api.py` concrete-function surface does not use `registry.register(recipe)` for model sources/surfaces — i.e., it may already violate §18.3's sole-authority rule. MA09's gate would surface this as a failure unless the standard and code are first reconciled.

3. MA09.md:20 (step 1) lists MA11 and MA10 as 'already completed' — verified: MA10.md and MA11.md both state `state: completed`, and foundation.steps/probes substrate they delivered is referenced by the active MA12/MA13 items. This part of MA09's precondition is sound.

4. MA09 depends on MA16 (deletion + API) and MA17 (graph) being complete; neither is complete while RV412/RV414 remain `created`. Treating MA09 as a validatable gate now overstates readiness of the whole program.

## Conclusion

RESOLVED (2026-07-15): the blocking premise is dissolved. RV441 assumed MA17 is blocked by an unresolved capability-gateway/§18.3-registry question; RV412's ratification removes that — there is no universal registry mandate, §18.3 governs automation authority (orthogonal to surface shape), and MA17's boundary graph audits requester→providers_api edges under the RV405 allowlist. MA09's REAL dependency is unchanged and legitimate: it cannot close until the tracked pre-existing import violations (MA08/MA02/MO02) are cleared and MA17's boundary graph re-runs clean — that is normal gate sequencing, not a contradiction. Captured in MA09 scope.
