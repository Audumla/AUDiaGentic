---
id: RV311
review-of: MA09
plan: managed-config-consistency
state: closed
reviewed-by: codex
reviewed-at: ''
---

# Startup and public CLI acceptance coverage missing

## Notes



## Findings

MA09 requires broad integration suites but does not explicitly prove distributable package starts through public `audiagentic` console script. Existing E2E helpers primarily invoke `python -m audiagentic.launcher` with source-tree PYTHONPATH, so packaging/entry-point/import failures can escape. No explicit clean-process tests cover import, help, component descriptor load, invalid invocation, or isolated filesystem behavior.

## Conclusion

Add dedicated MA15 startup/CLI smoke suite. Make MA15 prerequisite and explicit acceptance matrix in MA09.
