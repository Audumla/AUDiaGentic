---
id: RV327
review-of: MA09
plan: plan-managed-config-consistency
state: closed
reviewed-by: codex
reviewed-at: ''
---

# Cross-component dependency leaks remain outside managed-mutation scope

## Notes



## Findings

Sweep found: (1) providers descriptors/services import coding_lsp types while coding_lsp sync imports providers LSP projection, creating a bidirectional component dependency; (2) providers skill surfaces and provider adapters import agent_jobs prompt launch/syntax directly; (3) agent_jobs imports providers descriptors/config/execution directly; (4) ledger imports release event symbol directly. These are concrete component-to-component implementation imports, not foundation contracts or declared composition-root ports. Existing MA03–MA08 cover selected mutation paths but not this dependency graph.

## Conclusion

Create MA17 to inventory, classify, and replace forbidden component implementation imports with foundation contracts, neutral event payloads, or composition-root ports. MA09 blocks on zero unapproved cycles/leaks.
