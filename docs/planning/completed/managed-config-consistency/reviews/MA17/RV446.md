---
id: RV446
review-of: MA17
plan: plan-managed-config-consistency
state: closed
reviewed-by: ''
reviewed-at: ''
---

# Plan agents execution public-boundary extraction

## Notes



## Findings

`agents_gateway_dispatch._dispatch_one_attempt` directly imports providers services execution, models, and provider_config. It owns agent-profile/retry/timeline concerns but reaches provider internals to check enabled state, load config, resolve model, and execute. Need a narrow providers_api execution invocation entry that accepts only existing provider-neutral invocation values, keeps profile/retry/timeline in agents, and preserves current normalized result/error behavior.

## Conclusion

RATIFIED. MA17 now extracts agents execution through one providers_api invocation entry. Exact forbidden imports and preserved agents responsibilities/test seams are recorded; execution remains separate from provider-operation modes.
