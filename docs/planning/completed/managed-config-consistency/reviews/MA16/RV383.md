---
id: RV383
review-of: MA16
plan: plan-managed-config-consistency
state: closed
reviewed-by: claude
reviewed-at: ''
---

# Canonical capability vocabulary v1 added (§8) — Block C pre-map

## Notes

New §8 in CAPABILITY_OPERATION_SCHEMA.md pins capability-ids, payload-schema ids, operations, and ownership-scope conventions for all Block C/D consumers: cli, cli-probe, mcp-entries, language-servers, config-fragment, hooks, plugin-entries, instructions, model-projection, model-catalog, launch-env, execute. Rules: repair is a requester-level flow (status→conditional install apply), not a schema operation; read-only capabilities declare modes not-applicable; payloads carry memory-library values but ids/schemas stay domain-neutral. Does not change the approved record shape; decision sections renumbered to §9.

## Findings



## Conclusion

Vocabulary pinned; MA12 step 2, MA02 step 4, MO02 step 2, MA08 step 2 consume the table instead of inventing ids.
