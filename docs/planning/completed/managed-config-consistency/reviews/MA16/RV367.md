---
id: RV367
review-of: MA16
plan: managed-config-consistency
state: closed
reviewed-by: claude
reviewed-at: ''
---

# Split MA16: core schema/catalog now, evidence-process and generated-reference machinery to sidecar

## Notes

User decision 2026-07-14: MA16 bundles three concerns — (a) the neutral capability-operation schema and machine catalog that MA17 gates on, (b) evidence/provenance process machinery (overlay compiler, quarantine/promotion mechanics), (c) generated agent-facing references and schema versioning/migration policy. Only (a) gates the program. Split (b)+(c) into a non-gating sidecar item plus research/evidence reference doc, keeping in MA16 a one-line rule that support-state promotion requires a reviewed fact-specific evidence record. Also record: modes plan|apply|prune|status stay mandatory in the contract (they implement extractability/disable of components e.g. dropping Hindsight), with a capability allowed to declare a mode not-applicable via canonical unsupported result.

## Findings



## Conclusion

Trim MA16 to core contract/loader/machine catalog; create sidecar item for evidence process, generated references, and versioning policy; MA16 keeps evidence-ref + review-state fields only.
