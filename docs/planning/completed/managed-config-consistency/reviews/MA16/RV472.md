---
id: RV472
review-of: MA16
plan: plan-managed-config-consistency
state: closed
reviewed-by: codex
reviewed-at: ''
---

# reconcile_provider/reconcile_all_providers still live across 5 modules — MA22 not actionable until MO02

## Notes

MA22 is the designated item to remove these. MA22 depends on all family migrations completing first (MA12, MA21, MA23-MA30, MO02). The key unmet dependency is MO02 — model projection must be migrated before reconcile can be decomposed into explicit resource/family calls.

## Findings

['providers_api.py exports reconcile_provider (line 727) and reconcile_all_providers (line 737) in __all__', 'config/components/providers.yaml lists both in the public API declaration with descriptions', 'providers_mcp.py exposes MCP tools for both (lines 125-133)', 'commands/refresh.py calls reconcile_all_providers (line 21)', 'commands/provider_prompt.py calls both reconcile_provider and reconcile_all_providers (lines 42-44)', 'commands/launch.py calls reconcile_all_providers (line 83)', 'services/reconcile.py implements both functions with full orchestration logic']

## Conclusion

VALID. Reconcile functions remain public and called across config/MCP/commands/services. MA22 already owns atomic removal after family migrations. Incorporated.
