---
id: RV407
review-of: MA16
plan: plan-managed-config-consistency
state: closed
reviewed-by: opencode
reviewed-at: ''
---

# Verification: provider capability system moved into providers (RV405/RV406)

## Notes

Independent verification performed 2026-07-15 against MA16 validation criteria 1-9.

PASS (verified):
- criterion 1: src/audiagentic/foundation/capability_catalog/ absent (Test-Path False).
- criterion 2: no foundation/ports package exists (Test-Path False); only docs/test references remain.
- criterion 3: components/providers/providers_api.py __all__ re-exports ProviderCapabilityRecipe, ProviderRecipeRegistry, ProviderRecipeResult (single sanctioned edge, arch §18).
- criterion 6/8/9: tests/unit/foundation/toolchains/test_architecture_boundaries.py — all 29 tests green (FoundationCapabilityCatalogDeleted, RequesterProvidersImportAllowlist, ProvidersNoReverseImports). Full `python -m pytest tests/unit` runs with only 1 unrelated failure (CC28 surface-consistency test staleness).

OPEN (allowlisted, owned by other items):
- criterion 4/5 (⚠️ in item notes): known pre-existing reverse/internal import violations are allowlisted in TestProvidersNoReverseImports and owned by MA02 (claude/gemini hooks -> agent_jobs.prompt_*), MA08 (codex/opencode/qwen language_servers + services/lsp_projection -> coding_lsp.language_servers), and MA17 (skill_surfaces -> agent_jobs.prompt_syntax). Gate test passes; these are tracked, not blocking.

Conclusion: MA16 implementation is complete and verified. Closure should occur via MA09 after MA02/MA08/MO02 resolve the allowlisted violations.

## Findings



## Conclusion

MA16 verified complete: capability_catalog deleted, no foundation/ports, single sanctioned providers_api edge, arch guards green. Remaining items 4/5 are allowlisted known violations owned by MA02/MA08/MO02 — non-blocking. Recommend closing MA16 via MA09 gate once dependents land.
