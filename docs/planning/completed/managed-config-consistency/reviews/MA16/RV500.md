---
id: RV500
review-of: MA16
plan: plan-managed-config-consistency
state: closed
reviewed-by: codex
reviewed-at: ''
---

# providers_api __all__ has drifted to 25 exports and omits manage_language_servers_all, which coding_lsp already imports

## Notes

PARTIALLY IMPLEMENTED 2026-07-16 by claude — keeping OPEN for the MA16 export re-classification, which is the architecture owner's call.

DONE:
- manage_language_servers_all added to providers_api.__all__ (it was imported by coding-lsp while unexported — the live boundary hole).
- Added the architecture test this review asked for: TestRequesterProvidersImportAllowlist::test_names_imported_from_providers_api_are_exported asserts every name a requester imports from providers_api appears in its __all__. It immediately found a SECOND hole: LanguageServerProjectionRequest was also imported-but-unexported. The guard earns its keep.
- Exported the typed contracts callers must construct to use the API at all: LanguageServerProjectionRequest, LspMcpProjectionEntry, LspMcpProjectionRequest, SelfProvidedLspRequest. Previously LanguageServerEntry was the ONLY exported type, so requesters were reaching into providers.contracts.* to get Request types — a boundary violation caused directly by the export gap.
- Added manage_self_provided_lsp_all so coding-lsp no longer needs descriptors.registry or services.feature_resolution. coding-lsp now imports zero providers internals; the MA08 known-violation allowlist in the boundary test is emptied, and test_coding_lsp_providers_import_allowlist passes (it was failing before).
- providers_api.__all__ is now 32 names; every one resolves.

STILL OPEN (owner decision):
1. Re-derive the MA16 export classification against the current 32 and record a target disposition per name. MA09 gate item 5 still cannot be verified against the stale '22 exports' figure. The count moved because this review's fixes closed real holes — the lock needs restating, not reverting.
2. Classify the transitional names slated for deletion so the count stays auditable: sync_provider_models, list_provider_models_config, reload_provider_models (MO02), reconcile_provider, reconcile_all_providers (MA22).
3. ManagedMcpEntry is imported from providers_api by memory/hindsight/mcp_recipe.py but is not exported and not referenced in providers_api — a live ImportError breaking hindsight tests right now. Left untouched: another agent is mid-refactor in that area. The new export guard only scans coding-lsp; widening it to memory/hindsight would catch this class there too, once that refactor settles.

## Findings

['providers_api.__all__ now lists 25 names. MA16 and the MA09 gate item 5 both refer to an exact 22-export classification, so the locked list is stale relative to the code.', 'manage_language_servers_all is defined at providers_api.py:111 but is NOT in __all__. coding_lsp/language_servers_sync.py imports it at lines 223 and 341 and calls it at 227 and 347. A permitted caller depends on an unpublished name.', 'Its sibling manage_lsp_mcp_projection_all IS exported, so the single/all pairing is inconsistent across families.', 'Three MO02 exports the slim path marks for removal are still published: sync_provider_models (to be deleted after callers migrate), list_provider_models_config (to be folded into model-projection status mode), reload_provider_models (public route to be removed). MO02 status mode already calls list_provider_models_config internally via model_projection_handler._do_status, so the fold is half-done — both routes are live.', 'reconcile_provider and reconcile_all_providers remain exported pending MA22.']

## Conclusion

VALID. Exported typed caller contracts are legitimate boundary API; historical export count is brittle. MA16/MA09 changed to named classification and transitional dispositions. Existing architecture import/export guard retained.
