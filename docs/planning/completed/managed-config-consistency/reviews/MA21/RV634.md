---
id: RV634
review-of: MA21
plan: plan-managed-config-consistency
state: closed
reviewed-by: codex
reviewed-at: ''
---

# Providers MCP descriptor still exposes deleted lifecycle and surface tools

## Notes

Post-completion implementation audit of MA21, consolidated with the adjacent MA18 CLI lifecycle cutover because both leave the same owner descriptor stale.

## Findings

HIGH — `src/audiagentic/config/components/providers.yaml` still declares six deleted MCP tools: `install_provider`, `uninstall_provider`, `repair_provider`, `apply_provider_surfaces`, and `prune_provider_surfaces` (five names; the lifecycle trio plus surface pair). The implemented MCP now exposes `manage_cli_lifecycle`, `operate_provider_surface`, and `operate_provider_surfaces`. This means the component's direct-tool contract/help is out of sync with the actual server and contradicts MA21 validation that all callers were migrated with no compatibility route. Existing integration tests still import the internal surface manager's split mechanics, which is acceptable only as internal mechanism coverage; they must not justify stale public tool declarations.

## Conclusion

Resolved in the owner descriptor without restoring wrappers. `ag-providers-mgmt` now declares `manage_cli_lifecycle`, `operate_provider_surface`, and `operate_provider_surfaces`; the five deleted lifecycle/surface tool names and descriptions are gone. A conformance test compares declared direct tools and descriptions exactly with the built MCP server. Focused descriptor, surface, and lifecycle validation passes (40 tests).
