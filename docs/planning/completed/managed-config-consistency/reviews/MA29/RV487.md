---
id: RV487
review-of: MA29
plan: managed-config-consistency
state: closed
reviewed-by: codex
reviewed-at: ''
---

# MA29 completes the second MA08 LSP-MCP slice after MA28

## Notes

MA29 is a bounded MA08 slice. It does not block MO02 or MA26/MA27 but blocks MA08 completion.

## Findings

['MA29 depends on MA28 (completed) for shared language_servers_config infrastructure', 'Generic mcp_config + receive_lsp_mcp flag — no per-provider handler needed', 'Requires typed LspMcpProjectionRequest/Result with concrete schemas']

## Conclusion

SUPERSEDED. Standalone LSP-MCP family was implemented, but architecture audit proves duplication with managed-mcp. MA29 now owns consolidation instead of preserving a distinct family.
