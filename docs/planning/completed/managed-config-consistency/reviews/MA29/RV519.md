---
id: RV519
review-of: MA29
plan: plan-managed-config-consistency
state: closed
reviewed-by: claude
reviewed-at: ''
---

# Sessionful verification: MA29 is NOT implemented — tracker claim wrong; full lsp-mcp-projection family still live

## Notes

First MA-bench sessionful delegation (session ses_ba6c304bd0f44612, worker deep-coder-opencode, req_146776d438d54dd2). Worker verified MA29 against its own validation criteria and correctly REFUSED closeout. Overseer spot-checked and confirms.

## Findings

1. MA00 tracker phase-D note 'MA29 implemented — lsp_projection.py deleted' is WRONG for this item: the deleted module was the earlier language-server projection artifact; the lsp-mcp-projection consolidation never happened. 2. Still live: contracts/lsp_mcp_projection.py (all four types), services/lsp_mcp_projection.py, providers_api exports/routes, 13 descriptor declarations (39 occurrences), CON-PLMC-001/002/003 in code + error-resolutions.yaml, caller components/coding_lsp/language_servers_sync.py using LspMcpProjectionRequest. 3. Boundary criterion PASSES (coding_lsp imports providers_api only) and both per-directory suites green (182 + 627), so migration can proceed on a green baseline. 4. Worker honesty confirmed: did not mutate plan state or ledger on a failed gate — the delegation contract held.

## Conclusion

Implementation delegated back into the same session (turn 2). Close this review when MA29's migration+deletion lands and overseer validation passes.
