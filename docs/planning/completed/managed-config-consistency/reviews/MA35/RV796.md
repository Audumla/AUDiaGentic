---
id: RV796
review-of: MA35
plan: plan-managed-config-consistency
state: closed
reviewed-by: ''
reviewed-at: ''
---

# Scope shrunk: qwen/copilot dead adapters deleted + build_launch_spec built in HA04

## Notes

HA04 (2026-07-24) delivered two of MA35's three pieces: (1) deleted the dead qwen/copilot adapter.py duplicates and rewrote their tests to drive the declarative execution: recipe via make_runner_from_execution; (2) built the shared build_launch_spec vocabulary MA35 depended on (base_runner.py). Remaining MA35 scope is now ONLY the Pi one-shot/execute stdin-delivery: pi/adapter.py exists solely because Pi's --print mode loses embedded newlines when the prompt is a CLI arg (SH21) -- the recipe would need a prompt-delivery: stdin option in build_launch_spec's vocabulary before pi/adapter.py can retire. Updating MA35 to reflect the shrunk scope.

## Findings



## Conclusion

scope-shrunk
