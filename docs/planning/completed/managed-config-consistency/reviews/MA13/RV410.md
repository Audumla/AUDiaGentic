---
id: RV410
review-of: MA13
plan: plan-managed-config-consistency
state: closed
reviewed-by: codex
reviewed-at: '2026-07-15'
---

# MA11 phantom prerequisite blocks MA13 and MA12

## Notes

CORRECTED: MA11 is completed, not phantom. MA13/MA12 are unblocked. Downgraded from blocking-gap to documentation-cleanup + probe-duplication analysis (actionable via MA13). Update MA13/MA12 stale MA11 wording.

## Findings

## CORRECTION (supersedes initial phantom-prerequisite finding)

Initial RV410 hypothesis: MA11 is a phantom prerequisite (does not exist in plan). VERIFIED FALSE.

`id_prefix=MA11` returned zero results from `ag_planning_plan_list_items` because that tool only queries ACTIVE items. MA11 exists in `docs/planning/completed/managed-config-consistency/MA11.md` with `state: completed`.

MA11 delivered exactly what MA13/MA12 need:
- `foundation/steps/protocol.py` — Step.run/plan, CompensableStep.compensate
- `foundation/steps/results.py` — StepResult, SequenceResult, WorkflowInvocationResult adapter
- `foundation/steps/sequence.py` — SequenceStep (canonical constructor)
- `foundation/steps/shell.py` — one ShellStep (argv OR shell flag)
- `foundation/steps/callable.py` — CallableStep
- `foundation/steps/control.py` — ConfirmStep, SelectStep, ConditionalStep, planned_commands
- `foundation/steps/structured.py` — ConfigSetStep, WriteFileStep, ManagedBlockStep
- `foundation/steps/factory.py` — typed StepFactory protocol + registry
- Deleted `foundation/toolchains/provision_steps/` and workflow concrete duplicates
- Ledger: 16 change events (chg_20260713_062406 through chg_20260714_085649)

MA11's step 34 even added the CC36 architecture guard (tests/unit/foundation/test_all_imports.py) verifying every __all__ export resolves — the same guard class that later caught the coding-lsp.yaml Direction token breakage during MA16 RV406 work.

## Actual finding (downgraded from blocking gap to documentation cleanup)

MA13 (step 0): "Implement after MA11." — MA11 is done, so MA13 is unblocked. The phrase is now stale; should read "Extend existing foundation/toolchains/probes.py and foundation/steps/ (both delivered by completed MA11)."

MA12 (notes): "MA12 depends on MA11 and optionally the reduced MA10 helper, not MA16/MA17." — MA11 done, MA10 closed-as-resolved (RV409). Both dependencies satisfied. The note is accurate in dependency-direction but phrased as if MA11 is pending. Should reference completed MA11 + RV409-closed MA10.

MA00 Block C: "MA12 after MA11 and MA10 decision" — both satisfied (MA11 completed, MA10 closed). Block C is unlocked.

## Residual value of the original investigation

The deeper finding from RV410's probe-duplication analysis REMAINS VALID and is now the actionable part:

`foundation/components/dependencies.py` L46-113 defines private `_resolve_probe_*` functions that DUPLICATE `foundation/toolchains/probes.py` functionality:
- `_resolve_probe_command` (L61) has NO shell-metachar guard; `probes.py CommandProbe` (L66) HAS `safe_command_parts` → VAL-CMD-001 (security gap in dependencies.py)
- `_resolve_probe_binary/path/all_binaries` overlap `tool_available` / `FileExistsCheck`
- Result type mismatch: dependencies returns `Callable[[], bool]`; probes returns `ProbeResult`

This duplication is what MA13 should unify — and MA13 is now UNBLOCKED (MA11 done). So RV410's corrected recommendation: MA13 can proceed; its MA11 prerequisite is satisfied; update the item's stale wording and begin the probe unification using the completed foundation.steps + probes infrastructure.

## Also confirmed completed (full inventory)
MA01, MA04, MA05, MA10, MA11, MA15 all state: completed. Active MA items: MA00, MA02, MA06, MA08, MA09, MA12, MA13, MA14, MA16, MA17, MA18, MA19 (12 total). MA16 implementation done (RV406); MA10 closed (RV409). All 18 MA items now accounted for.

## Conclusion

CORRECTION: MA11 is NOT a phantom — it is COMPLETED (state: completed, in docs/planning/completed/managed-config-consistency/MA11.md). It delivered the unified foundation.steps substrate (protocol.py, results.py, sequence.py, shell.py, callable.py, control.py, structured.py, factory.py) that MA13/MA12 reference. Therefore MA13 and MA12 are UNBLOCKED, not blocked. The only issue is stale wording in MA13/MA12 notes that still phrase MA11 as a pending prerequisite ("Implement after MA11", "depends on MA11") — a documentation cleanup, not a planning gap. Recommend updating MA13/MA12 notes to reference the completed foundation.steps API directly, and removing the "after MA11" framing.
