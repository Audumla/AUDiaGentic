---
id: RV476
review-of: MA13
plan: plan-managed-config-consistency
state: closed
reviewed-by: ''
reviewed-at: ''
---

# probe_from_spec missing and duplicate resolver family still present in dependencies.py

## Notes

MA13 is a bounded refactor item (P2 priority) that does not block other MA items. However it blocks MA09 closure because MA09 step 1 requires all MA items complete.

## Findings

['probe_from_spec function does not exist in foundation/toolchains/probes.py', '_resolve_probe_binary still exists at dependencies.py:46', '_resolve_probe_all_binaries still exists at dependencies.py:51', '_PROBE_RESOLVERS dict still exists at dependencies.py:96-100', 'All five resolver functions (_resolve_probe_binary, _resolve_probe_all_binaries, _resolve_probe_path, _resolve_probe_command, _resolve_probe_custom) remain in dependencies.py']

## Conclusion

MA13 step 1 requires adding probe_from_spec(spec: str) -> Probe in foundation/toolchains/probes.py. The function does not exist. Steps 2-4 require replacing _resolve_probe_binary, _resolve_probe_all_binaries, _PROBE_RESOLVERS and the resolver family in foundation/components/dependencies.py with a single _resolve_probe(spec) adapter. All five old resolver functions and the _PROBE_RESOLVERS dict remain intact (lines 46-113). MA09 step 1 requires all MA items to be complete, so MA13 blocks MA09 closure.
