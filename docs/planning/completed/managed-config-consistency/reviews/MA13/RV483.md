---
id: RV483
review-of: MA13
plan: plan-managed-config-consistency
state: closed
reviewed-by: ''
reviewed-at: ''
---

# MA13 completion adds probe_from_spec and deletes duplicate resolver family

## Notes

MA13 is a bounded refactor that resolves RV476 directly. It does not block other MA items but is required for MA09 closure.

## Findings

['MA13 step 1 adds probe_from_spec to probes.py — single parser for all probe: spec prefixes', 'MA13 step 4 replaces duplicate resolver family in dependencies.py with thin structured-probe-to-bool adapter', 'Step 8 confirms _resolve_probe_binary, _resolve_probe_all_binaries, _PROBE_RESOLVERS absent from codebase']

## Conclusion

MA13 completion adds probe_from_spec(spec: str) -> Probe to foundation/toolchains/probes.py, replaces _resolve_probe_binary, _resolve_probe_all_binaries, _resolve_probe_path, _resolve_probe_command, _resolve_probe_custom, and _PROBE_RESOLVERS in dependencies.py with a single _resolve_probe(spec) adapter. Step 8 deletes duplication confirming all five resolver functions and the resolver table are absent. This resolves RV476.
