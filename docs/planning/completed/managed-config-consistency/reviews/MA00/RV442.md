---
id: RV442
review-of: MA00
plan: managed-config-consistency
state: closed
reviewed-by: claude
reviewed-at: ''
---

# Consolidated ruling on RV415/418/426/427/436-441: notes overclaim a registry gateway the code (correctly) does not have

## Notes

Verified against src/audiagentic/components/providers/providers_api.py (2026-07-15): the public surface is concrete per-operation functions — model_source_add/update/remove/set_enabled, sync_provider_models, reload_provider_models, list_provider_models_config, describe_provider, list_providers. There is NO capability gateway, NO ProviderRecipeRegistry dispatch surface, and NO registered-handler resolution exposed. ROOT CAUSE of the new-review cluster (RV436-441): plan notes and any §18.3 'registry is sole authority' standard got ahead of the code and describe a pure-registry gateway that was never built. CRITICAL: per my own RV406 ruling (issue 4), BOTH surfaces are sanctioned — thin wrapper functions are the RECOMMENDED standard surface, registry export is for full-lifecycle callers. So the concrete functions are NOT defects; they are the blessed surface. The defect is DOCUMENTATION: notes claiming 'registry IS the capability service, no concrete functions' contradict RV406 and the code.

## Findings

DISPOSITIONS:

RV436 (MA16) VALID-DOC: notes claim 'validation satisfied' while admitting violations and misdescribe the delivered providers_api surface. FIX: reword MA16 notes to state the delivered surface is concrete wrapper functions per RV406 (not a registry gateway); drop any 'validation fully satisfied' claim — MA16 remains pending pending the intent-format + §8↔ProviderRecipeKind work.

RV437 (MA02) VALID-DOC: 'capability gateway resolves explicit registered handler' describes no existing mechanism. FIX: MA02 notes must say requesters call the providers public API wrapper functions; the 'gateway' is those functions, not a handler registry. Requester-blindness + import-boundary guarantees are unchanged.

RV438 (MA08) VALID-GAP: no coding-LSP reconciliation function exists in providers_api yet, so MA08 has nothing to implement against. FIX: MA08 gains an explicit prerequisite — a providers public wrapper (e.g. sync_provider_language_servers) must be added first; until then MA08 is BLOCKED, not ready. This is real scope, route to the implementing agent.

RV439 (MA17) VALID-DOC+STANDARD: Step 0 gate assumes a capability gateway MA16 does not establish; §18.3 registry-sole-authority mandate diverges from concrete-call code. FIX: (a) MA17 Step 0 gate rewrites to 'providers public API surface exists' not 'capability gateway'; (b) §18.3 must be CORRECTED — RV406 supersedes any registry-sole-authority rule; the sanctioned authority is 'explicit registration OR provider-owned public wrapper functions', concrete functions are compliant. Route the §18.3 edit to the implementing agent.

RV440 (MO02) VALID-DOC: notes claim 'registry IS capability service, no new types' — contradicted by the model_source_* functions that ARE the surface. FIX: MO02 notes reword to 'model_source_* + sync/reload wrapper functions are the sanctioned public surface per RV406'; no registry gateway is required or coming.

RV441 (MA09) VALID-BLOCKER: MA09 cannot re-run the MA17 boundary graph while the surface model is unsettled, and the §18.3 sole-authority rule may be self-contradictory. FIX: MA09 stays blocked until RV439's §18.3 correction lands; then the graph audit targets the public-wrapper import boundary (RV405 layer rule), not a registry. Add this dependency to MA09.

RV415 (MA09) VALID: three MA16 import violations + untracked HA01/HA02 — the import violations dissolve with foundation/capability_catalog now DELETED (RV406, chg_20260714_133432); re-verify the grep is clean; HA01/HA02 tracking already exists in MA00 Block S / harness-runtime-ownership plan — confirm MA09 excludes them explicitly.

RV418 (MO13) VALID: 'Implement' title vs documentation-only blocked completion — retitle MO13 to 'Declare Codex/OpenHands model projections (blocked-pending-evidence)' and confirm V2-V3 validation rows are gated on unblock, not required for blocked completion (already established in MO13 notes 2026-07-14).

RV426 (MA19) VALID: evidence-promotion 'fact-specific anchor required' doesn't define sufficiency — MA19 (sidecar, non-gating) must define the sufficiency bar (what makes an anchor sufficient to promote support state). Low priority; MA19 is not on the critical path.

RV427 (MO01) VALID: Step 7b delegates to MO02/MO10 with no enforcement test — add a validation test asserting the delegation boundary; MO01 is completed, so record as a follow-up test item, do not reopen.

RV428 (MA11) CONFIRMED CLEAN — independently verified foundation/steps complete, zero legacy imports; CLOSE as no-action (this matches my RV388 re-baseline).

## Conclusion

SUPERSEDED by the RV412 deep pass (2026-07-15). My RV442 consolidation framed the concrete functions as 'wrappers over the registry per RV406' — RV412 is sharper and correct: they are purpose-built functions per operation family, and the registry is NOT the universal surface (it fits provisioning only). All member findings (RV415/418/426/427/436-441) now individually dispositioned and closed with RV412-aligned conclusions. This coordination review is retired.
