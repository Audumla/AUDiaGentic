---
id: CO04
order: 0
plan: plan-config-leanness
state: completed
validate-first: false
priority: P1
work: M
---

# Auto-wire Hindsight provisioning + fix recipe bugs + per-provider tests

## Description

Proof-drove Hindsight provisioning across providers, fixed two latent bugs, and wired automatic provisioning on memory config change with strict encapsulation (providers never know about Hindsight; memory implementation owns the matrix; a runtime composition bridge is the only both-knower). Added per-provider file-validation tests runnable in the Docker suite.

## Steps



## Files

src/audiagentic/components/memory/hindsight/{recipes,provision}.py; src/audiagentic/runtime/lifecycle/provider_recipes.py; src/audiagentic/components/memory/memory_mcp.py; tests/unit/memory/*; tests/integration/memory/test_hindsight_provider_files.py

## Validation



## Effort & Risk



## Standards



## Notes

Bug A: _stamp assumed ProviderRecipeResult (inner is foundation RecipeResult). Bug B: MCP config path ignored project_root (leaked to CWD). Both fixed + regression-tested. Follow-up: component-enable (vs config-change) does not auto-trigger — only memory_set_config/select does; a Dockerfile exercising real CLI installers (hooks/plugin/wrapper writing ~/.hindsight) is not yet added (network-dependent).
