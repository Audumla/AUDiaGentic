---
id: CP11
order: 3
plan: plan-component-profiles
state: completed
validate-first: true
priority: P2
work: M
---

# Descriptor bloat: centralize ComponentDescriptor construction via from_yaml()

## Description

Migrated from plan-simplify-layering/SL01 (2026-07-04). RE-SCOPED per review RV94 (2026-07-05, verified against current code): the originally-claimed 'two construction sites (provisional ~line 123, final ~line 144)' is stale - grep + direct read of loader.py confirms exactly ONE production ComponentDescriptor( construction, at loader.py:155 inside register_from_yaml. The codebase was evidently refactored since SL01 was originally drafted. The real, smaller duplication that still exists: the `files` tuple is built TWICE inside register_from_yaml - once at loader.py:67-75 from the raw YAML, and again at loader.py:94-102 after a synthesized marker ComponentFile is inserted when no file entry matches the detection-marker. register_from_yaml is ~125 lines of inline parsing (files/mcp_servers/external_mcp_servers/harness_instructions/scalar fields) that could still reasonably move to a ComponentDescriptor.from_yaml() classmethod on base.py purely as loader-slimming, and the files-tuple double-build is worth folding into a single helper - but this is no longer the 'two divergent construction paths must be unified' problem originally described. Field consumers (read .field) still span ~17 files/38 attribute reads and must NOT need editing regardless.

## Steps

["Re-verify at implementation start: grep 'ComponentDescriptor(' across src/ - confirm still exactly one production site (loader.py:155) before proceeding; if a second site has appeared, treat as a scope change requiring re-review, not a silent assumption", 'Extract the files-tuple construction (loader.py:67-75 and 94-102) into a single module-level helper (e.g. _build_files_tuple(raw_files: list[dict]) -> tuple[ComponentFile, ...]) called once before the marker-synthesis check and once after, eliminating the actual duplication', "OPTIONAL, lower priority now that the multi-site premise is gone: move register_from_yaml's remaining inline parsing (mcp_servers, external_mcp_servers, harness_instructions, scalar fields) into a ComponentDescriptor.from_yaml(data, *, yaml_path) classmethod in base.py, purely to shrink loader.py to orchestration - keep ComponentDescriptor flat and frozen so all 38 .field reads in the 17 consumer files keep working unchanged; do this only if it's still wanted for readability, not because two divergent construction paths need unifying", 'Add field-group doc comments in base.py clustering the ~20 fields by concern (identity / files / mcp / lifecycle-hooks / feature) so per-component relevance is documented even though storage stays flat']

## Files

['src/audiagentic/foundation/components/base.py', 'src/audiagentic/foundation/components/loader.py']

## Validation

['ComponentDescriptor( construction count unchanged in production code (still loader.py only, verified at both start and end)', 'All 38 .field attribute reads across the 17 consumer files still resolve (full suite green)', 'The files-tuple is built via one shared helper, not duplicated inline', 'Round-trip: load every YAML in config/components via register_all_components without error']

## Effort & Risk

Approach A-lite: effort LOW-MED, risk LOW (no data-model change, no consumer edits). Do not silently pick a full mixin split (approach B) - get an explicit decision if that's ever wanted; the flat-attribute-preserving approach is the default here.

## Standards



## Notes

Migrated in from plan-simplify-layering/SL01 rather than left as a separate plan, specifically because of the loader.py construction-site collision with CP02/CP04 - not because it's thematically related to component profiles in general. SL04 (the other item from that plan) was deliberately NOT migrated since it touches a different file (foundation/features/state.py) with no direct overlap with anything in this plan. Per review RV94 (2026-07-05): premise corrected, complexity downgraded from complex to mid, priority from P1 to P2. Ordering before CP02 is now optional rather than required - CP02 does NOT strictly need to wait on this item since the 'two divergent construction paths' problem it was meant to resolve doesn't exist; keep the extraction only if still wanted for loader-slimming/readability.
