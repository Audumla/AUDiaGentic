---
id: CP07
order: 11
plan: plan-component-profiles
state: completed
validate-first: true
priority: P2
complexity: simple
---

# Add end-to-end profile-switch regression test

## Description

Validate the full feature end to end: launching under a named profile loads that profile's components layered over base components, while launching with no profile is unaffected.

## Steps

['Follow the existing tmp_path + register_all_components([tmp_path]) pattern from tests/unit/foundation/test_feature_implementations.py:177,186-224 and tests/unit/coding_lsp/test_component_registration.py:51,78 (write synthetic component YAML into tmp_path, register, assert via get_descriptor) rather than inventing a new fixture pattern', 'Test: launch/register with --component-profile pointing at a fixture profile dir containing a distinct test-only component descriptor confirms both the fixture component and a known base component are registered together', 'Test: launch/register with no --component-profile confirms base-only behavior unchanged from pre-feature baseline', "Test: attempting to register a second, different profile within the same process fails per CP05/CP08's guard", 'Place these under tests/unit/foundation/ or tests/integration/ per tests/TESTING.md conventions (unit = isolated/mocked boundaries, integration = real I/O with tmp_path) - this feature does not mutate the host filesystem outside tmp_path, so it does NOT need the Docker-gated mutates_host path (AUDIAGENTIC_DOCKER_TESTS=1 in tests/conftest.py:43-48) unless a later item adds host-level profile installation', 'ADD (2026-07-05, closing a gap from the architectural review): the above are all in-process register_all_components() tests - none of them actually run the real audiagentic entrypoint. Add at least one subprocess-level test that invokes the actual CLI (audiagentic --component-profile <fixture-dir> <args>, e.g. via subprocess.run or the existing pattern used for other CLI-level tests in tests/integration/ or tests/e2e/) and asserts on real stdout/exit code, not just in-process registry state - this is the only place in the plan that exercises the feature the way an actual user would invoke it']

## Files

['tests/']

## Validation

['All scenarios pass as ordinary unit/integration tests (no Docker gate needed per the analysis above)', 'Test run via standard pytest, not requiring AUDIAGENTIC_DOCKER_TESTS=1 since no test is marked mutates_host', 'The subprocess-level CLI test passes with the real audiagentic entrypoint, not just in-process function calls']

## Effort & Risk



## Standards



## Notes

This is the final acceptance item for the component-profiles plan - depends on CP01 through CP05/CP08. Deep-dive (2026-07-04) found the project's actual Docker-gate mechanism (tests/conftest.py:43-48, AUDIAGENTIC_DOCKER_TESTS env var, mutates_host marker) and confirmed this feature's tests don't need it since nothing mutates the real host outside tmp_path.
