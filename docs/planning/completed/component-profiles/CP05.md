---
id: CP05
order: 9
plan: plan-component-profiles
state: completed
validate-first: true
priority: P2
work: M
---

# Guard against cross-profile pollution of the event bus and feature registry

## Description

The event bus (foundation/event/event_bus.py get_bus(), a true process-global singleton with unnamespaced string event types and module-level import-time subscribe() side effects) and the feature/implementation registry (foundation/features/registry.py, module-level global dicts keyed only by (parent, kind, id)) are safe across separate process invocations - each launch gets a fresh singleton - but are NOT safe if two different profiles' components were ever loaded into the same process. Add a defensive guard so this unsupported state fails loudly instead of silently producing event-name collisions or registry bleed. NOTE: a deep-dive (2026-07-04) found roughly a dozen MORE process-global singletons with the same risk profile beyond these two - split out to CP08, since fixing all of them under one item would be unreasonably large. This item (CP05) stays scoped to the two originally-identified singletons; CP08 covers the rest.

## Steps

["Add a process-level record of which profile's components have been registered (e.g. a module-level guard alongside the loader's discovery state)", 'If register_all_components() (or the profile-aware wrapper from CP03/CP04) is invoked a second time within the same process with a different profile than the first invocation, raise a clear error rather than silently merging incompatible state', 'Document this constraint (single profile per process) directly in the docstring of get_bus() and register_all_components()', 'Add a regression test simulating a second, different-profile registration attempt within one process and asserting it fails clearly - follow the existing snapshot/clear/restore precedent at tests/unit/foundation/conftest.py:9-26 (isolated_features_registry fixture)']

## Files

['src/audiagentic/foundation/event/event_bus.py', 'src/audiagentic/foundation/features/registry.py', 'src/audiagentic/foundation/components/loader.py']

## Validation

["Test confirms same-profile re-registration (idempotent case, already relied upon by ~23 existing call sites per CP02's audit) still succeeds silently", 'Test confirms different-profile re-registration within one process raises a clear, actionable error']

## Effort & Risk



## Standards



## Notes

This is a safety net, not a redesign - the recommended usage pattern remains one profile per process/launch. Depends on CP03 for profile identity to check against. See CP08 for the broader singleton audit this investigation also surfaced. CORRECTED (2026-07-05): dropped the tests/unit/foundation/test_capabilities.py fixture-pattern citation -- that test file covers foundation/capabilities.py, which foundation-event-migration/EV06 deletes entirely, so the precedent would dangle. The conftest.py isolated_features_registry precedent remains valid and sufficient on its own.
