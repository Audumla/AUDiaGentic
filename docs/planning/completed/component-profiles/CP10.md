---
id: CP10
order: 5
plan: plan-component-profiles
state: completed
validate-first: true
priority: P2
complexity: complex
---

# Extract generic keyed-registry utility with lazy-load + alias support; migrate ad hoc registries with no shims

## Description

Six ad hoc registries found during investigation are structurally identical or near-identical - a module-level dict keyed by string, a register_X() function, a lazy _loaded/_LOADED guard, and a getter - with inconsistent collision behavior: components/coding_lsp/language_registry.py:26-27,167-173, components/providers/services/host_adapter.py:71-72,75-91, components/providers/tags/registry.py:6-7,22-28, foundation/toolchains/provision_steps/base.py:41-46 (_STEP_TYPES), components/providers/surfaces/registry.py:7-9,20-38. SCOPE CHANGE (2026-07-05): foundation/lifecycle/provider_recipes.py is REMOVED from this item's scope. Deeper review (see plan foundation-event-migration/EV03) found provider_recipes.py is not just a sixth instance of the ad hoc registry pattern - its entire vocabulary (ProviderRecipeReconciler, _all_provider_ids, capability keys providers.descriptors.all_ids / providers.provider-config.resolve_enabled) is providers-component domain language sitting in foundation, passing the import-direction check only because it goes through get_capability() lookups instead of a static import - a workaround for the layering rule, not compliance with it. It also fits the fire-and-forget lifecycle-event pattern (no caller consumes refresh_provider_recipes()'s per-reconciler results directly), unlike the five true keyed-lookup registries below. EV03 deletes it from foundation entirely and moves the reconciliation logic into the providers component as an event-bus reaction, rather than migrating it onto this item's Registry utility. CORRECTED per review RV95 (2026-07-05, verified against code): foundation/components/registry.py's register() (registry.py:17-23) does NOT destructively strip a DIFFERENT component's aliases on collision - it only clears aliases owned by the SAME incoming component_id, which is correct idempotent-re-registration cleanup. The real latent defects there are (a) cross-component alias collisions are silently last-wins (_aliases[alias] = id with no conflict check against a different existing owner) and (b) an alias can silently shadow-collide with a real component_id already in _registry, with no check either way. Per ARCHITECTURE_STANDARDS.md:96-97 ('No backward compatibility shims... do not create shim functions... migrate code as we refactor, always'), this is a direct call-site migration, not a wrapper-preserving one: every caller of the old register_*/get_* functions is updated to call the new generic registry instance directly, and the old module-level functions are deleted in the same change. To make the utility capable enough to genuinely absorb more than the bare minimum, it supports two features beyond a plain dict: a built-in lazy-loader (invoked once on first access, replacing hand-rolled _ensure_loaded() implementations across the five remaining registries) and an optional alias index (register(key, value, aliases=[...]) that raises/warns on cross-component alias conflict and alias-vs-primary-key shadowing, while same-owner re-registration stays a silent no-op) - correct alias semantics proven here first is what lets foundation/components/registry.py (see CP08) adopt the same utility internally later without becoming a shim. ADDITIONAL CORRECTION per RV95: components/providers/surfaces/registry.py is NOT a 1:1 fit - it holds TWO separate dicts (_renderer_registry, _contribution_renderer_registry) behind ONE shared lazy-load flag (_providers_imported). Its migration needs two Registry instances that share a single loader callable (or one loader that populates both), not the simple one-dict-one-instance mapping the other four registries get.

## Steps

["Design foundation/registry_utils.py: a Registry class/factory with register(key, value, aliases=None), get(key), all(), reset(), and an optional constructor-supplied loader: Callable[[], None] invoked lazily on first access. Collision policy modeled on foundation/capabilities.py:41-53 (raise a clear, catalogued error on registering a different value under an existing key; idempotent re-registration of the same value is a no-op). Alias policy (per RV95 correction): raise/warn on registering an alias already owned by a DIFFERENT key, and raise/warn if an alias string collides with an existing primary key - same-owner re-registration (re-adding the same key's own aliases) stays a silent no-op, matching registry.py's current (correct) same-owner cleanup behavior", 'Migrate language_registry.py, host_adapter.py, tags/registry.py, and provision_steps/base.py to construct and use a single Registry instance internally, moving their lazy-load bodies into the loader= callable', "Migrate surfaces/registry.py to TWO Registry instances (one per dict: renderer and contribution-renderer), sharing a single loader= callable (or two loaders both gated by the same underlying _ensure_provider_modules_registered()-equivalent) so _providers_imported's shared-gate semantics are preserved", "Update every external caller of the old register_*/get_* functions to call the Registry instance's methods directly instead (7 callers for language_registry, 2 for host_adapter, plus callers of tags/registry and surfaces/registry) - delete the old module-level functions in the same change, no deprecated pass-throughs", "Add a single shared test fixture/helper (e.g. in tests/conftest.py or a foundation test-support module) that resets all migrated registries between tests, replacing the need for five separate ad hoc fixtures - this reset must also invalidate CP02's register_all_components() cache guard (see CP02's RV96 fix) since clearing registries without invalidating that cache would leave it stale", "Verify CP08's scope still matches after this item lands: it should already reflect foundation/components/registry.py being a follow-up candidate for adopting this Registry - if it doesn't, update it then, not before", "Coordinate with plan foundation-event-migration/EV03: land either order, but do not migrate provider_recipes.py onto this item's Registry utility - it is being deleted from foundation by EV03, not genericized in place"]

## Files

['src/audiagentic/foundation/registry_utils.py', 'src/audiagentic/components/coding_lsp/language_registry.py', 'src/audiagentic/components/providers/services/host_adapter.py', 'src/audiagentic/components/providers/tags/registry.py', 'src/audiagentic/foundation/toolchains/provision_steps/base.py', 'src/audiagentic/components/providers/surfaces/registry.py', 'src/audiagentic/components/coding_lsp/lsp_session_resolution.py', 'src/audiagentic/components/coding_lsp/runtime_resolver.py', 'src/audiagentic/components/coding_lsp/language_servers_sync.py', 'src/audiagentic/components/coding_lsp/lsp_config_api.py', 'src/audiagentic/components/coding_lsp/lsp_recipe.py', 'src/audiagentic/components/coding_lsp/coding_lsp_config.py', 'src/audiagentic/components/providers/surfaces/extensions_json.py', 'src/audiagentic/components/providers/services/host_capabilities.py', 'src/audiagentic/components/memory/memory_observer.py']

## Validation

['All migrated registries pass their existing unit tests with call sites updated, not shimmed', 'grep confirms zero remaining references to the deleted old register_*/get_* function names anywhere in the repo', "New test confirms the shared utility raises on conflicting re-registration, allows idempotent same-value re-registration, and correctly adds/removes aliases without corrupting the primary key's entry", 'Single shared reset fixture successfully isolates all six migrated registries between tests']

## Effort & Risk



## Standards



## Notes

Should land before or alongside CP08. foundation/components/registry.py stays out of this item's direct scope (21 callers, plus genuine extra responsibilities - install/enable lifecycle state - beyond storage) but is now set up to adopt the same Registry instance internally in a later pass per CP08's updated notes, since correct alias semantics (cross-conflict + shadow detection, not a nonexistent stripping bug) are proven here first. The two logging singletons remain out of scope entirely - they are single-instance config caches, not keyed registries. The built-in loader= callable is now the reference implementation of ARCHITECTURE_STANDARDS.md section 11 (Lazy Initialization) - prefer it over hand-rolled _loaded/_ensure_loaded() guards, per the new anti-pattern table entry added in the same discussion. Per review RV91 (2026-07-05): the last step previously said to update CP08's scope, but CP08 already reflects this - rewritten to a verification check instead of a redundant edit instruction. Per review RV95 (2026-07-05): corrected the mischaracterized 'destructive alias-stripping' description (it's actually correct same-owner cleanup; the real gaps are cross-component silent last-wins and shadow collision) and added surfaces/registry.py's two-dicts-one-loader structural requirement. SCOPE CHANGE (2026-07-05): provider_recipes.py removed from this item's registries list and files - see plan foundation-event-migration/EV03, which deletes it from foundation entirely rather than migrating it onto this item's Registry utility. Discussion also produced EV01/EV02 (deleting/relocating two other foundation capabilities that were the same naming-violation pattern) and EV04 (a new ARCHITECTURE_STANDARDS.md rule making domain-neutral naming an explicit, separate test from import-direction).
