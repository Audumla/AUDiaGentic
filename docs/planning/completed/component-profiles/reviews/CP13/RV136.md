---
id: RV136
review-of: CP13
plan: component-profiles
state: closed
reviewed-by: Claude (implementation review, 2026-07-06)
reviewed-at: ''
---

# Env propagation correct, but the required third subprocess test was not added — validation bullet 1 unexecuted

## Notes



## Findings

The code change is correct and minimal: launcher._main resolves project_root then sets AUDIAGENTIC_REPO_ROOT only when --project was passed, in the same post-parse block as the profile propagation, before any command handler runs -- matching the decided solution exactly, including not clobbering a user's env when the flag is absent. The loader comment update landed. However, CP13's step 3 / validation bullet 1 required extending tests/integration/runtime/test_component_profile_cli.py with a third subprocess test (--project <tmp project> --component-profile <name> from an unrelated cwd, no AUDIAGENTIC_REPO_ROOT in env, assert the profile component in `component list` output). The file still contains exactly 2 tests (verified 2026-07-06); the new launcher behavior has zero coverage, and the one validation criterion that exercises it end-to-end was not executed. Item completed anyway -- Definition-of-Done violation.

## Conclusion

Add the specified subprocess test (a ~15-line addition mirroring the two existing tests, with env stripped of AUDIAGENTIC_REPO_ROOT and args ['--project', str(tmp), '--component-profile', name, 'component', 'list'], cwd=an unrelated directory). Until it exists and passes, CP13's acceptance is unmet.
