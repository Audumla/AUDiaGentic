---
id: RV133
review-of: CP12
plan: plan-component-profiles
state: closed
reviewed-by: Claude (plan review, 2026-07-06)
reviewed-at: ''
---

# §11 rewrite (transparency mandatory) extends this item's scope: rename load-state-leaking APIs, fold the nested coding_lsp guard

## Notes



## Findings

ARCHITECTURE_STANDARDS.md §11 was rewritten after this item was drafted: transparency is now mandatory (no exposed loader functions, priming methods, or load-state queries; the guard lives inside the accessor; §12 gained 'Call site must invoke a loader or check load state before using a value' as an anti-pattern). Two consequences for this item, verified against code 2026-07-06: (1) tags/registry's public accessor is named all_tags_loaded() — the name itself leaks load state to callers ('loaded' is an implementation detail under the new principle); the migration must rename it (e.g. all_tags()) across its callers (prompt_syntax.py:25-27, prompt_templates.py:45-50, skill_surfaces.py:99-101, surfaces/contributions.py:60-144 four sites, surfaces/manager.py:144-146) — direct call-site migration, no alias per §10. (2) language_registry has a SECOND nested guard the item didn't enumerate: _ensure_feature_catalog_loaded (language_registry.py:157-164), which itself calls register_all_components() — both guards must fold into the single Registry loader= callable, and the register_all_components() call inside it interacts with the new AR item on transparent component-registry accessors (once accessors self-populate, this explicit call becomes redundant — coordinate whichever lands second).

## Conclusion

Update CP12's steps to (a) rename all_tags_loaded during the tags migration with its 7 call sites, (b) explicitly fold both language_registry guards into one loader, (c) cross-reference the new transparency item for the register_all_components call inside the coding_lsp loader.
