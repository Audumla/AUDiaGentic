---
id: RV94
review-of: CP11
plan: component-profiles
state: closed
reviewed-by: claude
reviewed-at: ''
---

# Premise stale: only ONE ComponentDescriptor construction site exists, not two

## Notes



## Findings

Grep of src/ finds exactly one production ComponentDescriptor( construction: loader.py:155, inside register_from_yaml. The claimed 'provisional at ~line 123, final at ~line 144' duplication does not exist in the current loader.py - the code appears to have been refactored since this item was drafted from SL01. The only real duplication remaining is much smaller: the files tuple is built twice inside register_from_yaml (loader.py:67-75 and 94-102) when a marker ComponentFile is synthesized. The item's validate-first step ('expect exactly 2 real sites... if different, stop and re-scope') would catch this at implementation time, but the complexity=complex / P1 rating and part of the 'must land before CP02/CP04' rationale rest on the stale premise.

## Conclusion

Re-scope now rather than at implementation time: the from_yaml() extraction to base.py is still defensible as loader slimming (register_from_yaml is ~125 lines of parsing), and the files-tuple double-build is worth folding in, but this is closer to complexity=mid. Keep the ordering before CP02 only if the extraction is still wanted; otherwise CP02 no longer needs to wait.
