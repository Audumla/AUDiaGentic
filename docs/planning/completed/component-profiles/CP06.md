---
id: CP06
order: 10
plan: component-profiles
state: completed
validate-first: false
priority: P2
work: S
---

# Update CLAUDE.md-generating doctrine and planning docs with profile process

## Description

Document the new profile mechanism so it is discoverable and consistent with the existing agent-profile / workflow-profile doctrine already in CLAUDE.md, making clear these are three distinct concepts (component profiles vs agent profiles vs workflow-profiles).

## Steps

["Add the documentation: block to src/audiagentic/config/components/session.yaml - identified (2026-07-05) as the correct owning component: it is core: true (always loaded regardless of profile), scope: harness, and its stated purpose ('Harness status and configuration') is exactly what component-profile selection is. No other existing component (providers, project, agent-jobs, release, source-control, agent-ledger, memory, agents, coding-lsp, planning) fits as well - none are core-harness-config in nature. Match the shape already used for other doctrine sections - title/preferred-targets/content.body, e.g. src/audiagentic/config/components/agents.yaml:109 (Agent profile doctrine), planning.yaml:108, release.yaml:53, source-control.yaml:99", "Explicitly call out the distinction from agent-profiles.yaml (provider+model binding) and workflow-profiles (lite/standard/strict stage pipelines) AND from the rig model-profile env vars (AUDIAGENTIC_RIG_MODEL_PROFILE etc, see CP03 naming note) to prevent future confusion - three unrelated things already use the word 'profile' in this codebase", "Document the --component-profile flag / AUDIAGENTIC_COMPONENT_PROFILE env var, default fallback to the base 'audiagentic' folder, and the one-profile-per-process constraint from CP05/CP08", "Regenerate via the actual mechanism: apply_provider_surfaces MCP tool (components/providers/providers_mcp.py:85-90), which calls apply_managed_blocks() (providers/surfaces/base.py:301) via apply_provider_surfaces() in providers/surfaces/manager.py:207 - there is no separate 'surface apply' CLI subcommand, it is this MCP tool"]

## Files

['src/audiagentic/config/components/session.yaml', 'CLAUDE.md']

## Validation

['Generated CLAUDE.md includes the new doctrine section after calling apply_provider_surfaces', 'No existing doctrine sections are altered or duplicated']

## Effort & Risk



## Standards



## Notes

RV104/RV107 incorporated 2026-07-06: session.yaml is the owning edit target; CLAUDE.md is generated output only. Doctrine must mention component-profile vs agent-profile vs workflow-profile, one-profile-per-process from CP05, EV04/EV06 terminology, and stale-term grep coverage. CP08 attribution removed from one-profile constraint.
