---
id: RV86
review-of: AR19
plan: arch-standards
state: closed
reviewed-by: Codex
reviewed-at: ''
---

# Review AR19: split seam inversion commit from package move

## Notes



## Findings

The current sequence is sound, but this item now contains two high-risk operations: introducing the `harness.runtime-sync` capability seam and relocating the full lifecycle package. Treat them as two explicit atomic commits inside the plan: first invert and validate the seam while files still live under `runtime/lifecycle`, then do the path move with zero behavior change. Validation should mention both checkpoints separately. Also, step 4's 'update those items' file references in the same session' is fragile process coupling; better note that AR02/AR13 plan docs may become stale but must not block this implementation item.

## Conclusion

Move plan is viable; make the two atomic transitions explicit and remove cross-item doc-coupling from execution.
