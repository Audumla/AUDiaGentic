---
id: RV137
review-of: AR08
plan: arch-standards
state: closed
reviewed-by: Claude (implementation review, 2026-07-06)
reviewed-at: ''
---

# Unification solid; two importers still route through the old re-export path, and the launch/smoke validation has no evidence of execution

## Notes



## Findings

The core is done well: exactly one AgentContext definition (runtime/harness/context.py, acceptance grep (a) passes), the field union matches the plan precisely (opencode's 13 fields + pi's four extras as Path|None defaults), both runner modules and command.py import the canonical class, no AR17 helper extraction crept in (acceptance (c) holds), and agent_run.py sensibly guards the now-optional agent_bin at use (a reasonable improvement over the plan's assert-at-construction). Two gaps: (1) Step 2 said update ALL importers to the canonical class, but two still route through the old pi module's re-export: runtime/harness/pi/runner/__init__.py:21 (from .context import ...) and components/providers/adapters/pi/hooks.py:33 (imports resolve_agent_bin via pi.runner.context). pi/runner/context.py was already a re-export hub for config helpers before AR08, so this is defensible as pre-existing pattern rather than a new shim -- but the plan text said otherwise; either update those two imports or record the deviation on the item. Note resolve_agent_bin/env_with_pythonpath moved to the shared context.py even though they are pi-only concepts (resolve_agent_bin hardcodes the pi CLI path) -- a small domain-purity wobble in a shared module; acceptable, but worth a comment or a future move back to a pi module. (2) Validation (b) 'both harness launch paths pass including smoke mode' -- no evidence this was run; the full unit+integration suite is green (1924 passed) but no test exercises a real harness launch, so the acceptance rests on the suite alone. Either run the two launch paths (pi + opencode, smoke mode) and note the result on the item, or explicitly record that this criterion was skipped and why.

## Conclusion

Minor follow-ups: update (or deviation-note) the two remaining re-export importers, and execute or explicitly waive the launch/smoke validation with a note on the item. No code defect found in the unification itself.
