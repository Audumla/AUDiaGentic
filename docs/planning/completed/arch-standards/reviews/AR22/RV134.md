---
id: RV134
review-of: AR22
plan: plan-arch-standards
state: closed
reviewed-by: Claude (plan review, 2026-07-06)
reviewed-at: ''
---

# Duplicate items AR20/AR21 created in parallel — consolidated into AR22/CP12, deltas merged both ways

## Notes



## Findings

AR20 ('Eliminate scattered defensive register_all_components() calls') and AR21 ('Consolidate 5 hand-rolled guards') were created in a parallel session responding to the same section-11 rewrite, duplicating AR22 and CP12 respectively. Comparison: AR20 vs AR22 -- AR20 found two call sites AR22's inventory missed (runtime/harness/system_prompt.py:51, runtime/harness/mcp_collector.py:35 -- verified real 2026-07-06; true count is 30 hits, not ~25) and carried two useful steps (double-loading verification in call chains; documenting the composition-root eager-bootstrap exemption), but lacked AR22's decided design (reentrancy in-progress flag -- the hard part), the ledger_events.register() caller-side fix, the features-registry story, and executable validation. AR21 vs CP12 -- AR21 misidentified the shared utility module (says components/registry.py; it is foundation/registry_utils.py), was unaware the utility has no loader support yet, MISSED provision_steps/base.py (_STEP_TYPES, in CP12's five), and lacked the two-instances-one-loader surfaces requirement, the VAL-REG error codes, and the all_tags_loaded rename; its one unique element is foundation/components/hooks.py (_INITIALIZED/initialize_lifecycle_hook_dispatch), which AR21's own step admits is 'event subscription initialization, not data loading' -- AR22's census classifies it as compliant loader-internal wiring (invoked by register_all_components itself, never by product callers), so it is documented out of scope rather than force-migrated onto Registry.

## Conclusion

Keep AR22 and CP12 as the canonical items; merge AR20's deltas (2 call sites, double-loading check, composition-root exemption doc step) into AR22 and record hooks.py's deliberate exclusion in CP12; delete AR20 and AR21 as superseded duplicates. Root cause worth noting: two sessions planned against the same standards change without checking active items first -- CREATING_PLANS.md could gain a 'search existing active items before creating' pre-flight rule.
