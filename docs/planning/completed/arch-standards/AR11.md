---
id: AR11
order: 15
plan: plan-arch-standards
state: completed
validate-first: true
priority: P2
work: S
---

# lsp_projection action elif chain — convert to (action, handler) registry

## Description

Standard 2 violation: components/providers/services/lsp_projection.py:195-208 branches elif action == 'sync-generic-mcp' / 'provision-support' / 'prune-language-servers' / 'prune-generic-mcp' on action tags. The standards prohibit if/elif chains on entity names — use a (key, handler) registry so new projection actions register without editing the dispatch. Smaller same-pattern candidates to sweep in the same change: release/release_please/manage.py:111-116 (state tags) and providers/workflow/provider_cli.py:142 (action tags) — fix if the dispatch is on an extensible entity set, leave if it is a closed two-state branch.

## Steps

1. CLASSIFICATION (decided): lsp_projection.py action chain = EXTENSIBLE (projection actions grow as LSP capabilities are added) → convert to a module-level _ACTION_HANDLERS: dict[str, Callable] registry with dispatch in handle_lsp_provider_projection. release_please/manage.py:111-116 state chain = CLOSED (fixed installation-state machine: legacy-detected/managed-unmodified/...; states are exhaustive outcomes of one detector, not a plugin point) → leave as if/elif, add a comment noting the closed classification. provider_cli.py:142 action chain = CLOSED (install/uninstall are the complete verb set of that CLI command) → leave, same comment.
2. Unknown action in lsp_projection dispatch → make_error(prefix='VAL', component='LSPPRJ', number=<next free>, ...) — a specific code, not a generic one.
3. Record the classification rationale in this item and in a short comment at each retained if/elif site so future reviews don't re-flag them.

## Files

src/audiagentic/components/providers/services/lsp_projection.py; src/audiagentic/components/release/release_please/manage.py; src/audiagentic/components/providers/workflow/provider_cli.py

## Validation

ACCEPTANCE: (a) handle_lsp_provider_projection dispatches via the registry dict (no elif chain on action names); (b) unknown action raises the new VAL-LSPPRJ code with a test; (c) the two retained chains carry closed-classification comments; (d) projection tests pass.

## Effort & Risk



## Standards

ARCHITECTURE_STANDARDS.md §2 (Config Over Code)

## Notes

COMPLETED 2026-07-04. Found already implemented in the working tree: lsp_projection.py dispatches handle_lsp_provider_projection through the module-level _ACTION_HANDLERS registry (five actions incl. sync-language-servers), unknown action raises VAL-LSPPRJ-001, and the two retained chains (release_please/manage.ensure_baseline state machine; provider_cli install/uninstall verb branch) already carry CLOSED-classification comments per step 1's decision. This session added the missing acceptance test: tests/unit/providers/test_lsp_projection_dispatch.py covers VAL-LSPPRJ-001 on unknown action and registry dispatch filling the result slot.
