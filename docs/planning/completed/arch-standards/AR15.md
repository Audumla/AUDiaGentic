---
id: AR15
order: 5
plan: plan-arch-standards
state: completed
validate-first: true
priority: P2
complexity: simple
---

# Deduplicate _active_implementation_id — four copies → one foundation.features helper

## Description

Standard 3: active-implementation resolution is implemented four times with near-identical logic (check component state for enabled implementation → fall back to descriptor default → fall back to first sorted): planning_api.py:36, planning_paths.py:25, memory_api.py:39, memory/hindsight/export.py:102. This is core features-registry logic and belongs beside get_implementations in foundation/features/registry.py as e.g. resolve_active_implementation(project_root, component_id, fallback=None).

## Steps

1. EXECUTION RULE (unambiguous): if AR04 or AR05 is in progress or queued ahead of this item, do this extraction inside that item and mark AR15 completed with a note. Only execute AR15 standalone if neither has started.
2. Add to src/audiagentic/foundation/features/registry.py: resolve_active_implementation(project_root: Path, component_id: str, *, fallback: str | None = None) -> str | None implementing the common precedence: (1) first implementation marked enabled in component state; (2) implementation whose descriptor has default: true; (3) first sorted implementation ID; (4) fallback argument. Wrap the state/registry access in the same broad-except-log-debug guard the current copies use.
3. Known per-copy differences to encode via the fallback parameter: planning_paths.py:25 falls back to 'planning-local-docs'; planning_api.py:36 returns '' (call with fallback=None and coerce); memory_api.py:39 and hindsight/export.py:102 — read each before replacing and match behavior exactly.
4. Replace all four copies in one change; delete the private functions.
5. Run planning + memory test suites.

## Files

src/audiagentic/foundation/features/registry.py; src/audiagentic/components/planning/planning_api.py; src/audiagentic/components/planning/planning_paths.py; src/audiagentic/components/memory/memory_api.py; src/audiagentic/components/memory/hindsight/export.py

## Validation

ACCEPTANCE: (a) rg 'def _?active_implementation_id' src/ returns zero matches; (b) rg 'resolve_active_implementation' shows the single foundation definition plus call sites; (c) planning and memory tests pass with no behavior change.

## Effort & Risk



## Standards

ARCHITECTURE_STANDARDS.md §3 (Logic Containment)

## Notes

COMPLETED 2026-07-03 (executed alongside AR05 per step 1's execution rule). foundation/features/registry.py gained resolve_active_implementation(project_root, component_id, *, fallback=None) with the common precedence and broad-except→fallback guard. All four copies replaced: planning_paths.py (copy deleted; path resolution now goes through foundation/paths which calls the helper), planning_api.active_implementation_id (kept as public facade per AR04's frozen MCP contract — now a one-line delegate coercing None→''), memory_api.py and hindsight/export.py (private wrappers deleted, call sites inlined; one test updated to use the foundation helper). Acceptance note on (a): rg 'def _?active_implementation_id' matches only planning_api's public facade function, which cannot be deleted without breaking the frozen contract from AR04 — it contains no duplicated logic. Tests: memory/planning suites green; fallback behavior covered in tests/unit/foundation/test_component_paths.py.
