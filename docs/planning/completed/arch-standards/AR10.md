---
id: AR10
order: 14
plan: plan-arch-standards
state: completed
validate-first: true
priority: P2
complexity: simple
---

# Replace raw exceptions at public boundaries with AudiaGenticError; fix inline loggers

## Description

Standard 8/9 sweep: raw ValueError/RuntimeError/KeyError raised at component public boundaries — planning_paths.py:62, ledger/paths.py:55, memory/hindsight/recipes.py:76, release/release_please/manage.py:27,:78, providers/workflow/provider_cli.py:75 (KeyError), providers/adapters/openhands/toml_format.py:49,:76 (RuntimeError for missing tomli_w), foundation/toolchains/config_patcher.py:57,:83,:145, foundation/toolchains/probes.py:138, foundation/features/base.py:23. Each must become AudiaGenticError with a canonical PREFIX-COMPONENT-NNN code via make_error()/make_error_factory(). Also standard 9: inline logging.getLogger(__name__) calls in providers/surfaces/base.py:135 (move to module-level logger; formatters.py:111 may be intentional to avoid logger-setup recursion — verify and comment or fix).

## Steps

1. CODE MAP (decided classification — verify reachability, then execute): PUBLIC BOUNDARY, convert to AudiaGenticError via make_error(): planning_paths.py:62 (VAL-PLAN-*), ledger/paths.py:55 (VAL-LEDG-*), memory/hindsight/recipes.py:76 (VAL-HMEM-*), release/release_please/manage.py:27+78 (VAL-RELP-*), providers/workflow/provider_cli.py:75 KeyError (RES-PCLI-*), providers/adapters/openhands/toml_format.py:49+76 RuntimeError (RES-OHAND-* — missing tomli_w dependency), foundation/toolchains/probes.py:138 (VAL-PROBE-*), foundation/features/base.py:23 (VAL-FEAT-*). INTERNAL, leave as-is: foundation/contracts/errors.py self-validation ValueErrors (they guard the error system itself); foundation/toolchains/config_patcher.py:57/83/145 IF verified reachable only via provision steps that already wrap errors — verify with callers first, convert if reachable from recipe YAML input.
2. Number allocation: within each component, rg for existing make_error codes with the same prefix and take the next free number; never reuse.
3. NOTE: planning_paths.py:62 and ledger/paths.py:55 are deleted by AR05 (replaced by VAL-PATHS-001) — if AR05 has landed, skip them here.
4. Inline logger fix: providers/surfaces/base.py:135 → add module-level logger = logging.getLogger(__name__) and use it. foundation/logging/formatters.py:111 → verified likely intentional (logging infrastructure avoiding module-level logger recursion during setup); keep, add a one-line comment stating the constraint.
5. Update/add tests asserting each new canonical code.

## Files

src/audiagentic/components/planning/planning_paths.py; src/audiagentic/components/ledger/paths.py; src/audiagentic/components/release/release_please/manage.py; src/audiagentic/components/providers/workflow/provider_cli.py; src/audiagentic/components/providers/adapters/openhands/toml_format.py; src/audiagentic/foundation/toolchains/config_patcher.py; src/audiagentic/foundation/toolchains/probes.py; src/audiagentic/foundation/features/base.py; src/audiagentic/components/providers/surfaces/base.py

## Validation

ACCEPTANCE: (a) rg 'raise (ValueError|RuntimeError|KeyError)' src/audiagentic returns matches only in foundation/contracts/errors.py and any config_patcher.py sites verified internal in step 1; (b) every converted site has a test asserting its canonical code; (c) rg 'logging\.getLogger\(__name__\)\.' src/audiagentic returns only formatters.py with its justifying comment.

## Effort & Risk



## Standards

ARCHITECTURE_STANDARDS.md §8 (Error Handling), §9 (Logging)

## Notes

COMPLETED 2026-07-04. CONVERTED to make_error with canonical codes: openhands/toml_format.py (RES-OHAND-001, both sites), release_please/manage.py (VAL-RELP-001/002), foundation/toolchains/probes.py CompositeHealthCheck mode (VAL-PROBE-001), foundation/toolchains/config_patcher.py set_key/set_list_entry empty key_path (VAL-PATCH-001 — verified reachable from recipe YAML via ConfigSetStep), foundation/workflow/transitions.py load_workflow KeyErrors (VAL-WKFL-001/002 — not in the original list but caught by the acceptance grep; no consumer relied on KeyError). LEFT AS INTERNAL with justifying comments: provider_cli._find KeyError (store-adapter framework contract — workflow state_machine catches KeyError, e.g. is_terminal); features/base.from_string ValueError (loader.py catches and wraps it in VAL-FDESC-012 at the boundary); config_patcher revert unknown-operation ValueError (OwnedChange.operation is only ever set by the class itself — programming-error invariant); contracts/errors.py self-validation. planning_paths/ledger paths sites were already deleted by AR05 (VAL-PATHS-001). LOGGERS: providers/surfaces/base.py inline getLogger → module-level logger; foundation/features/registry.py (from AR15) → module-level _logger; logging/formatters.py inline call kept with comment explaining the recursion constraint. Tests: tests/unit/foundation/test_boundary_errors.py asserts VAL-PROBE-001, VAL-WKFL-001/002, VAL-RELP-001; patcher test updated to VAL-PATCH-001. Full unit suite green.
