---
id: DO01
order: 0
plan: plan-docker-testing
state: completed
breadth: ''
skill: 2
---

# Repair stale Docker packaging lifecycle gate

## Description

Dockerfile.packaging still calls the removed `audiagentic install` command. This makes the consolidated Docker run fail even though later tests pass.

Replace it with the current public bootstrap/harness installation workflow.
The test must use AUDiaGentic to install the harness rather than assuming a preinstalled binary.

## Steps

['Find all references to removed `audiagentic install` command in Dockerfile.packaging', 'Replace with current public bootstrap/harness installation workflow', 'Verify test uses AUDiaGentic to install the harness, not a preinstalled binary', 'Run Docker packaging test to confirm it passes']

## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files

['Dockerfile.packaging', 'docker/']

## Validation

Dockerfile.packaging builds successfully. Test installs harness via AUDiaGentic, not preinstalled.

## Effort & Risk



## Standards



## Notes

Completed: replaced removed `audiagentic install` with `audiagentic bootstrap` in run_packaging.sh (check 4) and Dockerfile.packaging comments. Bootstrap calls the same install_to function plus installs core components — functionally equivalent for the packaging gate.

## Priority

P0
