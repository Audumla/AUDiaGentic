---
id: DO04
order: 0
plan: docker-testing
state: completed
breadth: ''
skill: 2
---

# Test real installation instead of pre-baked hosts in Docker

## Description

Base images should contain only language/runtime prerequisites (Python, Node.js, etc.).
AUDiaGentic should install supported harnesses and generate their configuration during the test.

Performance images may layer cached dependencies, but must be clearly separated from clean-install proof.
Do not allow a preinstalled harness to turn an installation test into a configuration-only test.

## Steps

['Audit base images — verify they only contain language/runtime prerequisites', 'Remove preinstalled harnesses from base images', 'Ensure AUDiaGentic installs harnesses during test, not before', 'Separate performance images (with cached deps) from clean-install proof images', 'Document the separation clearly']

## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files

['Dockerfile*', 'docker/']

## Validation

Base images contain only language/runtimes. Harnesses installed by AUDiaGentic during test. Clean-install and performance images are separate.

## Effort & Risk



## Standards



## Notes

Audit complete — Docker setup already meets requirements:
1. audia-test-base contains only Python, Node.js, git, system deps (no harnesses)
2. Harnesses installed by AUDiaGentic bootstrap during test
3. Performance images (mcp-tools-e2e) pre-install LSP servers but are clearly separate from clean-install proof (Dockerfile.test excludes mutates_host tests)
4. Dockerfile.lsp-install-test comment: 'Compiler prerequisites only; AUDiaGentic installs the language servers'
5. No pre-baked harnesses turning install tests into config-only tests

## Priority

P1
