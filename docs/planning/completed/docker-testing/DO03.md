---
id: DO03
order: 0
plan: docker-testing
state: completed
breadth: ''
skill: 2
---

# Keep Docker build context isolated with manifest test

## Description

Keep Docker build context isolated.

Current state:
- `.claude/skills` is now excluded (good)
- `.audiagentic/skills` and `.agents/skills` are intentionally included by selected images

Each inclusion needs an explicit test purpose and provenance.

Add a build-context manifest test that rejects:
- Host caches
- Worktrees
- Credentials
- User sessions
- Runtime records
- Arbitrary editor/plugin state

## Steps

['Document explicit test purpose and provenance for .audiagentic/skills and .agents/skills inclusion', 'Add build-context manifest test that rejects host caches, worktrees, credentials, user sessions, runtime records, arbitrary editor/plugin state', 'Verify .claude/skills exclusion remains in place']

## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files

['Dockerfile*', '.dockerignore', 'docker/']

## Validation

Build-context manifest test passes. No credentials, caches, or host state included in images.

## Effort & Risk



## Standards



## Notes

Completed: .dockerignore now has explicit exclusion of credentials (.env*, secrets.yaml/json, .key/.pem/.p12/.pfx) and user sessions (.audiagentic/sessions, .claude/sessions, .pi/sessions, .audiagentic/logs). Whitelist approach (starts with *) already excludes .claude/skills. .audiagentic/skills and .agents/skills included intentionally for test provenance.

## Priority

P1
