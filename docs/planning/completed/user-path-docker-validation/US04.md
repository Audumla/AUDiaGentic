---
id: US04
order: 40
plan: plan-user-path-docker-validation
state: completed
validate-first: true
priority: P1
work: L
created-by: codex
---

# Harden Docker user-path suite and failure triage workflow

## Description

Integrate the lifecycle/component/recipe scenarios into the test framework with reusable reporting, bounded runtime, deterministic isolation, and linked planning reviews for newly discovered failures.

## Steps

1. Add shared scenario runner and artifact diagnostics. 2. Partition cases for parallel CI without sharing mutable homes. 3. Add retry-free readiness checks and deterministic teardown. 4. Document local/CI execution. 5. File and link review items for reproducible failures, then rerun fixed cases.

## Files

tests/docker; test framework configuration; testing documentation

## Validation

2026-07-18: clean Docker image rebuild succeeds with mutable runtime excluded. Focused clean-image unit, provider, job, and architecture-boundary gates pass. Docker worker tests cover multi-project roots/profiles, secret canary non-bleed, unsupported policy outcome, gateway dispatch, descendant timeout cleanup, and three-profile worker launch. Clean lane now installs its evidenced ACP/process test dependencies and runs serially because lifecycle hooks share mutable process registries/toolchain caches; it excludes only opt-in, host-mutating, and nested Docker/daemon tests. Focused clean-image ACP, lifecycle, and SH06 worker checks pass. RV674 tracks an independent provider lifecycle projection/registration failure found by focused planning coverage.

## Effort & Risk

Keep helpers test-only and small; do not create a second production installer model inside the suite.

## Standards



## Notes

2026-07-18: Added focused Docker image and a portable credential-free matrix. Runtime is bounded to deterministic materialization; real third-party package/model provisioning remains in existing opt-in provider images. Exact clean-image command: `docker build -f tests/docker/Dockerfile.harness-user-path -t audiagentic-harness-user-path:local .` then `docker run --rm -e AUDIAGENTIC_DOCKER_TESTS=1 audiagentic-harness-user-path:local python -m pytest tests/integration/harness/test_harness_user_path.py tests/integration/harness/test_harness_mcp_lifecycle.py -q` (11 pass). 2026-07-18 Docker lane repair: `tests/docker/run_suite.sh` excludes `docker`/`requires_docker` (host daemon) tests and runs remaining selected suite serially; `Dockerfile.test` installs `[acp]` plus psutil; SH06 tests are isolation tests, not nested-Docker tests, and remain selected.

## Ledger Events

- chg_20260717_155335_removed-a-python-deprecation-w_5351
- chg_20260717_165334_harness-lifecycle-validation-n_5799
- chg_20260717_171546_gateway-jobs-now-run-through-i_8630
- chg_20260717_173156_clean-docker-validation-now-st_6444
- chg_20260718_082722_gateway-lifecycle-states-are-n_7927
