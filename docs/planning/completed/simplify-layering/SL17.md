---
id: SL17
order: 0
plan: plan-simplify-layering
state: completed
validate-first: true
priority: P2
work: M
---

# Assess collapsing the RecipeSpec assembler back to two concrete subclasses (does config-driven assembly earn its keep?)

## Description

DECISION item (do NOT implement without maintainer direction). Follows SL15 completion (2026-07-09, RV188). SL15 landed a HINDSIGHT-LOCAL config-driven recipe assembler (recipe_spec.py) that replaces two per-kind recipe classes (GuidanceOnlyRecipe, HooksInstallerRecipe) with declarative RecipeSpec data. It is correct, tested, and doctrinally clean — but it does not pass SL15 gate 7 (net LOC down), and the reason is structural, not fixable by more trimming.

THE TENSION (measured 2026-07-09):
- Current assembler footprint: recipe_spec.py = 341 lines + tests/unit/memory/test_recipe_spec.py = 145 lines = ~486 lines, plus the _GUIDANCE_SPEC constant + inline hooks spec + 8 assemble_hindsight_recipe call sites in strategies.py, plus duplicate spec construction in the goldens.
- The concrete-class alternative it replaced: GuidanceOnlyRecipe (~40 lines) + HooksInstallerRecipe (~68 lines) = ~108 lines in recipes.py, no recipe_spec.py, no test_recipe_spec.py, goldens instantiate classes directly.
- So the assembler costs roughly +380 LOC over two hand-written subclasses to serve exactly TWO kinds.

WHY STRUCTURAL: a config/assembler layer only pays for itself when the number of config-shaped consumers is large or growing. Per SL15's own post-SL13 classification table, the remaining kinds (_McpConfigAdapter, PluginConfig, _PluginUrlConfig, _PluginArray) are all >~30% custom logic and EXPLICITLY STAY AS CODE. There is no pipeline of future config-shaped kinds to amortize the assembler — it is a 2-consumer generic mechanism, exactly the regime where two small subclasses win on LOC and readability (no runtime type() synthesis, no VAL-RSPEC validation of developer-authored constants, directly steppable classes).

THE OTHER SIDE (why keep): the user explicitly chose the config-driven direction in SL15; RecipeSpec is declarative and could later be sourced from YAML (matrix rows) so non-programmers add integrations; and the schema validation catches spec-authoring mistakes. If any of those becomes real, keep the assembler. This item is the deliberate call between the two, made on evidence not taste.

## Steps

VALIDATE-FIRST (decision, no code):
1. Confirm the consumer count is still exactly two config-shaped kinds (guidance + hooks) and no roadmap item introduces a third (grep the matrix + strategies factories; re-read the SL15 classification table). If a third config-shaped kind is planned/landed, STOP — keep the assembler, close as 'keep'.
2. Confirm no external/YAML spec source has been introduced (specs are still Python module constants). If specs now come from config, STOP — the assembler earns its keep, close as 'keep'.
3. Record the LOC delta both ways from the CURRENT tree so the decision is on real numbers.

IF DECISION = COLLAPSE (only with maintainer sign-off):
4. Restore GuidanceOnlyRecipe (no_automation delegate + probe status override) and HooksInstallerRecipe (DeclaredStepRecipe delegate + configure/prune/dry_run status behaviour) as small _RowRecipe subclasses in recipes.py — behaviour byte-identical to the assembled output the goldens pin.
5. Point strategies factories back at the classes: _build_guidance_only_recipe -> GuidanceOnlyRecipe(row); _build_hooks_recipe -> HooksInstallerRecipe(row, backend) preserving the source-gate (verified=False path). Remove _GUIDANCE_SPEC and the inline hooks RecipeSpec.
6. Delete recipe_spec.py and tests/unit/memory/test_recipe_spec.py (grep-confirm zero importers of assemble_hindsight_recipe / RecipeSpec / ParamBinding / StatusOverride / validate_recipe_spec outside docs/planning).
7. Update goldens (test_hindsight_stamping_golden.py) to instantiate the restored classes directly; pinned stamped values must NOT change.
8. Update docs/standards/USING_RECIPES.md: down-scope section 8 to a short 'considered and reverted for a 2-consumer scope' note, keeping the layering-on-top anti-pattern.

IF DECISION = KEEP: close with the rationale (a third config-shaped kind or a YAML spec source materialised); leave the assembler as-is.

## Files

src/audiagentic/components/memory/hindsight/recipe_spec.py (DELETE if collapse)
src/audiagentic/components/memory/hindsight/recipes.py (restore GuidanceOnlyRecipe + HooksInstallerRecipe if collapse)
src/audiagentic/components/memory/hindsight/strategies.py (factories back to classes; drop specs if collapse)
tests/unit/memory/test_recipe_spec.py (DELETE if collapse)
tests/unit/memory/test_hindsight_stamping_golden.py (instantiate classes directly if collapse)
docs/standards/USING_RECIPES.md (down-scope section 8 if collapse)

## Validation

1. ACCEPTANCE GATE: test_hindsight_stamping_golden.py stamped values unchanged either way — behaviour is the invariant, not implementation.
2. test_hindsight_registry_provision.py green (guidance + hooks provision through the registry without exception) — the crash-regression guard must survive the collapse.
3. Strategy fallback tests green (platform fallback, blocked->guidance, no mcp_config, stdio+remote->fallback).
4. grep: zero references to assemble_hindsight_recipe / RecipeSpec / recipe_spec outside docs/planning after a collapse.
5. Full unit suite + ruff green.
6. If collapsed: net LOC down ~380 vs current (informational, the point of the item).

## Effort & Risk

Mid / LOW risk. Behaviour-preserving reshaping gated by the goldens; the classes existed before SL15 so restoration is mechanical. The real work is the DECISION (steps 1-3), which is cheap. Primary risk is thrash: SL15 just landed the assembler, so only collapse on a firm maintainer decision that the 2-consumer scope is stable — otherwise this oscillates. Do NOT auto-collapse.

## Standards



## Notes

RESOLVED 2026-07-14 — DECISION = KEEP (RV380, closing per this item's own 'IF DECISION = KEEP' branch). Rationale: revisit trigger (b) fired — the approved MA16 capability-operation schema (docs/reference/CAPABILITY_OPERATION_SCHEMA.md, APPROVED shape v0) makes YAML declarations the authoring source for recipe specs, ending the 'specs remain Python constants' regime; trigger (a) is also flipping as MA12 converts 16 provider cli_install declarations onto the same declarative path. The assembler earns its keep in the declaration-sourced regime; the collapse to two concrete subclasses is dead on this item's own terms. The assembler's future home is foundation via MA10 (managed-config-consistency), whose step 1 census now serves extraction characterization only. No code change in this item.

--- Original decision record below ---

Source: SL15 completion caveat (2026-07-09). REVISIT TRIGGER: implement the collapse only if BOTH hold — (a) no third config-shaped recipe kind is planned/landed, and (b) specs remain Python constants. If either flips, close as 'keep'. Doctrine: a generic mechanism must be justified by real consumers. Related: [[SL15]] (assembler landed), [[SL16]] (attempt-1 revert). RV369 (2026-07-14) had moved the disposition to MA10 step 1; RV380 resolves it ahead of MA10 because the trigger fired by explicit user decision, not census outcome. Consumer census evidence retained in MA10's characterization inputs (.audiagentic/evidence/ + hindsight_matrix.yaml baseline: 8 of 15 provider rows through the assembler on two patterns).

## Ledger Events

- chg_20260714_021016_simplified-the-capability-sche_8992
- chg_20260714_081210_completed-the-remaining-archit_9577
