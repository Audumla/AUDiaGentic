---
id: RV167
review-of: SL13
plan: plan-simplify-layering
state: closed
reviewed-by: claude
reviewed-at: ''
---

# Implementation review: MCP consolidation (A1-A6) landed correctly; A7 rules dual-writer + A8 + doc cleanup outstanding

## Notes

Findings 2 and 4 fixed 2026-07-08. A7 investigation: two writers target same instruction files (AGENTS.md, CLAUDE.md). surfaces uses HTML-comment region; managed_block uses Markdown #-comment sentinels. Both survive each other's writes today. Fix: add apply_surface_block/remove_surface_block to manager.py that operate within shared region, preserving other blocks.

## Findings

Reviewed the landed SL13 implementation 2026-07-08 (SL13 still correctly marked pending). VERIFIED DONE and correct:
- A1 get_managed_entry_status (providers/services/mcp.py:295): contract matches the spec exactly — all keys always present, no-mcp_config→ok=False reason='no mcp_config', read-failure→reason='read failed', absent/stale/match. Good.
- A2 _McpConfigAdapter: stores backend/server_name/entry/config_path directly (no _inner); probe/verify via get_managed_entry_status; configure/prune via _sync_hindsight_mcp_entry; THE DOUBLE-REMOVAL IS GONE (the key HM21 leftover this item targeted). Correct.
- A3 _CompositeRecipe MCP half rewired to the same provider machinery.
- A4 HindsightMcpRecipe deleted; mcp_recipe.py reduced to payload builders.
- A5 PluginConfigRecipe rewired to provider machinery (no _inner/HindsightMcpRecipe).
- A6 ManagedEntryRecipe + ConfigEntryTarget deleted from foundation recipe_patterns.py.
All targeted tests green (memory + toolchains + test_provider_mcp_config); ruff clean; behaviour-preserving (goldens hold).

OUTSTANDING / GAPS:
1. A7 NOT IMPLEMENTED (the biggest correctness item). RulesOnlyRecipe still writes via standalone apply_managed_block/remove_managed_block (recipes.py:316,361) into descriptor.instruction_file — the SAME file the provider surfaces machinery manages. INVESTIGATION COMPLETE: active clobbering absent today (surfaces strips only HTML-comment region; managed_block uses Markdown #-comments). Architectural divergence remains: no co-location in shared region, implicit dependency on strip logic, no unified ownership tracking. apply_surface_block/remove_surface_block not yet added.
2. [INCORPORATED] A6 docstring fix applied — ManagedEntryRecipe reference removed from providers/services/recipes.py layering.
3. A8 PARTIAL: _CompositeRecipe kept as minimal combiner (~180 lines) — acceptable end state. Artifact aggregation + provision/teardown ordering preserved.
4. [INCORPORATED] Prune now checks sync result in both _McpConfigAdapter and _CompositeRecipe.

## Conclusion

MCP consolidation is well-executed and behaviour-preserving. Findings 2 (stale docstring) and 4 (prune sync check) incorporated. A7 (rules dual-writer) remains the key correctness item for SL13 completion; investigation confirms architectural divergence without active clobbering — surfaces region survives managed_block writes and vice versa. A8 (_CompositeRecipe end state): decision to keep as minimal combiner documented.
