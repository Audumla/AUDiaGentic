---
id: RV168
review-of: SL15
plan: plan-simplify-layering
state: closed
reviewed-by: ''
reviewed-at: ''
---

# Validate-first classification table from landed post-SL13/SL14 code

## Notes

Classification based on actual line counts and custom-logic percentage of each recipe class.

## Findings

CLASSIFICATION TABLE (landed code, 2026-07-08):

(a) PURE CONFIG — migrate to RecipeSpec immediately:
- GuidanceOnlyRecipe (recipes.py:227-264, ~38 lines, 0% custom): wraps NoAutomationRecipe + _stamp. Data: action_needed, skip_status strings.

(b) FOUNDATION-PATTERN BINDING — migrate to RecipeSpec:
- HooksInstallerRecipe (recipes.py:158-224, ~67 lines, 10% custom): wraps DeclaredStepRecipe/InstallManifest from row fields. dry_run formatting + fixed configure/prune strings.
- RulesOnlyRecipe (recipes.py:266-406, ~141 lines, 25% custom): managed block apply/register/prune with RULE_TEXT constant and artifact registry. All operations are compositions of existing primitives.
- _McpConfigAdapter (recipes.py:434-574, ~141 lines, 15% custom): dispatches to get_managed_entry_status + _sync_hindsight_mcp_entry. provision_steps builds ConfigSetStep from row data.
- _PluginArrayRecipe (plugin_recipes.py:452-531, ~80 lines, 10% custom): resolve_ref'd reader/writer/remover hooks. All provider knowledge from matrix row fields.

(c) GENUINELY-CUSTOM — STAY CODE (>30% custom logic):
- PluginConfigRecipe (plugin_recipes.py:35-198, ~164 lines, 40% custom): conditional _config_path branching (managed sync vs CLI), source gates, multiple fallback paths.
- _PluginUrlConfigRecipe (plugin_recipes.py:301-450, ~150 lines, 35% custom): JSON payload builder with conditional key inclusion, file I/O, Windows repair hook call.
- _CompositeRecipe (recipes.py:576-796, ~221 lines, 50%+ custom): composite ordering, dual-result aggregation across MCP+rules halves, artifact merging, complex error handling.

(d) STRATEGY DECISIONS — STAY in strategies.py:
- Platform fallback: _platform_supported → _external_fallback_row
- Source gating: source_status != verified → GuidanceOnly downgrade
- Remote-MCP capability fallback: spec.remote check for stdio-only providers
- Path resolution: _resolve_harness_config_path, _resolve_rule_path with descriptor lookups and fallback chains
- Plugin subvariant selection: plugin_array_package > url_config_path > generic MCP target

## Conclusion

5 classes migrate to RecipeSpec (2 × category-a + 3 × category-b). 3 classes stay code (>30% custom). Strategy decisions remain in strategies.py. Proceed with RecipeSpec implementation.
