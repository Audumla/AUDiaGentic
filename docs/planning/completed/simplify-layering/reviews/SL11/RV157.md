---
id: RV157
review-of: SL11
plan: plan-simplify-layering
state: closed
reviewed-by: claude
reviewed-at: ''
---

# action_needed promotion validated against all consumers — detail step 2 with the full inventory and mechanics

## Notes



## Findings

Full-repo action_needed audit 2026-07-07:
1. FIELD DEFINITION: only ProviderRecipeResult (services/recipes.py:75) carries it; foundation RecipeResult does not — this is the exact coupling forcing foundation-eligible patterns to return the provider type.
2. WRITERS: hindsight recipes.py has 30+ construction sites (row.audia_action / row.notes); services/lsp_recipes.py:91 is a SECOND capability already using the semantics ('provider has no LSP integration') — rule-of-two satisfied in-repo for the generic concept; services/mcp.py:166 uses the same key in a plain dict result (restart guidance).
3. READERS: hindsight status.py:60 via getattr(status,'action_needed',''); _RowRecipe._stamp (recipes.py:142) and ProviderCapabilityRecipe.to_result (services/recipes.py:197) via getattr-with-default — both defensive precisely because plain RecipeResult lacks the field; registry legacy dispatch copies it field-by-field (services/recipes.py:301,313); integration test reads result.action_needed directly (test_provider_hindsight_lifecycle.py:186).
4. MECHANICS VALIDATED: RecipeResult is a plain (non-frozen) dataclass — adding `action_needed: str = ""` with ok()/fail() kwargs is constructor-compatible (all call sites use kwargs; none positional past 'state') and dataclasses.replace()-safe (provision/teardown orchestration uses replace). ProviderRecipeResult is standalone-frozen (NOT a subclass) and keeps its own field — no inheritance change; the promotion makes the two types structurally aligned so to_result/_stamp become straight attribute copies.
5. §10 CLEANUP IN SAME CHANGE: once the field exists on RecipeResult, the getattr(...,'action_needed','') defensive reads in _stamp, ProviderCapabilityRecipe.to_result, and status.py become direct attribute access (getattr-with-default is a compat shim once the field is universal); run_steps() gains the ability to carry gate-refusal guidance.
6. NO serialization consumers of RecipeResult found (status.py builds dicts explicitly; ledger/status paths read named fields).

## Conclusion

Promotion is safe and doubly-motivated (hindsight + LSP recipes). Update SL11 step 2 with the validated inventory, the exact mechanics (field + ok/fail kwargs + replace-safety), the getattr→direct-access cleanup, and a new-test requirement (RecipeResult round-trips action_needed through orchestration replace() paths and run_steps).
