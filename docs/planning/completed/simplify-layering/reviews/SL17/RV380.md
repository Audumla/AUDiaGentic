---
id: RV380
review-of: SL17
plan: simplify-layering
state: closed
reviewed-by: claude
reviewed-at: ''
---

# RULING: keep the assembler — revisit trigger (b) has fired

## Notes

SL17's own decision rule: 'implement the collapse only if BOTH hold — (a) no third config-shaped recipe kind is planned/landed, and (b) specs remain Python constants (no YAML/matrix-sourced spec). If either flips, close as keep.' Trigger (b) has now fired by explicit user decision: the approved MA16 capability-operation schema (CAPABILITY_OPERATION_SCHEMA.md, APPROVED shape v0, RV373) makes YAML declarations the authoring source for recipe specs, and MA10 will promote the assembler to foundation consuming that schema. Trigger (a) is also on track to flip: MA12 converts 16 provider cli_install declarations onto the same declarative path. The collapse is dead on SL17's own terms.

## Findings



## Conclusion

Close SL17 as KEEP per its own IF DECISION = KEEP branch. MA10 step 1 census remains as extraction characterization input only — the keep-vs-collapse decision is no longer open.
