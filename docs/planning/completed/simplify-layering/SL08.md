---
id: SL08
order: 8
plan: simplify-layering
state: done
validate-first: true
priority: P1
work: M
---

# Deep state nesting is error-prone

## Description

Feature state uses 3-level nesting: `component -> features[kind][id]` and `component -> implementations[id] -> features[kind][id]`. The `get_implementation_feature_enabled_explicit` function requires 30 lines of defensive `isinstance` checks and `.get()` chaining. This nesting makes it easy to introduce None dereference bugs.

## Proposed Solution

Flatten the state structure to use composite keys:
```yaml
coding-lsp:
  features:
    "language:python":
      enabled: true
      options: {}
    "language:cpp":
      enabled: false
  implementations:
    ag-lsp:
      enabled: true
      features:
        "language:python":
          enabled: true
```
Or introduce a `FeaturePath` dataclass (`parent`, `scope`, `kind`, `id`) that encapsulates the nesting logic and provides safe accessors. The flattened key approach is simpler and avoids deep nesting while preserving the logical grouping.

## Architecture Standards Assessment

**Standards risk: Standard #2 (Config Over Code).** Current code has defensive `isinstance` chains on entity names, which is prohibited. Flattening to composite keys eliminates the need for these chains. Migration logic must be data-driven, not hardcoded per-component.

## Resolution Assessment

**Complexity: Moderate.** Requires state file format migration.

**Solution:** Flatten state structure to composite keys (`"language:python"`) or introduce `FeaturePath` dataclass. Composite key approach is simpler and avoids deep nesting while preserving logical grouping. Requires updating state reader/writer and migration logic for existing YAML. Breaking change to state file format — needs backward-compatible migration.

**Files:** 3-4 — state reader/writer updates, migration logic, tests.

## Effort & Risk

Effort: Medium. Risk if unchanged: Bug surface area, but contained.
