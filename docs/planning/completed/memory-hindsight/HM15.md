---
id: HM15
order: 123
plan: memory-hindsight
state: completed
validate-first: true
priority: P1
work: M
---

# HM07 chunk C: extend existing Hindsight status output

## Description

Final HM07 chunk. Reuse `build_hindsight_status` and existing recipe-registry status plumbing to expose `source_status`, `source_date`, and owned-artifact inventory, while keeping top-level `memory_status` provider-agnostic.

## Steps

['Add tests first around `build_hindsight_status` output shape and `memory_status` non-goals.', 'Extend existing Hindsight status assembly only; do not create a second status path.', 'Surface owned artifact info from artifact registry and include source freshness fields from matrix rows/results.', 'Keep `memory_status` limited to backend/config health plus neutral refresh-needed/change signal state.']

## Files

['src/audiagentic/components/memory/hindsight/recipes.py', 'src/audiagentic/components/memory/memory_api.py', 'tests/unit/memory/test_hindsight_recipe.py', 'tests/unit/memory/test_memory_component.py']

## Validation

['Per-provider Hindsight status includes `kind`, `state`, `status`, `action_needed`, `source_url`, `source_status`, `source_date`, and owned artifacts.', '`memory_status` still exposes no per-provider Hindsight detail.', 'No duplicate status assembly path introduced.']

## Effort & Risk

Mid. Depends on HM14. Main risk is status duplication or leaking provider detail into memory core.

## Standards



## Notes

Closes HM07 implementation scope once HM13/HM14 land.
