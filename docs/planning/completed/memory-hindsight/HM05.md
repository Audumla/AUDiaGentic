---
id: HM05
order: 5
plan: plan-memory-hindsight
state: completed
validate-first: true
priority: P1
work: L
---

# MCP-config + recall/retain rule strategy

## Description

Implement the mcp-config(+rule) strategy in the HM03 builder: write the remote Hindsight entry through controlled provider target data/generic provider writer seams, and insert a managed recall/retain rule block. Hindsight-specific logic remains in `components/memory/hindsight/`; provider core exposes only generic config/descriptor seams. Includes the openhands adapter prerequisite (no generic mcp_config today).

## Steps

['Builder mcp-config branch: construct desired remote MCP entry from HindsightBackendConfig (name=backend.server_name, url=backend.mcp_url, headers=backend.headers(), transport=backend.transport) and write it using a controlled HindsightTarget derived from generic provider descriptor/config metadata.', 'Rule: if spec.rule_block, apply_managed_block(rule_target, "hindsight-recall-retain", RULE_TEXT) and register the BlockChange in artifact_registry under recipe id memory:hindsight:<provider>.', 'verify: provider reader/ConfigKeyCheck confirms entry and rule block present. prune/uninstall removes only AUDiaGentic-owned entry/block.', 'OPENHANDS PREREQ landed: generic McpConfigSpec exists with config_path=".openhands/config.toml" and TOML reader/writer/remover over [mcp_servers.<name>] using the HM02 remote shape.', 'COPILOT PATH (sub-task): confirm whether running Copilot reads .vscode/mcp.json (Hindsight target) vs our adapter .mcp.json; set rule_target=.github/copilot-instructions.md.', 'Providers covered by memory/hindsight matrix: copilot, openhands, continue, roo, gemini(self-hosted); fallback-mcp (no official rule, our rule only): goose, pi, qwen.']

## Files

['src/audiagentic/components/memory/hindsight/recipes.py', 'src/audiagentic/components/memory/hindsight/mcp_recipe.py', 'src/audiagentic/components/providers/services/mcp.py (generic provider MCP writer only; no Hindsight imports)', 'tests/unit/memory/test_hindsight_recipe.py']

## Validation

['test_mcp_strategy_writes_remote_entry: provider config gets {url:<mcp_url>,headers} under its container key', 'test_rule_block_idempotent: applied once, second provision no-op', 'test_uninstall_clean: entry + rule removed, user/external entries preserved', 'test_openhands_toml_writer: remote [mcp_servers.hindsight] round-trips (after prereq)', 'Live (HM09): entry url reachable via MCP initialize']

## Effort & Risk

Complex. DEPENDS ON HM02 (remote entry) + HM03 (builder). OpenHands McpConfigSpec has landed. Provider-specific reader/writer/remover callbacks must be used when available; generic ConfigPatcher is only fallback for default JSON shapes.

## Notes

Per STRATEGY_MATRIX.md, mcp-config+rule applies to: copilot, openhands, continue, roo, gemini; fallback-mcp to goose, pi, qwen. Remaining gaps: (1) copilot: our adapter writes .mcp.json but Hindsight targets .vscode/mcp.json + .github/copilot-instructions.md — confirm which the running Copilot reads. (2) gemini Cloud needs an OAuth proxy; self-hosted is direct.

## Execution guidance for lighter agents

- Do not implement provider-specific Hindsight modules under providers to make
  mcp-config work. Add only generic provider seams/adapters such as McpConfigSpec.
- Hindsight-specific rule text, matrix rows, and recipe decisions stay under
  `components/memory/hindsight/`.
- If a provider lacks a generic config writer, mark that provider blocked rather
  than overloading provider YAML with Hindsight-specific keys.
- Unit tests should use temporary config files and fake provider targets; live
  Hindsight server checks belong to HM09.
