---
id: HM17
order: 132
plan: plan-memory-hindsight
state: completed
validate-first: true
priority: P2
work: M
---

# HM12 chunk B: plugin-repair metadata and recipe hardening

## Description

Second HM12 cleanup chunk. Remove hardcoded Claude plugin repair paths, add logging/error normalization at public recipe boundaries, and only then split modules if `recipes.py` still holds multiple responsibilities.

## Steps

['Move plugin repair path metadata into matrix YAML row fields and extend `HindsightRecipeRow` accordingly; empty fields must no-op generically.', 'Add module logger(s) and ensure external-boundary exceptions log with `exc_info=True`.', 'Replace raw public-boundary `ValueError` from command parsing with canonical `AudiaGenticError`/error helper using memory error-code prefix.', 'After cleanup, re-measure `recipes.py`; split only if still carrying multiple major responsibilities, keeping matrix/base modules dependency-light.']

## Files

['src/audiagentic/components/memory/hindsight/recipes.py', 'src/audiagentic/components/memory/hindsight/matrix.py', 'src/audiagentic/config/components/memory/hindsight_matrix.yaml', 'src/audiagentic/components/memory/hindsight/mcp_recipe.py', 'tests/unit/memory/test_hindsight_recipe.py']

## Validation

['Plugin repair config test proves empty metadata no-ops and Claude paths come from YAML, not Python constants.', 'Logger/error tests verify canonical error shape and `exc_info=True` logging on external failures.', 'If files split, no new module exceeds one major responsibility and no import cycles appear.']

## Effort & Risk

Mid. Depends on HM16. Main risk is turning cleanup into file churn without behavior gain.

## Standards



## Notes

Completes HM12. Agent may stop after hardening if `recipes.py` responsibilities are clean enough without more splitting.
