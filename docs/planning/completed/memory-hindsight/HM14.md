---
id: HM14
order: 122
plan: plan-memory-hindsight
state: completed
validate-first: true
priority: P1
work: M
---

# HM07 chunk B: enabled-provider filtering and backend-switch reconcile

## Description

Second HM07 chunk. Reconcile only enabled providers, prune stale disabled-provider artifacts, and prove backend URL/token/bank switches rewrite owned MCP/config artifacts through the HM10 generic sync path.

## Steps

['Compute enabled vs all provider IDs in orchestration seam and pass existing `provider_ids`/`all_provider_ids` parameters through to contained Hindsight reconcile helpers.', 'Add pruning coverage for disabled providers; recipes and matrix stay unaware of enabled/disabled semantics.', 'Add backend-switch regression that provisions with backend A, then backend B, and verifies all owned MCP/config artifacts now point at B.', 'Do not expand recipe-class duplication here; reuse current provision/reconcile helpers.']

## Files

['src/audiagentic/components/memory/hindsight/provision.py', 'src/audiagentic/components/memory/memory_observer.py', 'tests/unit/memory/test_hindsight_provision.py', 'tests/unit/memory/test_hindsight_recipe.py']

## Validation

['Disabled providers lose Hindsight-owned artifacts while enabled providers stay configured.', 'Backend URL/token/bank change rewrites every owned provider artifact through provider MCP sync path.', 'Memory disable still tears down across all providers.']

## Effort & Risk

Mid. Depends on HM13. Main risk is mixing stale-provider prune with full uninstall semantics.

## Standards



## Notes

Agent should stop if provider enablement knowledge starts drifting into matrix rows or recipe classes.
