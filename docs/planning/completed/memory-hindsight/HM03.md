---
id: HM03
order: 3
plan: plan-memory-hindsight
state: completed
validate-first: true
priority: P0
complexity: complex
---

# Hindsight implementation strategy contract + resolution; drop wrappers

## Description

Define the Hindsight-implementation-owned declarative install-spec, recipe builder, and per-provider resolver under `components/memory/hindsight/`. Installers are DECLARED (data), not hand-coded per provider. Reuse generic toolchain primitives and generic provider recipe seams without adding Hindsight knowledge to `foundation/toolchains` or `components/providers`. Provider component code must stay Hindsight-agnostic.

## Steps

['Create contained package `src/audiagentic/components/memory/hindsight/` with `matrix.py`, `mcp_recipe.py`, and `recipes.py`. This package is the only place Hindsight may contain provider-specific integration facts.', 'Declarative spec/row lives in memory/hindsight, not provider descriptors. Fields include kind("native-installer"|"launch-wrapper"|"mcp-config"|"fallback-mcp"|"rules-only"), platforms, command tuples/strings, probes, config artifact metadata, source_url, source_checked_date, source_status("verified"|"unconfirmed"|"blocked").', 'Command placeholders resolved from HindsightBackend at build time: {mcp_url}{api_key}{bank_id}{base_url}. No provider descriptor stores project-specific URL/token values.', 'Builder lives in memory/hindsight/recipes.py and returns generic ProvisioningRecipe/ProviderRecipeResult-compatible objects. It may import only generic provider seams (`providers.services.recipes`) and provider descriptor discovery needed to locate harness config; it must not add Hindsight modules under providers.', 'Resolver precedence: verified native/launch-wrapper > mcp-config where provider has generic writer support > fallback-mcp for MCP-capable unsupported providers > rules-only. Platform gate drops unsupported strategies and re-resolves.', 'Source gate: builder refuses source_status!="verified" for native-installer/launch-wrapper commands. Unconfirmed paths may exist in matrix notes but must not be executable specs.', 'Remove flat `src/audiagentic/components/memory/hindsight_recipe.py`; do not create `src/audiagentic/components/providers/services/hindsight*.py`. Keep generic foundation/toolchains recipe primitives and LSP recipe code if boundary-clean.']

## Files

['src/audiagentic/components/memory/hindsight/matrix.py', 'src/audiagentic/components/memory/hindsight/mcp_recipe.py', 'src/audiagentic/components/memory/hindsight/recipes.py', 'src/audiagentic/components/providers/services/recipes.py (generic only; no Hindsight imports)', 'src/audiagentic/foundation/toolchains/recipe_steps.py (keep generic; no Hindsight/MCP/provider imports)', 'src/audiagentic/components/memory/hindsight_recipe.py (delete)']

## Validation

['test_resolver_precedence: native > mcp-config > fallback-mcp > rules-only as matrix dictates', 'test_platform_gate: cline spec (platforms=linux,darwin) on win resolves to fallback/rules-only as matrix dictates', 'test_source_gate: native/launch-wrapper specs with source_status!="verified" cannot execute commands', 'test_builder_mcp_config: Hindsight implementation recipe writes through generic config patch/provider target data only', 'Architecture gate: no `providers/services/hindsight*.py`; foundation/toolchains imports no components, MCP, providers, or Hindsight modules; memory core outside `memory/hindsight` imports no providers', 'Architecture gate: `memory/hindsight` provider imports are limited to controlled generic seams (`providers.services.recipes`, descriptor lookup)']

## Effort & Risk

Complex. DEPENDS ON HM01 (backend) + HM02 (remote entry). Blocks HM04/HM05/HM06/HM07. Risk: over-fitting the spec — validate it expresses all 5 strategy kinds from STRATEGY_MATRIX before wiring providers.

## Notes

Strategy resolution is authoritatively listed in docs/planning/active/memory-hindsight/STRATEGY_MATRIX.md. New strategy kinds vs original plan: launch-wrapper (aider) and explicit platform gating + openhands adapter prerequisite.
