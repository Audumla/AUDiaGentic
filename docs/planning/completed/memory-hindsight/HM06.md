---
id: HM06
order: 6
plan: memory-hindsight
state: completed
validate-first: true
priority: P2
work: M
---

# Rules-only fallback; remove legacy prompt contribution

## Description

Implement the Hindsight-implementation-owned rules-only strategy (no-MCP/no-native providers and unsupported-platform fallbacks) and remove the old memory prompt-contribution path so there is exactly one source of recall/retain guidance. Hindsight-specific rule text lives in `components/memory/hindsight/`; provider core stays generic.

## Steps

['Builder rules-only branch lives under components/memory/hindsight/recipes.py and uses apply_managed_block(rule_target=<provider surface instruction file>, block_id="hindsight-recall-retain", RULE_TEXT including backend.mcp_url) + register BlockChange for prune.', 'Define RULE_TEXT once in memory/hindsight code so mcp-config(+rule), fallback-mcp, and rules-only emit identical guidance.', 'Providers: plandex, local-openai, and any provider whose native strategy is platform-gated out with no MCP fallback (e.g. cline on win32). These provider mappings live only in memory/hindsight matrix.', 'Delete memory_api.build_memory_contributions and its call site in providers/surfaces/contributions.py. Do not keep shims or compatibility aliases; memory core must not produce provider surface text.', 'Provider surface regeneration must drop stale memory/<impl> contribution blocks as ordinary removed managed content. Do not add a memory-core migration path.', 'Ensure disabling memory / switching backend triggers prune through the contained memory/hindsight recipe lifecycle, not by memory_api importing providers.']

## Files

['src/audiagentic/components/memory/memory_api.py (remove old contribution API only)', 'src/audiagentic/components/providers/surfaces/contributions.py (remove memory special-case)', 'src/audiagentic/components/memory/hindsight/recipes.py']

## Validation

['test_rules_only_writes_block: non-MCP provider surface gets the recall/retain block referencing mcp_url', 'test_old_api_absent: build_memory_contributions no longer exists; contributions.py no longer special-cases memory', 'test_stale_memory_block_dropped_by_provider_regeneration: old memory/<impl> contribution block disappears without a memory-owned migration shim', 'test_block_pruned_on_disable: removing memory strips Hindsight-owned block via HM07 teardown', 'No double-write: only one hindsight block per surface', 'Architecture gate: no Hindsight modules under providers/services; memory core outside memory/hindsight contains no provider imports']

## Effort & Risk

Medium. DEPENDS ON HM03. Risk: removed memory contribution blocks must not remain in generated surfaces. User directive: no legacy/no shims; delete old API rather than wrapping it.

## Notes

User directive: no legacy/no shims. The rules-only strategy supersedes the old prompt contribution entirely.
