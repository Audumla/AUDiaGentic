---
id: HM07
order: 12
plan: memory-hindsight
state: completed
validate-first: true
priority: P1
work: L
---

# Lifecycle/status wiring after Hindsight cleanup

## Description

Wire contained Hindsight orchestration into memory lifecycle/status after HM10/HM11 cleanup. Memory core owns backend config and emits neutral refresh/change signals. `memory_observer.py` is the guaranteed consumer that invokes memory/hindsight apply/prune/teardown, so correctness never depends on a specific MCP management-tool caller.

## Steps

['Parent scope only. Implement via HM13 (observer-triggered lifecycle reconcile), HM14 (enabled-provider filtering + backend-switch reconcile), and HM15 (status/output extension).', 'Keep memory core provider-agnostic throughout; if new wiring needs provider knowledge, it belongs in memory/hindsight or lifecycle observer seam, not memory_api.', 'Do not expand recipe duplication here; HM10/HM11 already reduced lifecycle duplication and HM12 owns remaining structural cleanup.']

## Files

['src/audiagentic/components/memory/memory_api.py', 'src/audiagentic/components/memory/memory_observer.py', 'src/audiagentic/components/memory/hindsight/provision.py', 'src/audiagentic/components/memory/hindsight/recipes.py', 'tests/unit/memory/test_memory_component.py', 'tests/unit/memory/test_hindsight_provision.py']

## Validation

['test_memory_core_does_not_call_providers: memory_api has no provider imports/calls and only updates state + emits neutral hint.', 'test_direct_api_state_change_reconciles: state/config change through memory_api directly still reaches memory_observer and triggers Hindsight reconciliation.', 'test_provider_not_enabled_pruned: disabled providers lose Hindsight-owned artifacts while enabled providers stay configured.', 'test_disable_teardown: memory disable removes all owned Hindsight artifacts across providers via contained Hindsight reconciliation.', 'test_url_switch_reconfigures: backend URL/token/bank change updates every owned provider entry/connection file through HM10 sync path.', 'test_status_report: Hindsight per-provider status includes state/source_status/source_date/owned_artifacts and memory_status stays provider-agnostic.']

## Effort & Risk

Complex. Depends on HM10 and HM11. Main risk is under-specified refresh semantics; observer consumption is required, caller-driven reconciliation is optional only.

## Standards



## Notes

Incorporates RV47. Parent epic only. Execute as HM13 -> HM14 -> HM15 after HM10/HM11. HM13 locks observer-triggered reconcile, HM14 covers enabled-provider pruning + backend-switch rewrites, HM15 extends existing status plumbing without leaking provider detail into memory core.
