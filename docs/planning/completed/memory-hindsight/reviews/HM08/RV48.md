---
id: RV48
review-of: HM08
plan: plan-memory-hindsight
state: closed
reviewed-by: claude-fable-5
reviewed-at: ''
---

# Review: authoritative per-provider strategy matrix (completed)

## Notes

Freshness check is cheap: a unit test asserting source_date within N days would do, with an explicit override list for stable rows. Schema migration details are duplicated into RV50/RV52 deliberately — implementers of those items need not read this review.

## Findings

1) DELIVERED AS SPECIFIED: `config/components/memory/hindsight_matrix.yaml` exists and is loaded by `memory/hindsight/matrix.py` (134 lines, imports only the generic provider recipes seam). STRATEGY_MATRIX.md and toolchain-provisioning INTEGRATION_MATRIX/TO10-12 were actually deleted, so the 'supersedes' claims were followed through — rare and good.
2) RESIDUAL RISK — EVIDENCE FRESHNESS: source_checked_date guards against guessing at authoring time but nothing re-checks staleness; a row verified months ago silently stays 'verified' after upstream Hindsight docs change an install command. Owned follow-ups: surface source_date in HM07's status output (RV47 finding 5) and add a freshness assertion in HM09's gates.
3) MINOR: completed items HM03/HM04 still point at the deleted STRATEGY_MATRIX.md as authority; HM08's notes correctly declare it superseded. Historical confusion risk only.
4) SCHEMA MIGRATIONS OWNED BY PENDING ITEMS — the YAML matrix will change shape three times during HM10/HM12 execution; recording them here so the data changes are not improvised: (a) HM10/RV50 finding 5: `status_command` placeholders become brace-delimited (`{URL}`, `{TOKEN}`, `{KEY}`, `{ID}`, `{MCP_URL}`) — bare-token values are a corruption bug; (b) HM12/RV52 finding 4: `platform_constraints` values normalize to exactly `darwin` / `linux` / `win` (the canonical platform_key() values), replacing free-text like 'macOS/Linux'; (c) HM12/RV52 finding 3: the claude row gains `plugin_repair_glob` and `plugin_data_dir` fields so the Windows plugin-repair paths move out of Python into config. Each migration must update YAML + loader + consuming code in one change (Standards §10 atomic migration).
5) LOADER SIMPLIFICATION (execute with HM10): `_RECIPE_KIND_MAP` in matrix.py duplicates the ProviderRecipeKind str-Enum values — RV50 finding 6 directs its deletion. The field-by-field `entry.get(...)` block in `_load_matrix` is acceptable as-is (explicit beats clever for a lesser agent); do not rewrite it with dataclass introspection.

## Conclusion

Solid keystone item; the config-over-code matrix with per-row provenance is the right shape. The matrix YAML schema is not frozen: three specific migrations land with HM10/HM12 (finding 4) and the freshness gap (finding 2) is discharged via HM07 status + HM09 gates. No standalone work remains under HM08 itself.
