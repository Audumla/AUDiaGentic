---
id: RV156
review-of: HM21
plan: plan-memory-hindsight
state: closed
reviewed-by: claude
reviewed-at: ''
---

# User direction: module split back in scope — execute without facade re-exports, migrating all importers

## Notes



## Findings

RV155 descoped the recipes.py module split, objecting specifically to the facade re-export variant (added surface). User review 2026-07-07: the monolith itself is the problem — recipes.py (~1400 lines) still mixes recipe classes, strategy resolution + builders, apply/teardown/prune orchestration, and status aggregation. Resolution: split WITHOUT any facade, migrating every importer in the same change (§10 atomic migration): recipes.py keeps the recipe classes and their direct helpers; strategies.py takes resolution/fallback/builders/factory registry + descriptor path resolvers; lifecycle.py takes register/_reconcile/apply/teardown/prune; status.py takes HindsightStatusState + build_hindsight_status. Importers to migrate: provision.py (2 sites), unit tests (strategy/recipe/managed-sync incl. one patch-target string), integration/e2e tests (4 files).

## Conclusion

Execute the no-facade split; update HM21 notes on completion.
