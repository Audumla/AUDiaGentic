---
id: RV47
review-of: HM07
plan: plan-memory-hindsight
state: closed
reviewed-by: claude-fable-5
reviewed-at: ''
---

# Review: contained orchestration/lifecycle wiring (pending)

## Notes

HM09's global-teardown and backend-switch tests depend on this item — see RV49. Line references verified 2026-07-03; locate by symbol name after HM10/HM11 shift the file.

## Findings

1) SEQUENCING NOT ENFORCEABLE FROM METADATA: effort_risk says HM07 depends on HM10 and HM11, but all three are P1 and the dependency is prose-only. Anyone picking work by priority alone can start HM07 first and re-create the duplication HM10/HM11 remove. Execution order is HM10 → HM11 → HM07; make it explicit in metadata or the item title.
2) GOOD NARROWING: the post-de-dup-review scope cut (lifecycle wiring + status only; MCP sync → HM10, recipe lifecycle → HM11) is exactly right and the 'do not expand recipes.py here' guard is the most important line in the item.
3) DESIGN DECISION REQUIRED BEFORE CODING — HINT SEMANTICS: the 'neutral refresh-needed hint' seam is under-specified — is it a return value the MCP tool caller must act on, or an observer event? If caller-driven, a config change via direct API (not the management tool) silently never reconciles. DIRECTIVE for the implementer: the observer (memory_observer.py) must be the guaranteed consumer — memory core emits the hint unconditionally on state change; the observer invokes memory/hindsight apply/prune/teardown. A management-tool caller may ALSO trigger reconciliation synchronously for immediate feedback, but correctness must never depend on which entry point was used. Write the architecture test for this (state change via memory_api directly → reconciliation still fires).
4) BOUNDARY BASELINE ALREADY GREEN: verified 2026-07-03 that memory core outside memory/hindsight has no provider imports — write the architecture-gate tests FIRST (cheap) to lock the current good state before wiring.
5) REUSE EXISTING STATUS PLUMBING — DO NOT ADD NEW: `build_hindsight_status` (recipes.py:1542-1583) and `ProviderRecipeRegistry.status` already produce per-provider {kind, state, status, action_needed, source_url}. DIRECTIVE: the HM07 status step extends `build_hindsight_status` output (add `source_status` and `owned_artifacts` from the artifact registry, and surface `source_date` so stale verification is visible per RV48 finding 2) — it must NOT introduce a second status assembly path. memory_status in memory core reports only backend/config health + the neutral refresh-needed flag; it never imports or embeds Hindsight per-provider detail.
6) PROVIDER FILTERING SEAM EXISTS: `apply_hindsight`/`teardown_hindsight`/`prune_hindsight` already accept `provider_ids`. HM07's 'installed+enabled providers only' filtering is therefore a matter of computing the enabled-provider set in the orchestration seam and passing it through — do not push enabled/disabled knowledge down into recipes or the matrix.

## Conclusion

Well-scoped after narrowing. Binding decisions for the implementer, in order: (a) execute only after HM10 and HM11 land; (b) observer is the guaranteed hint consumer — write the direct-API-change architecture test first; (c) extend build_hindsight_status rather than adding any new status path; (d) provider filtering happens in the orchestration seam via the existing provider_ids parameter.
