---
id: RV80
review-of: HM07
plan: plan-memory-hindsight
state: closed
reviewed-by: ''
reviewed-at: ''
---

# RV77/RV78 resolution: observer spy, status exposure, placeholder consolidation

## Notes

All 105 memory tests pass. Architecture gate test_memory_api_no_provider_imports remains green after moving status report logic into hindsight/provision.py.

## Findings

RV77 Finding 1 — Observer path unproven: FIXED by installing memory component before test (so is_installed passes), monkeypatching provision.reconcile_hindsight before config change, calling bus.wait_idle() to flush async events, and asserting spy was called.

RV77 Finding 2 — Status helper unexposed: FIXED by adding memory_hindsight_status() API function and MCP tool. Provider knowledge contained in hindsight.provision.build_hindsight_status_report(); memory_api delegates without importing providers/services directly (architecture gate preserved).

RV78 — Duplicate placeholder path: FIXED by refactoring _parameterize_command to use _hindsight_params for consistent substitution via shared parameter mapping.

## Conclusion

Both findings resolved: observer path proven via spy + wait_idle, per-provider status exposed via memory_hindsight_status API/MCP tool, and _parameterize_command consolidated onto _hindsight_params.
