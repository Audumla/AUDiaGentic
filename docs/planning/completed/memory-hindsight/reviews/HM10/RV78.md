---
id: RV78
review-of: HM10
plan: plan-memory-hindsight
state: closed
reviewed-by: Codex
reviewed-at: ''
---

# Code review: duplicate placeholder path still survives HM10 cleanup

## Notes

Current tree no longer has the old bare-token replacement bug, but the cleanup item's de-duplication goal remains incomplete.

## Findings

["`src/audiagentic/components/memory/hindsight/recipes.py` still defines and uses `_parameterize_command()` for `status_command` probing, so HM10's intended consolidation onto one placeholder system did not fully land.", 'Because `_parameterize_command()` remains a second substitution path beside `_hindsight_params()`/step substitution, future matrix changes can drift between probe commands and provision steps even if current brace-delimited replacement avoids the old bare-token corruption bug.']

## Conclusion

Reopen HM10 follow-up or create a child cleanup item. Route status-command substitution through the shared parameter source and remove `_parameterize_command()` for real.
