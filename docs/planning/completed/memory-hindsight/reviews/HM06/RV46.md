---
id: RV46
review-of: HM06
plan: plan-memory-hindsight
state: closed
reviewed-by: claude-fable-5
reviewed-at: ''
---

# Review: rules-only fallback, legacy prompt contribution removed (completed)

## Notes

No action needed beyond landing HM07.

## Findings

1) VERIFIED CLEAN: `build_memory_contributions` has zero occurrences in src/ — the no-shim deletion directive was followed, and the single-source-of-guidance goal (one RULE_TEXT) is structurally enforced by living in memory/hindsight.
2) SCOPE DISCIPLINE: correctly declined to add a memory-core migration path for stale contribution blocks, relying on ordinary managed-block regeneration — this is the cheapest correct design and avoided a shim.
3) MINOR: validation item 'test_block_pruned_on_disable' delegates to 'HM07 teardown', but HM07 is still pending — same completed-item-depends-on-pending-validation pattern as HM04. Until HM07 lands, disable-driven prune of rules-only blocks is asserted by design, not by test.

## Conclusion

Cleanest item in the plan: tight scope, verified deletion, no compatibility residue. Only open edge is disable-triggered prune coverage, which correctly belongs to HM07/HM09.
