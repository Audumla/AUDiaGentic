---
id: RV42
review-of: HM02
plan: plan-memory-hindsight
state: closed
reviewed-by: claude-fable-5
reviewed-at: ''
---

# Review: remote MCP server entry support (completed)

## Notes

Pattern to watch: 'confirm/verify X' steps in HM items completing without a recorded answer (also present in HM05 copilot sub-task).

## Findings

1) INTERNAL INCONSISTENCY: the description declares codex and opencode adapters out of scope ("native installers, not mcp-config"), yet the files list includes `providers/adapters/codex/` and `providers/adapters/opencode/`. A completed item whose scope statement and files list disagree makes it impossible to audit what was actually delivered under this ID.
2) GOOD: keeping new McpServerEntry fields optional+defaulted (url, headers, transport) with `is_remote` derived was the right backward-compatible move; stdio consumers untouched.
3) OPEN QUESTION LEFT UNRESOLVED IN A COMPLETED ITEM: step 3 says "confirm nested container key" for .gemini/settings.json — there is no recorded confirmation outcome. Container-key correctness now rests implicitly on HM05/HM10 tests.

## Conclusion

Foundation change was well-designed and low-risk by construction. Item hygiene issue: scope/files contradiction and an unclosed confirmation step should not survive into a completed record — future completed items should record the resolution of every 'confirm X' step.
