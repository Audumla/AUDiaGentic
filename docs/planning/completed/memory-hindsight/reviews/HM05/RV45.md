---
id: RV45
review-of: HM05
plan: plan-memory-hindsight
state: closed
reviewed-by: claude-fable-5
reviewed-at: ''
---

# Review: mcp-config + recall/retain rule strategy (completed)

## Notes

The copilot question is a correctness risk, not hygiene: writing the entry to a file Copilot never reads passes all file-content tests and still delivers nothing.

## Findings

1) UNRESOLVED GAPS BURIED IN A COMPLETED ITEM: notes record two open questions with no owner — (a) copilot: does the running Copilot read `.vscode/mcp.json` (Hindsight's documented target) or the adapter-written `.mcp.json`? (b) gemini Cloud requires an OAuth proxy (only self-hosted is direct). Neither is tracked by any pending HM item; HM09's test plan will exercise whatever was written without answering (a). These should be promoted to explicit follow-up items or folded into HM09 validation, otherwise they are silently lost.
2) GOOD: single RULE_TEXT source shared across mcp-config(+rule), fallback-mcp, and rules-only (with HM06) prevents guidance drift — correct call.
3) DELIVERY VS FILES: `providers/services/mcp.py` gained `sync_managed_provider_mcp_subset` (verified present), but HM10 shows the Hindsight recipe still bypasses it via HindsightTarget writer/reader/remover callbacks — i.e. this item built the generic seam AND a parallel private path. The de-dup was deferred rather than avoided.

## Conclusion

Functionally complete but left two provider-correctness questions unowned (copilot config target, gemini OAuth) and a duplicated write path that HM10 must now collapse. Recommend adding the copilot/gemini questions to HM09's validation or a new HM item before live testing.
