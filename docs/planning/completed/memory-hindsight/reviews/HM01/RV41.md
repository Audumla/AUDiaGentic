---
id: RV41
review-of: HM01
plan: memory-hindsight
state: closed
reviewed-by: claude-fable-5
reviewed-at: ''
---

# Review: backend config + export contract (completed)

## Notes

Verified against current tree on 2026-07-03: HindsightBackendConfig found only under memory/hindsight/.

## Findings

1) STALE RECORD: the item's deliverable file `src/audiagentic/components/memory/hindsight_export.py` no longer exists — the contract now lives in `src/audiagentic/components/memory/hindsight/export.py`. The item was never updated after the move, so its files/validation lists point at dead paths.
2) SPEC DRIFT: options-schema transport is specified as enum http|sse, but the shipped implementation also supports stdio transport (`build_hindsight_entry` in mcp_recipe.py branches on transport=="stdio" and emits a `hindsight-mcp` command entry). The stdio strategy was never added to this contract item, so the authoritative options-schema description understates the real surface.
3) SOUND: the core design decision — memory core owns backend identity only, no provider names/paths — held up in implementation; grep confirms no provider imports in memory core outside memory/hindsight.

## Conclusion

Design validated by implementation; the item record itself is stale (moved file path, missing stdio transport). Low-value to rewrite a completed item, but the stdio transport gap should be reflected wherever the options-schema is documented.
