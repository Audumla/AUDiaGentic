---
id: HM12
order: 13
plan: plan-memory-hindsight
state: completed
validate-first: true
priority: P2
work: L
---

# Split Hindsight recipe strategies by concern

## Description

Last structural cleanup after HM10/HM11. Split `recipes.py` only as far as post-cleanup size/responsibilities justify, and replace remaining standards violations: kind if/elif dispatch, hardcoded plugin repair paths, free-text platform constraints, brittle config path resolution, and missing logging/error handling.

## Steps

['Parent scope only. Implement via HM16 (matrix-driven dispatch + config loading cleanup) and HM17 (plugin-repair metadata + recipe hardening).', 'Do not start with file shuffling. Land behavior-preserving cleanup gates first, then split modules only if responsibility boundaries still warrant it.', 'Keep matrix/base modules dependency-light and avoid introducing a second config-loading pattern.']

## Files

['src/audiagentic/components/memory/hindsight/recipes.py', 'src/audiagentic/components/memory/hindsight/matrix.py', 'src/audiagentic/config/components/memory/hindsight_matrix.yaml', 'src/audiagentic/components/memory/hindsight/mcp_recipe.py', 'tests/unit/memory/test_hindsight_recipe.py', 'tests/unit/memory/test_hindsight_strategy.py']

## Validation

['Existing memory hindsight tests pass with updated imports.', 'Factory coverage test proves all matrix kinds dispatch through registry or intentional fallback.', "Platform gate test: row with ['darwin','linux'] gates out on win.", 'Plugin repair config test: empty repair fields no-op; claude row fields drive path patterns without hardcoded Python paths.', 'Logger/error test: command parsing failures return canonical error and external probe exceptions log with exc_info.', 'Config path resolution uses same mechanism as other component config loaders.', 'Module size/ownership review: no new module exceeds one major responsibility.']

## Effort & Risk

Complex but last. Depends on HM10 and HM11. Do not start as pure file shuffle while duplicate MCP/lifecycle logic remains. Main risk: import cycles; keep matrix types and base metadata low-level.

## Standards



## Notes

Incorporates RV52 and RV48 schema-migration notes. Parent epic only. Execute as HM16 -> HM17 after HM10/HM11 and preferably after HM07 churn settles. HM16 covers factory registry/platform/config-loader cleanup; HM17 covers plugin-repair metadata, logging/error normalization, and optional file split if still justified.
