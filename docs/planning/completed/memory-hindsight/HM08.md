---
id: HM08
order: 8
plan: plan-memory-hindsight
state: completed
validate-first: true
priority: P1
complexity: simple
---

# Authoritative per-provider strategy matrix

## Description

Authoritative per-provider strategy resolution lives in `src/audiagentic/config/components/memory/hindsight_matrix.yaml` and is loaded by `components/memory/hindsight/matrix.py`. It must not be copied into provider descriptors. The matrix is not allowed to imply confidence without source evidence: every native/launch-wrapper command/path needs source_url, source_checked_date, source_status, and exact official command text before HM04 can execute it.

## Steps

['YAML matrix exists with provider rows -> resolved strategy + gaps. Each row has source_url, source_date, source_status(verified|unconfirmed|blocked|no_hindsight), and notes. Do not use generic "verified" unless exact docs/script evidence is captured.', 'Do NOT write HindsightSpec literals into provider descriptors. Hindsight-specific facts remain in `config/components/memory/hindsight_matrix.yaml` and `components/memory/hindsight/`.', 'Unconfirmed/blocked entries must remain non-executable. Native/launch-wrapper source gaps block HM04 implementation for that provider. MCP-config path gaps block HM05 for that provider.', 'OpenHands is intentionally blocked in the AUDiaGentic matrix until a generic OpenHands mcp_config writer for config.toml or structured Hindsight installer steps exist. Official docs alone do not make AUDiaGentic automation supported.']

## Files

['src/audiagentic/config/components/memory/hindsight_matrix.yaml', 'src/audiagentic/components/memory/hindsight/matrix.py']

## Validation

['Every provider has exactly one resolved strategy in the YAML matrix', 'Every native/launch-wrapper command is quoted verbatim from official docs/script notes with source_url + source_date', 'No executable command runs for source_status!="verified"', 'Provider descriptors contain no Hindsight-specific fields or command literals', 'Each gap has an owner item (openhands->HM05 prereq, source/command gaps->HM04 source audit)']

## Effort & Risk

Simple/completed with caution. YAML matrix exists and supersedes the old strategy markdown. Some provider rows may remain blocked until generic descriptor seams or structured installer support exist.

## Notes

Canonical deliverable is `src/audiagentic/config/components/memory/hindsight_matrix.yaml`. If `docs/planning/active/memory-hindsight/STRATEGY_MATRIX.md` still exists, treat it as historical/superseded notes only. Do not move Hindsight source facts into provider descriptors. Supersedes toolchain-provisioning/INTEGRATION_MATRIX.md.
