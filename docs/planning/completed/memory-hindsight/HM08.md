---
id: HM08
order: 8
plan: plan-memory-hindsight
state: completed
validate-first: true
priority: P1
complexity: simple
---

# Authoritative per-provider strategy matrix

## Description

Authoritative per-provider strategy resolution, materialized as STRATEGY_MATRIX.md and then as concrete HindsightSpec values in provider descriptors. The matrix is not allowed to imply confidence without source evidence: every native/launch-wrapper command/path needs source_url, source_checked_date, source_status, and exact official command text before HM04 can execute it.

## Steps

['STRATEGY_MATRIX.md exists with all 15 providers -> resolved strategy + gaps, but must be revised so each row has source_url, source_checked_date, source_status(verified|unconfirmed|blocked), and source_notes. Do not use generic "verified" unless exact docs/script evidence is captured.', 'For each provider, write descriptor HindsightSpec literal (or rely on auto-derivation) consistent with matrix: native-installer (claude,codex,opencode,cline), launch-wrapper (aider), mcp-config+rule (copilot,openhands,continue_,roo,gemini), fallback-mcp (goose,pi,qwen), rules-only (plandex,local_openai).', 'Unconfirmed/blocked entries must remain non-executable. Native/launch-wrapper source gaps block HM04 implementation for that provider. MCP-config path gaps block HM05 for that provider.', 'Remaining source confirmations to close: claude exact marketplace/plugin commands; opencode exact plugin install/uninstall; aider wrapper/package/config; copilot config path (.vscode/mcp.json vs .mcp.json); pi/qwen MCP behavior; gemini Cloud OAuth-proxy path; codex codex.json URL/base/MCP precedence and exact installer flags.']

## Files

['docs/planning/active/memory-hindsight/STRATEGY_MATRIX.md']

## Validation

['Every provider has exactly one resolved strategy (matrix + descriptor agree)', 'Every native/launch-wrapper command is quoted verbatim from official docs/script notes with source_url + source_checked_date', 'No descriptor executable command exists for source_status!="verified"', 'Each gap has an owner item (openhands->HM05 prereq, aider->HM03 launch-wrapper, cline-win->HM06, unverified commands->HM04 source audit)']

## Effort & Risk

Simple/in-progress. Matrix doc exists but needs confidence/source hardening before descriptor transcription. Supersedes toolchain-provisioning/INTEGRATION_MATRIX.md.

## Notes

DELIVERABLE EXISTS: docs/planning/active/memory-hindsight/STRATEGY_MATRIX.md. Update it before HM04/HM05 consume it: remove over-broad "verified" language, attach exact official source evidence, and mark unknowns unconfirmed/blocked. Supersedes toolchain-provisioning/INTEGRATION_MATRIX.md.
