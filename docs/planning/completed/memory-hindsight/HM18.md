---
id: HM18
order: 141
plan: plan-memory-hindsight
state: completed
validate-first: true
priority: P1
work: M
---

# HM09 chunk A: offline gates, freshness, and provider-correctness proofs

## Description

First HM09 chunk. Build deterministic non-network coverage first: architecture/source gates, env-driven live fixture behavior, matrix freshness checks, and explicit provider-correctness decisions for Copilot/Gemini before broad live/Docker execution.

## Steps

['Add architecture/source gate tests that always run even when live URL is unset or unreachable.', 'Add live-fixture config that reads `HINDSIGHT_TEST_MCP_URL` from env or non-committed local config only; no committed local IP defaults.', 'Add freshness assertion over verified matrix rows with allowlist support for intentionally stable rows.', 'Resolve/document Copilot target-file behavior and Gemini OAuth-proxy status in validation so later live tests assert the right thing instead of merely checking file writes.']

## Files

['tests/integration/memory/test_hindsight_live.py', 'src/audiagentic/config/components/memory/hindsight_matrix.yaml', 'tests/unit/foundation/toolchains/test_architecture_boundaries.py']

## Validation

['Architecture/source gates pass regardless of live-server availability.', 'Live fixture skips only network-dependent handshake when URL unset/unreachable.', 'Freshness gate flags stale verified `source_date` values unless allowlisted.', 'Copilot target-file and Gemini OAuth status are tested/documented before claiming integration works.']

## Effort & Risk

Mid. Depends on HM15 and recommends HM16/HM17 first. Main risk is silently asserting wrong provider behavior in later e2e tests.

## Standards



## Notes

This chunk should land before any Docker/native-installer execution work.
