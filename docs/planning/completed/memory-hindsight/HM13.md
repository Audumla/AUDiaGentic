---
id: HM13
order: 121
plan: plan-memory-hindsight
state: completed
validate-first: true
priority: P1
work: M
---

# HM07 chunk A: observer-triggered lifecycle reconcile

## Description

First HM07 execution chunk. Lock the lifecycle contract: memory core stays provider-agnostic, emits neutral refresh/change hints, and direct state/config changes still reach the memory observer reconciler without requiring a specific management caller.

## Steps

['Write/adjust tests first for direct memory-api config/state changes and component enable/disable events reaching the observer path.', 'Keep provider imports out of memory core; if more wiring is needed, put it in `memory_observer.py` or runtime lifecycle seam, not `memory_api.py`.', 'Ensure management tools may reconcile synchronously for UX, but correctness still comes from observer-driven reconcile.', 'Leave per-provider status expansion for later HM07 chunk; this chunk is lifecycle trigger only.']

## Files

['src/audiagentic/components/memory/memory_api.py', 'src/audiagentic/components/memory/memory_observer.py', 'tests/unit/memory/test_memory_component.py', 'tests/unit/memory/test_hindsight_provision.py']

## Validation

['Direct `memory_set_config`/implementation changes return neutral refresh hint only.', 'Observer/reconciler path runs after direct API changes and component lifecycle events.', 'Architecture gate remains green: memory core outside `memory/hindsight` has no provider imports/calls.']

## Effort & Risk

Mid. Execute first within HM07. Main risk is accidental provider logic leakage into memory core while adding trigger coverage.

## Standards



## Notes

Derived from HM07/RV47. Sequence: HM10 -> HM11 -> HM13 -> HM14 -> HM15.
