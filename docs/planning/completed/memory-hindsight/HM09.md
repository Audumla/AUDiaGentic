---
id: HM09
order: 14
plan: plan-memory-hindsight
state: completed
validate-first: true
priority: P1
complexity: complex
---

# End-to-end tests against live Hindsight + Docker for installers

## Description

End-to-end coverage for Hindsight after MCP config, recipe lifecycle, lifecycle wiring, and structural cleanup are complete. Prove provider setup/teardown against an optional live Hindsight server and Docker-isolated native installers without hiding architecture/source-gate failures.

## Steps

['Parent scope only. Implement via HM18 (offline gates, freshness, provider-correctness proofs) and HM19 (live + Docker e2e reconcile coverage).', 'Never let live-server unavailability hide architecture/source/freshness failures; those always run first.', 'Docker/native-installer execution must remain source-verified and isolated; blocked/unconfirmed rows assert guidance instead of running host-mutating commands.']

## Files

['tests/integration/memory/test_hindsight_live.py', 'tests/e2e/memory/test_hindsight_provider_setup_docker.py', 'src/audiagentic/config/components/memory/hindsight_matrix.yaml']

## Validation

['Architecture/source gates pass even when live server URL is unset or unreachable.', 'No committed local IP literal/default for Hindsight test server; env/non-committed config only.', 'Live MCP initialize handshake returns capabilities when HINDSIGHT_TEST_MCP_URL is reachable.', 'Copilot target-file decision is tested/documented; Gemini Cloud OAuth path is either source-verified or explicitly blocked/guidance-only.', 'Each provider config/hooks/rules match expected source-verified mechanism after HM10/HM11/HM07 cleanup paths.', 'Unconfirmed/blocked native/launch-wrapper specs report blocked and are not executed.', 'Matrix freshness assertion flags stale verified source_date values unless allowlisted.', 'Full uninstall leaves zero AUDiaGentic-owned Hindsight artifacts by sweep.', 'Env-changing installer tests run in Docker only.']

## Effort & Risk

Complex. Depends on HM10, HM11, HM07; HM12 recommended before broad e2e to reduce import/path churn. Main risk is expensive/flaky live testing, so split architecture/source/freshness gates from network checks.

## Standards



## Notes

Incorporates RV49 plus RV45/RV48 correctness/freshness gaps. Parent epic only. Execute as HM18 -> HM19 after HM13 -> HM14 -> HM15 and preferably HM16 -> HM17. HM18 is deterministic offline gating/correctness work; HM19 is optional live/Docker execution coverage.
