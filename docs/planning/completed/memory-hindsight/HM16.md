---
id: HM16
order: 131
plan: plan-memory-hindsight
state: completed
validate-first: true
priority: P2
work: M
---

# HM12 chunk A: matrix-driven dispatch and config loading cleanup

## Description

First HM12 cleanup chunk. Replace `build_hindsight_recipe` kind branching with factory registry, normalize matrix platform keys, and move matrix loading onto the repo’s standard config-resource mechanism.

## Steps

['Inventory current `recipes.py` responsibilities/tests before edits; do not split files yet.', 'Introduce `_RECIPE_FACTORIES` keyed by `ProviderRecipeKind` and add coverage proving every matrix kind dispatches through registry or intentional fallback.', 'Normalize `platform_constraints` to canonical `darwin`/`linux`/`win` keys and simplify platform gate logic.', 'Replace hardcoded config path resolution in `matrix.py` with existing config-resource loader pattern used elsewhere in repo.']

## Files

['src/audiagentic/components/memory/hindsight/recipes.py', 'src/audiagentic/components/memory/hindsight/matrix.py', 'src/audiagentic/config/components/memory/hindsight_matrix.yaml', 'tests/unit/memory/test_hindsight_recipe.py', 'tests/unit/memory/test_hindsight_strategy.py']

## Validation

['Factory coverage test passes for all matrix kinds.', 'Platform gate test uses canonical keys and remains green.', 'Matrix config loads through same mechanism as other component config resources.']

## Effort & Risk

Mid. Execute before any physical module split. Main risk is import-cycle creation if matrix loader starts depending on orchestration code.

## Standards



## Notes

Leaves plugin repair, logging/error, and optional file split to next chunk.
