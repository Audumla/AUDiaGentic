---
id: HM11
order: 11
plan: memory-hindsight
state: completed
validate-first: true
priority: P1
work: L
---

# Collapse Hindsight recipe lifecycle duplication

## Description

Second cleanup item after HM10. Collapse Hindsight-local provider recipe lifecycle duplication onto `ProviderCapabilityRecipe`, `ProviderRecipeRegistry`, and foundation `ProvisioningRecipe`, while keeping Hindsight-specific provenance/action metadata in one Hindsight base adapter.

## Steps

["Make `_RowRecipe` subclass `ProviderCapabilityRecipe`; call super with provider_id, capability_id='hindsight', recipe_kind, display_name, source_url, source_date; keep only Hindsight row metadata locally.", 'Replace `_stamp` with one Hindsight-base `to_result` override that preserves source/action-needed metadata. Route all result conversions through it; delete redundant `RulesOnlyRecipe.to_result`.', 'Delete local `_run_provision` and `_run_teardown`; `apply_hindsight` calls `ProviderRecipeRegistry.install`, `teardown_hindsight` calls `ProviderRecipeRegistry.uninstall`. Any missing action-needed behavior moves into the shared conversion path.', 'Extract one private `_reconcile(project_root, op, *, backend, provider_ids, context)` helper so apply/teardown/prune do not triplicate registry construction + provider filtering.', 'Centralize repeated source-gate/step execution into one Hindsight-base helper for install/uninstall step definitions. Preserve semantic difference: unverified install fails; unverified uninstall returns OK/ABSENT because nothing was installed.', 'Merge `_PluginUrlConfigRecipe` into `PluginConfigRecipe` with optional URL config path and optional MCP target; delete `_PluginUrlConfigRecipe`.', 'Route `GuidanceOnlyRecipe` and `RulesOnlyRecipe` results through shared stamped ok/fail helpers so provenance is set once.', 'Add `_CompositeRecipe._combine(...)` helper for paired MCP/rules results; remove six repeated merge blocks.', 'Extend generic provider recipe contract if needed rather than keeping Hindsight-local orchestration copies.']

## Files

['src/audiagentic/components/memory/hindsight/recipes.py', 'src/audiagentic/components/providers/services/recipes.py', 'src/audiagentic/foundation/toolchains/recipe_contract.py', 'tests/unit/memory/test_hindsight_recipe.py', 'tests/unit/providers/test_recipe_lifecycle.py']

## Validation

['Unit tests prove apply/teardown still provision selected providers and preserve source/action metadata.', 'Unit tests cover failed install/configure/verify with artifact ownership retained.', 'Deletion-proof test: `_run_provision`, `_run_teardown`, `_stamp`, and `_PluginUrlConfigRecipe` no longer exist in module namespace.', 'Architecture test: no Hindsight imports added to providers core beyond generic contracts.', 'No behavior regression in existing provider recipe lifecycle tests.', 'recipes.py line count drops materially; if still near monolith size, document remaining responsibilities for HM12.']

## Effort & Risk

Complex. Depends on HM10. Main risk: `ProviderRecipeResult` carries source/action-needed metadata not present in foundation `RecipeResult`; centralize conversion before deleting local flows.

## Standards



## Notes

Incorporates RV51. Work sequence: base unification -> registry lifecycle -> reconcile helper -> step helper -> plugin merge -> result stamping -> composite helper -> deletion-proof tests.
