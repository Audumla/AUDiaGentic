---
id: HM19
order: 142
plan: plan-memory-hindsight
state: completed
validate-first: true
priority: P1
complexity: complex
---

# HM09 chunk B: live and Docker e2e reconcile coverage

## Description

Second HM09 chunk. Add optional live MCP handshake coverage plus Docker-isolated provider setup/teardown/install tests, including backend-switch and full-disable teardown sweeps after HM07 lifecycle wiring is complete.

## Steps

['Add optional live MCP initialize handshake test gated by reachable `HINDSIGHT_TEST_MCP_URL`.', 'Add provider config/rules assertions proving MCP-config and rules providers write/remove only AUDiaGentic-owned artifacts and preserve user content.', 'Run native-installer/launch-wrapper cases in Docker only, and only for matrix rows with verified source status; blocked/unconfirmed rows assert blocked/guidance instead of executing commands.', 'Add backend-switch and global-disable teardown sweeps covering every provider artifact class.', 'Explicitly discharge HM04 Docker installer proof and HM06 disable-prune proof here.']

## Files

['tests/integration/memory/test_hindsight_live.py', 'tests/e2e/memory/test_hindsight_provider_setup_docker.py', 'tests/unit/memory/test_hindsight_provision.py']

## Validation

['Reachable live MCP server returns initialize capabilities.', 'Verified provider config/hooks/rules are proven end-to-end; blocked/unconfirmed installer rows are not executed.', 'Backend switch updates all owned artifacts to new base URL.', 'Disable/uninstall sweep leaves zero AUDiaGentic-owned Hindsight artifacts across providers.', 'Env-changing installer tests run in Docker only.']

## Effort & Risk

Complex. Depends on HM18 and should follow HM12 cleanup to reduce churn. Main risk is flakiness and host mutation if Docker/source gates are bypassed.

## Standards



## Notes

Completes HM09. Execution order: HM13 -> HM14 -> HM15 -> HM16 -> HM17 -> HM18 -> HM19.
