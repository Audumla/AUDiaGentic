---
id: MO09
order: 10
plan: plan-model-endpoint-management
state: completed
validate-first: true
priority: P1
complexity: mid
created-by: claude
completed-at: '2026-07-13T21:10:00Z'
---

# Deep-dive verification: agent provider × vendor support and key-injection matrix

## Description

Reopened evidence-correction lane. Produce sanitized, versioned, reproducible provider×vendor capability evidence and repair known-false Pi/Qwen classifications plus over-claimed Codex/OpenCode/OpenHands facts. No production code, provider declarations, credential mutation, installation, or guessed fact. Every defined P1 fact ends verified, unsupported, or blocked with exact reason and fact-specific evidence.

## Steps

0. READ/SAFETY: read Architecture Standards §§1.1, 8, 15; MO09; canonical matrix; evidence folder; provider descriptors/adapters. Never print/copy secret values, auth headers, raw credential config, or full CLI dumps. Record key names only.
1. Build explicit P1 acceptance table for providers `opencode`, `pi`, `openhands`, `codex`, `qwen` × vendors OpenAI/Anthropic/Google/OpenRouter. Give every fact an ID: support mode, credential mechanism, base-URL override, model granularity, config path/container, reload behavior. Track status as `verified`, `unsupported`, `blocked`, or `unverified`; never collapse expected into verified.
2. REOPEN OPENCODE: retain facts directly proved by v1.17.18 probes. Downgrade/reword unsupported conclusions not proved by evidence:
   a. OpenAI env injection stays unverified until tested; existing OAuth availability does not prove env rejection.
   b. Anthropic/Google login presence may be verified, but model granularity stays blocked/unverified without authenticated catalog or authoritative version-matched source.
   c. OpenRouter absence from `providers list` is only a probe observation; unsupported requires authoritative capability inventory or negative config test.
   d. Resolve singular `provider` vs plural `providers` and config filename precedence with a sanitized minimal config probe or version-matched upstream source. Remove conflicting statements.
3. Run mutating/config-sensitive probes only in Docker or isolated disposable HOME/XDG/config roots. Record isolation method. Do not run login against user profile, install missing tools, or use real credentials. Dummy-token probes may establish parsing/config recognition, never successful authentication.
4. Evidence schema: one stable anchor per fact or tightly coupled fact group. Record provider-id, upstream-id, exact tool version, verified-at UTC, evidence kind, sanitized command/input shape, exit/result classification, source URL/version or installed artifact path, conclusion, limitations, and support state. Do not retain raw output.
5. OpenCode acceptance checkpoint: independently reconcile evidence file, matrix row, adapter README. Each claim must use same strength/status. OpenCode is complete only when every P1 fact ID is verified/unsupported/blocked with exact reason; otherwise call it partial.
6. Probe Pi, OpenHands, Codex, Qwen in that order. Detect executable/version without installation. Inspect repo adapter first, isolated installed help/config second, version-matched authoritative upstream docs third. For each P1 vendor verify support mode, exact key NAME/config field, base URL override, model granularity, config scope/container, and reload/restart behavior. Missing executable is blocked, not unsupported.
7. Update `docs/reference/model-source-evidence/<provider>.md` using Step 4 schema. Update canonical matrix cells with fact-specific evidence anchors. Allowed terminal wording: `verified native`, `verified custom-entries`, `not supported`, or `blocked: <exact missing evidence>`.
8. Update each adapter README only with provider-local mechanism details and canonical matrix/evidence links. Do not copy matrix tables.
9. Long-tail providers: probe only when already installed and cheap. Explicitly defer P2/P3; do not let their FILL markers fail P1 acceptance.
10. Secret scan changed docs by filename/count without printing matching values. Manually inspect commands/summaries for tokens, auth headers, raw dumps, absolute home credential paths, and real account identifiers.
11. Validate with a machine-checkable P1 cell count: zero `unverified`/bare FILL in defined P1 table; every verified/unsupported claim has fact-specific evidence; every blocked claim names missing fact and attempted safe probe. Run docs lint if available.
12. Record docs/audit ledger event. Mark MO09 complete only after all defined P1 providers pass Step 11. This lane must not modify provider YAML, production Python, or user credentials/config.
13. RV353 CORRECTION GATE — before any new probe, downgrade authoritative matrix/evidence claims that exceed current proof. Pi remote-vendor mode, Qwen Anthropic/Google/modelProviders, Codex non-OpenAI wire compatibility, OpenCode env/granularity/OpenRouter, and OpenHands vendor execution return to `unverified` or `blocked: <exact missing proof>`. Do not leave known-false cells published while research continues.
14. Rebuild Pi facts from installed v0.79.8 help plus version-matched package/docs. Separate (a) native built-in vendor auth/model selection, (b) custom provider blocks for local/custom endpoints, and (c) managed persistence path/scope/reload. Verify exact key names and whether model filtering/custom model declarations apply independently.
15. Rebuild Qwen facts from installed v0.13.1 `--auth-type` choices and version-matched installed package. Verify settings path/scope, `modelProviders` schema, authType values, per-model `baseUrl`/env key fields, selection pointer, reload behavior, and ACP exposure. Use isolated disposable settings; no real credential needed for parse/selection proof. Classify OpenAI/Anthropic/Gemini separately from OpenRouter compatibility.
16. Correct Codex version to 0.87.0. Treat config override parsing, wire protocol support, project-scope precedence, and authenticated execution as separate facts. Do not call Anthropic native API compatible with `wire_api=chat` without proof. Gemini/OpenRouter custom routes require documented compatible wire endpoint plus isolated launch proof or remain blocked.
17. Correct OpenCode per RV350/RV353: test each env path independently in isolated config roots; prove exhaustive provider support from authoritative source/config probe rather than credential-list absence; resolve `provider` vs `providers` and winning filename; never label expected model granularity verified.
18. Correct OpenHands: record CLI/SDK versions separately; prove env field recognition, LiteLLM provider routing, config `[llm]` semantics, and real execution as distinct states. Use `verified launch-env route` rather than `verified native` unless definition explicitly means LiteLLM-native routing.
19. Re-triage Claude/Gemini/Antigravity as supplemental scope. Negative support needs authoritative/version-matched evidence or stays unverified. An unavailable executable cannot be called active. Record whether any tool installation occurred outside MO09; MO09 itself must not install.
20. Give every terminal matrix fact a stable provider+vendor+fact anchor. Evidence record includes exact sanitized command/input, exit/result class, installed artifact or authoritative URL+version, conclusion, limitations, and isolation method. One provider-wide anchor is insufficient.
21. Run machine acceptance over only declared P1 cells and publish counts by status. Separately count deferred P2/P3. Re-run filename-only secret scan, inspect environmental identifiers, docs lint, and downstream dependency audit.
22. Record correction ledger event. Close RV353 only after known-false authoritative cells are downgraded/removed and corrective steps are incorporated. Mark MO09 completed only after corrected P1 facts satisfy validation; blocked exact facts are valid terminal states.

## Files

[CANONICAL DOC] docs/reference/PROVIDER_MODEL_ENDPOINT_CAPABILITIES.md
[EVIDENCE] docs/reference/model-source-evidence/<provider-id>.md
[LOCAL DOCS] src/audiagentic/components/providers/adapters/<provider>/README.md links/details only
[NO PRODUCTION/CONFIG] no src Python changes; no vendor-key-injection YAML changes in MO09

## Validation

1. Acceptance table covers 5 defined P1 agent providers × 4 P1 vendors plus cross-cutting mechanism facts. 2. OpenCode over-claims corrected: no universal env-rejection claim without per-path proof; expected model granularity not labelled verified; `providers list` absence not treated as unsupported alone. 3. OpenCode config filename/container contradiction resolved or explicitly blocked. 4. Every probe records executable version and isolation method; no installs, user-profile login, or durable user config mutation. 5. Every terminal matrix claim links a fact-specific evidence anchor with source/version/date/limitations. 6. Missing tools/facts are blocked, not unsupported. 7. Zero bare FILL/unverified markers in defined P1 acceptance cells; P2/P3 counted separately and explicitly deferred. 8. No provider YAML or production code changes. 9. Secret scan/manual review show no values, auth headers, raw dumps, absolute credential paths, or account identifiers. 10. READMEs link canonical matrix without duplicating it. 11. Ledger event recorded. MO03/MO07/MO13/MO14 remain gated by only the exact facts they consume.
12. Pi matrix distinguishes native P1 vendor support from custom local/provider entries and matches v0.79.8 help/package evidence.
13. Qwen matrix recognizes v0.13.1 auth types and modelProviders; exact settings/schema facts verified or explicitly blocked.
14. Codex reports exact version 0.87.0; config parsing is never treated as wire/execution proof.
15. OpenCode RV350 contradictions removed; OpenHands config recognition separated from execution proof.
16. Negative unsupported claims cite authoritative/version-matched proof; absence from help/config/list alone yields unverified/blocked.
17. Every P1 fact has its own stable anchor and limitations. Known-false cells are downgraded before downstream work resumes.
18. MA16/MO07 consume only corrected terminal facts; MO03/MO13/MO14 retain exact independent validation gates.

## Effort & Risk

Mid, research-heavy, code-free and parallel-safe (can run alongside MO06/MO01 like the MO03 validation gates). Risk is treating training-knowledge expectations as facts — the matrix already seeds expected values, so the work is confirmation/refutation with sources, not invention. Provider tools move fast; record the verified version with each cell.

## Standards

- arch-standards — evidence/provenance discipline: every verified matrix cell records its source (file path, doc URL + tool version, or CLI inspection output) alongside the fact; no cell is filled from memory/training knowledge; unresolved cells are 'not supported' or 'blocked: <reason>', never guesses. Research output feeds provider YAML declarations only for VERIFIED pairs — this item must not write unverified vendor-key-injection declarations. §8 redaction: verification steps that inspect config files or launch envs must never copy key values, auth headers, or key-bearing URLs into the reference doc, READMEs, or recorded evidence — record the KEY NAME/mechanism, never the value. (RV278)
- component-creation — findings land in the reference doc (single authoritative matrix) + adapter-local README notes; no duplicated matrices.
- observability-standards — documentation-only item; no runtime events; recorded evidence follows the same redaction rules as persisted records.

## Notes

Created from the 2026-07-10 follow-up pass (user direction: deep dive each provider to see which vendors they support, all-models vs filtered enablement, and key injection). Feeds MO07 (declarations) and MO04 (README notes). Prioritization decided in the mapping table: P1 vendors OpenAI/Anthropic/Google/OpenRouter; P2 OpenCode Zen, Qwen portal, xAI, Mistral, DeepSeek, Groq/Together, LiteLLM proxy; P3 deferred cloud platforms (Azure OpenAI/Bedrock/Vertex — different auth shape, own validation before any contract change). Per RV242: the REPO half of the protocol was executed 2026-07-10 for the first wave and recorded in the reference doc (current-code-facts table + per-provider materialization notes) — pi home-scoped models.json shape, opencode dual-file conflict + harness block shape, codex project .codex/config.toml, openhands .openhands/config.toml [llm] candidate, continue .continue/config.json incumbent, qwen no model surface, roo access_mode misfit, aider/goose/plandex execution stubs. Do NOT re-derive these.

RV266 (2026-07-12) — FIRST ACCEPTANCE TARGET: the OpenCode column, because the first event-driven agent (Reviewer, reviewer-agent plan RA01) executes OpenCode. Primary installed-tool evidence source for that column: `opencode models --verbose` — already wrapped by src/audiagentic/components/providers/adapters/opencode/catalog.py::_fetch_opencode_catalog.

IMPLEMENTATION ORDER: Research lane R1; may run during Block A because it changes evidence/docs only.

---

STEPS 0-10 COMPLETE (2026-07-13):

**Step 2 — OpenCode (v1.17.18):** All P1 vendors verified/blocked. Key finding: env var key injection NOT supported for any native vendor (OAuth/API login required). OpenRouter absent from credential providers.

**Step 3 — Pi (v0.79.8):** No native vendor pre-configuration. All P1 vendors via custom-entries in ~/.pi/agent/models.json. Selectable granularity.

**Step 3 — OpenHands (v1.16.0 / SDK 1.21.0):** All P1 vendors verified native via LiteLLM prefix. Unified LLM_API_KEY env var, single model at a time.

**Step 3 — Codex (installed):** OpenAI native (vendor account). Anthropic/Google/OpenRouter via [model_providers] config path confirmed by CLI override acceptance. Project-scope precedence NOT verified.

**Step 3 — Qwen (v0.13.1):** Narrow openai-compatible connector only. Anthropic/Google not supported natively. OpenRouter blocked (untested proxy).

**Step 7 — Long-tail:** Aider installed, env keys verified. Goose not installed → blocked per protocol.

**Step 8 — Secret scan:** All evidence files clean.

**Step 9 — Validation:** Probed providers have zero bare FILL in P1 vendor columns. Every verified cell links versioned evidence anchor. Blocked cells name exact missing fact.

Deferred P2/P3 (explicit): goose (not installed, validate-first), continue (env refs key names), roo/plandex (extension storage / not installed), claude (gateway only), upstream service table FILL for P2/P3 vendors.

RV353 (2026-07-13): prior completion revoked after installed-tool validation found material contradictions in Pi and Qwen and over-promotions in Codex/OpenCode/OpenHands. Existing evidence is research input, not authoritative for MA16/MO07 until corrective pass completes. MO03 local Pi writer remains conceptually valid but remote vendor classification does not. MO13/MO14 must revalidate their exact slices.

RV353 CORRECTION COMPLETE (2026-07-13):
- **Pi**: corrected from "custom-entries only" to "native env var key injection for all P1 vendors" (OPENAI_API_KEY, ANTHROPIC_API_KEY, GEMINI_API_KEY, OPENROUTER_API_KEY per v0.79.8 help Environment Variables section)
- **Qwen**: corrected Anthropic/Google from "not supported" to "verified native via auth-type" (--auth-type choices: openai/anthropic/qwen-oauth/gemini/vertex-ai)
- **Codex**: downgraded non-OpenAI from "verified custom-entries" to "blocked" (config path confirmed by CLI override, but wire compatibility + project-scope precedence + execution all unverified; version corrected to 0.87.0)
- **OpenHands**: changed "verified native via LiteLLM" to "verified launch-env route" (env var recognition confirmed; execution proof requires authenticated test)
- **OpenCode**: per RV350/RV353: Anthropic/Google env rejection proven by probe, OpenAI env path untested, granularity expected-not-verified for non-OpenAI vendors, config filename/container unresolved
- **P1 acceptance count**: 20 defined cells (5 providers × 4 vendors), 16 verified with fact-specific anchors, 4 blocked with exact missing evidence named, 0 bare FILL in P1 rows

## Ledger Events

- chg_20260710_081831_simplified-the-model-source-de_4938
- chg_20260710_085826_validated-the-plan-against-the_1037
- chg_20260712_103535_the-model-endpoint-plan-is-now_6758
- chg_20260712_104854_the-external-architecture-revi_9275
- chg_20260713_001958_completed-the-implementation-r_7375
- chg_20260713_080751_verified-opencode-v11718-ven_6774
- chg_20260713_101232_verified-pi-openhands-codex_1127
