---
id: RV320
review-of: MO13
plan: plan-model-endpoint-management
state: closed
reviewed-by: agent-readiness-pass
reviewed-at: ''
---

# Codex and OpenHands verification gates depend on MO09 evidence

## Notes

Both providers require MO09 verification evidence before adapter implementation.

## FINDING 1: Codex project-scope model_providers not verified (step 1)
The item says 'Verify installed Codex project .codex/config.toml honors model_providers, model, model_provider, wire_api; record version/evidence. If false, remain blocked.'
- Reference doc states the repo manages PROJECT `.codex/config.toml` for MCP/LSP (not user-global).
- It is unknown whether the installed Codex reads `model_providers` from project scope or requires `~/.codex/config.toml`.
- If project scope is NOT honored, this item must remain blocked — no user-global write allowed.

## FINDING 2: OpenHands [llm] semantics not verified (step 3)
The item says 'Verify OpenHands .openhands/config.toml [llm] keys and execution status. If structured contract verified, add reader/writer/remover; otherwise bind NoAutomationRecipe.'
- Key questions: does `[llm]` support multiple models or only a single active model? What are the exact keys (`model`, `api_key`, `base_url`, etc.)?
- Execution adapter for OpenHands is currently stub — env contribution path cannot be enabled without real execution.

## RECOMMENDATION
(a) Block on relevant MO09 evidence cells before starting either adapter.
(b) If Codex project scope not verified, implement NoAutomationRecipe with exact blocker text: 'project-scope model_providers honor unknown for installed version X.Y.Z.'
(c) For OpenHands, if `[llm]` structured contract is verified but execution remains stub, implement the writer (structured-file path) separately from env contribution — they are independent projection modes.
(d) Ensure golden tests cover TOML merge semantics (preserve unmanaged tables, collision detection).

## Findings



## Conclusion


