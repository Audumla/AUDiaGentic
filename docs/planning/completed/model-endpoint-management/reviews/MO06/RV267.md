---
id: RV267
review-of: MO06
plan: model-endpoint-management
state: closed
reviewed-by: claude
reviewed-at: ''
---

# Implementability pass: pre-existing failing tests sit in the validation gate

## Notes



## Findings

1. MO06 validation item 5 requires 'architecture-boundary tests still pass', but tests/unit/foundation/toolchains/test_architecture_boundaries.py currently has two pre-existing failures unrelated to this plan (hindsight matrix source gate, plugin-config recipe manual-instruction skip — observed failing 2026-07-12 on an untouched tree). An implementing agent gating on 'all green' would stall or, worse, 'fix' unrelated tests inside a refactor-only item.
2. Same applies to the full-unit-suite Std §10 verification: tests/unit/planning/test_planning_api.py (created-by fixtures), test_check_baseline_assets, and tests/integration/jobs/test_job_approvals.py fail on the current tree for unrelated reasons.

## Conclusion

Add a baseline instruction to MO06: record the failing set BEFORE starting (pytest run captured in notes), and gate completion on 'no NEW failures relative to that baseline'; pre-existing failures are out of scope and must not be fixed inside this refactor item.
