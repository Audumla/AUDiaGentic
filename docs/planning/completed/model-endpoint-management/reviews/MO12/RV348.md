---
id: RV348
review-of: MO12
plan: plan-model-endpoint-management
state: closed
reviewed-by: opencode-agent
reviewed-at: ''
---

# Missing error code VAL-SRCCAT-001 in providers error-resolutions.yaml

## Notes

Review assessment: The review identified `_catalog_error = make_error_factory("VAL", "SRCCAT", "providers")` at source_catalog.py:36 as unregistered. However, inspection shows that ALL catalog error raises use `_catalog_conflict` (CON-SRCCAT-*) — not `_catalog_error`. Specifically:
- Line 88: `_catalog_conflict(1, ...)` → CON-SRCCAT-001 (registered)
- Line 110: `_catalog_conflict(1, ...)` → CON-SRCCAT-001 (registered) 
- Line 133: `_catalog_conflict(2, ...)` → CON-SRCCAT-002 (registered)
- Line 212: `_catalog_conflict(2, ...)` → CON-SRCCAT-002 (registered)

The `_catalog_error` factory at line 36 is defined but never called. The VAL-SRCCAT prefix codes are not raised anywhere in the source tree. This appears to be a leftover factory from initial development.

Action: Remove unused `_catalog_error` factory to prevent future misuse, or register it preemptively. Given §8 discipline (register every error code before first use), the safer approach is to remove the unused factory entirely.

## Findings

CRITICAL — Unregistered error code violates §8. The code is live and reachable through get_source_catalog() → _read_cache() → _validate_catalog_payload() on corrupt cache, and through load_static_catalog() / _write_cache().

## Conclusion

no_action_required — _catalog_error factory defined but never invoked; all catalog errors use CON-SRCCAT-* which are registered
