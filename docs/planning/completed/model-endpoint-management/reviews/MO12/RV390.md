---
id: RV390
review-of: MO12
plan: plan-model-endpoint-management
state: closed
reviewed-by: codex
reviewed-at: ''
---

# Complete degraded catalog observability contract

## Notes



## Findings

`source_catalog._degraded()` writes timeline attributes containing only `failure-class` and `fallback`. MO12 Step 7/validation and Observability Standards require degraded outcomes to include failure_class, fallback, stale/stale_age when known, action_needed, canonical error_code, and correlation propagation. Cached and missing outcomes therefore persist incomplete diagnostics; key spelling also differs between result fields (`failure_class`) and timeline (`failure-class`) without a documented adapter. Timeline path is one aggregate `source-catalog-timeline.jsonl`, so confirm whether this is an operational sidecar or per-resource timeline and use the matching foundation helper/contract. Remedy: characterize expected record shape, choose timeline-versus-sidecar explicitly, include required safe fields, test cached/missing correlation and secret canary, then update plan evidence/ledger.

## Conclusion

Action required before treating this item's completion/tracker evidence as fully authoritative.
