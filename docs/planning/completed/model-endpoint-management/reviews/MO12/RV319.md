---
id: RV319
review-of: MO12
plan: model-endpoint-management
state: closed
reviewed-by: agent-readiness-pass
reviewed-at: ''
---

# Connector fetcher registry will be sparse at v1 — scope to confirmed endpoints only

## Notes

Step 3's connector fetcher registry may be sparse at launch.

## FINDING: Only OpenAI and OpenRouter have confirmed public /models endpoints
The item says 'Add connector fetcher registry keyed by connector id; fetchers receive redacted connectivity inputs.' But:
- `openai-compatible` → confirmed `/v1/models` endpoint (OpenAI, most gateways)
- `openrouter` → confirmed `/models` endpoint  
- `anthropic` → marked FILL in reference doc — 'no public list API assumed' (PROVIDER_MODEL_ENDPOINT_CAPABILITIES.md:91)
- `gemini` → marked FILL — needs verification
- `ollama`, `litellm`, `native-vendor` → discovery mode is likely `none` or `static-catalog`

At v1 launch, the fetcher registry may contain only 2 connectors (openai-compatible, openrouter). The remaining connectors use `static-catalog` or `none` discovery.

## RECOMMENDATION
(a) Implement the registry architecture correctly — it supports adding new connectors without code changes to source_catalog.py (already specified: step 2 validation test 'adds fixture connector without core code edit').
(b) Ship with openai-compatible and openrouter fetchers; add others when MO09 verifies their discovery mode.
(c) Do not block MO07 on having all connectors — a connector with `none` discovery still works for native key injection (no catalog needed).
This is a scoping note, not a blocker. The item architecture is correct.

## Findings



## Conclusion


