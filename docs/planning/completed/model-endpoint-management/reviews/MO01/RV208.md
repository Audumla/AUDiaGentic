---
id: RV208
review-of: MO01
plan: plan-model-endpoint-management
state: closed
reviewed-by: gpt-5.5-thinking
reviewed-at: '2026-07-09'
---

# Distinguish current code from target managed-config contract

## Notes

Revalidated MO01 against the current repo state after main was refreshed. The current descriptor code still has separate `McpConfigSpec` and `LanguageServersConfigSpec`, and the loader still has separate `_build_mcp_config` / `_build_language_servers_config` builders. The model-endpoint contract should therefore describe the desired target as a consolidation step, not as an already-present shared spec.

## Findings

1. MO01 is directionally correct: model endpoints belong under providers and should not become an agent-owned or recipe-owned config subsystem. 2. The reference doc needed stronger language that `ManagedConfigSpec` is a MO06 target extraction from current duplication. 3. The ownership rule remains valid: `owner_scope = provider id`, `managed_id = model-endpoints/<endpoint-id>`. 4. The endpoint connector taxonomy belongs in the project endpoint schema and per-provider supported connector declarations, not hardcoded branches.

## Conclusion

Incorporated. The reference doc now has a current-code validation table and keeps MO01 pointing at MO06 as a prerequisite consolidation rather than implying the managed-config abstraction already exists.
