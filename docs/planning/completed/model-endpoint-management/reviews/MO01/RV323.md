---
id: RV323
review-of: MO01
plan: plan-model-endpoint-management
state: closed
reviewed-by: agent-readiness-pass
reviewed-at: ''
---

# Connector-specific fields (step 3) are FILL — use free-form connector-options

## Notes

Step 3 connector-specific fields are marked FILL — schema completeness at v1 is ambiguous.

## FINDING: Connector taxonomy has FILL fields in step 3
Step 3 says 'Include connector-specific fields (anthropic version, gemini api-version, openrouter routing prefs / namespaced model-id) — many are FILL until confirmed per agent.'
- The JSON schema will need to declare these fields as optional at v1 launch.
- It is unclear which connector-specific fields are truly required for the first-wave connectors (openai-compatible, ollama) versus deferred.

## RECOMMENDATION
(a) Define the schema with connector-specific fields in a free-form `connector-options: map[string, any]` field — this avoids hardcoding per-connector schemas that may need revision.
(b) Document the known required keys per verified connector in the reference doc (PROVIDER_MODEL_ENDPOINT_CAPABILITIES.md Connector taxonomy section).
(c) When MO09 verifies a connector's specific fields, update the reference doc and add validation rules — no schema change needed if using free-form options.
This keeps the v1 schema stable while allowing connector-specific behavior to evolve as evidence is gathered.

## Findings



## Conclusion


