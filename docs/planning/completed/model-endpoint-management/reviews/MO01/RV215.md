---
id: RV215
review-of: MO01
plan: model-endpoint-management
state: closed
reviewed-by: gpt-5.5-thinking
reviewed-at: '2026-07-09'
---

# Capability matrix must mark unverified fields as blockers

## Notes

Revalidated the provider matrix against the refreshed repo state. Several provider facts are known from repo code (OpenCode path conflict, Goose MCP YAML, Pi template shape, local-openai schema bug), while several upstream/provider fields remain unverified.

## Findings

1. The matrix must keep `⟨FILL⟩` placeholders for unverified path/schema/reload fields. 2. `⟨FILL⟩` values are blockers for auto writers, not defaults. 3. Support status and priority must remain separate columns. 4. Provider-specific facts discovered during implementation must update `docs/reference/PROVIDER_MODEL_ENDPOINT_CAPABILITIES.md` before the adapter is promoted to auto-supported. 5. Native/account-derived providers should stay out of generic local endpoint propagation unless a dedicated gateway recipe is scoped.

## Conclusion

Incorporated. The refreshed reference doc now includes current-code facts, corrected Goose status, explicit blocker language, and open verification rows.
