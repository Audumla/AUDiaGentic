---
id: RV211
review-of: MO04
plan: model-endpoint-management
state: closed
reviewed-by: gpt-5.5-thinking
reviewed-at: '2026-07-09'
---

# Manual and blocked providers must be guidance-only until storage/path contracts exist

## Notes

Revalidated MO04 against the planning reference and current provider descriptors. Cline/Roo are extension-storage providers, and Roo currently declares `access_mode: env`, which is misleading because extension-managed model profiles are not equivalent to a provider env auth ref. Plandex and Aider also need path/launch validation before auto support.

## Findings

1. Manual, unsupported, and blocked states should be expressed through `NoAutomationRecipe` with action-needed text. 2. Cline/Roo must not write VS Code extension storage until a host-adapter path and storage schema are verified. 3. Roo descriptor semantics should be corrected before model endpoint work uses descriptor access mode for model config support. 4. Aider env projection should become auto only when there is a real execution launch seam to receive env/args. 5. Plandex remains blocked until custom model config path/schema are verified.

## Conclusion

MO04 is valid after preserving these gates. The reference doc now explicitly separates manual/blocked guidance from env-projection automation.
