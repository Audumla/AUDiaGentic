---
id: RV322
review-of: MO04
plan: model-endpoint-management
state: closed
reviewed-by: agent-readiness-pass
reviewed-at: ''
---

# Continue YAML vs JSON decision depends on MO09 installed-version evidence

## Notes

Step 1's YAML vs JSON decision cannot be made without installed-tool evidence.

## FINDING: Continue config format decision depends on unverified installed version
Step 1 says 'extend the INCUMBENT .continue/config.json with models[] first' and 'Adopt the newer YAML config only if the installed Continue version requires it (MO09 decides).'
- MO09 is responsible for verifying which version of Continue is installed and whether it requires YAML.
- If MO09 marks the Continue column as `blocked: tool not installed`, this item cannot decide between JSON and YAML.

## RECOMMENDATION
(a) Default to the incumbent `.continue/config.json` (JSON) with `models[]` — this is explicitly stated as the first step.
(b) If MO09 later verifies that the installed version requires YAML, migrate atomically per §10 (same change updates MCP + models).
(c) Do not block Continue adapter implementation on MO09 — ship JSON-first, migrate if needed. The same-file rule (MCP + models never split across files) is already specified.

## Findings



## Conclusion


