---
id: RV241
review-of: MO04
plan: plan-model-endpoint-management
state: closed
reviewed-by: claude
reviewed-at: ''
---

# Continue: repo manages .continue/config.json — JSON is the incumbent surface, not legacy-second

## Notes



## Findings

The item orders Continue work 'YAML models[] writer first; legacy .continue/config.json JSON decided separately'. Repo sweep 2026-07-10: the Continue descriptor already reads/writes .continue/config.json (JSON) for MCP — that file is the surface AUDiaGentic actively manages today. Writing models to a YAML file the repo doesn't otherwise touch while MCP lands in config.json risks split-brain config and doubles the adapter surface.

## Conclusion

Invert the ordering: extend the already-managed .continue/config.json with models[] first (same file, same reader/writer conventions); adopt the newer YAML config only if the installed Continue version requires it (MO09 verification decides), and then migrate MCP+models atomically per §10 — never manage the two kinds in different files.
