---
id: RV281
review-of: MO10
plan: plan-model-endpoint-management
state: closed
reviewed-by: claude
reviewed-at: ''
---

# Correction: list_provider_models already exists — extend it, do not add provider_models_available

## Notes



## Findings

1. providers_api.list_provider_models + the matching providers_mcp tool already exist (providers_api.py:74, providers_mcp.py:44) and read the runtime catalog via read_model_catalog — MO10 part (A) as written would create a duplicate parallel tool (§10 violation).
2. What the existing tool lacks: (a) a refresh flag (caller must separately invoke refresh_provider_catalog); (b) a reason field distinguishing 'provider has no catalog support' (no fetch_catalog_fn) from 'catalog not yet fetched' — both currently collapse into error: 'no catalog found'; (c) staleness signal (fetched-at is returned but not evaluated against the provider's catalog-refresh max-age).
3. User direction 2026-07-12: extend the management MCP tools to interrogate providers in more detail (models available within them, etc.) — the deep-describe surface goes to a new sibling item (MO11) so MO10 stays a minimal Reviewer prerequisite.

## Conclusion

Rewrite MO10 part (A) to EXTEND list_provider_models (refresh flag, no-catalog-support vs not-fetched reason, staleness) with no new tool name; keep part (B) (adapter model-id fix) unchanged; deep interrogation moves to MO11.
