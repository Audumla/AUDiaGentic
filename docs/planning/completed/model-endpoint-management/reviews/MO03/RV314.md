---
id: RV314
review-of: MO03
plan: model-endpoint-management
state: closed
reviewed-by: agent-readiness-pass
reviewed-at: ''
---

# OpenCode path/container Gate 1 evidence missing

## Notes

Gate 1 (step 1) is unresolved and blocks the OpenCode path.

## FINDING: OpenCode config path/container conflict not resolved
Step 1 says 'Validate installed OpenCode file/container contract: .opencode/config.json vs .opencode/opencode.json and provider vs providers.'
- Reference doc (PROVIDER_MODEL_ENDPOINT_CAPABILITIES.md:29) confirms the conflict: descriptor MCP/LSP uses `.opencode/opencode.json`; harness `_build_opencode_provider_config` writes `.opencode/config.json` with top-level `providers`.
- No evidence recorded for which path/container the INSTALLED tool version actually reads at runtime.
- The item correctly says 'If unresolved, keep OpenCode NoAutomationRecipe and complete Pi independently.' But this should be explicit as a blocking gate, not buried in step 1.

## RECOMMENDATION
Run `opencode models --verbose` or inspect the installed tool's config loading code to determine:
(a) Which file path does the installed OpenCode read for provider config?
(b) Which top-level key (provider vs providers) does it expect?
If evidence cannot be gathered, close this review with 'incorporated: gate documented, agent to skip OpenCode if unresolved' and proceed Pi-only.
Pi delivery is not blocked by this — the item already allows independent completion.

## Findings



## Conclusion


