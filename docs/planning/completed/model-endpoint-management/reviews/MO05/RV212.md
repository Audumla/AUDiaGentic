---
id: RV212
review-of: MO05
plan: model-endpoint-management
state: closed
reviewed-by: gpt-5.5-thinking
reviewed-at: '2026-07-09'
---

# local-openai catalog bug is confirmed against schema

## Notes

Revalidated MO05 against `local_openai/catalog.py` and `provider-model-catalog.schema.json`. The adapter emits `status: "available"` and `context-window: None`, while the schema requires `status` in `active|deprecated|experimental` and integer `context-window >= 1`.

## Findings

1. MO05 is correctly isolated and should run before the broader model-endpoint work. 2. The fix should map missing/unknown upstream status to a valid enum, defaulting to `active`. 3. Context window must have a positive integer fallback from endpoint declaration, provider override, model metadata, or a documented conservative default. 4. The test should validate the full emitted catalog payload, not only individual fields.

## Conclusion

MO05 is valid and remains first in the implementation order.
