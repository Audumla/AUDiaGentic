---
id: RV349
review-of: MO05
plan: plan-model-endpoint-management
state: closed
reviewed-by: opencode-agent
reviewed-at: ''
---

# Silent exception handlers in local_openai catalog.py violate §8, §8.1, §9

## Notes

Review findings incorporated into local_openai/catalog.py: module logger added, all exception handlers now emit structured warnings via _logger.warning(exc_info=True), HTTP errors distinguished by code/reason from network failures.

## Findings

FIXED — Added `import logging` and `_logger = logging.getLogger(__name__)`. Three silent returns replaced: (1) AudiaGenticError on secret resolution now logs warning with exc_info=True; (2) non-200 HTTP status logs status code before returning empty; (3) bare Exception replaced with specific urllib.error.HTTPError (logs code+reason), and (urllib.error.URLError, OSError, json.JSONDecodeError) fallback. All except blocks log at warning level with exc_info=True per §8/§9.

## Conclusion

fixed — added module logger, structured warning logging with exc_info=True on all exception handlers, specific HTTPError handling
