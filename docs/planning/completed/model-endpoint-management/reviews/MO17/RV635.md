---
id: RV635
review-of: MO17
plan: plan-model-endpoint-management
state: closed
reviewed-by: claude
reviewed-at: ''
---

# Sponsor direction 2026-07-18: OpenRouter free-model vendor is the concrete consumer — MO17 stays as scoped; successors own key injection, limits, overview, and agent use

## Notes

Sponsor (user) direction recorded 2026-07-18: full cross-harness model management — introspect providers and exposed models, configure local models and enabled vendors, see usage/limits, add free-model vendors (OpenRouter first), and later run those free models as agents across harnesses. This supplies the 'concrete execution consumer' MO17 step 5 was waiting on.

## Findings

1) MO17's scope is correct and should NOT broaden — it delivers the catalog read/refresh + materialization seam everything else consumes. 2) One addition inside its existing filter step: the materialization tests should include a free-tier filter proof (include glob '*:free' against the openrouter connector fixture, and record as MA19 evidence whether OpenRouter's free markers are the ':free' id suffix, pricing fields, or both) so the vendor slice (successor item) doesn't discover filter gaps late. 3) The exclusions in step 5 (native vendor key injection, routed-backend introspection) now have owning successor items rather than being indefinitely out of scope: key injection → new MO item created today; usage/limits → PT01 (review filed there); cross-harness overview and agent-profile consumption → new items created today. RV622/RV626's deletion posture toward source-catalog infrastructure is superseded by this sponsor direction — the infrastructure now has a consumer chain and must be retained.

## Conclusion

Proceed with MO17 as written plus the free-filter proof; successors are itemized separately so each stays bounded.
