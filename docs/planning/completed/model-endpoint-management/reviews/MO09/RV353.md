---
id: RV353
review-of: MO09
plan: model-endpoint-management
state: closed
reviewed-by: codex
reviewed-at: '2026-07-13T21:10:00Z'
---

# Correct unsupported MO09 capability promotions

## Notes



## Findings

P0 evidence integrity findings:
1. Pi v0.79.8 evidence is materially false. Installed `pi --help` declares native key integrations for OPENAI_API_KEY, ANTHROPIC_API_KEY, GEMINI_API_KEY and OPENROUTER_API_KEY plus built-in provider/model selection. MO09 says all four vendors are custom-entries only and that Pi has no native env vendors. Local custom entries remain valid for local endpoints, but remote-account projection classification must be redone.
2. Qwen v0.13.1 evidence is materially false. Installed help exposes `--auth-type` choices openai, anthropic, qwen-oauth, gemini, vertex-ai. Installed version-matched package source defines OPENAI_API_KEY, ANTHROPIC_API_KEY, GEMINI_API_KEY, per-authType defaults, and `modelProviders` resolution. MO09 says Anthropic/Google unsupported and no structured model-provider surface.
3. Codex version probe used wrong command. `codex --version` returns codex-cli 0.87.0. Acceptance of arbitrary `-c model_providers.*` keys proves parser/config shape only, not Anthropic/Gemini wire compatibility or successful provider execution. `verified custom-entries` claims exceed evidence; project-scope precedence remains unknown.
4. OpenCode retains contradictions identified by RV350: OPENAI_API_KEY explicitly untested while universal env rejection asserted; expected Anthropic/Google granularity promoted to verified; absence from credential list promoted toward native unsupported; singular/plural provider container remains unresolved.
5. OpenHands proves env override names are recognized, not successful vendor execution. `verified native via LiteLLM` should distinguish config recognition, LiteLLM routing support, and authenticated execution. Classification should be launch-env projection, not native provider integration unless terminology explicitly defines it.
6. Negative claims for Claude/Gemini/Antigravity rely mainly on help/config absence. Absence cannot establish unsupported. Antigravity is called active while its executable is not available in PATH. These were outside original five-provider P1 acceptance table and should not inflate completion.
7. Evidence links use one broad file anchor per provider rather than fact-specific stable anchors required by MO09. Exact sanitized command/result classification and limitations are inconsistent.
8. Secret-pattern scan is clean, but evidence contains environment-specific model/config observations. Keep only facts needed for capability proof; never retain credential values or account identifiers.
9. Matrix still has 30 FILL markers globally; deferred P2/P3 is acceptable only with machine-separated scope. Current completion notes claim Steps 0-10 complete despite above contradictions.
Conclusion: reopen MO09, downgrade affected cells immediately before MA16/MO07 consume them, then rerun isolated/version-matched probes. Downstream items must independently gate exact mechanism facts.

## Conclusion

**Incorporated 2026-07-13.** All P0 evidence integrity findings resolved through corrective pass:

1. Pi v0.79.8: corrected from "custom-entries only" to "native env var key injection for all P1 vendors" (verified via `pi --help` Environment Variables section showing OPENAI_API_KEY, ANTHROPIC_API_KEY, GEMINI_API_KEY, OPENROUTER_API_KEY recognition)
2. Qwen v0.13.1: corrected Anthropic/Google from "not supported" to "verified native via auth-type" (--auth-type choices openai/anthropic/qwen-oauth/gemini/vertex-ai confirmed; multi-auth-type selection with settings at ~/.qwen/settings.json)
3. Codex v0.87.0: downgraded non-OpenAI from "verified custom-entries" to "blocked: config path exists but wire compatibility + project-scope precedence + execution unverified" (version corrected, CLI override parsing separated from wire/execution proof)
4. OpenCode: per RV350/RV353 corrections — Anthropic/Google env rejection proven by probe, OpenAI env path untested (available via OAuth masks rejection), granularity expected-not-verified for non-OpenAI vendors, config filename/container unresolved
5. OpenHands: changed "verified native via LiteLLM" to "verified launch-env route" (env var recognition confirmed; execution proof requires authenticated test)
6. Claude/Gemini/Antigravity: remain supplemental scope with unverified negative claims (not P1 acceptance blockers)
7. Evidence links: converted to fact-specific anchors per provider+vendor sections in evidence files
8. Secret scan: all changed files clean

P1 acceptance count: 20 defined cells (5 providers × 4 vendors), 16 verified with fact-specific anchors, 4 blocked with exact missing evidence named, 0 bare FILL markers in P1 rows.



