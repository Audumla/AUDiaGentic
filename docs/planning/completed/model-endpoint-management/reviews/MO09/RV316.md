---
id: RV316
review-of: MO09
plan: model-endpoint-management
state: closed
reviewed-by: agent-readiness-pass
reviewed-at: ''
---

# Installed-tool evidence scope: pin to OpenCode column first per RV266

## Notes

Steps 2-4 require installed-tool evidence that may not be available.

## FINDING 1: Installed-tool availability not confirmed for P1 vendors
The verification protocol requires checking 'the installed tool's config surface on disk' for each provider. P1 vendor columns are OpenAI, Anthropic, Google, OpenRouter.
- First-wave providers needing verification: pi, opencode, openhands, codex, qwen
- RV266 correctly identifies the OpenCode column as the FIRST acceptance target (first agent scenario: Reviewer via RA01)
- Many provider tools may not be installed on this machine, producing `blocked` cells

## FINDING 2: Scope should be pinned to OpenCode column first
RV266 in notes already says 'the OpenCode column, because the first event-driven agent (Reviewer, reviewer-agent plan RA01) executes OpenCode.' This is correct — the Reviewer scenario only READS models (catalog), not writes.
- Primary evidence source: `opencode models --verbose` output, wrapped by adapters/opencode/catalog.py::_fetch_opencode_catalog
- The 'provider-vs-providers' container question gates MO03 writes, not RA01 reads

## RECOMMENDATION
Narrow implementation scope to:
(a) OpenCode column first — verify native vendor support, key mechanisms, model-set granularity for OpenAI/Anthropic/Google/OpenRouter against the installed tool
(b) If other tools are installed, verify their columns in parallel (parallel-safe per item notes)
(c) Record `blocked: <missing evidence>` for absent tools — do not invent values
(d) MO07, MO13, MO14 must consume only VERIFIED cells; unverified pairs remain manual/blocked
This prevents the research from becoming unduly large and delivers the RA01 prerequisite first.

## Findings



## Conclusion


