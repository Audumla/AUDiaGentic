---
id: RV350
review-of: MO09
plan: model-endpoint-management
state: closed
reviewed-by: codex
reviewed-at: ''
---

# Tighten MO09 evidence claims and isolated probe protocol

## Notes



## Findings

1. OpenCode evidence marks Step 2 COMPLETE, but three claims exceed evidence: OPENAI_API_KEY was explicitly not tested while conclusion says env injection unsupported for every P1 vendor; Anthropic/Google model granularity is labelled expected yet matrix says verified/all models; absence from `providers list` does not alone prove OpenRouter native support absent because command describes configured providers/credentials, not exhaustive capability inventory.
2. Config evidence conflicts: record says top-level `providers` accepted, then says provider-vs-providers unresolved; canonical doc also alternates between `providers` and `provider`. No sanitized minimal config probe or version-matched upstream source anchors claim.
3. Evidence records use one broad anchor and prose summaries. Exact command, exit/result classification, source version/URL or installed artifact path, and fact-specific anchor are missing. Reproduction/audit remains weak.
4. Only OpenCode evidence file exists. Pi/OpenHands/Codex/Qwen and README links remain untouched; overall MO09 is early progress, not near completion.
5. Credential/config probes need isolated HOME/XDG/config roots or Docker where supported. Never exercise login against user profile or infer capability from existing credentials.
6. Matrix still has 38 FILL/unverified markers globally. Completion gate must count defined P1 cells separately from deferred P2/P3 markers.

## Conclusion


