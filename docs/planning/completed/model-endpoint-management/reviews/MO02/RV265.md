---
id: RV265
review-of: MO02
plan: model-endpoint-management
state: closed
reviewed-by: claude
reviewed-at: ''
---

# Reviewer-scenario pass: no read surface answers 'which models are available for provider X'

## Notes



## Findings

1. MO02's tool surface mutates desired state (model_source_*) and lists AG-MANAGED config entries (list_provider_models_config), but nothing exposes provider model AVAILABILITY — the runtime catalog produced by fetch_catalog_fn (e.g. `opencode models --verbose`). providers_mcp today has refresh_provider_catalog/refresh_all_catalogs (write/refresh) but no list/read tool; services/provider_catalog.read_model_catalog exists unexposed.
2. The first event-driven Reviewer test needs exactly this: enumerate models available in OpenCode so the Reviewer agent profile can bind provider=opencode + a real model, and so alias/default validation (MO01 step 7) has a source of truth.
3. This read surface does NOT depend on MO06/MO01 — it wraps the existing read_model_catalog. It is pulled out into MO10 so the Reviewer scenario is not blocked behind the consolidation chain; MO02 should reference MO10's tool rather than adding a second one.

## Conclusion

Add a cross-reference in MO02: the availability read tool (provider_models_available) lands in MO10 ahead of MO02; MO02's status fields must report BOTH managed-model-count and availability-count (from the runtime catalog) so materialized-vs-observed drift is visible.
