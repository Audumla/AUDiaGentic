---
id: RV473
review-of: MO02
plan: model-endpoint-management
state: closed
reviewed-by: codex
reviewed-at: ''
---

# MO02 steps 3-8 not implemented — model-source CRUD still has apply/dry_run, sync_provider_models still public, no typed contract

## Notes

This is the most critical gap on the critical path. MO02 must create typed ModelProjectionRequest/Result, register through MA20, and replace all four public exports with one model-projection family operation.

## Findings

['providers_api.model_source_add/update/remove/set_enabled accept apply and dry_run parameters (lines 559/588/603/619)', 'providers_api.sync_provider_models is a public export (line 649) — should be replaced by MA20-conformant model-projection family operation', 'providers_api.list_provider_models_config is a separate function (line 680) — should fold into model-projection status per MO02 step 7', 'providers_api.reload_provider_models is a public export (line 688) — should be private apply mechanic per MO02 step 8', 'No ModelProjectionRequest/Result types exist in contracts/', 'No model-projection family registration in build_automation_registry()', 'services/models.py still has sync_managed_provider_models which is the pre-MO02 path']

## Conclusion

PARTLY VALID, STALE DETAILS. ModelProjectionRequest/Result, schemas, handler, family, and Pi registration now exist. Legacy CRUD flags and sync/config/reload public routes remain. MO02 notes corrected to deletion-only gap.
