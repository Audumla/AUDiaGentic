---
id: RV598
review-of: MO02
plan: model-endpoint-management
state: closed
reviewed-by: codex
reviewed-at: ''
---

# Closeout audit: typed model projection exists but legacy mutation routes remain

## Notes

Audit against current MA20 family pattern, shared ManagedConfigSpec/ManagedFragmentRegistry foundation, and provider capability reference.

## Findings

Confirmed the legacy mutation routes were real and actionable. Model-source CRUD mixed desired-resource persistence with provider automation; MCP exposed three overlapping public routes; reconcile bypassed the typed family; and the public family seam lacked a focused test.

## Conclusion

Implemented the contained correction: CRUD is desired-state only; reconciliation and MCP use manage_model_projection; old public sync/list/reload routes and flags are gone; public contracts, stale ownership reconciliation, and Pi plan dispatch are tested. Reused the existing model contract, explicit registry, ManagedConfigSpec, ManagedFragmentRegistry, and sync_managed_config; no new abstraction was added.
