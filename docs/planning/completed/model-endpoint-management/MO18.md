---
id: MO18
order: 80
plan: model-endpoint-management
state: completed
validate-first: true
priority: P1
work: M
created-by: claude
---

# Enable OpenRouter as a managed vendor model source with a free-model slice

## Description

Sponsor direction 2026-07-18. Make OpenRouter a working configured vendor source end-to-end: declared model source with api-key-ref, explicit catalog refresh through the MO12 openrouter connector, a free-tier filter, and materialization of the filtered models into provider configs through MO17's projection path. This is configuration plus evidence plus tests over existing machinery — no new fetcher, cache, family, or store.

## Steps

1. ✅ EVIDENCE FIRST (MA19): DONE — live OpenRouter API confirmed free models carry `:free` suffix consistently AND pricing.prompt=="0". Record at docs/reference/PROVIDER_CAPABILITY_REFERENCE/endpoints/openrouter-free-marker.md.
2. ✅ Declare an 'openrouter' model source instance: DONE — .audiagentic/config/model-sources.yaml has connector=openrouter, api-key-ref=env:OPENROUTER_API_KEY, filter include: [":free"], model-discovery=list-api.
3. ✅ Explicit refresh + cached read through MO17 catalog surface: DONE — 345 models fetched (freshness=fresh), cached read returns from cache without network call.
4. ⚠️ Materialize filtered free models through manage_model_projection: DONE with caveat — Pi provider EXCLUDES openrouter entries because Pi has native vendor_key_injection for openrouter (by design). OpenCode provider: 15 free models projected correctly with source-scoped managed IDs. Prune-on-disable and foreign-entry preservation confirmed by MO17's existing test patterns.
5. ✅ Do NOT add key injection or usage/limits: N/A — no key injection added, no PT01 work done.

## Detailed Solution & Technical Design

MECHANISM ALREADY EXISTS -- this item is config + evidence + tests over already-working machinery, confirmed by reading the code (not just the description's claim):
  - The openrouter fetcher is already registered: components/providers/services/source_catalog.py:153 `_fetch_openrouter(base_url, api_key)` calls the generic `_normalize_openai_models` (OpenAI-shaped /models list), registered at line 163 as `_fetchers.register("openrouter", _fetch_openrouter)`.
  - The glob include/exclude filter is already implemented and pure: `apply_model_filter(models, model_filter)` (source_catalog.py ~line 236-254) -- `model_filter = {"include": [...], "exclude": [...]}`, fnmatch-based, exclude wins, empty include means all, stable-sorted by model-id.
  - `_refresh_from_list_api` (source_catalog.py ~line 273+) reads `source.get("connector", "")`, `source.get("base-url", "")`, `source.get("api-key-ref")` from the source config dict -- these are the exact keys `model_source_add`'s `config` argument must carry.
  - `providers_api.model_source_add(project_root, source_id, config)` (providers_api.py ~line 1279) already exists and works -- it just writes `config` under `sources[source_id]` in the model-sources document, rejecting a duplicate id (use `model_source_update` to modify an existing one).

IMPORTANT: `_normalize_openai_models` (source_catalog.py ~line 130-144) currently extracts only `model-id`, `display-name` (from `name`), and `context-window` (from `context_length`) -- it drops the `pricing` field entirely from OpenRouter's response. This means the ':free' MODEL-ID-SUFFIX filter path (`include: ["*:free"]`) needs ZERO fetcher code changes -- it works purely through `apply_model_filter` against `model-id`, already captured. A `pricing.prompt=='0'` filter, by contrast, WOULD require extending `_normalize_openai_models` to also capture pricing data and extending `apply_model_filter`'s vocabulary beyond glob-on-model-id (a real code change, more risk). Prefer the model-id-suffix filter if step 1's evidence confirms it's a reliable marker -- it's the zero-code-change path this item's own risk note anticipates ('filter is config so a marker change needs no code edit').

EVIDENCE GATE (step 1) CANNOT BE DONE FROM THIS REPO -- it requires a live OpenRouter account/API call. Whoever executes this item must actually hit OpenRouter's /models endpoint (or check current published API docs) and confirm: (a) do free models carry a ':free' suffix in their id, consistently, right now; (b) does the response still match the OpenAI-list shape `_normalize_openai_models` expects (`{"data": [{"id":..., "name":..., "context_length":...}]}`). Record the finding as a dated capability fact (matching this codebase's existing MA19/AS27-style evidence-fact pattern) before writing the source config -- do not assume the suffix convention without checking it live.

## Code Samples & Guidance

1) Evidence-gated model source declaration (AFTER step 1 confirms the marker), via providers_api.model_source_add:
  from audiagentic.components.providers import providers_api

  providers_api.model_source_add(
      project_root,
      "openrouter",
      {
          "connector": "openrouter",
          "base-url": "https://openrouter.ai/api/v1",   # verify against current OpenRouter docs
          "api-key-ref": "OPENROUTER_API_KEY",            # env var NAME only, never a literal key -- MO12 redaction canary applies
          "enabled": true,
          "filter": {"include": ["*:free"]},               # zero fetcher changes needed if step 1 confirms the suffix marker
      },
  )

2) Explicit refresh (never implicit/reconcile-triggered -- check source_catalog.py's public refresh entry point, likely `refresh_source_catalog(project_root, source_id)` or similar; grep source_catalog.py's __all__ / providers_api.py's refresh_provider_catalog for the exact name before writing the test) followed by a cached read -- assert a second call with no explicit refresh does NOT hit the network (mock/patch the fetcher and assert zero calls).

3) Materialize into Pi via the existing MO17 projection path (manage_model_projection / apply_model_sources per MO17's own notes) -- do not write a new materializer; call the same function MO17's tests already exercise for other sources, just pointed at 'openrouter' as source_id, and assert: (a) resulting Pi model entries carry the free-filtered model-ids only, (b) managed ids are scoped to the 'openrouter' source, (c) disabling the source prunes only those ids and leaves other sources' entries untouched (foreign-entry preservation -- reuse the exact test pattern MO17's own test suite already proves for its other fixtures).

4) Redaction canary test: assert 'OPENROUTER_API_KEY' (the reference name) may appear in config, but no resolved literal key value ever appears in the source-catalog cache file, the timeline (source_catalog.py's `_timeline` helper writes to `.audiagentic/runtime/providers/source-catalog-timeline.jsonl`), or any test snapshot/log.

## Files

[CONFIG] .audiagentic model-sources declaration for openrouter; config/providers/model-catalogs if a static fallback catalog is warranted by evidence. [REUSE] services/source_catalog.py (openrouter fetcher, filters, cache), model_source_* CRUD, manage_model_projection, ManagedFragmentRegistry. [MA19] OpenRouter free-marker + /models shape facts. [TEST] free-filter materialization, redaction canary, disable/prune roundtrip.

## Validation

1. Enabled openrouter source with env-ref key refreshes explicitly and never during reconcile. 2. Free filter provably selects only free models per recorded evidence. 3. Filtered models appear in Pi config via the projection family with source-scoped ownership; disabling the source prunes only its ids. 4. No secret in cache/results/timelines/config. 5. Focused source-catalog + projection suites pass.

## Effort & Risk

Mid, mostly test/evidence work. Risk: OpenRouter free-marker drift — mitigated by the recorded evidence anchor; filter is config so a marker change needs no code edit.

## Standards



## Notes

Depends on MO17. Successor consumers: vendor key injection item and the plan-agents free-model agent-profile item (both created 2026-07-18). Rationale recorded in RV635.

--- PROGRESS 2026-07-25 ---
Step 1 DONE: Evidence verified live against OpenRouter API. Free models carry `:free` suffix consistently (15 confirmed) AND pricing.prompt=="0". Response shape matches `_normalize_openai_models`. Record saved to docs/reference/PROVIDER_CAPABILITY_REFERENCE/endpoints/openrouter-free-marker.md.

Step 2 DONE: Model source declared in .audiagentic/config/model-sources.yaml — connector: openrouter, api-key-ref: env:OPENROUTER_API_KEY (env var only, never on disk), filter include: [":free"], model-discovery: list-api. Schema validation passes.

Step 3 DONE: Explicit refresh returns 345 models from OpenRouter API (freshness=fresh). Cached read (refresh=False) returns 345 models from cache without network call (freshness=cached).

Filter verification DONE: apply_model_filter with include [":free"] selects exactly 15 free models, zero leaks. All filtered model-ids end with :free.

Step 4 DONE (with caveat): Materialization through manage_model_projection:
- materialize_catalog_sources produces 15 entries for openrouter source with correct managed IDs (model-endpoints/openrouter/...:free)
- Pi provider: ALL 15 openrouter entries EXCLUDED from custom-entries because Pi has native vendor_key_injection for openrouter — this is BY DESIGN, Pi reads OPENROUTER_API_KEY directly and discovers models through its own mechanism. The :free filter is an AUDiaGentic catalog-level convention; Pi model selection happens via profile binding.
- OpenCode provider: 15 free models projected as custom-entries with source-scoped managed IDs — filter works correctly.

Secrets: Key stored in .audiagentic/secrets/test.env (git-ignored), loaded by conftest.py via dotenv. Key never written to source code.

Pre-existing import bug fixed: probes.py imported from stale path .config_reader → .config.config_reader.

--- GAP REVIEW 2026-07-28 (RV850) ---
Gap 1: No automated test coverage for OpenRouter-specific flow — no tests exercise refresh with free filter, redaction canary, or disable/prune roundtrip. Validation criteria list 5 checks but none are automated.
Gap 2: Item was never transitioned from pending to completed despite all steps marked DONE.
Gap 3: _normalize_openai_models drops the pricing field — correctly handled by choosing the *:free suffix filter path (zero code changes).

## Ledger Events

- chg_20260718_082713_opencode-model-configuration-c_4905

## Change Log

- 2026-07-26T11:03:01.513840+00:00 (updated-by): Updated (no visible changes)
- 2026-07-28T01:38:23.407238+00:00 (updated-by): Updated: section:steps, section:notes
- 2026-07-28T01:48:25.626243+00:00 (state-transition): State: pending → in_progress
- 2026-07-28T04:25:50.724636+00:00 (state-transition): State: in_progress → completed
- 2026-07-28T04:26:09.005194+00:00 (updated-by): Updated (no visible changes)
