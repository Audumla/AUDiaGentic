---
id: RO03
order: 20
plan: plan-role-capabilities
state: pending
validate-first: true
priority: P1
complexity: mid
created-by: codex
---

# Integrate role selection into job submission and shared-gateway execution

## Description

Allow MCP and event-triggered jobs to specify agent-role-id through thin protocol adapters. Resolve the role into the immutable launch manifest through the framework-neutral agents public API before dispatch, persist its identity/provenance with the request, and make context-isolated workers materialize that manifest. Preserve explicit agent-profile selection and existing workflow-profile semantics; MCP/event modules do not resolve roles or inspect component/provider internals.

## Steps

1. Extend submission contracts/events with optional agent-role-id and clear precedence rules. 2. Resolve and validate role intent at the authoritative boundary; carry the resolved manifest/fingerprint through SH02 rather than re-reading publisher process state. 3. Make SH06 workers enable only the declared components/features/MCPs and permissions. 4. Include safe role/capability identity in lifecycle events and timelines. 5. Add compatibility behavior for callers without a role during migration. 6. Test MCP, event, retry/resume, session, and cross-project paths.

## Files

agents gateway request/event schemas and APIs; shared-gateway execution envelope/worker integration; lifecycle events/timelines; tests

## Validation

A role-submitted job executes with exactly its resolved capability set from another process; retries/resume keep the same manifest; callers without a role follow the documented migration default; conflicting explicit profile/role requirements fail before dispatch. Architecture checks prove MCP/event adapters only translate and call the public role/gateway APIs; all role resolution and policy is independently testable without MCP.

## Effort & Risk

Mid, after contracts exist. The main risk is resolving twice at different boundaries and producing drift.

## Standards

AR25; Architecture Standards §§1,2,3,6,7; CREATING_A_COMPONENT §2 and §6.

## Notes

Depends on RO02 plus SH02, SH06, and AR25. The gateway transports and enforces the resolved manifest; it does not own role semantics.

## Ledger Events

- chg_20260717_012851_planned-the-migration-from-an_6657
- chg_20260717_013833_tightened-the-shared-gateway-r_7367
