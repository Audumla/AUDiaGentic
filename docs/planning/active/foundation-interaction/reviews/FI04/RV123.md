---
id: RV123
review-of: FI04
plan: plan-foundation-interaction
state: created
reviewed-by: Claude (critical implementation review, 2026-07-05)
reviewed-at: ''
---

# Not implemented: _print_progress/on_progress plumbing untouched; CLI and MCP push paths remain un-unified

## Notes



## Findings

FI04 required migrating commands/provider_prompt.py's _print_progress and services/lifecycle.py's on_progress callbacks onto foundation.interaction.push_status and deleting the ad hoc plumbing. None of that happened: provider_prompt.py still passes on_progress=_print_progress to reconcile_all_providers (visible in the CP00 diff hunks), services/lifecycle.py's on_progress/_emit chain is untouched, McpOutputBridge in component_server.py is untouched, and the delivery-mechanism decision (event-bus broadcast vs ctx-scoped sink) was never made. The only push_status caller added anywhere is the new harness lifecycle handler. The item's validation criteria (CLI output unchanged AFTER migration, MCP notifications unchanged AFTER migration) were never exercisable because there was no migration. FI04 was P3 consolidation and skipping it is defensible — but then it should have stayed pending, not been moved to completed/.

## Conclusion

Return FI04 to pending. It remains blocked on FI01's final shape (RV120): push_status currently falls back to logger, which would silently change CLI-visible progress output if call sites migrated now.
