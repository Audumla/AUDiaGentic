---
id: OB02
order: 0
plan: plan-observability-persistence
state: pending
breadth: ''
skill: high
---

# Implement CC43 shared resource-record mechanics — consolidate without losing domain ownership

## Description

Confirmed duplication exists across:

- Agent-jobs records
- Gateway request records
- Gateway session records

Shared mechanics include:

- ID generation
- Per-resource locks
- Schema validation
- Atomic writes
- List/read behavior
- Redacted errors
- State transitions
- Timeline emission

Recommended placement: a neutral foundation records/persistence capability — NOT automatically system and not necessarily observability.

Implement CC43's shared resource-record mechanics carefully. Preserve domain ownership when consolidating stores.

## Steps

['Audit duplication across agent-jobs, gateway request, and gateway session records', 'Design shared layer for ID generation, locks, schema validation, atomic writes, list/read, redacted errors, state transitions, timeline emission', 'Place in neutral foundation records/persistence capability (not system, not observability)', 'Preserve domain ownership: each component retains schema, workflow, paths, public error codes, record-specific fields, component-specific transition policy', 'Centralize cross-process locking later through same seam']

## Detailed Solution & Technical Design

## Code Samples & Guidance

## Files

['src/audiagentic/foundation/', 'src/audiagentic/components/']

## Validation

Shared layer owns mechanics. Each component retains schema, workflow, paths, error codes, and transition policy.

## Effort & Risk

## Standards

## Notes

- **Overlap with CC43**: CC43 (code-cleanup) covers the exact same work — extracting a foundation ResourceRecordStore from agent_jobs/jobs_store, agents/agents_gateway_store, and agents/agents_gateway_sessions_store. Both items describe: shared mechanics (ID generation, per-resource locks, schema validation, atomic writes, list/read behavior, redacted errors, state transitions, timeline emission), domain ownership preservation (schema, workflow, paths, error codes remain component-owned), and the cross-process locking seam for future SH07/PR06 integration. Coordinate implementation — these are essentially the same work.
- **CC43's additional detail**: CC43 includes specific migration order (sessions_store first, then gateway_store, then jobs_store) and specific file references that should be incorporated.

## Priority

P1
