---
id: AG24
order: 24
plan: agents
state: pending
created-at: '2026-08-24T01:58:29.559924+00:00'
breadth: ''
skill: advanced
created-by: agent
work: M
---

# Finalize global Agents catalog wire schema and migration

## Description

Own the operational cutover of the machine-global .audiagentic/config/agents.yaml wire contract after the canonical immutable models are stable. Define one versioned schema, migrate the existing v2 document, remove dual wire formats and legacy fallback authority, and preserve admission CAS/digest semantics.

## Steps

1. Specify the target versioned global Agents catalog wire schema, canonical key spelling, required fields, reference rules, and explicit obsolete/unknown-version behavior.\n2. Implement an idempotent v2-to-target migration for the machine-global catalog; do not make project-local files or gateway-profiles.yaml runtime fallbacks.\n3. Update repository load/save/replace and CAS digest handling to use canonical serialization, with golden round-trip fixtures.\n4. Remove dual wire-format acceptance once migration is complete, retaining only an explicit one-time migration seam.\n5. Update management/configuration surfaces and architecture scans so the global catalog is the only hosted authority.

## Detailed Solution & Technical Design

Keep domain value-object immutability and per-type fingerprints in SU03. This item owns deployment compatibility: version bump, migration, canonical serialization, idempotence, CAS/digest behavior, and fail-closed handling of malformed, obsolete, ambiguous, or unknown documents. The hosted gateway must read exactly one machine-global catalog authority.

## Code Samples & Guidance



## Files

src/audiagentic/components/agents/configuration/contracts.py; repository/validation/migration/global-catalog modules; global configuration management surfaces; canonical schema/fixtures; tests/unit/agents/; hosted gateway integration tests.

## Validation

Golden serialization and canonical round-trip tests; v2 migration and migration-idempotence tests; CAS/digest stability across equivalent ordering; malformed/unknown/obsolete-version rejection; proof gateway-profiles.yaml and project-local agent files are ignored as runtime authority; focused configuration/gateway suites; ruff; pyright; git diff --check.

## Effort & Risk

Medium blast radius with deployment risk. The migration must be recoverable and idempotent, and must not silently reinterpret an existing catalog.

## Standards

arch-standards §§1, 3, 4, 6, 7, 8; observability-standards for migration and authority-change records.

## Acceptance Criteria

- One target versioned global agents.yaml wire schema is documented and enforced; no dual runtime wire formats remain.
- v2 migration is idempotent, preserves semantic content, and is the only supported legacy conversion path.
- Canonical serialization, CAS/digest behavior, unknown/obsolete-version rejection, and global-only hosted authority are covered by tests.

## Notes

Dependency order: AG16/RV983 → SU02 → SU03 → SU04 → this wire cutover. Keep AG15/AG18 and AS86–AS94 out of this schema-cleanup path. No second runtime authority is permitted.

## Change Log

- 2026-08-24T01:58:29.559924+00:00 (created-by): Created by agent
- 2026-08-24T01:59:05.918572+00:00 (updated-by): Updated: section:acceptance_criteria
