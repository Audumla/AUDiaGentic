---
id: PT01
order: 0
plan: plan-ptm-telemetry
state: pending
validate-first: true
priority: P1
complexity: complex
created-by: MO16
---

# Provider Telemetry Capability Model

## Description

Design and implement a multi-dimensional provider telemetry model that replaces the binary 'supports credits' flag. Each provider declares which telemetry categories it can expose: balance (account credits), quota (rate limits), subscription (usage windows), session (remaining context/tokens), runtime (GPU/VRAM/local metrics), cost_estimation, and observability (streaming token counts, billing events). This becomes the foundation for cross-provider scheduling and budget management.

## Steps

['Define telemetry data model and add to provider-descriptor schema (provider-telemetry.schema.json)', 'Create ProviderTelemetry dataclass in descriptors/base.py with frozen=True', 'Wire telemetry field into PROVIDER_SPEC loader with _build_telemetry converter', 'Populate telemetry blocks for all 16 provider YAML files using verified evidence', 'Implement telemetry query service module (audiagentic/components/providers/services/telemetry.py)', 'Add MCP tools: get_provider_balance, get_provider_quota, get_session_telemetry', 'Integrate telemetry into scheduling logic in the gateway or dispatcher', 'Document the telemetry model in AGENTS.md or a dedicated provider-telemetry doc']

## Files

['src/audiagentic/components/providers/contracts/provider-telemetry.schema.json', 'src/audiagentic/components/providers/descriptors/base.py', 'src/audiagentic/components/providers/descriptors/loader.py', 'src/audiagentic/config/providers/*.yaml', 'src/audiagentic/components/providers/services/telemetry.py', 'docs/reference/PROVIDER_TELEMETRY_CAPABILITIES.md']

## Validation

['Schema validates against all provider YAML files without errors', "Each provider's telemetry block has only fields that are verifiably supported — no guesses", 'Telemetry queries return structured data or None (never crash on unsupported category)', 'Provider descriptor round-trips: load YAML → ProviderDescriptor → serialize → load YAML']

## Effort & Risk

{'effort': 'high - spans schema, loader, 16 provider YAMLs, service layer, MCP tools', 'risk': 'medium - provider API endpoints change; scraping Claude may break; token counting inaccuracy'}

## Standards



## Notes



## Ledger Events


