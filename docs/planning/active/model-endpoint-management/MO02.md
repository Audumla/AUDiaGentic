---
id: MO02
order: 4
plan: plan-model-endpoint-management
state: pending
validate-first: true
priority: P1
complexity: complex
created-by: claude
---

# Bind model-endpoint kind onto shared reconcile + capability recipes

## Description

Add provider-owned model endpoint sync by binding a new model kind onto the consolidated managed-config machinery from MO06 — NOT by cloning services/mcp.py into services/models.py. The reconcile engine (foundation/toolchains/fragments.py, payload is Any), the generalized ManagedFragmentRegistry, and the sync/reload core already exist after MO06; this item supplies the model-specific desired-entry builder, registers model management as a provider capability recipe (reusing ProviderCapabilityRecipe/Registry for status/dry_run/lifecycle/provenance/action_needed), and wires the reconcile trigger. Env-projection and extension-storage providers are handled by different vehicles (see steps), not the fragment reconciler.

## Steps

1. Add model endpoint dataclasses/helpers producing (name, payload) desired entries keyed by managed_id = model-endpoints/<endpoint-id>.
2. Expose thin model APIs (sync_managed_provider_models, sync_managed_provider_models_subset, list_provider_models_config, reload_provider_models) as wrappers over the shared sync_managed_config / reload_provider_config from MO06 — no reimplementation of the reconcile loop.
3. Instantiate the generalized ManagedFragmentRegistry at .audiagentic/runtime/providers/managed-model-endpoints.json (a second instance, not a second module).
4. Register model-endpoint management as a provider capability recipe (ProviderRecipeKind for model config) so status/dry_run/lifecycle/provenance/action_needed are reused; bind NoAutomationRecipe for manual/blocked providers.
5. Handle env-projection providers (OpenHands/Aider) as a launch-env contribution capability recipe, NOT via reconcile_fragments.
6. Add a sibling _sync_provider_models call in reconcile.py::reconcile_provider and register model-sync action handlers in the existing dispatch-table style (lsp_projection _ACTION_HANDLERS pattern).
7. Add provider status fields (model-config-supported, managed-model-count, model-config-path, refresh mode, collisions, action-needed reload text).

## Files

[COMPONENT:providers] src/audiagentic/components/providers/services/models.py — desired-entry builder + thin sync wrappers over foundation sync_managed_config (MO06)
[COMPONENT:providers] src/audiagentic/components/providers/services/connector_render.py (NEW) — (connector -> renderer) registry; renders one endpoint into a provider's native form. NO if/elif on connector/provider (§2)
[COMPONENT:providers] src/audiagentic/components/providers/services/reconcile.py — sibling _sync_provider_models call in reconcile_provider
[COMPONENT:providers] src/audiagentic/components/providers/services/recipes.py — register model-config ProviderRecipeKind; bind NoAutomationRecipe for manual/blocked
[COMPONENT:providers] src/audiagentic/components/providers/services/status.py — model-config status fields
[COMPONENT:providers] src/audiagentic/components/providers/providers_api.py, providers_mcp.py — expose model ops after service tests green
[FOUNDATION] consumes foundation sync_managed_config + ManagedFragmentRegistry (MO06); env-projection reuses the execution/launch seam, not reconcile_fragments
[COMPONENT:providers instance] .audiagentic/runtime/providers/managed-model-endpoints.json — second instance of the foundation registry (not a new module)
[CONFIG] src/audiagentic/config/components/providers/error-resolutions.yaml — model-endpoint remediation text (§8: resolution only, not the raised message)
[TEST] tests/unit/providers/test_provider_status.py, test_model_catalog.py, connector-render tests, reconcile/model-sync tests

## Validation

1. Unit tests for ownership preserve/update/remove/rename behavior.
2. Unit tests for collisions with unmanaged provider entries.
3. Unit tests for status output and reload-required/file-watch behavior.
4. Unit test proving local-openai catalog output validates.
5. Dry-run and rollback tests for model sync, including user-global write preview.
6. Run focused provider unit tests: model catalog, provider config, status, MCP sync analogs.

## Effort & Risk

Complex. Primary risk is re-mirroring MCP sync instead of reusing the MO06 foundation core, forcing env-projection/extension-storage through the reconciler, or branching on connector/provider with if/elif (§2 violation). Keep the model service to desired-entry building + capability-recipe registration only. local-openai catalog fix moved to MO05.

## Standards

- arch-standards — §1 providers may consume foundation sync primitives but foundation names stay domain-neutral; §2 connector/provider dispatch must be registry/YAML driven with no if/elif on provider_id or connector; §8 use canonical VAL-MEP-*/CON-MEP-* errors and keep remediation in error-resolutions.yaml; §9 module logger and no secret logging; §10 no cloned reconcile loop or parallel model-sync API; §11 use shared lazy registry utility.
- component-creation — provider component owns downstream projection/status surfaces; providers MCP exposure waits until service tests pass.
- observability-standards — model sync mutates durable provider config, so emit redacted timeline events for create/update/remove/collision/reload outcomes.

## Notes

Depends on MO06 for implementation. Incorporates RV202, RV207, RV209, RV214, RV217. External review assessment 2026-07-09: accepted. `ProviderRecipeKind` gets an explicit model-config/model-endpoints value, but recipes stay lifecycle/status/dry-run wrappers; structured entry reconciliation belongs to the shared managed-config core. Existing provider status is extended with model-config fields instead of adding a parallel status API. Desired-entry rendering is connector-aware through a renderer registry and provider YAML, never provider/connector if/elif chains. Env-projection (OpenHands/Aider) remains a launch-env contribution recipe, not fragment reconciliation. `NoAutomationRecipe` remains reserved for manual/unsupported/blocked providers.
