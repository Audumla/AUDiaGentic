---
id: MO13
order: 7
plan: plan-model-endpoint-management
state: pending
validate-first: true
priority: P1
complexity: mid
created-by: codex
---

# Implement Codex and OpenHands model projections

## Description

Implement bounded TOML/launch-env slice after MO02/MO06: Codex structured model_providers and OpenHands structured [llm] or blocked env fallback.

## Steps

1. Verify installed Codex project .codex/config.toml honors model_providers, model, model_provider, wire_api; record version/evidence. If false, remain blocked—no user-global write in this item.
2. Extend existing Codex TOML merge primitive with model reader/writer/remover; share file transaction with MCP/LSP and preserve unmanaged tables.
3. Verify OpenHands .openhands/config.toml [llm] keys and execution status. If structured contract verified, add reader/writer/remover; otherwise bind NoAutomationRecipe. Env contribution is enabled only when real execution adapter consumes it.
4. Declare ManagedConfigSpec/connector support in provider YAML only for verified paths.
5. Add golden, cross-kind non-clobber, collision, reload/action-needed tests.

## Files

[COMPONENT:providers] adapters/{codex,openhands}/ model config modules
[CONFIG] config/providers/{codex,openhands}.yaml
[TEST] tests/unit/providers/ adapter TOML/env tests
[DOCS] adapter READMEs + reference evidence links

## Validation

1. Codex project-scope verification recorded; no home config write.
2. Sequential MCP/LSP/model mutations preserve every unrelated section.
3. Owned model update/remove preserves unmanaged model_providers.
4. OpenHands unsupported/unverified path reports blocked; stub execution never receives active env projection.
5. Secret canary absent from results/files unless intentional credential provisioning contract explicitly requires value.

## Effort & Risk

Mid. Both use TOML but distinct semantics. Installed-tool gates prevent speculative writers.

## Standards

- arch-standards §1 provider adapter ownership; §2 descriptor declarations; §10 atomic same-file migration; §15 output redaction.
- component-creation provider-native rendering stays adapter-local.

## Notes

Depends on MO01, MO02, MO06, MO15 (env contribution path for OpenHands consumes the MO15 launch-env seam), and relevant MO09 installed-tool evidence. Does not block MO03 Pi/OpenCode slice. TRIAGE DECISIONS (RV320): (a) both adapters BLOCK on their MO09 evidence cells — Codex on 'project-scope model_providers honored (version X)', OpenHands on '[llm] key semantics (version X)'; start neither writer before the cell is verified. (b) If Codex project scope is not verified, bind NoAutomationRecipe with the exact blocker text 'project-scope model_providers honor unknown for installed version <X.Y.Z>' — no user-global write in this item under any outcome. (c) OpenHands structured-file writer and env contribution are INDEPENDENT projection modes: if [llm] is verified while execution remains stub, implement the writer and leave the env contribution inert (MO15 rule) — do not couple them. (d) Golden tests must cover TOML merge semantics explicitly: preserve unmanaged tables/keys across sequential MCP/LSP/model mutations, collision detection on same-named tables, and single-active-model semantics for [llm].

## Ledger Events

- chg_20260713_001958_completed-the-implementation-r_7375
