---
id: MO14
order: 50
plan: plan-model-endpoint-management
state: pending
validate-first: true
priority: P2
complexity: mid
created-by: codex
---

# Validate Qwen and Goose model config support

## Description

Validate and implement Qwen's actual multi-auth modelProviders surface, then bound Goose. Installed Qwen v0.13.1 proves auth types and modelProviders exist, but writer still requires isolated path/scope/schema/merge/reload verification. Keep Goose manual unless exact contract and real execution exist.

## Steps

1. Verify installed Qwen settings path/scope, modelProviders shape, selection pointer, merge/reload behavior; record version/evidence.
2. If verified, implement Qwen reader/writer/remover through MO06 core and descriptor declaration. If any contract missing, bind NoAutomationRecipe with exact blocker.
3. Verify Goose config path/schema/reload and current execution bridge. Do not infer model config from existing MCP .goose/config.yaml.
4. Goose automation requires both verified config and non-stub execution; otherwise status remains blocked/manual.
5. Add golden tests only for verified automation; add status tests for every blocked branch.
6. RV355 QWEN CORRECTION: use installed v0.13.1 package as version-matched primary evidence. Characterize `modelProviders` for auth types openai, anthropic, gemini and vertex-ai; exact settings path and precedence; entry fields (`id`, `baseUrl`, API-key env reference/override, model selection); duplicate behavior; cross-authType switching; persistence; reload. Treat OpenRouter as OpenAI-compatible route only after isolated base-url/model test. Never infer schema solely from bundled symbol names.
7. Reconcile MO09 Qwen evidence/matrix with this characterization before declaring capability. Provider YAML exposes only verified operations/states. ACP symbols observed in installed package belong MA18 transport research, not Qwen model writer.

## Files

[COMPONENT:providers] adapters/{qwen,goose}/ verified model modules or NoAutomationRecipe bindings
[CONFIG] config/providers/{qwen,goose}.yaml
[TEST] tests/unit/providers/ adapter/status tests
[DOCS] evidence + adapter README links

## Validation

1. Evidence contains tool version/path/schema/reload facts.
2. Qwen roundtrip preserves unmanaged entries and reports collisions if automated.
3. Goose cannot report auto while execution stub or contract unknown.
4. No guessed FILL value enters provider YAML.
4. Qwen v0.13.1 evidence records help auth-type choices and installed-artifact modelProviders behavior without credentials. Anthropic/Gemini are not labelled unsupported.
5. Writer shape/path/scope/merge/reload each independently verified; partial proof yields blocked status, not guessed serializer.
6. OpenRouter compatibility and ACP transport remain separate facts and scopes.

## Effort & Risk

Mid, validate-first. Successful outcome may be honest blocked status.

## Standards

- arch-standards §2 config-driven support; §4 no hardcoded local paths; §10 tested migration.
- component-creation adapter-local mechanics, shared provider status surface.

## Notes

Depends on MO01, MO02, MO06, and MO09 evidence. Blocked/manual is valid completion when tool contracts remain unverified — do NOT invent writers to satisfy this item (RV321). PINNED BLOCKER TEXT (RV321): if unverified, bind NoAutomationRecipe with exactly — Qwen: 'settings path/scope/modelProviders shape unverified for installed version <X>'; Goose: 'config contract and non-stub execution both required; neither verified (installed version <X>)'. If a tool is not installed, the evidence record states blocked with the probe result and exact missing information. Status tests must prove the blocked state surfaces with that text (validation 3). Self-contained: no other MO item blocks on this one.

IMPLEMENTATION ORDER: Block D-MO3 after MO09 evidence, rebased MO02, MO06 core, MA16/MA17. Validate first; blocked/manual is valid completion. No guessed config surface.

RV355: MO09's OpenAI-only Qwen conclusion is invalid. Installed tool proves multi-auth/modelProviders machinery. MO14 remains validate-first: this raises probability of automation but does not authorize writer until path/scope/merge/reload contract is proven.

ACP/EXECUTION HANDOFF 2026-07-14: provider model projection is a MA16 capability operation resolved through MA17 provider capability gateway. ACP/CLI/HTTP are separate agent-execution transport manifestations resolved through MA17 execution resolver. This item must not import `foundation.execution.acp`, call agents gateway, choose execution transport, create session/event storage, or add provider/transport string branches. It may declare evidence-backed model/config capabilities only. Any execution proof used as evidence must remain fact-specific and must not imply model-writer support.

## Ledger Events

- chg_20260713_001958_completed-the-implementation-r_7375
- chg_20260714_014514_made-remaining-mamo-plans-imp_1251
