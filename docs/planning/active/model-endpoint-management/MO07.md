---
id: MO07
order: 70
plan: plan-model-endpoint-management
state: pending
validate-first: true
priority: P1
complexity: complex
created-by: claude
---

# Enable remote-account model sources (key injection + filtered custom entries)

## Description

Implement remote-account projection after a separate source-catalog item supplies normalized discovered models. This item owns projection selection, native key injection, custom-entry materialization, disable/prune, alias validation, and status; it does not implement network fetch/cache logic.

## Steps

1. Consume normalized SourceCatalogResult from the source-catalog prerequisite; no network calls in model_sources.py.
2. Select projection from descriptor data only: vendor-key-injection exact vendor key first, else supported connector custom-entries, else none. Return a typed ProjectionDecision; no provider/vendor/source-class branches.
3. Resolve api-key-ref EXCLUSIVELY through MO15's secret-ref resolver + launch-env contribution seam (RV315 resolved: the prerequisite item now exists — MO15 delivers resolve_secret_ref, ProviderRecipeKind.LAUNCH_ENV, and the launch-time injection path). Do not add local env parsing. Resolved key values exist only inside writer/launch invocation, per MO15's redaction guarantee.
4. Native env injection contributes {env-name: secret-ref} via the MO15 launch-env seam (deferred resolution at spawn); native config injection resolves via MO15 inside the writer call and emits the connectivity managed entry. Custom entries filter normalized catalog and feed MO02 MaterializedModelEntry.
5. Disable is transactional by ownership kind: prune config/custom entries and remove launch-env contribution; failures report exact residual managed ids/action_needed and never claim disabled projection complete.
6. Alias/default validation runs after materialization and before provider config write. VAL-MEP-003 includes source/model ids only.
7. Status consumes projection decision + SourceCatalogResult: mode, enabled, count, freshness/staleness, last error class, residual ownership, action_needed. No secret refs/values.
9. RV353 EVIDENCE GATE: do not consume the current provider×vendor matrix until reopened MO09 completes correction. Bind declarations only from fact-specific terminal anchors. Pi native-vendor injection and custom-entry projection are separate modes; Qwen auth types/modelProviders replace the false OpenAI-only assumption; Codex config parsing, OpenCode credential listing, and OpenHands env recognition are not execution proof. Any affected pair still blocked/unverified remains unprojected.

## Files

[COMPONENT:providers] src/audiagentic/components/providers/services/model_sources.py (NEW) — remote-account materializer + per-(provider, source) projection-mode selection from YAML declarations, feeding the MO02 desired-entry builder input
[CONFIG] src/audiagentic/config/providers/model-catalogs/*.yaml (NEW) — curated vendor model families (data, config-over-code)
[CONFIG] src/audiagentic/config/providers/*.yaml — vendor-key-injection declarations (populated only from MO09-verified matrix cells)
[CONTRACT] reuses model-sources.schema.json from MO01 — no schema change expected
[COMPONENT:providers instance] .audiagentic/runtime/providers/source-catalogs/*.json — cached list-api results
[CONFIG] src/audiagentic/config/components/providers/error-resolutions.yaml — external-connectivity remediation text per MO08 classification
[DOCS] docs/reference/PROVIDER_MODEL_ENDPOINT_CAPABILITIES.md — keep mapping table + provider×vendor matrix current
[TEST] tests/unit/providers/ — materializer/projection-mode tests, key-injection env + config tests (incl. no-key-in-output assertions), model-filter tests, catalog degradation tests, alias-vs-disabled-source tests
[DEPENDENCY] normalized source catalog API delivered by dedicated source-catalog item

## Validation

1. Projection decision table tests cover key injection/custom/none and undeclared pairs without name branches.
2. Secret resolver spy proves key value appears only in launch/writer call and nowhere in result, registry, status, log, or timeline.
3. Disable failure test reports residual ownership and action_needed; successful disable removes only source-owned projections.
4. Custom entries consume normalized catalog/filter output and use MO02 builder/core—no second reconciliation path.
5. Alias/default validation occurs before provider-file mutation.
6. Architecture tests forbid network calls and adapter/provider imports in model_sources.py.
8. No declaration originates from pre-RV353 broad provider anchors. Each automatic pair traces to corrected fact-specific evidence; known-false Pi/Qwen classifications cannot appear in generated catalog or runtime projection.

## Effort & Risk

Complex. Risks: re-deciding taxonomy at implementation time (forbidden — the reference doc is authoritative); guessing unverified matrix cells instead of consuming MO09 output; letting catalog refresh failures fail sync; leaking key values into fragments/status/logs; if/elif on source-class, vendor, or provider instead of declaration-driven selection. Network access happens only during explicit list-api refresh — never during descriptor load or ordinary reconcile of unchanged state.

## Standards

- arch-standards — §2 projection-mode selection is DECLARED dispatch: provider YAML vendor-key-injection/supported-connectors + the connector fetcher registry drive all behavior; no if/elif on source-class, vendor, or provider id. §8 secret-safe boundaries: key VALUES resolve from api-key-ref only at write/launch time and never appear in fragments text beyond the target field, registries, status, timelines, logs, or error details; error codes are canonical VAL-MEP-*/CON-MEP-* registered before use. External failures (unreachable/401/429/contract) are classified per the MO08 guideline — transient vs configuration vs authorization vs contract — and best-effort refresh NEVER fails the enclosing sync. §10 one materializer, no parallel sync path — remote-account feeds the MO02 builder input.
- component-creation — providers own sources/catalogs/projection; agents keep provider/model binding only; curated catalogs are data files under config (config-over-code).
- observability-standards — cached-catalog degradation emits a redacted operational record/warning including failure class and cache staleness (per MO08's observability note); per-source status fields expose discovery/projection/refresh outcomes without any key material. (RV276)

## Notes

RV401 REBASE: depends on MO02 explicit projection handlers, MO12, MO15, MO09 evidence, and MA17—not full MA16 catalog. Provider/vendor projection facts remain config-driven and deeply evidenced, but only explicit handler registrations execute. Do not create Python enums/types for projection/support states when logic can use provider config strings or explicit absence. Execution transport findings remain separate soft knowledge.

## Ledger Events

- chg_20260710_080326_extended-the-model-endpoint-pl_1780
- chg_20260710_081831_simplified-the-model-source-de_4938
- chg_20260712_104854_the-external-architecture-revi_9275
- chg_20260713_001958_completed-the-implementation-r_7375
- chg_20260714_014514_made-remaining-mamo-plans-imp_1251
- chg_20260714_122529_kept-deep-provider-capability_7070
