---
id: GP04
order: 4
plan: gpt-auto-conversation
state: pending
created-at: '2026-08-15T11:22:30.218731+00:00'
breadth: ''
skill: advanced
created-by: agent
work: L
---

# Harden GPT-auto lifecycle, diagnostics, and agent testing operations

## Description

Document and complete the GPT-auto reliability work carried out across the CDP adapter, persistent conversation/session lifecycle, gateway status projection, and live operational test process. The item is the durable runbook for diagnosing a retained ChatGPT project/chat lineage, recovering an unresolved prompt without resubmitting side effects, and continuing multi-stage reviews through one long-lived agent session.

Work completed so far:
- Added durable unresolved-turn checkpoints with provider session URL, prompt/assistant markers, prompt-text digest, baseline counts, and recovery state.
- Fixed checkpoint persistence to use ChatSnapshot.user_count/assistant_count rather than nonexistent user_message_count/assistant_message_count.
- Added lazy READY admission recovery and quiescence gating; failed/cancelled turns retain the tab by default.
- Added prompt-ID-first reconciliation with bounded prompt-text fallback; missing IDs are evidence gaps, not automatic proof of failure.
- Added request-status reconciliation so provider-turn-pending is recovered from the durable session checkpoint.
- Classified provider surface probe/adapter-resolution failures as unavailable with typed reasons.
- Added allow-listed GPT-auto error.details projection so recovery reasons and safe next-step evidence survive gateway persistence without prompt contents, tracebacks, or secrets.
- Restarted the shared gateway safely, closed the orphaned durable source record, and resumed the same ChatGPT project/chat as successor session ses_c8e35e2ef75c42e3; the retained provider chat URL and turn markers were preserved.
- Pushed implementation to branch agent-surface-refactor at commit 4cd32b1c1.

## Steps

- Keep one gateway process and one live GPT-auto session per review lineage; never create parallel review sessions for polling or recovery.
- Before any new GPT prompt, inspect gateway overview, session list, request list, and the target request status; distinguish queued/running/provider-turn-pending from terminal failure.
- For a timed-out or ambiguous turn, do not resubmit automatically. Reconcile the existing provider conversation using prompt marker, assistant marker, prompt-text digest, prompt text, URL/project/chat identity, and DOM quiescence evidence.
- CONCRETE SAFE GATEWAY RESTART PROCEDURE (validated live, 2026-08-16): (1) locate the machine service record at `<user home>/.audiagentic/services/machine/agent-execution-gateway/default/service.json` -- read its `endpoint.address` (e.g. 127.0.0.1:8765) and confirm `state`/`process.pid`; the sibling `auth.token` file in the same directory is the token. (2) set `AUDIAGENTIC_GATEWAY_ENDPOINT=http://<address>` and `AUDIAGENTIC_GATEWAY_TOKEN_FILE=<path to auth.token>` in the shell, then run `audiagentic gateway stop` (no flag) first -- it correctly refuses with CON-AGLC-001 if sessions are still live/non-quiescent, which is the intended safety check; only add `--force` once you've confirmed it's safe/necessary to interrupt live work. (3) confirm the old PID has actually exited (process no longer found). (4) relaunch by calling `start_or_attach_gateway()` from `audiagentic.components.agents.gateway.service.bootstrap` in a fresh short-lived Python process (this is the same function `get_gateway_client()`'s 'automatic' mode calls internally on a cache miss) -- do NOT use the foreground-blocking `audiagentic gateway serve` CLI form for this. (5) verify via `agent_task_gateway_overview` that `runtime-fingerprint.process-instance-id`/`started-at` reflect the new process. Code changes to the adapter (chat.py/runtime.py/etc) do NOT take effect in an already-running gateway process -- a restart via this procedure is required after such changes for them to actually be live.
- KNOWN GAP (see GP06): after a restart, any MCP client process that had already cached a gateway connection (via get_gateway_client()'s per-process _CLIENT cache) will NOT automatically recover -- it keeps failing with NET-AGSV-002 indefinitely since the cache is never invalidated/retried on failure. Until GP06's fix lands, a gateway restart may require manually re-running the relaunch step (4) above even when a client appears to already exist, and any other concurrent MCP client (including other projects/agents sharing this machine's gateway) will need their own client to somehow re-attach -- currently only achievable by that client happening to make its very first-ever gateway call after the restart, or by the same manual bootstrap procedure.
- If the gateway dies, verify the exact process and durable session state, restart only the shared gateway process (per the procedure above), close only an orphaned non-live source record, and call session_resume with the stored identity/execution fingerprints; retain the browser tab.
- After each substantive fix, run focused unit/lifecycle tests, lint/type checks, commit and push the branch, then use the same GPT session for a narrow adversarial review of the pushed commit -- attempt to RESUME the same session_id first (this is exactly what PC14 exists to make reliable) rather than opening a new chat, even across a gateway restart; only fall back to a new session if resume is genuinely unavailable.
- Use live monitoring as a state machine: provider-turn-pending means the provider may still be processing; only explicit terminal state plus response evidence permits a new turn.
- When a request fails, capture request-id, session-id, attempt, error code/message/details, recovery reason, watchdog state, last transition, provider metadata, and suggested next action; never collapse these into a generic unexplained error.
- Close the item only when deterministic simulation coverage, live submit/reconcile/resume coverage, tab-retention behavior, gateway restart recovery, and multi-stage same-session review all pass.

## Detailed Solution & Technical Design

Layering and state invariants:
- GPT-auto adapter owns DOM/CDP observation, prompt proof, response policy, prompt/assistant correlation, quiescence, and provider-specific error diagnostics.
- PersistentChat owns project/chat binding, page ownership, unresolved-turn checkpointing, recovery admission, and READY/RECOVERING transitions.
- Session runtime/transport owns durable provider binding and lazy session recovery; gateway owns queue, request lifecycle, persistence, watchdog observations, and shared process coordination.
- Project/session identity is the ChatGPT project URL plus conversation/chat URL. A prompt/response cycle is a turn within that conversation and may have a provider message id; the id is optional and must not be treated as the session id.
- READY is admitted only after page binding is valid, provider state is quiescent across stable observations, and no unresolved side effect remains. RECOVERING is alive but not prompt-admissible until ensure_ready proves the invariant.
- EXT-GPTAUTO-002 means response policy timeout after submission was confirmed; retain the tab and do not resubmit.
- EXT-GPTAUTO-003 means provider submission/DOM proof or provider failure policy could not be confirmed; inspect structured evidence and treat side effects as ambiguous.
- EXT-GPTAUTO-004 means a prior side effect may have started but outcome is unproven; reconcile lazily and report recovery-reason/recovery-details. It is not evidence that the tab or conversation is lost.
- RES/CON gateway errors describe session/request lifecycle, not provider DOM state; use the stored provider binding and close-reason to decide resume versus fresh session.
- Public errors expose only bounded allow-listed GPT-auto evidence; prompt bodies, exception messages, stdout/stderr, and secrets remain private.

## Code Samples & Guidance

Operational status fields to preserve:
request-id, session-id, state, provider-turn-pending, latest-transition, attempt-epoch, watchdog-state, watchdog-reason, error.code, error.message, error.details.failure-reason, error.details.recovery-reason, error.details.recovery-details, metadata.provider-session-id, metadata.chat-url, metadata.prompt-message-id, metadata.assistant-message-id, metadata.unresolved-turn-pending.

Single-session recovery outline:
1. agent_task_gateway_overview({})
2. agent_task_session_list({state:"active"})
3. agent_task_status({request_id})
4. If source is non-live/terminal, agent_task_session_resume(source_session_id, stored identity/execution fingerprints, new control id)
5. agent_task_submit(agent_id:"gpt-auto-agent", session_id:<successor>, session_keep_alive:true, timeout_seconds:3600, one focused prompt)
6. Poll status; do not infer completion from watchdog inactivity alone.

## Files

- src/audiagentic/components/providers/adapters/gpt_auto/chat.py
- src/audiagentic/components/providers/adapters/gpt_auto/turn.py
- src/audiagentic/components/agents/gateway/store/_records.py
- src/audiagentic/components/agents/gateway/session/
- src/audiagentic/foundation/contracts/schemas/agent-execution-record.schema.json
- tests/gpt_auto/
- tests/unit/agents/
- docs/releases/CURRENT_RELEASE_LEDGER.ndjson

## Validation

Deterministic:
- Run the GPT-auto lifecycle, turn, snapshot, CDP bridge, gateway session, queue, and store suites.
- Include positive and negative cases for prompt ID present/absent/stale, unique/ambiguous prompt-text fallback, provider still generating, not-quiescent, missing completion evidence, stale assistant, duplicate submit prevention, cancellation, tab loss, CDP disconnect, late response, gateway restart, and durable status projection.
- Assert no leaked page handles/sessions/tasks/locks and exact transition/error details.

Live:
- Use one shared gateway process and one GPT-auto session.
- Submit a short review, observe running/provider-turn-pending, wait for terminal response, then submit one follow-up in the same conversation.
- Close only the conversation tab while idle, resume the durable session, and verify the same project/chat URL and no duplicate tab.
- Force a gateway-only restart, verify the durable source becomes orphaned, close the orphan record, resume with stored fingerprints, and confirm the retained tab/conversation can be reconciled.
- Push the tested commit before each GPT review and record request/session IDs, timings, status transitions, and any provider evidence.
- Never use a second concurrent review session to work around a slow request.

## Effort & Risk

High. Browser DOM behavior and provider completion evidence are external and can be ambiguous. Recovery must remain conservative: preserve tabs, never duplicate an attempted prompt, and fail with actionable evidence when proof is unavailable.

## Standards

AS72 provider surface/error contract; GP03 persistent GPT-auto conversation lifecycle; shared gateway single-process/session coordination; sparse public status and redaction rules; transition-driven state management.

## Acceptance Criteria



## Notes

2026-08-16 (cont'd) FOLLOW-UP BUG (not yet fixed): user reported closing the ChatGPT tab for the live audiagentic-project session causes it to immediately reopen, but the reopened tab lands on the bare project URL (not the specific chat) and never resumes/submits -- the turn can then never resolve. reconcile() in chat.py has two navigate branches: lines ~835-845 navigate to self.chat_url (the exact conversation) when provider_session_id is set but no matching page is found; lines ~879-891 navigate to self.project_url (bare project root) only when provider_session_id/target_id are unset and there's no unresolved_turn_pending. The live session at the time (ses_49bffe11ab954349) DID have a provider_session_id/chat_url in its durable metadata, so in principle the first branch should have fired -- landing on project root instead points at either (a) a second/earlier-lifecycle PersistentChat instance in the shared runtime for the same project without a bound conversation yet, or (b) chat_url going stale/None by the time reconcile() ran. Needs live DOM/in-memory instrumentation to pin down; not chased further because it's a separate investigation from GP05 test-profile live testing. Worth a dedicated review pass.

2026-08-16 (cont'd) CRITICAL CONFIG-PATH CORRECTION for future sessions: `.audiagentic/config/agent-definitions.yaml` and `.audiagentic/config/execution-profiles.yaml` are LEGACY, migration-only files (see agents_paths.py's execution_profiles_path/agent_definitions_path docstrings: "used only by one-time migration") -- editing them has ZERO effect on the live gateway. The real, live agent/execution-profile document is the single merged `.audiagentic/config/agents.yaml` (v2 contract: top-level `execution_profiles:` dict keyed by profile_id, `agents:` dict keyed by agent_id, plus `prompts:`/`roles:`). agents_config_path() in agents_paths.py is the authoritative resolver. Also confirmed: the entire `.audiagentic/config/` tree is project-instance runtime state and is gitignored (not committed) -- only `src/audiagentic/config/providers/*.yaml` (the packaged provider descriptors) are real committed source. Wasted significant effort this session editing the legacy files before catching this via a direct load_agent_definitions()/get_agent_definition() probe against the real code path -- always verify against the actual resolver function, not by pattern-matching an old file's shape.

2026-08-16 (cont'd) REAL LIVE INCIDENT, caused by this session's own work: the gpt-auto TurnConfig schema gained two new required fields (submission-proof-progress-lease-seconds/submission-proof-absolute-ceiling-seconds, GP07) partway through tonight. This project's own .audiagentic/config/providers/gpt-auto.yaml was updated to match, but the machine-shared gateway process (serving ALL projects, not just this one) was not restarted at that exact moment -- so for a window, the OLD running code rejected the NEW config as 'unknown keys' (VAL-GPTAUTO-001). After restarting the gateway to run the NEW code, a DIFFERENT real project on the same machine (H:\development\projects\bigcherry, working on an unrelated RE14 review) immediately broke the OTHER way: its own gpt-auto.yaml still had the OLD turn: schema (missing the two new keys), so the NEW code rejected IT as 'missing keys'. Fixed by updating bigcherry's config to match (both the new turn: fields and the earlier stop-control/message-finalized response-complete schema from the same session's first fix) -- but this is not a one-off, it is a structural hazard: EXPLICIT USER OBSERVATION (2026-08-16): 'we may need to move the config when running shared to the global config for the agents and related config - otherwise projects will conflict - this is already an issue.' The gateway SERVICE is machine-scoped (one shared process, confirmed via service.json scope:machine), but provider SETTINGS (dom-signals/evidence-policies/turn timers, the actual schema this incident hit) are project-local (.audiagentic/config/providers/*.yaml per project root) with no coordination mechanism between them. Any schema-affecting code change to a shared-gateway-served adapter therefore requires updating every project's local config in lockstep with the code, with no tooling today to detect drift, enumerate affected projects, or apply the update centrally -- exactly the two-sided failure just reproduced live. FOLLOW-UP (not implemented, real architecture decision needed): consider moving provider settings that must stay in lockstep with the shared gateway's own code version to a genuinely global/machine-scoped location (matching the gateway's own scope) rather than per-project, or add explicit schema-version negotiation/migration so an old-schema project config degrades gracefully (or the shared gateway serves multiple schema versions) instead of hard-failing every request. This is a broader instance of the SAME machine-vs-project scoping mismatch already known from GP05's runtime work (GptAutoProviderRuntime is machine-scoped while project semantics live elsewhere) -- worth reviewing together, not as two unrelated gaps.

2026-08-16 (cont'd) NEW EDGE CASE (real DOM, user-supplied): ChatGPT's hard rate-limit block ("Too many requests") renders as a Radix role="dialog" popover, NOT role="alert" -- the existing error-alert dom-signal (selector `[role="alert"]`) does not match it at all. Added a new `rate-limit-dialog` dom-signal (selector `[role="dialog"]` + text-contains-any: ['too many requests', 'making requests too quickly', 'temporarily limited']) across gpt-auto.yaml/gpt-auto-t1.yaml/gpt-auto-t2.yaml (this project only -- bigcherry's separate config was not touched this time, another instance of the per-project config drift problem noted above). CORRECTION (same night): initially wired into response-complete's none-of and response-failed's any-of, but LIVE USER OBSERVATION -- 'even though the panel was visible the request was still executing behind it' -- proved this wrong, the exact same failure mode as the earlier stop-control finding (a real, correctly-matching DOM signal that is NOT reliably correlated with actual turn outcome). Also directly demonstrated by an L5 test run itself: a real EXT-GPTAUTO-003 fired with dom-signals=['rate-limit-dialog','stop-control'] while generating=True and response-active was still asserted. FIXED: rate-limit-dialog demoted to advisory-only across all three configs -- the dom-signal stays (diagnostics/observation-markers only) but is no longer in response-failed's any-of or response-complete's none-of, mirroring stop-control's advisory-only precedent exactly. Independently reconfirmed a second time later the same night on a different, fully-complete essay conversation (copy-button present, no stop-button, truly finished) where the dialog was still rendered well after real completion -- the panel can linger/stick around past actual completion, just like stop-control. FOLLOW-UP (not implemented): no auto-dismiss ('Got it' click) or backoff/pause behavior exists yet. DESIGN REFINEMENT per user: the dialog pops on ALL open tabs at once once the account-level limit is reached, not just the triggering tab -- confirmed live, seen simultaneously on multiple gpt-t2 tabs. This means dismissal can't be scoped to just the active turn's page/target -- it would need to either (a) sweep every open tab bound to the affected project when a rate-limit is detected, or (b) dismiss lazily on whichever tab is next actually interacted with (simpler, avoids touching idle tabs, and per tonight's findings is harmless since the panel doesn't block real completion). Open design questions: per-turn vs adapter-level backoff scope, cooldown duration (fixed vs. reading a hint), cross-project queue independence (a rate limit on one project's account is unrelated to other projects sharing the gateway process). Given the panel can coexist with real progress, any future design must NOT assume the panel's presence alone means the turn is stuck -- it would need genuine stall evidence in addition to the panel, not the panel alone.

2026-08-16 (cont'd) LIKELY ROOT CAUSE of tonight's real rate-limit hit, per user's live observation: 'many new sessions get limited more than continuing a session -- so we need to conserve sessions and continue where possible.' tests/gpt_auto/test_live_stress_scenarios.py's _open_transport() opens a brand-new ChatGPT conversation (fresh ag_session_id) on every test, so L1/L3/L4/L5 collectively created 5 new sessions on gpt-t1/gpt-t2 in a short window -- almost certainly what tripped the real rate-limit-dialog hit during L5, not raw request volume. This matches this item's own existing runbook guidance ('never create parallel review sessions for polling or recovery,' 'RESUME rather than opening a new chat') but the live-test file itself does not follow it. FOLLOW-UP (not implemented): reconsider the live stress test file's session strategy -- reuse/resume one session across scenarios where isolation allows (L3 inherently needs 2 sessions; L1/L4/L5 could plausibly share one sequentially).

2026-08-16 (cont'd) UNRESOLVED FINDING, left open deliberately (session paused here per explicit user caution about triggering a harder rate-limit/ban -- do not reproduce with more live runs without deliberate spacing): an immediate follow-up prompt sent right after a turn resolves failed with EXT-GPTAUTO-003 'composer action was not confirmed by the browser' -- details showed chat-state:'busy', observed-user-text-length:122 vs expected-prompt-length:45, prompt-text-match:false, but also enter-dispatched:true (send-button-clicked:false) -- a FALSE NEGATIVE on submission confirmation, not necessarily garbled input: Enter was really dispatched and likely really submitted something for real, with ChatGPT then genuinely generating a response to it (user-observed: 'that last one is still completing though'). RECONCILIATION ATTEMPT (read-only CDP inspection only, no new sessions/submissions): checked open gpt-t2 tabs; found 2 leftover tabs from earlier L5 attempts, each containing only the original essay exchange -- no trace of a garbled/second message. The SPECIFIC ambiguous conversation (chat id 6a81826e-a9ac-83ec-ba3f-62aef6ecdba0) was not among currently open tabs (likely closed by transport.close() in the test's finally block, despite close-tabs-on-session-close:false -- worth checking why later) before it could be reconciled. A follow-up user-supplied URL (.../c/6a8182c0-14d4-83ec-ad78-a55b0646f545) initially looked like a match (122-char user text) but turned out to be a DIFFERENT, earlier essay conversation where 122 chars is simply the essay prompt's own natural length -- a false lead, corrected. CONCLUSION: the original ambiguous-submission question remains genuinely unresolved and likely unrecoverable (the tab is gone) -- not confirmed safe, not confirmed harmful, simply unknown. This is exactly the gap the durable unresolved-turn checkpoint exists to close (survive across a closed tab via provider-session-id + prompt-text-digest even without the DOM), but no checkpoint_sink was wired in this test harness (_open_transport doesn't pass one to build_session_transport), so there was nothing to reconcile from even in principle -- worth wiring one into the live test harness for next time. tests/gpt_auto/test_live_stress_scenarios.py's L5 test currently fails on this follow-up-prompt step and is committed in that known-failing state deliberately; do not keep retrying live without spacing. Also worth checking whether this is a realistic caller pattern at all before treating it as high severity (the gateway/session layer likely already serializes with some latency between prompts).

## Change Log

- 2026-08-15T11:22:30.218731+00:00 (created-by): Created by agent

## Ledger-events

- chg_20260815_113111_fixed-a-bug-where-resuming-a-g_7620
- 2026-08-15T11:31:11.859541+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-15T11:31:28.504067+00:00 (updated-by): Updated: section:notes
- chg_20260815_122214_fixed-a-follow-on-bug-from-the_7542
- 2026-08-15T12:22:14.766883+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-15T12:22:29.749702+00:00 (updated-by): Updated: section:notes
- chg_20260815_131740_found-that-gpt-auto-review-tur_1422
- 2026-08-15T13:17:40.108497+00:00 (updated-by): Updated: section:ledger-events
- chg_20260815_141934_found-two-more-real-gpt-auto-s_8021
- 2026-08-15T14:19:34.627318+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-15T14:20:00.156189+00:00 (updated-by): Updated: section:notes
- chg_20260815_142050_found-that-the-gateway-leaves_2632
- 2026-08-15T14:20:50.349275+00:00 (updated-by): Updated: section:ledger-events
- chg_20260816_004843_corrected-an-earlier-misdiagno_1099
- 2026-08-16T00:48:43.602357+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-16T00:49:08.275373+00:00 (updated-by): Updated: section:notes
- 2026-08-16T01:26:38.788095+00:00 (updated-by): Updated: section:notes
- 2026-08-16T01:46:46.765722+00:00 (updated-by): Updated: section:steps
- 2026-08-16T01:48:19.317544+00:00 (updated-by): Updated: section:notes
- 2026-08-16T01:48:42.045167+00:00 (updated-by): Updated: section:notes
- 2026-08-16T02:35:46.891662+00:00 (updated-by): Updated: section:notes
- 2026-08-16T04:31:05.369415+00:00 (updated-by): Updated: section:notes
- chg_20260816_045346_you-can-now-target-the-dedicat_6686
- 2026-08-16T04:53:46.516619+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-16T07:56:44.848757+00:00 (updated-by): Updated: section:notes
- 2026-08-16T07:57:31.063920+00:00 (updated-by): Updated: section:notes
- 2026-08-16T08:10:01.789333+00:00 (updated-by): Updated: section:notes
- 2026-08-16T09:22:22.516414+00:00 (updated-by): Updated: section:notes
- 2026-08-16T09:24:33.052806+00:00 (updated-by): Updated: section:notes
- 2026-08-16T09:25:02.048031+00:00 (updated-by): Updated: section:notes
- 2026-08-16T09:29:37.747157+00:00 (updated-by): Updated: section:notes
- 2026-08-16T09:29:56.022777+00:00 (updated-by): Updated: section:notes
- 2026-08-16T09:32:18.486542+00:00 (updated-by): Updated: section:notes
- 2026-08-16T09:33:29.956544+00:00 (updated-by): Updated: section:notes
- 2026-08-16T09:33:49.881926+00:00 (updated-by): Updated: section:notes
- 2026-08-16T09:35:39.646588+00:00 (updated-by): Updated: section:notes
