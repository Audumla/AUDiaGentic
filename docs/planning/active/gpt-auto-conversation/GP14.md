---
id: GP14
order: 0
plan: gpt-auto-conversation
state: pending
created-at: '2026-08-16T10:02:01.014662+00:00'
breadth: ''
skill: intermediate
created-by: agent
work: M
---

# Review and clean up the gateway status/state response schema

## Description

The status/state payloads returned by the gateway (agent_task_status, agent_task_gateway_overview, agent_task_session_list, etc.) have grown organically and are messy: odd/inconsistent field naming (mixed hyphen-case and terms that don't clearly convey meaning), duplicated information appearing under multiple keys, and poor functional grouping (related fields scattered rather than nested together by concern -- e.g. timing, provider metadata, watchdog/diagnostics, correlation identifiers). This makes the schema harder to consume correctly and easier to misread during live debugging, which this session did repeatedly tonight.

## Steps

- Inventory the actual current response shapes for agent_task_status, agent_task_gateway_overview, agent_task_session_list, agent_task_list_requests, and agent_task_session_close/control -- collect real examples (this session has several in its transcript/GP04 notes) rather than working from documentation alone.
- Identify duplication (the same fact exposed under two different keys/paths) and inconsistent naming (hyphen-case vs different terminology for the same concept across endpoints).
- Propose a cleaner grouping by functional concern (e.g. identity/correlation, lifecycle/state, timing, provider metadata, watchdog/diagnostics, error) and a consistent naming convention.
- This is a public/consumer-facing contract (MCP tool responses) -- any change needs a compatibility plan (versioned response, deprecation window, or a documented breaking-change contract-version bump) rather than silently changing field shapes under existing callers.

## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files



## Validation

Capture current response examples for the gateway status, overview, session, request, and session-control operations, then add contract tests for required fields, state values, nullability, and backward-compatible parsing across each response shape.

## Effort & Risk



## Standards

Use the repository’s existing schema and contract-test conventions; preserve additive compatibility where possible, keep provider-specific details behind the provider boundary, and avoid undocumented status aliases.

## Acceptance Criteria

The gateway status/state responses have one documented, internally consistent schema for shared identity, lifecycle, activity, error, and resumability fields. Client-facing behavior remains backward-compatible and ambiguous or contradictory states are represented explicitly.

## Notes

2026-08-16 LIVE DESIGN CONSULTATION (gpt-auto-agent, session ses_a99e14f3c7334bf1, completed cleanly). Full response read via read-only CDP (14,305 chars).

REFRAMES the item: not merely inconsistent naming -- the public API currently LEAKS internal persistence/runtime projection shapes directly, so the same concept moves between paths/vocabulary depending on which tool you call. project_public_status() alone exposes ~50 flat fields spanning durable identity, dispatch ownership, cancellation, replay, provider selection, session policy, result, timing, activity leases, watchdogs, profile snapshots, capacity resolution.

WORST CONCRETE OFFENDER (confirmed via direct code reading, not hypothetical): 'contract-version' means completely different things across endpoints -- agent_task_status exposes the request's persisted contract-version:'v5' (internal durable-record schema) AND separately response-version:3 (the actual public projection contract) from get_execution_request(); session-list independently exposes ITS OWN contract-version:'v3' from session persistence's own schema. A caller sees three different 'contract-version'-shaped numbers meaning three different things. Recommends: internal storage version disappears entirely from the public contract; only response-version (the MCP/public projection version) remains, and is centralized rather than each endpoint assembling its response ad hoc.

OTHER CONCRETE, DIRECTLY-VERIFIED DUPLICATION (this item's own steps asked for real not hypothetical examples): provider-turn-pending appears under THREE different paths depending on endpoint -- agent_task_status's top-level provider-turn-pending (read_public_status() manufactures this from the attached session's provider.metadata['unresolved-turn-pending']), request_runtime_status's progress.provider-turn-pending (project_request_progress() re-reads the SAME value into a different location), and agent_task_session_list's session.provider.metadata['unresolved-turn-pending'] (the raw persisted field, since _public_session_projection() doesn't strip provider.metadata the way request-status redacts it). Same fact, three shapes, one endpoint even leaks the raw persistence object while another manufactures a safe derived boolean.

SEMANTIC COLLISION (worse than naming -- literally means two different things): 'latest-transition' in agent_task_status is the actual last durable timeline event {event,state,timestamp}; request_runtime_status's progress.latest-transition is SEPARATELY SYNTHESIZED from max(started-at, finished-at, attempt-timestamps) and only has {state,timestamp} -- these can legitimately point at different moments but share a name implying they're the same fact.

OTHER FINDINGS: gateway_overview's active-count is actually a live-runtime-attachment count (sum of live==True), not a count of durably-active sessions -- misleadingly named 'active'. gateway-execution-lane-key and resolved-queue-limits are explicitly constructed as None under the current architecture (retired concepts) but remain on the public allow-list. overview's total_requests/by_state/recent_failures/providers_loaded/skipped_count are snake_case inside an otherwise hyphen-case API.

RECOMMENDED SHAPE: one-level functional grouping (not deep nesting) -- request/lifecycle/timing/dispatch/admission/provider/session/cancellation/liveness/recovery/result/attempts/metadata as top-level groups. Explicitly warns against over-grouping (e.g. don't dump every timestamp under 'timing' -- dispatch.claimed-at/cancellation.acknowledged-at/liveness.last-activity-at should stay with their functional concept, not get shoved into one giant timing bucket, or you've just relocated the scattering problem). provider-id vs resolved-provider-id should become STRUCTURALLY distinct (admission.provider-id = admission-time candidate vs provider.provider-id = actual execution-time resolved identity) rather than two same-level near-duplicate keys. agent-status's nested identity fields (request-id/session-id/turn-id/etc, already present at top level) should be removed from the nested projection, keeping only its genuinely unique semantic content (lifecycle/outcome/wait-reason/decisions) under lifecycle.canonical.

MIGRATION PATH (explicitly answers the 'is nesting worth the breaking-change cost' question): do NOT ship both flat and grouped representations simultaneously in every response ('technically additive but makes the core problem WORSE during migration -- every fact now has two apparently authoritative locations'). Instead: add an optional response_version parameter at the MCP boundary (None/3 = exact legacy response, 4 = new grouped response), existing callers stay byte-for-byte compatible by default, new callers explicitly opt into V4, only after a documented deprecation window does the default switch, only in a later breaking release does V3 actually get removed. Centralize the V4 projection into explicit functions (project_request_status_v4(), project_request_runtime_status_v4(), project_session_v4(), project_gateway_overview_v4()) rather than ad hoc per-endpoint dict assembly -- this is itself a fix for HOW today's duplication happened (endpoints independently reimplementing similar projections).

RECOMMENDED ACCEPTANCE TARGET: 'Introduce a versioned V4 public projection with consistent hyphen-case and functional one-level grouping. Preserve V3 exactly by default during migration. Internal persistence contract-version is not part of V4. Each logical fact has one canonical V4 path across tools; endpoint-specific projections may omit a fact but must not rename or relocate it.' Plus three specific invariants: provider.turn-pending has exactly one name/path everywhere; lifecycle.latest-event (durable timeline) and derived progress timing get different names, never collide; runtime attached/live is never called 'active' (durable-active and process-local-live are independent facts under GP06's own corrected model).

NOT implemented tonight -- this is real, substantial, well-specified future work (new V4 projection functions, the response_version negotiation parameter, a real deprecation-window plan) that needs its own session, not a rushed implementation this late.

## Change Log

- 2026-08-16T10:02:01.014662+00:00 (created-by): Created by agent
- 2026-08-16T12:16:57.181931+00:00 (updated-by): Updated: section:notes
- 2026-08-17T11:06:39.176267+00:00 (updated-by): Updated (no visible changes)
- 2026-08-17T11:52:00.479518+00:00 (updated-by): Updated: section:validation, section:acceptance_criteria, section:standards

## Ledger-events

- chg_20260817_115225_repaired-the-incomplete-active_5611
- 2026-08-17T11:52:25.736122+00:00 (updated-by): Updated: section:ledger-events
