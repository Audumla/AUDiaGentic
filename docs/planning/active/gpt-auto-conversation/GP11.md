---
id: GP11
order: 0
plan: gpt-auto-conversation
state: pending
created-at: '2026-08-16T09:52:55.802453+00:00'
breadth: ''
skill: advanced
created-by: agent
work: S
---

# Reconcile the ambiguous follow-up-submission finding and wire checkpoint persistence into live testing

## Description

During live L5 testing (2026-08-16), an immediate follow-up prompt sent right after a turn resolved failed with EXT-GPTAUTO-003 'composer action was not confirmed by the browser' -- but the error's own details showed enter-dispatched:true (send-button-clicked:false), a FALSE NEGATIVE on submission confirmation, not necessarily garbled input. Enter was likely really dispatched and the browser was observed still generating a response afterward. Read-only reconciliation attempts (CDP inspection of open tabs, no new sessions) could not resolve what actually happened -- the specific conversation's tab was already closed by the time it was checked, and durable checkpoint persistence was not wired into the test harness that hit this (build_session_transport was called without a checkpoint_sink), so there was nothing to reconcile from even in principle.

## Steps

- Wire a real checkpoint_sink into tests/gpt_auto/test_live_stress_scenarios.py's _open_transport() so future live ambiguous-submission cases can actually be reconciled via the existing unresolved-turn checkpoint mechanism (GP04's durable design) instead of being unrecoverable once a tab closes.
- Reproduce the immediate-back-to-back-prompt scenario deliberately (with checkpointing wired in this time) to determine whether ensure_ready()'s DOM/composer-quiescence check has a real gap after a turn resolves, or whether this was purely a test-harness artifact (immediate back-to-back transport.prompt() calls with zero settle gap is not how a real caller would normally behave -- check whether the gateway/session layer already serializes with enough latency to make this moot in production).
- Only implement a fix (e.g. a real DOM-settle wait in ensure_ready()) if reproduction confirms a genuine production gap, not blind.

## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files



## Validation



## Effort & Risk



## Standards



## Acceptance Criteria



## Notes

2026-08-16 SECOND CLEAN LIVE REPRODUCTION (session ses_b2b8867d882649d2, req_e85725ab75ba4236/GP10 attempt): submitting a new prompt immediately after the previous turn (GP09, req_31fbaf5e309542f8) was reported 'completed' hit the exact same EXT-GPTAUTO-003 composer-action-not-confirmed pattern (enter-dispatched:true, send-button-clicked:false, chat-state:busy, generating:true) -- but this instance resolved CLEANLY: read-only CDP inspection confirmed the real browser was still actively rendering GP09's genuine, full answer (grew from a 19-char in-progress snapshot to a complete 17,344-char response matching its actual question content, stop-button eventually disappearing) and the DOM turn_count never exceeded the real 4 nodes (2 user + 2 assistant) -- i.e. this time the failed submission did NOT create a duplicate/phantom user turn or any ambiguous content, unlike the original incident's unresolved outcome. This strengthens confidence that the root cause is exactly what GP11 already hypothesized (a composer-quiescence race immediately after a turn resolves, not garbled typing) and suggests the FAILURE MODE, while still a real reliability gap worth fixing, is not itself unsafe/corrupting when it happens -- the submission attempt fails visibly rather than silently succeeding with wrong content. Two real data points now: this session's own GP09->GP10 sequence (clean failure, no side effect) and the earlier essay->RECOVERED sequence (side effect possibly occurred, tab closed before reconciliation, genuinely unresolved). The difference may be timing-window-dependent (how far into the 'still settling' window the second submission lands) -- worth investigating as part of this item's existing reproduction plan rather than as a new concern.

## Change Log

- 2026-08-16T09:52:55.802453+00:00 (created-by): Created by agent

## Ledger-events

- chg_20260816_095515_closed-out-two-finished-gpt-au_2847
- 2026-08-16T09:55:15.697723+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-16T09:56:31.152850+00:00 (updated-by): Updated: section:notes
- 2026-08-16T10:35:53.475836+00:00 (updated-by): Updated: section:notes
