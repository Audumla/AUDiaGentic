---
id: GP11
order: 0
plan: gpt-auto-conversation
state: pending
created-at: '2026-08-16T09:52:55.802453+00:00'
breadth: ''
skill: advanced
created-by: agent
work: S
---

# Reconcile the ambiguous follow-up-submission finding and wire checkpoint persistence into live testing

## Description

During live L5 testing (2026-08-16), an immediate follow-up prompt sent right after a turn resolved failed with EXT-GPTAUTO-003 'composer action was not confirmed by the browser' -- but the error's own details showed enter-dispatched:true (send-button-clicked:false), a FALSE NEGATIVE on submission confirmation, not necessarily garbled input. Enter was likely really dispatched and the browser was observed still generating a response afterward. Read-only reconciliation attempts (CDP inspection of open tabs, no new sessions) could not resolve what actually happened -- the specific conversation's tab was already closed by the time it was checked, and durable checkpoint persistence was not wired into the test harness that hit this (build_session_transport was called without a checkpoint_sink), so there was nothing to reconcile from even in principle.

## Steps

- Wire a real checkpoint_sink into tests/gpt_auto/test_live_stress_scenarios.py's _open_transport() so future live ambiguous-submission cases can actually be reconciled via the existing unresolved-turn checkpoint mechanism (GP04's durable design) instead of being unrecoverable once a tab closes.
- Reproduce the immediate-back-to-back-prompt scenario deliberately (with checkpointing wired in this time) to determine whether ensure_ready()'s DOM/composer-quiescence check has a real gap after a turn resolves, or whether this was purely a test-harness artifact (immediate back-to-back transport.prompt() calls with zero settle gap is not how a real caller would normally behave -- check whether the gateway/session layer already serializes with enough latency to make this moot in production).
- Only implement a fix (e.g. a real DOM-settle wait in ensure_ready()) if reproduction confirms a genuine production gap, not blind.

## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files



## Validation



## Effort & Risk



## Standards



## Acceptance Criteria



## Notes

2026-08-16 SIGNIFICANT ADDITIONAL EVIDENCE, widens this item's scope: gateway overview's recent_failures shows 4 of the last 5 real request failures tonight are the SAME composer-typed-text-mismatch pattern this item was scoped around from a single case -- on fresh sessions across gpt-t1/gpt-t2/audiagentic profiles, all large prompts (3400-7300 chars) with small, consistent-magnitude length mismatches. Reframes GP11 as a likely general large-prompt submission reliability bug, not just an immediate-follow-up edge case.

2026-08-16 SECOND CLEAN LIVE REPRODUCTION (session ses_b2b8867d882649d2, req_e85725ab75ba4236/GP10 attempt): submitting a new prompt immediately after the previous turn (GP09, req_31fbaf5e309542f8) was reported 'completed' hit the exact same EXT-GPTAUTO-003 composer-action-not-confirmed pattern (enter-dispatched:true, send-button-clicked:false, chat-state:busy, generating:true) -- but this instance resolved CLEANLY: read-only CDP inspection confirmed the real browser was still actively rendering GP09's genuine, full answer (grew from a 19-char in-progress snapshot to a complete 17,344-character response matching its actual question content, stop-button eventually disappearing) and the DOM turn_count never exceeded the real 4 nodes (2 user + 2 assistant) -- i.e. this time the failed submission did NOT create a duplicate/phantom user turn or any ambiguous content, unlike the original incident's unresolved outcome. Two real data points now: this session's own GP09->GP10 sequence (clean failure, no side effect) and the earlier essay->RECOVERED sequence (side effect possibly occurred, tab closed before reconciliation, genuinely unresolved). Also exercised the correct admission-gate behavior: EXT-GPTAUTO-004 correctly refused a retry after the unresolved prior attempt, exactly as GP04's recovery model intends -- resolved via close-session-then-resume, which worked as a manual workaround.

2026-08-16 CODE-CONFIRMED GAP, per user's direct questions ('do we have confirmation the submit button hit has been accepted or worked? do we have a retry or ability to detect if it ends up here again? what state change indicates a real submission'). Read the actual submit() implementation (gpt_auto_cdp.py:124-169 at the time): (1) composer clearing IS already correct -- select-all (range.selectNodeContents) + execCommand('insertText') replaces any existing content, so stale leftover text from a prior failed attempt is NOT a corruption risk for a subsequent retry. (2) 'Click confirmation' was weak by design: the only proof was 'a matching, non-disabled send-button was found and .click() was called on it' -- no verification afterward that the composer actually emptied or that React genuinely processed the click. The REAL authoritative proof of submission is deliberately deferred to the separate submission-proof phase (a new matching user message appearing in the DOM) -- correct in principle, but that phase is never reached if BOTH the click AND the Enter-dispatch fallback fail to register. (3) CONFIRMED GAP (now fixed, see below): there was NO bounded retry of the type-and-click sequence itself within a single turn.

2026-08-16 PARTIAL FIX IMPLEMENTED: added a bounded retry (3 attempts, 200ms apart) around the send-button-click + Enter-fallback sequence in GptAutoCdp.submit() (gpt_auto_cdp.py). Composer clear-and-retype was already confirmed safe for retries. Locked in with two new deterministic tests: test_gpt_provider_submit_retries_and_recovers_from_transient_send_failure and test_gpt_provider_submit_gives_up_after_bounded_retries. Full gpt_auto suite passes.

This is a mitigation for the SYMPTOM (the click/Enter action itself failing to register), not a fix for whatever underlying DOM-settle timing causes the button to be transiently unavailable in the first place -- the deeper investigation this item's steps call for (is a real DOM-quiescence wait needed in ensure_ready(), does this correlate with heavy resubmission history) remains open. The ambiguous-submission reconciliation gap (checkpoint_sink not wired into the live test harness) also remains open. A live GPT design consultation on this item was also attempted (session ses_39ed04c132a74dd9) but its response was truncated (only reached "I would" before this session moved on) -- not usable, would need a genuine re-run.

DATA-LOSS NOTE: an earlier append call to this item's notes on 2026-08-16 unexpectedly replaced rather than appended despite passing append=["notes"] -- the four blocks above were reconstructed from this session's own conversation history after the fact. Root cause not yet determined (append had worked correctly on this same item several times earlier the same night) -- worth investigating separately if it recurs. Always verify a plan_update_item append call by re-reading the item immediately afterward, do not trust it blindly.

## Change Log

- 2026-08-16T09:52:55.802453+00:00 (created-by): Created by agent

## Ledger-events

- chg_20260816_095515_closed-out-two-finished-gpt-au_2847
- 2026-08-16T09:55:15.697723+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-16T09:56:31.152850+00:00 (updated-by): Updated: section:notes
- 2026-08-16T10:35:53.475836+00:00 (updated-by): Updated: section:notes
- 2026-08-16T10:41:01.352401+00:00 (updated-by): Updated: section:notes
- 2026-08-16T11:07:29.249516+00:00 (updated-by): Updated: section:notes
- 2026-08-16T11:08:54.553432+00:00 (updated-by): Updated: section:notes
