---
id: GP06
order: 0
plan: gpt-auto-conversation
state: pending
created-at: '2026-08-15T14:21:11.910040+00:00'
breadth: ''
skill: advanced
created-by: agent
work: M
---

# Reconcile durable gateway records against runtime ownership on restart (extend existing reaper, don't build a new sweeper)

## Description

The gateway's session/request bookkeeping has no reconciliation pass for records whose owning process has exited without a clean close. CORRECTED 2026-08-16 (final full-pack review): this is now split into request-ownership reconciliation and session-ownership reconciliation, which must be handled INDEPENDENTLY and differently -- do not treat them the same way, and do not close active sessions merely because their runtime owner is gone.

## Steps

CORRECTED 2026-08-16, final review -- supersedes the earlier 'sessions -> closed with runtime-owner-lost' proposal, which was WRONG and is now explicitly rejected:

CRITICAL CORRECTION: gateway runtime death does NOT prove provider-session death -- it proves only that the former in-memory transport owner is gone. This codebase ALREADY has a deliberate, working gateway-restart recovery contract for sessions: production session dispatch calls `runtime.rehydrate_session()` when it encounters an active durable session whose process-local handle is absent, and `SessionRuntime.rehydrate_session()` explicitly documents that live handles are process-local, the durable session remains active across a gateway restart, and the same provider conversation is reattached in place (not a successor generation). GP06 must NOT blanket-close active sessions on owner loss -- that would turn an intentionally recoverable state into a terminal one, contradict GP04's own recovery semantics, and undermine PC14's resumability work. PRESERVE the existing active-session-plus-absent-local-handle-equals-lazily-rehydratable model.

CORRECTED MODEL: reconcile REQUEST ownership and SESSION ownership independently:
- A request that was actively executing when its gateway owner vanished CAN correctly become `interrupted` (already the right terminal state for 'AUDiaGentic lost authoritative execution ownership/outcome evidence' -- explicitly NOT 'provider failed' or 'prompt definitely did not complete').
- Its durable SESSION should remain active-but-detached from a process-local runtime, exactly as today, eligible for lazy rehydration via the EXISTING mechanism. Only positive evidence that the provider conversation itself cannot be reattached should ever terminalize a session -- runtime-owner-loss alone is not that evidence.
- If a more explicit durable representation is wanted later, an orthogonal `runtime-attachment = detached/lost` marker (separate from the provider-session lifecycle) would be cleaner than overloading session state -- not required for the initial fix.

NEW HIGHEST-PRIORITY SUB-PROBLEM (P0, do this first, before any general reconciliation mechanism): the bigcherry incident is not simply 'a stale record' -- it means a durability invariant was violated AFTER admission. Normal admission constructs and atomically persists the request record in `queued` state BEFORE enqueue() is called (the queue contract requires this). So 'admitted request existed -> provider side effect occurred -> durable request became absent/unaddressable' is a genuine data-loss defect, not just a reconciliation gap. Before implementing any general sweep: reproduce/instrument this exact transition and establish precisely how/where the record becomes unavailable. Do not paper over this with a generic orphan sweeper until the actual loss mechanism is understood.

IMMEDIATE, SEPARATE, SAFE QUICK FIX (do this now, independently of the rest of GP06): the current admission-recovery error wording ('the conversation remains recoverable and no prompt was sent') is actively FALSE/unsafe given the confirmed incident -- a prior unresolved turn may already have been sent and may even have completed. Correct the wording to only claim what is actually known: 'this new admission attempt did not submit another prompt; the previous unresolved turn's outcome is not yet proven.' This is a small, low-risk, high-value patch independent of the rest of GP06's design work.

Cache invalidation on NET-AGSV-002 (confirmed correct by the reviewer via direct code inspection of get_gateway_client()), WITH an important safety refinement: separate 'reacquire a live gateway client' from 'is it safe to replay the RPC that failed.' NET-AGSV-002 covers URLError/TimeoutError/OSError -- a timeout does NOT prove the RPC never reached the server; the request may have committed with only the response lost. So: automatic mode should invalidate the dead cached client and reacquire/bootstrap a live gateway unconditionally (safe), but a SEPARATE retry policy must decide whether the original failed RPC is replay-safe -- reads/status/list calls may be retried freely, but mutating operations (submit, resume, control, close) need either proven idempotency for that specific request identity or transport-level proof the failure occurred before server acceptance, before being replayed. Do not blindly replay arbitrary RPCs after reconnecting. StandaloneGatewayClient._call() already has a bounded reattach precedent for a specific owner/lease conflict (CON-AGSV-018) worth reusing as a pattern, though NET-AGSV-002 itself is less semantically precise than that case.

Deterministic duplicate-tab selection (GP04's finding): confirmed safe and correct to implement as proposed, with one addition -- if a candidate tab is already claimed by a different LIVE gpt-auto chat (per the existing conversation/page ownership maps), do not silently hop to it; that would bypass the ownership invariant those maps exist to enforce.

Overall priority within GP06 itself: (1) the false-error-wording quick fix, immediately; (2) reproduce/instrument the request-record-loss mechanism and fix request-side reconciliation to `interrupted` for proven-dead owners, WITHOUT touching session lifecycle; (3) the gateway-client cache invalidation + replay-safety split. Do not implement the original session-closing proposal at all.

## Detailed Solution & Technical Design

Live audit evidence (2026-08-15/16, via agent_task_list_requests(state='running') and agent_task_session_list(state='active')): 5 gpt-auto requests dated 2026-08-06 through 2026-08-10 are persisted as state=running with cancel-requested=true and watchdog-state=not-started -- cancellation was requested 5-10 days before this audit and never reconciled. Separately, 6 more gpt-auto sessions dated 2026-08-12 through 2026-08-14 are persisted as state=active despite runtime-state=stale-non-live (owning process long gone). None of these are related to any live testing performed today; they predate it. This strongly suggests there is currently no sweep/reconciliation pass that runs against the persisted store to close out records whose owning process/worker has disappeared -- the gateway only reconciles state when something actively touches a given record (e.g. a status query or a resume attempt), not proactively.

## Code Samples & Guidance



## Files

- src/audiagentic/components/agents/gateway/session/
- src/audiagentic/components/agents/gateway/queue/
- src/audiagentic/components/agents/gateway/store/

## Validation

After the fix, re-run the same audit (agent_task_list_requests(state='running'), agent_task_session_list(state='active')) and confirm no record older than a reasonable grace period remains non-terminal without an active, live-verified owner.

## Effort & Risk

Medium. Touches shared gateway session/request persistence used by every provider, not just gpt-auto -- must not race with legitimately long-running requests (gpt-auto turns can validly run up to an hour) or misclassify a slow-but-alive request as abandoned.

## Standards

GP04's bulletproof completion/failure state requirement.

## Acceptance Criteria



## Notes

2026-08-16 LIVE DESIGN CONSULTATION (gpt-auto-agent, session ses_f7c913eb31e54656, req_177d9c3c24e94aed -- note: the GATEWAY marked this request 'failed' via a false-positive response-stall-timeout, but the real ChatGPT response completed genuinely and correctly; recovered via read-only CDP inspection after the fact -- see the new false-stall-timeout finding item for that separate bug). Key findings, condensed from the full 23K-char response:

A. Request-record-loss reproduction plan: focus on store._admission.admit_record() -- the service-root work-index write (the mechanism recover_gateway_requests() uses to rediscover admitted-but-unclaimed work after restart) is written as NON-FATAL best-effort ('except OSError: logger.warning(...)'), so this state is currently legal: project request record EXISTS + idempotency index EXISTS + service work-index MISSING + request returned as admitted=True -- exactly the 'durable but service has lost its address to it' bug. Also: an idempotent re-submit does NOT repair a missing work-index entry (admit_record() returns early on the idempotency-hit path before re-establishing it), so once broken, an ordinary retry doesn't self-heal. Recommended: extend the existing SH07 crash-harness pattern (AUDIAGENTIC_GATEWAY_TEST_STALL_CLAIM_TO_START_MS precedent in queue.py) with barriers at each admission boundary, and instrument _records.write_record()/_read_record_payload() to distinguish 4 distinct failure buckets (file physically gone / read fails / exists-but-undiscovered-by-recovery / client-never-learned-the-id) -- do not patch until the real incident is sorted into one of these. Proposed fix: make the work-index write part of admission's success contract (_ensure_request_discoverable() called on every successful admit_record() return, admission fails closed if discovery-write fails) rather than best-effort metadata.

B. Replay-safety split: the branch's existing NET-AGSV-002 reconnect-then-read-only-retry pattern in call_gateway_method() is the right shape; replace the current hardcoded _READ_ONLY_GATEWAY_METHODS set (which is missing wait_execution_request and read_agent_work_output) with a closed 3-state policy: ReplayMode.ALWAYS (observational) / IF_PROVEN (mutating, needs call-specific idempotency proof) / NEVER (ambiguous mutation, fail closed by default for anything not explicitly classified). Keep the policy check inside call_gateway_method(), not StandaloneGatewayClient._request() (that layer has no operation semantics) and keep it separate from the existing CON-AGSV-018 lease-retry in _call() (different safety proof: the server validates the lease before dispatch, so that retry is not equivalent to retrying an ambiguous network failure). Ship v1 with ALL mutations at NEVER (submit/cancel/control/resume/close all currently lack crash-safe idempotency proof per direct code inspection -- resume_execution_session's own docs say it's still check-then-act with a known concurrency gap), promote individual operations to IF_PROVEN only once each one's contract is actually strong enough. Also found and worth fixing while touching this: reset_gateway_client() has a cache race under concurrent failures (A fails on client-old, resets to client-new; B (also failed on client-old) then resets AGAIN and closes the fresh client-new) -- fix via compare-and-swap invalidation (invalidate_gateway_client(expected_client) that only clears/closes if the cached client still IS the one that failed).

C. Current-code conflicts with the corrected independent-reconciliation model (direct contradictions found via code inspection, not speculation):
1. SessionRuntime.shutdown() -> _close_all() blanket-transitions EVERY nonterminal durable session to closed with binding retirement on graceful shutdown, while restart recovery's own rehydrate_session() path correctly preserves an active durable session with an absent local handle for lazy reattachment. This means restart-preservation currently depends on HOW the old gateway died (violent death -> rehydratable; graceful shutdown -> closed) -- exactly the ownership/lifecycle conflation GP06 exists to fix. Fix direction: split transport.detach() (release this runtime's local attachment, provider conversation survives) from transport.close() (semantically end the provider session) -- shutdown should call detach, not close, for providers where local-transport death doesn't imply provider-session death (gpt-auto is exactly this case).
2. queue/recovery.py's _terminalize_stale_request() sends BOTH queued-with-no-owner AND running-under-dead-owner through the same queued->interrupted / running->interrupted path -- contradicts the rule that only requests actively executing when ownership vanished should become interrupted. A queued request that never crossed the durable start_owned_attempt() boundary should NOT be 'interrupted' (implies uncertain provider-side execution when there was none) -- should be a distinct rejected/resubmit-required outcome instead.
3. recover_gateway_requests()'s path 1 can ERASE the only discovery references (active-work + work-index) for a nonterminal request that lacks a valid dispatch-owner-epoch, rather than repairing/quarantining it -- creating a second class of 'durable but undiscoverable' request. Notable interaction: _records._migrate_v1_payload() deliberately resets dispatch-owner fields on contract-version upgrade, so a legitimately-migrated legacy request can trigger this exact deletion path. 'Owner field missing' must not be treated as 'safe to forget.'
4. The reaper's is_alive()-false -> _fail_session() path (and orphaned-child-death handling) conflates local transport death with provider-session death for ALL providers uniformly -- valid for providers where the conversation is inseparable from the local child process, wrong for gpt-auto where the CDP/runtime attachment can die while the ChatGPT conversation remains recoverable by provider ref. Should route through the same provider-capability check rehydrate_session() uses to decide DETACHED/REHYDRATABLE vs PROVIDER-SESSION-PROVEN-DEAD.

Revised priority (same order as before, sharpened): #1 (error-wording) done. #2: prove which of the 4 loss-failure-buckets the real incident is BEFORE patching, then make work-index-on-admission mandatory + stop recovery from deleting discovery references + fix queued-stale-owner routing -- AND fix shutdown's blanket session-close in the same pass (it's a correctness contradiction of the same invariant, not a separate feature -- fixing only the request half while shutdown still destroys the session half leaves GP06 incomplete). #3: replace _READ_ONLY_GATEWAY_METHODS with the closed GatewayRetryPolicy + compare-and-swap cache invalidation, ALWAYS-only auto-replay initially.

## Change Log

- 2026-08-15T14:21:11.910040+00:00 (created-by): Created by agent
- 2026-08-16T01:15:10.976903+00:00 (updated-by): Updated: section:title, section:description, section:steps, section:notes
- 2026-08-16T01:26:30.902101+00:00 (updated-by): Updated: section:notes
- 2026-08-16T01:34:29.389115+00:00 (updated-by): Updated: section:notes
- 2026-08-16T01:35:37.962071+00:00 (updated-by): Updated: section:notes
- 2026-08-16T01:43:26.120472+00:00 (updated-by): Updated: section:notes
- 2026-08-16T01:57:22.913878+00:00 (updated-by): Updated: section:description, section:steps, section:notes
- 2026-08-16T02:34:06.318217+00:00 (updated-by): Updated: section:notes
- 2026-08-16T03:22:01.000742+00:00 (updated-by): Updated: section:notes
- 2026-08-16T03:31:29.424491+00:00 (updated-by): Updated: section:notes
- 2026-08-16T10:07:33.120341+00:00 (updated-by): Updated: section:notes
