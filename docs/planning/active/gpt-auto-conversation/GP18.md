---
id: GP18
order: 0
plan: gpt-auto-conversation
state: pending
created-at: '2026-08-16T11:26:47.997500+00:00'
breadth: ''
skill: advanced
created-by: agent
work: M
---

# CDP Target.getTargets hangs (30s timeout) on the test immediately following a concurrent-session close

## Description

tests/gpt_auto/test_live_stress_scenarios.py's live suite reproducibly fails on the SECOND and THIRD tests in a run (test_live_t1_scenarios, test_live_cancel_mid_turn_leaves_session_usable) but not the first (test_live_concurrent_turns_on_both_test_projects_stay_isolated), across two consecutive full runs tonight (2026-08-16) with identical timing both times (~81s total, both failures hitting the exact same 30.0s CDP client default_timeout on client.command('Target.getTargets') inside runtime.py's ensure_dedicated_window_anchor -> bridge.call('list_pages') -> _refresh_pages()). Closing the 4 leftover ChatGPT tabs this session had accumulated made no difference between the two runs (identical failure, identical timing) -- ruling out simple CDP target-count/enumeration slowness as the cause.

## Steps

- Investigate runtime.py's unregister_chat(): when the last chat releases its page (self._dedicated_window_anchor set and self._bridge present), it fires `asyncio.create_task(self.refresh_status_page())` -- fire-and-forget, not awaited, not tracked. The concurrent-isolation test (L3) closes TWO transports back-to-back at teardown, spawning two of these concurrently. If either is still in-flight (making its own CDP calls) when the very next test immediately opens a fresh transport and issues Target.getTargets, the two could contend for the same underlying CDP client's request/response correlation (e.g. a shared request-id counter or websocket read loop), plausibly explaining why the next command never receives its response and times out at exactly the configured 30s.
- Reproduce in isolation: run test_live_concurrent_turns_on_both_test_projects_stay_isolated alone, then IMMEDIATELY (same process) try a bare Target.getTargets call and confirm whether it hangs -- versus waiting a few seconds after L3 finishes before trying, to see if it's purely a race window vs a permanent broken state.
- If confirmed, fix direction: track refresh_status_page()'s task explicitly (store the task reference, await/cancel it during shutdown or before the next command that needs a clean CDP round-trip) rather than leaving it truly fire-and-forget; or serialize it behind the same lock/queue other CDP commands already go through if one exists.
- Also worth checking whether this could affect PRODUCTION multi-session usage, not just this test file's specific back-to-back pattern -- any real caller that closes a session and immediately opens another against the same shared runtime could hit the same race.

## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files



## Validation



## Effort & Risk



## Standards



## Acceptance Criteria



## Notes

2026-08-16 FURTHER INVESTIGATION, ruling out simpler theories: (1) closing this session's own 4 leftover ChatGPT tabs made no difference (identical failure/timing on retry) -- rules out simple target-count/enumeration slowness. (2) Killed and relaunched Brave entirely (fresh CDP browser-level websocket, fresh browser-target UUID) -- ALL THREE tests failed on the very next run, including the concurrent-isolation test that had previously passed twice -- this DISPROVES the earlier hypothesis that L3's own teardown (fire-and-forget refresh_status_page() tasks) races with the next test, since now even the FIRST test in the run fails identically. (3) Directly timed a bare Target.getTargets call against the live browser via a standalone script (fresh websocket connection, same discovery method the adapter uses: GET /json/version -> connect to webSocketDebuggerUrl) -- responded in 0.01s, proving the browser and raw CDP protocol are NOT slow or unresponsive. (4) Read _read_loop()'s disconnect handling in client.py -- confirmed correct: a genuinely dead connection immediately fails all pending futures with a clear CdpError, it does not silently time out. A real 30s timeout with no error therefore means the connection object appears healthy but the specific request's response never arrives or never gets matched. (5) Retried a 4th time after the browser had settled (55+ pages restored, confirmed responsive) -- still failed identically on all three tests, one of them additionally surfacing a secondary 'RuntimeError: Event loop is closed' during a send() call (likely a downstream symptom of an earlier cancellation/timeout in the same test run, not the root cause itself).

COMPLICATING FACTOR during this investigation: partway through, the user manually closed what they described as one of two GPT-related browser windows (one from Brave's --restore-last-session, one newly created by the automation) -- after that, zero gpt-t1/gpt-t2/audiagentic ChatGPT tabs remained open, and ensure_dedicated_window_anchor()'s own self-healing path (create a brand new window via bridge.call('create_window_page') when no existing anchor/dashboard page is found, runtime.py:186-199) did NOT visibly produce a new window/dashboard page during the subsequent failing run -- worth checking directly whether create_window_page itself is the actual hang point, separately from Target.getTargets, since the failures observed all show the SAME Target.getTargets stack trace but that call happens BEFORE the create-new-window fallback would even be reached (list_pages is called first to check for an existing anchor).

STATUS: root cause NOT yet found despite substantial investigation. Confirmed NOT: browser responsiveness, raw protocol speed, simple target-count scaling, L3-teardown-races-next-test, connection-death-handling-bug. Still unconfirmed: whether the adapter's own persistent CdpClient connection (as opposed to a fresh ad hoc one) is somehow different/stale, and whether the user's manual window-close mid-session left the runtime's in-memory _dedicated_window_anchor state inconsistent with reality. STOPPING live investigation here for tonight given the very late hour and the environment now being in a confusing state (multiple browser restarts, manual window closure) that makes further live attempts hard to interpret cleanly. Live validation of GP15/GP17/GP11 remains genuinely incomplete -- needs a fresh, dedicated session with careful before/after CDP state capture at each step, not more blind retries.

## Change Log

- 2026-08-16T11:26:47.997500+00:00 (created-by): Created by agent
- 2026-08-16T11:35:33.842964+00:00 (updated-by): Updated: section:notes
