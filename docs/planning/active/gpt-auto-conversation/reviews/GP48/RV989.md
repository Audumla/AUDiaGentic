---
id: RV989
review-of: GP48
plan: gpt-auto-conversation
state: created
reviewed-by: gpt-auto/dev-gpt-agent via live CDP extraction plus gateway evidence
reviewed-at: '2026-08-24T00:20:00Z'
---

# GPT design validation and live routing failure

## Notes

The fresh validation request was resent into a new GPT-auto session with workspace AUDiaGentic, repository URL, and branch supplied. The durable record froze the correct project/source-control context and ChatGPT project URL. The post-implementation request for commit 1d7bd11d057f788b16de5cd99f1f1b128a99a171 reached turn-accepted and in-progress, then correctly entered activity-lease-expired-diagnostic after five minutes without terminal evidence. The session close record preserved unresolved-turn-pending=true and recovery-state=side-effect-may-have-started. The response artifact was unavailable, so no GPT verdict was fabricated. A safe resume was attempted but the running gateway rejected the newer resume fields as an operation-schema mismatch.

## Findings

POST-IMPLEMENTATION VERDICT=BLOCK. P0=0. P1-1: diagnostics.py classification can still convert gateway/browser proof failures into provider-error: EXT-GPTAUTO-002 response-policy-timeout falls through to provider-error, and an exception after submission-attempted=true can be classified browser-adapter-error with not-started/retry-safe. transition_record replaces diagnostics without a monotonic side-effect merge. P1-2: recover_execution_request advertises adopt/retire but only implements reconcile/abandon/clear-not-submitted; reconcile does not query the provider, abandon does not retire/quarantine the conversation, and recovery fences (unresolved-turn id, candidate fingerprint, idempotency key) are missing. Abandon also weakens CAS by updating diagnostics with expected_revision=None. P2: legacy session-capacity migration is silent rather than explicit, service capability discovery omits diagnostics/recovery, and session-status projection lacks the diagnostic rollup. GPT confirmed bounded/redacted evidence and project active-task capacity are sound.

## Conclusion

BLOCK remains. Implement the smallest fixes: evidence-driven monotonic SideEffectState merge; classify response-policy timeout and attempted-but-unproven submission as timeout/ambiguous-side-effect rather than provider failure; add incident regression tests; either implement truthful zero-send adopt and abandon+retire with CAS/idempotency/reconciliation or narrow advertised actions; add capability declarations and explicit legacy-capacity migration handling. Then commit/publish and obtain a new GPT post-review. Do not close RV989 or GP48 yet.
