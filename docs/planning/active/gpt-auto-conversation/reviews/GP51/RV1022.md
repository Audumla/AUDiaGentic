---
id: RV1022
review-of: GP51
plan: gpt-auto-conversation
state: created
reviewed-by: agent + dev-gpt-agent
reviewed-at: '2026-09-13T15:03:44Z'
---

# Live restart rehydration rejects active GPT request

## Notes

Do not restart the live gateway during this review. Preserve same request ID, same session ID, and durable provider binding; never create a fresh provider prompt as a recovery shortcut. Current pushed implementation: e49f56b63. Exact-head reassessment remains in the existing GPT conversation; do not create a new provider session. After a GPT GO and clean local validation, ask the user for explicit permission before the live restart/same-session continuation test.

## Findings

- GPT static audit of exact fd885aa52 confirmed pre-prompt EXT-AGW-118 was non-terminal and identity/default replacement protections were correct.
- GPT then identified P1 gaps: resume_existing post-reattach errors terminalized; stale profile validation could reject/strand recovered running work; retry scheduling raced _run_one cleanup; cancellation during recovery backoff could strand may-have-started work; auto-resume could create a successor session; retry deadline was not preserved across takeover/restart.
- GPT's second audit of 241068612 identified the remaining P1/P2 set: cancellation could be acknowledged before _active_turn existed; definitive provider failure needed an explicit terminal disposition rather than looping forever; queue shutdown needed timer/capacity/worker fencing and queue-first finalization; durable quiescence needed machine-wide active-work accounting; terminal transitions needed retry-deadline clearing.
- Commit e49f56b63 implements those findings: exact unresolved-turn cancellation is latched and consumed by resume_existing; TERMINAL_FAILED is exposed only after EXT-GPTAUTO-003 provider-failure-policy-matched evidence and cleared unresolved checkpoint; queue shutdown fences delayed recovery and post-reservation workers; composition finalizes queue first; durable active work and delayed recovery feed quiescence; takeover preserves recovery metadata and terminal transitions clear retry deadlines.
- Local evidence for e49f56b63: Ruff clean; focused queue, lifecycle, service-application, GPT-auto reconciliation, and bridge suites pass. New tests cover cancellation latching, definitive terminal disposition, delayed retry cleanup, and shutdown cancellation.
- Exact-head GPT reassessment request req_f4d3d4555b69493c is active in the existing session ses_4b29677ed6af4b8c and has produced tool and response-progress activity; no terminal review artifact is available yet. The first attempt req_13a0edec217e409d reproduced the old runtime's ambiguous-side-effect terminalization before GPT answered and was safely reconciled without resend.

## Conclusion

OPEN: e49f56b63 is locally validated and addresses the prior GPT findings, but remains NO-GO for live restart until the exact-head GPT reassessment is terminal and independently confirms no remaining P1 defect. Even after GPT approval, the live gateway must be explicitly approved by the user before restart; the current process is an older generation and has not loaded e49f56b63.
