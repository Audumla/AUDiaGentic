---
id: RV1022
review-of: GP51
plan: gpt-auto-conversation
state: created
reviewed-by: agent
reviewed-at: '2026-09-13T13:45:00Z'
---

# Live restart rehydration rejects active GPT request

## Notes

Do not restart the live gateway again until GPT has reviewed the exact failure and the implementation/tests have been updated and validated. Preserve same request ID and durable provider binding; never create a fresh provider prompt as a recovery shortcut.

## Findings

- Live request req_6748be034eea4035 was active with activity_seq=5 before restart and remained active after the replacement listener came up.\n- Recovery claimed the same request under the new owner epoch, then runtime.rehydrate_session() failed during provider transport.open(); the durable error is EXT-AGW-118 / provider rejected durable session rehydration.\n- The request record retains recovery-required=true and side-effect state may-have-started; it was nevertheless transitioned to failed at 2026-09-13T13:35:55Z.\n- Required correction: a restart-time rehydration/open failure after provider side effects are possible must remain nonterminal and retry/reconcile under the same request/session/provider identity. Only a verified provider terminal result or definitive no-send proof may terminalize; the recovery worker must not drop the active-work marker.

## Conclusion

OPEN: live managed restart preserved the request ID and transferred ownership, but the replacement failed while rehydrating the durable GPT session and terminalized the request as EXT-AGW-118. The request had already submitted and emitted activity, so this is not safe to terminalize without provider outcome proof.
