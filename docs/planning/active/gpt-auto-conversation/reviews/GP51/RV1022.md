---
id: RV1022
review-of: GP51
plan: gpt-auto-conversation
state: created
reviewed-by: agent + dev-gpt-agent
reviewed-at: '2026-09-13T14:16:00Z'
---

# Live restart rehydration rejects active GPT request

## Notes

Do not restart the live gateway again until GPT has reviewed the exact failure and the implementation/tests have been updated and validated. Preserve same request ID and durable provider binding; never create a fresh provider prompt as a recovery shortcut.

## Findings

- GPT static audit of exact fd885aa52 confirmed pre-prompt EXT-AGW-118 is now non-terminal and identity/default replacement protections are correct.
- GPT found P1 gaps: resume_existing post-reattach errors still terminalized; stale profile validation could reject/strand recovered running work; retry scheduling raced _run_one cleanup; cancellation during recovery backoff could strand may-have-started work; auto-resume could create a successor session; retry deadline was not preserved across takeover/restart.
- Additional P2 gaps: retiring queue timers/capacity-wait workers lacked shutdown fencing; deferred recovery was absent from quiescence; defer metadata failures were swallowed; terminal recovery-required was not cleared; recovered capacity source could drift; non-finite retry config was accepted.

## Conclusion

OPEN: fd885aa52 is NO-GO for live restart. The current uncommitted correction addresses the first four lifecycle/identity/backoff findings (including post-reattach deferral, profile-drift bypass for recovered running, cleanup-ordered retry scheduling, successor-session prevention, takeover metadata preservation, terminal flag clearing, source fallback, and finite backoff validation). Cancellation semantics, queue-manager shutdown/quiescence, durable startup retry scheduling, and race coverage still require implementation and review before restart.
