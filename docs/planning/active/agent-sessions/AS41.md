---
id: AS41
order: 41
plan: agent-sessions
state: pending
validate-first: true
priority: P1
work: L
created-by: codex
---

# Map tapped Pi RPC evidence as enrichment onto the existing ACP surface

## Description



## Steps

**Codebase validation (2026-07-29, RV875).** `docs/reference/PROVIDER_CAPABILITY_REFERENCE/harnesses/profiles/pi-acp-status-mapping.md` (pre-existing) already documents that the AS19/AS21 evidence pipeline is currently SKIPPED entirely for Pi -- not partially wired, fully skipped -- because Pi has no validated entry in `harness_observability_inventory.py` (§4.1) and no `HarnessStatusObserverCapability` declared anywhere under `providers/adapters/pi/`. This is the same root gap this session's AS29 validation found from the other direction (pi.yaml has zero `session_surfaces` entries).

**Hard blocking dependency, not yet satisfied: AS29's pi-community-acp descriptor must land first.** Without it, `providers_api.open_harness_status_observer()` (already called generically by `agents_gateway_sessions.py._open_session()`/`_observation_sink()` -- no code change needed there) has nothing to resolve for Pi, so no observer lease is ever acquired and AS41's tap evidence has no ingress path to attach to, regardless of how good the tap-frame mapper is.

**Concrete step sequence once AS29 lands:**

1. Follow `pi-acp-status-mapping.md §3.3` 'Path A' exactly: in `harness_observability_inventory.py`, set `validation_state=VALIDATED`, `effective_production_level=O1`, `lifecycle_source=TRANSPORT` for `pi-community-acp`, add `supported_statuses` and `probe_anchor` pointing at `tests/integration/providers/test_pi_rpc_tap_transcript_e2e.py` (this session's real Docker E2E transcript proof -- genuine probe evidence, not a placeholder), and `platform_evidence` for windows-amd64 (this session's real-binary proof) and linux-amd64 (Docker).
2. Declare `HarnessStatusObserverCapability` for Pi under `providers/adapters/pi/` (the doc notes only OpenCode ACP declares this today).
3. Write the tap-frame-to-`TransportObservation` mapper, following the EXACT pattern of `foundation/transports/acp.py:856` `_map_acp_event_to_observation(acp_event, ag_session_id, turn_id) -> TransportObservation`: closed-vocabulary kind mapping (map real observed pi RPC frame types -- `agent_start`, `message_update`, `tool_execution_start`/`update`/`end`, `turn_end`, `agent_end`, captured for real this session via AS40's tap tests -- to `TransportObservationKind` values), bounded attribute allowlist matching the existing `_ALLOWED_ATTRIBUTE_KEYS` convention, unknown frame types degrade to `TRANSPORT_UNKNOWN` with zero attributes (never leak raw pi RPC payload). This directly fills the gaps `pi-acp-status-mapping.md §4.2` lists as currently dropped by the ACP-only path (`file-change`, `terminal-output`, `plan-update`, fine-grained tool-call status) -- the raw pi RPC stream the tap observes carries this detail even though ACP's narrower `sessionUpdate` vocabulary does not.
4. **Critical correlation constraint (the top implementation risk for this item)**: the tap-frame mapper must reuse the SAME `turn_id` the ACP transport is already using for that session generation -- never mint a second correlation identity from the tap side. Thread `turn_id` into the tap consumer the same way `AS40's rpc_tap_receiver.iter_tap_frames(listener)` is already wired per-session (one tap connection per session, opened before launch) -- the turn_id is known at the call site that owns both the ACP transport and the tap listener for that session, so pass it through explicitly rather than trying to re-derive it from tap content.
5. Wire the tap-derived `TransportObservation`s into the SAME `StatusEvidenceSink`/`SessionEvidenceProjection` pipeline ACP evidence already uses (both exist and are tested -- see AS19/AS21 closure notes) -- do not create a second evidence ingress. Where tap and ACP evidence would imply different states for the same turn, ACP evidence wins (per this item's own `state_inspection` section) -- implement this as a simple precedence check at the point evidence is accepted, not a new conflict-resolution subsystem.
6. Update the `pi-community-acp` surface's evidence-quality declaration (step 1's inventory entry) once the above passes real/synthetic transcript replay, per this item's `capability_maturity` section.

**Testing**: extend the existing real/synthetic transcript replay pattern already established in `tests/integration/providers/test_pi_rpc_tap_real_binaries.py` and `test_pi_rpc_tap_transcript_e2e.py` (both passing, real pi-acp + real pi) rather than building new test infrastructure -- add assertions that decoded tap frames map to the expected `TransportObservationKind`/`EvidenceKind` sequence and reach `SessionLifecycleDecision` via the real `project_session_lifecycle()` reducer, not just that frames were captured. Cover explicitly: tap disconnect mid-session degrades evidence richness only (reuse the existing fail-open `_TapSink` behavior from AS40, already tested); malformed tap frame isolated to tap ingestion (reuse `JsonlTapDecodeError` handling, already tested at the codec level in `test_pi_rpc_tap.py`); ACP-vs-tap state conflict resolves to ACP per step 5.

## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files



## Validation

Use real and synthetic transcript replay for:

- success with settled evidence enriched by tap detail;
- activity and tool use at finer granularity than ACP alone provides;
- partial/final output enrichment;
- tool-only turn;
- cancel via the existing ACP control path, corroborated by tap evidence of the abort taking effect;
- tap disconnect/error mid-session (evidence degrades to ACP-only, session unaffected);
- malformed tap frame (isolated to tap ingestion, does not affect the driven session);
- duplicate/replayed/out-of-order tap events;
- state reads across every phase, verifying ACP-derived state is never overridden by tap evidence.

End-to-end tests must prove AS19-only evidence ingress, AS21 conservative projection, AS31 final-output behavior, SH07 terminal authority, no Pi-specific branch above provider adapters, and that tap presence/absence never changes session outcome -- only evidence richness.

## Effort & Risk



## Standards



## Notes

SUPERSEDED-FIELD NOTICE (2026-07-29, RV878): this item's step 1 (harness_observability_inventory.py validation_state=VALIDATED, effective_production_level=O1) targets the current O0-O4/SurfaceValidationState shape. AS59 simplifies that shape to validated:bool + reference:str. Write the pi-community-acp inventory entry against AS59's shape once AS59 lands, not the field names in step 1 as currently written.

## Goal

Turn the read-only RPC event stream tapped by AS40 into richer AS19 evidence layered onto the session `pi-acp` already drives, without introducing a second control/lifecycle authority. State inspection, final-output retrieval, cancellation, and close continue to be served by the existing `AcpAgentSessionTransport`/`pi-acp` path; this item only widens the evidence available for that same session.

## Dependencies

AS40 complete with pinned real/synthetic tap transcripts and proven byte-transparency. AS29 `pi-community-acp` surface's evidence quality remains candidate/O0 for tap-derived kinds until the evidence matrix below passes for each platform. This item does not create or promote a `pi-rpc` surface.

## Lifecycle Mapping

Map only events proven by Pi's exact RPC transcript/source (reusing the AS27 probe evidence), potentially including message/tool/turn-level detail the ACP `sessionUpdate` vocabulary currently drops (per the gaps already catalogued in docs/reference/PROVIDER_CAPABILITY_REFERENCE/harnesses/profiles/pi-acp-status-mapping.md §4.2 -- `file-change`, `terminal-output`, `plan-update`, partial tool-call detail, compaction, retries, queue state). Pi parsing and native identifiers stay in the Pi adapter. Emit bounded neutral observations with provider-safe correlation, source sequence/order, observed time, and correlation quality, explicitly correlated to the same turn/session the ACP transport is already reporting on -- tapped evidence must never be attributed to a different session generation than the ACP stream it accompanies. The AS19 generic mapper/evidence sink and AS21 projector remain the only route to normalized lifecycle/status.

Do not assume Pi RPC permission events because Pi ACP or documentation contains them. Missing settled/terminal/correlation evidence downgrades confidence/effective capability instead of inventing completion. If the tap disconnects or errors mid-session, evidence quality degrades to what the ACP stream alone already provides -- it must never be treated as session failure.

## State Inspection

State inspection continues to be served by the existing ACP-derived path; this item only adds tap-sourced corroborating evidence (e.g. more granular in-flight tool/message detail) to the same state signal, and must not introduce a second, potentially divergent state source. Where tap evidence and ACP evidence would imply different states for the same turn, ACP evidence (the actual control-plane source) wins; tap evidence is enrichment, never override.

## Final Output Retrieval

Where the tap proves a richer or earlier-available signal for final/partial assistant text than the ACP stream alone, feed it into AS31's existing output-event/final-selector path as an additional evidence source correlated to the same turn -- not a second output channel. Cover: normal assistant text; incremental/partial text; tool-only turn; cancel before/after partial text; provider end without final text; tap disconnect mid-turn (falls back to ACP-only evidence, no data loss beyond reduced granularity); duplicate/out-of-order tap events. Raw Pi message objects, session refs, hidden reasoning, and tool payloads remain private.

## Cancellation

No new cancellation path. `CANCEL_TURN` continues to route exclusively through `AcpAgentSessionTransport.control()` (already ACCEPTED per pi-acp-status-mapping.md §2.4) -- the tap is read-only and must never issue commands. This item may use tap evidence to corroborate that an accepted cancellation actually reached the underlying Pi process (i.e. observe the abort take effect in the RPC stream), improving confidence without adding a control path.

## Close

No new close path. Session close continues through the existing ACP transport's `CLOSE_SESSION` handling; this item ensures the tap and its socket/shim are torn down as part of that same close (per AS40's cleanup requirements), not as an independent lifecycle.

## Capability Maturity

Update the AS29 `pi-community-acp` surface snapshot to reflect improved evidence quality/coverage only to the proven level. There is no separate Pi RPC surface or support matrix to maintain -- tap-derived evidence strictly augments the existing surface's declared capability.

## Acceptance Criteria

- Tapped RPC evidence enriches the existing pi-acp-driven session's AS19 evidence; it never establishes a second lifecycle authority.
- Pi events enter lifecycle only through AS19, correlated to the same session/turn the ACP stream reports.
- Generic clients need no Pi-specific vocabulary.
- Control (prompt/cancel/close) remains exclusively owned by the existing ACP transport; the tap never issues commands.
- Tap disconnect/failure degrades evidence richness only, never session outcome.

## Change Log

- 2026-07-28T22:14:07.931636+00:00 (updated-by): Updated: section:steps
- 2026-07-28T22:14:40.634664+00:00 (updated-by): Updated (no visible changes)
- 2026-07-28T22:22:24.754501+00:00 (updated-by): Updated: section:notes
- 2026-07-28T22:22:50.996623+00:00 (updated-by): Updated (no visible changes)
