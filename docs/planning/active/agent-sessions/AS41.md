---
id: AS41
order: 0
plan: plan-agent-sessions
state: pending
breadth: medium
skill: high
---

# Select Pi ACP or native RPC session surface by configuration

## Description

Provide an explicit Pi session transport selection that retains both ACP and native RPC. Projects choose a declared Pi session surface through standard provider configuration; gateway/session code resolves and snapshots that choice once per session.

## Steps



## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files

src/audiagentic/config/providers/pi.yaml; standard provider config schema/reader/writer; providers session surface resolver/factory registry; providers_api; agents session binding/store snapshot fields; tests/unit/providers/test_pi_session_surface_selection.py; tests/unit/agents/test_session_surface_snapshot.py; tests/docker/test_pi_surface_matrix.py.

## Validation

Focused provider session-surface resolver/config tests, session continuation snapshot tests, architecture test that agents has no Pi-specific selector, then Docker ACP/RPC matrix after AS40. No surface becomes default merely because it is installed; it needs validated declared evidence.

## Effort & Risk



## Standards



## Notes

Reference validation anchors: `docs/reference/PROVIDER_CAPABILITY_REFERENCE/harnesses/profiles/pi.md` plus `pi-status-probe.md` distinguish Pi provider configuration from native RPC runtime facts. `protocols/acp-capabilities.md` sections “Session and external-control matrix”, “Persisted resume is not live attachment”, “ACP and native control transports can coexist”, and “Generic capability doctrine” require ACP and RPC to remain separately selected surfaces. `harness-observability-lifecycle.md` “Capability levels” and “Platform eligibility” require selection/launch availability to stay separate from validated status publication.
