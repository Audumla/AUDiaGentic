---
id: AS49
order: 49
plan: agent-sessions
state: pending
validate-first: true
priority: P2
work: M
created-by: codex
---

# Explicitly resume a dead provider session as a new linked generation

## Description

Add explicit resume-after-death only for an exact AS29 surface whose installed version, identity scope, and native resume operation are validated. Preserve terminal history by creating a new AUDiaGentic Interaction Session generation linked to the terminal source binding. Never reactivate the source record, resume implicitly, select “latest”, silently change Agent Definition/Role/Execution Profile, or silently create a fresh conversation.

## AS59 alignment and precedence

Disposition: keep/amend  
Authority: AS59

- AS49 remains the sole explicit resume-after-terminal item.
- Resume is not same-generation continuation (AS47), attach, replacement, task retry, A2A task continuation or external ACP reconnect.
- The source remains terminal. Success creates one new AS30 Interaction Session generation with `RESUMED_FROM` and predecessor linkage.
- Resume must compare immutable SH02 execution context plus AS60 Execution Profile, AS61 Role, AS62 Agent Definition and AS64 effective-capability context.
- A changed composition is rejected unless an explicit compatibility policy approved through AS59 permits it; default is exact match.
- AS29 exact `resume_by_ref` capability and provider-local adapter remain mandatory. No protocol, CLI or harness fallback is allowed.
- AS65 external ACP and AS66 A2A may request resume through the same application service, but their session/task/context IDs do not authorize provider resume.
- AS63 AgentTask retry or continuation cannot invoke resume implicitly.
- AS49 creates no new session store, task store, protocol mapping store or provider-ref index; AS30 owns all binding/index mutation.
- No implementation under AS49 proceeds before AS59 Gate 0 confirms continuation, resume, attach and replacement plans have non-overlapping ownership.

## Dependencies

- AS29 exact surface capability `resume_by_ref` and compatibility predicate.
- AS30 protected binding, predecessor relation, identity/project/execution/composition fingerprints, and resume transaction.
- SH02 immutable execution context and SH07 durable terminal authority.
- AS60-AS64 immutable composition snapshots/digests when available.
- A provider-local native resume adapter registered through `providers_api`.

## Steps

1. Add one explicit public/application resume request containing the exact terminal source Interaction Session ID plus a client idempotency/control ID. Ordinary prompt continuation and AgentTask submission never invoke resume.
2. Resolve the source record and require it to be terminal, protected, readable in the same canonical project, and associated with an AS30 binding containing a stable opaque provider ref.
3. Resolve the exact current AS29 surface and require validated `resume_by_ref`, matching ref namespace, compatible surface version, same provider identity context, same project, same SH02 execution context, and compatible frozen Agent Definition/Role/Execution Profile/effective-capability context. Unknown values reject the request.
4. Create a new AUDiaGentic Interaction Session ID/generation and `RESUMED_FROM` binding linked to the source binding. The source record and source outcome remain immutable and terminal.
5. Dispatch exactly one provider-local native resume operation through `providers_api`. ACP `loadSession`, RPC resume, App Server thread reopen, or CLI resume are distinct private surface adapters; none is an automatic fallback for another.
6. Persist the new record/binding transactionally through AS30. If provider resume succeeds but persistence fails, detach/close according to ownership and remove provisional binding/index state. If provider resume fails or is uncertain, do not expose a live new generation and do not retry automatically.
7. Return typed errors for unsupported capability, source-not-terminal, stale/missing binding, identity mismatch, project/execution/composition mismatch, version/ref incompatibility, already-resumed conflict, provider rejection, uncertain submission, and persistence failure.
8. Enforce idempotency so repeated client resume requests with the same control/request ID return the original result and cannot create duplicate successor generations.
9. Probe each provider surface independently before promotion. A successful probe resumes known history by exact ref, rejects an invalid ref without opening unrelated history, and proves close/detach cleanup.
10. Map external ACP/A2A requests to this operation only after canonical authorization; never expose the raw provider ref or private adapter choice.

## Files

- Existing agents gateway application/request schema and session workflow.
- AS30 binding service/index and current session schema.
- AS29 resolver and provider-private resume operation contracts exposed through `providers_api`.
- AS60-AS64 immutable snapshot references consumed through AS30.
- Focused unit, integration, race, crash, external-protocol and provider Docker fixtures.

Do not add provider-name branching, CLI fallback heuristics, transcript matching, UI selection, “continue latest”, another session-generation store, a runtime-instance store, an A2A task store, or an external ACP resume store.

## Validation

- explicit resume succeeds only from a terminal source with exact validated capability;
- source remains terminal and immutable;
- successor has a new Interaction Session ID/generation and `RESUMED_FROM` predecessor binding;
- same project, provider identity, execution context, composition context, ref namespace, and version compatibility are enforced;
- changed Agent Definition/Role/Execution Profile is rejected by default;
- missing/invalid ref cannot open an unrelated session;
- unsupported or uncertain operation does not create a fresh conversation;
- idempotent duplicate cannot create two successors;
- persistence rollback leaves no usable orphan binding/transport;
- real installed-version provider probe is required before enabling support;
- external ACP/A2A callers receive the same canonical result and cannot bypass exact-ref authorization;
- ordinary AgentTask continuation/retry never invokes resume.

## Acceptance criteria

- Resume is explicit and never triggered by ordinary continuation, task retry or protocol reconnect.
- A successful resume creates exactly one linked successor generation while preserving source terminality.
- No surface, CLI, account, project, session name, task ID, Agent Card, ACP context, A2A context or “latest” route is used as fallback.
- Unsupported, mismatched, stale or uncertain resume fails deterministically.
- Provider-native resume details and refs remain private behind `providers_api`/AS30.
- AS47 continuation and AS49 resume remain distinct application operations.

## Effort & Risk

Mid, evidence-gated work. Main risks are corrupting terminal history, duplicate successors, changing agent composition across history without explicit policy, and claiming exact resume where the provider only opens a new conversation.

## Standards

Use the canonical AS29 capability snapshot, AS30 transaction/index, SH02 context identity, AS60-AS64 composition identity, SH07 terminal authority, idempotency primitives, redaction, and provider API boundary.
