---
id: AS96
order: 16
plan: agent-sessions
state: in_progress
created-at: '2026-08-06T23:29:18.964082+00:00'
breadth: ''
skill: advanced
created-by: agent
work: M
---

# Complete the Pi RPC tap evidence mapping

## Description

Complete Pi-native, fail-open activity evidence for both supported execution paths without creating a second gateway lifecycle authority. Stateless Pi work stays on the established `pi --print` route; persistent Pi sessions may retain their adapter-private ACP RPC tap.

## Steps

1. Keep the existing Pi `--print` stdin/stdout/exit contract for normal queue work.
2. Materialize an explicit request-private Pi lifecycle extension that appends opaque, bounded facts to the existing request event file.
3. Relay only actual extension/stream events through the existing isolated-worker activity mechanism; never fabricate activity after an observation failure.
4. Retain ACP tap events as optional unsequenced (`None`) enrichment for persistent sessions and ensure neither path can terminalize a request.
5. Prove both qwen profiles return durable output and their activity sequence advances during live one-shot work.

## Detailed Solution & Technical Design

Pi now has two deliberately separate, provider-private observation mechanisms beneath the same gateway activity contract. Normal stateless requests continue to use the descriptor's `pi --print --model` command with stdin prompt delivery and shared completion normalization. `adapters/pi/adapter.py` invokes that existing runner with a narrow launch hook that writes an explicit ESM extension under the request runtime and passes its event-file location only in child environment. The extension appends opaque `pi-lifecycle` records for documented Pi agent/turn/message/tool hooks; the existing worker tails the normalized event file and emits coalesced `provider-progress` activity. If runtime materialization or append fails, Pi's normal output and exit contract continue unchanged and no activity is invented. Persistent ACP sessions retain the optional RPC tap using unsequenced auxiliary facts; the gateway-owned activity sequence remains authoritative in both routes.

## Code Samples & Guidance



## Files

- the evidence sink (source tag + precedence)
- `components/providers/adapters/pi/rpc_tap_evidence.py`
- Pi's descriptor evidence declarations
- the existing real/synthetic transcript replay tests

## Validation

Focused tests prove Pi resolves through its narrow adapter, preserves multiline stdin/no-prompt-argv semantics, materializes an explicit contained extension with a request-private event path, and leaves a missing runtime as a no-observation normal launch. Streaming and worker/ACP tap regressions remain green. Live `dev-mid-agent` request `req_1c48e3210a2849df` completed with exact output `PI_ONE_SHOT_ACTIVITY_OK` and activity sequence 1→6. Live `dev-high-agent` request `req_5a7d13b45fe94f4c` completed with exact output `PI_HIGH_PROFILE_OK` and activity sequence 4. The prior generated agents MCP process startup probes (`agents-runtime`, `agents-config`, `agents-admin`, git, GitHub) passed after their bootstrap-name entrypoint correction. A gateway restart remains deferred while unrelated GPT work is active; the disposable Pi worker loaded and exercised the new code directly.

## Effort & Risk



## Standards

- `arch-standards` §6 Platform and Provider Boundaries — tap evidence maps to neutral observations inside the provider adapter; no Pi-native frame or branch crosses into agents.
- `arch-standards` §3 Evidence-led scope — claim only frame types a real transcript proved.

## Acceptance Criteria

- Tap-origin and ACP-origin evidence are distinguishable at the sink, and ACP wins on conflict by an explicit rule rather than by arrival order.
- Tap evidence enriches the session the ACP transport already drives; it never becomes a second lifecycle authority.
- Only transcript-proven frame types are mapped; everything else degrades to `TRANSPORT_UNKNOWN` with no attributes.
- No raw Pi RPC payload reaches neutral evidence, and no Pi-native type or branch crosses into agents.
- Tap disconnect or failure degrades evidence richness only — session outcome is unchanged.
- Control stays exclusively with the ACP transport; the tap never issues commands.
- The surface evidence declaration matches exactly what is proven, per platform.

## Notes

Supersedes AS40 and AS41, which were the tap-capture half and the evidence-mapping half of one deliverable and had accumulated substantial overlapping progress notes.

The architectural question that stalled the old pair — how to open a tap listener before spawn without leaking `AcpLaunch` across the provider boundary — was resolved with inversion of control and is built. What remains is the source-precedence tag and proof coverage.

RV977 (gpt-auto-reviewer-agent, 2026-08-17, adversarial): this item is **over-justified in its current form**; the thin disposition is a heavy RESIZE toward "optional Pi-private, fail-open auxiliary evidence for transcript-proven facts", with real risk that the correct outcome is DROP. Incorporated consequences:

- **The claimed unique semantic gain is stronger than the evidence.** `rpc_tap_evidence.py` maps `agent_start -> ACTIVITY -> model-thinking`, but the real transcript test proves only presence of `agent_start`/`agent_end`/`message_update`/`turn_end` — it does not prove `agent_start` means model thinking, nor inspect a thinking-specific `message_update` subtype. Do not describe the tap as producing model-thinking evidence unless a transcript proves an explicit thinking event.
- **Do not promote Pi tap observability to a first-class generic feature.** The `PreSpawnHook`/`AuxiliaryObservationSource` seams may stay (they are provider-blind), but no generic capability should depend on the tap; treat it as optional adapter-private enrichment.
- **Unproven frame families are not AS96 obligations.** `tool_execution_*`, `compaction_*`, `queue_update`, `auto_retry_*` should be declared unsupported/out-of-scope for this item and removed from any implied completion horizon; a later transcript justifies a later change.
- **DROP the source-tag/precedence work.** Both `TransportObservation.sequence` and `StatusEvidence.sequence` already allow `None`, and the sink accepts unsequenced evidence carrying no ordering claim. A Pi-local counter is not a canonical turn sequence; emit auxiliary Pi observations with `sequence=None` rather than teaching the shared evidence schema an "ACP wins" precedence rule to resolve a synthetic conflict.
- **"Source-tag is the only open item" is also overstated.** Steps 4 (process-tree cleanup/crash-restart coverage) and 5 (surface-evidence declaration) remain; close only the bounded fail-open cleanup/crash proof and make the descriptor declare exactly the evidence actually proven.
- **Deletable without losing capability:** the source-origin field, ACP-vs-tap precedence policy, speculative mappings for unproven Pi events, and duplicate tap lifecycle interpretation. If an explicit thinking signal cannot be transcript-proven, the strongest thin outcome is to drop the Pi evidence tap entirely — the only loss is advisory status richness.

The earlier ephemeral `params.turn-transport: session` experiment was rejected before release after reviewer feedback: it would change the established stateless Pi transport merely to gain activity. This item instead keeps normal `pi --print` and adds real extension lifecycle observation at the Pi adapter seam. No generic provider routing or sequence-precedence feature is introduced.

## Change Log

- 2026-08-06T23:29:18.964082+00:00 (created-by): Created by agent
- 2026-08-06T23:36:13.686507+00:00 (updated-by): Updated: section:acceptance_criteria

## Codebase Verification 2026 08 05

**Verified against the tree. Step 1's "add a source tag" needs a design correction — the obvious implementation is wrong.**

`StatusEvidenceSourceKind` (`foundation/transports/harness_status_observer.py:21`) is documented as **"Source mechanism for a status observation"** with four values: `transport-observation`, `managed-hook`, `managed-plugin`, `structured-output`. `StatusEvidence` (line 50) carries it as `source_kind`.

**The trap:** the natural-looking fix is to add a fifth value such as `transport-auxiliary` for tap-origin evidence. That is wrong. ACP frames and Pi RPC tap frames are **both** the `transport-observation` *mechanism* — they differ in *origin within that mechanism*, not in mechanism. Adding a fifth enum value would collapse two distinct axes into one field, and §3 permits unifying types only when ownership, lifecycle, validation and semantics all match. Mechanism and origin share none of those.

**Do this instead:** add a separate, narrow origin field to `StatusEvidence` (an identifier for the publishing source within a mechanism), leave `source_kind` meaning exactly what it means today, and implement ACP-wins-on-conflict as a precedence check against that new field in `harness_status_evidence.py::accept()` (line 108).

**Why the conflict is real and not theoretical:** `accept()` validates monotonic sequence ordering, and the ACP transport and the tap allocate sequence numbers from independent counters for the same turn. Without an origin field the sink cannot express "ACP wins" — it can only express "first arrival wins", which is arrival-order-dependent and therefore non-deterministic across runs.

**Bounded by prior art:** `normalize_harness_status_observation` already drops `TERMINAL`/`TRANSPORT_ERROR` observations from every source before they reach evidence, so the tap can only currently affect `ACTIVITY` (-> `model-thinking`). The conflict surface is small today — which is exactly why this is worth fixing before step 3 widens the mapped frame vocabulary on top of it.
- 2026-08-06T23:51:00.287271+00:00 (updated-by): Updated: section:codebase_verification_2026_08_05

## Ledger-events


- chg_20260806_235426_checked-the-new-plan-items-aga_1545
- 2026-08-06T23:54:26.540050+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-26T02:41:03.641803+00:00 (state-transition): State: pending → in_progress
- 2026-08-26T02:42:49.588554+00:00 (updated-by): Updated: section:description, section:steps, section:validation
- chg_20260826_024257_reconciled-the-remaining-plans_2394
- 2026-08-26T02:42:57.424397+00:00 (updated-by): Updated: section:ledger-events
- chg_20260826_025130_pi-backed-agents-now-use-their_3697
- 2026-08-26T02:51:30.659056+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-26T02:53:54.776613+00:00 (updated-by): Updated: section:detailed_solution, section:validation
- chg_20260826_025632_fixed-the-agent-mcp-entry-poin_9605
- 2026-08-26T02:56:32.097493+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-26T02:56:38.982487+00:00 (updated-by): Updated: section:validation
- 2026-08-26T03:07:09.400050+00:00 (updated-by): Updated: section:description, section:steps, section:detailed_solution, section:validation, section:notes
- chg_20260826_030717_pi-midhigh-agents-now-return_3934
- 2026-08-26T03:07:17.648906+00:00 (updated-by): Updated: section:ledger-events
