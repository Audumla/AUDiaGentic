---
id: AS29
order: 29
plan: agent-sessions
state: pending
validate-first: true
priority: P0
work: L
created-by: codex
---

# Resolve, declare, and freeze provider session surfaces and capabilities

## Description



## Steps

**Codebase validation (2026-07-29, RV872): resolver is real and tested, but has zero usable declarations for Pi.** `resolve_session_surface()` (`providers/services/session/session_surface_resolution.py:433`) and `providers_api.resolve_session_surface`/`prepare_provider_session_transport` (`providers_api.py:1440,1467`) are fully implemented -- not stubbed -- with every resolution step from the spec present: enablement check (L456/464), declaration selection (`_select_declaration`, L266-331), blocked-declaration check (L490), platform match (L497), validation-ceiling enforcement (L506), AS27 inventory cross-check (L522), adapter-factory existence check (L539).

**The concrete gap**: `src/audiagentic/config/providers/pi.yaml` has zero `session_surfaces:` entries. Only `opencode.yaml` has them populated (two full entries: `opencode-acp`, `opencode-cli-session`). `pi-community-acp` currently exists ONLY as probe-tracking metadata in `harness_observability_inventory.py` (`HarnessSurfaceCapabilityFact`, `validation_state=PROBE_REQUIRED`) -- not as an actual `SessionSurfaceDeclaration`. This means `resolve_session_surface(project_root, 'pi', ...)` cannot resolve anything today, and AS40/AS41's `build_acp_launch` calls bypass AS29 entirely (called directly from `adapters/pi/acp.py`, not through `providers_api.prepare_provider_session_transport`).

**Steps to close this**:

1. Author a `session_surfaces:` entry in `pi.yaml` for `pi-community-acp`, following `opencode.yaml:219-271`'s `opencode-acp` entry as the exact template (same field shape, same validation).
2. Fields to set, grounded in evidence already gathered this session (AS40 increments 1-5) rather than guessed: `version_constraint: ">=0.80"` (AS27's probe was against pi 0.80.10; this session's real-binary proofs ran against 0.82.1 -- both pass); `identity_operations: {open: supported, attach-existing: unsupported, resume-by-ref: unsupported, discover: unsupported}` (nothing in AS40's tap work proved attach/resume); `ownership_modes: [owned]`; `controls: {cancel-turn: supported, interrupt-turn: unsupported, steer-turn: unsupported, respond-permission: unsupported, close-session: supported}` (per AS41's own notes, cancel-turn is 'already ACCEPTED per pi-acp-status-mapping.md §2.4' via the existing `AcpAgentSessionTransport`); `lifecycle_source: transport`; `lifecycle_installation: none`; `content_channels: [{channel: assistant-text, max_bytes: 65536, max_events: 100}]` (matches AS31's `AgentOutputEvent.MAX_OUTPUT_TEXT_BYTES = 65_536`); `adapter_ref: "audiagentic.components.providers.adapters.pi.acp:build_acp_launch"`.
3. `validation_state`/`effective_level`/`platforms`: set the top-level `validation_state: declared`, `effective_level: O1` (matching opencode's own O1, since Pi ACP has less proven than opencode-acp's O1 baseline -- do not claim O2 without a passed steer/interrupt probe). For `platforms`, add a `windows-amd64` row with `validation_state: validated`, `effective_level: O1`, `probe_artifact: "tests/integration/providers/test_pi_rpc_tap_real_binaries.py"` (this session's real byte-transparency + concurrency proof on this exact host) and a `linux-amd64` row with `validation_state: validated`, `effective_level: O1`, `probe_artifact: "tests/docker/Dockerfile.pi-rpc-tap-e2e"` (this session's real Docker E2E transcript proof). Leave `darwin-arm64`/`darwin-amd64` at `declared`/`O0` (no macOS evidence exists).
4. Run `descriptors/loader.py`'s validation path (`pytest tests/unit/providers -k session_surface` or the loader's own test suite) after adding the entry, to confirm `validate_session_surface_declarations()`'s 5 rules pass (no duplicate `(surface_id, version_constraint)` key, no non-validated O2+, parent-VALIDATED-requires-a-validated-platform-row, content channel bounds present).
5. Once the descriptor entry exists, verify `providers_api.resolve_session_surface(project_root, "pi", ...)` actually resolves it end-to-end with a new integration test (real project root, real pi.yaml) -- this closes the loop AS40/AS41 currently skip by calling `build_acp_launch` directly.
6. Decide, as a separate follow-up (out of AS29's own scope, but blocking full AS19/AS29 integration): should `adapters/pi/acp.py`'s direct `build_acp_launch` callers (in AS40's tap wiring and elsewhere) be migrated to go through `providers_api.prepare_provider_session_transport` instead? Flag this as a finding for whoever picks up AS41, since AS41 explicitly says 'the AS19 generic mapper/evidence sink ... remain the only route' -- but the transport itself isn't currently AS29-resolved.

**Do not** re-derive the resolver, `ResolvedSessionSurface`, or `SessionSurfaceDeclaration` schema -- all three are complete and tested. The remaining work is descriptor authoring plus one integration test, not design.

## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files

- `foundation/transports/session_surface.py` and capability contracts;
- provider descriptor dataclasses, loader/spec/schema validation, and provider YAML;
- provider surface resolver/factory registry and `providers_api`;
- AS19/AS30/AS31 integration;
- Pi/OpenCode/provider descriptor fixtures;
- architecture, descriptor, resolution, platform, and immutability tests.

## Validation

Required matrices:

- exact supported/unsupported/unavailable/misconfigured surface;
- provider disabled and factory absent;
- exact version and platform match/mismatch;
- candidate vs validated effective level;
- two surfaces for one provider with no identity/control/lifecycle/content bleed;
- Pi RPC/ACP explicit selection and frozen continuation;
- config change affects future session only;
- unsupported selection launches no process;
- no provider descriptor/native type imported by agents;
- declared generic capability remains non-executable until validated;
- redacted public snapshot exposes no factory, command, path, token, raw native name, or provider secret.

## Effort & Risk



## Standards



## Notes

SUPERSEDED-FIELD NOTICE (2026-07-29, RV876): the pi.yaml authoring guidance in this item's steps (validation_state/effective_level fields) targets the current O0-O4/SurfaceValidationState shape. AS59 simplifies that shape to validated:bool + reference:str. Author the pi.yaml session_surfaces entry against AS59's shape once AS59 lands, not the field names in this item's steps as currently written.

PROGRESS (2026-07-29, this session — recorded directly, ag-planning MCP was disconnected): `pi.yaml`'s `pi-community-acp` session_surfaces entry has been authored against AS59's `ValidationEvidence{validated, reference}` shape (AS59 was already substantially migrated in-tree by a concurrent session when this work started; `foundation/transports/session_surface.py`, `descriptors/session_surface_declarations.py`, and `harness_observability_inventory.py` already carried the new shape). Two files were still broken mid-migration and were fixed here: `descriptors/loader.py::_build_session_surfaces` (was still parsing `validation_state`/`effective_level` YAML keys — rewrote to parse `evidence: {validated, reference}` at both declaration and platform level) and `services/session/session_surface_resolution.py` (was still importing/branching on the deleted `SurfaceValidationState`/`EffectiveObservationLevel` enums — rewrote `_unsupported_snapshot`, `_build_resolved_surface`, and the `resolve_session_surface` body to carry `ValidationEvidence` end to end; dropped the now-meaningless `blocked-declaration`/`unvalidated-high-level` reasons since AS59 has no ceiling-ladder concept, keeping only the `evidence.validated`-gated AS27 inventory-proof check). `opencode.yaml` was migrated to the new YAML shape alongside pi.yaml so the loader has two consistent real examples.

`pi-community-acp`'s evidence: surface-level `validated: true` referencing `tests/integration/providers/test_pi_rpc_tap_real_binaries.py`; `windows-amd64` platform row validated via that same real-binary proof (pi 0.82.1); `linux-amd64` platform row validated via the AS40 Docker E2E transcript proof (`tests/integration/providers/test_pi_rpc_tap_transcript_e2e.py`, `tests/docker/Dockerfile.pi-rpc-tap-e2e`); `darwin-arm64`/`darwin-amd64` left `validated: false` — no macOS evidence exists. Controls: `cancel-turn`/`close-session` supported per `pi-acp-status-mapping.md` §2.4; identity: `open` only. `adapter_ref` points at the existing `adapters/pi/acp.py:build_acp_launch`.

Verified manually (not yet as a checked-in test — deprioritized this session per direct user instruction to move past test-fixing): `get_descriptor("pi").session_surfaces` loads the entry with correct `ValidationEvidence` on both the declaration and its platform rows, and `resolve_session_surface(root, "pi", SurfaceHint(surface_id="pi-community-acp"))` runs cleanly through the full resolution pipeline (reaches the `disabled-provider` gate on a bare temp project root, as expected — no schema/parsing/type error). Remaining for full closure: (a) a checked-in integration test asserting successful resolution with the provider enabled (step 5 of this item); (b) the ~8 test files under `tests/unit/providers/` and `tests/unit/foundation/transports/` that still import the deleted `SurfaceValidationState`/`EffectiveObservationLevel` names and will fail collection until migrated (tracked under AS59, not this item); (c) the direct-`build_acp_launch`-callers-vs-AS29-resolution question from step 6 remains open, unchanged by this session's work.

## Goal

Provide one provider-owned, exact surface/version/platform manifest and resolver for identity, transport, controls, lifecycle observation, content, generic management capabilities, and validation. It is the sole session-surface declaration and resolution work item.

## Sole Declaration Owner

Extend the existing typed `ProviderDescriptor`/loader/spec path with exactly one `session_surfaces` list keyed by `(surface_id, version_constraint)`. Do not add parallel `session_control_surfaces`, `harness_status_observers`, runtime YAML registries, documentation scanners, or generic capability dispatch registries.

A session-surface entry contains typed sections for:

- transport factory registration;
- identity/mapping operations;
- ownership and ref scope/namespace;
- generic controls;
- lifecycle observation source, installation, correlation, ordering, and effective level;
- user-content channels and bounds;
- generic external-management capability families;
- exact platform/version validation evidence.

Native commands, config paths, adapter refs, tokens, raw event names, payload samples, and secrets remain provider-private and are absent from the resolved snapshot.

## Foundation Value Model

Define or consolidate frozen provider-neutral types such as:

- `SessionSurfaceRef(provider_id, surface_id, resolved_version, platform)`;
- `ResolvedSessionSurface`;
- identity operations: open, attach-existing, resume-by-ref, discover;
- mapping facts: returns opaque ref, concurrent attachments, attach while active, share, replacement relation, same project/context requirements, identity-context requirements, ref scope/namespace;
- ownership modes: owned, adopted, external;
- control support for AS19 canonical actions;
- lifecycle source: transport, hook, plugin, native-api, structured-output, none;
- installation: none, managed-hook, managed-plugin, structured-output;
- content channels: assistant text/final only where bounded and proven;
- validation: declared, probe-required, validated, blocked, unsupported;
- effective observability/capability maturity levels.

`REPLACED` is normally an AUDiaGentic relation implemented by explicit open-new, not an assumed native provider action. `share-existing` remains non-executable until a real consumer and probe exist.

## Generic Capability Families

Carry generic session management capabilities in the resolved surface as small typed family/action records. They may include state read, final-output read, cancel, interrupt, steer, permission response, follow-up enqueue, inspection, compaction/retry evidence, attach/resume/discovery, and close/detach semantics.

Rules:

- declaration and executable validation are separate;
- one provider may introduce a generic family when semantics are clear and unsupported behavior is explicit;
- agents dispatches typed family/action only and never branches on provider name;
- a declared candidate is not executable until exact adapter/probe validation passes;
- AS58 exposes selected capabilities externally; AS29 only declares/resolves them.

## Resolver

Implement/consolidate `providers/services/session_surface_resolution.py` and expose only `providers_api.resolve_session_surface(...)` plus transport preparation APIs.

Resolution must:

1. validate project/provider enablement;
2. read explicit configured surface selection or the provider's explicit declared default;
3. determine exact installed version and runtime platform using existing provider/foundation helpers;
4. select only an exact matching descriptor entry;
5. verify registered transport/observer/capability factories;
6. enforce validation/effective-level ceilings;
7. return an immutable redacted `ResolvedSessionSurface` snapshot;
8. return deterministic unsupported/misconfigured results without launching a child.

No installed-first selection, name similarity, alternate surface fallback, runtime surface switching, or provider-specific branch in agents is allowed.

## Pi Selection

Pi RPC and Pi ACP are independent entries and factories. Project configuration selects exactly one for a new session. The resolved choice and capability snapshot are persisted by AS30 and remain frozen for the generation. Configuration changes affect future sessions only. Neither surface becomes default merely because its executable is installed, and neither inherits capabilities from the other.

## Validation And Evidence Rules

- Product-supported platforms and locally validated platform evidence are distinct.
- A surface is publisher/executor eligible only for the current platform when exact version/mode/correlation proof is validated.
- Linux Docker evidence cannot promote Windows support and vice versa.
- Non-validated routes cannot claim effective O2+ lifecycle, content streaming, attach/resume, or advanced controls.
- Content channels require explicit byte/event bounds.
- Duplicate surface/version, invalid enum/control combinations, missing factories, and incompatible declared/effective levels are schema/loader errors.
- Legacy mixed OpenCode ACP/CLI facts must be split into exact separate surfaces; ACP resume and CLI `run --session` are not one route.

## Migration

Migrate AS19 observer lookup, AS19 transport preparation, AS30 bindings, AS31 content, and provider configurations to the frozen snapshot. Temporary internal readers may translate old fields for one implementation window; no new writer emits old fields. Delete superseded descriptor fields after all callers migrate.

Internal traffic uses frozen dataclasses. Do not add a standalone JSON schema unless an actual external serialized contract requires it. Public diagnostics, if needed, use an explicit allow-list and exclude factories/native details.

## Acceptance Criteria

- `session_surfaces` is the sole runtime declaration owner.
- Exactly one immutable surface/capability snapshot is resolved before execution.
- Capability truth is surface/version/platform specific.
- Pi ACP and Pi RPC coexist without dual ownership or fallback.
- AS19, AS30, AS31, AS40/41, and AS58 consume the same snapshot.
- Shared agents/gateway code contains no provider-specific selector.

## Change Log

- 2026-07-28T11:47:03.885894+00:00 (updated-by): Updated: section:notes
- 2026-07-28T11:48:36.933971+00:00 (updated-by): Updated: section:notes
- 2026-07-28T22:11:02.054400+00:00 (updated-by): Updated: section:steps
- 2026-07-28T22:14:40.626361+00:00 (updated-by): Updated (no visible changes)
- 2026-07-28T22:22:21.159616+00:00 (updated-by): Updated: section:notes
- 2026-07-28T22:22:50.978044+00:00 (updated-by): Updated (no visible changes)
