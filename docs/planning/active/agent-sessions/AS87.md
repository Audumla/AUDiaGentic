---
id: AS87
order: 7
plan: agent-sessions
state: pending
created-at: '2026-08-06T23:26:50.694224+00:00'
breadth: ''
skill: intermediate
created-by: agent
work: M
---

# Final-output selector and internal output read

## Description

Reconcile the two disconnected output writes into one authoritative final result, and add the internal cursor read over the relay's event stream.

Today the durable request-record `output` field (written at terminal transition) and the `OutputRelay` sidecar stream are two independent writes with nothing reconciling them. There is no selector deciding what the public final output actually is when a turn is partial, cancelled, timed out, or produced malformed content.

## Steps

1. Build the final-output selector. It consumes bounded relay state, lifecycle/finalization facts, and the neutral turn result, and handles: normal assistant final; coalesced streaming text plus explicit final; partial text with transport failure; tool-only turn with no assistant text; cancellation; timeout; missing terminal output; malformed or conflicting content events.
2. Insufficient evidence returns a typed degraded/unknown result. Never fabricate success.
3. Transports and adapters cannot choose or persist the public final output directly — only the selector can.
4. The durable request-record `output` field remains the authoritative public result. Do not duplicate final text into sidecar events merely because a final request output exists.
5. Add `read_request_output(request_id, after_sequence, limit)` — a cursor read with a page limit over the relay's events. Today's `read_request_output(project_root, request_id)` has neither.

## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files

- `components/agents/gateway/output.py` (read primitive)
- the final-selector module and its wiring into terminal dispatch
- `components/agents/gateway/dispatch.py` / `session/dispatch.py` at the single terminal transition call site
- tests

## Validation

Six selector scenarios each asserted directly: normal, partial, tool-only, cancel, timeout, and missing-final/conflicting. Plus:

- Insufficient evidence yields a typed degraded result, not a fabricated success.
- A transport cannot write the public final output directly.
- The cursor read returns ordered non-duplicated pages and honours its limit.
- Terminal dispatch still calls the durable transition helper exactly once.

## Effort & Risk



## Standards

- `arch-standards` §3 Ownership and Abstraction — one final-output authority.
- `arch-standards` §4 Public Contracts — a degraded result is typed and explicit, never an invented success.

## Acceptance Criteria

- One selector decides the public final output; transports and adapters cannot choose or persist it directly.
- All six scenarios resolve correctly: normal, partial, tool-only, cancel, timeout, and missing-final/conflicting.
- Insufficient evidence returns a typed degraded/unknown result — never a fabricated success.
- The durable request-record output stays the authoritative public result, and final text is not duplicated into sidecar events merely because it exists.
- A cursor read with a page limit exists over the relay's events, returning ordered non-duplicated pages.
- Terminal dispatch still calls the durable transition helper exactly once.

## Notes

Supersedes AS31 steps 5-6.

The read here is the internal primitive only. The authorized public live-read surface that combines this with status metadata is a separate, later item — splitting them is what lets this one close without waiting on the authorization model.

## Change Log

- 2026-08-06T23:26:50.694224+00:00 (created-by): Created by agent
- 2026-08-06T23:35:38.642254+00:00 (updated-by): Updated: section:acceptance_criteria

## Codebase Verification 2026 08 05

**Verified against the tree. The "two disconnected writes" premise is confirmed, and the disconnection is total.**

The durable write: `gateway/queue/dispatch.py:299-305` takes `output_text = result.get("output")` straight from the provider result and returns it for the terminal record transition. It consults no relay state whatsoever.

The relay write: `gateway/output.py`'s `OutputRelay` persists per-event files and an index under `<request-dir>/output/`.

**Nothing joins them.** A repo-wide scan for callers of `read_request_output` or `get_turn_summary` outside `output.py` itself returns **zero hits** — the relay's persisted stream is written and never read by any production path. So the selector this item builds is genuinely absent, not merely incomplete, and `OutputRelay.get_turn_summary()` (line 494) is currently dead code awaiting exactly this consumer.

**Implementation note for step 5:** the existing `read_request_output(project_root, request_id)` (line 548) reads *all* events by listing and sorting the events directory. Adding `after_sequence`/`limit` is a natural extension of that listing rather than a rewrite — event filenames are 20-digit zero-padded sequences, so a cursor filter is a filename comparison, not a full parse of every file.

**One gap this item should close while it is in here:** `dispatch.py` reads `result.get("output")` with no bound. Every byte cap in the system lives on the relay path; the durable path has none. Whatever the selector produces should be bounded on the way into the durable record, or the caps in AS86 protect only the sidecar and not the authoritative field.
- 2026-08-06T23:53:01.412407+00:00 (updated-by): Updated: section:codebase_verification_2026_08_05

## Ledger-events

- chg_20260806_235426_checked-the-new-plan-items-aga_1545
- 2026-08-06T23:54:26.548407+00:00 (updated-by): Updated: section:ledger-events
