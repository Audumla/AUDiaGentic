---
id: AS61
order: 61
plan: agent-sessions
state: pending
blocked-by: AS60 reassessment
validate-first: true
priority: P0
work: M
created-by: chatgpt
---

# Add minimal reusable roles

## Goal

Add a small provider-independent `Role` that can be used with different Execution Profiles.

For this item, a Role contains only:

- instructions or references to managed instruction assets;
- simple required capabilities where a demonstrated role needs them;
- optional output guidance;
- an optional reserved tool-policy reference/placeholder with no runtime effect.

A Role does not own or alter tools, MCP servers, permissions, provider/model selection, session surfaces, queues, runtime behavior or protocol settings.

Each harness continues to use its existing configured/default tools and permissions.

## Boundary

Expose Role lookup through one narrow public API only where current storage/I/O requires it. Keep concrete loading behind that boundary and inject it into callers. Do not add a service wrapper around pure validation or create a tool, MCP, permission or instruction registry.

## Work

1. Characterize existing instruction handling and reuse its owner.
2. Add one simple Role definition and the minimum storage/resolution needed.
3. Support one Role per Agent Definition.
4. Support only capability checks required by demonstrated Roles.
5. Leave tool/MCP/permission policy as an inert optional placeholder/reference; do not resolve or enforce it.
6. Update all callers directly; delete superseded names, paths, handlers, fixtures and tests in the same change.
7. Add architecture checks preventing provider/runtime fields and concrete-store imports in consumers.

## Do not add

- copied tool, MCP or permission declarations;
- tool-set normalization, merging or overrides;
- tool-policy resolution or enforcement;
- role inheritance or stacking;
- arbitrary merge behavior;
- policy revisions or overlays;
- memory/workflow/delegation frameworks;
- service locators, global registries or compatibility paths.

## Live behavior

Do not add live Role mutation. A different Role applies to a future task or new session unless an existing harness path already supports a safe change without new infrastructure.

## Reassess before AS62

Prove one Role works with two Execution Profiles and one Execution Profile works with two Roles. Remove every field without a current consumer. Confirm current harness tool sets are unchanged and a fake Role boundary is sufficient for composition tests.

## Acceptance criteria

- Role is a small reusable behavioral definition.
- Existing harness tool, MCP and permission configuration remains authoritative and unchanged.
- Tool policy is at most an inert placeholder for a future owning plan.
- No provider/runtime configuration leaks into Role.
- The boundary is DI-ready without adding a container or unnecessary interface.
- No legacy or parallel Role path remains.
