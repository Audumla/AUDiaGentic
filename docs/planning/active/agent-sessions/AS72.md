---
id: AS72
order: 0
plan: agent-sessions
state: in_progress
created-at: '2026-08-02T13:08:55.434701+00:00'
breadth: ''
skill: intermediate
created-by: agent
priority: P1
work: M
---

# Register make_error codes and close the conformance scan gap

## Description

38 of 70 statically-resolvable `make_error(...)` error codes are absent from the error catalogue. Every one of them raises `ValueError: Error code '...' is not registered` instead of the intended AudiaGenticError whenever error resolutions are loaded — so the framework that exists to enforce arch-standards §4 is itself failing, silently, at 38 sites.

Found while closing AS71's first follow-up, which disproved its own premise. AS71 recorded that a "registered, perfectly descriptive `RES-PIACP-001`" was discarded by a bare `except Exception`. `RES-PIACP-001` is not registered; constructing it raises `ValueError`, and *that* was what the bare handler swallowed. The pi-acp-missing path has therefore never produced its intended error message.

The reason no guard caught this: `tests/unit/contracts/test_error_resolutions.py` scans for **quoted code literals** (`"RES-PIACP-001"`). `make_error` assembles the code from `prefix`/`component`/`number` kwargs, so no literal exists anywhere and the scan never sees it. The guard's own docstring claims to cover codes "carried as plain `error_code=` strings" — `make_error` is a third construction path nobody scanned.

## Steps

1. Register the 38 unregistered codes in the owning component's `error-resolutions.yaml`, with real resolution text (what the operator should actually do), not placeholders.
2. Extend `test_error_resolutions.py` with an **AST pass** over every `make_error` call site, resolving `prefix`/`component`/`number` constants into a code and asserting registration. This is the guard that would have caught all 38.
3. Decide the policy for the 22 call sites that compute `number=` dynamically (mostly `runtime/rig/errors.py`, `coding_lsp/lsp_bridge.py`, `language_registry.py`) — they derive the number from a caller-supplied value and cannot be resolved statically. Likely a declared per-component number range validated at registration, so a dynamic code is provably inside a registered block.
4. Fix the code-format validator: `CFG-session-001` and `VAL-release-001` show lowercase component segments pass validation. Decide whether to uppercase the codes or the validator, then enforce one.
5. Re-verify with the full unit suite.

## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files

- `src/audiagentic/config/components/*/error-resolutions.yaml` — register the missing codes
- `tests/unit/contracts/test_error_resolutions.py` — add the AST-based `make_error` pass
- `src/audiagentic/foundation/contracts/errors.py` — code-format validation (step 4), and any per-component range policy from step 3
- Call sites only if a code is genuinely wrong rather than merely unregistered

## Validation

- A new conformance test fails on any unregistered `make_error` code; verified by temporarily adding one.
- The 38 known codes are registered and the new test passes.
- Each registered resolution is checked to actually describe a remedy — a catalogue full of restated codes satisfies the test while defeating its purpose.
- Constructing `RES-PIACP-001` succeeds and produces the intended message rather than `ValueError`.
- Full unit suite green.

## Effort & Risk

Low risk, moderate volume. Steps 1-2 are mechanical and independently verifiable. Step 3 is the only design decision and can be deferred without blocking 1-2. Main risk is registering 38 codes with hollow resolution text, which would pass the test while leaving operators no better off — the validation criteria call this out explicitly.

## Standards

- `arch-standards` §4 Public Contracts — error codes are a stable public contract; a code that cannot be constructed is a broken contract, and 38 of them are.
- `arch-standards` §7 Enforcement — "architecture rules must have proportionate automated checks". The existing check is disproportionate to the rule: it covers two of three construction paths and silently passes the third.
- `arch-standards` §3 Evidence-led scope — the 38/70 and 22-dynamic counts come from an AST sweep of every call site, not from sampling.

## Acceptance Criteria

- Every statically-resolvable `make_error` code is registered; the new AST-based
  conformance pass fails if one is not.
- `RES-PIACP-001` and the other pi/openhands/surface/release codes construct
  successfully and carry their intended messages instead of raising `ValueError`.
- The policy for dynamically-numbered `make_error` sites is decided and recorded,
  so the remaining 22 sites are either covered or explicitly exempted with a
  rationale.
- Error-code format validation is consistent: either all component segments are
  uppercase or the validator accepts lowercase deliberately, not by omission.
- Full unit suite green.

## Notes

Discovered 2026-08-02 while completing AS71's swallowed-error follow-up. AS71's Notes carry the original recording of this finding, made while the ag-planning MCP was unavailable; that note should now point here.

Worth noting the failure chain: an unregistered code raised ValueError, a bare `except Exception` swallowed it, and the caller saw only `transport=None`. Three separate defenses failed in sequence, which is why the AS41 symptom looked like test pollution for as long as it did. The `except Exception` half is already fixed.

### Scope correction, 2026-08-03: a third construction path exists

While starting step 1 (registering the 38 known codes), found `make_error_factory(prefix, component, kind)` in `foundation/contracts/errors.py` — a **third** error-construction path this item's original 38-static/22-dynamic count does not cover at all. It returns a closure with `prefix`/`component` baked in; call sites then invoke it as `_some_error(code_number, message, **details)`. Confirmed at least 20 distinct factory bindings across the tree (`agents_gateway_bootstrap.py`, `agents_gateway_client.py`, `agents_gateway_http_transport.py` x2, `agents_gateway_lifecycle.py` x2, `agents_gateway_remote_client.py` x2, `agents_gateway_service_application.py` x2, `dead_letter.py` x2, `event_observer.py`, `codex/hooks_format.py`, `codex/language_servers.py`, `continue_/mcp_format.py`, `opencode/mcp_format.py`, `openhands/toml_format.py`, `catalog/models.py`, `catalog/source_catalog.py`, and more not yet fully enumerated).

Auditing this path correctly means: (1) find every `make_error_factory(...)` binding and resolve its literal `prefix`/`component`, (2) find every call site of the bound name — including across module boundaries if the binding is imported elsewhere, not just in its defining file — and resolve the literal `code_number` argument to a code, (3) cross-reference against the catalogue same as the direct-call path. This is a materially different (and larger) static-analysis problem than the original AST pass scoped for direct `make_error(prefix=, component=, number=)` calls; it is not a straightforward extension.

**Disposition:** step 1 (registering the 38 already-identified codes) is unaffected and can proceed independently — those are real regardless of this finding. But step 2 (the AST conformance pass) cannot be built correctly until this third path is designed for, and the 38/70 count in this item's Description is now known to be an undercount of the true unregistered-code surface, not a hard total. Re-scope step 2 before implementing it: either extend the same AST pass to resolve factory bindings, or (simpler, more honest about the two mechanisms being genuinely different) write a second, separate conformance check for the factory pattern rather than forcing one scanner to understand both. Not started — stopping here rather than shipping a codes registration pass whose completeness claim would repeat this item's own premise.

## Change Log

- 2026-08-02T13:08:55.434701+00:00 (created-by): Created by agent
- 2026-08-02T13:09:56.565967+00:00 (updated-by): Updated (no visible changes)
- 2026-08-02T13:10:54.131232+00:00 (updated-by): Updated (no visible changes)
- 2026-08-03T00:52:07.591859+00:00 (updated-by): Updated: section:notes
- 2026-08-03T00:58:44.060903+00:00 (state-transition): State: pending → in_progress

## Ledger-events

- chg_20260803_005844_completed-as72-step-1-added-a_9597
- 2026-08-03T00:58:44.111807+00:00 (updated-by): Updated: section:ledger-events

## Step 2 And 4 Landed 2026 08 05

**Steps 2 and 4 complete. Step 1's original 38-code count is now known to have been an undercount (confirmed by this pass) -- 46 more codes fixed/registered here across two distinct defect classes. Only step 3 (dynamic-number policy) remains.**

### Step 4: code-format validator (small, done first)

The two examples this item originally named (`CFG-session-001`, `VAL-release-001`) turned out to be two of **13** real codes with a lowercase component segment, all in `release`/`session`'s own `error-resolutions.yaml` files. Traced each to its real `make_error(component="release"/"session", ...)` call site and confirmed `make_error` always does `component.upper()` when building the actual code -- so all 13 registrations were **completely unreachable** for the real (uppercase) codes actually raised (`get_error_resolution("CFG-SESSION-001")` returned `None` before this fix, despite `CFG-session-001` sitting right there in the YAML with real resolution text). Fixed by uppercasing all 13 keys (`config/components/release/error-resolutions.yaml` x10, `config/components/session/error-resolutions.yaml` x3) -- verified each against every real call site first so no collision with an already-correctly-cased entry occurred. Decision per this item's own step 4 question: **uppercase the codes**, not loosen the validator -- `ERROR_CODE_PATTERN` already requires uppercase and is correct; the registrations were wrong. Added `test_every_registered_error_code_matches_the_canonical_format` (scans every `error-resolutions.yaml` file directly, not the loaded registry, so a load-time collision can't mask a bad key) as the permanent guard.

### Step 2: the make_error_factory conformance pass (the bulk of this session's find)

Built the AST scanner this item's own notes called for, confirming the scope correction already recorded (2026-08-03): `make_error_factory(prefix, component, kind)` closures are a real third construction path alongside direct `make_error(...)` calls and literal code strings, and neither existing literal-string-regex test (`test_every_provider_error_code_literal_has_a_resolution`, `test_every_foundation_error_code_literal_has_a_resolution`) can see either, since the code is never written as a literal string at the call site.

Scope simpler than feared: confirmed by direct grep audit that **no factory binding is ever imported cross-module** (all ~48 `make_error_factory(...)` assignments are module-private helpers, consumed only in their own defining file) -- so same-module resolution is sufficient, no cross-module import-alias tracking needed.

Ran the scanner: **258 statically-resolvable call sites, 171 unique codes, 33 missing** (22 dynamically-numbered call sites correctly excluded -- matches this item's own earlier count). All 33 traced to real call sites and registered with real remedy text (not restated codes, per this item's own validation bar) across two files: `config/components/agents/error-resolutions.yaml` (+7: `CON-AGLC-001/002/003`, `VAL-AGLC-004`, `VAL-AGSV-021/025/026` -- gateway service lifecycle/application) and `config/components/foundation/error-resolutions.yaml` (+26: `CON-ART-002`, `CON-MB-001`, `VAL-MAT-001..004`, `VAL-MCFG-002`, `VAL-RCG-001..004`, `VAL-REC-001/003..009`, `VAL-SPOOL-001..005`, `VAL-STEP-001`, `VAL-STUB-002` -- recipe catalogue/loader/materializer, managed-config, event spool, step factory).

Added the permanent test `test_every_statically_resolvable_make_error_code_has_a_resolution` (AST walk, same technique as the ad-hoc scan) -- self-verified non-vacuous by temporarily renaming a registered key and confirming the test fails with the expected code, then restored.

Constructing `RES-PIACP-001` already worked (step 1's earlier registration); all 33 of this pass's codes now construct successfully too -- verified directly, not just via the new test.

`python -m pytest tests/unit/contracts tests/unit/agents tests/unit/release tests/unit/foundation/toolchains tests/unit/foundation/event -q` green (2 pre-existing unrelated skips).

### Still open: step 3 only

The 22 dynamically-numbered call sites (mostly `runtime/rig/errors.py`, `coding_lsp/lsp_bridge.py`, `language_registry.py`, confirmed by this pass's scan) still need a declared policy -- this item's own suggestion ("a declared per-component number range validated at registration, so a dynamic code is provably inside a registered block") is real design work, not a quick fix, and is explicitly deferrable per this item's own effort_risk note ("Step 3 is the only design decision and can be deferred without blocking 1-2"). Not attempted in this pass.
- 2026-08-04T23:07:37.624302+00:00 (updated-by): Updated: section:step_2_and_4_landed_2026_08_05

## Ledger-events

- chg_20260804_230744_fixed-13-error-codes-that-were_6111
- 2026-08-04T23:07:44.850314+00:00 (updated-by): Updated: section:ledger-events
