---
id: AS66
order: 66
plan: agent-sessions
state: pending
blocked-by: AS65
validate-first: true
priority: P1
work: L
created-by: chatgpt
---

# Publish logical agents and tasks through A2A

## Goal

Expose selected AUDiaGentic Agent Definitions as interoperable A2A agents and allow AUDiaGentic to call remote A2A agents through the same canonical task model.

A2A is a task-oriented external protocol adapter. It does not replace the gateway, SessionRuntime, Execution Profiles, Roles, AS29 Session Surface capabilities, AS30 bindings, observability or output authorities.

## Execution prerequisite

AS59 Gate 0, AS63 reassessment gate R4, AS64 R5 and AS65 R6 must be closed before implementation begins. AS59 must record the final task/session identity model, external protocol application seams and any remaining unsupported ACP/A2A semantics.

## Dependencies

- AS62 logical Agent Definitions, identity, Skills and interface policy;
- AS63 canonical AgentTask submission/status/result;
- AS64 effective capability snapshots;
- AS31/AS57 canonical content and status reads;
- AS58 canonical controls;
- AS30/AS47/AS49 Interaction Session identity and continuation semantics where tasks use persistent context;
- AS65 findings where ACP/A2A share application seams.

## Server surface

For each A2A-enabled Agent Definition:

- generate a bounded Agent Card from logical identity, advertised Skills, supported content modes, authentication scheme and endpoint information;
- accept A2A messages/tasks and map them to `AgentTaskSubmission`;
- create or continue an Interaction Session only according to explicit task/session policy;
- project working/input-required/completed/failed states from canonical task/lifecycle services;
- project assistant output and artifacts from AS31/task result services;
- map cancellation through AS58/current task control;
- persist explicit bindings between A2A context/task IDs and AUDiaGentic task/session IDs without reusing provider refs.

Provider, model, selected harness surface, launch command and raw session identity remain private implementation details.

## Client and delegation transport

Add an A2A client transport behind an internal `AgentTaskService`/delegation port:

- discover/describe configured remote agents from explicit trusted Agent Card sources;
- submit bounded tasks/messages/artifacts;
- stream or poll task status/results;
- cancel where supported;
- preserve remote task/context identity as opaque external refs;
- apply authentication, allowlists, budgets, timeouts and audit.

Local agent-to-agent dispatch should use direct internal task submission when possible. Remote or third-party agents use A2A. Callers should not need to know which transport was selected.

## Harness access to delegation

Do not inject the A2A SDK directly into Pi, Codex, Claude Code or OpenCode.

Expose one narrow provider-neutral AUDiaGentic delegation contract:

```text
agents_find
agents_describe
agents_delegate
agents_task_status
agents_task_result
agents_cancel
```

The tool/extension layer invokes the canonical AgentTaskService. That service selects local dispatch or A2A transport. Tool descriptions provide mechanics; the resolved Role/Agent delegation policy controls when delegation is permitted.

### Harness integration matrix

| Harness | Initial integration | Native delegation replacement | Required provider surface | First-slice behavior |
|---|---|---|---|---|
| Pi | managed Pi extension registering a `subagent`-compatible or `agents_*` tool | yes, where the profile selects `audiagentic-only` or `prefer-audiagentic` | existing generated-surface/extension install plus isolated launch context | preserve single, parallel and chained delegation shapes where bounded; stream progress and cancellation through AgentTaskService |
| OpenCode | generated custom tool or MCP delegation server; optional plugin only after contract stability | partial initially; native `task` remains unless policy disables/discourages it | existing custom skill/tool/plugin/MCP projection paths | expose `agents_*`, generate instructions, later assess plugin-based native UI/interception |
| Claude Code | managed MCP delegation server plus generated CLAUDE.md/Skill policy | no transparent native Agent-tool replacement in first slice | existing managed-MCP and generated-surface paths | model invokes `agents_delegate`; optionally policy denies/discourages native subagents; no wrapper subagent that adds another LLM hop |
| Codex | managed MCP delegation server plus AGENTS.md/Skill policy | no transparent native `spawn_agent` replacement in first slice | existing project-scoped MCP and generated-surface paths | model invokes `agents_delegate`; native multi-agent remains separate unless policy disables it |
| Deprecated Gemini/other MCP harnesses | managed MCP delegation server | no | verified MCP config | generic `agents_*` only; no bespoke investment without active provider status |

### Delegation mode policy

Each Agent Definition or resolved Role may select one closed mode:

```text
audiagentic-only
prefer-audiagentic
native-allowed
native-only
```

Rules:

- `audiagentic-only`: provider launch/config must disable, hide or explicitly instruct against native delegation where the harness supports that safely; failure to enforce is a capability/policy error.
- `prefer-audiagentic`: publish AUDiaGentic tools and instructions; native delegation remains available as fallback only under policy.
- `native-allowed`: both are available with no automatic interception.
- `native-only`: do not install AUDiaGentic delegation tools for that materialization.

Do not claim transparent replacement where the harness has no supported interception/extension seam.

### Provider integration capability facts

Add a typed provider-owned `delegation_integration` declaration only after the AgentTaskService/tool contract is fixed. It may describe:

```text
mechanism: extension | plugin | custom-tool | mcp | instructions
native_tool_replacement: true|false
supports_streaming
supports_cancel
supports_parallel
supports_chain
supports_background
supports_progress_ui
refresh_mode
validation_state/evidence
```

This is installation/integration evidence, not Session Surface capability truth and not A2A support. AS29 remains unchanged as session transport authority.

## Delegation policy

Initial policy is deterministic/default-deny:

- explicit allowed source Agent/Role;
- explicit allowed target Agents/Skills;
- bounded subtask and artifact sizes;
- no secret, credential, hidden-reasoning or unrelated workspace disclosure;
- explicit workspace relationship: same workspace read-only, isolated copy/worktree, artifact-only, or no filesystem sharing;
- depth/recursion limit;
- task, token/cost/time budgets;
- source Agent remains responsible for the original request;
- returned results are advisory until validated;
- no automatic broad network discovery or arbitrary Agent Card registration from harnesses;
- no delegation to the same logical Agent/session in a recursion loop unless an explicit workflow permits it.

Begin with gateway-controlled workflows. Model-directed delegation is enabled only per Role/Agent policy after tooling and audit are proven.

## Streaming and task semantics

Map canonical events rather than raw harness events:

- task accepted/working/status -> A2A task status and harness tool progress;
- AS31 assistant deltas/final -> A2A message/artifact updates and bounded delegation tool output;
- permission or missing information -> `input-required` only where the caller can respond and correlation is preserved;
- terminal result -> completed/failed/cancelled plus final artifacts;
- AS58 cancellation -> canonical task/turn cancellation acknowledgement, never fabricated terminality.

Coalesce high-frequency internal events, preserve terminal/input-required events, bound buffers and allow detached continuation/reconnect according to the A2A task contract. Do not automatically kill a persistent task/session merely because one streaming client or harness tool call disconnects.

## Steps

1. Consume completed AS65/R6 and AS63/R4 decisions from AS59.
2. Select and pin a current A2A SDK/spec version behind a component-local adapter.
3. Freeze the provider-neutral AgentTaskService/delegation tool contract and authorization model.
4. Add Agent Card projection from `AgentDefinitionSnapshot` and effective capabilities.
5. Add A2A server executor mapping to canonical task services.
6. Add task/context ID bindings, authorization and redaction.
7. Add status/output/artifact streaming and reconnect behavior.
8. Add A2A client transport for trusted configured remote Agents.
9. Add local-vs-remote dispatch abstraction.
10. Implement Pi extension proof first, using the existing Pi extension/generated-surface ownership and isolated launch context.
11. Implement shared MCP delegation server and provider projections for Claude Code, Codex and OpenCode.
12. Add generated harness-specific instructions and closed delegation mode policy.
13. Add OpenCode custom-tool proof; defer richer plugin integration until the tool contract and plugin API are stable.
14. Add end-to-end implementer -> reviewer proof with one local cross-harness Agent and one remote/wrapped A2A Agent.
15. Document third-party A2A/MCP bridges as compatibility/test fixtures only, not runtime authorities.
16. Record R7 findings and amend AS59/follow-on plans before broader discovery or autonomous delegation work.

## Reassessment gate R7

After the first end-to-end proof:

- review whether task-to-turn mapping is sufficient or AS63 needs a dedicated multi-turn task coordinator;
- review whether Agent Skill schema maps cleanly to real Agent Cards and revise without leaking runtime details;
- review local direct dispatch versus A2A semantic parity;
- review delegation security, recursion, budgets, artifact/workspace transfer and failure handling;
- compare Pi extension, OpenCode custom tool/plugin and MCP integrations for common contract versus provider-specific behavior;
- decide whether transparent native delegation replacement is justified for additional harnesses;
- decide which third-party A2A/MCP bridges remain useful as test fixtures or optional adapters;
- decide whether broader discovery, push notifications or additional transports are justified by demonstrated use.

## Validation

- Agent Card correctness/redaction/stability;
- task submission, streaming, reconnect, cancellation and final artifact;
- optional persistent context across multiple tasks only when policy/session binding allows;
- A2A IDs cannot be used as provider session refs;
- local and remote dispatch produce equivalent canonical task results;
- cross-project/Agent/Skill authorization;
- malformed/untrusted Agent Card and authentication failure;
- bounded buffering, backpressure and disconnected client continuation;
- delegation default-deny, recursion, workspace and budget enforcement;
- Pi extension single/parallel/chain/cancel behavior through AgentTaskService;
- Claude/Codex/OpenCode MCP projections use the same schema and authorization;
- `audiagentic-only` enforcement fails closed when native delegation cannot be disabled or reliably discouraged;
- no wrapper subagent introduces a redundant model hop for Claude/Codex;
- no raw ACP/provider events, hidden reasoning, secrets or unrestricted filesystem data;
- end-to-end two-Agent collaboration across different underlying harnesses;
- one remote A2A target behaves through the same delegation tool contract as a local target.

## Acceptance criteria

- Selected Agent Definitions are discoverable and callable through A2A.
- AUDiaGentic can invoke trusted remote A2A Agents through the canonical task service.
- Local and remote Agents share one task/delegation abstraction.
- Pi can route its subagent experience through AUDiaGentic using a managed extension.
- Claude Code, Codex and OpenCode can invoke AUDiaGentic Agents through a shared MCP/tool contract.
- Native delegation replacement claims are evidence-backed per harness rather than assumed.
- Existing gateway/session/transport/output/control authorities remain singular.
- R7 findings and any follow-on plans are recorded in AS59.
