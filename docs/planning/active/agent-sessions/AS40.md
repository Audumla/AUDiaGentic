---
id: AS40
order: 40
plan: agent-sessions
state: in_progress
validate-first: true
priority: P1
work: L
created-by: codex
---

# Implement a read-only Pi RPC observation tap over the existing pi-acp launch

## Description



## Steps



## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files

- Pi RPC tee shim executable/script and its AG-owned counterpart in `providers/adapters/pi/`;
- revision of `_materialize_pi_command_wrapper` in `providers/adapters/pi/acp.py` to install the shim instead of (or in addition to) the current transparent passthrough;
- Pi AS29 surface capability update only after probe;
- existing runtime-path, managed-process/adopted-child, configuration rendering, and cleanup primitives (reused, not duplicated);
- unit transcript/codec/tap tests;
- integration and Docker exact-version fixtures proving pi-acp transparency with the shim installed;
- architecture tests keeping Pi-specific parsing inside the adapter.

## Validation

- byte-transparency proof: pi-acp's ACP output is identical with/without the shim, across the full validate-first transcript fixture set;
- codec/frame/bounds tests on the AG-bound tap copy;
- tap failure/crash tests proving the underlying pi-acp session is unaffected;
- per-request runtime/socket isolation tests;
- simultaneous two-session non-contamination proof (single real process per session);
- normal/cancel/timeout/crash/restart cleanup tests for the shim and socket;
- AS30 binding compatibility (no new binding shape required);
- Windows named-pipe and Linux Docker Unix-domain-socket proof for each platform promoted.

## Effort & Risk



## Standards



## Notes

See dependencies field for the 2026-07-28 status update (RV860) -- blocking preconditions are now satisfied.

STATUS UPDATE 2026-07-28 (RV864): design's use of a 'tee shim' asserted against ARCHITECTURE_STANDARDS.md and CREATING_A_HARNESS.md -- COMPLIANT, no design change required. See RV864 for full finding.

IMPLEMENTATION STARTED 2026-07-28 -- first increment landed:
1. New probe evidence (docs/reference/PROVIDER_CAPABILITY_REFERENCE/harnesses/profiles/pi-acp-bridge-probe.md): inspected the actually-installed pi-acp 0.0.32 bundle -- previously undocumented; the prior AS27 probe (pi 0.80.10, 2025-07-19) found no separate pi-acp package at all. Confirms: PI_ACP_PI_COMMAND is the real spawn hook (matches acp.py's existing wrapper); argv is hardcoded to [--mode, rpc, --no-themes, (--session <path>)?]; the child inherits pi-acp's own process env (already reachable via AcpLaunch.environment, no new plumbing needed); stdio is fully piped (stdin/stdout/stderr); pi-acp reads stdout via readline, LF-delimited, tolerant of non-JSON prelude lines (ANSI-stripped, buffered separately) rather than erroring; pi-acp drains and discards stderr entirely; on child exit all pending RPC promises reject (cleanup path already exists on the pi-acp side).
2. New src/audiagentic/components/providers/adapters/pi/rpc_tap.py: JsonlTapDecoder -- strict UTF-8, LF-delimited JSONL codec with bounded frame size (default 1 MiB) and bounded parse-error tolerance (default 50) for the AG-bound tap copy only. Never raises; a bad/oversized frame degrades observation evidence for that frame only and the decoder goes inert once its error budget is spent, matching the cleanup_and_recovery contract ('a tap failure degrades observation evidence, never terminates or corrupts the driven session').
3. tests/unit/providers/test_pi_rpc_tap.py -- 13 tests covering single/multi-frame decode, split-chunk reassembly across feed() calls, CRLF stripping, blank-line skipping, invalid-JSON/invalid-UTF-8/oversized-frame handling (both newline-terminated and unterminated-buffer cases), bounded-error-then-inert behavior, and interleaved good/bad frames resolving independently. All passing.

NOT YET BUILT (remaining AS40 scope): the tee shim executable/process substituted at PI_ACP_PI_COMMAND itself (spawns the real pi child, fans stdout to two sinks); the socket (POSIX)/named-pipe (Windows) transport and AG-side receiver connecting to it; byte-transparency proof that the pi-acp-bound passthrough copy is untouched; per-request runtime/socket isolation tests; the two-simultaneous-session non-contamination proof; full normal/cancel/timeout/crash/restart cleanup-and-recovery tests; AS29 surface capability update after probes pass; AS41's evidence-mapping consumption. This item stays in_progress, not completed.

## Goal

Give AG full-fidelity `pi --mode rpc` observation evidence for sessions launched via the existing maintained `pi-acp` ACP wrapper, without spawning a second Pi process, without AG independently driving/commanding the session, and without modifying or forking `pi-acp` itself.

## Dependencies

AS19, AS21, AS29, AS30, and AS31 must be merged/present and green. Pi ACP (`pi-acp`) remains the sole command driver (stdin writer, prompt/control owner) of the underlying Pi process; this item adds a read-only observation channel alongside it, it does not replace, fork, or independently drive Pi.

STATUS UPDATE 2026-07-28 (RV860, codebase-wide reassessment): this precondition is now SATISFIED, not open. AS19/AS21/AS29's canonical contracts are confirmed substantially implemented (see their own STATUS UPDATE notes); AS31 (foundation/transports/agent_output.py OutputSink, agents_gateway_output.py OutputRelay) confirmed present and matching spec. AS30 has one named gap (resume_binding/replace_binding) that does not block this item's read-only observation-tap scope. AS40 is ready to move toward implementation -- it is the most concrete unblocked next AS item found in this reassessment. See RV860 for full finding.

## Validate First

Inspect the installed Pi version and RPC protocol source/docs (reuse the AS27 probe evidence already captured in docs/reference/PROVIDER_CAPABILITY_REFERENCE/harnesses/profiles/pi-status-probe.md, and the new pi-acp-bridge-probe.md covering the actually-installed pi-acp 0.0.32 bridge's spawn mechanics). Freeze exact-version transcript fixtures for initialization, prompt, incremental messages, tool events, agent end/settled behavior, abort, and shutdown, as observed on the tapped stdout stream -- STILL PENDING, not yet captured. Prove the tee shim is fully byte-transparent to `pi-acp` (no observable difference in `pi-acp`'s ACP behavior with vs. without the shim installed) -- STILL PENDING, shim not yet built. Do not implement guessed messages or infer RPC parity from Pi ACP's own (narrower) event vocabulary.

## Runtime Isolation

Reuse the existing per-request runtime root and isolation already implemented by `pi/acp.py::build_acp_launch` (unique `PI_CODING_AGENT_DIR`, `HOME`, session-dir, etc. under `request_runtime_root`). No separate Pi-specific runtime tree is created by this item. Add, within that same runtime root, a per-request Unix domain socket (POSIX) or named pipe (Windows) at a deterministic path that AG creates and listens on before launch. This is a duplex IPC endpoint, not a file being polled/tailed.

## Codec Process Layer

Implement a tee shim substituted at the existing `PI_ACP_PI_COMMAND` hook in `providers/adapters/pi/acp.py::_materialize_pi_command_wrapper` (today a transparent exec/passthrough `.cmd`/`.sh` script). The shim:

- spawns the real `pi --mode rpc` binary as its own child process (the one and only Pi process for this session);
- passes `pi-acp`'s stdin straight through to the real child's stdin, byte-for-byte, untouched -- `pi-acp` remains the single writer, so there is no command multiplexing and no request-ID correlation risk;
- fans out the real child's stdout to two sinks: (a) back to `pi-acp` unchanged, so its ACP behavior is provably identical to today, and (b) connected out to AG's per-request socket/named pipe, read-only;
- performs strict UTF-8 LF-delimited JSONL parsing only on the AG-bound copy (sink b) -- the pi-acp-bound copy (sink a) is raw passthrough and must never be transformed;
- applies maximum frame size and bounded parse errors on the AG-bound copy only;
- captures stderr as bounded diagnostics only;
- terminates deterministically when either the real Pi child exits or `pi-acp` closes its end, and its own exit must not be distinguishable by `pi-acp` from today's direct exec.

On the AG side, implement a private receiver in `providers/adapters/pi/rpc_tap.py` (or similar) that connects to the socket/pipe, applies the same strict JSONL codec, and treats the connection as read-only telemetry ingress. No raw frame publication outside the adapter.

## As19 Transport Implementation

This item does NOT implement an independent `AgentSessionTransport` (no open/prompt/control lifecycle of its own). Prompt, control (cancel/close), and session lifecycle continue to flow exclusively through the existing `AcpSessionTransport`/`pi-acp` path. This item implements only an observation ingestion component that layers additional `TransportObservation`s onto the same session the ACP transport already owns, sourced from the tapped RPC stream instead of (or in addition to) the ACP `sessionUpdate` frames. AS41 maps these into AS19 evidence as enrichment, not as a competing lifecycle authority. No hook/plugin/tap becomes a parallel prompt path.

## Binding And Capability Registration

After exact probes pass:

- extend the existing `pi-community-acp` AS29 surface's declared observation capability/evidence quality using tapped-RPC evidence, rather than registering a new `pi-rpc` surface;
- persist no additional session binding -- the tap rides on the binding already created for the `pi-acp`-driven session (AS30);
- leave unproven evidence kinds unsupported/effective O0 until validated per kind;
- do not claim RPC-derived control capabilities (cancel/steer/permission-response) beyond what `AcpAgentSessionTransport.control()` already proves -- the tap is observation-only.

## Cleanup And Recovery

Tie the shim's and the socket/pipe's lifetime to the same gateway session/request lifecycle already governing the `pi-acp` launch. Cleanup must run on:

- normal close (pi-acp session close cascades to shim exit);
- prompt cancellation;
- timeout;
- tap codec/protocol error (must not affect the underlying pi-acp session -- a tap failure degrades observation evidence, never terminates or corrupts the driven session);
- process crash (either the shim or the real Pi child);
- gateway/service shutdown;
- gateway restart discovery of abandoned runtime directories/sockets;
- failed shim installation before pi-acp launch (fall back to the existing transparent passthrough so the ACP session still launches without the tap, degraded to no enrichment).

Recovery must identity-check owned processes/sockets and remove or quarantine abandoned runtime directories deterministically. The tap must never become a single point of failure for the underlying ACP session.

## Required Concurrency Proof

Run two simultaneous Pi sessions (each launched via pi-acp with the tap installed) and prove:

- exactly one real `pi --mode rpc` process per session (never two);
- distinct runtime roots, sockets/pipes, and shim processes per session;
- no event/response cross-contamination between the two tapped streams;
- `pi-acp`'s own behavior (session outcome, ACP event stream received) is bit-for-bit identical whether or not the tap is installed;
- independent cleanup of each tap without affecting the other session.

## Acceptance Criteria

- Exactly one `pi --mode rpc` process exists per pi-acp-driven session; the tap never causes or requires a second Pi process.
- pi-acp's stdin remains single-writer; AG never issues commands into the tapped session.
- pi-acp's own ACP behavior is provably unaffected by the tap's presence.
- AG receives full native RPC event/response fidelity via a read-only, non-file-based (socket/named-pipe) channel.
- Tap failure degrades observation evidence only; it never terminates, corrupts, or duplicates the underlying ACP session.
- AS41 can map tapped RPC evidence as enrichment through shared AS19 contracts with no Pi branch in agents.

## Change Log

- 2026-07-28T11:47:09.632719+00:00 (updated-by): Updated: section:notes
- 2026-07-28T11:59:57.954469+00:00 (updated-by): Updated: section:notes
- 2026-07-28T12:06:14.756645+00:00 (state-transition): State: pending → in_progress
- 2026-07-28T12:11:52.400770+00:00 (updated-by): Updated (no visible changes)
- 2026-07-28T12:12:09.098250+00:00 (updated-by): Updated: section:notes
