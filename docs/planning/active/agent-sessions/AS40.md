---
id: AS40
order: 0
plan: plan-agent-sessions
state: pending
breadth: large
skill: high
---

# Implement Pi native RPC session transport and status mapping

## Description

Expose Pi native `--mode rpc --no-session` through the existing AS28 provider-neutral session transport, then publish only proven correlated status evidence through AS19/AS21. Pi ACP remains optional/secondary; do not promote it as Pi’s primary route.

## Steps

1. Inspect the installed Pi RPC protocol source and freeze a fixture transcript before adapter code.
2. Implement a private strict-JSONL process/codec layer with deterministic shutdown.
3. Implement AgentSessionTransport and inbound normalized observation mapping.
4. Add Pi RPC capability adapter hooks that use AS44 contracts, with no external gateway API.
5. Build fake-process unit/integration tests; run Docker exact-version probe.
6. Only then add/update AS27/AS29 proof facts.

## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files

src/audiagentic/components/providers/adapters/pi/rpc_transport.py; adapters/pi factory/exports; config/providers/pi.yaml after proof only; tests/unit/providers/test_pi_rpc_codec.py; tests/unit/providers/test_pi_rpc_transport.py; tests/integration/agents/test_pi_rpc_transport_status.py; tests/docker Pi probe fixture/image.

## Validation

Focused Pi transport and provider session-surface tests plus AS19/AS21 end-to-end fixture; Docker native Pi RPC probe before production promotion. Assert no ACP imports in Pi RPC adapter public seam; no raw frames in agents events/timeline; terminal protocol evidence remains non-authoritative to request/job state.

## Effort & Risk



## Standards



## Notes

Reference validation anchors: `docs/reference/PROVIDER_CAPABILITY_REFERENCE/harnesses/profiles/pi-status-probe.md` is the authoritative Pi native RPC command/event/cleanup discovery record; use its exact strict-LF JSONL and observed event vocabulary, not a guessed RPC protocol. `docs/reference/PROVIDER_CAPABILITY_REFERENCE/harnesses/profiles/pi-acp-status-mapping.md` identifies the separate ACP mapping and explicitly prevents assuming Pi RPC permission events. `harness-observability-lifecycle.md` sections “Evidence model”, “Evidence precedence”, “Capability levels”, “Harness capability mapping”, and “Validation scenarios” define correlation, terminal, O-level, and Docker proof acceptance. `protocols/acp-capabilities.md` validates that Pi ACP is a distinct optional adapter surface, not a substitute for Pi native RPC.
