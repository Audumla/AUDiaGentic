---
id: AS94
order: 14
plan: agent-sessions
state: pending
created-at: '2026-08-06T23:28:44.696270+00:00'
breadth: ''
skill: intermediate
created-by: agent
work: M
---

# Live turn read surface combining status and output

## Description

Expose deterministic public status/output polling over AS86 artifacts and AS102 sanitization, with lifecycle semantics owned by AS92. Do not retain a separate selector or diagnostic contract.

## Steps

1. Implement the thin status projection over canonical lifecycle/activity/outcome fields.\n2. Dereference only request-owned verified artifacts for the full response operation.\n3. Apply AS102’s allowlist and bounds at the public boundary; never expose paths, raw records, or diagnostics in ordinary status.\n4. Add endpoint convergence and artifact fail-closed tests.

## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files

- one read module composing the metadata poll and the output read
- gateway application/client/service operations and one MCP read tool
- tests including a service-restart fixture with a fake streaming transport

## Validation

Status is bounded and stable; response dereference verifies size and digest; wrong request/project/path/digest cases fail closed; all public surfaces converge on the same projection.

## Effort & Risk



## Standards

- `arch-standards` §3 Ownership and Abstraction — this composes two existing authorities; it owns neither and creates no third store.
- `arch-standards` §8 Protocol Adapter Boundaries — the MCP tool translates and serializes only.

## Acceptance Criteria

- An in-project caller can observe a live turn without controlling it — there is no caller-identity concept here, only project scoping (see threat_model_correction_2026_08_05).
- Status and output stay separate channels with distinct kinds, retention and source-native sequences — assistant text never merges into lifecycle evidence.
- One canonical live-read façade exposes two source-native lanes (or one operation with channel selection); NO public/global sequence and NO delivery-state field.
- The vector cursor packages the two source continuation positions plus request/session/turn context binding and does not assign domain ordering.
- Reconnect from a vector cursor delivers no duplicate accepted events.
- Metadata-restart and output-truncation gaps are each owned by their source (AS93 and AS87 respectively, RV926) and are deterministic and distinguishable from one another; AS94 owns only outer cursor syntax and request/session/turn context binding. No aggregate outer gap/truncated state is synthesized.
- Reading has no execution side effect — no runtime creation, turn lock, queue mutation or lifetime renewal.
- No third store is created; both channels read from their existing owners.
- Durable replay is never claimed for in-memory metadata, and the final durable record stays readable after the live stream ends.
- Protocol adapters consume this surface rather than reading transports, output files or event stores directly.

## Notes

Supersedes AS57.

Removed from the old item and owned later: the authorization model (lease validation, project/session scoping, protocol-id-cannot-authorize), and the redaction rules for what may cross into the envelope. The old item's own text made those roughly half its content and was the reason it read as unfinishable.

The protocol adapters consume this surface. They must not read provider transports, output files or event stores directly — that constraint stays here because it is a boundary rule, not a security rule.

RV976 (gpt-auto-reviewer-agent, 2026-08-17, adversarial): **RESIZE AS94 substantially, do not drop it.** The thin version is a side-effect-free canonical live-read façade over two independently authoritative reads, NOT one globally ordered stream. Incorporated consequences:

- **DROP the public/global sequence.** There is no common ordering authority (AS93 uses session-sequence+process-generation; AS87 uses its own durable ordinal; the output store persists its own index). A stable scalar interleaving would require either a new shared ordering authority/store (violating no-third-store) or an unstated cross-source fence/clock contract; read-time timestamp sorting is unstable under late arrivals and ties. Keep source-native coordinates. If a mixed `channel=status|output` list is retained for convenience, its ordering must be explicitly presentation-local and non-authoritative.
- **RESIZE to one canonical façade returning two source-native lanes** (e.g. `status_page` + `output_page`, or a vector-cursor response). Keep "one operation"; drop "one globally ordered stream". AS94's strongest legitimate purpose is preventing protocol adapters from reaching into transports, output files or event stores directly.
- **Outer cursor = opaque/versioned vector** carrying the two source cursors plus context binding (request/session/turn). Drop aggregate outer `gap`/`truncated` — those facts have different meanings and authorities on each lane; keep them on the owning channel. `has_more` may remain as a mechanical `status.has_more OR output.has_more`.
- **DROP `delivery state`** from the envelope. If it means acknowledged/watermark state, reconnect semantics require mutation and consumer-specific persistence, contradicting the pure-read contract. If it only derives availability from the two reads, it is a misnamed derived field.
- **Reconnect = per-source continuation positions.** A cursor carrying AS93's next position/generation and AS87's next ordinal is sufficient to prevent source-level redelivery; the outer cursor packages them and binds them to request/session/turn but must NOT assign domain ordering.
- **KEEP the protocol-adapter boundary** unchanged (adapters consume the canonical surface rather than transports, relay files or event stores).
- **FOLD/RESIZE AS93's private metadata-read primitive into AS94** if no separate metadata-only consumer emerges — rewrite AS94 around capabilities/authorities (metadata read capability + AS87 output read capability → canonical live-read façade), not around plan-item symmetry.

RV926 (2026-08-09): three edits incorporated above — (1) step 4 no longer validates cursor *generation* itself (that's AS93's metadata-source semantics) or output corruption/truncation (that's AS87's); AS94 owns outer syntax/context binding only. (2) Acceptance criteria's stale "authorized caller" wording replaced with the project-isolation framing already established in threat_model_correction_2026_08_05. (3) "request/task id" ambiguity resolved to request id only, per AS63's landed AgentTask-as-handle shape — there is no second task-id space.

RV925 (2026-08-09): of the two blocked-by items, only AS87's paged-relay-read half (step 5, now returning a typed page/source-state contract) is a hard prerequisite; AS87's final-output-selector half (steps 1-4) is independent and does not gate this item. Blocked-by is left as "AS93, AS87" rather than split, since AS87 is kept as one item.

GPT review classified AS93 as redundant standalone work. Its useful mechanics are now explicitly owned here; AS93 is superseded and must not be revived as a separate endpoint or store.

## Change Log

- 2026-08-06T23:28:44.696270+00:00 (created-by): Created by agent
- 2026-08-06T23:36:06.234900+00:00 (updated-by): Updated: section:acceptance_criteria

## Threat Model Correction 2026 08 05

**This item previously deferred an authorization model to AS101. That model is gone — read this before implementing.**

The deployment is local, internal, non-hostile, not externally facing, with one shared gateway serving multiple projects. AS101 has been rewritten from a caller-authorization model to **project isolation**, and the caller-authentication, lease-validation and protocol-id-cannot-authorize requirements are dropped entirely.

**What this item should consume from AS101 instead:** a read must be scoped to the requesting project. That is the only isolation boundary. Within a project there is no per-caller filtering — a harness may observe another harness's turn, and that is intended.

**What does not change:** the side-effect-free property (no runtime creation, no turn lock, no lifetime renewal, no queue mutation) stays exactly as specified. It was never a security constraint — it is a correctness constraint about reads not perturbing execution, and it is the most important thing this item asserts.

**Simplification available:** with no lease model to consult, the read path needs only the project context already carried on the request. Do not build a caller-identity concept for this item.
- 2026-08-07T00:06:24.200802+00:00 (updated-by): Updated: section:threat_model_correction_2026_08_05

## Blocked-by

AS93, AS87
- 2026-08-07T04:40:56.166751+00:00 (updated-by): Updated: section:blocked-by, section:steps

## Ledger-events


- chg_20260807_044121_finished-rationalizing-both-pl_2461
- 2026-08-07T04:41:21.929096+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-10T08:41:43.418386+00:00 (updated-by): Updated: section:steps, section:acceptance_criteria, section:notes


- chg_20260810_084237_reviewed-all-open-as-plan-age_9308
- 2026-08-10T08:42:37.177725+00:00 (updated-by): Updated: section:ledger-events
- chg_20260822_054608_large-terminal-responses-are-n_6435
- 2026-08-22T05:46:08.205877+00:00 (updated-by): Updated: section:ledger-events
- chg_20260823_034148_large-gateway-responses-no-lon_9402
- 2026-08-23T03:41:48.822304+00:00 (updated-by): Updated: section:ledger-events
- chg_20260823_041753_stopped-sending-bounded-respon_4257
- 2026-08-23T04:17:53.707359+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-26T02:18:10.643348+00:00 (updated-by): Updated: section:description, section:steps, section:notes
- chg_20260826_021824_the-active-architecture-plan-s_9536
- 2026-08-26T02:18:24.639261+00:00 (updated-by): Updated: section:ledger-events
- chg_20260826_022419_the-session-plan-set-is-smalle_9542
- 2026-08-26T02:24:19.518456+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-26T02:42:49.664417+00:00 (updated-by): Updated: section:description, section:steps, section:validation
- chg_20260826_024257_reconciled-the-remaining-plans_2394
- 2026-08-26T02:42:57.413756+00:00 (updated-by): Updated: section:ledger-events
