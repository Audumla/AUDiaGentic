---
id: AS53
order: 53
plan: agent-sessions
state: pending
validate-first: true
priority: P1
work: M
created-by: codex
---

# Recover proven orphaned turns through task-owned cleanup

## AS59 disposition

Disposition: keep  
Authority: AS59  
Position: Platform track, position 7.

- Sole proven-orphan recovery path. SH07 remains the durable request authority; AS53 adds no second reconcile API.
- Not blocked by AS59. Consumes AS52's process evidence, so it follows AS52 directly.

## Description

Detect a proven dead owned harness during an active turn and recover without manually releasing asyncio locks or mutating scheduler sets. Track the owning turn task/request and process evidence, move through normal lifecycle, and let existing owner finally blocks reclaim lock/semaphore/accounting exactly once.

## Steps

0. Preserve the already-landed slices: turn timeout/silence timeout/cancel wiring are policy/cancel mechanisms, not orphan proof; `agents.session.orphaned` is declared but must not be emitted until proven-dead ownership evidence exists.

1. Locate the live session runtime ownership structures before editing: `rg "class SessionRuntime|request_cancel|turn_task|session.turn|orphaned|child_pid|creation" src/audiagentic/components/agents src/audiagentic/foundation`. Reuse existing task ownership, process evidence, and finally blocks; do not manually release `asyncio.Lock`, semaphores, `pq.running`, or `pq.idle`.

2. Extend `SessionRuntime`/session handle state with explicit current-turn ownership if it is not already present:
```python
current_turn_request_id: str | None
current_turn_id: str | None
current_turn_task: asyncio.Task | None
owned_process_evidence: OwnedProcessEvidence | None
closing_deadline_at: float | None
```
Use existing AS52/foundation process evidence types if available. Evidence must include owned/adopted vs external, pid/process identity/creation time where supported, and an observation timestamp.

3. Add a pure predicate helper, e.g. `is_proven_owned_turn_dead(handle, now) -> ProvenDeadResult`, with these rules:
- true only when the handle owns/adopted the process and the process identity proves the same child/descendant is dead or unrecoverably exited during a non-terminal current turn.
- false/conservative for unknown process state, missing identity, external/unowned process, stale pid reuse risk, timeout/silence alone, or missing current-turn task.
- result carries a bounded reason code, never raw process command lines, prompts, protocol payloads, or secrets.

4. Implement reaper entry point, e.g. `SessionRuntime.reap_proven_orphan(now=None) -> OrphanReapResult`, or a queue/session manager helper if that is the existing ownership boundary. It may inspect non-quiescent handles but may act only on `is_proven_owned_turn_dead(...) == true`.

5. On proven death, transition through normal lifecycle:
- record one redacted session timeline event on `agents.session.orphaned` with session id, turn id/request id, evidence reason code, and ownership mode.
- mark session closing/failed through existing session store helpers, using close reason `failed-orphaned` or an existing registered equivalent; extend schema/error resolution only if missing.
- cancel/signal the owning turn task or close the transport through existing foundation seams.
- let the owning task's `finally` path release locks/slots/accounting exactly once.

6. Add bounded force-close only after a closing deadline and only for proven owned/adopted descendants through AS52/foundation process cleanup. Never signal external processes. Unknown remains non-actionable and should produce diagnostic evidence at most.

7. Coordinate SH07 request outcome by calling the existing durable request transition authority exactly once. Do not add a new recovery/reconciliation API. Terminalize the active request with the existing registered failed/interrupted reason that matches proven harness death. If the required reason is not yet registered, add it to the existing SH07 transition/error vocabulary in this plan and migrate every in-repository consumer; do not leave an unresolved contract gap or create a second authority.

8. Add tests before broad integration:
- fake owned child exits mid-turn -> active to closing/failed once, one orphan event, owner finally releases lock/slot once.
- unknown process state -> no orphan event, no kill, no state change.
- external process -> no signal/kill.
- pid reuse/stale identity -> conservative no-action.
- silence timeout -> no orphan event.
- force-close deadline uses only owned descendant cleanup seam.
- pending next turn fails/unblocks without deadlock after owner cleanup.

9. Docker validation is required before AS53 completion: real owned process tree dies mid-turn, no descendant remains after force-close deadline, and an external process fixture is not signalled.

## Files

Likely implementation: `src/audiagentic/components/agents/agents_gateway_sessions.py` or the current `SessionRuntime` module; AS52/foundation process evidence module discovered by `rg "OwnedProcess|ProcessEvidence|creation_time|descendant" src/audiagentic`.

Event/constants: `src/audiagentic/components/agents/agents_event_topics.py`, `src/audiagentic/config/components/agents/events.yaml` only if additional event metadata is missing; `agents.session.orphaned` already exists and should be reused.

Request coordination: existing SH07 gateway request store/dispatch/recovery modules only through their public transition helpers.

Tests: `tests/unit/agents/test_agents_gateway_sessions.py` plus a focused orphan/reaper file if clearer; Docker/integration test under `tests/integration/agents/` for real process-tree proof.

Forbidden edits: manual lock/semaphore release, direct queue set mutation, second project-wide reconcile API, provider-specific orphan heuristics, signalling external/unowned processes.

## Validation

AS53 is valid only when tests prove: proven owned death emits exactly one orphan event; unknown/silence/external evidence does not; cleanup runs through existing owner/foundation finally paths exactly once; SH07 request outcome is coordinated through the durable request authority; Docker proves real owned descendant cleanup and external process non-interference. Unit-only event declaration is not sufficient to close AS53.

## Effort & Risk

Mid operational correctness. Main risk is double cleanup, addressed by task ownership and existing finally paths.

## Standards

Architecture Standards; Architecture Guidelines; Observability Standards where events/timelines are involved; docs/reference/PROVIDER_CAPABILITY_REFERENCE. Reuse foundation paths, workflow, events, process evidence, and managed-service seams before adding component-local machinery.


## Existing baseline to preserve

Turn timeout, silence timeout, cancellation signalling, child identity capture, and the declared `agents.session.orphaned` topic may already be implemented. Timeout/silence remain policy, never proof of death. SH07 recovery remains the only durable request recovery authority. AS53 implements only proven-owned-death detection, emission, close/failure coordination, and bounded force-close through AS52.

## Acceptance criteria

- Exactly one orphan event is emitted only for proven dead owned/adopted process identity during a nonterminal owned turn.
- Unknown, silence-only, external, and PID-reuse evidence cause no kill and no orphan state change.
- Cleanup flows through the owning task/transport/foundation `finally` paths exactly once.
- SH07 terminal outcome is coordinated through its existing durable transition authority.
- Docker proves owned descendant cleanup and external-process non-interference.
