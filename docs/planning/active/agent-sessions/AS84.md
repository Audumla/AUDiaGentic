---
id: AS84
order: 4
plan: agent-sessions
state: pending
created-at: '2026-08-06T23:26:11.908168+00:00'
breadth: ''
skill: advanced
created-by: agent
work: M
---

# Role capability vocabulary and launch manifest resolver

## Description

Complete only the mechanism-only capability vocabulary/evidence/manifest resolver needed by AS85. SU02/SU03 own canonical models and immutability; do not duplicate them here.

## Steps

1. Define typed capability evidence and requirement values using the existing authority discriminator.\n2. Implement pure deterministic Role→launch-manifest resolution and fingerprinting through providers_api only.\n3. Reject unknown, evidence-only, missing, conflicting, or dependency-invalid requirements without partial success.\n4. Integrate the frozen manifest into AS85 admission without downstream re-resolution.

## Detailed Solution & Technical Design

Use frozen CapabilityRequirement, LaunchCapabilityEvidence, LaunchContribution, and RoleManifest types. Vocabulary is data, not provider-name branching. Resolver validates dependency closure, returns all unmet requirements in deterministic order, and never returns partial success. Fingerprint canonical JSON with sorted keys and normalized tuples using the repository digest. Explanation stays outside the digest. Resolver performs no I/O, registry access, mutation, or clock reads.

Steps 1+2 are one invariant: Role must be frozen/slots AND RoleStore must enforce store-only replacement (no _roles mutation, no aliasing of mutable instances) in a single commit — no window exists where the resolver could see mutable state.

Fingerprint scope is explicitly limited to typed requirement IDs. Governance_policy, recommended_tools, and mutable metadata fields on the broader Role model are excluded from fingerprint material by design; those may survive as advisory/preset data but never enter the mechanism-only resolver.

## Code Samples & Guidance



## Files

agents models/role.py, role store/API, new vocabulary/resolver/manifest modules and schema; narrow providers_api evidence type/read; test_roles_architecture.py and resolver/fingerprint tests; AS85 seam.

## Validation

Resolver and admission tests cover unknown/evidence-only requirements, authority mismatch, deterministic ordering/fingerprint, immutable nested values, and no downstream config reads.

## Effort & Risk

Medium blast radius, advanced design. Risks are provider policy leakage, incidental fingerprint ordering, and mutable escape hatches.

## Standards

Typed immutable contracts; pure core; API-only boundaries; mechanism/policy separation; deterministic identity; no hidden aliases.

## Acceptance Criteria

- A role plus project/provider/surface evidence resolves to an immutable manifest with a stable fingerprint, reproducible across processes.
- The manifest contains only what the role requires — unrelated components and MCP surfaces are absent from it, not merely disabled.
- Missing or unknown evidence resolves to unsupported. No permissive fallback exists anywhere in the resolver.
- Every vocabulary term traces to a demonstrated Role that needs it, and the vocabulary contains no provider-name conditionals.
- The resolver imports no component or provider internals; `providers_api` is the only approved cross-component import.
- The resolver is not a composition binding and mutates no process-global registry — both asserted directly.
- Provider capability facts come from the provider catalogue and surface facts from AS29. No second capability model is defined for either.
- Role fixtures ship as tests, not as product presets.
- No policy is implemented here — privileged-capability authorization, escalation behaviour and presets are absent by design.
- (RV938) Evidence-only capabilities are rejected at the Role boundary — not just documented but enforced. Fingerprint explicitly excludes governance_policy, recommended_tools, and metadata fields.

## Notes

RV905 incorporated. Immediate fix 2026-08-09: replaced provider-tree substring/path allowlist with AST import-boundary check and focused negative fixture. Mutable Role/store/resolver work remains open; TE02 no longer carries false-positive failure.

RV938 (gpt-auto review, 2026-08-10): Claims 1-4 verified correct. Claim 5 ('no mutable escape hatches') is FALSE — and broader than stated: RoleStore.get() returns stored mutable Role objects by reference; role_api.py directly mutates store._roles; callers can mutate after passing to add(). Steps 1+2 must be treated as one invariant (immutable Role values + store-only replacement) before deterministic resolver/fingerprint is built. Authority discriminator not enforced at Role boundary — evidence-only capabilities can appear in required_capabilities because requirements are just strings. Fingerprint scope must explicitly exclude governance_policy, recommended_tools, mutable metadata fields from manifest inputs.

V4 runsheet linkage: AS84 owns the mechanism-only role vocabulary/resolver portion of Phase 1/Phase 3 convergence. Blocked-by SU01 and AS72 for the inventory/error-outcome contract. It is the existing owner for Role/RoleStore/manifest immutability; SU03 must not create a duplicate Role model. The later equivalence audit is the stop gate before any canonical CapabilityId implementation.

GPT review resized AS84: SU02/SU03 absorbed the legacy store/model cleanup. AS84 now owns only the distinct capability vocabulary/evidence/manifest resolver needed by AS85; do not duplicate immutable model work or introduce role presets.

## Change Log

- 2026-08-06T23:26:11.908168+00:00 (created-by): Created by agent
- 2026-08-06T23:35:25.793891+00:00 (updated-by): Updated: section:acceptance_criteria

## Codebase Verification 2026 08 05

**Verified against the tree. The Detailed Solution's domain list was slightly wrong — corrected here with the real inventory, which is the concrete starting point for step 1.**

Loaded via `capability_catalogue.load_catalogue()`. The catalogue exposes exactly three public members — `kinds_by_id`, `kinds_by_domain`, `families` — and holds **13 domains / 20 kinds**:

| Domain | Kinds |
|---|---|
| `acp` | `acp` |
| `cli` | `cli-install` |
| `exec` | `execution-isolation`, `launch`, `launch-isolation` |
| `ext` | `host-extensions` |
| `file` | `agent-files` |
| `hook` | `hooks` |
| `lsp` | `lsp-config`, `lsp-self-support` |
| `mcp` | `mcp` |
| `model` | `model-config-projection`, `model-local-state`, `models` |
| `obs` | `obs-transport-observability` |
| `perm` | `permissions` |
| `plugin` | `plugins` |
| `surface` | `surface-instruction`, `surface-skill`, `surfaces` |

The earlier list in this item omitted `ext` and gave domains without kinds. **Kinds, not domains, are the useful unit for step 1** — a role requirement maps to a kind (`execution-isolation`, `launch-isolation`, `permissions`), not to a domain.

**`CapabilityKind` already carries an `authority` discriminator** — `provisioned` / `operational` / `evidence-only`. That distinction is directly load-bearing for this item's least-capability rule: an `evidence-only` kind describes an observed fact and can never be *requested* by a role, whereas a `provisioned` kind can. Use the existing discriminator rather than inventing a parallel one; §3 forbids a second model over the same facts.

The two isolation-tier read APIs named in the Detailed Solution are confirmed present on `providers_api` and are the right first vocabulary terms: `get_provider_execution_isolation_tier` and `get_provider_mcp_launch_isolation_tier`. Note both are **provider-wide** facts, not per-capability entries in this catalogue — so the vocabulary will need to consume from two shapes (descriptor-level tiers and catalogue kinds), which is worth designing for explicitly rather than discovering mid-implementation.
- 2026-08-06T23:52:53.507822+00:00 (updated-by): Updated: section:codebase_verification_2026_08_05

## Ledger-events


- chg_20260809_001907_fixed-immediate-error-contract_3697
- 2026-08-09T00:19:07.092131+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-10T09:59:36.049788+00:00 (updated-by): Updated: section:notes, section:detailed_solution, section:acceptance_criteria
- 2026-08-10T09:59:58.541705+00:00 (updated-by): Updated: section:notes
- 2026-08-10T10:00:27.276066+00:00 (updated-by): Updated: section:notes, section:steps
- 2026-08-10T10:01:19.523217+00:00 (updated-by): Updated: section:notes, section:steps
- 2026-08-10T10:01:40.590502+00:00 (updated-by): Updated: section:notes
- 2026-08-15T02:31:03.062811+00:00 (updated-by): Updated: section:notes
- chg_20260815_023117_added-the-v4-convergence-runsh_4511
- 2026-08-15T02:31:17.200893+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-26T02:18:03.440904+00:00 (updated-by): Updated: section:description, section:steps, section:notes
- chg_20260826_021824_the-active-architecture-plan-s_9536
- 2026-08-26T02:18:24.568590+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-26T02:42:49.702244+00:00 (updated-by): Updated: section:description, section:steps, section:validation
- chg_20260826_024257_reconciled-the-remaining-plans_2394
- 2026-08-26T02:42:57.369935+00:00 (updated-by): Updated: section:ledger-events
