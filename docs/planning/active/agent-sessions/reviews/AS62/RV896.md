---
id: RV896
review-of: AS62
plan: agent-sessions
state: created
reviewed-by: ''
reviewed-at: ''
---

# AS62 implementation review via gpt-auto

## Notes

Review performed by gpt-auto agent session (req_6a638f4838da4ce7, ses_62785fc9c2a54587) against commit 4a52979.

## Findings

## HIGH — AS63 does not consume the resolved Agent Definition

AgentTaskFactory.submit() calls get_agent_definition() and forwards only execution_profile_id. It never calls resolve_agent_definition(). Consequences:
- A definition referencing a missing Role can still submit if its Execution Profile exists
- Role instructions and required_capabilities are not part of admission
- Publication flags and advertised skills are ignored
- AS64 has no resolved Role/surface input for capability checks
- The execution manifest records profile/provider/model but not agent/Role identity
- AS63 test claims to prove a genuine consumer of resolve_agent_definition() but would pass with the current raw lookup

## HIGH — Step 3 deferral blocks AS64

Step 3 (surface resolution) is legitimately blocked locally, but AS64 explicitly requires AS29 resolved surface facts and Role requirements during admission. AS64 declares only blocked-by: AS63 reassessment, which is incomplete.

## HIGH — AS62 and AS63 contradict on direct profile submission

AS62 requires deletion of the direct Execution Profile work-selection path and no dual selection path. AS63 retains AgentTaskFactory.submit_raw(execution_profile_id=...) as a supported lower-level API. These positions cannot both remain acceptance criteria.

## MEDIUM — Publication flags test doesn't prove no effect

Test only creates a definition with acp=True/a2a=True, resolves it, asserts booleans round-trip. Never compares MCP projection or provider permissions before and after changing flags. No enforcement of internal semantics — Agent Definition with internal=False remains selectable.

## MEDIUM — Architecture tests are string scanning, not import inspection

One test performs unrestricted substring checks; one splits source into lines and scans lines beginning with import/from. Only the forbidden dataclass-field check performs actual Python introspection. Can miss multiline imports, aliased imports, dynamic imports, importlib calls.

## MEDIUM — RV890 incomplete at consumer boundary

Correct for core resolver/store, but:
- Step 3 provider/session-surface dependency is absent (deferred)
- Production callers don't explicitly inject the resolver
- AgentTask consumer doesn't call the resolver at all
- Step 6 partially implemented by AS63 but bypasses the resolver

## MEDIUM — implementation_landed_2026_08_04 is materially stale

Multiple claims don't match commit 4a52979:
- File paths are under components/agents/models/ not components/agents/
- Step 4 (validate Role requirements) only verifies existence/schema loading
- Step 5: no agent/Role identity reaches execution manifest
- "No AgentTask consumer exists yet" is false — AS63 landed in same commit
- Publication non-effect only proven by boolean round-trip
- Architecture checks use string matching, not import inspection
- AS63 partially implements step 6 but bypasses the resolver

## Conclusion

AS62 should remain in_progress. Core AgentDefinition model, CRUD API, resolver shape, and RV890 no-composition correction are broadly sound. However, the resolved Agent Definition is not the effective admission input, AS64 is blocked by work not represented in its dependency chain, and the implementation_landed section is materially stale against the final integrated commit.
