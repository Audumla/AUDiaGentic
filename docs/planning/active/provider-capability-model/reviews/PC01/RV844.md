---
id: RV844
review-of: PC01
plan: provider-capability-model
state: created
reviewed-by: claude
reviewed-at: ''
---

# FINAL taxonomy: naming (provisioned + bare nouns), boundary rule, and full-field gap check

## Notes

Consolidated record of all naming/structural decisions taken with the user. Supersedes the naming in the shipped _capabilities.yaml/_families.yaml. Component findings RV841 (hooks), RV842 (isolation tiers + alignment), RV843 (obs-neutral + surfaces) fold into this. The shipped catalogue must be updated to match.

TERMINOLOGY (user decisions):
- Authority tier 'automation' -> 'provisioned' (it is NOT automation: nothing auto-runs; the harness reconciles declared resources only when code is registered). Field automation_capabilities -> provisioned_capabilities.
- Kind ids: bare resource nouns; drop the '-config' suffix (domain+tier already give context).

BOUNDARY RULE (pin in PC01, quotable):
`capabilities:` describes what a provider can do and what the harness manages or reads ABOUT those capabilities. A provider's IDENTITY, LIFECYCLE, and LAUNCH WIRING (name, url, access_mode, deprecation, isolation tiers, launch/exec specs) stay orthogonal fields — they are not capabilities. Classify by CONSUMER, not topic: consumed by a family handler -> provisioned; read by a component decision/listing -> operational; read by nothing but the fact renderer -> evidence-only; consumed as launch/admission wiring or pure identity -> orthogonal.

THREE-DOMAIN NAMING BOUNDARY (do NOT unify): the word 'capability' names three UNRELATED concepts in this codebase. (1) PROVIDER capabilities — this model. (2) LSP PROTOCOL capabilities — coding_lsp/lsp_constants.py (textDocument methods, client/server caps); external spec term. (3) SESSION capabilities — agents gateway AS33 (per-session runtime snapshot). Only (1) is governed by this catalogue. Keep our usage precise ('provider capability'); never fold (2)/(3) into the catalogue.

FEATURES vs CAPABILITIES (PC05): foundation 'features' (FEATURE_SCOPE_IMPLEMENTATION, enable/disable gating) are a PROJECTION of provider capabilities, not a separate list. feature_mapping._CAPABILITIES is a parallel field->kind table (a 4th enumeration) and is retired in PC05 — impl features derive from capability instances. Capabilities = declaration; features = enable-gated projection.

## Findings

FINAL KIND CATALOGUE (renamed + corrected):

PROVISIONED (family-backed; harness writes/reconciles; modes plan/apply/prune/status):
- cli-install    (family cli-lifecycle)      <- cli_install (+ cli_probe as inner modifier)
- mcp            (family managed-mcp)         <- mcp_config (+ remote inner modifier)
- model-endpoints(family model-projection)    <- model_config (+ model_entry_renderer folded into mechanism)
- model-catalog  (family model-projection)    <- fetch_catalog_fn
- language-servers(family language-server)    <- language_servers_config + on_lsp_enabled + lsp_support_probe {approach: config|hook|mcp-fallback}
- plugins        (family plugin-entry)        <- plugin_config
- hooks          (family managed-hooks)        <- hooks_config   [RV841: previously MISSING, orphaned family]
- surfaces       (family generated-surfaces)  <- surfaces + skill placement (skill_surface_path | contribution-file)
- instructions   (family generated-surfaces)  <- instruction_file  [distinct SECTION capability; may share a target file with surfaces per user decision; block-per-capability composition; agent_files ownership DERIVED from block paths]

OPERATIONAL (no family; read for a decision/listing):
- permissions       <- permissions           (display/status)
- host-extensions   <- host_capabilities      (extension detect/status)
- agent-files       <- agent_files            (file ownership/feature map; surface-file ownership derived, not re-declared)
- connectors        <- supported_connectors   (model-source projection eligibility)
- model-credentials <- vendor_key_injection   [MOVED from evidence-only: it is READ by key-injection logic in models.py:182 + providers_api.py:1143, so it is operational not evidence]
- lsp-mcp-receive   <- receive_lsp_mcp         (ag-lsp MCP projection gate)

EVIDENCE-ONLY (recorded, not read by logic; render to docs; mandatory external: anchor):
- acp-support           (consolidates 4 acp-* )
- model-local-state     (was model-config-surface)
- model-config-projection
- obs-harness-status    [RV843: replaces provider-named obs-opencode-*; opencode carries member-scoped evidence instances under this neutral kind]
- execution-isolation   [RV842: provenance for execution_isolation_tier; mechanism none]
- launch-isolation      [RV842: provenance for mcp_launch_isolation_tier; mechanism none]

ORTHOGONAL (NOT capabilities): provider_id, display_name, description, url, prompt_aliases, access_mode, annotations, session_surfaces, launches, execution, interactive, acp, execution_isolation_tier (value), mcp_launch_isolation_tier (value), deprecated.

GAP CHECK / THINGS WE MISSED (full 37-field pass):
1. DROP exec-adapter kind. execution is launch wiring (like launches/interactive/acp) -> orthogonal per the boundary rule. The shipped catalogue's exec-adapter (tier-enum) violates the rule and should be removed.
2. DROP depr-status kind. deprecated is provider lifecycle -> orthogonal.
3. model_entry_renderer was UNMAPPED. Fold into the model-endpoints mechanism (alongside reader/writer/remover), not a separate kind.
4. model-connectors renamed connectors; keep operational (consumed by models.py:278).
5. cli_probe and lsp_support_probe are NOT separate kinds: cli_probe folds into cli-install mechanism; lsp_support_probe into language-servers mechanism (companion probe).
6. NO dead fields found: every orthogonal field has a live consumer (prompt_aliases->tag resolution, model_entry_renderer->projection, session_surfaces->AS29, launches/execution/interactive/acp->launch builders).

GOVERNANCE VALIDATIONS to add in PC01 (each would have caught a real defect this pass):
- provider-neutral kind ids: reject a kind id containing any known provider_id token (would have caught obs-opencode-*).
- orphaned-family: every family in _families.yaml must be referenced by >=1 kind (would have caught managed-hooks).
- evidence-vs-consumer: an evidence-only kind whose field is read by non-renderer logic is misclassified (would have caught vendor_key_injection).
- retire-guard renumber: PC04 retired-key check must NOT reuse VAL-PCAP-013 (already mechanism-schema); use VAL-PCAP-014.

## Conclusion

AUTHORITATIVE END-STATE + DECISIONS.

Final kinds by tier (20 total): PROVISIONED (8): cli-install, mcp, models, plugins, hooks, language-servers, surfaces, instructions. OPERATIONAL (6): permissions, host-extensions, agent-files, connectors, model-credentials, launch. EVIDENCE-ONLY (6): acp-support, model-local-state, model-config-projection, obs-harness-status, execution-isolation, launch-isolation. ORTHOGONAL: provider_id, display_name, description, url, prompt_aliases, access_mode, annotations, session_surfaces, execution_isolation_tier (value), mcp_launch_isolation_tier (value), deprecated, receive_lsp_mcp.

MODELS (open question CLOSED — model-catalog is MANAGED, not a query): model-endpoints + model-catalog merge into ONE `models` managed-reconcile kind. The catalog IS the managed set of models — add/remove/edit (curation via model_config ManagedConfigSpec + model_entry_renderer) plus refresh/discovery (fetch_catalog_fn as a refresh facet of the same mechanism, like cli_probe folds into cli-install). fetch_catalog_fn is no longer its own kind.

LAUNCH (corrected): ONE operational `launch` capability = the COMPOSABLE launch surface. mechanism = {intents: {execute|interactive|agent -> {interaction[], observability[]}}, recipes: {execution, interactive, acp}}. interaction (native, acp) + observability (pipe, acp, rpc, hooks) COMPOSE; caller picks intent at runtime. ACP composes across ALL intents. Isolation is an independent per-launch facet from the orthogonal tier values. acp-support stays evidence-only (ACP protocol provenance). The three launch-execute/interactive/agent kinds were WRONG (not mutually exclusive) and are removed.

USER DECISIONS (this session):
1. SUBSTRATE = CONFIG-OWNED vocabulary (families-as-config accepted).
2. MIGRATION = CLEAN-CUT, COORDINATED (one branch, suite green, no dual-read/aliases).
3. FAMILY CONSOLIDATION: 5 managed-* families -> ONE managed-reconcile (shared contract; per-kind entry sub-schema).
4. FEATURES = projection of capabilities (PC05); feature_mapping._CAPABILITIES retired.
5. LAUNCH = one composable capability (above).
6. MODELS = one managed kind (endpoints + catalog merged; fetch is a refresh facet).

No OPEN sub-questions remain. PLAN: PC01 corrects catalogue to this target (config, pending); PC02 single-read loader; PC03 rewrites 16 providers; PC04 deletes legacy parsing + regenerates taxonomy doc (retire-guard VAL-PCAP-014); PC05 derives features from capability instances. All land together.
