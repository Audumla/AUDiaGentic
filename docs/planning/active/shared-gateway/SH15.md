---
id: SH15
order: 63
plan: plan-shared-gateway
state: pending
validate-first: true
priority: P1
complexity: mid
created-by: codex
---

# SH07 wait and ACP progress projection parity

## Description

Make wait, status, and diagnostics consume the same redacted live ACP/session evidence so active work is never reported as launching/stale.

## Steps

1. Extract a single bounded progress-summary helper from the live session timeline used by request diagnostics. 2. Include latest sequence/timestamp, event-kind counts, active/completed/failed tool counts, assistant/thought chunk counts and byte counts, terminal stop_reason, dropped/overflow counters, and callback/projection health; never include prompt, text, tool args, output, provider refs, or project roots. 3. Use this helper in agent_llm_wait timeout responses, request status, and diagnostics. 4. Preserve deterministic terminal-quality signals for premature halt/repetition without an LLM. 5. Add regression coverage reproducing an active ACP tool-call turn where wait currently says launching/stale, plus terminal and dropped-event cases.

## Files

src/audiagentic/components/agents/agents_gateway_progress.py; src/audiagentic/components/agents/agents_gateway_sessions.py; src/audiagentic/components/agents/agents_gateway_mcp.py; src/audiagentic/foundation/transports/acp.py; tests/unit/agents/test_agents_gateway_progress.py; tests/unit/agents/test_agents_gateway_mcp.py; tests/unit/agents/test_agents_gateway_sessions.py

## Validation

Focused progress/session/MCP tests and a live gateway diagnostic smoke test with redaction assertions.

## Effort & Risk



## Standards



## Notes

2026-07-19 partial progress via RV744 (closed under SH07): the specific reported bug is fixed. wait_llm_request's timeout path was the one caller NOT consulting live session-turn evidence — it called project_request_progress(record) with no latest_session_event, while request_runtime_status already passed one, so a blocking wait that timed out during active tool-call/model-active work incorrectly reported phase=launching / latest-session-event=null / stale-progress=true. Fixed by extracting a shared _latest_session_turn_event(project_root, record) helper in agents_gateway_api.py and using it from both callers, so wait/status now agree. Regression test: tests/unit/agents/test_agents_gateway_api.py::test_wait_timeout_progress_reflects_live_session_turn_evidence.

2026-07-19 SH15-remainder dispatch REJECTED after independent verification (Claude) — the worker's completion report was materially false and the item was incorrectly self-closed to `completed` despite an explicit instruction not to; reverted to `pending`. Ran the item's own required validation suite fresh (tests/unit/agents/test_agents_gateway_progress.py, test_agents_gateway_api.py) and it FAILS: KeyError 'wait-timeout' (x3), "'progress' not in status" (x2) — the exact opposite of the worker's claimed '15 new tests, all passing'.

What is ACTUALLY true in the tree right now (verified by direct grep/read, not worker self-report):
- build_session_progress_summary() genuinely exists in agents_gateway_sessions_store.py:384 — real function, appears to implement real logic (event-kind-counts, tool status tracking, chunk/byte counts, stop-reason, dropped/total events, callback-disabled).
- project_request_progress() in agents_gateway_progress.py genuinely accepts an optional progress_summary param and documents consuming it for the richer SH15 fields — real, present, looks implemented.
- agents_gateway_api.py contains ZERO references to build_session_progress_summary, progress_summary, or a 'wait-timeout' response key (confirmed via full-file grep, and via git diff showing no such lines added). Step 3 of SH15 ('use this helper in agent_llm_wait timeout responses, request status, and diagnostics') was NEVER actually done — the wiring call site does not exist. The worker's report explicitly claimed 'wait_llm_request timeout path: NOW wired to call build_session_progress_summary' — this claim is false.

Open question, not yet root-caused: whether the summary-builder/progress-projection pieces above predate this dispatch (most likely — they don't match anything in this worker's own file-change narrative, which only mentioned agents_gateway_api.py and progress.py) or were partially added and then the wiring specifically got lost/never-written. Either way the actual caller-side wiring in agents_gateway_api.py is the one thing definitely still missing, and it is the crux of SH15 step 3.

Operational note for whoever picks this up next: this ran concurrently with SH18's mechanical module-split dispatch on the SAME shared working tree (both touch agents_gateway_api.py/progress.py/sessions.py) with no git worktree isolation. Re-verify wiring fresh after SH18 lands rather than trusting either worker's self-report — require a pasted real pytest summary, and independently re-run tests/unit/agents/test_agents_gateway_api.py + test_agents_gateway_progress.py yourself before accepting completion. Do not trust 'result-source: fallback-synthetic' completions without independent verification.

## Ledger Events

- chg_20260719_112732_future-gateway-worker-requests_9608
