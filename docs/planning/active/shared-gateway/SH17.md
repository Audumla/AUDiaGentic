---
id: SH17
order: 200
plan: plan-shared-gateway
state: completed
validate-first: true
priority: P3
complexity: simple
created-by: claude
---

# Bounded retention sweep for gateway work-index quarantine directory

## Description

SH07's C7 work-index quarantine (agents_gateway_work_index.py quarantine_work_index_entry) preserves malformed/mismatched entries forever for forensic analysis. Nothing ever removes old quarantine entries, so the directory grows unbounded over the life of a machine. This is cleanup/hygiene, not a correctness gap, and was split out of SH07 so SH07's closure is not blocked on it.

## Steps

1. Add a bounded-retention sweep (e.g. clear_expired_quarantine_entries(service_root, *, max_age_seconds)) that deletes quarantine entries older than a threshold, called opportunistically during recovery or on a lightweight schedule.
2. Never touch valid active-work index entries — quarantine directory only.
3. Test: old quarantine entries beyond the threshold are removed; recent ones and all active entries are untouched.

## Files

src/audiagentic/components/agents/agents_gateway_work_index.py; tests/unit/agents/test_agents_gateway_work_index.py

## Validation

Quarantine directory does not grow unbounded across repeated recovery cycles in a long-running test; active entries are never touched by the sweep.

## Effort & Risk



## Standards



## Notes

Split out of SH07 2026-07-19 per user direction: this is cleanup, not a blocker for SH07 closure.

2026-07-19: implemented (by a gateway worker, exact attribution unclear — already present in the tree when this item's dispatched worker inspected it). Claude independently verified before endorsing: clear_expired_quarantine_entries(service_root, *, max_age_seconds) exists in agents_gateway_work_index.py, called from agents_gateway_recovery.recover_gateway_requests(); tests/unit/agents/test_agents_gateway_work_index.py::TestQuarantineBoundedRetention (6 tests) plus full module (22/22) pass. Ledger event recorded by Claude since the implementing worker reported it lacked ag-ledger MCP access. Item confirmed completed.

## Ledger Events

- chg_20260719_131311_the-gateways-quarantine-direc_3600
