---
id: CC35
order: 100
plan: plan-code-cleanup
state: pending
validate-first: true
priority: P3
work: M
created-by: claude
---

# Placeholder: reorganize foundation/toolchains package for cohesion (post-Block-C)

## Description

PLACEHOLDER — to be reviewed and defined after managed-config-consistency Block C completes; do NOT implement earlier. Consolidation survey (2026-07-14, MA00 program) found foundation/toolchains/ has become a grab-bag: toolchain detection (detect.py, loader.py, toolchains.yaml) + managed-config core (managed_config.py, fragments.py) + recipe machinery (recipe_contract.py, recipe_patterns.py, plus MA10's incoming recipe_spec) + probes.py + artifact_registry.py + config_patcher.py + config_reader.py + managed_block.py. Nothing crosses incorrectly — this is discoverability/cohesion debt, not a defect — but MA10/MA13 will add more, and new contributors cannot infer ownership from the package name.

## Steps

1. WAIT for managed-config-consistency Blocks B-C completion (MA10/MA12/MA13 land their modules) so the final module census is stable — moving files mid-program is pure churn.
2. Re-survey the package and propose a cohesion split by concern, e.g. toolchain detection vs managed-config/ownership core vs recipe engine vs probes — exact shape decided at definition time with the then-current import graph.
3. Define the move as mechanical renames with whole-repo import updates, zero behavior change, full suite green; public export paths preserved or deprecated per standards.
4. Update Architecture Standards / component-creation docs for the new homes.

## Files



## Validation

1. Item is re-reviewed and fully defined before any implementation (this is a placeholder).
2. If implemented: pure-move refactor, zero behavior change, full unit suite green, whole-repo grep shows no stale import paths.

## Effort & Risk

Mid, mechanical once defined. Only risk is doing it too early and colliding with MA10/MA12/MA13 module additions — hence the hard Block-C gate.

## Standards



## Notes

RV401 REBASE: prior expectation that MA10 adds a generic recipe_spec is withdrawn. Re-survey only after actual Block C code lands. Do not create package areas for planned abstractions that were never needed.

## Ledger Events

- chg_20260714_030354_locked-in-the-cross-cutting-co_3200
- chg_20260714_122529_kept-deep-provider-capability_7070
