---
id: CC31
order: 0
plan: plan-code-cleanup
state: pending
validate-first: false
priority: P2
complexity: mid
created-by: opencode-agent
---

# Config-driven provider surface templates: static template files and adapter-specific rendering

## Description

Provider surface file generation uses a mix of static template files and hardcoded adapter-specific rendering. The content is doctrine text that should live in config.

## Steps

['1. Move default_instruction.md and default_prompt_tags.md content to provider YAML as surface-templates: blocks\n2. Add heading_level field to adapter descriptors; wire into contribution renderers\n3. Move local_openai system prompt template to provider YAML under execution-prompt:\n4. Verify generated surface files are byte-identical before/after']

## Files

- templates/default_instruction.md — replace with YAML doctrine (can delete file)
- templates/default_prompt_tags.md — replace with YAML doctrine (can delete file)
- claude/surface.py, cline/surface.py, roo/surface.py — use config-driven heading level
- local_openai/adapter.py — load system prompt template from provider YAML
- Provider YAML files — add surface-templates and execution-prompt sections

## Validation

['- Generated AGENTS.md, CLAUDE.md, and skill files are byte-identical to pre-refactor\n- System prompt for local-openai adapter is identical after loading from config']

## Effort & Risk



## Standards



## Notes

## Exact locations and content

### Static template files (templates/ directory)
Path: src/audiagentic/config/components/providers/surfaces/templates/
- default_instruction.md (11 lines) — loaded via make_standard_surface_renderer() when instruction_file="default_instruction"
  Only variable: $display_name substitution. Content is prompt tag doctrine.
- default_prompt_tags.md (9 lines) — identical content minus the # heading. Used when no custom instruction file is specified.

### Adapter-specific contribution rendering (3 adapters with different heading levels)
Path: src/audiagentic/components/providers/adapters/
- claude/surface.py line 34:
  `f"## {contribution.title}\n\n{contribution.body.strip()}"` — level-2 headings
- cline/surface.py line 32:
  `f"# {contribution.title}\n\n{contribution.body.strip()}"` — level-1 headings
- roo/surface.py line 30:
  `f"# {contribution.title}\n\n{contribution.body.strip()}"` — level-1 headings

The heading level difference is real and intentional (claude uses ##, others use #). Moving to config would allow provider-specific customization.

### System prompt in local_openai adapter
File: src/audiagentic/components/providers/adapters/local_openai/adapter.py
Lines 143-167:
- system_prompt = "AUDiaGentic execution request. job={job-id} packet={packet-id} provider={provider-id} workflow={workflow-profile}. Return a concise execution summary..."
- user_content = IDENTICAL string, duplicated from system_prompt with prompt-body appended
This is the same text sent to both system and user roles. Should be a single template entry.

### Provider YAML files for config additions
- src/audiagentic/config/components/providers/local-openai.yaml — add execution-prompt: block
- All provider YAMLs — add heading_level: field under adapter configuration

## Ledger Events


