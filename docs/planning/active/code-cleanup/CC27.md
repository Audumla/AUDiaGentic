---
id: CC27
order: 0
plan: plan-code-cleanup
state: pending
validate-first: true
priority: P1
complexity: mid
created-by: opencode-agent
---

# Define MCP metadata config-driven standard: tool descriptions, server instructions, parameter docs, surface contributions

## Description

Establish a single standard for all MCP-related text metadata (tool descriptions, server instructions, parameter docs, surface contributions) to live in YAML component config files — eliminating hardcoded string duplication across Python code and configuration.

## Steps

['1. Finalize YAML schema for rich tool-descriptions (string vs mapping) with backward compatibility\n2. Update architecture standards document to codify the MCP metadata standard\n3. Deliver a complete touch-point inventory as part of CC27 (no implementation yet)']

## Files



## Validation

["- Rich YAML format is backward-compatible with existing flat string tool-descriptions\n- Architecture standards document exists and references the error-resolutions pattern\n- Touch-point inventory for CC27 matches this item's categories"]

## Effort & Risk



## Standards



## Notes

STEP 1-3 COMPLETE 2026-07-14:

**Step 1 — YAML schema defined.** `McpServerDeclaration.tool_descriptions` is open dict (`dict[str, Any]`). Known standard keys: `description` (str), `parameters` (dict of param-name → description or nested mapping). Additional keys are provider/component-owned extension; foundation passes them through unchanged. No rigid dataclass types — §2 Config Over Code allows arbitrary depth for lower-level config.

**Step 2 — Architecture standards codified.** Added ARCHITECTURE_STANDARDS.md §6.1 MCP Metadata Standard: all MCP-facing text metadata lives in component YAML under `mcp-servers`/`tool-descriptions`; zero hardcoded descriptions in Python code.

**Step 3 — Touch-point inventory delivered.** Evidence at `.audiagentic/evidence/CC27_touchpoint_inventory.md`. 18 servers, 125 tools total. 37 already YAML-driven (project/providers/session). 88 need migration across 15 servers.

DISPATCH: CC28 implementation may start against this standard.

## Ledger Events


