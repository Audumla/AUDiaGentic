---
id: CC39
order: 0
plan: plan-code-cleanup
state: pending
validate-first: true
priority: P2
work: S
created-by: agent
---

# Add get_items batch retrieval for full item details

## Description

Add a `get_items` tool that returns full item details (frontmatter + body sections) for multiple items in one call. Currently `list_items` returns summary dicts and `get_item` returns full details for one item — there's no batch variant.

This is useful when an agent needs to read the content of several plan items (e.g., all items in a plan, or all active items) without making repeated MCP tool calls.

## Steps

['Add get_items(project_root, item_ids) in items.py: takes a list of IDs, returns full item dicts (same shape as get_item)', 'Add plan_get_items MCP tool in planning_mcp.py with item_ids parameter', 'Add tool description in planning.yaml', 'Consider adding validation: reject unknown IDs or skip them with a warning']

## Files

- src/audiagentic/components/planning/items.py — add get_items function
- src/audiagentic/components/planning/planning_mcp.py — add plan_get_items tool
- src/audiagentic/config/components/planning.yaml — tool description

## Validation



## Effort & Risk



## Standards



## Notes



## Ledger Events


