---
id: CC41
order: 34
plan: plan-code-cleanup
state: pending
validate-first: true
priority: P1
complexity: mid
created-by: opencode
---

# Add shared config-managed declarative recipe loader

## Description

Add the smallest shared config-managed recipe target needed by Hindsight, CLI installation, LSP, and future owners. A strict YAML schema loads into a frozen validated recipe template; typed string parameters materialize existing probes and step-factory steps; existing DeclaredStepRecipe executes the lifecycle. Owners retain family-specific Request/Result contracts and explicitly select a recipe. CC41 does not migrate production callers or replace MA20 registry composition.

## Steps

STATUS 2026-07-16: Activities 1-5 are LANDED. Activity 6 and 7 remain. Verified on disk — do not re-do 1-5.

ACTIVITY 1 — DONE. declarative-recipe.schema.json exists and is strict: all nine step kinds (shell, write-file, config-set, managed-block, callable, conditional, select, sequence, confirm) and all four probe kinds (command, file-exists, config-key, composite), additionalProperties:false throughout. Three fixtures present (hindsight-codex, npm-cli, lsp-pyright).

ACTIVITY 2 — DONE. foundation/toolchains/recipe_loader.py present.

ACTIVITY 3 — DONE. foundation/toolchains/recipe_materializer.py present (plus recipe_execution.py).

ACTIVITY 4 — TODO (strong mid-level). Extend DeclaredStepRecipe ONLY where fixtures prove a gap.
4.1 Add `dry_run` to recipe_patterns.py — still absent (grep count 0), and CC41's own dependency notes flagged it as the gap Activity 4 must fill.
4.2 Add explicit verify-probe support (verify currently delegates to probe).
4.3 Add configure-steps support (configure is currently a no-op).
4.4 Extend provision_steps to cover configure and dry-run phases.
4.5 Provide the internal mode adapter: plan -> dry_run, apply -> lifecycle sequence, prune -> uninstall, status -> probe/verify. Returns the INTERNAL semantic result, never a public family result.
4.6 Keep raw command/returncode/stdout/stderr inside step/probe diagnostics; owner adapters map only safe semantic fields.
DONE WHEN: absent -> apply -> verified -> prune -> absent works; repeated apply is idempotent; plan/status DO NOT MUTATE (assert it with a test that fails if a subprocess is spawned or a file is written — RV497 and MA26 step 7 are both instances of exactly this defect); failed sequences compensate.

ACTIVITY 5 — DONE. recipe_catalogue.py implements 5.1-5.8:
- Explicit sources in fixed precedence: packaged < project < active component-profile
- Indexed by recipe-id via RecipeCatalogue.get_recipe(id) / list_recipes()
- Whole-definition replacement only with replaces-version matching (no deep merge)
- Duplicate ids at the same level fail with VAL-RCG-001
- Reuses existing one-profile-per-process selection via get_active_profile() (no second profile mechanism, no cache)
- Provenance tracking via RecipeSource and CatalogueEntry
- Lazy build: _templates populated on first access only
- Convenience make_catalogue(project_root) constructor

ACTIVITY 6 — TODO (senior seam review, then mid-level tests). No DeclarativeRecipeHandlerFactory exists and no test CLI registrar.
6.1 DeclarativeRecipeHandlerFactory(provider_id, project_root, recipe_id, request/outcome mapper) returning a RecipeHandler-compatible closure.
6.2 It must slot into the NEW registrar table, not the old hardcoded switch: automation_registry.py is now a `_REGISTRARS` tuple of FamilyRegistrar(pin, eligible, make_handler). A declarative-backed family becomes ONE more row whose make_handler is this factory.
6.3 Prove no CC41 family id and no universal declarative-lifecycle family is needed.
6.4 One Hindsight and one LSP materialization proof, no production caller changes.
CONSTRAINT (do not violate): the factory supplies the HANDLER only. The FamilyPin stays in code.

ACTIVITY 7 — TODO (new). TESTS. No CC41 test files exist under tests/unit/foundation/toolchains (no loader/materializer/catalogue/registrar suites), so FINAL VALIDATION cannot pass today. Cover: schema accept/reject, loader frozen-template round-trip, strict substitution and sensitive redaction, every probe and step kind, DeclaredStepRecipe lifecycle, catalogue precedence/replacement/duplicate rejection, and the registrar fixture.

FINAL VALIDATION
- Focused schema, loader, substitution/redaction, probes, all step types, DeclaredStepRecipe lifecycle, catalogue, and registrar tests pass.
- Foundation contains no provider/Hindsight/LSP vocabulary.
- No universal public recipe Request/Result or family.
- No raw unchecked recipe dictionaries after the loader boundary.
- No production callers are migrated and no old routes are deleted by CC41.

## Files

[SCHEMA] src/audiagentic/config/recipes/declarative-recipe.schema.json.
[TEMPLATE/LOADER] foundation/toolchains/recipe_loader.py with frozen template/parameter/lifecycle/probe/step wrappers.
[MATERIALIZATION] foundation/toolchains/recipe_materializer.py using existing step factory and probes.
[LIFECYCLE] minimal additions to foundation/toolchains/recipe_patterns.py only for configure/verify/dry-run gaps proven by fixtures.
[CATALOGUE] foundation/toolchains/recipe_catalogue.py with explicit package/project/active-profile sources, deterministic whole-definition replacement, version-match validation, provenance, and duplicate rejection.
[REGISTRAR SEAM] small declarative handler factory callable by MA20 family registrars; test-only CLI registrar.
[FIXTURES/TESTS] Hindsight, CLI, and LSP YAML fixtures; schema, loader, materializer, lifecycle, catalogue, redaction, and registrar tests.
[DOC] concise declarative recipe format and owner migration example.
[OUT OF SCOPE] production migrations, MA20 hardcoded-family removal, component-profile overlay/caching, new secret store, universal binding schema, new step implementations.

## Validation

1. One strict v1 schema covers current shared recipe needs. 2. Loader returns frozen validated objects and leaks no raw mapping. 3. V1 parameters are strings with strict defaults/substitution and sensitive redaction. 4. Existing step/probe factories are reused; no duplicate execution framework. 5. DeclaredStepRecipe handles required private lifecycle with minimal extension only. 6. Catalogue sources are explicit; package < project < active profile precedence uses version-checked whole-definition replacement only; same-level duplicates and deep merges fail. 7. Owners explicitly select recipes and retain distinct typed Request/Result contracts. 8. Test CLI registrar proves MA20 compatibility without a declarative-lifecycle family. 9. Hindsight and LSP fixtures prove reuse. 10. No production route migration, provider vocabulary in foundation, universal payload/result, public repair/dry_run, or unchecked recipe blob.

## Effort & Risk

Mid overall with two senior review points: schema boundary and MA20 registrar seam. Main risks are letting raw step dictionaries escape the loader, duplicating DeclaredStepRecipe, or turning owner selection into a universal binding/request model. Keep v1 string parameters and explicit catalogue sources; expand only from proven migration needs.

## Standards



## Notes

VERIFIED STATUS 2026-07-16 (claude) — CC41 IS NOT COMPLETE. It is roughly Activities 1–3 of 6. Recorded because it was reported as complete; the state is correctly still 'pending'.

LANDED (verified on disk):
- Activity 1 (schema): src/audiagentic/config/recipes/declarative-recipe.schema.json exists and is strict — all NINE step kinds present (shell, write-file, config-set, managed-block, callable, conditional, select, sequence, confirm) and all FOUR probe kinds (command, file-exists, config-key, composite), additionalProperties:false throughout. Step 1.4/1.5 satisfied. Note it uses draft-07 style `definitions` rather than `$defs` — cosmetic, but the item text says $defs.
- Activity 1.6 (fixtures): all three present — config/recipes/fixtures/{hindsight-codex,npm-cli,lsp-pyright}.yaml. These are SCHEMA fixtures only, not production recipes: hindsight-codex.yaml's install step is literally `python -c "print('{URL}', '{TOKEN}')"`. Do not mistake it for a working Codex recipe.
- Activity 2 (loader): foundation/toolchains/recipe_loader.py exists.
- Activity 3 (materializer): foundation/toolchains/recipe_materializer.py exists (plus recipe_execution.py).

NOT LANDED:
- Activity 4: `dry_run` is STILL ABSENT from recipe_patterns.py (grep count 0). CC41's own DEPENDENCY VALIDATION notes flagged this as a known gap Activity 4 must fill. configure/verify exist but the explicit verify probe and dry-run steps are unproven.
- Activity 5: foundation/toolchains/recipe_catalogue.py DOES NOT EXIST. There is no RecipeCatalogue, so no recipe-id selection, no package/project/profile precedence, no version-checked replacement.
- Activity 6: no DeclarativeRecipeHandlerFactory and no test-only CLI registrar anywhere in src. The MA20 registrar seam is unproven.
- Tests: no CC41-specific test files under tests/unit/foundation/toolchains (no loader/materializer/catalogue/registrar suites). Final validation cannot pass.
- Callers: zero production or test callers of load_recipe_from_yaml/materialize_recipe outside the recipe_* modules themselves. That is per design (CC41 explicitly migrates no callers), but it means the loader is currently unexercised.

WHY THE REMAINING ACTIVITIES MATTER MOST: 5 and 6 are exactly the two that make the loader usable by an owner. Without Activity 5 an owner cannot select a recipe by id; without Activity 6 a MA20 family registrar cannot bind one. Activities 1–3 alone deliver a validated loader that nothing can reach.

SEQUENCING IMPACT ON MA26 (recorded there too): MA26 must implement the Hindsight Codex/Pi lifecycle. If CC41 finishes 4–6 first, that lifecycle becomes YAML + a small parameter mapper — which is precisely CC41's stated owner flow ('Hindsight, CLI, and LSP keep separate parameter mapper functions'). If MA26 goes first, it will hand-write typed Python recipe classes that CC41 would immediately want to replace. Recommend finishing 4–6 before MA26 implements its step 6.

EXPRESSIBILITY CHECK (for MA26): the real Codex recipe looks expressible in this schema — write-file for ~/.hindsight/codex.json and ~/.codex/hooks.json, config-set for the [features] codex_hooks TOML upsert, callable or shell for the 11 script downloads, and file-exists/config-key probes for the staleness checks. Confirm the download step before committing; that is the one mechanic with no obvious first-class step kind.

---

SIMPLIFIED AFTER RV494.

Essential flow:
YAML -> strict schema -> frozen recipe template -> validated string parameters -> existing probes/step factory -> existing DeclaredStepRecipe.

Owner flow:
typed family/requester desired object -> owner mapper -> string parameters -> materialized recipe -> internal semantic outcome -> owner-specific typed result.

CC41 owns only shared recipe loading/materialization and proof of registrar compatibility. MA20 owns replacing its hardcoded family imports with registrar contributions. MA12, Hindsight, LSP, and other owning items own production cutovers and deletion.

V1 intentionally avoids: typed parameter algebra, secret resolver, profile overlays, automatic discovery, new lifecycle engine, nine duplicate step-class models, universal RecipeBinding schema, and three production migrations.

DEPENDENCY VALIDATION (2026-07-15): All 14 substrates confirmed operational.
1. Step factory: 9 types, build_steps_from_defs with strict substitution
2. DeclaredStepRecipe + InstallManifest
3. DeclaredStepRecipe lifecycle methods: probe/install/configure/verify/uninstall/prune/provision_steps present
4. Probes: CommandProbe, FileExistsCheck, ConfigKeyCheck, CompositeHealthCheck, check_with_retry
5. ProviderAutomationRegistry: 24 registrations, 4 family contracts (cli-lifecycle, generated-surfaces, model-projection, self-provided-lsp)
6. jsonschema Draft 2020-12 validation
7. Redaction primitives (OU01): redact_text, is_sensitive_key
8. Hindsight matrix: 15 rows as proof of declarative pattern
9. Error factory: make_error_factory
10. Config reader: read_config_value, UNSET sentinel
11. Artifact primitives: atomic_write_text, ConfigPatcher, apply_managed_block
12. MA20 dispatch path: codex cli-lifecycle status -> CliLifecycleResult
13. JSON schema validation: new v1 schema validates fixtures, rejects unknown fields and missing required
14. DeclaredStepRecipe provision() end-to-end: probe returns ABSENT/VERIFIED correctly

KNOWN GAPS CC41 WILL FILL (not blockers):
- dry_run method: MISSING from DeclaredStepRecipe — Activity 4 adds this [STILL MISSING as of 2026-07-16]
- configure-steps support: configure() currently no-op — Activity 4 extends this
- verify probe: verify uses probe() — Activity 4 adds explicit verify probe
- provision_steps returns install steps only — Activity 4 extends for configure/dry-run

NO BLOCKERS — Activities 1–3 can start immediately.

HANDOFF:
A. Activity 1 requires senior schema approval.
B. Activities 2–3 can run as one mid-level implementation batch.
C. Activity 4 is a strong mid-level lifecycle batch.
D. Activity 5 is a simple catalogue batch.
E. Activity 6 requires senior seam review, then mid-level fixture tests.
F. Final validation and documentation close CC41.

RV492 remains the architecture correction restoring config-managed recipes. RV494 records this scope reduction.

## Ledger Events

- chg_20260715_225531_prevented-declarative-recipes_2189
- chg_20260715_225934_restored-config-managed-recipe_6442
- chg_20260715_230502_made-cc41-execution-ready-with_1982
- chg_20260715_231132_simplified-cc41-to-the-minimum_4414
- chg_20260715_231615_added-safe-project-and-profile_1350
- chg_20260716_050538_declarative-recipes-can-now-de_2604
- chg_20260716_061700_recipe-catalogue-indexed-reci_3107
- chg_20260716_063502_fixed-recipecatalogue-added-p_5490
