---
id: MA09
order: 99
plan: plan-managed-config-consistency
state: pending
validate-first: true
priority: P1
complexity: mid
created-by: codex
---

# Close managed mutation audit and integration gates

## Description

Final cross-item gate after MA10–MA15, MO06, and MA02–MA08: remove temporary debt allowlists, prove every audited site is compliant or meets exemption rules, verify foundation step/recipe/probe/reconcile reuse, prove the distributable starts through its public CLI, and validate end-to-end lifecycle consistency.

## Steps

1. Require MO06 and MA02–MA08 plus MA10–MA15 completed; list their ledger events and migrations.
2. Re-run MA01 scanner; update inventory for moved/deleted/new symbols. Every remaining row action is `keep` with compliant primitive or explicit approved exemption—no plan ids, follow-up, or candidate wording.
3. Remove temporary MA02/MA08 known-violation expectations from architecture tests. Guards assert zero provider-name branches, zero component type leaks, zero direct writer/remover calls outside core, one generic registry implementation, and exact audit parity.
4. Verify generic lifecycle contract across MCP/LSP/model/plugin entries: plan/dry-run, apply, exact adoption, collision, update/rename, disable/prune, corrupt registry, reload/action_needed, status counts.
5. Verify component/harness/recipe whole-file and nested-key lifecycle: ownership registration, collision, prune, dry-run, atomic failure.
6. Run MA15 in disposable packaging Docker: clean-container import; built-wheel installed `audiagentic --help`; installed isolated-project `component list`; negative CLI/error behavior; filesystem containment. No real startup/lifecycle command may run against host environment.
7. Run full unit suite and relevant integration/e2e provider/harness/coding-lsp/memory/release/source-control suites. Compare to pre-plan baseline; zero managed-mutation architecture failures and zero new failures.
8. Regenerate architecture audit evidence summary with final counts, exemptions, Docker startup/CLI results, and ownership diagram; record ledger audit event.

## Files

[DOCS] MANAGED_MUTATION_AUDIT.md final state
[TEST] managed mutation boundary + cross-kind integration tests
[PLANNING] MA/MO review and ledger linkage

## Validation

1. Audit/scanner exact parity; zero unresolved actions.
2. Zero temporary violation allowlists.
3. Every exemption satisfies all §16 gates with linked tests.
4. Cross-kind lifecycle matrix green.
5. MA15 packaging Docker proves installed console entrypoint starts/loads package data, negative CLI paths are stable, writes remain container-contained, and no host-mutating startup tests run.
6. Full suite zero new failures; prior Hindsight architecture failures resolved.
7. Ledger contains linked unreleased events for all implementation items.

## Effort & Risk

Mid integration gate. Does not implement missing fixes; reopens owning item via review if any gate fails.

## Standards

- Architecture Standards §1–§16, especially §10/§16.
- observability and component-creation ownership doctrines.

## Notes

Runs last after MA10–MA15, MO06, and MA02–MA08. Also verifies zero active UN items remain and no absorbed UN requirement was lost. MA15 is mandatory: source-only `python -m` tests do not satisfy packaged CLI acceptance. Any failure creates a review against owning MA/MO item; MA09 remains pending.

## Ledger Events


