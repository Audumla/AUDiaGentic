---
id: MA09
order: 900
plan: plan-managed-config-consistency
state: pending
validate-first: true
priority: P1
complexity: mid
created-by: codex
---

# Close managed mutation audit and integration gates

## Description

Final current-scope gate after MA10–MA17, MO06, MA02, MA04–MA06, and MA08: remove temporary debt allowlists, prove every audited non-runtime managed site is compliant or meets exemption rules, prove all installable capabilities use MA16 common declarative schema/MA11 execution, verify allowed provider integration and generic event boundaries, prove distributable starts through public CLI, and validate end-to-end lifecycle consistency. Runtime harness migrations are explicitly out of scope — they live in plan harness-runtime-ownership (HA01 ex-MA03, HA02 ex-MA07).

## Steps

1. Require MO06, MA02, MA04–MA06, MA08, and MA10–MA17 completed; list ledger events/migrations. HA01/HA02 (harness-runtime-ownership, ex-MA03/MA07) remain deferred in their own plan and are excluded.
2. Re-run MA01 scanner for current non-runtime scope. Every row is compliant `keep` or approved explicit exemption; no follow-up wording.
3. Re-run MA17 graph. Provider descriptor/data consumption is documented; requester components have zero direct imports/calls to provider handlers, serializers, or lifecycle mutation services; foundation imports zero components; providers has zero Hindsight/memory vocabulary; release-to-ledger uses generic event contract.
4. Verify MA16 is sole declarative operation schema and MA11 sole step substrate. No legacy `cli_install`, Hindsight matrix recipe dialect, component-local schema, workflow step duplicate, or compatibility translator remains.
5. Remove temporary violation expectations. Guards assert zero foreign-domain leakage, zero direct writer/remover outside approved core, one generic registry implementation, exact audit parity.
6. Verify lifecycle: request plan/dry-run, gateway apply, exact adoption, collision, update/rename, scoped prune, corrupt registry, reload/action_needed, status.
7. Verify whole-file/nested-key lifecycle: ownership registration, collision, prune, dry-run, atomic failure. Runtime assets/patches excluded.
8. Run MA15 Docker packaging gate and full relevant suites; zero new failures.
9. Verify MA00 program tracker is current: progress notes and item-state snapshot match actual plan state, and no active MA/MO item is missing from its block structure.
10. Regenerate audit evidence with counts, exemptions, Docker results, import graph, gateway delegation matrix, ownership diagram; record ledger audit event.

## Files

[DOCS] MANAGED_MUTATION_AUDIT.md final state
[TEST] managed mutation boundary + cross-kind integration tests
[PLANNING] MA/MO review and ledger linkage

## Validation

1. Current non-runtime audit/scanner exact parity; zero unresolved actions.
2. Zero temporary violation allowlists.
3. Every exemption satisfies all §16 gates with linked tests.
4. MA17: descriptor/data provider consumption classified/green; requesters make zero direct provider handler/service calls; providers has zero Hindsight/memory vocabulary; generic release→ledger event works; foundation imports zero components.
5. Cross-kind lifecycle matrix green.
6. All durable non-runtime managed writes/removals use approved atomic/ownership primitives.
7. MA15 packaging Docker proves installed console entrypoint starts/loads package data, negative CLI paths are stable, writes remain container-contained, and no host-mutating startup tests run.
8. Full suite zero new failures; prior Hindsight architecture failures resolved.
9. Ledger contains linked unreleased events for all implementation items.

## Effort & Risk

Mid integration gate. Does not implement missing fixes; reopens owning item via review if any gate fails.

## Standards

- Architecture Standards §1–§16, especially §10/§16.
- observability and component-creation ownership doctrines.

## Notes

Runs last after MA10–MA15, MO06, and MA02–MA08. Also verifies zero active UN items remain and no absorbed UN requirement was lost. MA15 is mandatory: source-only `python -m` tests do not satisfy packaged CLI acceptance. Any failure creates a review against owning MA/MO item; MA09 remains pending.

RV364 incorporated 2026-07-14: deferred runtime migrations relocated to plan harness-runtime-ownership (HA01 ex-MA03, HA02 ex-MA07); MA09 gate no longer references MA03/MA07 and additionally checks MA00 tracker currency (step 9).

IMPLEMENTATION ORDER: Block E final gate after Blocks A–D and required MO projection items complete. HA01/HA02 explicitly excluded. Any failed gate reopens owning item; MA09 implements no fixes itself.

## Ledger Events

- chg_20260714_015420_reorganized-the-managed-config_9031
