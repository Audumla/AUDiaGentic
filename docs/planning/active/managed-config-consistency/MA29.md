---
id: MA29
order: 92
plan: plan-managed-config-consistency
state: pending
validate-first: true
priority: P1
complexity: mid
created-by: codex
---

# Migrate generic LSP-MCP projection recipe family

## Description

Second MA08 slice. Replace generic LSP-MCP apply/prune calls with one MA20-conformant managed-MCP projection family using serializable desired entries and opaque scope.

## Steps

1. Characterize generic MCP projection callers/results.
2. Freeze payload/result/modes without merging it with language-server payload.
3. Implement definitions/registrations and MO06-backed adapter.
4. Migrate callers/tests.
5. Delete old sync/prune/action-handler routes.
6. Validate ownership, collision, reload, and non-mutation.

## Files

[CODING-LSP] one named caller family
[PROVIDERS] one family definition/registration/adapter
[TEST] caller parity, ownership, import and stale-route guards

## Validation

Generic LSP-MCP family remains distinct, validates through MA20, preserves ownership behavior, and leaves no old route.

## Effort & Risk

Bounded MA08 slice suitable for a strong mid-level agent after architecture owner freezes payload/result.

## Standards

- Architecture Standards: Dependency Boundaries; Approved cross-component APIs; Ownership and Abstraction; Enforcement.
- Architecture Guidelines: Provider Platform API; Writing External Artifacts; Migration Validation.

## Notes

Depends on MA28. Create distinct typed LspMcpProjectionRequest/Result and schemas; do not reuse managed-MCP merely because wire entries look similar unless semantics/ownership are proven identical. LOCKED TYPED-CONTRACT RULE: define frozen family-specific Request/Result objects and concrete JSON schemas for their serialized forms before handler work. RecipeDefinition contract refs must resolve to those schemas. Config/API mappings parse once at boundary into typed objects; handlers and callers exchange typed objects only. No Any payload/result, nested payload blob, unchecked dict, or universal cross-family request/result. Recipe owner builds desired request; provider descriptors expose generic capabilities/mechanics and remain requester-blind.

## Ledger Events

- chg_20260715_003936_restored-the-shared-internal-r_2493
- chg_20260715_023608_provider-automation-families-n_3875
