---
id: MA20
order: 35
plan: plan-managed-config-consistency
state: pending
validate-first: true
priority: P1
complexity: complex
created-by: codex
---

# Implement universal internal provider recipe schema

## Description

Define one provider-owned declarative RecipeDefinition schema and typed loader for automation families, plus an explicit provider capability catalogue. Each provider enumerates the capability-family IDs it supports and declares typed facts/configuration for those capabilities. Reusable generic mechanics are allowed only as independently named, schema-backed typed families selected by an explicit provider descriptor declaration. Public APIs retain family-specific payloads/results and plan|apply|prune|status modes. Resource APIs and agent execution are excluded. Schema validation describes recipes; only explicit provider+family code registration enables execution.

## Steps

1. Characterize ProvisioningRecipe, ProviderCapabilityRecipe, ProviderRecipeResult, ProviderRecipeKind, every current recipe declaration/registration, and every existing provider descriptor capability/mechanic. Produce a migration inventory mapping each caller family to: enumerated provider capability id, typed family contract, descriptor facts/configuration, and explicit executable registration if any.

2. Freeze the capability model. A provider descriptor explicitly enumerates each supported capability-family id; each declaration has a stable id, supported public modes, typed descriptor facts/configuration reference, and contract references where it is automatable. The global family-id namespace remains open: no closed central taxonomy or catch-all enum. Unknown ids fail unless a provider declares them and the relevant typed family contract/registration exists.

3. Freeze the minimal RecipeDefinition envelope: recipe id, provider id, open family id, supported modes, family payload/result contract references, recipe version, ownership-scope requirement, and non-authoritative provenance reference where required. Do not add requester identity, provider paths, commands, serializers, handler dotpaths, or a closed family taxonomy.

4. Freeze the generic-mechanic rule. A reusable mechanic (for example managed MCP entry, plugin entry, model-source, generated surface, or a future native-config contribution) is a separately named provider capability family with its own frozen Request/Result and concrete schemas. A provider opts in by explicit descriptor declaration and supplies typed adapter facts. Reuse only when desired-state semantics, ownership, modes, and result behavior are identical. Do not make a generic “capability request”, arbitrary configuration blob, generic command runner, or universal request/result.

5. Freeze metadata boundary. annotations and capability_facts may carry free-form evidence/provenance only. They are inert: they cannot name/enable a handler, add a mode, supply commands/paths/serializers, or bypass a typed family contract. Validation rejects executable/configuration fields in free-form metadata.

6. Add provider-owned JSON schemas and typed internal representation/loader for the RecipeDefinition envelope and capability declarations; keep provider vocabulary out of foundation. Resolve all contract refs during loading.

7. Keep private lifecycle mechanics in the typed recipe protocol. The schema records supported public modes; callers and declarations never select probe/install/configure/verify/uninstall/dry_run.

8. Change explicit registration identity to provider+family with recipe id as diagnostic identity. Configuration cannot register or enable implementation. Descriptor-backed generic capabilities need no requester-specific registration; custom provider behavior requires explicit code registration.

9. Make duplicate registration and unknown provider/family/mode fail deterministically with canonical provider errors. Reject undeclared family use, undeclared generic-mechanic use, missing contract refs, and metadata-driven execution.

10. Migrate fixture recipes for CLI, managed config, and generated surfaces; prove family payload schemas remain distinct. Add descriptor fixtures proving: declared capability accepted, declared generic mechanic accepted, unknown capability rejected, and annotation-only capability cannot execute.

11. Guard every automation registration with a valid definition; queries, resource commands, and execution cannot register; facts cannot execute; recipe types remain private. Document schema and the catalogue/mechanic distinction without creating a public request/result schema.

## Files

[SCHEMA] components/providers/contracts/provider-recipe.schema.json; provider capability-declaration schema if separate from descriptor schema; distinct family payload/result schemas.
[COMPONENT:providers internal] typed capability declaration/loader, RecipeDefinition loader, registry, mode adapter, declaration/contract validation.
[CONFIG] provider descriptors enumerate supported capabilities and typed adapter facts; provider-owned recipe declarations only where current automation exists.
[TEST] schema, loader, declaration, registration, duplicate, unknown/undeclared family, metadata non-authority, generic-mechanic opt-in, exclusion, and distinct family fixtures.
[DOC] docs/reference/CAPABILITY_OPERATION_SCHEMA.md — capability catalogue, open family ids, generic-mechanic, and inert-metadata rules.

## Validation

1. Each provider descriptor lists its supported capability-family IDs explicitly. 2. A declared capability resolves to one distinct typed family contract; undeclared/unknown ids fail deterministically. 3. Generic mechanics are separately named opt-in families with their own Request/Result schemas; no generic payload/blob/command family exists. 4. CLI, managed-config, and surface fixtures validate with distinct payload schemas. 5. annotations/capability_facts cannot enable behavior or carry executable/configuration mechanics. 6. Queries, resource commands, and execution are rejected from recipe registration. 7. Config/facts cannot enable behavior. 8. Duplicate provider+family registration fails. 9. Only public modes cross boundary; mechanics remain protocol-private. 10. Foundation has no provider recipe schema/type. 11. No universal public request/result or closed family taxonomy.

## Effort & Risk

Architecture owner freezes the envelope and registration identity. Strong mid-level agent may implement schema/loader/tests afterward. Stop for review if a family requires provider paths, commands, requester identity, or mechanics in the common envelope.

## Standards

- Architecture Standards: Dependency Boundaries; Extensions and Configuration; Ownership and Abstraction; Public Contracts and Safety; Enforcement.
- Architecture Guidelines: Provider Platform API; Extension Registries; Migration Validation.

## Notes

Child implementation item of MA16 created by RV460. Substrate implemented: provider-recipe.schema.json, frozen RecipeDefinition loader, open family-contract mapping, and ProviderAutomationRegistry keyed by (provider_id, family_id). MA20 remains pending until explicit provider capability declarations are validated, real families migrate, and legacy registry is deleted.

LOCKED ANSWER — BOTH, WITH STRICT ROLES:
• Enumerated capabilities: every provider descriptor explicitly declares the stable capability-family IDs it supports, modes, and typed descriptor facts. This is discoverable, validateable, and drives safe selection.
• Generic mechanics: reusable provider mechanics are permitted only as separately named, explicit capability families with their own frozen typed Request/Result and schemas. A provider opts in explicitly; reuse requires identical semantics/ownership/modes/results.
• Free-form metadata: annotations/capability_facts are allowed only for non-authoritative evidence/provenance. They cannot define a capability, mode, command, path, serializer, handler, payload, or execution behavior.

Common RecipeDefinition is metadata envelope only; it never carries family fields or executable mechanics. Every family defines frozen family-specific Request/Result objects and concrete JSON schemas for serialized forms. RecipeDefinition refs resolve to those schemas. Config/API mappings parse once at boundary; handlers and callers exchange typed objects only. No Any payload/result, nested payload blob, unchecked dict, universal cross-family request/result, generic command runner, or backwards-compatibility shim. Recipe owner builds desired request; provider descriptors expose generic capabilities/mechanics and remain requester-blind. RV467 records this clarification.

IMPLEMENTATION PROGRESS (2026-07-15):
- Added typed ProviderAutomationCapability declarations to ProviderDescriptor and YAML loader validation.
- Every current provider descriptor explicitly declares its supported current automation families: managed-mcp where mcp_config exists; opencode additionally declares plugin-entry; providers with neither declare an empty list.
- Declaration validation resolves each declared <name>/vN contract ref to a checked provider JSON schema and rejects duplicate families, non-public/duplicate modes, incomplete declarations, and missing/invalid schemas.
- ProviderAutomationRegistry now requires a matching provider declaration and exact mode/contract/scope parity before explicit registration can bind code.
- managed-mcp and plugin-entry public services now reject descriptor mechanics not explicitly declared as automation capabilities. list_provider_descriptors exposes the declarations for discovery.
- Added plugin-entry payload/result v1 schemas and typed serialization; options are now string-only values rather than Any-shaped nested payload data.
- Validation: all tests under tests/unit/providers pass; focused ruff passes.
- Remaining MA20 work: migrate additional real family registrations and delete legacy registry/routes in their owning items. Full architecture boundary suite currently has one MA25 test using the removed HindsightBackendConfig plugin constructor; migrate that test and remaining MA25 callers to HindsightPluginDefinition/HindsightPluginDesired rather than adding compatibility.

## Ledger Events

- chg_20260715_003936_restored-the-shared-internal-r_2493
- chg_20260715_004353_removed-remaining-ambiguity-fr_8955
- chg_20260715_011559_added-the-shared-internal-reci_8906
- chg_20260715_012450_made-ma23-ma27-detailed-and-ex_6494
- chg_20260715_023608_provider-automation-families-n_3875
- chg_20260715_033227_clarified-ma20-so-providers-ca_3741
- chg_20260715_034311_providers-now-advertise-automa_8875
