---
id: MA33
order: 89
plan: plan-managed-config-consistency
state: pending
validate-first: true
priority: P1
complexity: simple
created-by: codex
---

# Restore feature registry state between provider/LSP tests

## Description

Fix deterministic cross-test pollution caused by coding-LSP tests clearing process-global feature registry state. Restore canonical registry state through one shared test fixture/reset seam; do not add production caching or per-file cleanup layers.

## Steps

1. Reproduce with coding-LSP provision tests before provider provisioning tests.
2. Snapshot/restore or canonically reload feature registry state through shared test infrastructure; include every coupled registry only if evidence proves it is mutated.
3. Remove file-local clear-only setup/teardown that leaves partial state.
4. Add order-reversal regression test or two-command validation.
5. Run coding-LSP then providers, providers then coding-LSP, and focused architecture suites.

## Files

[TEST] shared fixtures plus test_provider_lsp_provision.py
[PRODUCTION] no change unless a minimal explicit test reset hook is required

## Validation

Both directory orders pass with identical results; no stale fake descriptors/features survive; no registry cache added; focused suites green.

## Effort & Risk

Simple test-isolation fix. Prefer canonical reload/restore over new registry abstraction.

## Standards



## Notes



## Ledger Events

- chg_20260716_052527_planning-review-backlog-audite_1375
