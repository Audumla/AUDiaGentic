---
id: MA21
order: 75
plan: plan-managed-config-consistency
state: pending
validate-first: true
priority: P1
complexity: mid
created-by: codex
---

# Migrate generated surfaces to recipe family

## Description

Implement the generated-surface automation family using MA20 RecipeDefinition and one providers_api family function. Replace split apply/prune public functions and migrate providers MCP without compatibility aliases.

## Steps

1. Characterize apply_provider_surfaces/prune_provider_surfaces callers, payloads, renderer selection, ownership ids, adoption, collisions, and results.
2. Freeze surface family payload/result and supported modes from callers.
3. Add provider-owned surface RecipeDefinition and explicit provider+family registrations; renderers remain provider-owned.
4. Implement one providers_api surface family operation. Plan/status do not mutate; apply renders desired contributions; prune removes only supplied owned scope.
5. Migrate providers MCP and any runtime/composition callers in the same change.
6. Delete split public functions, aliases, patch seams, and compatibility tests.
7. Prove unmanaged content preservation, exact adoption, scoped prune, redaction, and no provider-id dispatch branch.

## Files

[PUBLIC] providers_api surface family
[INTERNAL] surfaces manager adapter and recipe definitions/registrations
[CALLERS] providers MCP and runtime/composition
[TEST] mode, ownership, adoption, collision, stale-name guards

## Validation

1. One surface family function replaces apply/prune exports. 2. Definition validates through MA20. 3. Plan/status non-mutating. 4. Prune scoped. 5. Unmanaged content preserved. 6. No compatibility route. 7. Focused tests pass.

## Effort & Risk

Strong mid-level after MA20 and signature approval. Escalate payload/result/ownership changes.

## Standards

- Architecture Standards: Dependency Boundaries; Ownership and Abstraction; Enforcement.
- Architecture Guidelines: Provider Platform API; Writing External Artifacts; Migration Validation.

## Notes

Depends on MA20. Create typed GeneratedSurfaceRequest/Result and concrete schemas. LOCKED TYPED-CONTRACT RULE: define frozen family-specific Request/Result objects and concrete JSON schemas for their serialized forms before handler work. RecipeDefinition contract refs must resolve to those schemas. Config/API mappings parse once at boundary into typed objects; handlers and callers exchange typed objects only. No Any payload/result, nested payload blob, unchecked dict, or universal cross-family request/result. Recipe owner builds desired request; provider descriptors expose generic capabilities/mechanics and remain requester-blind.

SCAFFOLD COMPLETE (2026-07-15):
- Frozen GeneratedSurfaceRequest(ownership_scope, contribution_ids) and GeneratedSurfaceResult with v1 payload/result schemas. contribution_ids are semantic caller-owned identities only; renderers retain provider-native paths, block text, and file mechanics.
- Added inert generated-surface RecipeDefinition builder: family id generated-surfaces; contracts provider-generated-surface-payload/v1 and provider-generated-surface-result/v1; modes plan|apply|prune|status; opaque ownership scope required. It performs no registration, dispatch, or mutation.
- Contract serialization/schema and scaffold contract tests pass.

MID-LEVEL HANDOFF — DO NEXT, IN ORDER:
1. Characterize current plan_provider_surfaces/apply_provider_surfaces/prune_provider_surfaces callers and map each current contribution identity to GeneratedSurfaceRequest.contribution_ids. If current callers cannot supply exact desired ids, stop and add a narrowly typed runtime-composition builder; do not pass contribution blobs or reintroduce provider-id dispatch.
2. Define the ownership-scope mapping. It must identify only blocks/files owned by this family/provider contribution set; it cannot mean "all provider files." Record adopted legacy markers, collision policy, and whole-file skill ownership before handler code.
3. Add generated-surfaces declaration only to descriptors that actually render supported surfaces. Declaration must exactly match scaffold contracts/modes/scope. Compose explicit provider+family registrations around one manager adapter; no configuration-only execution.
4. Implement one family handler: plan/status read only; apply uses desired contribution ids; prune removes only supplied scope. Preserve unmanaged text and report adopted/colliding blocks through GeneratedSurfaceResult.
5. Migrate providers_mcp, observer, lifecycle/reconcile/runtime callers in one change; delete split public exports, component manifest entries, old action handlers, and compatibility tests. Do not add aliases/shims.
6. Add parity, scope, plan/status non-mutation, unmanaged-content, adoption, collision, redaction, and stale-route tests; run provider, integration, architecture-boundary, and ruff checks.

Do not mark MA21 complete until the scaffold is registered and exercised by real callers. RV469 records the frozen contract.

IMPLEMENTATION COMPLETE (2026-07-15):
- Added generated-surfaces capability to 7 provider descriptors (cline, roo, codex, opencode, claude, aider, local-openai) with exact scaffold contracts/modes/scope.
- Created operate_provider_surface(operate_provider_surfaces public API replacing apply_provider_surfaces/prune_provider_surfaces. Added GeneratedSurfaceMode type alias.
- Implemented _make_handler that dispatches plan/status through plan_provider_surfaces, apply through apply_provider_surfaces, prune through prune_provider_surfaces, returning typed GeneratedSurfaceResult.
- Migrated all callers in one change: observer (operate_provider_surfaces), lifecycle (operate_provider_surface with _build_surface_request), reconcile (operate_provider_surface with _build_surface_request), providers_mcp (operate_provider_surface/operate_provider_surfaces tools).
- Removed apply_provider_surfaces, prune_provider_surfaces from providers_api and __all__.
- Updated unit tests: test_surface_contributions (operate_provider_surface), test_surface_observer_wiring (operate_provider_surfaces).
- All 604 provider unit tests pass. Architecture boundary test passes. Pre-existing failures unrelated to MA21.

## Ledger Events

- chg_20260715_003936_restored-the-shared-internal-r_2493
- chg_20260715_023608_provider-automation-families-n_3875
- chg_20260715_051709_prepared-ma21s-typed-surface_2554
- chg_20260715_054845_migrated-generated-surface-aut_5382
