---
id: MA18
order: 25
plan: plan-managed-config-consistency
state: pending
validate-first: true
priority: P1
complexity: complex
created-by: codex
---

# Validate ACP as shared agent execution transport

## Description

Mid-level execution packet to characterize and close only proven gaps in the existing provider-neutral ACP transport and OpenCode launch binding. Architecture is frozen by `AGENT_EXECUTION_TRANSPORTS.md`: preserve current public dataclasses, neutral event contract, canonical errors, one event stream, and strict foundation agnosticism. MA17—not MA18—owns requester/provider composition.

## Steps

0. BASELINE — Run `python -m pytest tests/unit/foundation/test_acp_execution.py tests/unit/providers/test_opencode_acp.py -q`. Record pass count and inspect `docs/reference/AGENT_EXECUTION_TRANSPORTS.md`, `foundation/execution/acp.py`, and the two test files. Do not edit before producing a contract-versus-test gap list.
1. CLASSIFY EACH REQUIREMENT — Mark cancellation, child termination, malformed updates, callback isolation, bounds, permission default, redaction, terminal-result uniqueness, OpenCode launch isolation, and Docker packaging as `covered`, `missing-test`, or `failing`. If covered, add no implementation change. If the frozen document and existing tests disagree, stop and request MA18 review.
2. FOUNDATION BATCH A: FAILURE NORMALIZATION — In `foundation/execution/acp.py`, change only code required by a failing/missing test so malformed updates become non-terminal canonical error events, unexpected child exits use the registered ACP error, and every run retains exactly one terminal `result` event. Preserve `AcpLaunch`, `AcpEvent`, `AcpResult`, error-code values, and public function signature.
3. FOUNDATION BATCH B: CANCELLATION — Add/verify tests for cancel-before-prompt and cancel-during-prompt. Cancellation attempts protocol cancel first, waits no longer than `CANCEL_GRACE_SECONDS`, terminates the child if needed, and yields one cancelled terminal result. No duplicate terminal event or leaked task/process is allowed. Escalate if the installed ACP SDK cannot provide the frozen cancellation behavior without changing the public contract.
4. FOUNDATION BATCH C: CALLBACKS AND BOUNDS — Verify callback failures are isolated, success resets the consecutive-failure count, and the callback disables after `CALLBACK_FAILURE_LIMIT` consecutive failures without recursive callback emission. Verify `MAX_EVENTS`, `MAX_PAYLOAD_BYTES`, and `MAX_TOTAL_BYTES`: non-terminal overflow is counted/dropped, the terminal event is retained, and retained text/error/ext data is redacted and bounded. Do not change constant values without review.
5. PERMISSIONS AND REDACTION — Verify permission requests default-deny without `policy_fn`, explicit grants flow through, and policy/callback exceptions do not expose secrets. Use the shared redaction implementation; add no local secret patterns. Raw prompts, tokens, authorization values, and raw exception text must not appear in events or `AudiaGenticError.details`.
6. OPENCODE BINDING — In `providers/adapters/opencode/acp.py`, preserve `build_acp_launch(project_root, model_id)` as launch declaration only: executable `opencode`, args `("acp", "--cwd", resolved_root)`, optional model through `OPENCODE_CONFIG_CONTENT`, and zero file/config mutation. Add tests only for uncovered behavior. Do not add retries, profile selection, timeline writes, or provider dispatch.
7. PACKAGE GATE — Run focused suites again. Then run the existing Docker ACP gate using its documented project command; if no command is documented, record that as the sole packaging blocker rather than inventing a new workflow. Optional real-provider E2E stays opt-in and cannot block unit completion.
8. EVIDENCE HANDOFF — Update `AGENT_EXECUTION_TRANSPORTS.md` and MA19-owned provider evidence only with facts directly proven by tests or installed-tool evidence. Do not create runtime fields, registrations, or code paths from evidence.
9. MA17 HANDOFF — Produce a short handoff listing the stable `AcpLaunch`/`AcpEvent`/`AcpResult` contract, cancellation behavior, OpenCode builder, test commands, and remaining provider limitations. Do not implement the agents-to-providers execution seam here.
10. FINAL VALIDATION — Run focused foundation/provider tests, foundation component-import scan, touched-file Ruff checks, Docker wheel gate when available, and optional E2E separately. Report exact tests passed/skipped and every unexecuted environmental gate.

## Files

[EDIT ONLY FOR PROVEN GAP] `src/audiagentic/foundation/execution/acp.py`.
[EDIT ONLY FOR PROVEN GAP] `src/audiagentic/components/providers/adapters/opencode/acp.py`.
[EDIT] `tests/unit/foundation/test_acp_execution.py` — contract/gap tests.
[EDIT] `tests/unit/providers/test_opencode_acp.py` — launch isolation tests.
[RUN/EDIT IF BROKEN] `tests/docker/test_acp_docker_gate.py`, `tests/docker/Dockerfile.acp-e2e`.
[OPTIONAL RUN] `tests/e2e/agents/test_opencode_acp_e2e.py`.
[DOC/EVIDENCE] `docs/reference/AGENT_EXECUTION_TRANSPORTS.md` and MA19-owned provider evidence, proven facts only.
[READ ONLY/HANDOFF] MA17 and provider execution composition.
[FORBIDDEN] foundation component imports; new transport/session/event-store abstraction; provider selection in foundation; agents/profile/retry/timeline ownership changes; Pi implementation without evidence; public dataclass/signature/kind/error-code changes without review.

## Validation

1. Existing baseline tests are recorded before edits. 2. Focused ACP and OpenCode suites pass after each batch. 3. Foundation imports no component/provider module. 4. Exactly one terminal result exists for success, malformed-update continuation, child exit, callback failure, cancel-before, and cancel-during cases. 5. Callback and bounded-delivery constants retain their values and tested semantics. 6. Permission default-deny and explicit grant behavior pass. 7. Secret canaries are absent from event text/error/ext, returned results, logs, and error details. 8. OpenCode binding performs zero durable mutation and adds no orchestration. 9. Docker wheel gate passes or is reported as a named environmental blocker; optional E2E is reported separately. 10. MA17 handoff is complete and no runtime composition is implemented in MA18.

## Effort & Risk

Mid-level execution in small batches; architectural leverage remains high. Most requirements appear already represented in code/tests, so expected work is characterization plus targeted gaps, not a rewrite. Escalate on any required public contract, event vocabulary, error-code, constant, provider-selection, foundation dependency, or ACP SDK semantic change. Do not combine foundation, provider binding, Docker, and evidence edits into one unreviewed batch.

## Standards

- Architecture Standards: Dependency Boundaries; Ownership and Abstraction; Public Contracts and Safety; Events and Async Work; Enforcement.
- Architecture Guidelines: Error, Logging, and Redaction Practice; Async Failure Handling; Migration Validation.
- Observability Standards: one bounded redacted timeline/event projection.

## Notes

EXECUTION AUTHORITY — Mid-level agent may add characterization tests, make minimal fixes inside the two named implementation files, run the Docker gate, and update proven evidence. Architecture owner retains changes to public ACP types/signatures, event vocabulary, error identities, bound constants, foundation placement, provider selection, or MA17 composition.

Existing code already defines `AcpLaunch`, `AcpEvent`, `AcpResult`, `run_acp_prompt`, canonical ACP error constants, bounds, callback failure limit, and cancellation grace. Existing focused tests cover ordering, kind mapping, permissions, malformed updates, child exit, callback disable, cancel-before, bounds, redaction, missing SDK, and result structure. First action is therefore gap verification, especially cancel-during and process cleanup.

## Ledger Events

- chg_20260713_120627_added-working-opencode-deep-co_8918
- chg_20260714_014514_made-remaining-mamo-plans-imp_1251
- chg_20260714_023024_froze-the-agent-execution-even_3332
- chg_20260714_074131_reviewed-and-accepted-the-firs_1720
- chg_20260714_080533_verified-and-accepted-the-hard_8274
- chg_20260714_103500_ma18-steps-6-7-docker-acp-w_4958
- chg_20260714_122529_kept-deep-provider-capability_7070
- chg_20260714_123930_kept-the-complete-capability-c_5189
- chg_20260714_234418_made-the-probe-consolidation-a_3348
- chg_20260716_060605_fixed-a-bug-where-a-freshly-in_9136
