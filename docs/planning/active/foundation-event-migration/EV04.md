---
id: EV04
order: 5
plan: plan-foundation-event-migration
state: pending
validate-first: false
priority: P2
complexity: simple
---

# Strike 'registered callbacks' as a sanctioned decoupling mechanism from ARCHITECTURE_STANDARDS.md; keep events + contribution registries only, and add domain-neutral naming rule

## Description

ARCHITECTURE_STANDARDS.md section 1 (line ~22) currently sanctions three mechanisms for avoiding a forbidden import: 'registered callbacks, events, or contribution registries'. The 'registered callbacks' option is the get_capability/register_capability single-slot RPC pattern (foundation/capabilities.py). Investigation across this entire plan (foundation-event-migration EV01-03/EV05) plus the CP00 review found EVERY real and planned use of this mechanism in the codebase -- harness.config-refresh, harness.runtime-sync, providers.mcp-projector, providers.provider-config.resolve_enabled, providers.descriptors.all_ids, providers.surface-validator, and CP00's six planned CLI-layer additions -- was a misapplication: either a fire-and-forget reaction that belonged on the event bus, logic that never needed indirection at all, a decision runtime orchestration could make and act on itself, or a CLI-composition-root call site that could just check the component registry and import directly (CLI is exempt from the import-direction rule in the first place, so it never needed capability indirection). Zero legitimate uses were found. Leaving 'registered callbacks' on the books as a standards-sanctioned option invites the same misapplication to recur the next time someone needs to avoid a forbidden import and reaches for the easiest-looking tool rather than the correct one. This item removes it as a sanctioned option -- 'events' and 'contribution registries' remain the only two -- separately from the original domain-neutral-naming fix (still needed, and still applies to whatever remains: contribution-registry keys and event-type names should be domain-neutral, not named after one specific component).

## Steps

["Amend ARCHITECTURE_STANDARDS.md section 1's rule ('Runtime must never import a specific optional component. Use registered callbacks, events, or contribution registries.') to drop 'registered callbacks', leaving 'Use events or contribution registries.' Apply the same edit everywhere else in the document that lists the same three options. LAND THIS NOW, independent of code cleanup: this is a normative documentation decision, not something gated on EV06/CP00 actually removing the mechanism's callers -- the standard should say what is correct immediately, and EV01-03/05/06 and the CP00 rewrite bring the code into conformance afterward, in whatever order they land.", "Add a short rationale note near that rule (or in section 12) explaining why registered-callback/capability-slot lookups are excluded: in practice every instance found was either a discarded-return-value reaction that belonged on the event bus, logic that didn't need indirection, or a composition-root call site that could check the component registry and import directly -- see plan foundation-event-migration for the audit trail", "Add a rule to ARCHITECTURE_STANDARDS.md section 1 (or a new subsection): foundation module names, function names, event-type strings, and contribution-registry keys must be domain-neutral -- a name that only makes sense in terms of one specific component's concepts (e.g. 'provider', 'recipe', a component id) is a layering violation even with zero forbidden imports", "Add two rows to the section 12 Anti-Pattern Quick Reference table: (1) 'Single-slot registered-callback/capability lookup standing in for a direct import of one specific optional component's function' -> 'Convert to an event the optional component subscribes to; or, in a composition-root layer that is already exempt from the import rule, check the component registry (get_descriptor/is_installed) and import directly'; (2) 'Component-domain vocabulary in a foundation module/function/event-type/registry-key name' -> 'Rename to a domain-neutral concept, or move the logic into the owning component and trigger via event'", "SCAN ALL PROJECT DOCTRINE, not only ARCHITECTURE_STANDARDS.md: grep CLAUDE.md-generating component config YAMLs (config/components/*.yaml doctrine blocks, e.g. the pattern CP06 documents at agents.yaml:109, planning.yaml:108, release.yaml:53, source-control.yaml:99) and any other docs/ file for mentions of 'registered callback(s)', 'capability registry', 'get_capability', or 'register_capability' as a sanctioned pattern, and correct or remove them so no doctrine surface still recommends the struck mechanism", "Coordinate with EV06 (deletes foundation/capabilities.py entirely) and the CP00 rewrite (drops its six planned capability registrations in favor of registry-presence-check + direct import) -- track that CODE conformance separately from this item's DOC decision; this item's own validation only covers the doc, EV06's validation covers the code"]

## Files

['docs/standards/ARCHITECTURE_STANDARDS.md']

## Validation

["grep confirms 'registered callbacks' no longer appears anywhere in ARCHITECTURE_STANDARDS.md, and this is true immediately once this item lands -- it does not wait on EV06/CP00", "grep across docs/ and config/components/*.yaml confirms no other doctrine surface still recommends 'registered callbacks'/capability lookups as sanctioned", "SEPARATELY (tracked by EV06, not blocking this item): once EV01-03, EV05, EV06, and the CP00 rewrite land, grep confirms zero remaining register_capability/get_capability call sites anywhere in src/, matching the standard's new stance"]

## Effort & Risk

Simple: documentation-only change, no code impact. Per review RV99 (2026-07-05): this item's own validation is now explicitly doc-only and unblocked -- code conformance is EV06's separate concern, not a gate on landing this item.

## Standards



## Notes

Directly implements the user's explicit direction (2026-07-05): 'I don't think we should keep registered callbacks as a legitimate workaround - by leaving it in the standards it only allows abuse later on.' Originating naming-rule motivation preserved from the earlier 'the fact we have provider in the name of code in foundation is exactly representative of a clear violation' discussion. Companion to EV01-03, EV05 (all the capability deletions this standards change reflects) and EV06 (deletes the mechanism itself). Per review RV99 (2026-07-05): split the doc decision from the code-deletion gate (this item can and should land first, independent of EV06) and broadened scope to scan all project doctrine surfaces, not just ARCHITECTURE_STANDARDS.md.
