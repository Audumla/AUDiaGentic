---
id: HM05
order: 5
plan: plan-memory-hindsight
state: pending
validate-first: true
priority: P1
complexity: complex
---

# MCP-config + recall/retain rule strategy

## Description

Implement the mcp-config(+rule) strategy in the HM03 builder: write the remote Hindsight entry through controlled provider target data/generic provider writer seams, and insert a managed recall/retain rule block. Hindsight-specific logic remains in `components/memory/hindsight/`; provider core exposes only generic config/descriptor seams. Includes the openhands adapter prerequisite (no generic mcp_config today).

## Steps

['Builder mcp-config branch: construct desired remote MCP entry from HindsightBackendConfig (name=backend.server_name, url=backend.mcp_url, headers=backend.headers(), transport=backend.transport) and write it using a controlled HindsightTarget derived from generic provider descriptor/config metadata.', 'Rule: if spec.rule_block, apply_managed_block(rule_target, "hindsight-recall-retain", RULE_TEXT) and register the BlockChange in artifact_registry under recipe id memory:hindsight:<provider>.', 'verify: ConfigKeyCheck(config_path, container+server_name) passes AND rule block present. prune/uninstall removes only AUDiaGentic-owned entry/block.', 'OPENHANDS PREREQ (sub-task): add generic McpConfigSpec to openhands adapter — config_path=".openhands/config.toml" (confirm), reader/writer/remover over TOML [mcp_servers.<name>] using the HM02 remote shape (needs tomli-w). Without this generic provider seam, openhands mcp-config is blocked.', 'COPILOT PATH (sub-task): confirm whether running Copilot reads .vscode/mcp.json (Hindsight target) vs our adapter .mcp.json; set rule_target=.github/copilot-instructions.md.', 'Providers covered by memory/hindsight matrix: copilot, openhands(after prereq), continue_, roo, gemini(self-hosted); fallback-mcp (no official rule, our rule only): goose, pi, qwen.']

## Files

['src/audiagentic/components/memory/hindsight/recipes.py', 'src/audiagentic/components/memory/hindsight/mcp_recipe.py', 'src/audiagentic/components/providers/services/mcp.py (generic provider MCP writer only; no Hindsight imports)', 'tests/unit/memory/test_hindsight_recipe.py']

## Validation

['test_mcp_strategy_writes_remote_entry: provider config gets {url:<mcp_url>,headers} under its container key', 'test_rule_block_idempotent: applied once, second provision no-op', 'test_uninstall_clean: entry + rule removed, user/external entries preserved', 'test_openhands_toml_writer: remote [mcp_servers.hindsight] round-trips (after prereq)', 'Live (HM09): entry url reachable via MCP initialize']

## Effort & Risk

Complex. DEPENDS ON HM02 (remote entry) + HM03 (builder). Internal prereq: openhands McpConfigSpec must land before openhands works. Reuses sync_managed_provider_mcp_subset (ownership registry, collisions, reload) — do NOT use generic ConfigPatcher.

## Notes

Per STRATEGY_MATRIX.md, mcp-config+rule applies to: copilot, openhands, continue_, roo, gemini; fallback-mcp to goose, pi, qwen. GAPS: (1) openhands adapter has NO mcp_config — add an McpConfigSpec (config.toml writer) to the openhands adapter BEFORE this strategy works for it. (2) copilot: our adapter writes .mcp.json but Hindsight targets .vscode/mcp.json + .github/copilot-instructions.md — confirm which the running Copilot reads. (3) gemini Cloud needs an OAuth proxy; self-hosted is direct.
