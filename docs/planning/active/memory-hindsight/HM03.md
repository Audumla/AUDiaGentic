---
id: HM03
order: 3
plan: plan-memory-hindsight
state: pending
validate-first: true
priority: P0
complexity: complex
---

# Provider Hindsight strategy contract + resolution; drop wrappers

## Description

Define the declarative Hindsight install-spec + the generic recipe builder + the per-provider resolver. Installers are DECLARED (data), not hand-coded per provider — consistent with the dependency workflow. Reuse ProvisioningRecipe/ShellStep/probes/managed_block/artifact_registry; mcp-config uses provider writers. Remove the superseded wrappers.

## Steps

['Declarative spec (providers/descriptors/base.py): @dataclass(frozen=True) class HindsightSpec{ kind:str("native-installer"|"launch-wrapper"|"mcp-config"|"rules-only"); platforms:tuple[str,...]=("linux","darwin","win"); install_cmd:tuple[str,...]|None=None; uninstall_cmd:tuple[str,...]|None=None; probe:str|None=None (dependency-style "path:.."/"command:.."); connection_file:str|None=None; connection_format:str="json"(json|toml|env-prefix); rule_block:bool=False; rule_target:str|None=None; launch_override:tuple[str,...]|None=None }. Optional field on ProviderDescriptor: hindsight:HindsightSpec|None=None.', 'Command placeholders resolved from HindsightBackend at build time: {mcp_url}{api_key}{bank_id}{base_url}.', 'Generic builder (providers/services/hindsight_strategy.py): build_hindsight_recipe(spec:HindsightSpec, backend:HindsightBackend, provider_id:str, project_root:Path)->ProvisioningRecipe. native-installer -> ShellStep(install_cmd)+connection-file write+ShellStep(uninstall_cmd)+probe; mcp-config -> provider-writer entry + optional managed_block rule; rules-only -> managed_block only; launch-wrapper -> records launch_override (consumed by provider launch).', 'Resolver: resolve_hindsight_strategy(provider_id, project_root)->HindsightSpec|None. Precedence: descriptor.hindsight (explicit) > derive: native if descriptor declares installer fields > mcp-config if descriptor.mcp_config present > rules-only. Platform gate: drop strategies whose platforms exclude current platform_key(), then re-resolve to next.', 'Delete recipe_steps.py (StepRecipe), coding_lsp/lsp_recipe.py, memory/hindsight_recipe.py (ConfigPatcher version) + their tests. Keep ProvisioningRecipe/probes/managed_block/artifact_registry/ShellStep additions.']

## Files

['src/audiagentic/components/providers/services/hindsight_strategy.py', 'src/audiagentic/components/providers/descriptors/base.py', 'src/audiagentic/foundation/toolchains/recipe_steps.py (delete)', 'src/audiagentic/components/coding_lsp/lsp_recipe.py (delete)', 'src/audiagentic/components/memory/hindsight_recipe.py (rewrite)']

## Validation

['test_resolver_precedence: explicit > native > mcp-config > rules-only', 'test_platform_gate: cline spec (platforms=linux,darwin) on win resolves to fallback', 'test_builder_native: produces recipe whose install runs install_cmd with placeholders substituted', 'test_builder_mcp_config: produces recipe that writes a remote entry via provider writer', 'Deleted modules absent; remaining toolchains tests still green']

## Effort & Risk

Complex. DEPENDS ON HM01 (backend) + HM02 (remote entry). Blocks HM04/HM05/HM06/HM07. Risk: over-fitting the spec — validate it expresses all 5 strategy kinds from STRATEGY_MATRIX before wiring providers.

## Notes

Strategy resolution is authoritatively listed in docs/planning/active/memory-hindsight/STRATEGY_MATRIX.md. New strategy kinds vs original plan: launch-wrapper (aider) and explicit platform gating + openhands adapter prerequisite.
