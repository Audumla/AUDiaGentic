---
id: HM08
order: 8
plan: plan-memory-hindsight
state: pending
validate-first: true
priority: P1
complexity: simple
---

# Authoritative per-provider strategy matrix

## Description

Authoritative per-provider strategy resolution, now materialized as STRATEGY_MATRIX.md AND as the concrete HindsightSpec value each provider descriptor declares (HM04/HM05/HM06 consume these).

## Steps

['STRATEGY_MATRIX.md created with all 15 providers -> resolved strategy + verified mechanism + gaps (DONE).', 'For each provider, write the descriptor HindsightSpec literal (or rely on auto-derivation) consistent with the matrix: native-installer (claude,codex,opencode,cline), launch-wrapper (aider), mcp-config+rule (copilot,openhands,continue_,roo,gemini), fallback-mcp (goose,pi,qwen), rules-only (plandex,local_openai).', 'Remaining source confirmations to close: copilot config path (.vscode/mcp.json vs .mcp.json), pi/qwen MCP behavior, gemini Cloud OAuth-proxy path, codex codex.json url precedence.']

## Files

['docs/planning/active/memory-hindsight/STRATEGY_MATRIX.md']

## Validation

['Every provider has exactly one resolved strategy (matrix + descriptor agree)', 'Native installer commands quoted verbatim from official docs', 'Each gap has an owner item (openhands->HM05 prereq, aider->HM03 launch-wrapper, cline-win->HM06)']

## Effort & Risk

Simple/in-progress. Matrix doc DONE. Remaining = small confirmations + transcribing specs into descriptors (part of HM04/05/06). Supersedes toolchain-provisioning/INTEGRATION_MATRIX.md.

## Notes

DELIVERABLE CREATED: docs/planning/active/memory-hindsight/STRATEGY_MATRIX.md — all 15 providers mapped to a resolved strategy with verified mechanisms and explicit gaps. Remaining confirmations: copilot config path (.vscode/mcp.json vs .mcp.json), pi/qwen harness MCP behavior. Supersedes toolchain-provisioning/INTEGRATION_MATRIX.md.
