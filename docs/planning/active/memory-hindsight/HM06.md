---
id: HM06
order: 6
plan: plan-memory-hindsight
state: pending
validate-first: false
priority: P2
complexity: mid
---

# Rules-only fallback; remove legacy prompt contribution

## Description

Implement the rules-only strategy (no-MCP/no-native providers and unsupported-platform fallbacks) and remove the legacy build_memory_contributions prompt path so there is exactly one source of the recall/retain guidance.

## Steps

['Builder rules-only branch: apply_managed_block(rule_target=<provider surface instruction file>, block_id="hindsight-recall-retain", RULE_TEXT including backend.mcp_url) + register BlockChange for prune.', 'Define RULE_TEXT once (shared constant) so mcp-config(+rule) and rules-only emit identical guidance.', 'Providers: plandex, local_openai, and any provider whose native strategy is platform-gated out (e.g. cline on win32).', 'Remove memory_api.build_memory_contributions and its call site in providers/surfaces/contributions.py (lines ~197-214). No duplicate path.', 'Ensure disabling memory / switching backend triggers prune of the rules block (via HM07 teardown).']

## Files

['src/audiagentic/components/memory/memory_api.py', 'src/audiagentic/components/providers/surfaces/contributions.py', 'src/audiagentic/components/providers/services/hindsight_strategy.py']

## Validation

['test_rules_only_writes_block: non-MCP provider surface gets the recall/retain block referencing mcp_url', 'test_legacy_removed: build_memory_contributions no longer exists; contributions.py no longer special-cases memory', 'test_block_pruned_on_disable: removing memory strips the block', 'No double-write: only one hindsight block per surface']

## Effort & Risk

Medium. DEPENDS ON HM03. Risk: build_memory_contributions removal must not orphan existing surface text — prune old contribution on migration. User directive: no legacy/no shims.

## Notes

User directive: no legacy/no shims. The rules-only strategy supersedes the old prompt contribution entirely.
