---
id: HM09
order: 9
plan: plan-memory-hindsight
state: pending
validate-first: false
priority: P1
complexity: complex
---

# End-to-end tests against live Hindsight + Docker for installers

## Description

Prove every provider sets up correctly against the live Hindsight server and that teardown fully reverses it. Mcp-config/rules verified by file assertions + a live MCP handshake; native installers validated in Docker.

## Steps

['Live reachability fixture (tests/integration/memory/test_hindsight_live.py): POST http://10.10.100.10:8888/mcp with JSON-RPC initialize {jsonrpc:2.0,id:1,method:initialize,params:{protocolVersion,capabilities,clientInfo}} and Accept: application/json,text/event-stream; assert 200 + server capabilities/tools. Skip-marker if server unreachable (CI offline).', 'mcp-config providers (copilot,continue_,roo,gemini,openhands,goose,pi,qwen): provision -> assert provider config file contains {url:http://10.10.100.10:8888/mcp,headers} under correct container + rule block present; teardown -> both gone, user content intact.', 'native-installer providers (claude,codex,opencode,cline) in tests/e2e/memory/test_hindsight_provider_setup_docker.py: inside Docker image run provision -> assert hooks/scripts/plugin + connection file created and point at test url; teardown -> assert zero managed artifacts remain; idempotent re-provision.', 'launch-wrapper (aider): assert provider launch command resolves to hindsight-aider after provision; revert after teardown.', 'backend-switch test: set base-url A, provision; set B, provision; assert all entries/connection files now reference B.', 'global teardown test: disable memory -> assert no "hindsight" managed entry, rule block, hook, or connection file remains across ALL providers (sweep).']

## Files

['tests/integration/memory/test_hindsight_live.py', 'tests/e2e/memory/test_hindsight_provider_setup_docker.py']

## Validation

['Live MCP initialize handshake returns server capabilities', 'Each provider config/hooks/rules match expected for the test url', 'Full uninstall leaves zero managed Hindsight artifacts (verified by sweep)', 'Env-changing installer tests run in Docker only (host gate respected)']

## Effort & Risk

Complex. DEPENDS ON HM04/HM05/HM06/HM07. Live server http://10.10.100.10:8888/mcp (control plane :9999). Native-installer tests are env-changing -> Docker image only per project doctrine. Mark live tests skippable when server unreachable.

## Notes

Test server: http://10.10.100.10:8888/mcp (control plane http://10.10.100.10:9999). Per project doctrine, env-changing tests run in the Docker image only.
