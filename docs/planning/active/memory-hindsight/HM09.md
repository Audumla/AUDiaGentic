---
id: HM09
order: 9
plan: plan-memory-hindsight
state: pending
validate-first: true
priority: P1
complexity: complex
---

# End-to-end tests against live Hindsight + Docker for installers

## Description

Prove every provider sets up correctly against the live Hindsight server and that teardown fully reverses it, without running host-native installers or masking boundary/source gaps. MCP-config/rules verified by file assertions + live MCP handshake; native installers validated in Docker only after source_status is verified.

## Steps

['Live reachability fixture (tests/integration/memory/test_hindsight_live.py): POST http://10.10.100.10:8888/mcp with JSON-RPC initialize {jsonrpc:2.0,id:1,method:initialize,params:{protocolVersion,capabilities,clientInfo}} and Accept: application/json,text/event-stream; assert 200 + server capabilities/tools. Skip-marker if server unreachable (CI offline).', 'Architecture fixture: verify no `providers/services/hindsight*.py`, memory core outside `memory/hindsight` has no provider imports, memory/hindsight provider imports are limited to controlled generic seams, and foundation/toolchains has no MCP/provider/Hindsight imports before running integration tests.', 'Source/execution fixture: native-installer and launch-wrapper provider cases are parametrized only when source_status="verified"; unconfirmed/blocked entries assert blocked status instead of executing; verified shell-compound commands (`|`, `&&`, redirection, `;`) assert blocked until structured shell-step support exists.', 'mcp-config providers (copilot,continue_,roo,gemini,openhands,goose,pi,qwen): provision through memory/hindsight orchestration -> assert provider config file contains {url:http://10.10.100.10:8888/mcp,headers} under correct container + rule block present; teardown -> both gone, user content intact. Skip blocked adapters such as openhands until HM05 prereq lands.', 'native-installer providers (claude,codex,opencode,cline) in tests/e2e/memory/test_hindsight_provider_setup_docker.py: inside Docker image run provision -> assert official hooks/scripts/plugin + connection file created and point at test url; teardown -> assert zero managed artifacts remain; idempotent re-provision.', 'launch-wrapper (aider): assert provider launch command resolves to official wrapper after provision; revert after teardown. Do not assert `hindsight-aider` unless HM08 source verifies that exact binary name.', 'backend-switch test: set base-url A, provision; set B, provision; assert all owned entries/connection files now reference B.', 'global teardown test: disable memory -> memory/hindsight reconcile removes every AUDiaGentic-owned "hindsight" managed entry, rule block, hook, or connection file across ALL providers (sweep).']

## Files

['tests/integration/memory/test_hindsight_live.py', 'tests/e2e/memory/test_hindsight_provider_setup_docker.py']

## Validation

['Live MCP initialize handshake returns server capabilities', 'Architecture gates pass before integration tests: providers/services has no Hindsight modules; memory core has no provider imports; memory/hindsight imports only controlled generic provider seams; foundation/toolchains has no MCP/provider/Hindsight imports', 'Each provider config/hooks/rules match expected for the test url and source-verified mechanism', 'Unconfirmed native/launch-wrapper specs report blocked and are not executed', 'Full uninstall leaves zero AUDiaGentic-owned Hindsight artifacts (verified by sweep)', 'Env-changing installer tests run in Docker only (host gate respected)']

## Effort & Risk

Complex. DEPENDS ON HM04/HM05/HM06/HM07/HM08. Live server http://10.10.100.10:8888/mcp (control plane :9999). Native-installer tests are env-changing -> Docker image only per project doctrine. Mark live tests skippable when server unreachable. Do not let skipped live server hide architecture/source-gate failures.

## Notes

Test server: http://10.10.100.10:8888/mcp (control plane http://10.10.100.10:9999). Per project doctrine, env-changing tests run in the Docker image only.
