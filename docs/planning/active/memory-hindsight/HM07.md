---
id: HM07
order: 7
plan: plan-memory-hindsight
state: pending
validate-first: false
priority: P1
complexity: complex
---

# Memory<->provider orchestration, lifecycle wiring, status

## Description

Orchestrate: on memory lifecycle/config changes, resolve and run each eligible provider's Hindsight strategy; on disable/uninstall/backend-switch, tear down. Report per-provider status. Memory triggers; the providers component executes.

## Steps

['providers/services/hindsight_strategy.py: apply_hindsight(project_root) -> iterate installed+enabled providers; for each resolve_hindsight_strategy + build_hindsight_recipe(backend); run recipe.provision({}). teardown_hindsight(project_root[,provider_id]) -> recipe.teardown({}).', 'Trigger seam: extend memory_api._trigger_surface_reconcile (or a new _trigger_hindsight) to call apply_hindsight after select_implementation/set_config when providers component enabled. Backend switch (url change) -> provision reconfigures (probe sees stale entry -> rewrites).', 'Lifecycle: in providers/surfaces/observer.py handle memory enable->apply_hindsight, memory disable/uninstall->teardown_hindsight for all providers. Reuse existing lifecycle.component.* subscriptions (no new bus).', 'Status: providers status tool / memory_status reports per provider {strategy_kind, state(verified|absent|error), owned_artifacts}. Pull state from probe + artifact_registry.', 'Concurrency/idempotency: provision is idempotent (probe short-circuit); teardown tolerant of already-removed (artifact_registry prune skips missing).']

## Files

['src/audiagentic/components/memory/memory_api.py', 'src/audiagentic/components/providers/surfaces/observer.py', 'src/audiagentic/components/providers/services/hindsight_strategy.py']

## Validation

['test_enable_provisions_all: enabling memory provisions every eligible provider; resolver-skipped providers untouched', 'test_disable_teardown: disabling memory removes all owned artifacts across providers', 'test_url_switch_reconfigures: change base-url -> every provider entry/connection file updated', 'test_status_report: states accurate (verified after provision, absent after teardown)', 'test_provider_not_enabled_skipped']

## Effort & Risk

Complex. DEPENDS ON HM03/HM04/HM05/HM06 (needs builder + all strategy kinds). Risk: native-installer provisioning is slow/env-changing — gate on opt-in + Docker for tests; do not run host installers in unit tests.

## Notes

Reuses the lifecycle.component.* observer seam already used for MCP projection and surfaces.
