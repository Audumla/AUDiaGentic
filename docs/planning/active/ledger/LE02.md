---
id: LE02
order: 0
plan: plan-ledger
state: pending
validate-first: false
priority: P2
work: M
created-by: assistant
---

# Runtime artifact accumulation — cleanup strategy

## Description

Several runtime directories under .audiagentic accumulate files indefinitely with no cleanup policy. This item catalogs the issues and evaluates options for auto-prune.

## Steps

['Add TTL config field to agent-ledger.yaml component config (default: 7 days for jobs and logs)', 'Create runtime/ledger/cleanup.py with _purge_expired_jobs() and _purge_old_cli_logs()', '_purge_expired_jobs: scan runtime/jobs dirs, read timeline.ndjson or job.json last-updated timestamp, unlink dirs exceeding TTL', '_purge_old_cli_logs: scan logs/cli/, delete files older than TTL', 'Call cleanup at end of sync_current_release_ledger (reuses existing lock)', 'Add ag-ledger MCP tool: purge_runtime_artifacts(category, days) for manual override on sessions/traces', 'Update SyncResult with purged_jobs, purged_logs counters', 'Write tests: TTL expiration boundary, in-progress job protection, zero-TTL edge case']

## Files

['src/audiagentic/components/agent_jobs/', 'src/audiagentic/runtime/harness/run_common.py', 'src/audiagentic/runtime/harness/pi/runner/agent_run.py', 'src/audiagentic/components/ledger/sync.py']

## Validation

['Existing 29 release/unit tests still pass', 'TTL purge correctly skips jobs with recent timeline activity', 'CLI logs older than threshold are removed; recent logs survive', 'MCP tool accepts category parameter and deletes matching files', 'Zero artifacts remain after aggressive (0-day) purge of completed items']

## Effort & Risk

Low risk if scoped to completed/terminal artifacts only. Risk of deleting in-progress job data if TTL is too aggressive. Sessions are written by external agents — pruning must not affect active sessions.

## Standards



## Notes



## Ledger Events


