---
id: RS08
order: 8
plan: plan-recipe-system-review
state: pending
validate-first: true
priority: P1
complexity: simple
created-by: gpt-5.5-thinking
---

# Add recipe contract characterization tests

## Description

Small execution slice for RS01. Add tests that lock current `ProvisioningRecipe` orchestration semantics before any refactor touches provider or Hindsight recipes.

## Proposed solution

Add test-only recipe fixtures that exercise every base orchestration branch. Do not change production code unless a test exposes behavior that directly violates standards.

## Implementation steps

1. Create/extend `tests/unit/foundation/toolchains/test_recipe_contract.py`.
2. Add fixtures for verified fast-path, primitive success, primitive install failure, primitive configure failure, step success, step failure, verify failure, prune failure, uninstall failure, cleanup hook failure, and post-uninstall verify failure.
3. Add a fixture whose `to_result()` records every conversion call.
4. Assert `RecipeState`, `success`, `artifacts_owned`, `status`, `error`, `details`, and `action_needed` for each branch.
5. Mark any unsafe cleanup exception text as expected-fail only if RS05 will fix it; otherwise fail now.

## Files

[TEST] `tests/unit/foundation/toolchains/test_recipe_contract.py`
[FOUNDATION] `src/audiagentic/foundation/toolchains/recipe_contract.py` — only if tests expose a standards violation.

## Validation

`python -m pytest tests/unit/foundation/toolchains/test_recipe_contract.py`

## Done when

- Every public path through `ProvisioningRecipe.provision()` and `teardown()` has a test.
- Later RS02/RS03 agents can rely on these tests before simplifying dependants.
