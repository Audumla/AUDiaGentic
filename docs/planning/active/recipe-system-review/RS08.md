---
id: RS08
order: 8
plan: plan-recipe-system-review
state: pending
validate-first: true
priority: P1
complexity: simple
created-by: gpt-5.5-thinking
---

# Add recipe contract characterization tests

## Description

Small execution slice for RS01. Add tests that lock current `ProvisioningRecipe` orchestration semantics before any refactor touches provider or Hindsight recipes. This is characterization only — no production code changes unless a test exposes a clear standards violation requiring minimal inline fix.

Dependency: none (first in characterize-modify chain for contract area).

## Steps

1. Do not implement this item independently. Open RS01 and follow its Steps instead — this item's original scope (characterization tests for `ProvisioningRecipe`) is already satisfied by the existing `tests/unit/foundation/toolchains/test_recipe_contract.py` (35 tests) discovered during RV228/RV229 audits.
2. Once RS01's gap-audit step is confirmed complete (its one new test `test_cleanup_hook_failure_details_are_safe` passing), run `plan_delete_item` on this item ID to remove it from the active board — keeping both RS01 and RS08 open long-term would let a future contributor re-do RS01's work under this ID.
3. If `plan_delete_item` is not available/desired, at minimum set this item's `state` to a terminal value distinct from `pending` (e.g. mark completed with a note pointing at RS01) so it does not appear as outstanding work in `plan_list_items`.

## Files

[TEST] `tests/unit/foundation/toolchains/test_recipe_contract.py`
[FOUNDATION] `src/audiagentic/foundation/toolchains/recipe_contract.py` — only if tests expose a standards violation requiring minimal inline correction.

## Validation

1. This item is either deleted via `plan_delete_item`, or its state is no longer `pending` and its notes point to RS01 as the authoritative execution record.
2. No test file is created under this item's name independent of RS01's changes to `test_recipe_contract.py`.

## Effort & Risk

Low risk, low effort. Read-only characterization: adds tests that capture current behavior. Risk is only in exposing undocumented edge cases — if tests reveal a violation, file must be tagged for RS01 follow-up, not fixed inline.

## Standards

Std §1: foundation remains domain-neutral — test names and fixtures must not reference provider or component vocabulary. Std §8: public failures use structured errors/results; tests must verify `RecipeResult` shape compliance. Std §10: test-driven migration — these characterization tests are the safety prerequisite for RS01 validation.

## Notes

RV228 incorporated: `tests/unit/foundation/toolchains/test_recipe_contract.py` already exists with substantive lifecycle tests, so this is no longer a from-scratch characterization task. Merged into RS01 as its gap-audit execution slice — do not schedule/execute RS08 independently. Superseded by RS01's updated notes; retained here only so the ID isn't silently dropped from history. Consider `plan_delete_item` once RS01's gap-audit is confirmed to cover this item's validation checklist.

## Ledger Events


