---
id: RS17
order: 17
plan: plan-recipe-system-review
state: pending
validate-first: true
priority: P2
complexity: simple
created-by: gpt-5.5-thinking
---

# Classify recipe dependant direct mutations

## Description

Small execution slice for RS06. Produce a concrete, classified list of every direct mutation in recipe dependants before replacing anything. Classification must cover file writes, JSON mutations, directory/file deletions, and config key modifications.

Dependency: none (characterize phase — must complete before RS18).

## Steps

1. Run `git grep -n "json.dump\|json.dumps\|shutil.rmtree\|os.unlink\|\.unlink(\|\.write_text(" -- src/audiagentic/components/memory/hindsight/ src/audiagentic/components/providers/services/mcp.py` and paste full output into a new `docs/planning/active/recipe-system-review/RS17-CLASSIFICATION.md` file (does not exist yet — confirmed in this audit).
2. For each match, classify into exactly one of 3 categories and record as a markdown table row `| file:line | snippet | category | rationale |`:
   a. **shared-primitive-available**: an existing foundation/provider primitive (`WriteFileStep`, `ArtifactRegistry`, `ConfigSetStep`, `sync_managed_provider_mcp_subset`) could replace this call with no loss of behavior.
   b. **intentional-one-off**: genuinely unique behavior (e.g. patches a file this recipe does not own, or writes optional JSON keys no declarative step can express) — must state WHY no primitive fits, not just assert it.
   c. **requires-investigation**: unclear ownership or behavior not fully understood from reading alone.
3. Confirm/refute RS06's preliminary classification for the 3 known sites in `plugin_recipes.py` (`_PluginUrlConfigRecipe.configure()`'s `write_text`, `.prune()`'s `unlink`, `_repair_windows_plugin_mcp()`'s `json.loads`/`write_text`) — write your own independent judgment, do not just copy RS06's Notes.
4. Check `providers/services/mcp.py` for the "sanctioned managed MCP sync" primitive (`sync_managed_provider_mcp_subset`, `get_managed_entry_status`) referenced throughout `recipes.py`/`plugin_recipes.py` — confirm it is the correct existing primitive for MCP entry writes (it already is used by `_McpConfigAdapter`/`PluginConfigRecipe` when `config_path` is set) and mark any direct MCP JSON write that bypasses it as "shared-primitive-available".
5. Do not modify any production code in this item — inventory only.
6. Cross-check every classified `requires-investigation` entry does not silently become a `plan_delete_item` candidate later — RS18 must explicitly defer these, not drop them.

## Files

[COMPONENT:memory] `src/audiagentic/components/memory/hindsight/recipes.py`
[COMPONENT:memory] `src/audiagentic/components/memory/hindsight/plugin_recipes.py`
[COMPONENT:memory] `src/audiagentic/components/memory/hindsight/strategies.py`
[FOUNDATION] `src/audiagentic/foundation/toolchains/*`
[COMPONENT:providers] `src/audiagentic/components/providers/services/mcp.py`
[DOCS] `docs/planning/active/recipe-system-review/RS17-CLASSIFICATION.md`

## Validation

1. `docs/planning/active/recipe-system-review/RS17-CLASSIFICATION.md` exists and every `git grep` match from step 1 has exactly one category assigned with a one-line rationale.
2. The 3 known `plugin_recipes.py` sites from RS06 are present in the table with an independently-reasoned (not copy-pasted) classification.
3. `git diff --stat src/` is empty — no production code touched.
4. Table is machine-checkable: every row has the exact format `| path:line | snippet | category | rationale |` so RS18 can grep the table for `shared-primitive-available` rows to know exactly what to touch.

## Effort & Risk

Low risk, low effort. Read-only classification work — no production code changes. Risk is incomplete classification leading to unsafe RS18. Mitigate by documenting every finding with file:line and category tag.

## Standards

Std §3: consolidate repeated logic used by multiple files — classification must identify which direct mutations have shared-primitive alternatives. Creating Components §6: provider-specific rendering stays in providers; Hindsight supplies only generic backend data. Std §10: whole-repo string grep required for any path identified as removable.

## Notes



## Ledger Events


