---
id: RS17
order: 17
plan: plan-recipe-system-review
state: pending
validate-first: true
priority: P2
complexity: simple
created-by: gpt-5.5-thinking
---

# Classify recipe dependant direct mutations

## Description

Small execution slice for RS06. Produce a concrete classification of every direct mutation in recipe dependants before replacing anything.

## Proposed solution

Inventory direct writes/deletes/config mutations and classify each as replace-with-existing-primitive, sanctioned-shared-primitive, keep-with-comment, needs-registration, or needs-new-primitive. This is a documentation/test slice, not a refactor slice.

## Implementation steps

1. Grep recipe dependants for `write_text`, `unlink`, `json.loads`, `read_text`, `run_steps`, `steps_from_defs`, `sync_managed_provider_mcp_subset`, `ConfigPatcher`, `apply_managed_block`, and provider config writers.
2. Create `docs/planning/active/recipe-system-review/RS17-CLASSIFICATION.md` or include the table in the implementation PR notes.
3. For each occurrence, record file, function/class, mutation type, current owner, existing primitive available, and decision.
4. Mark `_sync_hindsight_mcp_entry()` / `sync_managed_provider_mcp_subset()` as sanctioned shared provider-managed MCP writes.
5. Record `ProviderRecipeResult(...)` direct construction in provider registry and Hindsight lifecycle as result-construction duplication owned by RS10.
6. Mark `_PluginUrlConfigRecipe` direct URL config writes as needs `ArtifactRegistry` registration or an explicit exemption.
7. Identify which follow-up task owns each replacement: RS10, RS12, RS14, RS16, RS18, or no change.
8. Do not modify production code in this slice.

## Files

[COMPONENT:memory] `src/audiagentic/components/memory/hindsight/recipes.py`
[COMPONENT:memory] `src/audiagentic/components/memory/hindsight/plugin_recipes.py`
[COMPONENT:memory] `src/audiagentic/components/memory/hindsight/strategies.py`
[FOUNDATION] `src/audiagentic/foundation/toolchains/*`
[COMPONENT:providers] `src/audiagentic/components/providers/services/mcp.py`
[DOCS] `docs/planning/active/recipe-system-review/RS17-CLASSIFICATION.md` optional

## Validation

- Whole-repo grep commands and results are captured in the PR notes or classification doc.
- No production code changes are made.

## Done when

- Every direct mutation has an owner and a decision.
- Every direct result-construction duplicate has an owner and a decision.
- RS18 can implement the replacements without rediscovering the codebase.
