---
id: RS18
order: 18
plan: plan-recipe-system-review
state: pending
validate-first: true
priority: P2
complexity: simple
created-by: gpt-5.5-thinking
---

# Replace or document direct recipe mutations

## Description

Small execution slice for RS06. After RS17 classification, replace direct recipe mutations with existing primitives where appropriate, or document intentional one-offs with tests.

## Dependencies

- RS17 must be complete.
- RS14/RS16 should be complete before replacing path/redaction-sensitive writes.

## Proposed solution

Only change mutations with clear existing primitives or ownership gaps. Do not invent broad new foundation mechanisms for one-off plugin repair or optional JSON settings unless RS17 identifies a second consumer.

## Implementation steps

1. For each RS17 item classified `replace-with-existing-primitive`, route through `ArtifactRegistry`, provisioning steps, or provider managed config.
2. For each `keep-with-comment`, add a short code comment and test explaining why the direct mutation remains.
3. For plugin URL config, add artifact registration or tests proving prune is safe and scoped.
4. For plugin repair writes, add tests proving it only edits expected plugin files and does not expose secrets.
5. Remove duplicated helper code only after tests prove replacement behavior.

## Files

[COMPONENT:memory] `src/audiagentic/components/memory/hindsight/plugin_recipes.py`
[COMPONENT:memory] `src/audiagentic/components/memory/hindsight/recipes.py`
[FOUNDATION] `src/audiagentic/foundation/toolchains/artifact_registry.py` — tests only unless bug found.
[FOUNDATION] `src/audiagentic/foundation/toolchains/provision_steps/*` — only if RS17 requires.
[TEST] `tests/unit/memory/hindsight/`
[TEST] `tests/unit/foundation/toolchains/`

## Validation

`python -m pytest tests/unit/memory/hindsight tests/unit/foundation/toolchains`

## Done when

- No direct mutation remains undocumented.
- Replaced mutations use existing primitives.
- Tests prove user/unmanaged config is preserved.
