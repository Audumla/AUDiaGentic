---
id: RS10
order: 10
plan: plan-recipe-system-review
state: pending
validate-first: true
priority: P1
complexity: simple
created-by: gpt-5.5-thinking
---

# Simplify provider recipe stamping after characterization

## Description

Small execution slice for RS02. After RS09 tests land, simplify provider result conversion and remove duplicate `ProviderRecipeResult` reconstruction where tests prove it is safe.

## Dependencies

- RS08 should be complete so base orchestration is characterized.
- RS09 must be complete so provider stamping is characterized.

## Proposed solution

Use `ProviderCapabilityRecipe.to_result()` / `_stamped()` as the single provider-layer conversion boundary. Remove or cover the `ProviderRecipeRegistry.install()` fallback primitive orchestration path. Refactor Hindsight `_RowRecipe` to delegate to provider conversion before applying row fallback guidance.

## Implementation steps

1. Replace manual `ProviderRecipeResult(...)` reconstruction in `ProviderRecipeRegistry.install()` fallback with a helper, or delete fallback if inventory proves there are no legacy recipe-like registrations.
2. Ensure `ProviderRecipeRegistry.uninstall()` fallback is either covered or removed under the same rule.
3. Refactor `_RowRecipe._stamp()` / `to_result()` to call `super().to_result(base)` first.
4. Keep Hindsight row-specific action-needed fallback in Hindsight only.
5. Run RS09 tests and Hindsight recipe-spec tests.

## Files

[COMPONENT:providers] `src/audiagentic/components/providers/services/recipes.py`
[COMPONENT:memory] `src/audiagentic/components/memory/hindsight/recipes.py`
[COMPONENT:memory] `src/audiagentic/components/memory/hindsight/recipe_spec.py`
[TEST] `tests/unit/providers/test_provider_recipes.py`
[TEST] `tests/unit/memory/hindsight/test_recipe_spec.py`
[TEST] `tests/unit/memory/hindsight/test_hindsight_recipes.py`

## Validation

`python -m pytest tests/unit/providers tests/unit/memory/hindsight`

## Done when

- Provider result stamping has one conversion boundary.
- Hindsight no longer rebuilds provider result fields that provider conversion can preserve.
- No behavior change outside tests expected.
