---
id: RS19
order: 19
plan: plan-recipe-system-review
state: pending
validate-first: true
priority: P2
complexity: simple
created-by: gpt-5.5-thinking
---

# Add recipe architecture and standards guard tests

## Description

Small execution slice for RS07. Add targeted tests that prevent recipe-system standards regressions.

## Proposed solution

Add tests that are narrow and allowlisted, not broad brittle grep bans. The goal is to catch foundation imports from components/runtime orchestration, recipe lifecycle duplication, provider-specific dispatch in generic code, and unsafe output patterns.

## Implementation steps

1. Add import-boundary tests for `src/audiagentic/foundation/toolchains/recipe_contract.py`, `recipe_patterns.py`, `artifact_registry.py`, and `provision_steps/*`.
2. Add allowlisted scan for classes overriding `provision()` or `teardown()` outside `recipe_contract.py`.
3. Add targeted scan for provider/capability/entity `if/elif` dispatch in foundation recipe code and generic provider recipe registry.
4. Add migration grep helper comments/tests to use when RS10/RS12/RS14 remove old names.
5. Wire RS15/RS16 redaction tests into the standards gate after they land.

## Files

[TEST] `tests/unit/test_architecture_boundaries.py`
[TEST] `tests/unit/foundation/toolchains/test_recipe_architecture.py`
[TEST] `tests/unit/foundation/toolchains/test_shell_provision_step.py`
[TEST] `tests/unit/providers/test_provider_recipes.py`
[TEST] `tests/unit/memory/hindsight/`

## Validation

`python -m pytest tests/unit/test_architecture_boundaries.py tests/unit/foundation/toolchains tests/unit/providers tests/unit/memory/hindsight`

## Done when

- Foundation recipe modules are protected from component/runtime orchestration imports.
- Lifecycle override duplication requires an explicit allowlist and rationale.
- Generic recipe code remains config/registry-driven.
