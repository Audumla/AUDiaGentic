---
id: RS15
order: 15
plan: plan-recipe-system-review
state: pending
validate-first: true
priority: P1
complexity: simple
created-by: gpt-5.5-thinking
---

# Characterize shell output and redaction gaps

## Description

Small execution slice for RS05. Add tests that expose current shell output behavior and prove where stdout, env values, token-like strings, and command args leak into structured results.

## Proposed solution

Write failing or characterization tests before adding redaction. The tests should make the unsafe propagation visible in `ShellProvisionStep`, `CompensatingSequence`, and `RecipeResult` details.

## Implementation steps

1. Add shell step tests for successful stdout, failed stdout, timeout, argv command, shell command, env values, and dry-run output.
2. Include secret-looking samples: bearer tokens, `api_key=...`, `Authorization`, `password=...`, and URL credentials.
3. Add sequence tests proving per-step outputs propagate into aggregate result outputs.
4. Mark expected unsafe behavior clearly so RS16 can implement redaction against these tests.

## Files

[FOUNDATION] `src/audiagentic/foundation/toolchains/provision_steps/shell.py`
[FOUNDATION] `src/audiagentic/foundation/toolchains/provision_steps/sequence.py`
[TEST] `tests/unit/foundation/toolchains/test_shell_provision_step.py`
[TEST] `tests/unit/foundation/toolchains/test_compensating_sequence.py`

## Validation

`python -m pytest tests/unit/foundation/toolchains/test_shell_provision_step.py tests/unit/foundation/toolchains/test_compensating_sequence.py`

## Done when

- Unsafe output behavior is documented by tests.
- RS16 has explicit failing/expected tests to fix.
