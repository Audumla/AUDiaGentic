---
id: RS09
order: 9
plan: plan-recipe-system-review
state: pending
validate-first: true
priority: P1
complexity: simple
created-by: gpt-5.5-thinking
---

# Characterize provider recipe result stamping

## Description

Small execution slice for RS02. Add tests that capture current provider recipe stamping behavior before simplifying `ProviderRecipeRegistry` and Hindsight `_RowRecipe`.

## Proposed solution

Write tests first. Cover plain `RecipeResult`, existing `ProviderRecipeResult`, registry `status/install/uninstall/dry_run/repair`, and Hindsight row fallback guidance. Refactor only after expected behavior is pinned.

## Implementation steps

1. Add tests for `ProviderCapabilityRecipe.to_result()` with plain `RecipeResult` and `ProviderRecipeResult` inputs.
2. Add registry tests for `status`, `install`, `uninstall`, `dry_run`, and `repair` using a simple `ProviderCapabilityRecipe` subclass.
3. Assert `status`, `dry_run`, and `repair` use `_stamped()` semantics without double-applying provenance.
4. Add one legacy recipe-like object test only if the codebase still registers non-`ProvisioningRecipe` objects; if covered, assert fallback handles plain `RecipeResult` with missing provider fields.
5. Add Hindsight `_RowRecipe` tests proving action-needed precedence: result action > row notes/action > empty.
6. Record any double-stamping, dispatch inconsistency, unsafe attribute access, or field-loss bug as the follow-up implementation work for RS10.

## Files

[COMPONENT:providers] `src/audiagentic/components/providers/services/recipes.py`
[COMPONENT:memory] `src/audiagentic/components/memory/hindsight/recipes.py`
[TEST] `tests/unit/providers/test_provider_recipes.py`
[TEST] `tests/unit/memory/hindsight/test_hindsight_recipes.py`

## Validation

`python -m pytest tests/unit/providers/test_provider_recipes.py tests/unit/memory/hindsight/test_hindsight_recipes.py`

## Done when

- Provider stamping semantics are locked by tests.
- Registry boundary stamping is locked for every operation.
- RS10 can safely remove duplicate conversion paths.
