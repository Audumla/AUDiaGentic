---
id: RS12
order: 12
plan: plan-recipe-system-review
state: pending
validate-first: true
priority: P1
complexity: simple
created-by: gpt-5.5-thinking
---

# Consolidate Hindsight recipe helper duplication

## Description

Small execution slice for RS03. After RS11 characterization tests land, extract only proven duplicated Hindsight recipe mechanics into local helpers.

## Dependencies

- RS09 if stamping behavior is touched.
- RS11 must be complete.

## Proposed solution

Extract Hindsight-local helpers for repeated source-gated step execution and probe/verify patterns. Do not create foundation abstractions. Do not merge plugin URL config, plugin array config, and MCP config classes.

## Implementation steps

1. Extract `_run_verified_steps(row, backend, steps, context, *, phase, subject)` or equivalent local helper if it removes duplicated install/uninstall logic in at least two classes.
2. Use the helper in `PluginConfigRecipe` and `_PluginUrlConfigRecipe`.
3. Fix project-root threading where `PluginConfigRecipe` managed MCP configure/prune currently falls back to `Path.cwd()`.
4. Extract `_verify_from_probe(...)` only if it removes repeated code without hiding distinct status messages.
5. Add comments explaining why plugin URL config and plugin array remain separate.

## Files

[COMPONENT:memory] `src/audiagentic/components/memory/hindsight/plugin_recipes.py`
[COMPONENT:memory] `src/audiagentic/components/memory/hindsight/recipes.py`
[TEST] `tests/unit/memory/hindsight/test_hindsight_recipes.py`
[TEST] `tests/unit/memory/hindsight/test_strategies.py`

## Validation

`python -m pytest tests/unit/memory/hindsight`

## Done when

- Duplicated source-gated step execution is consolidated.
- Managed MCP calls use the intended project root.
- No `ManagedEntryRecipe` or speculative foundation abstraction is introduced.
