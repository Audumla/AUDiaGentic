---
id: task-0181
label: Define documentation surface config shape
state: draft
summary: Review and finalize the documentation-surface config/schema proposal, including ownership and validation impacts
spec_ref: spec-0021
---

# Description

Review the proposed `documentation.yaml` and `documentation-config.schema.json` shape and make it implementation-ready.

# Acceptance Criteria

1. The task identifies how `Config`, `Paths`, and validation should load or ignore `documentation.yaml`.
2. The task records which fields are required, optional, and derived.
3. The task defines how each documentation surface maps to template ownership modes such as runtime ownership modes and install seeding.
4. The task calls out which surfaces are safe defaults for installed projects and which are AUDiaGentic-specific.

# Notes

- Suitable with revision.
- The current pack proposes a schema, but it does not yet wire the config into existing validation.
- `README.md` and `CHANGELOG.md` must be treated carefully because target projects may already own them.
- The activity must explicitly classify each surface against architecture doc 50 before implementation starts.

# Implementation Notes

- Update the pack docs to state whether `documentation.yaml` is required or optional.
- List the exact code paths that must change: `Config.validate`, any config loaders, and tests.
- Keep documentation surfaces human-editable and visible in installed projects.
