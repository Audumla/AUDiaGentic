# Audit Summary

Total events: 12

- chg_20260716_222628_delegated-a-real-ma28-closeout_6970: Ran a real OpenCode worker overseer trial for MA28 closeout with a narrow validation prompt. Worker returned terminal end_turn, verified MA28 completion, recorded ledger chg_20260716_222456_validated-ma28-migration-of-la_6320, and marked MA28 completed. Controller independently reran focused validation (11 passed), verified no remaining worker process tree, and appended the trial to the overseer capability log.
- chg_20260716_223841_acplive-session-support-is-no_9068: Declared the ACP capability family as provider capability_facts (sponsor directive): four ids — acp-stdio-transport, acp-live-session, acp-session-resume, acp-shared-live-session — on opencode (verified/partial/blocked/blocked) and codex (partial/partial/blocked/blocked), with evidence anchors, constraints from overseer R&D findings (event-cap pressure on long runs, unsafe in-flight bump), and action_needed pointing at AS07/AS10/AS13/AS14. Facts are evidence-only per CAPABILITY_OPERATION_SCHEMA boundary rules; executable authority stays with the adapter acp-submodule hook + UNS-AGW-001 dispatch gate. Also removed an unreachable duplicated block merged into transports/acp.py _extract_text, and updated the opencode facts-ordering test. Plan bookkeeping: RV518 recorded+closed (transport findings into AS06/AS09), RV517 closed (overseer graduates into agents component; AS11 notes updated with the passed MA28 live trial).
- chg_20260716_225419_live-agent-sessions-passed-the_2406: AS07 live gate executed and PASSED on brutus/opencode through the restarted gateway MCP server: two-turn context retention in one keep-alive session (token recalled exactly; 10s continuation vs 97s cold open), explicit close terminated the session's child process (verified by pid), and a 60s idle-timeout session was reaped by the sweeper 61s after last activity with its process gone. Gate found one defect, fixed here: _session_output_from_result joined streamed agent_message_chunk fragments with '\n', corrupting request output; now ''.join with a pinning test. Running server needs a restart to pick up the fix. AS07 marked completed.
- chg_20260716_232601_added-real-subprocess-tests-fo_7560: Real-subprocess test slice for AcpSessionTransport (AS06). fake_acp_agent.py implements a minimal ACP server using acp.run_agent with a factory callback — handles initialize, new_session (session_id embeds child PID), and prompt (emits agent_message_chunk 'turn-N' via session_update, returns end_turn). test_acp_session_subprocess.py drives the real transport with 4 tests: two-prompts context retention (turn-1 then turn-2 in one process), no-orphan proof (child PID extracted from session_id, os.kill confirms death after close), prompt-after-close raises CON-ACP-001, and is_alive lifecycle tracking. All marked with @pytest.mark.timeout(30). Windows-compatible: OSError/ProcessLookupError both caught for dead-PID check.
- chg_20260716_232919_real-process-testing-of-live-a_6149: AS06 real-subprocess gate (delegated to the coder-quality-lite worker) immediately caught two real transport defects the mock suites could not see: (1) a turn-finalization race — an agent writes session_update frames before its prompt response, but the SDK dispatches them as tasks that may still be pending when the prompt future resolves, so the per-turn pipeline missed them (empty assistant text, update miscounted as between-turns); fixed with a bounded post-response drain (10 loop yields + 10ms) before detaching the turn pipeline. (2) close() used wait_for around the SDK spawn-context aclose — a timeout CANCELLED aclose mid-unwind and corrupted the async generator ('aclose(): already running' at loop shutdown); now waits bounded WITHOUT cancellation and lets the unwind finish in the background while force-killing the child. Subprocess suite stable across 4 consecutive runs; transports + agents suites green.
- chg_20260716_233024_added-typed-desired-state-data_8627: MA26 step 2: Frozen Hindsight-owned typed desired state for Codex and Pi. Created CodexHindsightDesired (base_url, bank_id, api_key, interpreter_path, script_dir, hook_commands tuple of HookCommand per event), PiHindsightDesired (base_url, bank_id, bank_strategy, pi_host_block with recall_mode/auto_recall_tags/observation_scopes). Both frozen dataclasses with to_mapping/from_mapping, from_backend factory, __post_init__ validation. v1 JSON schemas under config/components/memory with additionalProperties false and Draft 2020-12. Boundary parse functions validate mapping against schema before returning frozen instance. 41 new tests cover round-trip, schema acceptance/rejection, __post_init__ rejection cases.
- chg_20260716_233308_consolidated-lsp-mcp-projectio_6144: MA29: Consolidated LSP MCP projection into managed MCP. Migrated coding-lsp caller from LspMcpProjectionEntry/Request to ManagedMcpEntry/ManagedMcpRequest with stable ownership_scope 'coding-lsp/ag-lsp'. Added ownership registry migration from legacy provider_id scope to scoped key (provider_id/coding-lsp/ag-lsp). Added manage_mcp_entries_all fan-out in providers_api. Added provider_id field to ManagedMcpResult for fan-out results. Deleted lsp-mcp-projection contracts, service module, providers_api routes, 13 provider YAML declarations, CON-PLMC error codes, and 4 old-family test files. Updated tests to use managed-mcp API.
- chg_20260717_000226_plugin-entry-management-now-us_4902: MA20: Eliminated PluginConfigSpec — plugin_entries.py now routes through sync_managed_config with ownership_scope. Reshaped opencode plugin adapter to name-keyed dict (reader returns {name: options}, writer upserts per-entry preserving foreign entries). Added ownership_scope field to PluginEntryRequest, updated schemas, added provider_id to PluginEntryResult. Updated hindsight caller to pass 'hindsight' as ownership_scope. Set ownership_scope_required: true on opencode plugin-entry capability.
- chg_20260717_011358_long-agent-work-sessions-no-lo_4397: Event-cap loss fix (RV520 on MA18), driven by live evidence from the MA29 delegation turn (3997/13998 events dropped, worker final report truncated): (1) assistant-message TEXT from budget-dropped events is banked in a bounded per-turn overflow buffer (1MiB) surfaced as AcpResult.overflow_text and appended to gateway request output — budgets and drop accounting unchanged, all frozen-contract tests pass unmodified; (2) new compact_events transport mode (gateway sessions default) stops buffering raw update payloads entirely — the gateway only consumes text/kind/counts, so payload retention was pure memory cost and the primary source of byte-budget pressure. Events remain in-memory only; nothing new is persisted and overseer token usage is unaffected (final record reads only).
- chg_20260717_012851_planned-the-migration-from-an_6657: Created a staged shared-gateway plan covering architecture, versioned execution context, client/control-plane seam, standalone service, race-safe discovery/self-start, context-isolated multi-project workers, durable ownership/recovery, resource arbitration, external event transport, lease/drain lifecycle, and final cutover. Created a separate role-capabilities plan for typed role contracts, resolution, gateway integration, and evidence-gated adoption. Narrowed AS08 through closed review RV521 so agent-sessions owns session integration while shared-gateway owns the machine-wide service.
- chg_20260717_013833_tightened-the-shared-gateway-r_7367: Added AR25 to promote thin MCP protocol adapters and framework-neutral API/core isolation into an explicit, test-enforced architecture invariant. Revised shared-gateway and role-integration plans through closed review RV522 so MCP, event, CLI, local-service, and service-manager surfaces only translate protocol concerns and call public APIs, while domain state, dispatch, discovery, recovery, lifecycle, and policy remain behind application layers. SH03 now explicitly migrates the legacy MCP timeout constant out of the gateway API.
- chg_20260717_014201_added-provider-telemetry-capab_6008: Created provider telemetry capability model and per-harness capability matrix reference docs under docs/reference/. PT01 doc covers the multi-dimensional telemetry data model (balance, quota, subscription, session, runtime, cost_estimation, observability) with per-provider implementations. PT02 doc covers connectivity, authentication, endpoint flexibility, and telemetry availability for each harness.
