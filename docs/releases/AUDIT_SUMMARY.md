# Audit Summary

Total events: 14

- chg_20260530_023919_logging-rollover: Added safe timed log rotation fallback for Windows locked-file rollover and covered it with regression tests.
- chg_20260530_025223_lsp-api-refactor: Extracted shared LSP orchestration into lsp_api, made MCP modules thin delegates, moved diagnostics behind SessionManager API, and added regression tests for root/language resolution.
- chg_20260530_040800_component-wrapper-refactor: Renamed generic optional-component root modules to component-prefixed names, extracted provider and source-control internal APIs, made MCP wrappers delegate to *_api modules, and added guardrail tests for naming and same-component MCP imports.
- chg_20260530_041900_refactor-cleanup-refs: Removed stale module-path and entrypoint references left by component renames, including legacy ledger bootstrap metadata and outdated knowledge README commands.
- chg_20260530_043800_tool-surface-canonicalization: Canonicalized MCP tool naming by removing project _tool suffixes, consolidating release status onto get_release_status, renaming provider_status to get_provider_status, and pruning public provider tools that directly edit provider MCP config or bypass managed reconciliation.
- chg_20260530_045400_provider-mcp-ownership: Validated that embedded ownership metadata was not safely provable across all provider config formats, then implemented a small managed MCP registry keyed by stable managed_id so AUDiaGentic can rename or remove only its own provider MCP entries while preserving external entries.
- chg_20260530_050600_ledger-read-efficiency: Made ledger summary reads reuse existing CURRENT_RELEASE.md instead of regenerating on every read, added explicit refresh_current_summary and optional record_change(sync=False), and made manifest writes atomic with tolerant status reads.
- chg_20260530_051200_ledger-batch-recording: Added record_changes(...) to batch multiple fragment writes and optionally sync once at the end, with tests proving deferred sync leaves the current ledger untouched until final merge.
- chg_20260530_052000_ledger-fragments-source-of-truth: Made current release ledger rebuild from fragments only, so CURRENT_RELEASE_LEDGER.ndjson is treated as generated output and stale/manual entries are discarded on sync.
- chg_20260530_060000_toolchain-consolidation: Deleted 12 individual toolchain delegate files (npm.py, uv.py, brew.py, etc.) and replaced with a single __init__.py that generates namespace objects dynamically from toolchains.yaml via _make_tc(). Moved toolchains/ out of foundation/invoke/ to foundation/toolchains/ and deleted the invoke/ directory (including follow_up_executor.py with no callers). Deleted log_context.py shim (zero callers). Winget acceptance flags moved from per-dep YAML into toolchains.yaml as toolchain-level install template behavior.
- chg_20260530_060100_dependency-yaml-driven: Removed hardcoded SYSTEM_DEPENDENCIES dict and DependencySpec/_PlatformStep/install_dependencies orchestration lane from dependencies.py. Dependency declarations moved to component YAML files (source-control.yaml, coding-lsp.yaml) using probe:/via:/toolchain:/package: vocabulary. lsp_dependencies.py deleted. New API: load_component_workflow(component_id, action) returns SequenceStep; load_component_probes(component_id) returns probe callables; detect_missing() takes probe dict. Component callers (source_control_api.py, lsp_api.py, coding_lsp_bootstrap.py) updated to use new loader API.
- chg_20260530_060200_select-step-and-cleanup: Added SelectStep to steps.py: generic N-variant runtime dispatch (select(ctx)->str|None; None=skipped, missing key+no fallback=failed). Replaced _PlatformStep with SelectStep(select=detect_pkg_manager). Added raw_step() factory to toolchains/loader.py so dependencies.py never imports ShellStep directly — layer boundary enforced. Extracted _guarded(dep_id, probe_fn, inner, skip_when_true) helper to remove duplicated probe-guard pattern. Moved SequenceStep import in source_control_api.py from deferred (inside function bodies) to module-level. Tests rewritten to cover SelectStep semantics and probe-guard pattern.
- chg_20260601_060000_provider-descriptor-cleanup: Added cli_recipe() factory to descriptors/base.py that calls build_step() via deferred import. Eliminated direct foundation.toolchains imports from all 12 standard-toolchain provider descriptor files (npm x8, uv x2, vscode x1, brew x1). goose (raw ShellStep script) and pi (CallableStep harness) unchanged. CliInstallRecipe structure and provisioning layer unchanged.
- chg_20260601_070000_provider-lifecycle-fixes: Fixed inv.status AttributeError in provider_cli_plan (dict not object). Added _ProviderCliConfig adapter to ProviderCliWorkflowContext so StateMachine ctx.config protocol is satisfied. Updated stale test references: _step_for_action->_build_step, _load_provider_cli_config replaced with state-table and supports_provider_cli_workflow checks, _find_pid_listening_on_port->find_pid_on_port, _kill_pid->kill_pid, pi tests patching _build_step instead of _pi_install, uv-tool->uv package-manager string, uv arg ordering updated.
