# Audit Summary

Total events: 44

- chg_20260720_003413_added-the-shared-session-surfa_7887: Added immutable provider-neutral session-surface capability value types as the AS29 foundation contract, with import-neutrality and scalar-only tests.
- chg_20260720_004911_improved-session-open-event-co_5699: Registered and validated the safe session-open child identity event projection fields introduced by AS17.
- chg_20260720_005921_fixed-several-real-bugs-in-the_5059: Fixed a chain of real bugs surfaced while independently verifying SH13/SH15/SH18 gateway worker output (worker self-reports had falsely claimed green/complete in several cases): (1) close_llm_session leaked provider-session-ref, now redacted via a shared _public_session_projection helper; (2) agents_gateway_api.submit_llm_request/run_llm_request rejected the _dispatch_service_root kwarg the HTTP service layer already sent, breaking every real service-path submission; (3) InMemoryGatewayRegistry.register() derived profile generation from a per-instance counter that reset on every registry rebuild, so reload never actually changed generation and silently defeated CON-AGW-101 stale-snapshot rejection — now a content digest; (4) submit_llm_request never resolved a snapshot from the live gateway-owned registry at all, so shared-mode admission was effectively unimplemented despite the validation machinery existing — now resolves and persists the full snapshot identity when a registry is installed; (5) deleted agents_gateway_api.reconcile_gateway_state, a dead second recovery authority with zero callers that violated the single-recovery-authority boundary; (6) wired SH15's progress-summary helper into request_runtime_status/wait_llm_request (result['progress']) and added gateway_overview's runtime-fingerprint; (7) fixed a regression where AS33's capability-snapshot projection had been silently dropped from request_runtime_status, restored via a new schema-tolerant raw session read; (8) added live session-handle latest-turn-event tracking. Also fixed several test-authoring bugs (fabricated client API calls, wrong event-handler arity, fake application double used where the real one was needed, a structurally-impossible test ordering) blocking SH13's integration suite. Entire tests/unit/agents/ + tests/integration/agents/ suite (267+ tests) is fully green.
- chg_20260720_010320_session-surface-provider-descr_6220: Corrected the AS29 session-surface descriptor YAML key to session_surfaces and replaced the broad tuple[Any, ...] annotation with a forward-referenced SessionSurfaceDeclaration tuple, preserving import layering.
- chg_20260720_010324_gateway-session-listing-now-co_9445: Restored stale-non-live runtime projection for persisted active or closing sessions without a live runtime, and made session listing observational by using the runtime peek path. Added coverage for stale closing and non-live runtime cases.
- chg_20260720_014640_provider-session-surfaces-now_7741: Added provider-neutral session-surface resolution with explicit project-root/provider inputs, installed-version and exact normalized platform selection, selected-evidence validation ceilings, neutral unsupported identities, adapter-ref containment, registered descriptor validation errors, and conservative OpenCode ACP/CLI declarations with platform-specific evidence.
- chg_20260720_015702_resolved-session-surface-snaps_7593: AS29 slice 5a: Added resolve_session_surface() and prepare_provider_session_transport() to the public providers API boundary. PreparedSessionTransport carries transport, resolved surface snapshot, and effective provider ref without exposing descriptor/adapter/protocol/native values. Unsupported surfaces produce no live transport (transport=None). Existing prepare_provider_acp_launch contract preserved for backward compatibility.
- chg_20260720_015712_resolved-session-surface-snaps_3002: AS29 slice 5a: Added resolve_session_surface() and prepare_provider_session_transport() to the public providers API boundary. PreparedSessionTransport carries transport, resolved surface snapshot, and effective provider ref without exposing descriptor/adapter/protocol/native values. Unsupported surfaces produce no live transport (transport=None). Existing prepare_provider_acp_launch contract preserved for backward compatibility.
- chg_20260720_020138_fixed-a-gap-where-the-gateway_7140: Fixed submit_llm_request never passing service_root into store.admit_record, silently skipping the admission-phase work-index entry (phase='admitted') that SH07 recovery relies on to discover admitted-but-unclaimed requests after a crash — found while building real Docker concurrency tests, since it made the crash-matrix suite's admitted-but-unclaimed scenario untestable. Also added a --gateway-profiles-config CLI flag to agents_gateway_service_process.py so the real subprocess entry point can run in shared/gateway-owned-registry mode (previously only the in-process GatewayServiceHost.create() supported it, meaning no real-subprocess test could ever exercise SH13's shared-registry guarantees).
- chg_20260720_031522_session-transport-migration-no_1809: Added AS28 characterization tests for current ACP session behavior and a bounded migration-debt inventory that will tighten to a zero-ACP agent boundary after neutral transport migration. Corrected a malformed comment that prevented module parsing.
- chg_20260720_032048_agent-sessions-now-have-a-prov_3056: Added the foundation-neutral agent session transport contract, bounded observations, canonical control request/result semantics, protocol conformance tests, and registered validation errors. SessionControlAction now has one canonical foundation home with compatibility re-export for session-surface consumers.
- chg_20260720_034624_acp-sessions-can-now-be-adapte_1201: Added the private ACP-to-neutral AgentSessionTransport wrapper, bounded raw-event mapping, canonical control semantics, and direct-module export boundary tests. Removed the ACP wrapper from general transport exports so agents cannot import it through the public foundation package.
- chg_20260720_040250_provider-session-transport-pre_3971: Added the provider-side AS28 transport factory bridge. It resolves the AS29 session surface once, returns the same frozen snapshot in PreparedSessionTransport, wraps supported OpenCode ACP launches privately, and returns no transport for unsupported routes without process launch or fallback.
- chg_20260720_055323_fixed-the-shared-agent-gateway_8329: Reclassified local-openai's execution_isolation_tier from no-isolation to full-isolation. The gateway's disposable-worker dispatch path (agents_gateway_worker_host.py) hard-rejects any non-full-isolation provider with UNS-AGW-076, which meant local-openai (a pure REST client with zero local config/CLI surface to isolate) could never successfully complete a real gateway dispatch. This was masked until test_gateway_concurrency.py needed a real successful completion rather than a mid-flight crash.
- chg_20260720_060515_agent-status-planning-now-dist_1314: Added AS37 with a capability-gated layered status projection design, linked it to AS21, and filled execution/standards detail in dependent AS plans. The design separates lifecycle from terminal outcome and defers unsupported activity/progress fields.
- chg_20260720_060549_gateway-session-opening-now-us_7706: Migrated the SessionRuntime open path to prepare and store a neutral PreparedSessionTransport through providers_api. Unsupported surfaces now fail before launch; legacy ACP prompt/cancel/close paths remain for later AS28 slices.
- chg_20260720_061912_session-surface-descriptor-val_6955: Corrected the session-surface enum parser's set-like input annotation so declared mutable and immutable value sets match the type contract without changing parser behavior.
- chg_20260720_065709_implemented-immutable-contenti_1667: SH08 Slice A rewrite: resource_key_for now resolves from provider isolation tier instead of profile params, distinct_projects is genuinely computed from project keys instead of hardcoded to 0, per_resource buckets keyed by resolved resource key instead of literal profile_id, ContentionSample schema matches RV732 immutable-snapshot spec with wait percentiles and config generation tracking. All R1-R6 fixes applied and validated (61 tests passing).
- chg_20260720_071439_gateway-durability-and-recover_1537: Completed SH07 closure validation: focused gateway durability tests cover profile lanes, replay-required recovery, transactional work-index handling, interrupted lifecycle mapping, and six Docker subprocess/SIGKILL crash scenarios including claimed-before-running and terminal-before-cleanup windows.
- chg_20260720_072409_session-transport-now-carries_8605: AS28 slice 4b-A foundation: SessionTurnResult now includes bounded final_summary field populated by AcpAgentSessionTransport.prompt() from assistant-message fragments. Dispatch reads final_summary instead of reconstructing output from AcpResult.events. Uses SessionTurnResult.observations_delivered + dropped_observations for total-events. Atomic migration of agents_gateway_sessions.py (group B) deferred to gateway agent work.
- chg_20260720_073255_worker-diagnostics-unexpected_4329: Added bounded traceback diagnostic capture to stderr on unexpected worker exceptions. The worker host now writes exception class/message and bounded traceback to operator-only stderr (never crosses the protocol pipe). The supervisor captures this stderr as a bounded redacted diagnostic in error details for INT-AGW-076. Protocol stdout remains clean with only the redacted error envelope.
- chg_20260720_074010_added-cli-lifecycle-commands-f_8275: SH10 Slice A: Added CLI wiring for gateway lifecycle operations. New commands: gateway status, drain, resume, stop [--force], recover [--confirm] [--reason]. Status/drain/resume/stop delegate exactly once through StandaloneGatewayClient (HTTP) and print redacted JSON. Recover invokes recover_unprovable_owner directly (never HTTP, because the service may be dead). CLI handlers contain no lifecycle/store/signal logic. Added 17 focused unit tests covering all commands, force flag forwarding, recover bypassing HTTP, busy graceful stop error surfacing, and architecture boundary enforcement.
- chg_20260720_074336_added-cli-commands-for-gateway_8530: SH10 Slice A: CLI wiring for gateway lifecycle operations. Added 5 operator sub-commands (status, drain, resume, stop [--force], recover [--confirm] [--reason]) to audiagentic gateway CLI, dispatching through StandaloneGatewayClient HTTP interface or direct record-only recovery module. 17 new unit tests verify dispatch-only semantics with no lifecycle-logic leakage into CLI handlers.
- chg_20260720_074839_simplified-the-agent-session-p_5013: AS28 slice 4b-A: removed AcpResult/AcpEvent reconstruction from the agents layer. agents_gateway_sessions.py now returns the neutral SessionTurnResult directly from AgentSessionTransport.prompt() instead of rebuilding a legacy AcpResult from collected AcpEvents. agents_gateway_turn_events.py's projector, timeline recorder, and on_event callback now consume TransportObservation/TransportObservationKind natively instead of AcpEvent. agents_gateway_session_dispatch.py already read SessionTurnResult.final_summary. Deleted the now-unused _transport_kind_to_acp_kind mapping table.
- chg_20260720_074927_fix-gateway-recover-to-honor_4572: Pass service_root=project_root in cmd_gateway_recover so gateway recover --project PATH operates on the resolved project root instead of CWD.
- chg_20260720_075506_unexpected-gateway-worker-fail_6514: SH21 partial: worker_host.py's except-Exception handler now writes a bounded, redacted traceback to operator-only stderr (never the protocol pipe stdout) before emitting the INT-AGW-076 error envelope. worker.py's supervisor now captures the child's stderr via spawn_supervised(stderr=subprocess.PIPE) and attaches a bounded diagnostic to the raised AudiaGenticError.details. Does not fix the underlying newline-truncation root cause documented in SH21 notes, which is reserved for concurrent Codex work.
- chg_20260720_082102_cleaned-boundary-between-agent_1881: AS28 slice 4b-A boundary repair: removed AcpResult/AcpEvent reconstruction from agents layer. SessionTurnResult now carries final_summary field populated by ACP adapter. _make_on_event_callback and _TurnEventProjector consume TransportObservation instead of AcpEvent. Atomic change group (B1+B2+B3+C) complete. 53/53 agents_gateway_sessions tests pass.
- chg_20260720_083136_sh21-fixed-windows-portabilit_2266: Fix test_real_subprocess_stdin_preserves_newline_boundary: replaced embedded Windows path in python -c f-string (caused unicodeescape on Windows) with a helper script file + env var. Added 7 new tests proving _load_runner("pi") resolves the custom adapter module and adapter.run delivers multiline prompt via input_text to run_streaming_command with no prompt body in the command array (mocking only subprocess/stream boundary).
- chg_20260720_084845_complete-as28-slice-4b-b-cance_6288: AS28 slice 4b-B: completed cancellation path migration from local asyncio.Event map to canonical SessionControlAction.CANCEL_TURN. Added missing imports (SessionControlRequest, SessionControlAction) to agents_gateway_sessions.py. Removed unsupported context_fingerprint parameter from open_session call in dispatch. Verified both neutral control path (via transport.control) and fallback local cancel event path work correctly. All 67 AS28-specific tests passing (behavioral characterization, sessions, dispatch).
- chg_20260720_084911_sh21-gateway-managed-pi-smoke_5325: SH21 validation slice: gateway-managed Pi smoke test with fake executable. Exercises full gateway worker/provider dispatch to prove multiline requests reach the Pi adapter via stdin (not CLI argument). Fake pi script records argv/stdin; verifies command is pi --print without prompt arg, full multiline input arrives via stdin with newlines intact, and public output remains redacted.
- chg_20260720_133425_added-foundation-contract-type_1369: AS19/AS30/AS31 Stage-1 foundation contracts implemented directly (gateway agents timed out). AS19: StatusEvidence, StatusObserverRequest/Result/Lease, StatusObserverState/SourceKind/SemanticStrength frozen dataclasses in harness_status_observer.py; normalizer in providers/contracts. AS30: BindingRelation, SessionOwnership, SessionMappingCapability/Capabilities, ProviderSessionRef (redacted repr), SessionBindingIntent in session_binding.py; providers contract uses BindingResolutionContext (renamed from conflicting ResolvedSessionSurface). AS31: AgentOutputKind (ASSISTANT_TEXT_DELTA, ASSISTANT_FINAL), AgentOutputEvent frozen dataclass, OutputSink protocol in agent_output.py. Self-review applied 6 fixes: renamed ResolvedSessionSurface→BindingResolutionContext, removed duplicate SessionMappingCapabilities, wired lease.binding_id as correlation_id in normalizer, exported AS30 types from foundation __init__, fixed observed_at/is_final error code numbers (007/008), added HarnessStatusObserverCapability to __all__. 18 new tests in harness_status_observer_contract.py, agent_output_contract.py tests also passing. 0 regressions.
- chg_20260720_212454_added-foundation-types-for-the_7055: AS37 Stage-1: foundation-neutral agent status projection types. 5 StrEnum (scope, lifecycle, outcome, wait_reason, confidence), 2 frozen dataclasses (decisions, snapshot) with __post_init__ validation enforcing terminal=outcome invariant and projected_at non-empty. No wiring, no provider imports. 33/33 new tests pass, 180/180 full transports suite passes (0 regressions).
- chg_20260720_224502_cleaned-up-type-checker-findin_2174: Resolved AS28/AS29 test-file type-safety diagnostics flagged by RV759/RV761/RV762: ObservationSink callback parameter renamed obs->observation across 18 test functions; possibly-unbound record/turn_id variables initialized before try blocks; None-typed public_binding_projection guarded before 'not in' check; log_message override parameter renamed _format->format to match BaseHTTPRequestHandler's base signature.
- chg_20260720_225037_fixed-type-safety-issues-in-th_8669: Fixed sort-key None handling in session_surface_resolution.py's version-compatibility disambiguation (unparseable version constraints now sort last instead of crashing the comparison). Implemented all 12 missing Client protocol abstract methods on _SessionClient in foundation/transports/acp.py (file/terminal I/O and elicitation methods raise NotImplementedError since this session-control seam never performs them); request_permission now returns the real RequestPermissionResponse type via a lazy import; AcpAgentSessionTransport.open() now raises CON-ACP-002 instead of propagating an Optional session id.
- chg_20260720_225641_as19-agents-side-evidence-sin_2433: AS19 Stage-2 Slice B: implemented agents_harness_status_evidence.py as sole agents-side evidence sink for StatusEvidence. Validates exact binding correlation, monotonic sequence dedupe, scalar allowlist enforcement, redacted timeline append (session.status.observed), and publishes exactly one agents.turn.status.observed event. Returns AcceptedEvidence/RejectedEvidence (best-effort). Wired after lease.observe_transport in _prompt via StatusEvidenceSink per turn. Added 22 focused unit tests covering accepted activity, duplicate/lower sequence rejection, binding mismatch, exception isolation, and no provider-native type imports.
- chg_20260720_225650_as19-agents-side-evidence-sin_5828: AS19 Stage-2 Slice B: implemented agents_harness_status_evidence.py as sole agents-side evidence sink for StatusEvidence. Validates exact binding correlation, monotonic sequence dedupe, scalar allowlist enforcement, redacted timeline append (session.status.observed), and publishes exactly one agents.turn.status.observed event. Returns AcceptedEvidence/RejectedEvidence (best-effort). Wired after lease.observe_transport in _prompt via StatusEvidenceSink per turn. Added 22 focused unit tests covering accepted activity, duplicate/lower sequence rejection, binding mismatch, exception isolation, and no provider-native type imports.
- chg_20260720_225709_session-lifecycle-evidence-pro_4513: AS21 consumer slice: added ephemeral SessionEvidenceProjection registry keyed (session_id, request_id). Maps AS19 StatusEvidence status strings to EvidenceKind (model-thinking/model-generating→activity, tool-calling/tool-pending→tool-active, waiting-permission→permission-wait, tool-completed→tool-completed). No terminal inference: observer data never produces terminal-success/terminal-failed. Wired into observation sink; surfaces redacted lifecycle-decision in session_runtime_status. 36 focused tests pass.
- chg_20260720_231352_as27-harness-observability-inv_9995: AS27 inventory/conformance slice: authoritative per-harness surface inventory with declared capability, validation state, and effective production level. OpenCode ACP (opencode-acp) is the only currently eligible transport-observation publisher; all others are probe-required/blocked/unsupported at O0. Conformance enforcement rejects unvalidated surfaces claiming transport-observation publishing. Recipe A eligibility gate derived from inventory.
- chg_20260720_231843_fixed-as21-accepted-evidence-only-contract-violation_9d17: Fixed AS21 accepted-evidence-only contract violation in _observation_sink: StatusEvidence was forwarded to self._evidence_projection.accept() regardless of StatusEvidenceSink.accept() result. Now only AcceptedEvidence is forwarded, using _sink_result.status_evidence (sanitized accepted evidence). Duplicate/lower/mismatched rejected evidence no longer changes lifecycle decision. Added integration test proving valid activity changes decision but rejected duplicate/lower/binding-mismatch evidence does not alter it, and verifying AS19 request_id binding does not spuriously reject per-turn evidence when lease was opened with request_id=None.
- chg_20260720_233251_as27-platform-metadata-correction_4254: Correct AS27 platform metadata: OpenCode native `opencode acp` supports Windows, macOS, and Linux; Windows config handles pwsh/cmd. Replaced linux-only platform_evidence with cross-platform (windows-amd64/darwin-arm64/darwin-amd64/linux-amd64) while preserving the real proof gate: status publication is eligible only if the configured AS29 surface is validated with exact launch/version/correlation evidence. Platform support alone is not status evidence. Added platform-neutral eligibility conformance tests ensuring any platform assertion derives from descriptor evidence, not inventory guesswork. Updated harness observability lifecycle reference.
- chg_20260720_234155_as27-pi-rpc-status-probe-evidence_7821: Read-only local Pi status capability probe for AS27. Installed @earendil-works/pi-coding-agent v0.80.10 confirmed: (1) `pi --mode rpc` exists and is fully implemented with dedicated dist/rpc-entry.js; (2) `--no-session` works with RPC mode creating in-memory ephemeral sessions; (3) 28 RPC command types documented with strict JSONL LF-only framing; (4) Core AgentEvent set (agent_start/end, turn_start/end, message_start/update/end, tool_execution_start/update/end) plus session-level events (agent_settled, queue_update, compaction_start/end, entry_appended, session_info_changed, thinking_level_changed, auto_retry_start/end); (5) Cancellation/close: abort command + SIGTERM/SIGHUP handlers with child process cleanup; (6) No separate pi-acp package — RPC protocol is part of main package only; (7) Extension UI dialog round-trips via extension_ui_request/response required for interactive extensions. Recommendation B: usable with gateway adaptation layer, but requires subprocess spawning, JSONL piping, extension UI relay, and external bash sandboxing.
- chg_20260720_234641_audited-pi-acp-implementation_1114: Pi ACP status-mapping report: documents exact files/symbols where Pi ACP emits TransportObservation, observation kinds and correlation available, AS19/AS21 mapping path without protocol leakage, and precise missing evidence. Key findings: Pi ACP is protocol-clean but blocked from AS19 pipeline by AS27 inventory (PROBE_REQUIRED, O0, LifecycleSource.NONE). Smallest fix: validate pi-rpc in inventory + declare HarnessStatusObserverCapability.
- chg_20260721_004333_opencode-acp-is-now-validated_9243: AS27 RV770: separated OpenCode ACP vendor product support from local observability validation. opencode.yaml now declares linux-amd64 as the only validated platform for opencode-acp; windows/darwin platforms are DECLARED/O0 (vendor-supported but unproven). Session surface resolver now enforces an inventory proof gate (step 7b) that rejects O1+ VALIDATED claims on platforms absent from the AS27 inventory platform_evidence. Rule 3b relaxed to allow mixed platform validation states.
- chg_20260721_004347_added-agent-session-status-pro_7347: Implemented agent session status projection, harness status observer transport resolution, and session capability families foundation. Added agents_status_projection.py for SH07 request status projection mapping, harness_status_observer_resolution.py for transport-observation-only resolver, agent_output.py and agent_status.py transports, session_capabilities.py foundation contracts, and consumer pipeline Docker tests. Updated provider capabilities profiles (pi-status-probe.md, pi-acp-status-mapping.md) and harness observability lifecycle docs.
