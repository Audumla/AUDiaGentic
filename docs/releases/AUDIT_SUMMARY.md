# Audit Summary

Total events: 27

- chg_20260804_105736_removed-unused-unreferenced-l_9455: Removed 42 stale checked-in llama.cpp llama-server Windows binaries (~46MB) from src/audiagentic/runtime/rig/embedded/bin/. Confirmed via resolution.py that runtime resolution never reads from this src-tree path (only global harness dir or project .audiagentic/provisioning/rig/embedded/bin), pyproject.toml only declares models.json as package data (not bin/), and LlamaCppRecipe/binaries.py install to the resolved runtime_bin_dir(), never to this location.
- chg_20260804_110220_removed-the-deprecated-agent_e_6632: Deleted the agent_execution_submit MCP tool (direct execution_profile_id submission, bypassing Agent Definition resolution). agent_task_submit is now the sole submission surface on ag-agents-gateway. Audited callers first via repo-wide grep: only the tool's own definition/config/tests/docs referenced it, no ACP/A2A/CLI/agent_jobs caller existed. Direct execution_profile_id submission stays available programmatically (AgentTaskFactory.submit_raw/submit_execution_request), just not over MCP. Verified set(agents.yaml direct-tools) == set(tool-descriptions keys) == set(@mcp.tool() registered names) for the 8 remaining tools.
- chg_20260804_114130_investigated-whether-agent-cap_6263: Performed AS64's prescribed inventory (grep for capability usage in providers/services/session, and the Role required_capabilities vocabulary from AS61) before building an admission-time capability-check layer. Found required_capabilities is defined on the Role model but read by zero code paths, the one real Role in this repo's config has it empty, and the one real Execution Profile has no model-facts fields to check. No demonstrated need exists. Closed the item as not-needed per its own reassess-or-close doctrine rather than building speculative machinery.
- chg_20260804_121351_documented-how-role-selection_1500: Added the agents/role-precedence contribution documenting Role vs Execution Profile vs workflow-profile vs component-profile resolution precedence and the fail-closed conflict rule, closing AS61's last open RO01-absorbed gap. Honest about current state: no standalone agent-role-id job field exists yet (that's AS78), so this fixes the rule ahead of the field rather than leaving it to be improvised later.
- chg_20260804_121857_added-a-public-api-to-query-wh_9754: Added get_provider_mcp_launch_isolation_tier(provider_id), a narrow public read API mirroring the existing get_provider_execution_isolation_tier exactly. Closes a gap found while inventorying evidence for AS77 (Role capability resolver): the mcp_launch_isolation_tier descriptor field had an internal consumer (prepare_provider_mcp_surface) but no narrow public read seam. Only pi and opencode currently declare exact MCP launch isolation.
- chg_20260804_124209_audited-as52-interactive-sess_7691: Characterized AS52 (child-process supervision) before implementation, per its own instruction. Found 7 of 8 remaining_scope items already fully implemented and tested (canonical adopt/observe/stop contract, identity-safe stop, session diagnostics projection, idempotent Job Object release, architecture-boundary enforcement). Shrunk the item down to its one real remaining bug: test_close_kills_child_process_no_orphan fails reproducibly on Windows today.
- chg_20260804_124441_completed-the-harness-observat_9052: Closed AS54 (per-harness observation recipe matrix) by adding the two missing Cline inventory rows (cline-sdk, cline-hooks) it was itself waiting on. The matrix infrastructure and conformance rules already existed (RV861); this was the one real remaining gap against the item's own surface_matrix table.
- chg_20260804_124557_corrected-as55s-scope-openco_1997: Re-scoped AS55 (OpenCode ACP vertical slice) against the tree per its own RV862 stale-premise finding. Confirmed opencode-acp is already validated/O1 with one real e2e round-trip proof, but permission stimuli, tool-call stimuli, cancellation semantics, AS30 binding rollback, and content-channel-disabled verification are all genuinely unproven anywhere in the tree (checked unit and e2e coverage directly). Corrected the stale files section (session_control.py does not exist; real module is acp.py). Did not implement the remaining proof work -- that's a real multi-test authoring effort, not a documentation fix.
- chg_20260804_230744_fixed-13-error-codes-that-were_6111: AS72 steps 2 and 4: fixed 13 error codes whose YAML registration key had a lowercase component segment, making them completely unreachable (make_error() always uppercases the component when building the real code). Built an AST scanner covering the make_error_factory construction path (never visible to literal-string regex scans) and registered 33 more previously-unregistered codes it found, all traced to real call sites with real remedy text. Added two permanent conformance tests. Step 3 (dynamic-number policy for 22 remaining call sites) remains open, deferred by the item's own design.
- chg_20260804_232704_fixed-provider-mcp-refresh-ag_7908: Config sync now reprojects AUDiaGentic-owned MCP entries to every provider before refreshing the active harness, repairing stale agent module paths. Git MCP runs under a pinned MCP 1.27.0 dependency compatible with mcp-server-git. OpenCode automatic model projection is disabled because its native model container is explicitly unverified, preventing routine refreshes from modifying user-managed model definitions.
- chg_20260804_233326_restored-automatic-opencode-mo_8223: Restored OpenCode managed model projection and aligned its materialization target with the provider descriptor at .opencode/opencode.json. Model ownership is tracked in .opencode/audiagentic-models.json, so apply/prune affect only AUDiaGentic-created entries and preserve unowned user providers/models. Added a regression test for mixed user and managed OpenCode model configuration.
- chg_20260804_233426_added-comprehensive-protection_3936: Expanded the OpenCode model-projection regression test to seed a realistic mixed opencode.json containing user theme, agent, MCP, plugin, LSP, and custom provider/model configuration, then assert all unowned content survives materialization alongside the AUDiaGentic-managed rig entry.
- chg_20260805_002257_fixed-the-opencode-hindsight-r_1201: Corrected the OpenCode Hindsight recipe to manage @vectorize-io/opencode-hindsight rather than the internal ag-hindsight ownership identifier, configure the guide-aligned typed plugin options, and retain ownership-managed preservation of foreign OpenCode config. Extended plugin option handling to preserve JSON scalar types.
- chg_20260805_003637_provider-installation-now-pres_1929: Deferred the Providers component post-install reconciliation until a reconciliation policy exists. This prevents the implicit auto policy from enabling detected provider CLIs before first-run provider choices are recorded. Added a regression test for the post-install path.
- chg_20260805_052817_gpt-auto-cdp-connect-to-exist_5671: Replaced Playwright chromium.launch() with puppeteer-core CDP connect. Node.js helper script (gpt_auto_cdp.cjs) runs puppeteer-core connecting to existing Brave (--remote-debugging-port=9222). Python cdp_client.py wraps it via asyncio subprocess JSON protocol. Prompt injector clicks .ProseMirror editor (not textarea). DOM reader polls <p> elements (not [data-message-author-role]). navigator.webdriver stays false — no bot detection.
- chg_20260805_081241_gpt-auto-full-conversation-fl_1624: Added click_js handler to gpt_auto_cdp.cjs for native puppeteer clicks. Rewrote inject_prompt to use JS evaluate (DOM injection + event dispatch) since puppeteer CDP keyboard/click events don't reach React/ProseMirror handlers when connected via CDP to an existing browser. Updated dom_reader to filter out user's prompt from response extraction. Fixed workspace navigation to re-find tab after window.location.href destroys context. Full end-to-end conversation test passes: inject prompt -> read ChatGPT response.
- chg_20260805_092138_gpt-auto-now-manages-puppeteer_3476: Moved gpt-auto's puppeteer-core dependency out of the repository root into a provider-owned managed npm runtime under the shared AUDiaGentic harness directory. Added callable install, uninstall, and probe lifecycle hooks modeled on the Pi provider recipe, and explicitly inject the managed node_modules path into the CDP helper environment. Removed accidental root npm manifest and installed node_modules artifacts.
- chg_20260805_092542_gpt-autos-npm-dependency-is-n_9704: Corrected the gpt-auto managed dependency location to use the provider runtime namespace rather than the harness runtime namespace. Added global_provider_runtime() and now resolves puppeteer-core under ~/.audiagentic/providers/gpt-auto/npm.
- chg_20260805_095626_fixed-chatgpt-project-navigati_3164: Fixed workspace navigation for gpt-auto provider. Key changes: 1) navigate to /projects page to find project by name in table rows (role=row), click via mouse emulation; 2) added new_tab and mouse_click CDP operations; 3) /project URL IS the project's new chat page — no redirect needed; 4) inject_prompt now uses keyboard Enter as fallback when send button not found; 5) workspace.py WorkspaceInfo gets conversation_id property for session tracking.
- chg_20260805_104137_prevented-repeated-planning-le_8909: Made the planning ledger-event subscription idempotent per event-bus instance and made ledger-to-plan linkage idempotent by checking the existing item text before appending an event ID.
- chg_20260805_111858_gpt-auto-chatgpt-automation-no_7782: Humanize gpt-auto browser automation to avoid ChatGPT bot detection and support persistent conversation reuse. New humanize.py provides jittered delays (typing cadence, think-pause, between-request gap). New prompt_pool.py provides a rotating pool of 29 realistic prompts with optional random online fetch (quotable.io / adviceslip fallback). inject_prompt now types short prompts word-by-word with jitter and pastes large prompts (>=300 chars) in one shot, with a randomized think delay before submit. Adapter reads humanize/typing-speed/think-delay/min-delay/use-prompt-pool from runtime provider config, applies a jittered pre-request pause, and picks pool prompts when no user prompt is given. workspace.find_workspace/ensure_workspace accept conversation_id and rebuild the workspace chat URL (base + /c/{id}) to continue the same conversation, falling back to the project /project new-chat page on failure. Adapter passes conversation-id from provider_cfg/packet_ctx and skips the fresh-chat reset when resuming. Test supports --conversation-id and uses pool prompts plus a human-like pause between turns.
- chg_20260805_115441_fixed-chatgpt-response-detecti_1080: wait_for_response now detects a new response by assistant-block identity (block count increased or last block text changed) instead of length-only baseline. The length heuristic missed any follow-up shorter than the previous response (e.g. response2 1600 chars under a 12639-char baseline timed out even though the DOM had the answer). Added _GET_RESPONSE_STATE_JS returning {count, text}; verified live that response2 of 6945 and 3559 chars under 8891/9558 baselines are now detected.
- chg_20260805_115441_gpt-auto-now-reuses-an-already_5941: Persistent tab-to-session reuse for gpt-auto. New tab_state.py persists .audiagentic/runtime/providers/gpt-auto-state.json mapping project_name -> {tab_id, workspace_url, conversation_id, updated_at}. CdpClient gains list_tabs()/activate_tab() (backed by new gpt_auto_cdp.cjs cases) and returns target()._targetId from new_tab/find_tab. find_workspace re-activates a mapped tab and navigates it (to /c/{conversation_id} or /project) instead of opening a new /projects tab; only opens a new tab when the mapped one can't be re-found. Adapter persists tab_id + conversation-id back into the mapping after each run. Removed pre-existing unused 'platform' import in cdp_client.py. Test now reports ChatGPT tab counts and asserts no tab growth on reuse.
- chg_20260805_115441_new-chatgpt-conversations-now_4376: New-conversation prompts no longer repeat: pick_prompt() now uses random.choice (dropped itertools cycle) and pick_plan_review_prompt() draws a random plan item from docs/planning/{active,completed} and builds a 'review this plan item' prompt with a 600-char description excerpt, falling back to the pool when no items exist. Fixed default planning_root path (audiagentic.__file__ three parents up to repo root; previously overshot by one). Fresh test conversations use pick_plan_review_prompt() for turn 1 and a random pool prompt for the follow-up.
- chg_20260805_125323_plan-item-review-prompts-now-e_6374: pick_plan_review_prompt previously embedded only the '## Description' section body, which most plan items don't have (e.g. AS65 uses ## Goal / ## Work / ## Validation) — so the prompt sent only the title and ChatGPT had to fetch the rest via its GitHub plugin, violating the pure-chat constraint. Rewrote _read_plan_item to strip YAML frontmatter and return the full body (all sections) after the H1 title; pick_plan_review_prompt now embeds title + body up to max_chars (default raised 600 -> 2000). Path imported at module level; _read_plan_item accepts str or Path.
- chg_20260805_141746_chatgpt-conversations-can-now_2040: Implemented BR04: added a CDP-backed AgentSessionTransport for gpt-auto (session_transport.py) implementing the 5-method AS28 protocol — open() resumes the mapped workspace tab / conversation-id from the gpt-auto runtime state and returns the canonical AG session ref; prompt() injects into ProseMirror, streams TURN_ACCEPTED/ACTIVITY/TERMINAL observations to the gateway sink, and cooperatively cancels via a per-turn asyncio.Event plus the gateway cancel_token (clicks the stop-generating control over CDP); control() maps CANCEL_TURN/CLOSE_SESSION to ACCEPTED and INTERRUPT/STEER/RESPOND_PERMISSION to UNSUPPORTED with no native escape hatch; close() is idempotent and never closes the user's browser tab (external ownership). Wired prepare_provider_session_transport (public_execution.py) with a gpt-auto-cdp branch that builds the transport via provider-local factory, kept the default surface hint in dispatch.py aligned (gpt-auto -> gpt-auto-cdp), and declared the AS29 session_surfaces block (gpt-auto-cdp, external ownership, lifecycle_source none) in gpt_auto.yaml. 13 unit tests with a fake CDP client pass; added a standalone multi-turn live smoke probe (random plan-item review prompt then a follow-up in the same conversation) that is excluded from normal pytest runs.
- chg_20260805_154221_fixed-truncated-chatgpt-respon_1904: Fixed gpt-auto session transport returning truncated responses. Root cause: ChatGPT pauses/aborts SSE streaming when the browser tab is backgrounded or occluded, so generations stopped after the first chunk (observed 7-52 chars while the DOM still showed streaming). Added bring_to_front() (page.bringToFront + focus) to the CDP helper and CdpClient, and call it in GptAutoSessionTransport.open() and before inject_prompt in prompt(). Also hardened _poll_response to use a 6s text-stability window instead of relying on the flickering is_generating() indicator, and dom_reader already excludes request-placeholder-request-* 'Thinking' blocks and covers .result-thinking in the loader selector.
