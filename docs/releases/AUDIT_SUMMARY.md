# Audit Summary

Total events: 25

- chg_20260803_074905_renamed-the-agent-llm-gateway_7103: SH12: Rename the LLM gateway contract to agent execution. Replaced technology-specific LLM vocabulary with provider-neutral agent execution vocabulary across gateway code, events, MCP tools, service operations, schemas, runtime paths, docs, and tests. Protocol version bumped from gateway-service-v1 to v2 for backward incompatibility detection.
- chg_20260803_094735_fixed-leftover-references-to-t_9504: Closed two gaps left by SH12's agent-execution vocabulary cutover: the agents and agent_jobs component READMEs still documented the retired agent_llm_* MCP tool names, agents.llm.* topics, and agent-llm-gateway paths (SH12's stale-vocabulary scan only walked .py/.yaml, missing .md). Updated both READMEs to the current agent_execution_* / agents.execution.* / agent-execution-gateway vocabulary. Hardened test_no_stale_llm_vocabulary.py: the legacy allowlist previously matched by filename alone (any stale hit anywhere in agents_paths.py silently passed); it now requires the hit's line to sit within the _LEGACY_* constant's own statement, and the scan now covers *.md alongside *.py/*.yaml so this class of miss fails loud next time.
- chg_20260803_095210_removed-unnecessary-legacy-pat_3042: Removed the SH12 legacy-path migration machinery from agents_paths.py per arch-standards §4 (no backward compatibility by default, no exception without a documented external obligation). AUDiaGentic has no deployed installs whose runtime state at the old agent-llm-gateway path needs preserving, so the one-shot os.replace/StartupLock rename for the project gateway root, and the separate _migrate_legacy_sessions_root move for the AS49-era accidental top-level sessions path, were unjustified speculative compatibility code. gateway_root() and gateway_sessions_root() are now plain path resolvers with no legacy branch, no module-level migration-state sets, and no fallback-to-old-path-on-failure behavior. Removed the now-unused os import. Dropped the corresponding legacy allowlist from test_no_stale_llm_vocabulary.py -- the stale-vocabulary scan is now a flat, exemption-free check across src, with zero remaining agent_llm/agents.llm/agent-llm hits anywhere in src.
- chg_20260803_102827_renamed-the-agent-profile-syst_3078: AS60 terminology-migration slice (steps 1/3/4/6/9/10-partial, not steps 2/7): renamed AgentProfile/AgentProfilesStore to ExecutionProfile/ExecutionProfileStore across models.py, agents_api.py (every CRUD/resolve function), both MCP servers (agent_list_profiles etc -> agent_list_execution_profiles etc, a deliberate no-alias contract break per arch-standards §4), error codes VAL/RES/IO-AGP-* -> VAL/RES/IO-EXP-* with re-registered prose, GatewayProfileSnapshot -> ResolvedExecutionProfile and InMemoryGatewayRegistry -> InMemoryExecutionProfileRegistry (this item names ResolvedExecutionProfile explicitly), config path agent-profiles.yaml -> execution-profiles.yaml, and the agents.yaml component doctrine block (hand-mirrored into CLAUDE.md/AGENTS.md since `config sync` regenerated a different harness's config rather than this repo's own doctrine files). Added test_no_stale_agent_profile_vocabulary.py as the step-10 architecture/search test. Fixed two real latent bugs found during the rename: agents_gateway_profiles.py and prompt_launch.py were still raising/comparing the just-deregistered RES-AGP-* codes, which would have thrown an unregistered-code error the first time actually hit. Composition-graph registration of the resolver (step 7) and the unified project/shared public resolver contract (step 2) are explicitly NOT done -- tracked as remaining work on AS60, which stays in_progress.
- chg_20260803_144855_implemented-the-execution-prof_2367: AS60 steps 2 and 7 (RV888's corrected design). Step 2: added ResolvedExecutionProfile.__post_init__ schema validation against a new resolved-execution-profile.schema.json (registered in canonical_ids.py), and one unified resolver contract agents_gateway_profiles.resolve_for_admission() that always returns a ResolvedExecutionProfile regardless of whether project-local or shared-gateway resolution answered; agents_gateway_api.submit_execution_request now calls it instead of inlining the registry-presence branch (persisted record semantics unchanged -- gateway-profile-id fields still None in embedded mode, decided by a separate get_gateway_registry() check kept for that purpose only). Step 7: added runtime/bootstrap/gateway_service_composition.py, a second composition root (RV888: composed singleton, not a binding with project/shared implementation IDs) serving the gateway-service process entrypoints -- its sole caller is agents_gateway_service_host.py, which now builds the graph in GatewayServiceHost.create() and calls graph.shutdown() in close() instead of hand-calling load_gateway_registry_from_config/set_gateway_registry directly; added exception-safety so a failure between graph construction and host construction still uninstalls the registry. foundation/composition/config.py's namespace became a parameter so this second root's own gateway-service-composition.yaml never collides with the launcher root's composition.yaml. Added architecture tests mirroring Stage 1's (sole-caller, no-python-paths, config/contributions agreement, error-code registration). Full tests/unit green except the two pre-existing unrelated failures (Windows ACP-subprocess-orphan flake, SH12's own stale plan-integrity baseline).
- chg_20260803_145432_added-the-dicomposition-candi_6412: Graduated AS59's Rule 0 (composition binding vs internal collaborator), the binding-vs-composed-singleton distinction, and RV888's corrected lifecycle-vs-mutability evaluation heuristic from AS59's plan item into a new 'Composition candidates' subsection of ARCHITECTURE_STANDARDS.md §1. This doctrine has now been tested against two real composition roots (Stage 1's launcher root, AS60's gateway-service root) and is durable, reusable project-wide guidance -- it belongs in the standards document, not stranded in a plan item that will eventually move to docs/planning/completed/.
- chg_20260803_151650_implemented-a-minimal-reusabl_6178: AS61: minimal reusable Role, scoped per RV889's revision (instructions stay a plain string, no reuse-dependency on agent_jobs/prompt_templates.py). Storage-shape check (revised step 7) found Role has no shared/gateway split -- purely project-scoped stateless config -- so it needs no composition binding, unlike AS60's execution-profile registry. Mirrors ExecutionProfile's existing shape: roles.py (Role dataclass, validate_role, RoleStore), roles_api.py (CRUD against .audiagentic/config/roles.yaml), error codes VAL/RES/IO-ROL-* registered, 5 new MCP tools added to the existing ag-agents-mgmt server (reused, not a new server). runtime_tool_policy_ref carried as an inert field only. Tests include the reassessment gate as executable tests (one Role usable against any Execution Profile since it carries no provider/model reference; two independently-defined Roles coexist with one Execution Profile) and an architecture test with a frozen, audited baseline for pre-existing unrelated 'role' hits in providers (OpenAI chat-message roles, a DOM attribute, a capability flag) so a genuine leak would still be caught. Not built, correctly deferred: Agent Definition (doesn't exist yet), capability vocabulary (AS77), active precedence/conflict checking against Execution Profile (nothing yet produces a mismatch to check). Full tests/unit green except two pre-existing unrelated failures (Windows ACP-subprocess-orphan flake, SH12's stale plan-integrity baseline) and one confirmed-flaky timing-sensitive queue-concurrency test unrelated to this item's changes.
- chg_20260804_001940_implemented-agentdefinition-t_8188: AS62: minimal AgentDefinition (steps 1,2,4,5,9,10) composing one ExecutionProfile reference and one Role reference plus advertised_skills and internal/acp/a2a publication flags. Storage-shape check (ARCHITECTURE_STANDARDS.md §1) found the same result as Role and project-local ExecutionProfile resolution: stateless project-scoped config, no composition binding needed -- corrected via RV890 against AS62's plan text, which had assumed a composition binding throughout (Foundation composition adoption section, §1 standards bullet, Reassess-before-AS63 gate, Validation bullets), mirroring AS60/AS61's earlier RV888/RV889 corrections. agent_definitions.py/agent_definitions_api.py mirror roles.py/roles_api.py's shape; resolve_agent_definition() takes execution_profile_lookup/role_lookup as optional keyword parameters defaulting to the real APIs, so fakes substitute via plain-Python injection at the call site, not a graph. 5 new MCP tools added to the existing ag-agents-mgmt server. Error codes VAL/RES/IO-AGD-* registered. Tests prove the reassessment gate directly (one profile backs two agents with different roles; re-pointing keeps the stable agent_id with no version/generation field and no second record; resolution calls only ID-lookup functions, nothing launch-shaped; publication flags round-trip unchanged) plus an architecture test checking actual imports (not string-matching the word "acp") for MCP/provider-adapter leakage. Step 3 (AS29 surface resolution) is genuinely blocked -- ExecutionProfile carries no surface_id field yet, AS60 step 5 remains deferred, so providers_api.resolve_session_surface has nothing to resolve against. Steps 6-8 (composition registration, now moot; updating agent-work callers; deleting the direct Execution Profile work-selection path) are deliberately deferred -- they touch the same agents_gateway_api free-function surface already assigned to AS63's AgentTask redesign; migrating callers before AgentTask exists risks the guess-then-redo cycle this session corrected three times already (RV888/889/890). Full tests/unit green except the two pre-existing unrelated failures (Windows ACP-subprocess-orphan flake, SH12's stale plan-integrity baseline).
- chg_20260804_012517_fixed-cross-project-planning-l_1798: Require MCP entry construction to receive the owning project root and propagate that resolved root into child-server environment configuration. Harness and provider projections now pass their explicit project roots, preventing inherited AUDIAGENTIC_REPO_ROOT values from pointing planning servers at another project. Removed the ambient environment fallback and added regression coverage.
- chg_20260804_012953_implemented-the-agent_id-prima_3123: AS63 Phase 1: AgentTaskFactory/AgentTask (RV891's concrete shape) over the existing gateway machinery, unchanged underneath. AgentTaskFactory.submit(agent_id=...) resolves via the plain resolve_agent_definition-equivalent lookup and dispatches through agents_gateway_client.get_gateway_client() -- the same mode-aware seam (in-process/standalone/automatic) every other gateway consumer already uses; submit_raw(execution_profile_id=...) is the explicit lower-level surface. AgentTask is a freely-constructed delegating handle (.status/.result/.wait/.cancel), never cached, re-reading the durable store every call. New MCP tool agent_task_submit added to ag-agents-gateway -- the only new tool needed, since status/wait/cancel are identical regardless of submission surface and reuse the existing agent_execution_status/wait/cancel tools. Module renamed agent_task.py -> agent_task_api.py after test_component_mcp_adapters_do_not_import_owning_internals correctly caught it violating the codebase's _api/_client public-module naming convention that every other API module built this session already follows -- fixed by renaming to match, not by weakening the test. Initial design routed through agents_gateway_api directly (in-process only); corrected before landing to route through get_gateway_client() so it works across all three gateway modes. Test-flakiness lesson: adopted test_agents_gateway_api.py's own established pattern (monkeypatch execute_isolated_provider_turn to hold dispatch) rather than asserting equality between live reads against a genuinely concurrent background dispatch thread. Explicitly NOT done (Phase 2, AS63's remaining steps 9-12): _QUEUE_MANAGER is untouched, still a module-global; AgentTaskFactory holds no injected dependencies yet and is not composed; the direct execution_profile_id-as-primary path is untouched and undeleted. Full tests/unit green except the two pre-existing unrelated failures (Windows ACP-subprocess-orphan flake, unrelated concurrent-agent plan-integrity noise in code-cleanup/onboarding-policy/other plans).
- chg_20260804_014028_fixed-fresh-opencode-projects_7534: OpenCode provider materialization no longer invokes automatic provider-surface apply. A fresh AGENTS.md now receives only the provider-owned template during install/materialize; managed component instruction blocks remain available through explicit surface reconciliation. Updated regression coverage to assert no managed region is injected during materialization and aligned adoption documentation.
- chg_20260804_015722_audiagentic-now-lets-you-contr_4617: Added a project-scoped provider reconciliation-policy (auto/allowlist/prompt modes, allowed/decided-provider lists) owned by the providers component: schema + storage (get/set/is_configured), enforcement in reconcile_provider's auto-enable branch (new 'skipped' status), providers_api + MCP exposure, and an interactive first-run resolver (resolve_reconciliation_policy) built on the existing foundation.interaction.ask() primitive, wired unconditionally into launch.py ahead of the stamp-gated auto-enable sweep so allowlist/prompt mode catches newly-installed CLIs without needing the reconcile stamp or env override. Also consolidated three duplicated _emit(on_progress-or-push_status) helpers in the providers component into a shared emit_or_push_status (foundation/contracts/output.py), fixing a latent bug where two of the three silently dropped messages instead of falling back to push_status. Foundation interaction package gained current_backend()/use_backend() accessors and a request()->request_interaction() rename to remove a local-variable shadowing risk.
- chg_20260804_015821_added-project-local-crud-for-i_8026: Added project-owned instruction and skill CRUD through the project component MCP server. Instructions are stored as project-local YAML contributions and skills as project-local frontmatter skill files; mutations reconcile provider surfaces. Provider contribution loading and skill loading now include project-owned resources. Moved the component-profile doctrine out of the universal session component into this repository's project-local instruction store, preventing it from leaking into newly initialized projects.
- chg_20260804_015828_corrected-the-implementation-d_6840: Supersedes the earlier intermediate materialization-only change: OpenCode surface application remains automatic. The final isolation boundary is project ownership—universal session doctrine was removed from session.yaml and moved into project-local instruction storage, while project CRUD surfaces are loaded per target project.
- chg_20260804_020642_fixed-fresh-installs-reporting_1804: Fresh project installation no longer installs every registered component. It now creates only project-scoped core infrastructure; optional capabilities and providers require explicit component installation. Baseline synchronization uses the same restricted component scope, preventing optional component markers from being created indirectly. Updated lifecycle tests and made provider-config preservation test explicitly install providers.
- chg_20260804_021701_removed-the-stray-test-hook-fr_4857: Removed the stray `echo should-not-land` SessionStart Codex hook from the local user hooks.json. The repository managed-config capability matrix remains gated by AUDIAGENTIC_DOCKER_TESTS=1 and therefore does not run in the local environment.
- chg_20260804_022526_completed-validation-for-proje_2689: Validation fixed Ruff-invalid one-line project MCP wrappers and exposed/fixed project-skill regeneration attempting to render every configured provider. Skill surfaces now render only for enabled skill-capable providers, allowing fresh projects to create local skills before optional components are installed. Added a real, non-mocked fresh-project instruction-and-skill regression test.
- chg_20260804_022942_added-real-docker-end-to-end-t_1321: Added Docker-only e2e coverage for the reconciliation-policy feature against real (non-mocked) provider CLIs: (1) a fresh-project/no-CLI smoke test in the hermetic audiagentic-test image, reproducing the original bug report's exact conditions; (2) a new Dockerfile.reconciliation-policy-e2e baking in real opencode+pi via npm at build time, proving auto mode auto-enables them and an allowlist correctly skips them; (3) a live install_provider_cli(codex)/install_provider_cli(claude) during the test itself, proving explicit install stays independent of reconciliation-policy and a subsequent reconcile reports them as already-in-sync. All 3 real-CLI tests + the smoke test pass.
- chg_20260804_024739_reorganized-the-agents-compone_4184: Reorganized the flat ~45-file src/audiagentic/components/agents/ directory into functional subpackages: models/ (ExecutionProfile, Role, AgentDefinition, AgentTask + their _api modules), mcp/ (manage_mcp, resolve_mcp, gateway_mcp), gateway/ (api/application/client/remote_client/events/ingress/output/profiles/mapping plus store/, queue/, session/, service/ subpackages), and status/ (harness status projection modules). Fixed fallout: relative Path(__file__) depth changes in 4 files, orphaned imports dropped during the rewrite sweep in 3 source and ~9 test files, hardcoded old flat filenames in 4 architecture/inventory tests, a text-substring false positive in test_architecture.py's composition-bindings check (rewritten to use AST attribute-access detection since the new gateway/session/bindings.py module's dotted path legitimately contains ".bindings"), and a pre-existing (pre-reorg) stale create_profile->create_execution_profile reference in tests/helpers/gateway_provider_conformance.py left over from the earlier AS60 ExecutionProfile rename.
- chg_20260804_054906_composed-the-agent-gateways-q_5922: AS63 Phase 2, steps 9/10 and part of 7: replaced gateway/api.py's bare _QUEUE_MANAGER module global with get_queue_manager()/set_queue_manager() accessors (mirroring the existing gateway/profiles.py registry pattern); the gateway-service composition root now composes a fresh GatewayQueueManager for that process's lifetime (installed on build, reset on shutdown), added as a new ServiceId/binding in gateway-service-composition.yaml; migrated all 4 test reach-in sites off the bare global; updated AS59's legacy exemption register to mark this resolved and refresh stale pre-reorg file paths. Separately, audited and removed the agent_execution_run MCP tool (blocking one-shot submit) as pure MCP-surface redundancy -- submit_execution_request(mode="blocking") and AgentTaskFactory.submit(mode="blocking") already provide the same capability one layer down for programmatic callers, with no MCP transport cap to work around; removed its config descriptor entries, test, and updated README framing to state the async-only-over-MCP / blocking-at-the-API-layer split explicitly.
- chg_20260804_060143_split-the-agent-execution-gate_9866: AS63 Phase 2 step 7: split the single ag-agents-gateway MCP server into two servers per direct user design decision. ag-agents-gateway keeps agent_task_submit (primary, agent_id-based) plus every submission-agnostic tool (status/wait/cancel/list_requests/gateway_overview/session_list/session_close), making it fully self-sufficient. New ag-agents-execution (mcp/execution_mcp.py) holds only agent_execution_submit (direct execution_profile_id, bypassing Agent Definition resolution) -- a thin, deprecation-candidate surface expected to be dropped once callers finish migrating to agent_task_submit. Also fixed a pre-existing config/code mismatch found while rewriting the tool list: agent_execution_session_list/session_close were registered @mcp.tool() functions never declared in agents.yaml's direct-tools.
- chg_20260804_061341_kept-the-agent-execution-gatew_8867: AS63 Phase 2 step 7 continued: reverted the earlier two-MCP-server split (ag-agents-gateway / ag-agents-execution) back to a single ag-agents-gateway server, per user feedback that the split didn't address the real naming inconsistency -- callers using agent_task_submit still had to call agent_execution_wait/agent_execution_status/etc. Fixed by renaming the seven submission-agnostic tools (status/wait/cancel/list_requests/gateway_overview/session_list/session_close) from agent_execution_* to agent_task_*, since they are grouped with the primary agent_task_submit surface and work identically regardless of which tool submitted the request. agent_execution_submit (raw execution_profile_id submission) keeps its original name as the sole remaining deprecation-candidate tool on the same server. Deleted the execution_mcp.py module and its test created for the reverted split; reverted the MCP propagation inventory changes in test_recipe_validation.py.
- chg_20260804_061540_fixed-the-reported-test-failur_9097: Aligned validation tests with the current capabilities.cli-install provider descriptor schema, made rig lifecycle assertions platform-aware and source-module based, updated agent MCP tests to the unified task/public-client surface, and fixed Windows staged rig directory promotion by removing any residual target directory after rollback preservation.
- chg_20260804_062304_added-a-test-proving-the-new-a_6803: AS63 Phase 2 steps 8/11/12 closed: added test_agent_task_api_imports_only_public_client_not_core_api proving AgentTaskFactory/AgentTask reach the gateway only through gateway.client, never gateway.api/store/queue/dispatch/application directly -- the architecture test AS63's acceptance criteria required for 'one agent-work path'. Steps 8 (reuse of cancellation/output/idempotency/redaction/recovery) and 11 (AS59 legacy exemption register) confirmed already satisfied/complete, no code changes needed for those.
- chg_20260804_071458_created-and-tested-the-project_9855: Created this repo's first real Agent Definition composition end-to-end via the actual CRUD API functions (create_execution_profile/create_role/create_agent_definition): execution profile rig-tester (local-openai/audiagentic-rig, the rig's local llama-server test model), role placeholder (minimal), and agent definition rig-tester-agent composing them. Added regression test tests/unit/agents/test_project_rig_tester_agent.py asserting the real on-disk project config resolves correctly end-to-end via resolve_agent_definition, distinct from the existing tmp_path-based AgentTaskFactory tests. Also created plan item AS80 to track the broader gap (nothing in the AS62/64/65/66/69 sequence owns seeding real Agent Definition content, only the mechanism).
