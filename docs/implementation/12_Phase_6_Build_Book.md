# Phase 6 — Migration hardening and cutover completion

This phase hardens migration, provider config conversion, and operator recovery. It does not redesign the system. It closes the rollout loop so existing projects can be moved cleanly from legacy to AUDiaGentic with deterministic reports and recovery guidance.

## Phase deliverables

See the packet files for exact build steps.

## Parallelization

Use the module ownership map to determine which packets may run at the same time after dependencies are merged.

## Exit gate

See `02_Phase_Gates_and_Exit_Criteria.md`.
