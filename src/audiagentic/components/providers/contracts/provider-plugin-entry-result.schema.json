{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://audiagentic.dev/schemas/providers/provider-plugin-entry-result.schema.json",
  "title": "Provider Plugin Entry Result",
  "type": "object",
  "additionalProperties": false,
  "required": ["ok", "supported", "changed", "present", "action_needed", "error_code"],
  "properties": {
    "ok": {"type": "boolean"},
    "supported": {"type": "boolean"},
    "changed": {"type": "boolean"},
    "present": {"type": "boolean"},
    "action_needed": {"type": ["string", "null"]},
    "error_code": {"type": ["string", "null"]}
  }
}
