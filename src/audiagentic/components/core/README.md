# components/core/

Always-on components required for any AUDiaGentic project.

## Functional Areas

- `project/` manages installed component inventory, project-scoped files, and component lifecycle entrypoints.
- `session/` manages live harness/runtime visibility, environment status, and embedded rig refresh behavior.

Core components define baseline project management. Optional components extend this surface but do not replace it.
