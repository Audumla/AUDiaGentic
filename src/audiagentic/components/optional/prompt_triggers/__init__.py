# Prompt trigger bridges — a layer over providers for handling workflow tags.
# Bridges normalize tagged prompts (e.g. @ag-plan, @ag-implement) and route
# them through the repo-owned bridge infrastructure.
