# runtime/rig/embedded/

Embedded rig launcher and process control.

Owns bundled-backend specifics: binary selection, launch config, background process startup, and embedded server process handling. Generic rig registry and HTTP probing stay in parent `runtime/rig/`.
