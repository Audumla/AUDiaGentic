# runtime/harness/opencode/install/

OpenCode install-time constants and helpers.

This area exists for OpenCode-specific asset/bootstrap details needed during harness setup. Shared harness installation logic stays above this folder.
