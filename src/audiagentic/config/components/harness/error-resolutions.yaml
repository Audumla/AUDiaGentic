CFG-HCFG-008: "No model configured for AudiaGentic agent. Set 'model' in ag.yaml or via AUDIAGENTIC_PI_MODEL environment variable."
CFG-HCFG-009: "No model configured for AudiaGentic agent. Set AUDIAGENTIC_AG_MODEL environment variable or set 'model' in harness config."
CFG-HCFG-010: "No model configured for opencode. Set 'model' in ag.yaml."
CFG-OCINST-001: "npm is required to install opencode. Install Node.js and npm, then retry."
CFG-OCINST-003: "opencode CLI not found on PATH. Ensure opencode is installed and available."
RES-HRNRUN-001: "No system-installed harness CLI found on PATH for the configured harness type. Install it, then run: audiagentic bootstrap"
EXT-OCINST-004: "opencode CLI returned an error during smoke test. Check the CLI installation."
RES-PIINST-006: "AudiaGentic agent package not found after npm install. Run: npm install --prefix <npm_dir>."
RES-PIINST-007: "AudiaGentic agent install incomplete — dist/ is empty. Run: npm install --prefix <npm_dir> to retry."
CFG-PIINST-005: "npm is required to install the AudiaGentic agent. Install Node.js and npm."
EXT-PIRUN-001: "Direct MCP smoke test failed. Check the MCP server configuration and logs."
