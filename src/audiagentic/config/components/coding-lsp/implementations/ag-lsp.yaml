type: implementation
contract-version: v1
parent: coding-lsp
id: ag-lsp
display-name: AG LSP
description: AUDiaGentic native MCP language-server bridge
# Active implementation when the user has not explicitly selected one.
default: true
options-schema:
  mutation-enabled:
    type: boolean
    default: false
    description: "Allow LSP mutation tools (rename, codeAction apply, formatting apply). When false, mutation tools return errors."
projection:
  generic-mcp:
    managed-id: coding-lsp/ag-lsp
    name: ag-lsp
    module: audiagentic.components.coding_lsp.lsp_mcp
