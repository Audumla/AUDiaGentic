type: feature
contract-version: v1
parent: coding-lsp
kind: language
id: make
display-name: Makefile (make-ls)
language-id: make
server:
  command: [make-ls]
  file-extensions: [Makefile, makefile, GNUmakefile]
  workspace-config-files: []
detection-markers: [Makefile, makefile, GNUmakefile]
dependencies:
  make-ls:
    display-name: make-ls
    probe: binary:make-ls
    toolchain: go
    package: github.com/owenrumney/make-ls/cmd/make-ls@latest
options-schema:
  server-settings:
    type: object
    default: {}
