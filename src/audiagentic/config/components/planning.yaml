type: component
contract-version: v1
id: agent-planning
display-name: Agent Planning
description: >
  Plan item management — track multi-step implementation work across sessions.
  Implementations may back this with local markdown files, a hosted issue tracker,
  or any other planning store; only one is active at a time.
implementation-cardinality: exclusive
status-hook: audiagentic.components.planning.planning_api.planning_status
lifecycle-observer: audiagentic.components.planning.events

item-section-headings:
  description: "Description"
  steps: "Steps"
  detailed_solution: "Detailed Solution & Technical Design"
  code_samples: "Code Samples & Guidance"
  files: "Files"
  validation: "Validation"
  effort_risk: "Effort & Risk"
  standards: "Standards"
  acceptance_criteria: "Acceptance Criteria"
  notes: "Notes"

review-sections:
  notes: "Notes"
  findings: "Findings"
  conclusion: "Conclusion"

# Fields that belong in YAML frontmatter (not body sections).
# Add new metadata fields here so they go into the frontmatter block instead
# of becoming spurious "## FieldName" section headings.
frontmatter-fields:
  - id
  - order
  - plan
  - state
  - work
  - skill
  - priority
  - created-by
  - created-at

# Metadata keys consumed during creation/update — never rendered as body sections.
# These include identity aliases and structural metadata that agents may pass
# but which are not section content (they go to frontmatter or internal logic).
metadata-keys:
  - created-by
  - created_by
  - creator_id
  - title

mcp-servers:
  - name: ag-planning-mgmt
    module: audiagentic.components.planning.planning_manage_mcp
    direct-tools:
      - planning_status
      - planning_list_implementations
      - planning_select_implementation
      - planning_get_config
      - planning_set_config
    description: Planning component management — status and implementation selection
    propagate: audiagentic
    instructions: >
      Inspect or switch the active planning implementation and check item counts.
    tool-descriptions:
      planning_status: >
        Return planning component status including active implementation, item counts,
        fragment count, and last sync time.
      planning_list_implementations: >
        List all registered planning implementations with their display name, description,
        default flag, and current active selection.
      planning_get_config: Return resolved config and its option schema.
      planning_set_config: Validate and persist descriptor-defined config updates.
      planning_select_implementation: >
        Switch the active planning implementation for this project. Returns success status
        and any rollback information if switching fails.

  - name: ag-planning
    module: audiagentic.components.planning.planning_mcp
    direct-tools:
      - plan_create_item
      - plan_list_standards
      - plan_list_groups
      - plan_list_items
      - plan_get_item
      - plan_set_state
      - plan_update_item
      - plan_delete_item
      - plan_create_review
      - plan_list_reviews
      - plan_get_review
      - plan_set_review_state
      - plan_update_review
      - plan_delete_review
    description: Plan item management — create, list, update, transition, and remove items; create and manage reviews
    propagate: providers
    instructions: >
      Use plan_list_standards when standards context is needed. Create items with
      plan and title; work is optional (S/M/L), and compatible supplied IDs are allowed.
      Filter lists by plan or ID prefix; item/review defaults are active/open. Transition
      state explicitly; close handled reviews and complete items only after validation.
    tool-descriptions:
      plan_list_standards: List configured planning standards.
      plan_create_item: "Create an item. Required: plan, title. Optional: id, work (S/M/L), skill, order, and sections."
      plan_list_groups: "List plan groups; filter by state/plan and paginate with limit/offset."
      plan_list_items: "List items; provide plan or id_prefix. Default state is active; paginate with limit/offset."
      plan_get_item: Read an item; set include_history=true for change history.
      plan_set_state: Transition to pending, in_progress, completed, superseded, or deprecated.
      plan_update_item: Update editable fields; append is section names also present in updates.
      plan_delete_item: Permanently delete a plan item by ID.
      plan_create_review: "Create a review; required: review-of and title."
      plan_list_reviews: List reviews; default state is open; paginate with limit/offset.
      plan_get_review: Read a review by ID, returning frontmatter + parsed body sections.
      plan_set_review_state: Transition to created, considered, or closed.
      plan_update_review: Update review content; identity and state are immutable.
      plan_delete_review: Delete a review.

contributions:
  - id: agent-planning/process
    owner: agent-planning
    title: Planning process
    preferred-targets:
      - skill
      - instruction
    content:
      body: |
        Use the ag-planning MCP tools to manage plan items in docs/planning/.

        ## When to use
        - User asks to create a plan or work items
        - Tracking multi-step implementation across sessions
        - Reviewing or updating the state of outstanding items

        ## Item lifecycle
        1. Create items with plan_create_item — lands in docs/planning/active/<plan>/
        2. Revise content with plan_update_item as work progresses; for a findings-driven correction (not routine progress), record a plan_create_review first, then close it once incorporated
        3. After review triage, close handled reviews with plan_set_review_state(review_id, 'closed')
        4. Mark done with plan_set_state(item_id, 'completed') only when implementation and validation are done
        5. Keep unfinished work pending or in terminal discard states (superseded, deprecated); remove stale items with plan_delete_item

        ## Item ID convention
        Combine a short uppercase plan prefix with a sequence number: CC07, LSP01, ML01.
        Choose a prefix matching the plan name (CC → code-cleanup, LSP → lsp-mcp-enhancement).

        ## Required fields for plan_create_item
        - plan: plan directory name (e.g. "code-cleanup")
        - title: short descriptive title

        ## State discipline
        - Do not leave incorporated reviews in created/considered; close them.
        - Do not mark a parent item completed just because reviews were handled.
        - If an active item is superseded, transition to 'superseded' or 'deprecated' rather than deleting (preserves history and linked reviews).

        ## Optional fields (fill out where applicable to provide complete planning context)
        - work: S / M / L — blast radius of the change
        - skill: basic / intermediate / advanced — cognitive difficulty
        - order: integer sort key (default 0)
        - description: Body section describing the work
        - steps: Implementation steps
        - detailed_solution: Detailed Solution & Technical Design (architecture, components, design)
        - code_samples: Code Samples & Guidance (including config samples and schemas)
        - files: Files to create/update
        - validation: How to validate the implementation
          (include comprehensive tests where possible)
        - effort_risk: Complexity and risk assessment
        - standards: Applicable standards/rules
        - notes: Key design principles and additional context
