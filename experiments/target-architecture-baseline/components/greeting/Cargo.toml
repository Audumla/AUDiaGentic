[package]
name = "target-baseline-greeting-component"
version = "0.1.0"
edition = "2024"
rust-version = "1.95"
license = "Apache-2.0"

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = "=0.60.0"

[profile.release]
lto = true
opt-level = "s"
strip = true
