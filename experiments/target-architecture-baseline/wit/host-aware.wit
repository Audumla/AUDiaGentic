package audiagentic:baseline@0.1.0;

/// A deliberately tiny host authority used only to prove that host access is
/// granted by WIT linking rather than through a global host/service object.
interface observer {
    observe: func(message: string);
}

world host-aware {
    import observer;
    export greet: func(name: string) -> string;
}
